import { UserDataType } from '@type/platform/user'

function UserDashboard({ userDetails }: { userDetails: UserDataType }) {
    return (
        <div>UserDashboard</div>
    )
}

export default UserDashboard