import EditorWrapper from '@organisms/editor/editorWrapper'

function Timings({ userDetails, onChangeValue }) {
    return (
        <EditorWrapper>
            User timing and shifts settings
        </EditorWrapper>
    )
}

export default Timings