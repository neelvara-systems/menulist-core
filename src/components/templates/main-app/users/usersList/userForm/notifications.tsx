import EditorWrapper from '@organisms/editor/editorWrapper'

function Notifications({ userDetails, onChangeValue }) {
    return (
        <EditorWrapper>
            User notofications settings
        </EditorWrapper>
    )
}

export default Notifications