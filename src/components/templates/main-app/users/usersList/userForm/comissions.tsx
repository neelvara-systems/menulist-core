import EditorWrapper from '@organisms/editor/editorWrapper'

function Comissions({ userDetails, onChangeValue }) {
    return (
        <EditorWrapper>
            Commision on services items/products
        </EditorWrapper>
    )
}

export default Comissions