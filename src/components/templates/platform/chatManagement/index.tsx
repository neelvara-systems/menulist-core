'use client';

import ConversationsList from './ConversationsList';

/**
 * Chat Management Template - Conversations Page
 * 
 * Now shows only conversations list.
 * ROI Calculator and Weekly Digest moved to separate sidebar navigation items.
 */
function ChatManagementTemplate() {
    return <ConversationsList />;
}

export default ChatManagementTemplate;
