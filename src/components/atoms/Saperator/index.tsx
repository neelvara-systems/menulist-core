import { Divider } from 'antd'

function Saperator({ margin = '15px 0' }) {
    return <Divider className='animate__animated animate__fadeInRight animate__delay-1s' style={{ margin }} />
}

export default Saperator