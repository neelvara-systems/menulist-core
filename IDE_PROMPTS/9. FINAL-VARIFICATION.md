Now do comprehensive review of everything we've done in this session:

## Review & Cross-check

1. **Deep codebase review** (line by line)
2. **Cross-check Cascade chat messages** with codebase to ensure full coverage
3. **Cross-check codebase with docs** (Codebase as primary source)
4. **Cross-check docs with codebase** (Docs as primary source)
5. **Cross-check against** `IDE_PROMPTS/00. MASTER RULES & WORKFLOW.md`
6. **Check consistency** with the overall project architecture
7. **Review all related files** including frontend, backend, and cloud functions

## Doc Staleness Sweep (CRITICAL — Added Feb 13, 2026)

> **Lesson Learned:** Stale `_firebase.md`, `_helpdoc.md`, `_website.md`, and `_marketing.md` files were missed during cross-checks because only `_impl.md` and `_spec.md` were verified. This phase prevents that.

For EVERY feature folder touched in this session:

7a. **List ALL doc files** in `__docs__/[feature-name]/` (excluding `_archive/`)
7b. **Check "Last Updated" date** on EVERY file — if older than current session date, flag it
7c. **Read and verify flagged files** against codebase truth: - `_firebase.md` — All reads/writes/deletes accurate? New DAL functions or API routes? - `_helpdoc.md` — Customer-facing info matches current behavior? - `_website.md` — Landing page copy matches current capabilities? - `_marketing.md` — Pitch messaging matches current feature set? - `_spec.md` — Scope and feature list matches implementation? - `_impl.md` — File paths, types, code references still correct?
7d. **Update stale files** — fix content, update date, add scope cross-references
7e. **Cross-check related feature folders** — if you touched permissions, also sweep stores-management, multi-outlet-consistency, roles-permissions (and vice versa)

## Redundancy & Consolidation

8. **Check for redundant constants/types** (Is the same data in multiple files?)
9. **Identify the PRIMARY data structure** as the Single Source of Truth
10. **Consolidate logic**: If adding a new item requires updating 2+ places, consolidate
11. **Refactor lookup tables** into fields on existing types
12. **Extend types** to include ALL related fields (display info, icons, descriptions)
13. **Consolidate small files** (1-3 exports) into related files
14. **Store icons as STRING names** in data; create `ICON_MAP` in component layer
15. **Update consumers** to use extended types directly instead of lookups
16. **Delete redundant files** and update imports in all dependent files

## UI/UX Review

17. **Provide honest feedback on UI components** (What works vs. what needs improvement)
18. **Cross-check for user-friendliness** and general ease of use
19. **Add visual previews** (emoji icons, thumbnails) and improve button prominence
20. **Add "Quick Action" buttons** for zero-config operations
21. **Improve toggle clarity** and collapse advanced options by default

## Research & Performance

22. **Search the web** for context-specific project improvements
23. **Check for performance improvements** considering the project doctrine
24. **Use free tier AI models** for development testing when available

## Fix & Verify

25. **Fix any bugs or issues** discovered during the review
26. **Update documentation** for items present in codebase but missing in docs
27. **Run type check**: `npx tsc --noEmit` to verify stability

## Documentation & Wrap-up

28. **Log everything to `verification.md`** (if not present then create new one or update existing) including all changes and decision rationale (the "why")
29. **Update `README.md`** version history
30. **Log "Scope for Improvement"** for future iterations
31. **Gather reusable instructions** to update `IDE_PROMPTS` and Master Rules file
32. **Report items needing discussion** at the end of the session
