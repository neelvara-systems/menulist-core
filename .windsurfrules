Framework: Next.js (v16.3.0) with React (v19.2.8)
Language: TypeScript (v5.8.3)
Dependency freeze gate: npm run verify:dependency-freeze
Next 16 migration status: locally complete; remaining Vercel/device/Firebase QA/Git evidence is external or operational, not missing migration code
Next private PostCSS exception: closed by stable Next 16.3.0, which carries patched PostCSS 8.5.23. Keep exact framework-aligned pins; never use canary/preview, node_modules patches, private-pin overrides, npm audit fix --force, or a framework downgrade.
Next 16.3 adoption boundary: retain the default build-cache, memory-eviction, SSR, prefetch, and immutable-asset improvements. Do not enable Cache Components, Partial Prefetching, experimental compiler/offline flags, or TypeScript 7 without a separate verified migration.

UI Components:
Ant Design (v5.25.1)
Framer Motion (v12.42.2)
React Icons (v4.12.0)

State Management:
Redux Toolkit (v2.12.0)
React Redux (v9.3.0)
Redux Persist (v6.0.0)

Rich Text Editing:
Tiptap Editor Suite (v2.11.0)

Authentication:
NextAuth.js (v4.24.15)

Backend Integration:
Axios (v1.18.1)
Firebase client (v11.7.3)
Firebase Admin root app (v14.2.0, modular entry points through the repo compatibility boundary)
MenuList Functions: firebase-admin (v13.10.0), firebase-functions (v7.3.0 stable)
Answerlattice Functions: firebase-admin (v13.10.0), firebase-functions (v7.3.0 stable)
SignalDesk Functions: firebase-admin (v13.10.0), firebase-functions (v7.3.0 stable)
Google GenAI SDK (@google/genai v2.13.0)
Gemini production routing: explicit stable IDs only; 3.5 Flash-Lite for structured/high-throughput work, 3.6 Flash for complex/escalation work, 3.5 Flash for balanced work, and 3.1 Flash Image variants for image work. All generateContent calls pass through the shared request compatibility compiler.

Build Tools:
Node.js (v22; .nvmrc v22.23.1)
Vercel Next/Turbopack builds: 4096 MiB V8 ceiling inside the standard 8 GiB container; browser/server source maps disabled; runtime-only filesystem paths require guarded Turbopack ignore plus explicit output tracing
Vercel deployment trace: never globally exclude node_modules/@swc/**; Next 16 routes require @swc/helpers, and build:vercel must pass verify:next-deployment-bundle from an isolated traced route bundle
Next server Firebase Admin bundle: keep firebase-admin in transpilePackages and out of serverExternalPackages/custom server externals; client aliases still block browser imports, build:verify runs the source/dependency-chain guard before compilation, and isolated sign-in plus NextAuth API traces must reject hashed native Firebase Admin externals that can trigger jwks-rsa CommonJS to jose 6 ERR_REQUIRE_ESM on Vercel
SASS (v1.101.7)
ESLint (v9.39.5)

Image Processing:
Compressor.js (v1.2.1)
React Cropper (v2.3.3)
Fabric.js (v7.4.0, bundled TypeScript types)

Additional Features:
PWA Support (Serwist v9.5.12 with owner/customer/MyCodex worker isolation)
Internationalization (next-intl v4.13.4)
Date handling (native Intl/Date plus dayjs v1.11.13 where needed)

Mobile Support:
Current mobile runtime: Tailwind-driven owner mobile shell/screens; no antd-mobile runtime dependency is installed.
Desktop: antd + SCSS modules (unchanged)
Mobile: Tailwind CSS + shared business logic; add antd-mobile only through an explicit dependency/freeze update guarded by npm run verify:dependency-freeze.
Icons: react-icons/lu (Lucide) only — never mix icon sets
ICP: Non-tech SMB owner — zero jargon, large touch targets, instant feedback

Mobile Rules (MANDATORY for every new feature):
1. Every feature MUST have [feature-name]_mobile-support.md
2. Feature Admission Test (4 gates): Frequency, Speed, Touch, Value — all must pass for mobile
3. DAL-first: DAL → Hook → Desktop UI → Mobile UI (if operational)
4. Mobile inherits: auth (NextAuth), localization (next-intl, RTL), settings (theme, language, timezone from Redux)
5. No desktop refactoring — mobile is a new clean layer using existing shared logic
6. Optimistic updates required on mobile — UI updates instantly, backend syncs after
