# Canonica Scheduler Architecture Marketing Notes

This is internal infrastructure, not a public marketing feature.

Allowed internal wording:

- Canonica runs governance checks automatically after each workspace's support day closes.
- Canonica keeps scheduler work centralized and cost-controlled.
- Canonica uses workspace-local time instead of one fixed global midnight.

Avoid public claims about guaranteed real-time processing. Canonica governance remains batch-oriented and deterministic.

