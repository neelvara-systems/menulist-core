# Roles & Permissions — Firebase Cost Tracking

**Feature:** Staff-Level RBAC (Layer 1)
**Status:** Firebase cost evidence; not current launch certification
**Last Updated:** July 16, 2026
**Priority:** LOW — Role checks use cached session data. Only role CRUD triggers writes.

> **Scope:** This doc covers Firebase ops for role definitions and user role assignments. For OutletPolicy (Layer 2) ops, see [Multi-Chain Permissions Firebase](../multi-chain-permissions/multi-chain-permissions_firebase.md).
>
> **Launch Boundary:** This file records role-permission cost evidence, not current production-launch approval. Current release approval requires the active [production-readiness audit](../audits/menulist-production-readiness-audit.md), [External Certification Runbook](../production-readiness/external-certification-runbook.md) evidence, permission-policy browser QA, staff role CRUD QA, Firebase deploy evidence where rules/functions change, and target-environment smoke.

---

## Summary

- **Collections Used:** `stores` (role definitions in `roles[]` array), `users` (authoritative role assignment in `stores[].role`), `staffStoreAccessState` (private active-assignment concurrency projection)
- **Storage Buckets:** None
- **Cloud Functions:** None
- **API Routes:** `GET/POST/PATCH/DELETE /api/staff`, `POST /api/staff/password-reset`, `POST /api/staff/force-signout`, `GET /api/auth/access-status`, `POST /api/auth/change-password`, `POST/PATCH/DELETE /api/staff/roles`, plus owner permission guards on analytics, domain/subdomain, and POS sync APIs
- **Estimated Monthly Cost:** **₹0.00 to ₹10/month for typical SMB use** — role checks stay in-memory; staff/role admin screens are rare owner actions; authenticated dashboard sessions run a lightweight access check while visible

---

## Data Storage Map

| Data                                  | Location                       | Size                      | Notes                                                                                                           |
| ------------------------------------- | ------------------------------ | ------------------------- | --------------------------------------------------------------------------------------------------------------- |
| Role definitions (3 default + custom) | `stores/{storeId}.roles[]`     | ~2KB per role × 3–5 roles | Array of `StoreRoleDataType`, each with `id`, `name`, `description`, `active`, `permissions` (29 boolean flags) |
| User's role assignment per store      | `users/{userId}.stores[].role` | ~20 bytes per store       | Role ID string (e.g., `"owner"`, `"manager"`, `"staff"`, `"custom-{ts}"`)                                       |
| Active assignment concurrency state | `staffStoreAccessState/{tenantId}_{storeId}` | At most 500 active compatible staff assigned to one store | Private Admin-SDK projection with exact tenant/store scope, nonnegative safe-integer revision, Firestore update timestamp and unique bounded `{userId, role}` assignments. It serializes last-owner, role-in-use, mapping and block/unblock invariants and fails closed if malformed or oversized. Explicitly unverified users are excluded; missing legacy `isVerified` stays compatible. No profile, email, phone or public field is copied. |

---

## Firestore Operations

### Reads

| Operation                       | Collection | Trigger                  | Frequency         | Docs Read | Notes                                                                                                                                                             |
| ------------------------------- | ---------- | ------------------------ | ----------------- | --------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Resolve user role + permissions | — | Session load | Per login/refresh | 0 incremental | `sessionProvider` uses loaded store/session data. |
| Permission check in UI | — | Any gated feature access | Per feature | 0 | `userPermissions` context set once at session load. |
| Staff list | `users`, `stores` | Staff screen open | Rare | Current-store users + at most 32 active tenant-store rows | Server API rejects inactive, soft-deleted, or platform-blocked target stores before listing users. It then queries current `users.storeIds array-contains {storeId}` plus legacy `users.storeId == {storeId}` numeric/string variants, filters by tenant, and returns only current-store mappings to non-master managers. Master store discovery filters `active == true` before applying the one-master-plus-30-outlet cap, so historical deactivated outlets cannot consume the sentinel. A 32nd active row fails closed. The already-authorized target store is merged back only for legacy rows missing the `active` field, with the same 31-store bound. |
| Legacy default-role repair | `stores/{storeId}`, `staffStoreAccessState/{tenantId}_{storeId}` | Staff screen open/create on an old store missing default roles or permission keys | One time per legacy store | Role transaction reads current store/access state | Recomputes missing `owner` / `manager` / `staff` definitions and permission keys from current transaction truth before staff role validation. |
| Role save validation | `stores`, `staffStoreAccessState` | Role create/update/deactivate | Rare | 1 store + 1 private access-state doc | Reads both documents in one transaction. Role mutations bump the access-state revision, so a concurrent assignment and role deactivation cannot both commit. |
| Copy/share login details | — | Owner copies, native-shares, or opens WhatsApp Web from the one-time login details popup | Rare | 0 | Uses the passcode already returned by create/reset and the selected staff phone number already loaded in the UI. No extra Firestore read. |

### Writes

| Operation                        | Collection         | Trigger                                        | Frequency     | Docs Written | Fields                                | Notes                                                                                                                                                                                     |
| -------------------------------- | ------------------ | ---------------------------------------------- | ------------- | ------------ | ------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Create default roles (new store) | `stores/{storeId}` | Store creation (onboarding or outlet creation) | Per new store | 0            | —                                     | Roles array is part of the store doc write. `createDefaultRoles()` generates the array in-memory; it's written as part of `addStore()` or outlet creation transaction. No separate write. |
| Update role definition           | `stores/{storeId}` | Admin edits role permissions                   | Very rare     | 1            | `roles[]` (merge update on store doc) | Owner changes a role's 29 permission flags via Team Management UI.                                                                                                                        |
| Create custom role               | `stores/{storeId}` | Admin adds new role                            | Very rare     | 1            | `roles[]` (array append on store doc) | New `StoreRoleDataType` with `id: "custom-{timestamp}"`.                                                                                                                                  |
| Backfill/normalize default roles | `stores/{storeId}`, `staffStoreAccessState/{tenantId}_{storeId}` | Staff list/create on a legacy store | One time | Transaction reads current store/access state; writes both revisions | `roles[]`, access-state revision | Recomputes missing default IDs/permission keys from fresh transaction data, preserving concurrent role edits. Custom roles are not normalized or reactivated. |
| Assign role to user              | `users/{userId}`   | Admin changes user's role                      | Very rare     | 1            | `stores[].role`                       | Updates the role ID on the user's store entry.                                                                                                                                            |
| Create user with role            | `users/{userId}`   | New user signup/invite                         | Per signup    | 1            | `stores[].role` (+ other user fields) | Role set during user creation. Part of user doc write.                                                                                                                                    |
| Update staff profile/mapping     | `users/{userId}`   | Owner edits staff                              | Rare          | 1            | Profile fields, `active`, `stores[]`, `storeIds[]`, `storeId`, `sessionRevokedAt`, `authDisabled` | Server validates tenant/store/role before writing, and new store mappings must point to active, not soft-deleted, not platform-blocked stores. Mapping admission supports the existing maximum of one master plus 30 outlets. Deactivation and every store/role mapping change revoke sessions; deactivation is also mirrored to Firebase Auth disabled state. |
| Reset staff password/passcode    | `users/{userId}`   | Owner resets staff password or new staff setup | Rare          | 2 transaction reads + 2 writes normally | Temporary `passcodeResetPending`; then `passwordResetRequestedAt`, `passcodeResetAt`, `staffLoginId`, `loginUsername`, `sessionRevokedAt`, `authTokensRevokedAt` | A leased reservation serializes concurrent resets before Firebase Auth password work. Finalization clears only the matching operation and returns the confirmed temporary passcode once. MenuList stores only reset metadata, never the passcode. |
| Self-service password/passcode change | `users/{userId}` | Signed-in owner or staff member changes own password/passcode | Rare | 1 | `modifiedOn`, `passwordChangedAt` | Route verifies the current password through Firebase Auth REST, updates Firebase Auth through Admin SDK, and writes only password-change metadata to Firestore. |
| Force sign out staff             | `users/{userId}`   | Owner signs out active staff                   | Rare          | Transaction reads current user; 1 user write | `sessionRevokedAt`, `sessionRevokedBy`, `sessionRevokedReason`, `authTokensRevokedAt` | Firestore revocation commits first; Firebase Auth refresh-token revocation follows after commit. Rejected transactions make no provider-side revocation call. |
| Verify existing placeholder      | `users/{userId}`, `staffStoreAccessState/{tenantId}_{storeId}` | Platform verifies an unverified user | Rare migration/admin action | Existing user/store/access-state transaction plus 1 user/access-state write | `firebaseUid`, `isVerified`, canonical mapping, optional password-reset metadata | Creates a new Firebase Auth user, commits its UID to the same placeholder, then returns `existing_user_auth_bound`. Auth email collision or incomplete prior UID binding fails closed; failed Firestore commit deletes only the Auth UID created by that request. |
| Remove staff from store          | `users/{userId}`   | Owner removes staff                            | Rare          | 1            | `stores[]`, `storeIds[]`, `active`, `deleted`, `deletedAt`, `sessionRevokedAt`, `authDisabled` | Every removal revokes prior sessions so removed store claims cannot persist. If no mappings remain, the user is also deactivated, soft-deleted and disabled in Firebase Auth. |
| Platform user block/unblock      | `users/{userId}`   | Platform Entity Blocks action                  | Rare          | 2-7 reads and 2 writes normally; up to 5 bounded reconciliation attempts under contention | `blocked`, `blockDetails`, `authDisabled`, `authSyncRevision`, temporary `authSyncPending`, `authSyncStatus`, `authSyncedAt`, session-revocation fields when blocked | Firestore desired access state commits before Firebase Auth work. Provider reconciliation clears pending state only when the operation revision remains current; superseded operations converge to the latest desired state and return conflict rather than stale success. |
| Staff access-state initialization | `users`, `staffStoreAccessState` | First staff/role/block mutation for a legacy store | Once per store | Up to two tenant queries capped at 501 rows each plus 1 private state write | Lazy compatibility initialization merges numeric/string tenant aliases, rejects an oversized tenant result, and filters exact authoritative `stores[]` mappings so missing/stale `storeId` or `storeIds` aliases cannot hide an active owner or role assignment. It excludes inactive, deleted or platform-blocked users and admits at most 500 unique active assignments. A transaction creates the state only if another request has not already done so. |
| Staff/role access mutation serialization | `users`, `stores`, `staffStoreAccessState` | Staff create/add/update/remove, role save/deactivate or platform user block/unblock | Rare | 1 user or store write plus 1 state write per affected store | Deterministic transaction read order prevents lost arrays; state revisions serialize assignment versus role changes, and active-owner checks use the same committed projection. |
| Session access check             | `users`, `tenants`, `stores` | Authenticated dashboard focus/interval check | While dashboard is open | 0-3 reads; shared `DATA_READ` gate runs first | None | `GET /api/auth/access-status` uses same-origin credentials, no-store cache policy, and manual redirect handling. Rate-limited checks return `429` without `valid: false`; accepted checks catch revoked sessions, inactive/deleted users, tenants and stores, cross-tenant scope, and direct/entity blocks. |

Staff create/update client diagnostics, desktop/mobile staff login-share diagnostics, server-side staff Auth/lifecycle/role-repair breadcrumbs, and staff security-event metadata shaping add no Firestore reads/writes and no Firebase Auth calls. `src/lib/staffManagement/diagnostics.ts` and the staff server helper only change failed desktop form logging, failed desktop/mobile copy/share/WhatsApp handoff logging, missing Firebase Auth user breadcrumbs during staff access sync/token revocation, password setup email failure diagnostics, default-role repair breadcrumbs, existing-user add breadcrumbs, new-staff creation breadcrumbs, passcode reset breadcrumbs, force-signout breadcrumbs, and authorization/validation/rate-limit security event details: unknown client errors show generic owner-facing copy, while secure diagnostics record bounded tenant/store/user/action/reason/provider-code metadata, staff login field presence/length, copy/share result metadata, bounded route/session metadata, and source error name/code/status without raw staff IDs, passcodes, phone numbers, generated login messages, raw staff data, server payloads, Firebase Auth missing-user context, password setup provider details, staff lifecycle identifiers, raw `buildSecurityContext()` output, or provider objects.

June 30 permission/staff security-log boundary hardening is Firebase-cost neutral. `src/lib/permissions/server.ts` and `src/lib/staffManagement/server.ts` now use bounded route/session metadata plus length/count-only label, permission, tenant, store, and role context for authorization, validation, and rate-limit security events. Valid permission checks, staff CRUD, role CRUD, Firebase Auth work, Firestore reads/writes, owner/mobile UI behavior, rules, indexes, Cloud Functions, Firebase deploy requirement, and Vercel deploy action are unchanged.

July 1 store-permission target hardening is Firebase-cost neutral. `src/lib/permissions/server.ts` now rejects inactive, soft-deleted, or platform-blocked store documents inside the existing store permission read before route-specific writes or provider calls proceed. This changes authorization behavior only and adds no new reads, writes, indexes, rules, Cloud Functions, Firebase deploy requirement, or Vercel deploy action.

July 6 store permission scope document-ID boundary is Firebase-cost neutral. `normalizeStorePermissionScopeDocumentId()` rejects malformed, reserved, empty, whitespace-mutated, path-shaped, decimal, zero, negative, unsafe, or nonnumeric session and explicit tenant/store scope IDs before the shared `stores/{storeId}` permission read. Valid permission checks keep the same one store read, tenant ownership comparison, blocked-store rejection, role resolution, owner/staff permission behavior, route-specific writes/provider calls, and Firestore read/write counts. This changes app-side permission-scope admission only and adds no Firestore rules/indexes, Cloud Functions, Firebase deploy requirement, or Vercel deploy action.

July 29 current-platform permission authority adds one exact
`users/{sessionUserId}` Admin read whenever a signed platform session requests
the shared store-role override. That read verifies current identity, email,
lifecycle, revocation and platform role before tenant/store operations; revoked
or stale platform sessions fail closed. Ordinary owner/staff permission checks
retain their existing read counts. The change adds no writes, indexes, rules,
Cloud Functions or deployment requirement.

July 28 exact permission-alias admission is also Firebase-cost neutral. All three shared permission overloads now reconcile root/nested session aliases before authority, reconcile every present persisted store tenant alias, and verify optional embedded store aliases against the selected document. Staff-management authority uses the same exact session projection. Conflicting or malformed present compatibility state stops before existing permission/store/role reads or mutations; valid-path read/write counts, rules, indexes, Functions, caches and provider behavior are unchanged.

July 1 staff/role target-store hardening adds no writes, deletes, rules, indexes, Cloud Functions, Firebase deploy requirement, or Vercel deploy action. Staff create/update mapping validation and role create/update/deactivate use existing store reads to reject inactive, soft-deleted, or platform-blocked stores before assignment or `roles[]` writes. Staff list now performs the target-store eligibility read before user enumeration; for master users this can add one target-store document read to a rare staff-list request.

July 1 owner AI permission hardening reuses the existing store permission read on protected owner AI/capacity/status routes. Rejected users do not reach capacity checks, media fetches, Cloud Tasks fanout, analytics Firestore reads, insight writes, provider calls, operation-log writes, credit consumption, or route-specific writes. This adds no writes, deletes, rules, indexes, Cloud Functions, Firebase deploy requirement, or Vercel deploy action.

Staff/role browser response parsing is cost-neutral. `src/lib/staffManagement/client.ts` caps staff and role API response JSON at 256KB, keeps only bounded `code` and status values from rejected responses, and requires successful staff list, staff mutation, and role mutation envelopes before desktop or mobile staff/role screens use the data. Staff create/update/remove/reset/sign-out calls also require the expected mutation `mode` plus returned `user` and `userId` acknowledgement before local staff rows, selected-user state, passcode sheets, or success copy can advance; returned `user.id` must match `userId`. This adds no Firestore reads/writes/deletes, Storage operations, Firebase Auth operations, Cloud Function logic, provider calls, cache invalidations, API routes, rules, indexes, schema changes, owner-facing settings, Firebase deploy requirement, or Vercel deploy action.

Staff mutation user-ID admission is cost-neutral for valid update, remove, reset-passcode, and force-sign-out requests. `src/lib/staffManagement/server.ts` validates target `userId` values through `StaffUserIdSchema`, does not trim `userId` before validation, rejects malformed, whitespace-mutated, empty, oversized, path-shaped, or reserved Firestore document IDs, and reuses local `targetUserId` for `.doc(targetUserId)`, staff mutation acknowledgements, bounded diagnostics, owner-safety checks, session revocation, and Firebase Auth disabled-state sync. This adds no Firestore reads/writes/deletes for valid requests, Storage operations, Cloud Functions, provider calls, cache invalidations, API routes, rules, indexes, owner-facing settings, Firebase deploy requirement, or Vercel deploy action.

Staff store scope document-ID admission is cost-neutral for valid staff list, default-role repair, and role save/delete requests. `src/lib/staffManagement/server.ts` validates staff store scope through `normalizeStaffStoreScopeDocumentId()`, rejects malformed, reserved, empty, whitespace-mutated, path-shaped, decimal, zero, negative, unsafe, or nonnumeric store IDs, and uses `.doc(storeScope.documentId)` for the existing `stores/{storeId}` reads and role/default-role writes. This adds no Firestore reads/writes/deletes for valid requests, Storage operations, Cloud Functions, provider calls, cache invalidations, API routes, rules, indexes, owner-facing settings, Firebase deploy requirement, or Vercel deploy action.

July 11 staff-scope follow-up extends the same exact positive safe-integer contract to request schemas, session authority, persisted user tenant/store fields, mapping arrays, last-owner/role-in-use checks and response filtering. Legacy coercive aliases are omitted before authorization or rewrite. Adding/changing/removing mappings now revokes existing refresh tokens and records session revocation in the existing user write. New-user creation compensates a failed first Firestore write by deleting only the Auth account created by that request. Passcode reset adds one reservation read/write and one finalization read/write to serialize provider side effects and retain a recoverable operation marker; it stores no passcode. These changes add no rule, index, Function, Firebase deploy or owner setting.

July 11 concurrency follow-up adds the private `staffStoreAccessState` projection and deterministic Firebase-UID user document creation. Concurrent staff additions, owner removals, role assignment/deactivation and role edits now share transactional conflict documents; canonical store names are copied from `stores`, not accepted from request mappings. Platform user block/unblock updates the same state in its Firestore-first transaction so a blocked owner cannot satisfy last-owner safety. This adds bounded reads and one private state write per affected store on rare staff/role/block mutations. The existing catch-all Firestore rule denies all client access to the new collection, so no rules, indexes, Functions or Firebase infrastructure source changed and no Firebase deploy is required.

July 16 owner-target and Auth-binding follow-up is app-side and uses the existing `users`, `stores`, and private access-state documents. Manager actions against Owner mappings fail before provider work and are rechecked from transaction-current user truth. Staff session-revocation fields commit before best-effort Firebase refresh-token revocation, so rejected mutations cannot sign a user out. Default-role repair now shares the role transaction instead of overwriting `stores.roles` from a stale snapshot. Existing-placeholder verification adds one Firebase Auth create plus the existing user/access-state transaction and optional password-reset metadata write; email collisions are rejected and never adopted. Password setup email waits are capped at 10 seconds, and a post-commit metadata failure cannot reverse the account response. Access status uses the existing tenant/store reads to reject inactive/deleted truth. No rules, indexes, Storage rules, Cloud Functions, scheduler, new owner setting, or Firebase deploy is required for this checkpoint.

July 28 access-state closeout makes the private guard an exact persisted contract. Tenant/store scope, revision, timestamp, assignment identities, roles, uniqueness and the 500-assignment ceiling are checked on every transactional read; invalid state blocks the mutation instead of being coerced or partially ignored. Legacy initialization uses bounded numeric/string tenant scans and authoritative `stores[]` mappings rather than denormalized store aliases, preventing an in-use role or last owner from disappearing from the guard. The collection remains Admin-only under root default deny, needs no composite index, and has no cache or public-output consumer. This is an app-side Admin read/write change only; no Firestore rules, indexes or Functions changed.

### Firestore Rules Boundary

`users/{userId}` direct writes are not part of the owner/staff runtime. Firestore rules allow platform-admin writes only; owner/staff profile updates, password/passcode changes, staff CRUD, role assignment, removal, and session revocation go through authenticated API routes with Admin SDK writes. This prevents direct client edits to `stores[]`, `storeIds[]`, `role`, `active`, or revocation fields.

### Deletes

None — roles are deactivated (`active: false`), never hard-deleted. Staff users are soft-deleted only when the last store mapping is removed; Firebase Auth accounts are disabled and refresh tokens are revoked for blocked/deactivated access but not hard-deleted by the owner UI.

---

## Cost Estimate

Typical SMB estimate: **₹0.00 to ₹10/month**. Permission checks remain in-memory. Staff list reads happen only when an owner opens Team/Staff management and are bounded to users assigned to the current store, including legacy `storeId`-only records; writes happen only on rare staff or role changes. The access-status check is a small authenticated-dashboard safety read while the app is visible, now guarded by the shared `DATA_READ` limiter before Firestore reads and by browser-side same-origin/no-store/manual-redirect request handling. The upper end assumes multiple staff dashboards stay open for long shifts; inactive browser tabs do not run the interval check.

---

## DAL Functions Used

| Function             | File                               | Operation Type               | Reads | Writes |
| -------------------- | ---------------------------------- | ---------------------------- | ----- | ------ |
| `createDefaultRoles` | `src/data/shared/defaultRoles.ts`  | In-memory (returns array)    | 0     | 0      |
| `getActiveSession`   | `src/lib/auth/getActiveSession.ts` | Session cache (no Firestore) | 0     | 0      |
| `updateStore`        | `src/database/stores/index.tsx`    | Write (updateDoc merge)      | 0     | 1      |
