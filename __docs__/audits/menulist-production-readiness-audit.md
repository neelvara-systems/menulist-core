# MenuList Production Readiness Audit

**Audit date:** June 11, 2026  
**Latest evidence update:** July 6, 2026
**Product:** MenuList  
**Stage:** Stage 6 system audit with Stage 5 feature hardening loop  
**Current verdict:** **Not production ready** as a full-system certification, because the high-risk feature clusters and whole-app visual/runtime QA are controlled-owner-testing ready after the fixes below, but provider sandbox smoke, real mobile device QA, Firebase Function deployment, Firebase Storage rules deployment, and production deploy smoke remain pending or blocked where noted. Authenticated platform-owner HTTP smoke passed for the audited ops/internal routes, Chrome visual QA found and fixed platform dashboard layout issues, and the follow-up pass covered public website, owner desktop, forced mobile shell, public tenant menu/OBP/compliance/feedback, public API fail-closed behavior, and authenticated localhost `npm run dev` route checks. Later 390px browser smokes covered key public website routes, the active `habibis` tenant/customer fixture, and the local tenant-host safe fallback; physical-device QA and authenticated owner-shell mobile QA remain pending. Firestore rules for Digital Screens, Analytics/Feedback/Reviews, and Roles/Auth slices were deployed successfully. The latest documented MenuList staging Firebase Function deploy attempts for the source-file path hardening subset (`functions:processMenuImages`, `functions:processMenuImagesJob`, `functions:startGeneration`, `functions:embedArticleWorker`, and `functions:regenerateEmbedding`), `functions:processMenuImagesJob`, `functions:menulistMaintenanceScheduler`, the staleness lifecycle delivery subset (`functions:computeDecisionBlocksScores`, `functions:triggerDecisionBlocksScoring`, and `functions:triggerStoreNightlyScheduler`), the scheduler-hour diagnostics subset (`functions:messagingOnboarding` and `functions:backfillStoresSummary`), the Maps Place Check raw provider output subset (`functions:mapsPlaceCheck`), and the owner-notification template-output, owner-notification flag/trigger diagnostics, and legacy lifecycle event/status diagnostics subsets (`functions:verifyMenuPublish`, `functions:computeDecisionBlocksScores`, `functions:triggerDecisionBlocksScoring`, and `functions:triggerStoreNightlyScheduler`) targeted `menulist-qa`, completed predeploy lint/build, and remain blocked by Cloud Resource Manager HTTP 403 caller permission before upload. The latest Answerlattice staging Firebase Function deploy attempt for `functions:answerlattice` targeted `answerlattice-qa`, completed predeploy build, and remains blocked by Cloud Resource Manager HTTP 403 caller permission before upload. The latest MenuList staging Storage rules deploy attempt also targeted `menulist-qa` and remains blocked by Service Usage HTTP 403: project not found or permission denied before rules upload.

This report tracks the feature-by-feature production-readiness pass requested for MenuList. Codebase truth is treated as primary. Docs are corrected only after runtime behavior is verified.

Answerlattice shared session scope boundary checkpoint: fixed in source. Answerlattice runtime/support routes that resolve tenant/store scope through `src/lib/answerlattice/sessionScope.ts` keep the same `productAccounts.AL` precedence, Answerlattice product-id fallback, scoped-session rewrite, support-client route detection, management-role checks, public-content scope fallback, route permission gates, rate-limit ordering, and downstream valid Firestore reads for valid sessions, but malformed tenant/store scope can no longer pass through shared scope resolution by whitespace trimming or loose numeric coercion. `normalizeAnswerlatticeScopeDocumentId()` now requires tenant/store values to be exact positive numeric Firestore document IDs before `resolveAnswerlatticeSessionScope()`, `getAnswerlatticeProductAccount()`, `getAnswerlatticeScopedSession()`, and `resolveAnswerlatticePublicContentScope()` expose scope to downstream routes. `npm run verify:answerlattice-runtime-truth` and `npm run verify:help-center-boundary` source-gate the exact helper, shared public-content reuse, old loose `normalizeNumber` / `normalizeScopeNumber` exclusions, route docs, audit evidence, and changelog evidence. This changes malformed shared Answerlattice session-scope admission only; it does not change valid Answerlattice owner/admin access, valid Help Center scoped search, valid widget/public-content routes, valid activation/dashboard routes, valid billing/session behavior, Firestore read/write counts for valid requests, Firestore rules/indexes, Cloud Functions, Firebase/Vercel deploys, production builds, browser/device QA, provider smoke, production-host smoke, launch approval, or release certification.

Answerlattice chat session scope boundary checkpoint: fixed in source. The HelpChat / Answerlattice chat-session DAL keeps the same Answerlattice Firebase client, `chatSessions` collection, valid chat image Storage path shape, new-session/update/delete acknowledgements, user history cap, admin conversation pagination, statistics scan caps, top-question/knowledge-gap scans, and chat-volume read shape for valid sessions, but malformed session `tId/sId` can no longer reach chat image Storage path composition or `chatSessions` Firestore filters through raw `session.tId/session.sId` values or loose numeric coercion. `src/database/chatSessions/index.ts` now normalizes chat-session scope as exact positive numeric Firestore document IDs before user/admin/stat reads or chat image uploads, and passes the normalized scope into `generateStoragePath()`. `npm run verify:answerlattice-runtime-truth` source-gates the exact scope helper, normalized Storage path input, normalized query filters, old raw session filter/coercion exclusions, Help Center docs, audit evidence, and changelog evidence. This changes malformed chat-session scope admission only; it does not change valid chat behavior, valid chat image uploads, valid owner/admin chat reads, Firestore read/write counts for valid requests, Storage operations for valid uploads, Firestore rules/indexes, Cloud Functions, Firebase/Vercel deploys, production builds, browser/device QA, provider smoke, production-host smoke, launch approval, or release certification.

Answerlattice chat analytics scope boundary checkpoint: fixed in source. The optimized Answerlattice chat analytics DAL keeps the same Answerlattice Firebase client, `chatAnalytics` and `chatSessions` collections, dashboard statistics behavior, top-question and knowledge-gap aggregation, chat-volume chart reads, paginated conversation reads, daily aggregate document shape, last-update read, read caps, bounded today-live fallback diagnostics, and one daily aggregate write for valid sessions, but malformed session `tId/sId` can no longer reach analytics query filters or `{tId}_{sId}_{YYYY-MM-DD}` aggregate document IDs through raw `session.tId/session.sId` values. `src/database/chatAnalytics/index.ts` now normalizes analytics scope through the shared Answerlattice exact positive numeric Firestore document-ID helper before reads or aggregate writes. `npm run verify:answerlattice-runtime-truth` source-gates the shared helper import, normalized query filters, normalized aggregate doc ID/data, old raw session filter/doc-ID exclusions, Help Center docs, audit evidence, and changelog evidence. This changes malformed chat analytics session-scope admission only; it does not change valid dashboard analytics behavior, valid aggregate writes, Firestore read/write counts for valid requests, Storage operations, Firestore rules/indexes, Cloud Functions, Firebase/Vercel deploys, production builds, browser/device QA, provider smoke, production-host smoke, launch approval, or release certification.

Menu Card Design Advisor session scope boundary checkpoint: fixed in source. The Pro/Premium layout suggestion route keeps the same `withAuth()` admission, feature flags, Safe Mode check, AI operation limiter, 128KB bounded body reader, request schema, route permission guard, Pro/Premium plan gate, AI capacity check, Gemini call, recommendation normalization, AI accounting, bounded diagnostics, and owner-safe failure copy for valid sessions, but malformed session tenant/store scope can no longer reach tenant access, request parsing, route permission checks, subscription lookup, capacity checks, provider calls, recommendation normalization, or AI accounting through loose numeric coercion. `src/app/api/menu-card-export/design-advisor/route.ts` now validates session tenant/store scope as exact positive numeric Firestore document IDs before route work. `npm run verify:menu-card-export` and `npm run verify:menulist-api-tenant-safety` source-gate the strict helper, normalized tenant/store usage, old raw `Number(session.tId/sId)` exclusions, Menu Card Export docs, audit evidence, and changelog evidence. This changes malformed Menu Card Design Advisor session-scope admission only; it does not change valid Pro/Premium advice behavior, valid plan/capacity checks, valid AI accounting, Firestore read/write counts for valid requests, Firestore rules/indexes, Cloud Functions, Firebase/Vercel deploys, production builds, provider smoke, browser/device QA, production-host smoke, launch approval, or release certification.

Answerlattice support ticket session scope boundary checkpoint: fixed in source. The MenuList Help Center / Answerlattice support-ticket owner reads and listeners keep the same Answerlattice Firebase client, platform-vs-owner session branching, `supportTickets` collection, `tId + sId + deleted` query shape, latest-ticket caps, platform-wide support queue behavior, scoped mutation guard, stable Firestore rules, and bounded Help Center source gate for valid sessions, but malformed owner session `tId/sId` can no longer reach ticket queries through raw `session.tId/session.sId` filters or loose numeric coercion. `src/database/tickets/index.ts` now normalizes support-ticket session scope as exact positive numeric Firestore document IDs before owner ticket reads/listeners or non-platform mutation scope comparison. `npm run verify:help-center-boundary` and `npm run verify:answerlattice-runtime-truth` source-gate the exact scope helper, normalized query filters, old raw session filter exclusions, Help Center docs, audit evidence, and changelog evidence. This changes malformed owner support-ticket session-scope admission only; it does not change valid owner ticket history, valid platform support-ticket views, valid ticket create/update/message/status behavior, Firestore read/write counts for valid requests, Storage operations, Firestore rules/indexes, Cloud Functions, Firebase/Vercel deploys, production builds, browser/device QA, provider smoke, production-host smoke, launch approval, or release certification.

Public Truth Monitor session scope boundary checkpoint: fixed in source. The authenticated summary and refresh APIs keep the same feature gate, DATA_READ/DATA_WRITE limiter placement, bounded refresh body parser, selected-project validation, tenant access check, Business Health permission check, paid entitlement gate, store/project/summary read shape, capped summary write, fixed owner copy, and bounded diagnostics for valid sessions, but malformed session tenant/store scope can no longer reach permission checks, store reads, subscription lookups, project summary reads, summary reads/writes, or report entry scope through loose numeric coercion. `src/lib/public-truth-tools/serverPublicTruthMonitorEntitlements.ts` now exposes `normalizePublicTruthMonitorSessionScopeDocumentId()` and `getPublicTruthMonitorSessionScope()`, and both `/api/public-truth-monitor/summary` and `/api/public-truth-monitor/refresh` use the normalized scope before route-local Firestore work. `npm run verify:public-truth-monitor-addon` source-gates the strict helper, normalized route refs and permission arguments, old `Number(session.sId/tId)` exclusions, Public Truth Monitor docs, audit evidence, and changelog evidence. This changes authenticated Public Truth Monitor session-scope admission only; it does not change valid summary/refresh behavior, valid project selection, valid report content, Firestore read/write counts for valid requests, Firestore rules/indexes, Cloud Functions, Firebase/Vercel deploys, production builds, browser/device QA, production-host smoke, launch approval, or release certification.

Linked outlet AI policy scope boundary checkpoint: fixed in source. Description generation, image generation, image editing, batch image trigger, and translation routes keep the same authenticated route admission, input validation, store permission checks, linked-outlet policy intent, inherited item/category blocking, fixed owner copy, AI capacity checks, provider calls, and bounded diagnostics for valid project references, but malformed project/session/master scope can no longer reach provider calls through loose numeric coercion or raw `masterProjectId` store parsing. `src/lib/multiOutlet/serverOutletPolicy.ts` now uses `normalizeMultiOutletProjectId` plus the shared exact numeric document-ID guard for session tenant/store scope, requested project scope, stored master project scope, and master-store tenant scope before reading linked project/master policy or allowing AI work. Cross-tenant stored master references fail before provider work. `npm run verify:menulist-api-tenant-safety` source-gates the helper normalizers, normalized project/store refs, same-tenant checks, old loose parser exclusions, Multi-Outlet docs, Multi-Chain Permissions docs, audit evidence, and changelog evidence. This changes malformed linked-outlet AI policy admission only; it does not change valid description/image/translation behavior, valid linked-outlet policy blocks, Firestore read counts for valid requests, Firestore writes/deletes, Storage operations, Cloud Functions, Firebase/Vercel deploys, production builds, provider smoke, browser/device QA, production-host smoke, launch approval, or release certification.

Multi-Outlet shared project ID boundary checkpoint: fixed in source. The shared multi-outlet resolver and linked outlet save route keep the same feature flag behavior, master-project cache, linked-master merge semantics, fail-open render fallback, `/api/projects/outlet-save` admission, OutletPolicy enforcement, local override writes, public/screen cache invalidation, Owner Business Assistant packet-cache invalidation, and bounded diagnostics for valid project references, but malformed project-derived tenant/store scope can no longer reach `projects/{tId}/{sId}/{projectId}` reads or linked-outlet writes through prefix parsing or loose numeric coercion. `src/lib/multiOutlet/projectIdBoundary.ts` now exposes `normalizeMultiOutletProjectId`, requiring `projectId` / `masterProjectId` to be exact Firestore document IDs with exact positive numeric tenant/store path segments before resolver master reads or outlet-save route reads/writes. `src/lib/multiOutlet/resolveProject.ts` fails open with `multi_outlet_master_project_reference_invalid` for malformed persisted master references, and `/api/projects/outlet-save` rejects malformed project references before tenant access, permission checks, Firestore reads, or cache invalidation. `npm run verify:multi-location-boundary` source-gates the shared helper, resolver usage, route usage, old `parseInt()` / loose `Number()` exclusions, Multi-Outlet docs, audit evidence, and changelog evidence. This changes malformed multi-outlet project-scope admission only; it does not change valid master reads, valid linked-outlet saves, valid inherited menu rendering, Firestore read/write counts for valid requests, cache invalidation for valid saves, Firestore rules/indexes, Cloud Functions, Firebase/Vercel deploys, production builds, browser/device QA, production-host smoke, launch approval, or release certification.

Linked outlet save session store document-ID boundary checkpoint: fixed in source. The linked outlet save route keeps the same `withAuth()` admission, multi-outlet feature gate, 2MB bounded body reader, request schema, project/master project ID boundary, tenant access check, user/project HMAC rate limiter, caller/outlet/master/tenant/project read shape, `MANAGE_MENU` permission check, OutletPolicy enforcement, local override writes, public/screen cache invalidation, Owner Business Assistant packet-cache invalidation, bounded diagnostics, and acknowledgement for valid sessions, but malformed authenticated session store scope can no longer reach tenant access, caller-store reads, permission checks, linked-outlet reads/writes, cache invalidation, or acknowledgement through raw `Number(session.sId || session.user?.storeId)` coercion. `/api/projects/outlet-save` now uses `normalizeMultiOutletNumericDocumentId(session.sId ?? session.user?.storeId)` before composing caller store refs or running linked outlet save authorization. `npm run verify:multi-location-boundary` and `npm run verify:menulist-api-tenant-safety` source-gate the normalized session store scope, invalid-session diagnostic, old raw numeric-coercion exclusion, Multi-Outlet docs, audit evidence, and changelog evidence. This changes malformed linked outlet save session-scope admission only; it does not change valid outlet/master saves, valid OutletPolicy behavior, Firestore read/write counts for valid requests, cache invalidation for valid saves, Firestore rules/indexes, Cloud Functions, Firebase/Vercel deploys, production builds, browser/device QA, production-host smoke, launch approval, or release certification.

Master job status session store document-ID boundary checkpoint: fixed in source. The authenticated outlet editor master-job polling route keeps the same `withAuth()` admission, multi-outlet feature flag, shared `DATA_READ` limiter, project-ID validation, tenant access check, `MANAGE_MENU` permission check, linked outlet project validation, capped active-job query, bounded diagnostics, bounded browser response contract, and store-link rule, but session store scope can no longer be numeric-coerced before the `stores/{sId}` read. `src/app/api/projects/master-job-status/route.ts` now requires the session store scope to be an exact positive numeric Firestore document ID before caller-store reads, tenant access checks, store-link comparisons, or outlet project validation. Whitespace-mutated, zero, negative, unsafe, leading-zero, nonnumeric, reserved, empty, or path-shaped session store IDs fail before Firestore document access. `npm run verify:menulist-api-tenant-safety` source-gates the session store normalizer, normalized caller-store ref, old numeric-coercion/ref exclusions, Multi-Outlet docs, audit evidence, and changelog evidence, and still source-gates the explicit `sessionStoreId !== masterStoreId` branch. This changes authenticated master-job status session-scope admission only; it does not change valid master-store polling, valid linked-outlet polling, project-ID admission, job status semantics, Firestore read counts for valid requests, Firestore writes/deletes, Storage operations, Cloud Functions, Firebase/Vercel deploys, production builds, browser/device QA, production-host smoke, launch approval, or release certification.

Platform Founder Monitor Revenue Document ID Boundary checkpoint: fixed in source. Founder revenue movement writes keep the same transaction-time Razorpay revenue ledger, `founderRevenueMovements/{movementId}` idempotency, `platformSummary/founderMonitorRevenue` summary updates, `platformSummary/founderMonitorRevenueDaily_YYYY-MM-DD` daily updates, new-store `founderOnboardingTransitions/{storeId}` seeding, fixed platform copy, and Firebase-cost-neutral valid write shape, but malformed, reserved, empty, path-shaped, whitespace-mutated, zero, negative, unsafe, leading-zero, or nonnumeric movement IDs and transition store IDs can no longer reach Founder Monitor revenue refs. `src/lib/ops/founderRevenueReadModel.ts` now validates movement IDs with the shared Firestore document-ID guard and requires payment-to-live transition store IDs to be exact positive numeric MenuList store document IDs before the same guard. `npm run verify:platform-founder-monitor-boundary` source-gates the exact numeric store helper, old trim-before-validate exclusion, Founder Monitor docs, audit evidence, and changelog evidence. This changes Founder Monitor revenue document-ID admission only; it does not change valid Razorpay-derived revenue movement writes, valid summary counters, valid onboarding transition writes, Firestore read/write counts for valid requests, Firestore rules/indexes, Cloud Functions, Firebase/Vercel deploys, production builds, provider smoke, browser/device QA, production-host smoke, launch approval, or release certification.

Public menu project document-ID boundary checkpoint: fixed in source. The public customer menu renderer keeps the same host/store resolution, `platformSummary/projects_{storeId}` slug/default selection, linked-master resolution, active special-menu resolution, one-read project fetch for valid menus, public-output fallback behavior, bounded diagnostics, and fixed guest copy, but malformed resolved project IDs can no longer reach `projects/{tId}/{sId}/{projectId}` Admin SDK refs through raw `projectId.split("-")` scope. `src/app/client/[[...slug]]/page.tsx` now normalizes public menu project refs with `normalizePublicMenuProjectDocumentScope`, requiring the project ID to pass the shared Firestore document-ID boundary and requiring the first and final project-ID segments to be exact positive numeric tenant/store document IDs before any project read. `npm run verify:public-business-truth` and `npm run verify:menu-extraction-pipeline` source-gate the normalizer, normalized refs, old raw split/ref exclusions, Client Menu docs, audit evidence, and changelog evidence. This changes public menu project-ref admission only; it does not change valid public menu routing, valid project IDs, valid linked master menus, valid active special menus, public cache behavior, Firestore read/write counts for valid requests, Firestore rules/indexes, Cloud Functions, Firebase/Vercel deploys, production builds, browser/device QA, production-host smoke, launch approval, or release certification.

Compliance Pages public override document-ID boundary checkpoint: fixed in source. Public `/privacy`, `/terms`, and `/refund` rendering keeps the same tenant host resolution, generated policy fallback, optional owner override composition, bounded override-read diagnostics, MenuList attribution policy, and fixed guest copy for valid stores, but malformed public store scope can no longer reach the optional `compliancePages/{sId}` Admin SDK override read through raw `String(sId)`. `src/app/client/compliance/CompliancePageContent.tsx` now normalizes public override refs with `normalizePublicComplianceStoreDocumentId()`, uses `storeData.storeId` first and the returned Firestore doc id only when `storeId` is absent, and requires exact positive numeric Firestore document-ID scope before the override read. Invalid scope logs bounded `public_compliance_override_read_failed` diagnostics with fixed `public_compliance_invalid_store_scope` source metadata and keeps the generated policy fallback. `npm run verify:compliance-pages-boundary` and `npm run verify:public-business-truth` source-gate the normalizer, normalized override ref, raw `doc(String(sId))` exclusion, Compliance Pages docs, audit evidence, and changelog evidence. This changes public compliance override-read admission only; it does not change generated policy text, valid owner override rendering, compliance API behavior, owner editor behavior, Firestore read/write counts for valid requests, Firestore rules/indexes, Cloud Functions, Firebase/Vercel deploys, production builds, browser custom-domain smoke, owner/legal approval, production-host smoke, launch approval, or release certification.

Owner Business Assistant strict document-ID boundary checkpoint: fixed in source. Business Health current/analytics/locations/thread/answer/feedback routes keep the same `withAuth()` admission, hashed OBA rate limits, bounded read-model response parsing, bounded answer/feedback body readers, selected-store scope checks, `VIEW_ANALYTICS` permission checks, context-packet cache behavior, thread persistence, answer-event logging, feedback writes, fixed owner copy, and read-only action boundary, but whitespace-mutated project IDs, thread IDs, feedback answer IDs, and answer-event IDs can no longer be trimmed into valid Firestore document IDs before read-model scope resolution, context-packet cache keys, thread document reads/writes, feedback document writes, answer-event writes, provider work, bounded diagnostics, or owner-facing cache keys. `src/lib/ownerBusinessAssistant/projectIdBoundary.ts` and `src/lib/ownerBusinessAssistant/threadIdBoundary.ts` now preserve raw values and require exact raw equality; `src/lib/ownerBusinessAssistant/schemas.ts` no longer trims project, thread, or answer IDs before shared document-ID validation; `src/lib/ownerBusinessAssistant/server/answerEventLogger.ts` rejects whitespace-mutated answer-event IDs before optional writes. `npm run verify:owner-business-assistant`, `npm run verify:owner-business-health-boundary`, and `npm run verify:menulist-api-tenant-safety` source-gate the strict helpers, raw schemas, route/helper ordering, Owner Business Assistant docs, audit evidence, and changelog evidence. This changes Owner Business Assistant document-ID admission only; it does not change valid Business Health read models, valid owner answer behavior, valid thread persistence, valid feedback writes, valid answer-event writes, Firestore read/write counts for valid requests, Firestore rules/indexes, Cloud Functions, Firebase/Vercel deploys, production builds, browser/device QA, provider smoke, production-host smoke, launch approval, or release certification.

Staff mutation strict user-ID boundary checkpoint: fixed in source. Staff update, remove, reset-passcode, and force-sign-out keep the same `withAuth()` admission, session eligibility checks, hashed mutation rate limits, 16KB bounded body readers, tenant/store authority checks, target-store eligibility checks, last-owner protection, Admin SDK user writes, session revocation, Firebase Auth disabled-state sync, fixed owner copy, and mutation acknowledgements, but whitespace-mutated `userId` values can no longer be trimmed into valid Firestore document IDs before request validation, `users/{userId}` reads, owner-safety checks, mutation writes, Firebase Auth disabled-state sync, session revocation, bounded diagnostics, or mutation response IDs. `src/lib/staffManagement/server.ts` now preserves the raw value in `normalizeStaffUserId()`, requires `userId === value`, and `StaffUserIdSchema` no longer trims `userId` before `normalizeStaffUserId(value) === value`. `npm run verify:menulist-api-tenant-safety` and `npm run verify:auth-security-failure-matrix` source-gate the strict helper boundary, raw schema boundary, normalized mutation refs, raw-ref exclusions, Roles/Permissions docs, audit evidence, and changelog evidence. This changes Staff/Roles target-user ID admission only; it does not change valid staff create/update/remove/reset/sign-out behavior, permission semantics, Firestore read/write counts for valid requests, Firebase Auth operations for valid requests, Firestore rules/indexes, Cloud Functions, Firebase/Vercel deploys, production builds, browser/device QA, production-host smoke, launch approval, or release certification.

Guest Feedback strict project ID boundary checkpoint: fixed in source. The public QR feedback page and public feedback submit route keep the same feature flag, public IP limiter, 16KB bounded body reader, honeypot, Turnstile verification, project/store/tenant eligibility checks, store feedback defaults, safe review URL handling, Admin SDK `guestFeedback` write, non-blocking MOL event logging, bounded diagnostics, and fixed guest copy, but whitespace-mutated `projectId` values can no longer be trimmed into valid Firestore document IDs by either the shared helper or submit schema before page lookup, validation success, honeypot checks, Turnstile verification, project reads, submit writes, MOL event logging, or bounded diagnostics. `src/lib/feedback/guestFeedbackProjectIdBoundary.ts` now preserves the raw value and requires `documentId === value` before applying the supported project-ID character rule plus `src/lib/firebase/firestoreDocumentId.ts`; `src/lib/validation/apiSchemas.ts` no longer trims `projectId` before `normalizeGuestFeedbackProjectId(value) === value`. `npm run verify:guest-feedback-boundary` and `npm run verify:menulist-api-tenant-safety` source-gate the strict helper boundary, raw submit schema boundary, helper-level route normalization, public page lookup, Guest Feedback docs, audit evidence, and changelog evidence. This changes public Guest Feedback project-ID admission only; it does not change valid feedback links, valid feedback submissions, feedback writes for valid requests, review URL behavior, owner inbox behavior, Firestore read/write counts for valid requests, Firestore rules/indexes, Cloud Functions, Firebase/Vercel deploys, production builds, browser/device QA, production-host smoke, launch approval, or release certification.

Onboarding compensation scope boundary checkpoint: fixed in source. Website onboarding and reseller onboarding provider-failure compensation keep the same authenticated onboarding admission, bounded payment bodies, hashed rate limits, centralized tenant/store transaction helper, Razorpay subscription setup, reseller online/offline billing behavior, provider-failure compensation writes, public cache revalidation behavior, fixed owner copy, and valid generated tenant/store IDs, but provider-failure compensation now requires exact positive numeric tenant/store document IDs before `tenants/{tenantId}`, `stores/{storeId}`, or `platformSummary/storesSummary.stores.{storeId}` compensation writes. `src/lib/onboarding/compensateFailedOnboarding.ts` now preserves document IDs separately from numeric comparison IDs and rejects whitespace-mutated, path-shaped, reserved, zero, negative, unsafe, leading-zero, or nonnumeric compensation scope before document path composition. `npm run verify:menulist-api-tenant-safety` source-gates the compensation scope normalizer, normalized tenant/store refs, old numeric-coercion/ref exclusions, Auth Onboarding docs, Onboarding Centralization docs, audit evidence, and changelog evidence. This changes provider-failure compensation scope admission only; it does not change valid onboarding, valid reseller onboarding, provider behavior, valid compensation write counts, Firestore rules/indexes, Cloud Functions, Firebase/Vercel deploys, production builds, browser/device QA, production-host smoke, launch approval, or release certification.

Onboarding strict user ID helper boundary checkpoint: fixed in source. Website onboarding, reseller onboarding, and provider-failure compensation keep the same authenticated onboarding admission, bounded payment bodies, hashed rate limits, centralized tenant/store transaction helper, Razorpay subscription setup, reseller online/offline billing behavior, provider-failure compensation writes, public cache revalidation behavior, fixed owner copy, and valid Firebase Auth UID behavior, but whitespace-mutated and oversized onboarding `userId` values can no longer be trimmed into valid Firestore document IDs or accepted before `users/{userId}` document refs in shared helper paths. `src/lib/onboarding/onboardingUserId.ts` now preserves the raw helper value and requires `userId === raw`, non-empty input, a 160-character cap, and `src/lib/firebase/firestoreDocumentId.ts` before `updateUserWithTenantStore()`, `compensateFailedTenantStoreOnboarding()`, or reseller-created owner doc writes can compose a user document path. `npm run verify:agent-readiness` and `npm run verify:menulist-api-tenant-safety` source-gate the strict helper boundary, normalized document refs, raw-ref exclusions, Auth Onboarding docs, Onboarding Centralization docs, audit evidence, and changelog evidence. This changes onboarding user-document ID admission only; it does not change valid onboarding, valid reseller onboarding, provider behavior, provider-failure compensation semantics, Firestore read/write counts for valid requests, Firestore rules/indexes, Cloud Functions, Firebase/Vercel deploys, production builds, browser/device QA, production-host smoke, launch approval, or release certification.

Menu Extraction strict project/retry ID boundary checkpoint: fixed in source. The protected owner upload job route, authenticated menu-link import route, menu-intake identity preflight route, extraction review apply/discard helpers, and protected retry loader keep the same valid project/job ID shapes, `withAuth()` admission where applicable, SAFE_MODE checks, tenant/store session access checks, hashed rate limits, bounded body readers, project ownership checks, Storage URL allowlists, active/completed job reuse, identity preflight, menu-link artifact/job writes, retry source preservation, failed-job ownership verification, bounded diagnostics, and fixed owner copy, but whitespace-mutated project IDs and retry job IDs can no longer be trimmed into valid Firestore document IDs by the shared helpers before project reads, menu-link Storage path construction, identity preflight context loading, review apply/discard refs, original-job reads, provider work, job writes, or cleanup deletes. `src/lib/menu-extraction/projectIdBoundary.ts` and `src/lib/menu-extraction/jobIdBoundary.ts` now preserve the raw value and require `documentId === value` before applying the existing bounded project/job shape rules plus `src/lib/firebase/firestoreDocumentId.ts`. `npm run verify:menu-extraction-pipeline` and `npm run verify:menulist-api-tenant-safety` source-gate the strict helper boundary, schema usage, route/helper usage order, Menu Extraction docs, audit evidence, and changelog evidence. This changes Menu Extraction project/retry ID admission only; it does not change valid project IDs, valid Firestore auto-ID job retries, valid owner upload extraction, valid menu-link import, valid identity preflight, valid review apply/discard behavior, valid public create-menu jobs, messaging onboarding jobs, Firestore read/write counts for valid requests, Storage writes for valid menu-link imports, Firestore rules/indexes, Cloud Functions, Firebase/Vercel deploys, production builds, browser/device QA, provider smoke, production-host smoke, launch approval, or release certification.

Messaging preview strict route-param boundary checkpoint: fixed in source. The unauthenticated `/api/msg-preview/[sessionId]`, `/approve`, and `/fix` token-link routes keep the same preview-token checks, hashed session/IP rate-limit keys, bounded request readers, publish transaction behavior, fix-request correction limits, bounded runtime diagnostics, and fixed owner copy for valid preview links, but whitespace-mutated route/helper session IDs can no longer be trimmed into valid Firestore auto-IDs before rate-limit keys, session document reads, publish transactions, event writes, or fix-request writes. `src/lib/messaging-onboarding/previewRouteBoundary.ts` now preserves the raw value, requires `sessionId === raw`, keeps the 20-character Firestore auto-ID shape, and still applies the shared Firestore document-ID guard. `npm run verify:menu-extraction-pipeline` and `npm run verify:menulist-api-tenant-safety` source-gate the strict raw-param normalizer, route usage order, publish helper boundary, Messaging Onboarding docs, audit evidence, and changelog evidence. This changes messaging-preview session route/helper admission only; it does not change valid preview links, token comparison, publish/fix behavior, Firestore read/write counts for valid requests, Firestore rules/indexes, Cloud Functions, Firebase/Vercel deploys, production builds, browser/device QA, production-host smoke, launch approval, or release certification.

Temporary Status strict session document-ID boundary checkpoint: fixed in source. The authenticated `/api/store/temp-status` route keeps the same `withAuth()` admission, `ENABLE_TEMP_STATUS` feature gate, `MANAGE_STORE` / `MANAGE_PUBLIC_PRESENCE` permission guard, hashed `DATA_WRITE` limiter, 4KB bounded JSON body, Zod validation, store `tempStatus` update/delete, public menu/OBP/client-store/screen cache invalidation, Digital Screens content-version touch, Owner Business Assistant packet invalidation, and fixed owner-safe failure copy for valid set/clear requests, but whitespace-mutated and oversized session tenant/store IDs can no longer reach route-local limiter material, `stores/{storeId}` refs, cache tags, screen invalidation, assistant-cache invalidation, or diagnostics. The optional session actor ID is also normalized before limiter material or `tempStatus.createdBy` metadata is written, so malformed actor IDs become `unknown` limiter material and `null` metadata instead of raw store-document content. `src/app/api/store/temp-status/route.ts` now uses `TEMP_STATUS_SESSION_DOCUMENT_ID_MAX_LENGTH` and `normalizeSessionDocumentId()` with an exact raw-value check, 160-character cap, and shared Firestore document-ID guard. `npm run verify:temporary-status-boundary` and `npm run verify:menulist-api-tenant-safety` source-gate the strict normalizer, normalized actor limiter/write metadata, raw actor limiter exclusion, docs, audit evidence, and changelog evidence. This changes temp-status session/actor document-ID admission only; it does not change valid status set/clear behavior, valid public cache invalidation, valid screen invalidation, valid assistant-cache invalidation, Firestore read/write counts for valid requests, Firestore rules/indexes, Cloud Functions, Firebase/Vercel deploys, production builds, browser/device QA, live Firestore writes, launch approval, or release certification.

Platform Pull API key-management strict session document-ID boundary checkpoint: fixed in source. The authenticated `/api/store/public-api-key` route keeps the same `withAuth()` admission, `ENABLE_PUBLIC_API` feature gate, `MANAGE_INTEGRATIONS` permission guard, hashed store-scoped limiter, 1KB bounded JSON body, generate/revoke schema, one-time raw `ml_` key return, hashed key storage, key-prefix storage, public API revoke behavior, bounded diagnostics, and fixed owner failure copy for valid key actions, but whitespace-mutated and oversized session tenant/store IDs can no longer reach route-local limiter material, `stores/{storeId}` refs, or diagnostics. `src/app/api/store/public-api-key/route.ts` now uses `PUBLIC_API_KEY_SESSION_DOCUMENT_ID_MAX_LENGTH` and `normalizeSessionDocumentId()` with an exact raw-value check, 160-character cap, and shared Firestore document-ID guard before permission, limiter, body, or store-ref work. `npm run verify:platform-pull-api-boundary` and `npm run verify:menulist-api-tenant-safety` source-gate the strict normalizer, length guard, hashed limiter key, store-ref ordering, docs, audit evidence, and changelog evidence. This changes key-management session document-ID admission only; it does not change valid API-key generation/revocation, public pull API key validation, business/menu pull responses, Firestore read/write counts for valid requests, Firestore rules/indexes, Cloud Functions, Firebase/Vercel deploys, production builds, browser/device QA, live key fixture/manual testing, launch approval, or release certification.

Auth change-password strict user document-ID boundary checkpoint: fixed in source. The authenticated `/api/auth/change-password` route keeps the same `withAuth()` admission, hashed `AUTH_SENSITIVE` limiter, 2KB bounded body reader, current-password verification against Firebase Auth REST with manual redirect handling, password-provider check, Firebase Auth password update, bounded auth/security diagnostics, fixed account copy, and desktop/mobile response parsing for valid sessions, but whitespace-mutated and oversized session user IDs can no longer pass the shared Firestore document-ID guard and then reach limiter key material, Firebase password verification, Firebase Auth update, or the direct `users/{userId}` timestamp write as raw unnormalized IDs. `src/app/api/auth/change-password/route.ts` now uses `normalizeChangePasswordUserDocumentId()` with an exact raw-value check and a 160-character cap before rate limiting, body parsing, provider calls, or Firestore writes; malformed IDs still return generic unauthenticated copy and fixed `change_password_invalid_session_user_id` diagnostics. `npm run verify:auth-security-failure-matrix` and `npm run verify:menulist-api-tenant-safety` source-gate the strict normalizer, whitespace/length checks, normalized ref ordering, raw session-user coercion exclusion, Auth docs, audit evidence, and changelog evidence. This changes authenticated change-password session user-ID admission only; it does not change valid password changes, valid Firebase Auth UID-backed user docs, current-password verification behavior, Firebase Auth password updates, account UI behavior, Firestore rules/indexes, Cloud Functions, Firebase/Vercel deploys, production builds, browser/device QA, launch approval, or release certification.

Profile Update User Document ID Boundary checkpoint: fixed in source. The authenticated `/api/auth/update-profile` route keeps the same `withAuth()` admission, `DATA_WRITE` limiter, HMAC-hashed limiter key material, 4KB bounded body reader, whitelisted profile fields, phone normalization, user-document read/update, bounded auth diagnostics, fixed owner copy, and desktop/mobile account response parsing for valid sessions, but malformed, reserved, empty, whitespace-mutated, path-shaped, or oversized session user IDs can no longer reach limiter key material, bounded body parsing, or `users/{userId}` document refs. `src/lib/userProfile/server.ts` now validates the session user ID with `src/lib/firebase/firestoreDocumentId.ts` before the limiter and builds the user ref only from the normalized ID. `npm run verify:menulist-api-tenant-safety` source-gates the profile user-ID normalizer, normalized user ref, raw session-user coercion exclusion, Auth docs, audit evidence, and changelog evidence. This changes profile-update user document-ID admission only; it does not change valid profile updates, password changes, Firebase Auth behavior, Firestore read/write counts for valid updates, Firestore rules/indexes, Cloud Functions, Firebase/Vercel deploys, production builds, browser/device QA, launch approval, or release certification.

Batch Image Worker Rate-Limit Boundary checkpoint: fixed in source. The internal `/api/image-generation/batch-generation` Cloud Tasks worker keeps the same `project-id` header check, `BATCH_IMAGE_GENERATION_WORKER_SECRET` timing-safe admission, SAFE_MODE behavior, 16MB bounded body reader, `BatchImageGenerationWorkerRequestSchema` validation, strict batch project/job ID normalization, job existence/idempotency checks, project/job/requested-item checks, prompt-cache behavior, capacity check, provider call, Storage upload, AI accounting, transactional job-progress write, owner-safe persisted item errors, and bounded worker diagnostics for valid tasks, but repeated worker attempts now hit the shared `BATCH_IMAGE_WORKER` limiter before the worker reads `imageBatchProcessingJobs/{tId}/{sId}/{jobId}`, checks capacity, calls Gemini, uploads generated images, writes AI accounting, or updates batch progress. The limiter uses HMAC-hashed tenant/store scope with the stable `batch-image-worker` namespace, returns 429 with retry headers, and records bounded security diagnostics only. The 600-per-minute per-store ceiling is intentionally generous because valid owner-triggered batches can enqueue up to 50 item tasks and Cloud Tasks retries need room; it bounds retry storms and worker-secret abuse without changing normal batch completion. `npm run verify:ai-accounting` and `npm run verify:menulist-api-tenant-safety` source-gate the limiter profile, imports, hashed key material, limiter-before-job-read/provider ordering, raw key exclusions, AI image docs, audit evidence, and changelog evidence. This changes batch worker admission only; it does not change valid batch trigger admission, valid worker secret behavior, valid Cloud Tasks enqueue/execution, valid prompt-cache behavior, valid AI accounting, valid Storage uploads, Firestore read/write counts for valid tasks, Firestore rules/indexes, Cloud Functions, Firebase deployment, Vercel deployment, production builds, browser/device QA, provider smoke, launch approval, or release certification.

Menu Cache Revalidation Rate-Limit Boundary checkpoint: fixed in source. The shared `/api/revalidate/menu` route keeps the same `x-revalidate-secret` server-caller admission, authenticated app-caller admission, 4KB bounded body reader, numeric `storeId` shorthand, explicit valid tag allowlist, platform-only explicit tag behavior for authenticated sessions, `menu-store-{storeId}`, `store-{storeId}`, `client-stores`, and `screen-data` invalidation, single-store explicit-tag assistant-cache derivation, Owner Business Assistant packet invalidation, and bounded failure diagnostics for valid public-cache invalidation calls, but repeated cache-revalidation attempts now hit the shared `MENU_CACHE_REVALIDATION` limiter before body parsing, tag validation, `revalidateTag()` calls, or assistant packet invalidation. The limiter uses HMAC-hashed session actor material for authenticated app callers and HMAC-hashed caller-source material for validated secret callers with the stable `menu-cache-revalidate` namespace, returns 429 with retry headers, and records bounded security diagnostics only. The 600-per-minute source ceiling is intentionally generous because owner saves and Cloud Function writes may legitimately burst cache invalidations, while still bounding runaway loops and leaked-secret churn. `npm run verify:public-business-truth` and `npm run verify:menulist-api-tenant-safety` source-gate the limiter profile, imports, hashed key material, limiter-before-body/cache-work ordering, raw key exclusions, Client Menu Firebase docs, audit evidence, and changelog evidence. This changes menu-cache revalidation admission only; it does not change valid public cache tags, valid store-access checks, valid explicit-tag handling, valid screen-data invalidation, valid assistant-cache invalidation, Firestore read/write counts, Firestore rules/indexes, Cloud Functions, Firebase deployment, Vercel deployment, production builds, browser/device QA, live Firestore writes, launch approval, or release certification.

Admin Subdomain Rename Rate-Limit and Scope Boundary checkpoint: fixed in source. The platform-only `/api/admin/subdomains/rename` route keeps the same `requiredPlatformRole: 'PLATFORM'` admission, 8KB bounded body reader, subdomain slug validation, reserved-slug guard, direct and rename-chain collision checks, store update, `platformSummary/storesSummary` update, `subdomainRenameLog` audit write, public menu/OBP/screen cache invalidation, Owner Business Assistant packet invalidation, and bounded success/failure diagnostics for valid legal/support rename actions, but repeated attempts now hit the shared `ADMIN_SUBDOMAIN_RENAME_MUTATION` limiter before body parsing and malformed tenant/store scope can no longer reach `stores/{storeId}` reads or writes. The limiter uses HMAC-hashed platform-operator key material with the stable `admin-subdomain-rename` namespace, returns 429 with retry headers, and records bounded security diagnostics only. The route now validates tenant/store IDs as exact positive Firestore document IDs and builds store refs through `db.collection(DB_COLLECTIONS.STORES).doc(storeIdStr)` instead of a raw path string. `npm run verify:menulist-api-tenant-safety` source-gates the limiter profile, imports, hashed key material, limiter-before-body ordering, scope normalization before store/collision reads, raw key/ref exclusions, URL Routing docs, audit evidence, and changelog evidence. This changes admin subdomain rename admission only; it does not change valid rename behavior, valid collision semantics, valid redirect history, valid public cache invalidation, Firestore read/write counts for valid requests, Firestore rules/indexes, Cloud Functions, Firebase deployment, Vercel deployment, production builds, browser/device QA, live Firestore writes, launch approval, or release certification.

Batch Image Project/Job ID Boundary checkpoint: fixed in source. The owner batch image trigger and Cloud Tasks worker keep the same `withAuth()` admission, worker project-header/secret gate, SAFE_MODE behavior, `BATCH_OPERATION` trigger limiter, 16MB bounded request readers, role permission gate, linked-outlet policy checks, Cloud Tasks config preflight, capacity/accounting order, prompt-cache behavior, provider calls, Storage uploads, image-batch job progress writes, client listener behavior, and owner review/result flows for valid requests, but malformed batch project IDs and job IDs can no longer reach `imageBatchProcessingJobs/{tId}/{sId}/{jobId}` ref composition. `src/lib/ai/imageBatchIdBoundary.ts` now validates project IDs as simple Firestore document IDs with exact positive numeric tenant/store scope in the existing first/third segments, validates batch job IDs as Firestore auto-ID shaped IDs, and is used by `/api/image-generation/batch-trigger`, `/api/image-generation/batch-generation`, `src/database/imageBatchProcessing/index.tsx`, and `src/database/imageBatchProcessing/server.ts` before Firestore job reads/writes, capacity/accounting work, provider calls, or Storage uploads. `npm run verify:ai-accounting` source-gates the helper, schemas, trigger, worker, client DAL, server DAL, AI image docs, AI System docs, audit evidence, and changelog evidence. This changes batch image request/job ID admission only; it does not change valid batch image generation behavior, valid Cloud Tasks enqueue/worker execution, valid AI accounting, valid prompt-cache behavior, valid generated-image Storage writes, Firestore read/write counts for valid requests, Firestore rules/indexes, Cloud Functions, Firebase deployment, Vercel deployment, production builds, browser/device QA, provider smoke, launch approval, or release certification.

Platform Entity Blocks Rate-Limit Boundary checkpoint: fixed in source. The platform-only `/api/platform/entity-blocks` route keeps the same `withPlatformAuth()` admission, feature flag, 64KB bounded body reader, strict tenant/store/user target normalization, entity writes, tenant-to-store block mirroring, Firebase Auth disable/token-revoke handling, public menu/OBP/screen cache invalidation, Business Health packet invalidation, desktop/mobile acknowledgement behavior, and fixed platform copy for valid platform-admin actions, but repeated platform entity-block attempts now hit the shared `PLATFORM_ENTITY_BLOCK_MUTATION` limiter before body parsing, Zod validation, entity reads, Firestore writes, Firebase Auth work, cache invalidation, screen wakeups, or Owner Business Assistant packet invalidation. The limiter uses HMAC-hashed platform-operator key material with the stable `platform-entity-block` namespace, returns 429 with retry headers, and records bounded security diagnostics only. `npm run verify:platform-entity-blocks-boundary` and `npm run verify:menulist-api-tenant-safety` source-gate the limiter profile, imports, hashed key material, limiter-before-body/read/write ordering, raw operator-key exclusions, Ops docs, audit evidence, and changelog evidence. This changes platform entity-block mutation admission only; it does not change valid tenant/store/user block behavior, valid Firebase Auth sync, valid public cache invalidation, Firestore read/write counts for valid requests, Firestore rules/indexes, Cloud Functions, Firebase deployment, Vercel deployment, production builds, browser/device QA, live Firestore writes, Firebase Auth provider smoke, launch approval, or release certification.

Razorpay payment verification rate-limit boundary checkpoint: fixed in source. The authenticated `/api/razorpay/verify-subscription` and `/api/razorpay/verify-topup` routes keep the same `withAuth()` admission, 8KB bounded body readers, Zod validation, checkout HMAC signature checks, Razorpay provider truth checks, payment capture/top-up idempotency behavior, tenant/store billing authorization, subscription/top-up document-ID guards, entitlement sync, Founder Monitor writes, and non-blocking lifecycle/internal notification behavior for valid checkout completions, but repeated verification attempts now hit the shared `PAYMENT_VERIFICATION` limiter before body parsing, signature checks, provider payment/order/subscription fetches, payment capture, subscription/top-up reads, or billing writes. The limiter uses HMAC-hashed authenticated user key material with route-specific `payment-verify:subscription` and `payment-verify:topup` namespaces, returns 429 with retry headers, and records bounded Razorpay security diagnostics only. `npm run verify:billing-entitlement-boundary` and `npm run verify:menulist-api-tenant-safety` source-gate the limiter profile, imports, hashed key material, limiter-before-body/provider ordering, raw user-key exclusions, Razorpay docs, audit evidence, and changelog evidence. This changes payment verification admission only; it does not change valid checkout verification, valid provider signatures, valid payment capture, billing document shapes, Firestore rules/indexes, Cloud Functions, Firebase/Vercel deploys, production builds, browser/device QA, Razorpay sandbox smoke, provider configuration, launch approval, or release certification.

Set-claims rate-limit boundary checkpoint: fixed in source. The authenticated `/api/auth/set-claims` route keeps the same `withAuth()` admission, 2KB optional bounded body, product-specific auth profile lookup, target-store membership guard, workspace-scope normalization, UID/email match check, Firebase custom-claim sync, MenuList Firebase custom-token creation, and Answerlattice custom-token sync for valid sessions, but repeated sync attempts now hit the shared `AUTH_CLAIM_SYNC` limiter before optional body parsing, profile lookups, Firebase Auth user reads/creates, custom-claim writes, or custom-token creation. The 30-per-15-minute actor ceiling is intentionally separate from stricter account-mutation limits because valid login handoff, session bootstrap, store switching, and multi-tab refreshes can legitimately make several set-claims calls. The limiter key uses HMAC-hashed session user/email material through `hashPublicRateLimitValue()` and returns 429 with retry headers plus bounded security diagnostics only. `npm run verify:menulist-api-tenant-safety` and `npm run verify:auth-security-failure-matrix` source-gate the limiter imports, stable `auth-set-claims` namespace, `AUTH_CLAIM_SYNC` profile use, hashed key material, rate-limit-before-body/user-read ordering, raw user/email key exclusions, Auth docs, audit evidence, and changelog evidence. This changes set-claims admission only; it does not change valid Google login, credential login, phone OTP login, MenuList Firebase sync, Answerlattice Firebase sync, store-switch claim refresh, custom claim values, Firestore rules/indexes, Cloud Functions, Firebase/Vercel deploys, production builds, browser/device QA, provider smoke, launch approval, or release certification.

AI Operations History Scope Document ID Boundary checkpoint: fixed in source. The protected `/api/ai-operations` route keeps the same `withAuth()` admission, DATA_READ limiter, owner/platform allowlists, cursor/date filters, capped pagination, and bounded diagnostics for valid transaction-history requests, but malformed, reserved, empty, whitespace-mutated, path-shaped, decimal, zero, negative, unsafe, or nonnumeric tenant/store scope can no longer reach limiter key material, cursor reads, or `menulistAiOperations/{tId}/{sId}` queries. `src/app/api/ai-operations/route.ts` now exposes `normalizeAiOperationHistoryScopeDocumentId()`, validates tenant/store transaction-history scope with `src/lib/firebase/firestoreDocumentId.ts` plus exact positive numeric admission, and builds history refs from normalized document IDs instead of raw `doc(String(tenantId))` / `collection(String(storeId))` scope. `npm run verify:ai-accounting` and `npm run verify:menulist-api-tenant-safety` source-gate the scope normalizer, exact numeric guard, normalized refs, raw ref exclusions, AI System docs, audit evidence, and changelog evidence. This changes app-side AI operations history scope admission only; it does not change valid transaction-history read counts, AI accounting writes, Firestore rules/indexes, Cloud Functions, Firebase/Vercel deploys, production builds, browser/device QA, provider smoke, launch approval, or release certification.

Auth entity snapshot document ID boundary checkpoint: fixed in source. The NextAuth inherited entity-block lookup keeps the same user lookup, tenant block inheritance, store block inheritance, resolved product-id fallback, and silent no-block behavior for valid login contexts, but malformed, reserved, empty, whitespace-mutated, path-shaped, decimal, zero, negative, unsafe, or nonnumeric tenant/store IDs can no longer reach `tenants/{tenantId}` or `stores/{storeId}` entity-block reads during auth session construction. `src/lib/auth/serverUserContext.ts` now validates generic entity IDs with `src/lib/firebase/firestoreDocumentId.ts`, reuses `normalizeStorePermissionScopeDocumentId()` for tenant/store scope IDs, and reads only the normalized `scopeDocumentId`. `npm run verify:auth-security-failure-matrix` source-gates the shared guard imports, numeric tenant/store scope guard, normalized entity read, raw `.doc(String(id))` exclusion, auth docs, audit evidence, and changelog evidence. This changes auth entity-block context admission only; it does not change valid login behavior, valid inherited tenant/store block semantics, Firebase Auth provider behavior, Firestore read/write counts for valid logins, Firestore rules/indexes, Cloud Functions, Firebase/Vercel deploys, production builds, browser/device QA, provider smoke, launch approval, or release certification.

Public client store lookup scope document ID boundary checkpoint: fixed in source. The shared public store resolver keeps the same subdomain, verified custom-domain, outlet-slug, public store-ID, denormalized `tenantBlocked`, legacy tenant-block fallback, 60-second `client-stores` cache, and active/deleted/platform-blocked public-output behavior for valid records, but malformed, reserved, empty, whitespace-mutated, path-shaped, decimal, zero, negative, unsafe, or nonnumeric direct store IDs and tenant-block fallback tenant IDs can no longer reach `stores/{storeId}` or `tenants/{tenantId}` refs. `src/lib/firestore/clientStoreLookup.ts` now validates public lookup scope with `src/lib/firebase/firestoreDocumentId.ts` plus exact positive numeric admission, uses normalized document IDs for direct public store and legacy tenant fallback reads, and fails closed as blocked output when a returned store record carries malformed tenant scope. `npm run verify:public-business-truth` source-gates the normalizer, exact numeric guard, normalized refs, raw tenant/store ref exclusions, OBP/client-menu/customer-app Firebase docs, audit evidence, and changelog evidence. This changes public store lookup document-ID admission only; it does not change valid public menu/OBP/compliance/customer-app lookup behavior, Firestore read counts for valid public requests, cache tags, cache invalidation, Firestore rules/indexes, Cloud Functions, Firebase/Vercel deploys, production builds, browser/device QA, live Firestore writes, provider smoke, launch approval, or release certification.

MenuList Billing Subscription Scope Document ID Boundary checkpoint: fixed in source. The server subscription DAL keeps the same active/past-due/cancelled/paused query, pending/paused fallback query, grace-period expiry behavior, entitlement sync side effect, outlet-to-master subscription fallback, and null-on-no-subscription behavior for valid tenant/store scope, but malformed, reserved, empty, whitespace-mutated, path-shaped, decimal, zero, negative, unsafe, or nonnumeric tenant/store scope can no longer reach subscription queries or the fallback `tenants/{tenantId}` read. `src/database/subscriptions/server.ts` now exposes `normalizeBillingSubscriptionScopeDocumentId()`, validates scope with `src/lib/firebase/firestoreDocumentId.ts` plus exact positive numeric admission, uses normalized numeric scope for subscription queries, and uses the normalized tenant document ID for fallback tenant reads. `npm run verify:billing-entitlement-boundary` source-gates the helper, exact numeric guard, normalized query values, normalized fallback tenant ref, raw `.doc(String(tenantId))` exclusion, Razorpay docs, audit evidence, and changelog evidence. This changes app-side billing subscription scope admission only; it does not change valid subscription read/write counts, valid outlet subscription inheritance, valid grace-period expiry writes, entitlement/cache sync behavior, Firestore rules/indexes, Cloud Functions, Firebase/Vercel deploys, production builds, browser/device QA, Razorpay sandbox smoke, launch approval, or release certification.

AI Menu Manager Scope Document ID Boundary checkpoint: fixed in source. The protected AMM fallback routes keep the same feature flag, `withAuth()` admission, selected-store authorization, `MANAGE_MENU` permission checks, hashed AMM limiter keys, bounded request readers, route/session/proposal/project ID admission, proposal tenant/store/project/action checks, stale-card checks, compact session/proposal writes, and fixed owner errors for valid requests, but malformed, reserved, whitespace-mutated, path-shaped, decimal, zero, negative, unsafe, or nonnumeric tenant/store scope values can no longer reach selected-store authorization, tenant access fallback checks, limiter scope material, scoped project refs, session/proposal scope comparisons, or AMM proposal/session writes. `src/lib/ai-menu-manager/routeIds.ts` now exposes `normalizeAiMenuManagerScopeDocumentId()`, requires exact positive safe-integer MenuList document IDs, and rejects whitespace-mutated AMM session/proposal/project IDs. `src/lib/ai-menu-manager/apiGuards.ts` normalizes session and selected-store scope before access checks and rate-limit scope material. `src/database/aiMenuManager/server.ts` normalizes tenant/store scope before scoped project refs, legacy project scope checks, inbox scope checks, and proposal mutation scope checks, excluding the raw `collection(\`${DB_COLLECTIONS.PROJECTS}/${params.tId}/${params.sId}\`)` path. `npm run verify:ai-menu-manager` and `npm run verify:menulist-api-tenant-safety` source-gate the AMM scope normalizer, no-whitespace-mutation rule, exact numeric guard, selected-store authorization, normalized tenant access fallback, normalized scoped project refs, raw scoped-path exclusion, implementation/Firebase docs, audit evidence, and changelog evidence. This changes protected AMM fallback tenant/store scope admission only; it does not change valid deterministic AMM session/proposal IDs, valid selected project IDs, valid numeric MenuList tenant/store scope, client-DAL compact session behavior, valid fallback route behavior, valid proposal approvals/completions, Firestore read/write counts for valid requests, Firestore rules/indexes, Cloud Functions, Firebase deployment, Vercel deployment, production builds, browser/device QA, provider smoke, production-host smoke, launch approval, or release certification.

Menu Intake Identity Scope Document ID Boundary checkpoint: fixed in source. The protected menu-intake identity preflight route keeps the same `withAuth()` admission, SAFE_MODE check, authenticated session scope, hashed `AI_OPERATION` limiter, 64KB bounded body reader, project-ID normalization, Storage URL allowlists, identity analysis, operation accounting metadata, non-blocking preflight fallback behavior, and fixed owner copy for valid preflight requests, but malformed, reserved, whitespace-mutated, path-shaped, decimal, zero, negative, unsafe, or nonnumeric tenant/store scope values can no longer reach tenant access checks, limiter hashes, `projects/{tId}/{sId}/{projectId}` context reads, `stores/{sId}` context reads, Storage prefix checks, operation metadata, or provider work. `src/lib/menu-extraction/menuIntakeIdentityServer.ts` now exposes `normalizeMenuIntakeScopeDocumentId()`, requires exact positive safe-integer MenuList document IDs, uses normalized tenant/store document IDs for the scoped project/store refs, and rejects invalid helper inputs through the existing `Invalid menu.` boundary. `src/app/api/menu-intake-identity/route.ts` uses the same normalized document IDs before tenant access and limiter hashing. `npm run verify:menu-extraction-pipeline` and `npm run verify:menulist-api-tenant-safety` source-gate the route/helper normalizer, no-whitespace-mutation rule, exact numeric tenant/store guard, normalized tenant access, normalized limiter hashes, normalized project/store refs, raw scope usage exclusions, implementation/Firebase docs, audit evidence, and changelog evidence. This changes app-side menu-intake identity tenant/store scope admission only; it does not change valid owner upload extraction, valid identity preflight, valid project IDs, valid provider analysis, Firestore read/write counts for valid requests, Storage operations, Firestore rules/indexes, Cloud Functions, Firebase deployment, Vercel deployment, production builds, browser/device QA, provider smoke, production-host smoke, launch approval, or release certification.

Platform Entity Blocks Strict Target Document ID Boundary checkpoint: fixed in source. The platform-only entity block route keeps the same feature flag, `withPlatformAuth()` admission, 64KB bounded body reader, tenant/store/user block writes, tenant-to-store block mirroring, Firebase Auth disable/token-revoke handling, public cache invalidation, Business Health packet invalidation, desktop/mobile acknowledgement behavior, and fixed platform copy for valid platform-admin actions, but malformed, reserved, empty, whitespace-mutated, path-shaped, decimal, zero, negative, unsafe, or nonnumeric tenant/store targets can no longer reach tenant/store refs, tenant-to-store mirror refs, store-summary fanout, or public-cache invalidation, and malformed, reserved, empty, whitespace-mutated, or path-shaped user targets can no longer reach user refs. `src/app/api/platform/entity-blocks/route.ts` now normalizes entity-block targets through `normalizePlatformEntityBlockTargetDocumentId()`, requires exact positive safe-integer document IDs for tenant/store targets, keeps strict simple Firestore document IDs for user targets, uses normalized entity document IDs for `getEntityDocRef()`, uses normalized store document IDs for tenant block mirror writes, and uses normalized store/tenant document IDs for entity-block cache invalidation. `npm run verify:platform-entity-blocks-boundary` and `npm run verify:menulist-api-tenant-safety` source-gate the strict target normalizer, no-whitespace-mutation rule, exact numeric tenant/store guard, normalized entity/store refs, raw `getEntityDocRef(db, entityType, entityId)` exclusion, raw `doc(String(storeId))` mirror exclusion, Ops docs, audit evidence, and changelog evidence. This changes app-side platform entity-block target admission only; it does not change valid tenant/store/user block behavior, valid Firebase Auth sync, valid public cache invalidation, Firestore read/write counts for valid requests, Firestore rules/indexes, Cloud Functions, Firebase deployment, Vercel deployment, production builds, browser/device QA, live Firestore writes, Firebase Auth provider smoke, launch approval, or release certification.

Owner notification recipient scope document-ID boundary checkpoint: fixed in source. The app-side owner notification recipient resolver keeps the same Answerlattice workspace preference read, MenuList canonical top-level store lookup, legacy tenant-scoped store fallback, manual recipient override behavior, WhatsApp consent handling, delivery-channel selection, event status updates, delivery rows, and valid 0-1 recipient scope read behavior, but malformed, reserved, empty, whitespace-mutated, or path-shaped Answerlattice workspace/store IDs can no longer reach Answerlattice `stores/{workspaceId}` reads, and malformed, reserved, empty, whitespace-mutated, path-shaped, decimal, zero, negative, unsafe, or nonnumeric MenuList tenant/store IDs can no longer reach MenuList `stores/{storeId}` or legacy `tenants/{tenantId}/stores/{storeId}` reads before delivery processing. `src/lib/owner-notifications/recipientResolver.ts` now exposes `normalizeOwnerNotificationRecipientDocumentId()` for simple recipient document IDs and `normalizeMenuListOwnerNotificationScopeDocumentId()` for exact positive numeric MenuList tenant/store scope, uses normalized document IDs for all app-side recipient scope refs, and rejects top-level MenuList store documents whose tenant does not match the normalized tenant scope. `npm run verify:owner-notifications-boundary` source-gates the app-side recipient-scope normalizers, normalized Answerlattice/MenuList refs, raw `String(event.storeId)`/`String(event.tenantId)` ref exclusions, Owner Notifications docs, audit evidence, and changelog evidence. Follow-up gates passed after this checkpoint patch: `node --check scripts/verification/verify-owner-notifications-boundary.js`, `npm run verify:owner-notifications-boundary`, `npx tsc --noEmit --incremental false --pretty false --noErrorTruncation`, `npm run verify:answerlattice-runtime-truth`, `npm run verify:menulist-api-tenant-safety`, `npm run docs:check-links`, and `git diff --check`. This changes app-side owner-notification recipient scope admission only; it does not change Functions-side owner-notification processor logic, valid delivery behavior, delivery logs, Firestore read/write counts for valid events, Firestore rules/indexes, Cloud Functions, Firebase deployment, Vercel deployment, SMTP/WhatsApp provider smoke, browser/device QA, production-host smoke, launch approval, or release certification.

Store Permission Scope Document ID Boundary checkpoint: fixed in source. The shared MenuList store-permission helper keeps the same platform bypass, one store authorization read, tenant ownership check, inactive/deleted/platform-blocked store rejection, role resolution, permission matching, bounded security diagnostics, and valid owner/staff API behavior, but malformed, reserved, empty, whitespace-mutated, path-shaped, decimal, zero, negative, unsafe, or nonnumeric session and explicit tenant/store IDs can no longer reach the shared `stores/{storeId}` permission read before route-specific writes or provider calls. `src/lib/permissions/server.ts` now exposes `normalizeStorePermissionScopeDocumentId()`, validates session and explicit scope before store permission refs, uses the normalized store document ID for `stores/{storeId}`, and compares store tenant ownership to the normalized numeric tenant ID. `npm run verify:menulist-api-tenant-safety` source-gates the helper, exact numeric document-ID guard, normalized store ref, raw `doc(String(storeId))` exclusion, Roles & Permissions docs, audit evidence, and changelog evidence. This changes app-side shared store-permission scope admission only; it does not change valid permission behavior, valid route writes/provider calls, Firestore read/write counts for valid requests, Firestore rules/indexes, Cloud Functions, Firebase/Vercel deploys, production builds, browser/device QA, live Firestore writes, provider smoke, launch approval, or release certification.

Public create-menu claim target document-ID boundary checkpoint: fixed in source. `POST /api/public/create-menu/claim` keeps the same `withAuth()` admission, public-menu-entry feature gate, hashed owner limiter, 8KB bounded claim body, draft ownership/expiry/completion checks, existing-account store/tenant eligibility reads, new-account tenant/store creation, default-project demotion, project creation, project-summary write, draft claim marker, public cache invalidation, Digital Screens invalidation, Owner Business Assistant cache invalidation, and bounded diagnostics for valid claims, but malformed, reserved, empty, whitespace-mutated, path-shaped, zero, negative, or nonnumeric tenant/store IDs can no longer reach `stores/{storeId}`, `tenants/{tenantId}`, `platformSummary/projects_{storeId}`, or `projects/{tId}/{sId}/{projectId}` refs. `src/app/api/public/create-menu/claim/route.ts` validates existing-account and newly-created tenant/store IDs with `src/lib/firebase/firestoreDocumentId.ts` and an exact positive numeric MenuList ID guard before building those refs. `npm run verify:menu-extraction-pipeline`, `npm run verify:public-business-truth`, and `npm run verify:menulist-api-tenant-safety` source-gate the claim target document-ID boundary, raw `doc(String(tenantId))` / `doc(String(storeId))` exclusions, raw project collection path exclusion, docs, audit evidence, and changelog evidence. This changes app-side public create-menu claim document-ID admission only; it does not change valid upload/link extraction, valid claim/publish behavior, valid project IDs, valid default-project demotion, response schema, public cache invalidation for valid claims, Firestore rules/indexes, Cloud Functions, Firebase/Vercel deploys, production builds, browser/device QA, live Firestore writes, provider smoke, launch approval, or release certification.

Platform Pull API target document-ID boundary checkpoint: fixed in source. `/api/public/v1/business` and `/api/public/v1/menu` keep the same `ml_` key admission, live key lookup, hashed rate-limit key, target eligibility check, private response headers, ETag/304 behavior, bounded diagnostics, active temporary-status output, menu summary selection, and public response schema for valid requests, but malformed, reserved, empty, whitespace-mutated, path-shaped, or nonnumeric stored target IDs can no longer reach public response ID construction or menu payload ID construction. `src/lib/publicApi/auth.ts` now normalizes credential store document IDs before returning validation results and requires exact positive numeric MenuList tenant IDs before tenant eligibility refs. The menu route normalizes tenant/store/project document IDs before `platformSummary/projects_{storeId}` and `projects/{tId}/{sId}/{projectId}` refs. `npm run verify:platform-pull-api-boundary`, `npm run verify:public-business-truth`, and `npm run verify:menulist-api-tenant-safety` source-gate the target document-ID boundary, MenuList numeric-ID admission, raw `Number(storeId)` coercion exclusion, raw `doc(String(tenantId))` exclusion, docs, audit evidence, and changelog evidence. This changes app-side public pull target-ID admission only; it does not change valid API-key generation/revocation, valid business/menu response schemas, valid key hash format, ETag behavior, private response-cache headers, Firestore rules/indexes, Cloud Functions, Firebase/Vercel deploys, production builds, browser/device QA, live Firestore writes, live pull-key fixture smoke, provider smoke, launch approval, or release certification.

Outlet Lifecycle Session Document ID Boundary checkpoint: fixed in source. The authenticated `/api/outlets/create`, `/api/outlets/rename`, `/api/outlets/deactivate`, and `/api/outlets/policy` routes keep the same feature gates, tenant-access admission, `MANAGE_OUTLETS` permission checks, bounded request bodies, tenant-scoped limiter behavior, master/outlet validations, billing quantity behavior, public cache tags, Digital Screens invalidation, Owner Business Assistant packet-cache invalidation, and bounded diagnostics for valid outlet mutations, but malformed, reserved, empty, whitespace-mutated, or path-shaped session tenant/store IDs can no longer reach tenant access checks, limiter keys, route diagnostics, Firestore document paths, cache tags, screen invalidation, or assistant-cache invalidation. `src/lib/multiOutlet/outletSessionScope.ts` validates session tenant/store IDs with the shared Firestore document-ID guard and preserves the original numeric/string session values for existing numeric comparisons, equality filters, and billing logic. Rename and deactivate also validate the body-provided target outlet store ID before target outlet refs/cache work. `npm run verify:multi-location-boundary` and `npm run verify:menulist-api-tenant-safety` source-gate the shared outlet session scope helper, target outlet body-ID normalization, raw session destructure exclusion, raw tenant limiter exclusion, docs, audit evidence, and changelog evidence. This changes app-side outlet lifecycle document-ID admission only; it does not change valid outlet create/rename/deactivate/policy behavior, valid billing quantity decisions, valid public cache invalidation, Firestore rules/indexes, Cloud Functions, Firebase/Vercel deploys, production builds, browser/device QA, Razorpay sandbox smoke, live Firestore writes, launch approval, or release certification.

Custom Domain session document-ID boundary checkpoint: fixed in source. The authenticated `/api/domain` POST/GET/DELETE route keeps the same `MANAGE_PUBLIC_PRESENCE` admission, DOMAIN_MANAGEMENT limiter, 4KB bounded add-domain body, normalized domain format, Vercel add/status/remove calls, duplicate-domain check, valid `stores/{storeId}` custom-domain writes, verification writes, removal writes, public menu cache invalidation, bounded diagnostics, and fixed owner/provider copy, but malformed, reserved, empty, whitespace-mutated, or path-shaped session tenant/store IDs can no longer reach permission checks, limiter keys, store reads/writes, Vercel-flow diagnostics, or `revalidateMenuCache`. `src/app/api/domain/route.ts` validates session tenant/store IDs with `src/lib/firebase/firestoreDocumentId.ts` before the permission helper and uses normalized IDs for every store ref and cache invalidation. `npm run verify:menulist-api-tenant-safety` source-gates the normalized scope helper, permission-before-provider order, raw session destructure exclusion, raw `doc(String(storeId))` exclusion, docs, audit evidence, and changelog evidence. This changes app-side custom-domain session document-ID admission only; it does not change valid domain add/status/remove behavior, Vercel provider calls for valid requests, DNS verification semantics, duplicate-domain checks, custom-domain routing, public cache invalidation for valid writes, Firestore rules/indexes, Cloud Functions, Firebase/Vercel deploys, production builds, browser/device QA, live provider smoke, live Firestore writes, launch approval, or release certification.

Compliance Pages session document-ID boundary checkpoint: fixed in source. The authenticated `/api/compliance` GET/POST route keeps the same feature flag, owner preview generation, missing-contact response, bounded store-read diagnostics, `MANAGE_PUBLIC_PRESENCE` / `MANAGE_STORE` save/reset admission, DATA_WRITE limiter, 32KB bounded body, Zod validation, sanitizer, shaped save/reset acknowledgement, and valid `compliancePages/{sId}` override writes, but malformed, reserved, empty, whitespace-mutated, or path-shaped session tenant/store IDs can no longer reach store lookup, override reads, permission checks, limiter keys, override writes, reset writes, or bounded diagnostics. `/api/compliance` validates session tenant/store IDs with the shared Firestore document-ID guard before compliance reads or writes, and `src/database/compliance/server.ts` now rejects malformed `compliancePages/{sId}` refs even if a future caller bypasses the route normalizer. `npm run verify:compliance-pages-boundary` and `npm run verify:menulist-api-tenant-safety` source-gate the route scope helper, server DAL document-ID guard, raw session destructure exclusion, raw `doc(String(sId))` exclusion, docs, audit evidence, and changelog evidence. This changes app-side owner compliance session document-ID admission only; it does not change valid compliance previews, generated policy text, custom override save/reset behavior, public compliance rendering, Firestore rules/indexes, Cloud Functions, Firebase/Vercel deploys, production builds, browser/device QA, owner/legal review, live Firestore writes, launch approval, or release certification.

Review States session document-ID boundary checkpoint: fixed in source. The dormant `GET /api/reviews/states` route keeps the same `ENABLE_REVIEWS_REPUTATION` disabled-by-default guard, authenticated admission, boolean-only response shape, review-state limiter, two capped `reviewsState` queries, non-expired `autoExpiresAt` filter, and bounded runtime diagnostics, but malformed, reserved, empty, whitespace-mutated, or path-shaped session tenant/store IDs can no longer reach limiter keys, query filters, or diagnostics when the feature is intentionally enabled. `GET /api/reviews/states` validates session tenant/store IDs with the shared Firestore document-ID guard, uses normalized document-ID strings only for hashed limiter key material, and preserves the original numeric/string session values for the `reviewsState` equality filters so existing typed review-state documents keep the same valid query behavior. `npm run verify:menulist-api-tenant-safety` source-gates the review-state session scope helper, query-value preservation, raw session destructure exclusion, docs, audit evidence, and changelog evidence. This changes app-side review-state session document-ID admission only; it does not enable Reviews/Reputation, change valid review-state query semantics, change review-state writes, add public surfaces, change AI reply behavior, change Firestore rules/indexes, run Cloud Functions, run Firebase/Vercel deploys, run production builds, run browser/device QA, write live Firestore data, approve GBP access, approve launch, or certify release.

Messaging Onboarding Health Snapshot Document ID Boundary checkpoint: fixed in source. The platform-only Messaging Onboarding monitor keeps the same feature gate, platform-role admission, DATA_READ limiter, control-doc read, bounded Admin SDK read windows, masked event/session rows, sanitized metadata, 256KB browser response parsing, desktop/mobile route mapping, and fixed platform copy for valid health snapshots, but malformed, reserved, empty, or path-shaped `systemHealth/messaging_onboarding_control.lastSnapshotId` values can no longer reach `systemHealth/{lastSnapshotId}` document refs. `src/app/api/ops/messaging-onboarding/route.ts` validates the stored latest snapshot ID with `src/lib/firebase/firestoreDocumentId.ts` before the follow-up health read, and falls back to the existing unknown-health response when the stored ID is invalid. `npm run verify:messaging-onboarding-monitor-boundary` source-gates the route guard, raw stored-ID fallback exclusion, Ops docs, Messaging Onboarding Firebase docs, audit evidence, and changelog evidence. This changes app-side ops health snapshot document-ID admission only; it does not change valid health snapshot writes, valid monitor reads, count queries, scheduler logic, Firestore rules/indexes, Cloud Functions, Vercel deployment, production builds, browser/device QA, provider smoke, live Firestore writes, production-host smoke, launch approval, or release certification.

Owner store session document-ID boundary checkpoint: fixed in source for the active Public API key-management and Temporary Status owner mutation routes. `POST /api/store/public-api-key` keeps the same feature gate, `MANAGE_INTEGRATIONS` admission, hashed store limiter, 1KB bounded body, one-time `ml_` key return, hashed key storage, bounded diagnostics, and valid `stores/{storeId}.publicApi` writes, but malformed, reserved, empty, whitespace-mutated, or path-shaped session tenant/store IDs can no longer reach permission checks, limiter keys, diagnostics, or store document refs. `POST /api/store/temp-status` keeps the same feature gate, `MANAGE_STORE` / `MANAGE_PUBLIC_PRESENCE` admission, DATA_WRITE limiter, 4KB bounded body, future-expiry validation, valid `tempStatus` writes/clears, public cache tags, Digital Screens content-version touch, Owner Business Assistant packet-cache invalidation, bounded diagnostics, and fixed owner copy, but malformed session tenant/store IDs can no longer reach permission checks, limiter keys, store writes, cache tags, Digital Screens invalidation, Owner Business Assistant cache invalidation, or diagnostics. `npm run verify:platform-pull-api-boundary`, `npm run verify:temporary-status-boundary`, and `npm run verify:menulist-api-tenant-safety` source-gate the shared Firestore document-ID guard usage, normalized route refs/order, raw `doc(String(storeId))` exclusions, docs, audit evidence, and changelog evidence. This changes app-side session store/tenant document-ID admission only; it does not change valid API-key generation/revocation, valid temporary-status set/clear behavior, public pull response schema, key hash format, public cache invalidation for valid writes, Firestore rules/indexes, Cloud Functions, Firebase/Vercel deploys, production builds, browser/device QA, live Firestore writes, provider smoke, launch approval, or release certification.

Billing Mutation Scope Document ID Boundary checkpoint: fixed in source. The shared MenuList billing mutation authorization helper keeps the same platform bypass, session role resolution, one store authorization read, tenant ownership check, inactive/deleted/platform-blocked store rejection, owner fallback, `canManageSubscription` permission semantics, and bounded Razorpay diagnostics for valid billing sessions, but malformed, reserved, empty, whitespace-mutated, path-shaped, decimal, zero, negative, unsafe, or nonnumeric session tenant/store IDs can no longer reach the shared `stores/{storeId}` authorization read before subscription, top-up, cancellation, pause, resume, or upgrade provider work. `src/lib/billing/billingAccess.ts` now exposes `normalizeBillingMutationScopeDocumentId()`, validates session tenant/store scope before the store read, uses the normalized store document ID for `stores/{storeId}`, and compares the store tenant to the normalized numeric tenant ID. `npm run verify:menulist-api-tenant-safety` source-gates the helper, exact numeric document-ID guard, normalized store ref, raw `doc(String(storeId))` exclusion, Razorpay docs, audit evidence, and changelog evidence. This changes app-side shared billing mutation scope admission only; it does not change valid billing permission behavior, valid Razorpay provider calls, valid subscription/top-up/cancel/pause/resume/upgrade writes, Firestore read/write counts for valid requests, Firestore rules/indexes, Cloud Functions, Firebase/Vercel deploys, production builds, browser/device QA, Razorpay sandbox smoke, launch approval, or release certification.

Platform Founder Monitor Revenue Document ID Boundary checkpoint: fixed in source. Founder revenue movement writes, live revenue summary increments, daily revenue summary increments, new-MRR/churn/expansion/downgrade/refund/cash movement semantics, and payment-to-live transition seeding keep the same behavior for valid Razorpay payment/order/subscription-driven IDs and valid store IDs, but malformed, reserved, empty, or path-shaped movement IDs and transition store IDs can no longer reach `founderRevenueMovements/{movementId}` or `founderOnboardingTransitions/{storeId}` document refs. `src/lib/ops/founderRevenueReadModel.ts` validates deterministic movement IDs and transition store IDs with `src/lib/firebase/firestoreDocumentId.ts` before Admin Firestore refs. `npm run verify:platform-founder-monitor-boundary` source-gates the guard, raw-helper regressions, Founder Monitor docs, audit evidence, and changelog evidence. This changes app-side Founder Monitor revenue document-ID admission only; it does not change valid Razorpay runtime hooks, valid revenue movement payloads, valid summary increments, scheduler reconciliation, Firestore rules/indexes, Cloud Functions, Vercel deployment, production builds, browser/device QA, provider smoke, live Firestore writes, production-host smoke, launch approval, or release certification.

MenuList Top-Up Scope Document ID Boundary checkpoint: fixed in source. MenuList top-up order creation, top-up verification, provider-note tenant/store comparison, active-subscription lookup, pending audit writes, paid audit writes, duplicate-verification handling, Founder Monitor cash movement, lifecycle/internal notification attempts, Answerlattice store-summary mirror refs, and subscription credit increment behavior keep the same behavior for valid numeric billing tenant/store scope, but malformed, reserved, empty, whitespace-mutated, decimal, zero, negative, unsafe, nonnumeric, or path-shaped scope IDs can no longer reach top-up provider work, Firestore store refs, or top-up writes. `src/lib/billing/topupDocumentIdBoundary.ts` exposes `normalizeBillingTopupScopeDocumentId()`, `src/app/api/razorpay/create-topup-order/route.ts` normalizes resolved billing scope before top-up rate limits, provider order creation, or pending writes, and `src/app/api/razorpay/verify-topup/route.ts` normalizes resolved billing scope before access checks, provider-note comparison, active-subscription reads, Answerlattice store-summary refs, or paid writes. `npm run verify:billing-entitlement-boundary` and `npm run verify:menulist-api-tenant-safety` source-gate the helper, normalized route variables, raw store-ref exclusions, Razorpay docs, AI enhancement docs, audit evidence, and changelog evidence. This changes app-side top-up billing scope document-ID admission only; it does not change valid Razorpay order creation, signature verification, payment capture, credit math, subscription document admission, entitlement/cache invalidation behavior, Firestore rules/indexes, Cloud Functions, Vercel deployment, production builds, browser/device QA, Razorpay sandbox smoke, production-host smoke, launch approval, or release certification.

MenuList Top-Up Order Document ID Boundary checkpoint: fixed in source. MenuList top-up order creation, top-up verification, idempotency reads, pending audit writes, paid audit writes, duplicate-verification handling, and subscription credit increment behavior keep the same behavior for valid Razorpay `order_...` IDs, but malformed, reserved, empty, whitespace-mutated order IDs, or path-shaped order IDs can no longer reach `topups/{orderId}` document refs. `src/lib/billing/topupDocumentIdBoundary.ts` validates top-up order IDs with exact raw-value comparison plus the shared Firestore document-ID guard, `src/app/api/razorpay/create-topup-order/route.ts` normalizes provider order IDs before pending top-up writes, and `src/app/api/razorpay/verify-topup/route.ts` normalizes checkout order IDs before idempotency reads and paid audit writes. `npm run verify:billing-entitlement-boundary` source-gates the helper, strict raw order-ID equality, normalized refs, raw-ref exclusions, Razorpay docs, AI enhancement docs, audit evidence, and changelog evidence. This changes app-side top-up order document-ID admission only; it does not change valid Razorpay order creation, signature verification, payment capture, credit math, subscription document admission, entitlement/cache invalidation behavior, Firestore rules/indexes, Cloud Functions, Vercel deployment, production builds, browser/device QA, Razorpay sandbox smoke, production-host smoke, launch approval, or release certification.

MenuList Billing Subscription Document ID Boundary checkpoint: fixed in source. MenuList subscription DAL create/update/get-by-id paths, AI capacity monthly-credit reset, AI credit consumption, entitlement sync subscription analytics mirror, top-up verification credit increment, and fixed owner-facing billing/capacity failure copy keep the same behavior for valid Razorpay subscription document IDs, but malformed, reserved, empty, whitespace-mutated subscription IDs, or path-shaped subscription IDs can no longer reach `subscriptions/{subscriptionId}` document refs. `src/lib/billing/subscriptionDocumentIdBoundary.ts` validates billing subscription IDs with exact raw-value comparison plus the shared Firestore document-ID guard, `src/database/subscriptions/server.ts` and `src/database/subscriptions/index.ts` normalize subscription IDs before server/client subscription document refs, `src/lib/ai/capacityCheck.ts` normalizes subscription IDs before lazy-reset and credit-consumption refs, `src/lib/billing/subscriptionEntitlementSync.ts` normalizes before the subscription entitlement mirror write, and `src/app/api/razorpay/verify-topup/route.ts` normalizes the active subscription ID before the top-up transaction. `npm run verify:billing-entitlement-boundary` and `npm run verify:ai-accounting` source-gate the helper, strict raw subscription-ID equality, normalized refs, raw-ref exclusions, Razorpay docs, AI enhancement docs, audit evidence, and changelog evidence. This changes app-side billing subscription document-ID admission only; it does not change valid Razorpay provider calls, valid subscription/top-up writes, credit math, entitlement/cache invalidation behavior, Firestore rules/indexes, Cloud Functions, Vercel deployment, production builds, browser/device QA, Razorpay sandbox smoke, production-host smoke, launch approval, or release certification.

Answerlattice App Billing Document ID Boundary checkpoint: fixed in source. Billing dashboard active-subscription reads, shared product-billing adapter create/update/get-by-id/entitlement refs, store-summary subscription fallback behavior, self-service onboarding subscription creation, Knowledge Intake active-license checks, AI accounting monthly-credit refresh and credit consumption, Knowledge Intake support-credit reservation, intake usage settlement, intake usage refund, and fixed owner-facing billing/intake failure copy keep the same behavior for valid subscription and intake-ledger IDs, but malformed, reserved, empty, or path-shaped subscription or ledger IDs can no longer reach `subscriptions/{subscriptionId}` or `answerlattice_intakeUsageLedger/{ledgerId}` document refs. The July 6 product-billing store-scope continuation also prevents malformed, reserved, empty, whitespace-mutated, path-shaped, decimal, zero, negative, unsafe, or nonnumeric Answerlattice billing tenant/store scope from reaching active-subscription `stores/{storeId}` summary reads, capped fallback tenant/store queries, or entitlement-summary `stores/{storeId}` writes. `src/lib/answerlattice/billingDocumentIdBoundary.ts` validates billing document IDs with the shared Firestore document-ID guard, `src/database/answerlattice/billing.ts` normalizes store-summary subscription IDs before direct subscription reads, `src/lib/billing/productBillingServer.ts` normalizes Answerlattice provider/store-summary/subscription IDs before product-billing create/update/get-by-id/active-lookup/entitlement refs and exposes `normalizeAnswerlatticeBillingScopeDocumentId()` for product-billing tenant/store scope, `src/app/api/answerlattice/onboard/route.ts` normalizes provider subscription IDs before subscription creation, `src/lib/answerlattice/knowledgeIntakeApi.ts` normalizes store-mirror subscription IDs before active-license direct-doc reads, `src/lib/answerlattice/aiAccounting.ts` normalizes subscription IDs before monthly refresh and credit-debit refs, and `src/lib/answerlattice/intakeUsageLedger.ts` normalizes subscription summary IDs and ledger IDs before Admin Firestore refs. `npm run verify:answerlattice-runtime-truth` and `npm run verify:billing-entitlement-boundary` source-gate the helper, client billing import, product-billing adapter refs, product-billing store-scope refs, direct-doc fallback behavior, onboarding subscription writes, Knowledge Intake license reads, AI accounting refs, intake ledger refs, raw-ref exclusions, Billing docs, Knowledge Intake docs, data inventory evidence, audit evidence, and changelog evidence. Follow-up gates passed after the store-scope continuation: `node --check scripts/verification/verify-billing-entitlement-boundary.js`, `node --check scripts/verification/verify-answerlattice-runtime-truth.js`, `npx tsc --noEmit --incremental false --pretty false --noErrorTruncation`, `npm run verify:billing-entitlement-boundary`, and `npm run verify:answerlattice-runtime-truth`. This changes app-side billing/intake document-ID and product-billing store-scope admission only; it does not change valid subscription reads, valid store-summary fallback, valid product-billing create/update/get-by-id/entitlement behavior, valid onboarding subscription creation, valid active-license checks, valid AI accounting refresh/consume semantics, valid support-credit reservation/settlement/refund semantics, Razorpay behavior, Firestore rules/indexes, Cloud Functions, Vercel deployment, production builds, browser/device QA, provider smoke, production-host smoke, launch approval, or release certification.

Answerlattice App Entity DAL ID Boundary checkpoint: fixed in source. Entity update/deprecate, relation create/delete, search-index upsert, alias sync, merge, canonical-answer reference transfer during merge, relation rewrite during merge, compiled-context source-version marking, merge audit metadata, and fixed owner-facing hook copy keep the same behavior for valid entity/relation/search-index IDs, but malformed, reserved, empty, path-shaped, or unresolved entity IDs can no longer reach `answerlattice_entities/{entityId}`, `answerlattice_entityRelations/{relationId}`, or `answerlattice_entitySearchIndex/{searchIndexId}` document refs or entity-scoped query keys. `src/lib/answerlattice/governanceIdBoundary.ts` now exposes entity relation and search-index ID helpers, and `src/database/answerlattice/entities.ts` normalizes entity, relation, search-index, alias-sync, and merge IDs before refs, query keys, source IDs, audit state, and api composer inputs. `npm run verify:answerlattice-runtime-truth` source-gates the helper exports, normalized ref builders, update/deprecate/relation/search-index/alias/merge callsites, raw-ref/raw-error exclusions, Entity System docs, data inventory evidence, audit evidence, and changelog evidence. This changes app-side Entity DAL document-ID admission only; it does not change valid entity creation, valid entity update/deprecation, valid relation creation/deletion, valid search-index upsert, valid alias sync, valid entity merge semantics, Firestore rules/indexes, Cloud Functions, Vercel deployment, production builds, browser/device QA, production-host smoke, launch approval, or release certification.

Answerlattice App Entity Candidate ID Boundary checkpoint: fixed in source. Entity candidate approval, rejection, promotion, merge, entity creation from candidates, search-index creation, promotion audit state, and candidate-review fixed owner-facing copy keep the same behavior for valid candidate IDs, but malformed, reserved, empty, or path-shaped candidate IDs can no longer reach `answerlattice_entityCandidates/{candidateId}` document refs or promotion audit metadata. `src/lib/answerlattice/entityCandidateIdBoundary.ts` validates candidate IDs with the shared Firestore document-ID guard, and `src/database/answerlattice/entityCandidates.ts` normalizes candidate IDs before status writes, promotion reads/writes, audit previous state, return metadata, and api composer inputs. `npm run verify:answerlattice-runtime-truth` source-gates the helper, DAL import, normalized ref builder, candidate action callsites, raw-ref/raw-error exclusions, Entity System docs, data inventory evidence, audit evidence, and changelog evidence. This changes app-side entity-candidate document-ID admission only; it does not change valid candidate extraction, valid candidate review, entity promotion authority rules, entity/search-index writes, Firestore rules/indexes, Cloud Functions, Vercel deployment, production builds, browser/device QA, production-host smoke, launch approval, or release certification.

Answerlattice App Product Surface ID Boundary checkpoint: fixed in source. Product-surface saves, starter-template creation, archive actions, direct surface reads, context summary rebuilds, and compiled-context source-version marking keep the same owner-managed product-surface mapping behavior for valid surface IDs, but malformed, reserved, empty, or path-shaped product surface IDs can no longer reach `answerlattice_productSurfaces/{surfaceId}` document refs or compiled-context source-version IDs. `src/lib/answerlattice/productSurfaceIdBoundary.ts` validates surface IDs with the shared Firestore document-ID guard, `src/lib/answerlattice/productSurfaceContent.ts` rejects malformed owner-provided IDs before save payloads, and `src/database/answerlattice/productSurfaces.ts` normalizes generated/save/archive/read IDs before refs and source metadata. `npm run verify:answerlattice-runtime-truth` source-gates the helper, parser normalization, DAL ref/source normalization, raw-ref exclusions, Product Surface Context docs, data inventory evidence, audit evidence, and changelog evidence. This changes app-side product-surface document-ID admission only; it does not change valid product-surface creation, valid template seeding, route/context matching, summary rebuild semantics, Knowledge Intake publishing semantics, Firestore rules/indexes, Cloud Functions, Vercel deployment, production builds, browser/device QA, production-host smoke, launch approval, or release certification.

Answerlattice App FAQ ID Boundary checkpoint: fixed in source. FAQ save, archive, article-link maintenance, and feedback paths keep the same owner-reviewed FAQ save flow, acknowledgement envelopes, linked article `faqIds` mirrors, review/archive maintenance, feedback counters, cache-version bumps, public cache invalidation, and fixed owner-facing action copy for valid FAQ/article IDs, but malformed, reserved, empty, or path-shaped FAQ IDs and linked KB article IDs can no longer reach FAQ document refs, article mirror refs, article-scoped FAQ queries, or FAQ cache-version source IDs. `src/lib/answerlattice/faqIdBoundary.ts` now validates FAQ IDs with the shared Firestore document-ID guard; `src/lib/answerlattice/faqContent.ts` validates optional saved/generated FAQ IDs, linked article IDs, generated-from article IDs, and canonical answer links before FAQ save payloads; and `src/database/answerlattice/faqs.ts` normalizes FAQ IDs and KB article IDs before FAQ refs, article refs, article queries, maintenance updates, archive actions, feedback transactions, and cache-version source IDs. `npm run verify:answerlattice-runtime-truth` source-gates the FAQ-ID helper, parser normalization, DAL ref/query normalization, raw-ref exclusions, FAQ Management docs, data inventory evidence, audit evidence, and changelog evidence. This changes app-side FAQ and linked-article ID admission only; it does not change valid FAQ creation, valid FAQ retrieval, valid article FAQ mirror behavior, Help Center search ranking, Knowledge Intake publishing semantics, Firestore rules/indexes, Cloud Functions, Vercel deployment, production builds, browser/device QA, production-host smoke, launch approval, or release certification.

Answerlattice App Canonical Answer ID Boundary checkpoint: fixed in source. Canonical answer reads and writes keep the same canonical creation flow, resolved entity binding, guided-workflow procedure validation, drift governance writes, signal metric writes, and canonical cache versioning for valid answer IDs, but malformed, reserved, empty, or path-shaped canonical answer IDs can no longer reach `answerlattice_canonicalAnswers/{answerId}` document refs or canonical cache-version source IDs. `src/database/answerlattice/canonicalAnswers.ts` now reuses `normalizeAnswerlatticeCanonicalAnswerId()` from `src/lib/answerlattice/governanceIdBoundary.ts` in the document-ref builder, scope resolver, direct reads, direct updates, governance updates, signal-metric updates, and canonical cache-version source metadata. `npm run verify:answerlattice-runtime-truth` source-gates the canonical answer-ID boundary import, normalized document ref builder, direct update/governance/signal callsites, raw-ref exclusions, data inventory evidence, audit evidence, and changelog evidence. This changes app-side canonical answer document-ID admission only; it does not change valid canonical answer creation, entity binding, retrieval ranking, drift detection logic, guided workflow validation, Firestore rules/indexes, Cloud Functions, Vercel deployment, production builds, browser/device QA, production-host smoke, launch approval, or release certification.

Answerlattice App Release ID Boundary checkpoint: fixed in source. Release activation and processing keep the same append-only release creation model, processing status write, advisory drift evaluation, active status write, bounded drift failure diagnostics, and compiled-context source marking for valid release IDs, but malformed, reserved, empty, or path-shaped release IDs can no longer reach `answerlattice_releases/{releaseId}` document refs, drift-audit entity IDs, or source-version invalidation IDs. `src/lib/answerlattice/releaseIdBoundary.ts` now validates release IDs with the shared Firestore document-ID guard, and `src/database/answerlattice/releases.ts` normalizes release IDs before get/processing/activation refs, audit metadata, bounded diagnostics, and `markAnswerlatticeCompiledContextSourceChanged()` source IDs. `npm run verify:answerlattice-runtime-truth` source-gates the helper, DAL import, normalized ref builder, activation/processing callsites, raw-ref exclusions, release docs, data inventory evidence, audit evidence, and changelog evidence. This changes app-side release document-ID admission only; it does not change valid release creation, valid release activation, drift-detection logic, canonical-answer writes, Firestore rules/indexes, Cloud Functions, Vercel deployment, production builds, browser/device QA, production-host smoke, launch approval, or release certification.

Answerlattice App Predictive Trigger ID Boundary checkpoint: fixed in source. Predictive trigger owner/admin actions keep the same manual CRUD flow, suggested-trigger activation, disabling, deletion, summary rebuilds, compiled-context source marking, and fixed action copy for valid trigger IDs, but malformed, reserved, empty, or path-shaped trigger IDs can no longer reach `answerlattice_predictiveTriggers/{triggerId}` document refs or hook audit entity IDs. `src/lib/answerlattice/predictiveTriggerIdBoundary.ts` now validates trigger IDs with the shared Firestore document-ID guard, `src/database/answerlattice/predictiveTriggers.ts` normalizes trigger IDs before get/update/activate/disable/delete refs and source IDs, and `src/hooks/answerlattice/usePredictiveTriggers.ts` normalizes action IDs before audit-log writes. `npm run verify:answerlattice-runtime-truth` source-gates the helper, DAL import, normalized ref builder, hook audit normalization, raw-ref exclusions, predictive-support docs, data inventory evidence, audit evidence, and changelog evidence. This changes app-side predictive trigger document-ID admission only; it does not change valid predictive trigger reads/writes, runtime summary reads, predictive API behavior, suggested-trigger generation, trigger scoring, Firestore rules/indexes, Cloud Functions, Vercel deployment, production builds, browser/device QA, production-host smoke, launch approval, or release certification.

Answerlattice signal entity ID boundary checkpoint: fixed in source. Public API signals, widget feedback signals, ticket feedback/resolution signals, and predictive-support signals keep the same feature gates, API/auth admission, dedupe keys, sanitized metadata, non-blocking behavior, deterministic retry document IDs, and bounded diagnostics, but malformed top-level `entityId` values can no longer enter `answerlattice_signalEvents` as path-shaped IDs for later scheduler grouping. `src/lib/answerlattice/signalEmitter.ts` now normalizes signal `entityId` values through `src/lib/answerlattice/governanceIdBoundary.ts` before server/admin signal writes and browser DAL signal writes; malformed values fall back to `unresolved`, preserving the existing entity-resolution path. `npm run verify:answerlattice-runtime-truth` source-gates the normalizer import, centralized fallback, normalized server/client writes, raw default exclusion, Answerlattice data inventory evidence, audit evidence, and changelog evidence. This changes signal entity-ID admission and source-gate coverage only; it does not change valid signal writes, valid retry dedupe behavior, signal metadata shape, signal mutation scheduling, Firestore read/write counts for valid requests, Firestore rules/indexes, Cloud Functions, Firebase deployment, Vercel deployment, production builds, browser/device QA, production-host smoke, launch approval, or release certification.

Answerlattice Legacy Signal Mutation Entity ID Boundary checkpoint: fixed in source. The legacy/manual app-side signal mutation utility keeps the same feature flag, recent-signal read, severity weighting, time decay, thresholding, active-answer lookup, mutation proposal creation, audit write, and bounded failure diagnostics, but malformed stored signal `entityId` values can no longer become cluster keys, active-answer lookup keys, proposal `relatedEntityIds`, or proposal diagnostic entity metadata. `src/lib/answerlattice/signalMutation.ts` now normalizes stored signal entity IDs through `src/lib/answerlattice/governanceIdBoundary.ts` before clustering; malformed or unresolved IDs are skipped before proposal creation. `npm run verify:answerlattice-runtime-truth` source-gates the normalizer import, normalized cluster reads/writes, raw cluster-key exclusions, Automatic Knowledge Creation docs, data inventory evidence, audit evidence, and changelog evidence. This changes legacy/manual signal-mutation ID admission only; it does not change production batch mutation, valid signal proposals, valid active-answer lookups, proposal semantics, audit writes, Firestore rules/indexes, Cloud Functions, Vercel deployment, production builds, browser/device QA, production-host smoke, launch approval, or release certification.

Answerlattice App Resolved Entity Link Boundary checkpoint: fixed in source. App-side entity-link writers and lookup helpers keep the same owner workflows, product-surface save shape, FAQ save/generation shape, Knowledge Intake publish destinations, canonical-answer writes, draft-regeneration grounding, active-answer lookups, signal grounding queries, cache/version bumps, and bounded diagnostics, but malformed or `unresolved` entity IDs can no longer become durable `entityIds`, canonical `scope.entityIds`, mutation proposal `relatedEntityIds`, active-answer query keys, or signal-event query keys. `src/lib/answerlattice/governanceIdBoundary.ts` now exposes resolved entity helpers that reject the `unresolved` sentinel while preserving signal emission's explicit fallback path. `src/lib/answerlattice/productSurfaceContent.ts`, `src/lib/answerlattice/faqContent.ts`, `src/app/api/answerlattice/faqs/generate-from-article/route.ts`, `src/lib/answerlattice/knowledgeIntake.ts`, `src/database/answerlattice/canonicalAnswers.ts`, `src/database/answerlattice/signalEvents.ts`, `src/lib/answerlattice/draftGenerator.ts`, `src/app/api/answerlattice/mutation-proposals/regenerate-draft/route.ts`, `src/database/answerlattice/mutationProposals.ts`, and `src/lib/answerlattice/signalMutation.ts` now use the resolved helper at those entity-link boundaries. `npm run verify:answerlattice-runtime-truth` source-gates the helper, callsites, raw trim-list exclusions, data inventory evidence, audit evidence, and changelog evidence. This changes app-side entity-link admission only; it does not change signal emission fallback, valid entity IDs, valid Knowledge Intake publishing, FAQ generation prompts, product-surface summary scoring, canonical answer semantics, Firestore rules/indexes, Cloud Functions, Vercel deployment, production builds, browser/device QA, production-host smoke, launch approval, or release certification.

Answerlattice App Runtime Resolved Entity Read Boundary checkpoint: fixed in source. Repeated-reply entity autocomplete, canonical-first retrieval, and app-side compiled-context bundle assembly keep the same query tokenization, tenant/store-scoped reads, capped option reads, entity status filtering, context-aware boosts, canonical confidence gate, source reads, bundle object shapes, cache headers, and fallback behavior, but malformed or `unresolved` entity IDs can no longer become direct entity document refs, runtime scoring keys, context-boost keys, relation endpoints, or compiled bundle entity arrays. `src/lib/answerlattice/entityLookup.ts`, `src/lib/answerlattice/canonicalRetrieval.ts`, and `src/lib/answerlattice/contextBundleBuilderServer.ts` now use the resolved entity helper in `src/lib/answerlattice/governanceIdBoundary.ts` for those read/output boundaries. `npm run verify:answerlattice-runtime-truth` source-gates the resolved helper imports, resolved callsites, sentinel-friendly helper exclusions, Repeated Reply Import docs, Entity System docs, compiled-context docs, data inventory evidence, audit evidence, and changelog evidence. This changes app-side runtime read and bundle-output ID admission only; it does not change signal emission fallback, valid entity autocomplete, valid canonical retrieval, valid context-bundle source reads, valid Storage object paths, Firestore rules/indexes, Cloud Functions, Vercel deployment, production builds, browser/device QA, production-host smoke, launch approval, or release certification.

Answerlattice App Support Board Card ID Boundary checkpoint: fixed in source. Support Board card detail updates, status moves, and note writes keep the same owner/staff workflows, fixed action copy, card status history, embedded-note cap, and source sync behavior for valid card IDs, but malformed, reserved, empty, or path-shaped card IDs can no longer reach `answerlattice_supportBoardCards/{cardId}` document refs. `src/lib/answerlattice/supportBoardCardIdBoundary.ts` now validates card IDs with the shared Firestore document-ID guard, and `src/database/answerlattice/supportBoard.ts` normalizes card IDs in the document-ref builder before update, status transaction, or note writes. `npm run verify:answerlattice-runtime-truth` source-gates the helper, DAL import, normalized ref builder, raw-ref exclusions, Support Board docs, data inventory evidence, audit evidence, and changelog evidence. This changes app-side Support Board card document-ID admission only; it does not change valid Support Board reads/writes, generated card IDs, source sync query counts, notes/status semantics, proposal approval, Firestore rules/indexes, Cloud Functions, Vercel deployment, production builds, browser/device QA, production-host smoke, launch approval, or release certification.

Answerlattice App Support Board Related Entity ID Boundary checkpoint: fixed in source. Support Board manual cards, bounded ticket sync, bounded signal sync, card badges, detail form initialization, private notes, status history, source identity display, and governed answer-proposal handoff keep the same behavior for valid IDs, but malformed or `unresolved` related entity IDs can no longer persist as Support Board card `relatedEntityId` values or become mutation proposal `relatedEntityIds`. `src/database/answerlattice/supportBoard.ts`, `src/hooks/answerlattice/useSupportBoard.ts`, and `src/components/templates/answerlattice/supportBoard/AnswerlatticeSupportBoard.tsx` now use the resolved entity helper in `src/lib/answerlattice/governanceIdBoundary.ts` before card writes, ticket/signal source-card links, UI proposal eligibility, and proposal creation. `npm run verify:answerlattice-runtime-truth` source-gates the Support Board DAL, hook, UI, raw unresolved-guard exclusions, Support Board docs, data inventory evidence, audit evidence, and changelog evidence. This changes app-side Support Board related-entity ID admission only; it does not change valid Support Board cards, source sync query counts, notes/status writes, proposal approval, signal emission fallback, Firestore rules/indexes, Cloud Functions, Vercel deployment, production builds, browser/device QA, production-host smoke, launch approval, or release certification.

Answerlattice Predictive Trigger Entity ID Boundary checkpoint: fixed in source. Predictive trigger sync keeps the same feature flag, friction snapshot read, existing-trigger coverage check, suggested-trigger creation, active-trigger cache rebuild, canonical-answer enrichment, effectiveness scoring, compiled-context invalidation, and fixed failure diagnostics, but malformed stored trigger/friction/canonical-answer entity IDs can no longer become suggested trigger `action.entityId` values, `frictionSource.entityId` values, answer summary query keys, or predictive runtime summary enrichment keys. `functions-answerlattice/src/answerlattice/predictiveTriggerSync.ts` now normalizes friction snapshot entity IDs, existing trigger action entity IDs, active trigger entity lookup keys, and canonical-answer scope entity IDs through `functions-answerlattice/src/answerlattice/entityIdBoundary.ts`; malformed or unresolved values are skipped before trigger writes, answer lookups, or resolved suggestion enrichment. `npm run verify:answerlattice-runtime-truth` source-gates the normalizer import, normalized friction/action/scope/cache callsites, raw trim-only exclusions, data inventory evidence, audit evidence, and changelog evidence. This changes predictive-trigger entity-ID admission only; it does not change valid predictive suggestions, valid cache rebuilds, valid trigger scoring, compiled-context source marking, Firestore rules/indexes, Vercel deployment, production builds, browser/device QA, production-host smoke, launch approval, or release certification. Firebase Functions deployment is required for the Functions-side effect.

Answerlattice Functions signal-source entity ID boundary checkpoint: fixed in source. Support Board nightly sync, product friction aggregation, ticket-resolution extraction, and scheduled draft generation keep the same scheduler gates, bounded source queries, fixed diagnostics, proposal lineage, derived-card source-text boundary, and summary writes, but stored malformed signal/search/proposal entity IDs can no longer become `answerlattice_entities/{entityId}` document refs or derived scheduler grouping keys. `functions-answerlattice/src/answerlattice/entityIdBoundary.ts` now normalizes Functions-side entity IDs and treats malformed or `unresolved` values as absent before entity reads, release-impact matching, related-entity assignment, friction stats, and ticket-resolution proposal context. `npm run verify:answerlattice-runtime-truth` source-gates the Functions helper, scheduler callsites, raw-ref exclusions, Support Board docs, audit evidence, and changelog evidence. This changes Answerlattice Functions malformed entity-ID handling and source-gate coverage only; it does not change valid scheduler reads/writes, valid entity IDs, proposal generation semantics, Firestore rules/indexes, Vercel deployment, production builds, browser/device QA, production-host smoke, launch approval, or release certification. Firebase Functions deployment is required for the Functions-side effect.

Answerlattice Nightly Scheduler Stored Entity ID Boundary checkpoint: fixed in source. Nightly drift detection, signal mutation, signal resolution, coverage KPI, founder trust metrics, recurring fallback proposal generation, and entity graph summaries keep the same scheduler cadence, source queries, cost caps, status writes, proposal generation, summary writes, graph hash behavior, and bounded diagnostics, but stored malformed signal/search/canonical-answer/search-index/relation entity IDs can no longer become derived scheduler grouping keys or signal-resolution writes. `functions-answerlattice/src/answerlattice/answerlatticeNightly.ts` now normalizes stored signal entity IDs, search-history `matchedEntityIds`, canonical-answer `scope.entityIds`, search-index candidate entity IDs, and graph relation endpoints through `functions-answerlattice/src/answerlattice/entityIdBoundary.ts` before drift, signal mutation, coverage/trust, recurring fallback, graph answer counts, relation graph edges, or unresolved-signal updates. `npm run verify:answerlattice-runtime-truth` source-gates the normalized array helper, signal resolver normalization, drift/trust/graph callsites, raw stored-ID exclusions, Founder Trust docs, data inventory evidence, audit evidence, and changelog evidence. This changes Answerlattice nightly stored-entity-ID admission only; it does not change valid scheduler reads/writes, valid signal resolution, valid trust metrics, valid graph summaries, proposal semantics, Firestore rules/indexes, Vercel deployment, production builds, browser/device QA, production-host smoke, launch approval, or release certification. Firebase Functions deployment is required for the Functions-side effect.

Answerlattice Compiled Context Bundle Entity ID Boundary checkpoint: fixed in source. Manual/server and Functions repair bundle builds keep the same source reads, source-version equality checks, lock/manifest writes, immutable public/private Storage paths, route/entity/private/MCP bundle object shapes, cache headers, and fixed diagnostics, but malformed or `unresolved` stored relation endpoints and malformed or `unresolved` answer/article/FAQ/surface/release entity ID arrays can no longer be copied into compiled context JSON. `src/lib/answerlattice/contextBundleBuilderServer.ts` now normalizes bundle entity IDs through the resolved helpers in `src/lib/answerlattice/governanceIdBoundary.ts`, while `functions-answerlattice/src/answerlattice/contextBundleBuilder.ts` normalizes through `functions-answerlattice/src/answerlattice/entityIdBoundary.ts`; malformed or unresolved relation endpoints are dropped from relation output and malformed or unresolved array entries are skipped before upload. `npm run verify:answerlattice-runtime-truth` source-gates both builders, compact relation normalization, compact array normalization, raw compact-field exclusions, compiled-context docs, data inventory evidence, audit evidence, and changelog evidence. This changes compiled-context bundle ID admission only; it does not change valid bundle builds, valid Storage object paths, valid manifest behavior, source-version equality, bundle proxy behavior, Firestore rules/indexes, Vercel deployment, production builds, browser/device QA, production-host smoke, launch approval, or release certification. Firebase Functions deployment is required for the Functions-side repair effect.

Answerlattice Nightly Mutation Impact Entity ID Boundary checkpoint: fixed in source. Post-mutation impact tracking keeps the same implemented-proposal scan, 14-day age gate, before/after signal-count comparison, impact result write, fixed scheduler diagnostics, and tenant/store scoping, but malformed implemented proposal `relatedEntityIds[0]` values can no longer become nightly signal-count query keys. `functions-answerlattice/src/answerlattice/answerlatticeNightly.ts` now normalizes the related entity ID through `functions-answerlattice/src/answerlattice/entityIdBoundary.ts`; malformed or `unresolved` values are skipped before the `answerlattice_signalEvents` query. `npm run verify:answerlattice-runtime-truth` source-gates the import, normalized query key, raw related-entity exclusion, Automatic Knowledge Creation docs, data inventory evidence, audit evidence, and changelog evidence. This changes Answerlattice nightly mutation-impact query admission only; it does not change valid impact tracking, proposal writes, scheduler cadence, Firestore rules/indexes, Vercel deployment, production builds, browser/device QA, production-host smoke, launch approval, or release certification. Firebase Functions deployment is required for the Functions-side effect.

Answerlattice Mutation Proposal Action and Scheduled Draft Diagnostic ID Boundary checkpoint: fixed in source. The mutation proposal approve/reject/mark-implemented DAL keeps the same status guards and transaction writes, but request-supplied proposal IDs are now normalized through `src/lib/answerlattice/governanceIdBoundary.ts` before transaction document refs or diagnostic context. Scheduled draft generation keeps the same proposal scan, pending/generated/failed status writes, Gemini call, audit write, and bounded failure diagnostics, but malformed proposal `relatedEntityIds[0]` values are normalized through `functions-answerlattice/src/answerlattice/entityIdBoundary.ts` before per-proposal/status-marker failure diagnostics. `npm run verify:answerlattice-runtime-truth` source-gates normalized action refs, raw action-ref exclusions, normalized scheduled-draft diagnostic entity IDs, Automatic Knowledge Creation docs, data inventory evidence, audit evidence, and changelog evidence. This changes Answerlattice mutation-proposal action ID admission and scheduled draft failure diagnostic metadata only; it does not change valid proposal actions, valid scheduled draft generation, provider calls, proposal status semantics, Firestore rules/indexes, Vercel deployment, production builds, browser/device QA, production-host smoke, launch approval, or release certification. Firebase Functions deployment is required for the Functions-side effect.

Answerlattice Manual Draft Helper Related Entity ID Boundary checkpoint: fixed in source. The legacy/shared manual draft helper and draft approval DAL keep the same feature flag, route-backed generation path, no-store browser request policy, fixed local error copy, human approval requirement, canonical-answer creation, search-index update, and audit write behavior, but malformed proposal IDs and malformed proposal `relatedEntityIds[0]` values can no longer reach client proposal document refs, entity reads, signal grounding queries, active-answer queries, canonical-answer `scope.entityIds`, or approval audit metadata. `src/lib/answerlattice/draftGenerator.ts` and `src/database/answerlattice/mutationProposals.ts` now normalize proposal and related entity IDs through `src/lib/answerlattice/governanceIdBoundary.ts` before those reads/writes. `npm run verify:answerlattice-runtime-truth` source-gates the helper imports, normalized proposal reads/writes, normalized related entity binding, raw related-entity exclusion, Automatic Knowledge Creation docs, data inventory evidence, audit evidence, and changelog evidence. This changes manual draft helper and approval-DAL ID admission only; it does not change valid manual draft generation, valid draft approval, route admission, provider calls, AI operation accounting, Firestore rules/indexes, Cloud Functions, Firebase deployment, Vercel deployment, production builds, browser/device QA, production-host smoke, launch approval, or release certification.

Answerlattice onboarding user ID boundary checkpoint: fixed in source. The Answerlattice `/get-started` onboarding API keeps the same `withAuth()` admission, widget feature gate, hashed payment-onboarding rate limit, bounded 32KB body reader, existing-account guard, centralized tenant/store transaction helper, Razorpay paid subscription setup, widget API key creation, tenant summary sync, compiled-context control-plane setup, and fixed browser-facing failure copy, but malformed authenticated session user IDs can no longer reach `users/{userId}` document refs or onboarding-created product-surface creator fields. `src/lib/answerlattice/onboardingUserIdBoundary.ts` now normalizes onboarding user IDs through `src/lib/firebase/firestoreDocumentId.ts`; `/api/answerlattice/onboard` validates `session.user.id` before the rate-limit key and transaction write, re-normalizes helper `params.userId` before default auth product-account sync and initial product-surface bootstrap, and source-gates out `.doc(params.userId)`. `npm run verify:answerlattice-runtime-truth` source-gates the helper, normalized route refs, helper re-normalization, raw-ref exclusion, Answerlattice Billing docs, Answerlattice Website docs, audit evidence, and changelog evidence. This changes Answerlattice onboarding user-document ID admission and source-gate coverage only; it does not change valid Answerlattice onboarding, existing-account checks, tenant/store creation, Razorpay subscription creation, widget key creation, product-surface bootstrap semantics, Firestore read/write counts for valid requests, Firestore rules/indexes, Cloud Functions, Firebase deployment, Vercel deployment, production builds, browser/device QA, production-host smoke, launch approval, or release certification.

Answerlattice staff user ID boundary checkpoint: fixed in source. The Answerlattice Team Access routes keep the same staff feature flag, `withAuth()` route admission, hashed staff/team action rate limits, team permission checks, bounded 16KB mutation body reader, email/passcode login model, role assignment rules, self-removal guard, last-owner guard, Firebase Auth token revocation, default `productAccounts.AL` bridge sync, and fixed dashboard failure copy, but malformed request `userId` values can no longer reach `users/{userId}` reads for staff update, remove, password/passcode reset, or force sign-out. `src/lib/answerlattice/staffUserIdBoundary.ts` now normalizes staff user IDs through `src/lib/firebase/firestoreDocumentId.ts`; `src/lib/answerlattice/staffAccessServer.ts` applies that boundary in the mutation schemas, rechecks before `getAnswerlatticeUserById()` reads, and validates derived create/default-auth bridge user IDs before writes. `npm run verify:answerlattice-runtime-truth` source-gates the helper, schema boundary, normalized document ref, raw-ref exclusions, Staff Access docs, audit evidence, and changelog evidence. This changes Answerlattice staff user-document ID admission and source-gate coverage only; it does not change valid staff create/update/remove/reset/sign-out behavior, permission semantics, Firebase Auth operations for valid staff users, default auth bridge semantics, Firestore read/write counts for valid requests, Firestore rules/indexes, Cloud Functions, Firebase deployment, Vercel deployment, production builds, browser/device QA, production-host smoke, launch approval, or release certification.

Answerlattice entity retrieval ID boundary checkpoint: fixed in source. The repeated-reply entity autocomplete and canonical-first retrieval paths keep the same query tokenization, tenant/store-scoped search-index reads, capped entity-option reads, entity status filtering, context-aware boosts, canonical confidence gate, and fallback behavior, but malformed or `unresolved` entity IDs from search-index rows or context-boost maps can no longer become direct `answerlattice_entities/{entityId}` reads. `src/lib/answerlattice/entityLookup.ts` now normalizes and deduplicates search-index entity IDs through the resolved helper before entity doc reads and remaps ranked results through resolved IDs; `src/lib/answerlattice/canonicalRetrieval.ts` normalizes entity IDs through the resolved helper before direct entity reads, search-index scoring keys, and context-boost scoring keys. `npm run verify:answerlattice-runtime-truth` source-gates the resolved entity normalizer reuse, normalized entity refs, malformed/unresolved-ID skip behavior, Repeated Reply Import docs, Entity System docs, audit evidence, and changelog evidence. This changes Answerlattice entity document-ID admission and source-gate coverage only; it does not change valid entity autocomplete, valid canonical retrieval, search-index query shapes, context-aware scoring for valid entity IDs, Firestore read counts for valid requests, Firestore rules/indexes, Cloud Functions, Firebase deployment, Vercel deployment, production builds, browser/device QA, production-host smoke, launch approval, or release certification.

Answerlattice cache freshness ID boundary checkpoint: fixed in source. The search-history and instant-cache freshness paths keep the same source-version manifest optimization, canonical source fallback validation, KB reference fallback validation, tenant/store source checks, status checks, and stale-cache rejection behavior, but malformed cached canonical answer IDs and malformed cached KB article references can no longer be accepted by manifest freshness or become fallback source-document refs. `src/lib/answerlattice/governanceIdBoundary.ts` now exports `normalizeAnswerlatticeCanonicalAnswerId()`, and `src/lib/answerlattice/cacheFreshness.ts` validates canonical answer IDs before manifest freshness and canonical doc reads, plus validates article reference IDs through `src/lib/answerlattice/kbArticleIdBoundary.ts` before KB doc reads. `npm run verify:answerlattice-runtime-truth` source-gates the canonical answer ID boundary, KB article reference boundary, raw-ref exclusions, Instant Response Infrastructure docs, Firebase cost audit evidence, production-readiness audit evidence, and changelog evidence. This changes malformed cache-row ID admission and source-gate coverage only; it does not change valid search-history cache hits, valid instant-cache hits, source-version manifest behavior for valid cache rows, fallback source-document validation for valid IDs, Firestore read counts for valid requests, Firestore rules/indexes, Cloud Functions, Firebase deployment, Vercel deployment, production builds, browser/device QA, production-host smoke, launch approval, or release certification.

Answerlattice FAQ article reference ID boundary checkpoint: fixed in source. FAQ/custom-answer retrieval keeps the same published/active FAQ query, deterministic scoring, canonical-miss placement, optional full-article enrichment, tenant/store article validation, published/active article checks, and fallback compact related-article references, but malformed linked `faq.articleId` values can no longer become `kb_articles/{articleId}` reads or raw related-reference IDs. `src/lib/answerlattice/faqRetrieval.ts` now normalizes linked article IDs through `src/lib/answerlattice/kbArticleIdBoundary.ts`, skips malformed linked IDs before returning references, and uses `.doc(articleId)` only after normalization. `npm run verify:answerlattice-runtime-truth` source-gates the linked article normalizer, normalized document ref, raw-ref exclusion, FAQ Management docs, Help Center docs, audit evidence, and changelog evidence. This changes FAQ linked-article ID admission and source-gate coverage only; it does not change valid FAQ retrieval, valid linked article references, valid full-article enrichment, FAQ scoring, Firestore read counts for valid requests, Firestore rules/indexes, Cloud Functions, Firebase deployment, Vercel deployment, production builds, browser/device QA, production-host smoke, launch approval, or release certification.

Onboarding user ID helper boundary checkpoint: fixed in source. The website onboarding, reseller onboarding, and provider-failure compensation paths keep the same authenticated onboarding admission, bounded payment bodies, hashed rate limits, centralized tenant/store transaction helper, Razorpay subscription setup, reseller online/offline billing behavior, provider-failure compensation writes, and public cache revalidation behavior, but malformed onboarding `userId` values can no longer reach `users/{userId}` document refs in shared helper paths. `src/lib/onboarding/onboardingUserId.ts` now normalizes onboarding user IDs through `src/lib/firebase/firestoreDocumentId.ts`; `updateUserWithTenantStore()` uses `.doc(normalizedUserId)`, `compensateFailedTenantStoreOnboarding()` normalizes `params.userId` before cleanup reads/writes, and reseller onboarding normalizes Firebase Auth-generated UIDs before new owner user document creation. `npm run verify:agent-readiness` and `npm run verify:menulist-api-tenant-safety` source-gate the helper, normalized document refs, raw-ref exclusions, Auth Onboarding docs, Onboarding Centralization docs, audit evidence, and changelog evidence. This changes onboarding user-document ID admission and source-gate coverage only; it does not change valid onboarding, valid reseller onboarding, provider behavior, provider-failure compensation semantics, Firestore read/write counts for valid requests, Firestore rules/indexes, Cloud Functions, Firebase deployment, Vercel deployment, production builds, browser/device QA, production-host smoke, launch approval, or release certification.

Business Health answer-event ID boundary checkpoint: fixed in source. The protected Business Health answer route keeps the same feature gates, `withAuth()` admission, selected-store scope checks, `VIEW_ANALYTICS` permission check, bounded 32KB JSON body reader, grounded answer resolver, optional compact thread write, optional answer-event logging, bounded diagnostics, and fixed owner copy, but malformed answer IDs can no longer become `ownerBusinessAssistantAnswerEvents/{answerId}` document refs. `src/lib/ownerBusinessAssistant/server/answerEventLogger.ts` now rechecks the server-generated answer ID through `src/lib/firebase/firestoreDocumentId.ts`, skips only the optional answer-event write before Firestore access when the ID is malformed, and writes `.doc(answerId)` only after normalization. `npm run verify:owner-business-assistant`, `npm run verify:owner-business-health-boundary`, and `npm run verify:menulist-api-tenant-safety` source-gate the event logger guard, direct raw-ref exclusion, Business Health docs, audit evidence, and changelog evidence. Follow-up gates passed after this checkpoint patch: `node --check scripts/verification/verify-owner-business-health-boundary.js`, `node --check scripts/verification/verify-menulist-api-tenant-safety.js`, `npm run verify:owner-business-health-boundary`, `npm run verify:owner-business-assistant`, `npm run verify:menulist-api-tenant-safety`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, and focused `git diff --check`. This changes protected Business Health answer-event ID admission and source-gate coverage only; it does not change valid server-generated answer IDs, valid answer behavior, valid thread writes, valid feedback writes, selected-store scope behavior, answer-event write counts for valid requests, Firestore rules/indexes, Cloud Functions, Firebase deployment, Vercel deployment, production builds, browser/device QA, production-host smoke, launch approval, or release certification.

Menu Extraction identity helper project ID normalization checkpoint: fixed in source. The protected owner upload job route, authenticated menu-link import route, and menu-intake identity preflight route keep the same `withAuth()` admission, SAFE_MODE checks, tenant/store session access checks, hashed rate limits, bounded body readers, project ownership checks, Storage URL allowlists, active/completed job reuse, identity preflight, menu-link artifact/job writes, and fixed owner copy, but the shared identity helper now re-normalizes the final project ID before building the scoped project document ref. `src/lib/menu-extraction/menuIntakeIdentityServer.ts` imports `src/lib/menu-extraction/projectIdBoundary.ts`, rejects malformed helper inputs through the existing `MenuIntakeIdentityServerError` path, uses `.doc(projectId)` after normalization, and no longer uses raw `params.projectId` in the context-loader project read. `npm run verify:menu-extraction-pipeline` source-gates the helper-level normalizer, direct raw-ref exclusion, Menu Extraction docs, audit evidence, and changelog evidence. Follow-up gates passed after this checkpoint patch: `node --check scripts/verification/verify-menu-extraction-pipeline.js`, `npm run verify:menu-extraction-pipeline`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, and focused `git diff --check`. This changes Menu Extraction identity helper ID normalization and source-gate coverage only; it does not change valid project IDs, valid owner upload extraction, valid menu-link import, valid identity preflight, valid provider analysis, Firestore read/write counts for valid requests, Storage writes for valid menu-link imports, Firestore rules/indexes, Cloud Functions, Firebase deployment, Vercel deployment, production builds, browser/device QA, provider smoke, production-host smoke, launch approval, or release certification.

Public Truth Monitor server DAL ID normalization checkpoint: fixed in source. The authenticated manual refresh route keeps the same feature gate, DATA_WRITE limiter, bounded JSON parser, tenant access verification, Business Health `VIEW_ANALYTICS` permission check, paid entitlement gate, project-summary selection, readiness-report generation, capped summary write, bounded diagnostics, and fixed owner copy, but selected, persisted-summary, and final project IDs are now trimmed into normalized Firestore document IDs before project refs are built. `src/database/publicTruthMonitor/server.ts` normalizes the selected project ID before matching summary projects, normalizes stored summary project IDs before returning a candidate project, normalizes the final project ID before scoped and legacy project reads, and no longer uses raw `params.projectId` in project document refs. `npm run verify:public-truth-monitor-addon` and `npm run verify:menulist-api-tenant-safety` source-gate the server DAL normalizer, direct raw-ref exclusion, Public Truth Monitor docs, audit evidence, and changelog evidence. Follow-up gates passed after this checkpoint patch: `node --check scripts/verification/verify-public-truth-monitor-addon.js`, `node --check scripts/verification/verify-menulist-api-tenant-safety.js`, `npm run verify:public-truth-monitor-addon`, `npm run verify:menulist-api-tenant-safety`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, and focused `git diff --check`. This changes authenticated Public Truth Monitor server DAL ID normalization and source-gate coverage only; it does not change valid summary reads, valid manual refresh behavior, valid project IDs, paid entitlement behavior, readiness-report content for valid projects, capped summary writes for valid requests, Firestore read/write counts for valid requests, Firestore rules/indexes, Cloud Functions, Firebase deployment, Vercel deployment, browser/device QA, production-host smoke, launch approval, or release certification.

Business Health Project ID Boundary checkpoint: fixed in source. The Business Health page, current/analytics read-model routes, answer route, context-packet cache, thread persistence, answer-event logging, feedback route, desktop/mobile panels, and browser hooks keep the same read-only behavior, selected-store scope checks, `VIEW_ANALYTICS` permission checks, bounded response parsers, thread ID boundary, feedback answer-ID boundary, generic owner errors, and cache-first cost posture, but malformed selected `projectId` values can no longer become Business Health query scope, answer context-packet scope, browser/server cache keys, or thread message project scope. `src/lib/ownerBusinessAssistant/projectIdBoundary.ts` now accepts only bounded alphanumeric/underscore/hyphen project IDs that also pass `src/lib/firebase/firestoreDocumentId.ts`, `src/app/(main)/business-health/page.tsx` drops malformed query `projectId` values before rendering client hooks, and `src/lib/ownerBusinessAssistant/schemas.ts` validates explicit `projectId` and `clientContext.selectedProjectId` before API routes use them. `npm run verify:owner-business-assistant` and `npm run verify:owner-business-health-boundary` source-gate the helper, page query guard, schema guard, Owner Business Assistant docs, audit evidence, and changelog evidence. Follow-up gates passed after this checkpoint patch: `node --check scripts/verification/verify-owner-business-health-boundary.js`, `npm run verify:owner-business-health-boundary`, `npm run verify:owner-business-assistant`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, and focused `git diff --check`. This changes Business Health selected-project admission and source-gate coverage only; it does not change valid project IDs, valid current/analytics reads, valid answer packets, valid thread writes, valid answer-event writes, valid feedback writes, Firestore read/write counts for valid requests, Firestore rules/indexes, Cloud Functions, Firebase deployment, Vercel deployment, browser/device QA, production-host smoke, launch approval, or release certification.

Owner Action Mark-Done Project And Receipt ID Boundary checkpoint: fixed in source. The protected owner dashboard mark-done route keeps the same `withAuth()` admission, `VIEW_ANALYTICS` permission check, exact-positive session tenant/store normalization from the session-scope checkpoint, hashed DATA_WRITE limiter, 6KB bounded JSON parser, dashboard-summary read, action availability check, capped receipt write, oldest-receipt pruning, bounded diagnostics, and fixed owner copy, but malformed or whitespace-mutated request `projectId` values can no longer reach the composed `analytics/{tenantId}_{storeId}_{projectId}_dashboard_summary` document read/write path. `src/app/api/analytics/owner-action/mark-done/route.ts` now validates raw `projectId` values through the existing analytics character rule plus `src/lib/firebase/firestoreDocumentId.ts`, rechecks the composed dashboard document ID before Firestore access, keeps new receipt IDs as server-derived 32-character lowercase hex hashes, and filters existing receipt keys before using them in dotted delete update paths. `npm run verify:menulist-api-tenant-safety` source-gates the route schema guard, no-trim project-ID rule, composed document-ID guard order, receipt-key filter, Analytics docs, audit evidence, and changelog evidence. Follow-up gates passed after this checkpoint patch: `node --check scripts/verification/verify-menulist-api-tenant-safety.js`, `npm run verify:menulist-api-tenant-safety`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, and focused `git diff --check`. This changes protected owner-dashboard action mark-done request/server ID admission and source-gate coverage only; it does not change valid project IDs, valid mark-done receipts, valid dashboard-summary reads, valid receipt pruning, dashboard card behavior, Firestore read/write counts for valid requests, Firestore rules/indexes, Cloud Functions, Firebase deployment, Vercel deployment, browser/device QA, production-host smoke, launch approval, or release certification.

Extraction review apply ID boundary checkpoint: fixed in source. The desktop and mobile extraction review apply/discard helpers keep the same preview-ready ownership check, tenant/store/user validation, single project update payload, linked-outlet save route, public cache revalidation, MOL event logging, generic owner errors, and bounded diagnostics, but malformed `projectId` and `jobId` values can no longer reach client Firestore project or `menuImageProcessingJobs` document refs. `src/lib/extraction/schemas.ts` now validates review project/job IDs through the existing Menu Extraction document-ID normalizers, and `src/lib/extraction/applyChanges.ts` rechecks IDs before building Firestore refs for apply or discard. `npm run verify:menu-extraction-pipeline` source-gates the shared schema guard, client helper guard order, Menu Extraction docs, audit evidence, and changelog evidence. Follow-up gates passed after this checkpoint patch: `node --check scripts/verification/verify-menu-extraction-pipeline.js`, `npm run verify:menu-extraction-pipeline`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, and focused `git diff --check`. This changes desktop/mobile extraction review apply/discard ID admission and source-gate coverage only; it does not change valid preview-ready jobs, valid project IDs, valid apply mutations, valid discard behavior, linked-outlet save behavior, public cache revalidation, MOL events for valid applies, Firestore read/write counts for valid requests, Firestore rules/indexes, Cloud Functions, Firebase deployment, Vercel deployment, browser/device QA, production-host smoke, launch approval, or release certification.

Business Health feedback answer ID boundary checkpoint: fixed in source. The protected feedback route keeps the same Business Health feature gates, usage-logging flag gate, hashed DATA_WRITE limiter, 8KB bounded JSON parser, selected-store scope resolution, `VIEW_ANALYTICS` permission check, 90-day feedback expiry, feedback acknowledgement, and fixed owner copy, but malformed `answerId` values can no longer reach the composed `ownerBusinessAssistantFeedback` document write as path-shaped or reserved document IDs. `src/lib/ownerBusinessAssistant/schemas.ts` now validates feedback answer IDs through `src/lib/firebase/firestoreDocumentId.ts`, and `src/app/api/owner-business-assistant/feedback/route.ts` rechecks the composed feedback document ID before writing. `npm run verify:owner-business-assistant`, `npm run verify:owner-business-health-boundary`, and `npm run verify:menulist-api-tenant-safety` source-gate the schema guard, feedback route guard order, Business Health docs, audit evidence, and changelog evidence. Follow-up gates passed after this checkpoint patch: `node --check scripts/verification/verify-owner-business-health-boundary.js`, `node --check scripts/verification/verify-menulist-api-tenant-safety.js`, `npm run verify:owner-business-health-boundary`, `npm run verify:owner-business-assistant`, `npm run verify:menulist-api-tenant-safety`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, and focused `git diff --check`. This changes protected Business Health feedback request/server ID admission and source-gate coverage only; it does not change valid server-generated answer IDs, valid feedback writes, selected-store scope behavior, feedback retention, platform monitor reads, Firestore write counts for valid requests, Firestore rules/indexes, Cloud Functions, Firebase deployment, Vercel deployment, browser/device QA, production-host smoke, launch approval, or release certification.

Public Truth Monitor project and scope ID boundary checkpoint: fixed in source. The authenticated summary and manual refresh routes keep the same feature gate, DATA_READ/DATA_WRITE limiter behavior, bounded JSON parser, tenant access verification, Business Health `VIEW_ANALYTICS` permission check, paid entitlement gate, project-summary selection, readiness-report generation, capped summary write, bounded diagnostics, and fixed owner copy, but malformed `selectedProjectId` values, whitespace-mutated selected project IDs, malformed persisted summary project IDs, and malformed session-derived tenant/store scope IDs can no longer reach store refs, project-summary refs, monitor-summary refs, scoped project reads, legacy project fallback reads, or summary writes. `src/lib/validation/publicTruthMonitorSchemas.ts` validates raw selected project IDs through `src/lib/firebase/firestoreDocumentId.ts`, and `src/database/publicTruthMonitor/server.ts` requires selected/persisted project IDs to exactly match their trimmed Firestore document ID before project selection or reads, excludes malformed summary project IDs, validates scope IDs before `stores/{sId}`, `platformSummary/publicTruthMonitor_{sId}`, `platformSummary/projects_{sId}`, and `projects/{tId}/{sId}/{projectId}` refs, returns `null` or an empty list before Firestore access for malformed read IDs, and throws before summary writes for malformed store IDs. `npm run verify:public-truth-monitor-addon` and `npm run verify:menulist-api-tenant-safety` source-gate the raw schema guard, server DAL raw-value guard order, Public Truth Monitor docs, audit evidence, and changelog evidence. Follow-up gates passed after this checkpoint patch: `node --check scripts/verification/verify-public-truth-monitor-addon.js`, `node --check scripts/verification/verify-menulist-api-tenant-safety.js`, `npm run verify:public-truth-monitor-addon`, `npm run verify:menulist-api-tenant-safety`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, and focused `git diff --check`. This changes authenticated Public Truth Monitor request/server ID admission and source-gate coverage only; it does not change valid summary reads, valid manual refresh behavior, valid project IDs, valid tenant/store IDs, paid entitlement behavior, readiness-report content for valid projects, capped summary writes for valid requests, Firestore read/write counts for valid requests, Firestore rules/indexes, Cloud Functions, Firebase deployment, Vercel deployment, browser/device QA, production-host smoke, launch approval, or release certification.

GrowthOS project, kit, and scope ID boundary checkpoint: fixed in source. The authenticated Growth Kits refresh, generate, and export routes keep the same feature gate, Pro/Premium entitlement gate, bounded JSON parser, DATA_WRITE limiter, tenant access verification, source-facts loading, deterministic kit building, export recording, bounded diagnostics, and fixed owner copy, but malformed `projectId`, `kitId`, or session-derived tenant/store scope IDs can no longer reach store refs, summary refs, scoped project reads, legacy project fallback reads, `growthosKits/{tId}/{sId}/{kitId}` reads/writes, or `growthosExports/{tId}/{sId}` writes. `src/lib/validation/growthosSchemas.ts` validates project and kit IDs through `src/lib/firebase/firestoreDocumentId.ts`, and `src/database/growthos/server.ts` also trims and normalizes project/kit document IDs before Firestore refs, validates scope IDs before every scoped server ref, returns `null` before Firestore access for malformed persisted project, kit, or scope IDs, and throws before summary/kit/export writes when an internal caller supplies malformed generated or scope IDs. `npm run verify:growthos` source-gates the schema guards, server DAL normalized-ID usage, direct `.doc(params.projectId)` / `.doc(params.kitId)` / `.doc(kit.id)` exclusions, scope-ID guard coverage, GrowthOS docs, audit evidence, and changelog evidence. Follow-up gates passed after this checkpoint patch: `npm run verify:growthos`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, and focused `git diff --check`. This changes authenticated Growth Kits request/server ID admission and source-gate coverage only; it does not change valid project IDs, valid kit IDs, valid tenant/store IDs, valid refresh/generate/export behavior, entitlement behavior, source-facts behavior, Firestore read/write counts for valid requests, Firestore rules/indexes, Cloud Functions, Firebase deployment, Vercel deployment, browser/device QA, production-host smoke, launch approval, or release certification.

Customer App dynamic asset store ID fallback checkpoint: source-gated existing behavior. The public generated icon, splash, and screenshot routes keep the same `ImageResponse` rendering, `PUBLIC_DYNAMIC_ASSET` hashed-IP rate limit, public-safe store lookup, generic generated fallback assets, CDN cache headers, bounded runtime diagnostics, and fixed install-flow behavior, but the verifier now locks the route ordering that malformed or rate-limited store IDs return generic assets before `getPublicStoreById(storeId)` can run. `npm run verify:customer-app-pwa` source-gates the numeric 1-20 digit store ID guard, invalid-store fallback, fallback-before-store-lookup ordering, Customer App implementation/Firebase docs, audit evidence, and changelog evidence. Follow-up gates passed after this checkpoint patch: `node --check scripts/verification/verify-customer-app-pwa.js`, `npm run verify:customer-app-pwa`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, and focused `git diff --check`. This changes Customer App dynamic asset source-gate coverage and docs only; it does not change valid icon/splash/screenshot rendering, valid public-safe store branding, fallback assets, Firestore read counts for valid requests, Storage operations, analytics behavior, Customer App manifest behavior, Firestore rules/indexes, Cloud Functions, Firebase deployment, Vercel deployment, browser/device QA, production-host smoke, launch approval, or release certification.

Linked outlet save ID boundary checkpoint: fixed in source. The protected linked outlet save route keeps the same multi-outlet feature gate, `withAuth()` admission, 2MB bounded body reader, tenant/store access verification, hashed user/project rate limit, caller/outlet/master/tenant reads, `MANAGE_MENU` permission check, linked outlet project ownership check, local-only ID validation, OutletPolicy enforcement, Admin SDK project merge, public/screen cache invalidation, owner-assistant packet invalidation, bounded diagnostics, and fixed owner copy, but malformed `projectId`, `masterProjectId`, or override map IDs can no longer reach project-scope parsing, `projects/{tId}/{sId}/{projectId}` reads, outlet-local override writes, cache invalidation, or save acknowledgement. `/api/projects/outlet-save` now validates those IDs with the existing linked-outlet character rule plus `src/lib/firebase/firestoreDocumentId.ts`. `npm run verify:multi-location-boundary` source-gates the helper usage, refined project/override schemas, Multi-Outlet docs, audit evidence, and changelog evidence; `npm run verify:menulist-api-tenant-safety` also source-gates the refined linked-outlet save schemas in the broader API safety sweep. Follow-up gates passed after this checkpoint patch: `node --check scripts/verification/verify-multi-location-boundary.js`, `node --check scripts/verification/verify-menulist-api-tenant-safety.js`, `npm run verify:multi-location-boundary`, `npm run verify:menulist-api-tenant-safety`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, and focused `git diff --check`. This changes protected linked outlet save request admission and source-gate coverage only; it does not change valid linked outlet saves, valid local override IDs, OutletPolicy behavior, Firestore read/write counts for valid saves, public/screen cache invalidation for valid saves, Firestore rules/indexes, Cloud Functions, Firebase deployment, Vercel deployment, browser/device QA, production-host smoke, launch approval, or release certification.

Guest Feedback project ID boundary checkpoint: fixed in source. The public QR feedback page and public feedback submit route keep the same feature flag, public IP limiter, 16KB bounded body reader, honeypot, Turnstile verification, project/store/tenant eligibility checks, store feedback defaults, safe review URL handling, Admin SDK `guestFeedback` write, non-blocking MOL event logging, bounded diagnostics, and fixed guest copy, but malformed `projectId` values can no longer reach `projects/{tId}/{sId}/{projectId}` reads, submit writes, or MOL event logging. `src/lib/feedback/guestFeedbackProjectIdBoundary.ts` validates feedback project IDs through the supported project-ID character rule plus `src/lib/firebase/firestoreDocumentId.ts` before the public page lookup and shared submit schema allow downstream work, and `src/app/api/public/feedback/submit/route.ts` now repeats `normalizeGuestFeedbackProjectId(data.projectId)` into local `projectId` before reading `.doc(projectId)`, writing feedback, logging MOL events, or emitting bounded diagnostics, excluding `.doc(data.projectId)`. `npm run verify:guest-feedback-boundary` source-gates the helper, submit schema, helper-level route normalization, public page lookup, old loose project-ID schema removal, implementation/Firebase docs, audit evidence, and changelog evidence. Follow-up gates passed after this checkpoint patch: `node --check scripts/verification/verify-guest-feedback-boundary.js`, `npm run verify:guest-feedback-boundary`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, and focused `git diff --check`. This changes public Guest Feedback page/submit request admission and source-gate coverage only; it does not change valid feedback links, valid feedback submissions, feedback writes for valid requests, review URL behavior, owner inbox behavior, Firestore rules/indexes, Cloud Functions, Firebase deployment, Vercel deployment, browser/device QA, production-host smoke, launch approval, or release certification.

Guest Feedback Target Document ID Boundary checkpoint: fixed in source. The public feedback page uses the same numeric target normalizer before project and store reads, and `POST /api/public/feedback/submit` validates caller-supplied `data.tId` and `data.sId` through `normalizeGuestFeedbackNumericDocumentId()` before honeypot, Turnstile, `projects/{tId}/{sId}/{projectId}` reads, `stores/{sId}` reads, `tenants/{tId}` reads, `guestFeedback` writes, MOL event logging, or bounded diagnostics. The helper requires exact positive numeric MenuList IDs plus `src/lib/firebase/firestoreDocumentId.ts`, so whitespace-mutated, path-shaped, reserved, zero, negative, decimal, unsafe, or nonnumeric tenant/store IDs fail with the existing validation response before Firestore work. Valid public feedback links, valid submissions, store/tenant eligibility checks, safe review URL behavior, store field defaults, owner inbox behavior, response shape, and valid Firestore read/write/delete counts are unchanged. `npm run verify:guest-feedback-boundary` and `npm run verify:menulist-api-tenant-safety` source-gate the helper, page-level tenant/store normalization, route-level tenant/store normalization, normalized project/store/tenant refs, raw `doc(String(data.tId))` / `doc(String(data.sId))` / `collection(String(data.sId))` exclusions, implementation/Firebase docs, audit evidence, and changelog evidence. This changes public Guest Feedback target-ID admission and source-gate coverage only; it does not change Firestore rules/indexes, Cloud Functions, Firebase deployment, Vercel deployment, browser/device QA, production-host smoke, launch approval, or release certification.

Menu Extraction project ID boundary checkpoint: fixed in source. The protected owner upload job route, authenticated menu-link import route, and menu-intake identity preflight route keep the same `withAuth()` admission, SAFE_MODE checks, tenant/store session access checks, hashed rate limits, bounded body readers, project ownership checks, Storage URL allowlists, active/completed job reuse, identity preflight, menu-link artifact/job writes, and fixed owner copy, but malformed `projectId` values can no longer reach `projects/{tId}/{sId}/{projectId}` reads, `menuLinkImports/{tId}/{sId}/{projectId}/...` Storage path construction, or identity preflight context loading. `src/lib/menu-extraction/projectIdBoundary.ts` now validates owner-supplied menu-extraction project IDs through the existing bounded project-ID character rule plus `src/lib/firebase/firestoreDocumentId.ts` before those downstream reads/writes/paths. `npm run verify:menu-extraction-pipeline` source-gates the helper, all three route schemas, loose project-ID schema removal, implementation/Firebase docs, audit evidence, and changelog evidence. Follow-up gates passed after this checkpoint patch: `node --check scripts/verification/verify-menu-extraction-pipeline.js`, `npm run verify:menu-extraction-pipeline`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, and focused `git diff --check`. This changes protected owner menu-extraction request admission and source-gate coverage only; it does not change valid project IDs, valid owner upload extraction, valid menu-link import, valid identity preflight, Firestore read/write counts for valid requests, Storage writes for valid menu-link imports, Firestore rules/indexes, Cloud Functions, Firebase deployment, Vercel deployment, browser/device QA, provider smoke, production-host smoke, launch approval, or release certification.

Public Analytics Project ID Boundary checkpoint: fixed in source. The public analytics route keeps the same public IP limiter, 64KB bounded body reader, tenant/store numeric validation, update-field caps, cached target validation, inactive/deleted/platform-blocked store rejection, tenant-block rejection, reserved `obp` and `customerApp` surface behavior, analytics preference filtering, trusted business-day window, Admin SDK daily-counter write, bounded diagnostics, and fixed public responses, but malformed or whitespace-mutated `projectId` values can no longer reach target validation or `analytics/{tenantId}_{storeId}_{projectId}_daily_{date}` writes. `/api/public/analytics/track` now validates raw `projectId` with the existing analytics character rule plus `src/lib/firebase/firestoreDocumentId.ts` before cached store/tenant/project target reads or daily analytics writes; whitespace-mutated project IDs fail during request validation instead of being trimmed. `npm run verify:menulist-api-tenant-safety` source-gates the shared document-ID helper usage, old trim-based/regex-only project-ID schema removal, Analytics docs, audit evidence, and changelog evidence. Follow-up gates passed after this checkpoint patch: `node --check scripts/verification/verify-menulist-api-tenant-safety.js`, `npm run verify:menulist-api-tenant-safety`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, and focused `git diff --check`. This changes public analytics request admission and source-gate coverage only; it does not change valid project IDs, valid reserved `obp`/`customerApp` analytics, valid public menu analytics, analytics preferences, Firestore write counts for valid events, Firestore rules/indexes, Cloud Functions, Firebase deployment, Vercel deployment, browser/device QA, production-host smoke, launch approval, or release certification.

Public Analytics Tenant/Store Document ID Boundary checkpoint: fixed in source. The public analytics route keeps the same public IP limiter, 64KB bounded body reader, update-field caps, cached target validation, inactive/deleted/platform-blocked store rejection, tenant-block rejection, reserved `obp` and `customerApp` surface behavior, analytics preference filtering, trusted business-day window, Admin SDK daily-counter write, bounded diagnostics, and fixed public responses for valid tenant/store IDs, but malformed, reserved, whitespace-mutated, path-shaped, decimal, zero, negative, unsafe, nonnumeric, or scientific-notation tenant/store IDs can no longer reach target reads or daily analytics writes. `/api/public/analytics/track` now normalizes `tenantId` and `storeId` through `normalizePublicAnalyticsNumericDocumentId()` after bounded body/schema validation and before `stores/{storeId}`, `tenants/{tenantId}`, `platformSummary/projects_{storeId}`, or `analytics/{tenantId}_{storeId}_{projectId}_daily_{date}` refs are built. `npm run verify:menulist-api-tenant-safety` source-gates the normalizer, exact positive safe-integer document-ID guard, normalized tenant/store assignments, raw `String(data.tenantId)` / `String(data.storeId)` exclusions, Analytics docs, audit evidence, and changelog evidence. This changes public analytics tenant/store document-ID admission only; it does not change valid public menu, OBP, or Customer App analytics events, valid reserved analytics surfaces, analytics preferences, Firestore write counts for valid events, Firestore rules/indexes, Cloud Functions, Firebase deployment, Vercel deployment, browser/device QA, provider smoke, production-host smoke, launch approval, or release certification.

Public Analytics Shared Write Helper Boundary checkpoint: fixed in source. The exported `writePublicAnalyticsEventAdmin()` helper keeps the same valid daily analytics write shape, field filtering, increment handling, customer scope, surface detection, timezone/cutoff fields, and merge semantics, but future callers can no longer bypass route-level scope admission and build `analytics/{docId}` from raw helper inputs. `src/lib/analytics/serverWrite.ts` now validates tenant/store scope through `normalizePublicAnalyticsWriteScopeDocumentId()`, validates project IDs through `normalizePublicAnalyticsWriteProjectId()`, validates canonical `YYYY-MM-DD` date keys through `normalizePublicAnalyticsWriteDateKey()`, builds doc IDs from `tenantDocumentId`, `storeDocumentId`, `analyticsProjectId`, and `analyticsDateKey`, and stores normalized `tId`, `sId`, `projectId`, and `localDate` values. `npm run verify:menulist-api-tenant-safety` source-gates the helper normalizers, exact positive safe-integer document-ID guard, project/date guards, normalized doc ID construction, normalized stored scope fields, raw doc ID and raw stored scope exclusions, Analytics docs, audit evidence, and changelog evidence. This changes shared public analytics write-helper admission only; it does not change valid public menu, OBP, or Customer App analytics events, valid reserved analytics surfaces, analytics preferences, Firestore write counts for valid events, Firestore rules/indexes, Cloud Functions, Firebase deployment, Vercel deployment, browser/device QA, provider smoke, production-host smoke, launch approval, or release certification.

POS Delivery Project ID Boundary checkpoint: fixed in source. The protected POS delivery route keeps the same POS feature flag, `withAuth()` admission, store permission checks, 8KB bounded body reader, tenant access verification, hashed store rate limit, target-store permission recheck, public HTTPS/DNS webhook target validation, scoped project read, atomic menu-version write, signed full-menu payload, delivery log write, status/failure-counter update, bounded diagnostics, and fixed owner-safe provider copy, but malformed or whitespace-mutated `projectId` values can no longer reach `projects/{tenantId}/{storeId}/{projectId}` reads. `/api/pos-sync/deliver` now validates raw `projectId` with the existing POS project-ID character rule plus `src/lib/firebase/firestoreDocumentId.ts` before store config reads, scoped project reads, menu-version writes, delivery logs, POS status writes, or outbound fetches. `npm run verify:pos-sync-boundary` and `npm run verify:menulist-api-tenant-safety` source-gate the shared document-ID helper usage, old trim-based/loose project-ID schema removal, POS implementation/Firebase docs, audit evidence, and changelog evidence. Follow-up gates passed after this checkpoint patch: `node --check scripts/verification/verify-pos-sync-boundary.js`, `npm run verify:pos-sync-boundary`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, and focused `git diff --check`. This changes POS delivery request admission and source-gate coverage only; it does not change valid project IDs, valid POS delivery behavior, webhook signing, delivery logs, status writes, Firestore rules/indexes, Cloud Functions, Firebase deployment, Vercel deployment, external provider smoke, browser/device QA, production-host smoke, launch approval, or release certification.

Platform Entity Blocks Entity ID Boundary checkpoint: fixed in source. The platform-only entity block route keeps the same feature flag, `withPlatformAuth()` admission, 64KB bounded body reader, tenant/store/user block writes, tenant-to-store block mirroring, Firebase Auth disable/token-revoke handling, public cache invalidation, Business Health packet invalidation, desktop/mobile acknowledgement behavior, and fixed platform copy, but malformed string `entityId` values can no longer reach `tenants`, `stores`, or `users` document reads/writes. `/api/platform/entity-blocks` now validates string entity IDs through `src/lib/firebase/firestoreDocumentId.ts` and accepts numeric IDs only when finite before `getEntityDocRef()` calls `.doc(String(entityId))`. `npm run verify:platform-entity-blocks-boundary` source-gates the shared document-ID helper usage, old slash-only guard removal, Ops docs, audit evidence, and changelog evidence. Follow-up gates passed after this checkpoint patch: `node --check scripts/verification/verify-platform-entity-blocks-boundary.js`, `npm run verify:platform-entity-blocks-boundary`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, and focused `git diff --check`. This changes platform entity-block request admission and source-gate coverage only; it does not change valid tenant/store/user block behavior, valid numeric IDs, Firebase Auth sync, public cache invalidation, Firestore rules/indexes, Cloud Functions, Firebase deployment, Vercel deployment, browser/device QA, production-host smoke, launch approval, or release certification.

Auth change-password user document boundary checkpoint: fixed in source. The authenticated password-change route keeps the same `withAuth()` admission, hashed `AUTH_SENSITIVE` rate limit, 2KB bounded body reader, current-password verification against Firebase Auth REST with manual redirect handling, password-provider check, Firebase Auth password update, bounded auth/security diagnostics, and fixed account copy, but malformed session user IDs can no longer reach Firebase password verification or the direct `users/{userId}` timestamp update. `/api/auth/change-password` now validates the session user ID through `src/lib/firebase/firestoreDocumentId.ts` before rate limiting, request-body parsing, provider calls, or Firestore writes, returning generic unauthenticated copy plus fixed bounded diagnostics for malformed session IDs. `npm run verify:auth-security-failure-matrix` source-gates the shared document-ID helper usage, guard ordering, Auth Firebase docs, audit evidence, and changelog evidence. Follow-up gates passed after this checkpoint patch: `node --check scripts/verification/verify-auth-security-failure-matrix.js`, `npm run verify:auth-security-failure-matrix`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes authenticated change-password session user-ID admission and source-gate coverage only; it does not change valid password changes, valid Firebase Auth UID-backed user docs, current-password verification behavior, Firebase Auth password updates, account UI behavior, Firestore rules/indexes, Cloud Functions, Firebase deployment, Vercel deployment, browser/device QA, production-host smoke, launch approval, or release certification.

Reseller Management Profile ID Boundary checkpoint: fixed in source. The platform-only reseller management route keeps the same reseller dashboard feature gate, `PLATFORM` admission, hashed `DATA_WRITE` rate limit, 16KB bounded body reader, create/update schema split, email/domain validation, reseller uniqueness checks, Firebase Auth reseller login sync, profile merge, bounded diagnostics, and desktop/mobile acknowledgement behavior, but malformed or whitespace-mutated update `profileId` values can no longer reach reseller profile lookup, Firebase Auth lookup, or `resellerProfiles` document merge work. `/api/reseller/manage` now validates the exact raw update `profileId` through `src/lib/firebase/firestoreDocumentId.ts` before `getResellerProfileById(profileId)` and the later `resellerProfiles/{profileId}` merge. `npm run verify:reseller-dashboard-boundary` source-gates the shared document-ID helper usage, old trim/slash-only guard removal, implementation/Firebase docs, audit evidence, and changelog evidence. Follow-up gates passed after this checkpoint patch: `node --check scripts/verification/verify-reseller-dashboard-boundary.js`, `npm run verify:reseller-dashboard-boundary`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes platform reseller-management update admission and source-gate coverage only; it does not change valid Firebase Auth UID-backed reseller profile IDs, valid reseller profile reads/writes, Firebase Auth account sync, monthly summary reads, route permissions, Firestore rules/indexes, Cloud Functions, Firebase deployment, Vercel deployment, browser/device QA, production-host smoke, launch approval, or release certification.

Answerlattice Widget Feedback Search History ID Boundary checkpoint: fixed in source. The public widget feedback route keeps the same Answerlattice widget feature gate, API-key admission, hashed rate limit, product/purpose/scope checks, tenant/store context validation, origin allowlist, 2KB bounded body reader, tenant/store-scoped `aiSearchHistory` ownership check, widget-row guard, feedback merge, optional negative-feedback signal emission, bounded diagnostics, and fixed widget response copy, but malformed `searchHistoryId` values can no longer reach the admin `aiSearchHistory` document read or merge. `src/lib/answerlattice/searchHistoryIdBoundary.ts` now normalizes request-supplied search-history IDs through the shared Firestore document-ID guard before `src/app/api/widget/feedback/route.ts` calls `.doc(searchHistoryId)`. `npm run verify:answerlattice-runtime-truth` source-gates the helper, route schema, ordering before search-history document access, Feedback System docs, Answerlattice data inventory evidence, audit evidence, and changelog evidence. Follow-up gates passed after this checkpoint patch: `node --check scripts/verification/verify-answerlattice-runtime-truth.js`, `npm run verify:answerlattice-runtime-truth`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes public widget feedback search-history ID admission and source-gate coverage only; it does not change valid Firestore auto-ID search history rows, widget search output, valid feedback writes, negative-feedback signal metadata, Firestore rules/indexes, Cloud Functions, Firebase deployment, Vercel deployment, browser/device QA, production-host smoke, launch approval, or release certification.

Answerlattice Draft Regeneration Governance ID Boundary checkpoint: fixed in source. The protected manual draft-regeneration route keeps the same `withAuth()` admission, Answerlattice workspace scope, safe mode check, AI-operation rate limit, `MANAGE_GOVERNANCE` permission check, 4KB bounded body reader, proposal tenant/store validation, related-entity tenant/store validation, grounding queries, provider call, AI operation logging, proposal update, and fixed owner-safe errors, but malformed proposal IDs and malformed persisted related entity IDs can no longer reach `answerlattice_mutationProposals` or `answerlattice_entities` document reads. `src/lib/answerlattice/governanceIdBoundary.ts` now normalizes mutation proposal and entity document IDs through the shared Firestore document-ID guard before `src/app/api/answerlattice/mutation-proposals/regenerate-draft/route.ts` reads either document. `npm run verify:answerlattice-runtime-truth` source-gates the helper, route schema, related-entity normalizer, ordering before proposal/entity document access, Answerlattice data inventory evidence, audit evidence, and changelog evidence. Follow-up gates passed after this checkpoint patch: `node --check scripts/verification/verify-answerlattice-runtime-truth.js`, `npm run verify:answerlattice-runtime-truth`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes protected Answerlattice draft-regeneration proposal/entity ID admission and source-gate coverage only; it does not change valid Firestore auto-ID proposals, valid deterministic Knowledge Intake proposal IDs, valid entity IDs, draft prompt construction, grounding queries, AI accounting behavior, mutation proposal update behavior, Firestore rules/indexes, Cloud Functions, Firebase deployment, Vercel deployment, browser/device QA, production-host smoke, launch approval, or release certification.

Answerlattice KB Article ID Boundary checkpoint: fixed in source. The protected article embedding, article entity extraction, FAQ-from-article, article translation, and authenticated public-content article-read routes keep the same Answerlattice feature gates, `withAuth()` admission, workspace access checks, safe mode checks, rate limits before permission/body/provider work, bounded body readers, scoped article existence checks, tenant/store validation, prompt caps, provider response caps, AI operation logging, cache/version behavior, tenant/store-scoped cached article reads, and fixed owner-safe errors, but malformed KB article IDs can no longer reach direct `kb_articles` document reads or writes. `src/lib/answerlattice/kbArticleIdBoundary.ts` now normalizes request-supplied article IDs through the shared Firestore document-ID guard before `src/app/api/helpCenter/article-embedding/route.ts` calls `.doc(embeddingPayload.articleId)`, `src/app/api/answerlattice/articles/extract-entities/route.ts` calls `.doc(article.id)`, `src/app/api/answerlattice/faqs/generate-from-article/route.ts` calls `.doc(articleId)`, `src/app/api/answerlattice/translate/route.ts` reads and updates article translations, or `src/lib/answerlattice/publicContentCache.ts` reads cached article detail for `src/app/api/answerlattice/public-content/route.ts`. `npm run verify:answerlattice-runtime-truth` source-gates the helper, route schemas, cache helper boundary, ordering before article document access, Help Center Firebase docs, Answerlattice data inventory evidence, audit evidence, and changelog evidence. Follow-up gates passed after this checkpoint patch: `node --check scripts/verification/verify-answerlattice-runtime-truth.js`, `npm run verify:answerlattice-runtime-truth`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes Answerlattice KB article ID admission and source-gate coverage only; it does not change valid Firestore auto-ID articles, valid deterministic Knowledge Intake article IDs, article embedding, entity extraction, FAQ suggestion generation, article translation output, authenticated public-content cache shape, AI accounting behavior, cache/version bumps, Firestore rules/indexes, Cloud Functions, Firebase deployment, Vercel deployment, browser/device QA, production-host smoke, launch approval, or release certification.

Menu Extraction retry job ID boundary checkpoint: fixed in source. The protected owner extraction job route keeps the same `withAuth()` admission, SAFE_MODE check, tenant/store session access check, hashed request and AI-expensive rate-limit keys, 128KB bounded JSON body reader, project ownership check, owner-upload Storage URL allowlist, menu-link retry source preservation, failed-job ownership verification, active/completed job reuse, pre-AI project-size gate, identity preflight, job creation write, bounded diagnostics, and fixed owner retry copy, but malformed `retriedFromJobId` values can no longer reach the original `menuImageProcessingJobs` document read. `src/lib/menu-extraction/jobIdBoundary.ts` now accepts only Firestore auto-ID shaped job IDs and rejects path-shaped or reserved document IDs through the shared Firestore document-ID guard. `src/app/api/menu-extraction/jobs/route.ts` applies that boundary in the Zod request schema, and `loadRetryContext()` repeats the same normalizer before reading `.doc(retriedFromJobId)`, excluding `.doc(params.retriedFromJobId)`. `npm run verify:menu-extraction-pipeline` and `npm run verify:menulist-api-tenant-safety` source-gate the helper, schema usage, route ordering before the original-job read, implementation/Firebase docs, audit evidence, and changelog evidence. Follow-up gates passed after this checkpoint patch: `node --check scripts/verification/verify-menu-extraction-pipeline.js`, `node --check scripts/verification/verify-menulist-api-tenant-safety.js`, `npm run verify:menu-extraction-pipeline`, `npm run verify:menulist-api-tenant-safety`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes protected owner extraction retry job ID admission and source-gate coverage only; it does not change valid Firestore auto-ID job retries, valid owner upload job creation, public create-menu jobs, messaging onboarding jobs, menu-link retry source preservation, Firestore rules/indexes, Cloud Functions, Firebase deployment, Vercel deployment, browser/device QA, production-host smoke, launch approval, or release certification.

Answerlattice Knowledge Intake ID boundary checkpoint: fixed in source. The protected Knowledge Intake job, source, media, analyze, review-item, and publish routes keep the same Answerlattice feature gates, `withAuth()` admission, workspace access checks, active beta/subscription checks, route-specific rate limits, bounded body readers, owner-safe errors, fixed failure diagnostics, media credit ledgers, analysis caps, review updates, publish idempotency, runtime cache/source-version updates, and client response validation, but malformed job or review-item IDs can no longer reach protected job/review document reads or provider-backed/mutating intake work. `src/lib/answerlattice/knowledgeIntakeIdBoundary.ts` now accepts only Firestore auto-ID shaped intake job IDs and deterministic `kii_` review item IDs through the shared Firestore document-ID guard. Job routes normalize `params.jobId` before `getKnowledgeIntakeBundle()`, `addKnowledgeSource()`, `processKnowledgeIntakeMediaSource()`, `analyzeKnowledgeIntakeJob()`, and `publishKnowledgeIntakeJob()`, while the review-item route normalizes both `params.jobId` and `params.itemId`, and publish body `itemIds` use the same review-item boundary. `npm run verify:answerlattice-runtime-truth` source-gates the helper, route ordering before protected DAL calls, publish item ID schema, implementation/Firebase docs, audit evidence, and changelog evidence. Follow-up gates passed after this checkpoint patch: `node --check scripts/verification/verify-answerlattice-runtime-truth.js`, `npm run verify:answerlattice-runtime-truth`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes protected Knowledge Intake route/body ID admission and source-gate coverage only; it does not change valid job IDs, valid `kii_` review item IDs, valid source/media/analyze/review/publish behavior, Firestore rules/indexes, Cloud Functions, Firebase deployment, Vercel deployment, browser/device QA, production-host smoke, launch approval, or release certification.

Answerlattice Knowledge Intake shared service ID boundary checkpoint: fixed in source. The same `src/lib/answerlattice/knowledgeIntakeIdBoundary.ts` boundary now includes deterministic `kis_` source IDs, and the shared `src/lib/answerlattice/knowledgeIntake.ts` job/source/review ref helpers normalize-or-throw before direct Firestore document refs. Exported Knowledge Intake service functions thread normalized job IDs through bundle reads, source creation, media extraction, analysis, review updates, publish selection, counter refreshes, summaries, and publish failure recovery; selected publish `itemIds` are normalized before review-item refs. This is a helper-level hardening layer under the already-normalized routes and does not change valid job IDs, valid `kis_` source IDs, valid `kii_` review item IDs, source/media/analyze/review/publish behavior, Firestore rules/indexes, Cloud Functions, Firebase/Vercel deploys, production builds, browser/device QA, launch approval, or release certification.

Owner Business Assistant thread ID boundary checkpoint: fixed in source. The protected Business Health answer and thread routes keep the same feature flags, `withAuth()` admission, hashed owner/tenant/store rate-limit keys, selected-store scope checks, `VIEW_ANALYTICS` permission checks, bounded answer body reader, bounded response parsing, compact thread retention, answer-event logging, and fixed owner-safe errors, but malformed or reserved thread IDs can no longer reach `ownerBusinessAssistantThreads` document reads/writes. `src/lib/ownerBusinessAssistant/threadIdBoundary.ts` now accepts only browser-generated `oba_` runtime thread IDs and rejects path-shaped or reserved Firestore document IDs. The answer request schema, thread route params schema, and thread persistence path use the shared guard; the browser answer hook discards malformed localStorage thread IDs and creates a fresh `oba_` ID before sending new answer requests. `npm run verify:owner-business-assistant`, `npm run verify:owner-business-health-boundary`, and `npm run verify:menulist-api-tenant-safety` source-gate the helper, route ordering, schema/writer/hook usage, docs, audit evidence, and changelog evidence. Follow-up gates passed after this checkpoint patch: `node --check scripts/verification/verify-owner-business-health-boundary.js`, `npm run verify:owner-business-assistant`, `npm run verify:owner-business-health-boundary`, `npm run verify:menulist-api-tenant-safety`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes protected Business Health thread ID admission and source-gate coverage only; it does not change valid `oba_` thread IDs, valid answer behavior, valid thread reads/writes, answer-event logging, feedback behavior, Firestore rules/indexes, Cloud Functions, Firebase deployment, Vercel deployment, browser/device QA, production-host smoke, launch approval, or release certification.

AI Menu Manager server DAL ID boundary checkpoint: fixed in source. The protected AMM fallback routes keep their existing route-edge validation, selected-store scope checks, permission checks, rate limits, bounded body parsing, proposal tenant/store/project checks, and fixed owner errors, but future internal callers and malformed stored proposal summaries can no longer build AMM session, proposal, or selected-project Firestore refs with unnormalized IDs. `src/database/aiMenuManager/server.ts` now normalizes session IDs, proposal IDs, and selected project IDs before building session, proposal, scoped project, or legacy project document refs; command persistence throws before invalid refs, read helpers return `null` for malformed IDs, proposal mutation paths skip invalid stored session refs, and inbox hydration filters stored malformed proposal-summary IDs before proposal reads. `npm run verify:ai-menu-manager` and `npm run verify:menulist-api-tenant-safety` source-gate the server DAL helper usage, direct `.doc(sessionId)` / `.doc(proposalId)` / `.doc(params.projectId)` exclusions, implementation/Firebase docs, audit evidence, and changelog evidence. This changes AMM server DAL ID admission and source-gate coverage only; it does not change valid deterministic AMM session/proposal IDs, valid selected project IDs, valid fallback route behavior, valid proposal approvals/completions, Firestore read/write counts for valid requests, Firestore rules/indexes, Cloud Functions, Firebase deployment, Vercel deployment, browser/device QA, production-host smoke, launch approval, or release certification.

AI Menu Manager route ID boundary checkpoint: fixed in source. The protected AMM fallback routes keep the same feature flag, `withAuth()` admission, selected-store scope checks, `MANAGE_MENU` permission checks, hashed AMM rate-limit keys, bounded JSON body readers, proposal tenant/store/project/action checks, stale-card checks, and fixed owner errors, but malformed route/query IDs can no longer reach AMM session, proposal, or selected-project document reads. `src/lib/ai-menu-manager/routeIds.ts` now accepts only deterministic `amm_` session IDs, deterministic `amm_prop_` proposal IDs, and simple bounded Firestore document IDs for selected projects. `/api/ai-menu-manager/sessions/[sessionId]` applies the session/project guards before `getAiMenuManagerInbox()`, proposal action/completion routes apply the proposal guard before `getAiMenuManagerProposal()`, and AMM command/inbox schemas use the same session/project guards for supplied fallback IDs. `npm run verify:ai-menu-manager` and `npm run verify:menulist-api-tenant-safety` source-gate the shared AMM route ID helper, route usage order, schema usage, docs, audit evidence, and changelog evidence. Follow-up gates passed after this checkpoint patch: `node --check scripts/verification/verify-ai-menu-manager.js`, `node --check scripts/verification/verify-menulist-api-tenant-safety.js`, `npm run verify:ai-menu-manager`, `npm run verify:menulist-api-tenant-safety`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes protected AMM fallback route/query admission and source-gate coverage only; it does not change valid deterministic AMM session/proposal IDs, valid selected project IDs, client-DAL compact session behavior, valid proposal approvals/completions, Firestore rules/indexes, Cloud Functions, Firebase deployment, Vercel deployment, browser/device QA, production-host smoke, launch approval, or release certification.

Answerlattice widget activity timestamp boundary checkpoint: fixed in source. The protected `/api/answerlattice/widget-activity` dashboard route keeps the same feature flag, shared dashboard read limiter, `MANAGE_WIDGET` permission check, Answerlattice tenant/store scope, indexed widget search-history query, tenant-scoped fallback scan, bounded response fields, and bounded diagnostics, but stored `createdOn` / `modifiedOn` values can no longer be parsed through permissive `Date.parse(String(value))` when fallback rows are sorted or `createdAt` is serialized. The route now accepts Firestore Timestamp-like values, Date/finite numeric values, and canonical ISO `...Z` strings that round-trip through `toISOString()`; malformed stored strings return `null` or sort as oldest. `npm run verify:answerlattice-runtime-truth` source-gates the canonical timestamp pattern, round-trip normalizer, route usage, old permissive parser exclusions, Help Widget docs, audit evidence, and changelog evidence. Follow-up gates passed after this checkpoint patch: `node --check scripts/verification/verify-answerlattice-runtime-truth.js`, `npm run verify:answerlattice-runtime-truth`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes protected dashboard serialization/sort fallback behavior and source-gate coverage only; it does not change valid widget activity reads, public widget search behavior, search-history writes, Firestore rules/indexes, Cloud Functions, Firebase deployment, Vercel deployment, browser/device QA, production-host smoke, launch approval, or release certification.

Auth claim-token lookup boundary checkpoint: fixed in source. The public claim-preview and claim-account routes keep the same `AUTH_SENSITIVE` hashed-IP limiter, bounded claim-account body reader, one-time token transaction guard, expiry handling, masked validate-claim preview response, login-page bounded response parsing, and fixed owner copy, but malformed or oversized claim-token values can no longer reach the indexed `users.claimToken` query. `src/lib/auth/claimTokenBoundary.ts` now caps claim tokens to 20-256 base64url/hex-safe characters, `/api/auth/validate-claim` applies that guard to the `token` query before Firestore reads, and `/api/auth/claim-account` applies the same guard before lookup and mode-specific body validation. `npm run verify:auth-security-failure-matrix` source-gates the shared claim-token helper, validate-claim usage, claim-account schema/manual guard usage, old loose length-check exclusions, Auth docs, audit evidence, and changelog evidence. Follow-up gates passed after this checkpoint patch: `node --check scripts/verification/verify-auth-security-failure-matrix.js`, `npm run verify:auth-security-failure-matrix`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes public claim-token lookup admission and source-gate coverage only; it does not change valid claim links, valid account-claim modes, token expiry behavior, Firebase Auth user creation/linking, tenant/store email sync, public cache revalidation, Firestore rules/indexes, Cloud Functions, Firebase deployment, Vercel deployment, browser/device QA, production-host smoke, launch approval, or release certification.

Auth claim-account tenant/store scope boundary checkpoint: fixed in source. The public claim-account route keeps the same `AUTH_SENSITIVE` hashed-IP limiter, bounded body reader, claim-token lookup guard, one-time transaction guard, Mode 1 Google linking, Mode 2 email/password setup, Mode 3 WhatsApp phone/passcode setup, fixed owner copy, subscription relinking, tenant/store email sync, public cache revalidation, and custom-claim minting, but raw tenant/store IDs from the messaging user can no longer reach Firebase Auth user mutation, tenant/store document refs, subscription queries, cache tags, custom claims, or success acknowledgement. `/api/auth/claim-account` now normalizes claimed tenant/store IDs with the shared Firestore document ID guard and an exact positive numeric check before any Firebase Auth create/update path, then re-checks the same normalized scope inside the final claim transaction after re-reading the messaging user document. Tenant/store writes now use normalized document IDs, and cache revalidation, subscription relinking, custom claims, and response identity use the normalized numeric scope. `npm run verify:auth-security-failure-matrix` source-gates the normalizer, exact numeric guard, pre-provider admission order, transaction re-check, raw `currentMessagingUser` ref exclusions, Auth docs, audit evidence, and changelog evidence. Follow-up gates passed after this checkpoint patch: `node --check scripts/verification/verify-auth-security-failure-matrix.js`, `npm run verify:auth-security-failure-matrix`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes public claim-account tenant/store scope admission and source-gate coverage only; it does not change valid claim links, valid claim-token lookup, valid account-claim modes, token expiry behavior, Firebase Auth provider semantics for valid claims, tenant/store email sync for valid claims, public cache revalidation for valid claims, Firestore rules/indexes, Cloud Functions, Firebase deployment, Vercel deployment, browser/device QA, production-host smoke, launch approval, or release certification.

Messaging preview session route-param boundary checkpoint: fixed in source. The unauthenticated `/api/msg-preview/[sessionId]`, `/approve`, and `/fix` token-link routes keep the same preview-token checks, hashed session/IP rate-limit keys, bounded JSON body readers, publish transaction behavior, fix-request correction limits, bounded runtime diagnostics, and fixed owner copy, but malformed or whitespace-mutated route `sessionId` values can no longer reach Firestore session document reads, publish transaction reads, event writes, or fix-request writes. `src/lib/messaging-onboarding/previewRouteBoundary.ts` now accepts only the Firestore auto-ID session shape generated by the Functions session engine and also rejects whitespace-mutated, path-shaped, or reserved Firestore document IDs through exact raw-value comparison plus the shared document-ID helper. `src/lib/messaging-onboarding/publish.ts` now applies the same normalizer before publish lifecycle event writes or session document access, so helper-level callers fail closed before publish Firestore work. `npm run verify:menu-extraction-pipeline` and `npm run verify:menulist-api-tenant-safety` source-gate the shared normalizer, route usage order before rate-limit/doc paths, publish helper usage order before event writes/session docs, old loose `sessionId.length < 10` exclusion, Messaging Onboarding docs, audit evidence, and changelog evidence. Follow-up gates passed after this checkpoint patch: `node --check scripts/verification/verify-menu-extraction-pipeline.js`, `node --check scripts/verification/verify-menulist-api-tenant-safety.js`, `npm run verify:menu-extraction-pipeline`, `npm run verify:menulist-api-tenant-safety`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes token-link preview route-param/helper admission and source-gate coverage only; it does not change valid preview links, valid token comparison, publish/fix behavior, Firestore rules/indexes, Cloud Functions, Firebase deployment, Vercel deployment, browser/device QA, production-host smoke, launch approval, or release certification.

Auth access-status entity reference boundary checkpoint: fixed in source. The authenticated `/api/auth/access-status` route keeps the same `withAuth()` admission, shared `DATA_READ` rate-limit, current-user lookup, user/tenant/store blocked checks, session revocation comparison, no-store JSON response, bounded security diagnostics, and browser-side bounded response parsing, but malformed user/tenant/store document references can no longer reach Firestore `doc()` as path-shaped or whitespace-mutated IDs and string revocation timestamps no longer use permissive `Date.parse()`. `src/app/api/auth/access-status/route.ts` now uses `src/lib/firebase/firestoreDocumentId.ts` plus exact raw-value comparison before direct user/tenant/store reads, returns fixed `TENANT_REFERENCE_INVALID` / `STORE_REFERENCE_INVALID` reasons for malformed entity references, fails malformed or whitespace-mutated session user IDs before user doc reads, and only accepts canonical ISO `...Z` string timestamps that round-trip through `toISOString()` for revocation math. `npm run verify:auth-security-failure-matrix` and `npm run verify:menulist-api-tenant-safety` source-gate the shared document-ID guard usage, strict raw document-ID equality, canonical timestamp guard, raw `Date.parse()` exclusion, raw `doc(String(...))` exclusions, Auth docs, audit evidence, and changelog evidence. Follow-up gates passed after this checkpoint patch: `node --check scripts/verification/verify-auth-security-failure-matrix.js`, `node --check scripts/verification/verify-menulist-api-tenant-safety.js`, `npm run verify:auth-security-failure-matrix`, `npm run verify:menulist-api-tenant-safety`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes authenticated access-status admission and source-gate coverage only; it does not change valid session polling, valid user/tenant/store block checks, valid session revocation behavior, browser sign-out behavior, Firestore writes/deletes, Firebase Auth operations, Firestore rules/indexes, Cloud Functions, Firebase deployment, Vercel deployment, browser/device QA, production-host smoke, launch approval, or release certification.

CampaignCue public website output/doc parity checkpoint: fixed in source. The CampaignCue public website keeps the same static product-site boundary, owner-readable homepage, feature pages, use-case page, product/use-case menus, manual handoff/export positioning, and no-provider/no-direct-posting claims, but local source gates no longer drift from current output and compression contracts. `src/app/sites/campaigncue/page.tsx` now names the `Email, SMS, and QR brief` and `Local creator brief` outputs in the public output ledger, and `__docs__/campaigncue/campaigncue-product/campaigncue-product_website.md` now records the simplified-homepage contract while routing deeper content to feature pages and use-case pages. `npm run verify:campaigncue` passes after checking the runtime website contract, pack template registry, and PWA assets. Follow-up gates passed after this checkpoint patch: `npm run verify:campaigncue`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes static CampaignCue website copy/doc parity and source-gate coverage only; it does not change CampaignCue owner workspace runtime, Firestore reads/writes/deletes, Storage operations, provider calls, direct posting/sending, social-account connection, payment behavior, Cloud Functions, Firebase deployment, Vercel deployment, browser/device QA, production-host smoke, launch approval, or release certification.

Platform notification event-id/action boundary checkpoint: fixed in source. The platform notification route keeps the same `withAuth()` platform-role admission, dashboard feature flag, manual refresh list model, severity/status/trigger filters, bounded action body, hashed per-operator action limiter, selected-alert detail read, explicit manual-alert creation, bounded browser response parsing, and fixed platform copy, but selected/action `eventId` values can no longer be path-shaped Firestore document paths and acknowledge/manual-handoff actions can no longer create partial alert documents for nonexistent alert IDs. `src/lib/firebase/firestoreDocumentId.ts` now rejects empty, slash-containing, `.`/`..`, and reserved `__*__` document IDs. `/api/ops/platform-notifications` uses that helper for GET detail and POST acknowledge/manual-handoff event IDs, and those two actions now update only existing alert documents, returning `404` for missing alerts. Platform notification boundary source gate: `npm run verify:platform-notifications-boundary` now checks the shared document-ID helper, route schema usage, existing-alert action update path, Ops Control Room docs, audit evidence, and source-only platform-notification registry/API/monitor/docs gate. Follow-up gates passed after this checkpoint patch: `node --check scripts/verification/verify-platform-notifications-boundary.js`, `npm run verify:platform-notifications-boundary`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes platform notification selected-event/action admission and source-gate coverage only; it does not change valid alert list reads, valid selected-alert reads, valid acknowledge/manual-handoff writes, valid manual alert creation, provider handoff behavior, Firestore rules/indexes, Cloud Functions, Firebase deployment, Vercel deployment, browser/device QA, production-host smoke, launch approval, or release certification.

Owner notification ops event-id boundary checkpoint: fixed in source. The owner-notification ops route keeps the same `withAuth()` platform-role admission, owner-notification feature gates, MenuList/Answerlattice product selection, manual refresh model, bounded action body, hashed per-operator action limiter, status filters, selected-event detail read, delivery detail query, retry/manual-send/manual-handoff behavior, bounded browser response parsing, and fixed platform copy, but selected/recovery `eventId` values are validated at route admission and re-normalized at the helper boundary before direct event reads, event-id delivery queries, retry processing, manual-send enqueueing, or manual-handoff writes. `/api/ops/owner-notifications` uses `src/lib/firebase/firestoreDocumentId.ts` for GET detail and POST retry/manualSend/manualHandoff event IDs; `getDetail()`, `loadRawEvent()`, `runManualSend()`, `recordManualHandoff()`, and POST action dispatch use local normalized IDs so path-shaped or reserved IDs cannot reach document path composition. Owner notification boundary source gate: `npm run verify:owner-notifications-boundary` now checks the shared document-ID helper, route schema usage, helper-level normalization, raw-ref exclusions, Owner Notifications docs, audit evidence, and source-only owner-notification registry/API/processor/monitor/docs gate. Follow-up gates passed after this checkpoint patch: `node --check scripts/verification/verify-owner-notifications-boundary.js`, `npm run verify:owner-notifications-boundary`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes owner-notification ops selected-event/action admission and source-gate coverage only; it does not change valid event reads, valid delivery detail reads, valid retry/manual-send/manual-handoff behavior, provider behavior, Firestore rules/indexes, Cloud Functions, Firebase deployment, Vercel deployment, browser/device QA, production-host smoke, launch approval, or release certification.

Reseller monthly-summary query boundary checkpoint: fixed in source. The reseller monthly-summary route keeps the same `withAuth()` admission, reseller dashboard feature flag, shared `DATA_READ` limiter, platform/reseller role separation, platform 50-profile cap, normal reseller direct/email profile lookup, reseller-scoped transaction filter, 2000 monthly transaction cap, bounded diagnostics, and desktop/mobile browser response parsing, but impossible explicit `month` query values no longer normalize into unintended Firestore ranges. Missing `month` still defaults to the current India month; explicit values must be calendar-valid `YYYY-MM` values from 2020 through 2100, and invalid explicit values return `400` before transaction/profile reads. `npm run verify:reseller-dashboard-boundary` source-gates the parser constants, invalid-month `400`, docs, audit evidence, and changelog evidence. Follow-up gates passed after this checkpoint patch: `node --check scripts/verification/verify-reseller-dashboard-boundary.js`, `npm run verify:reseller-dashboard-boundary`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes reseller monthly-summary query admission and source-gate coverage only; it does not change normal current-month dashboard loads, valid explicit month reads, reseller/profile authorization, Firestore writes/deletes, Storage operations, billing behavior, Razorpay calls, Cloud Functions, Firebase deployment, Vercel deployment, browser/device QA, production-host smoke, launch approval, or release certification.

AI operation transaction-history query boundary checkpoint: fixed in source. The MenuList and Answerlattice operation-history routes keep the same authenticated admission, tenant/store scope, shared read-rate limits, Answerlattice billing permission check, nested operation-ledger read paths, owner/platform response filtering, capped pagination, bounded action-scan windows, bounded diagnostics, and browser response envelopes, but loose cursor/date query values can no longer reach Firestore cursor/query calls. `src/lib/ai/operationHistoryQuery.ts` now accepts only simple Firestore document ID cursors that also pass the shared Firestore reserved/path guard, strict `YYYY-MM-DD` or browser ISO `...Z` date filters, ordered ranges, and ranges no wider than 366 days. `/api/ai-operations` and `/api/answerlattice/ai-operations` now use that helper before `startAfter()` and `createdOn` filters. `npm run verify:menulist-api-tenant-safety` source-gates the helper constants, shared Firestore document-ID guard, MenuList route import/usage, old permissive parser exclusions, AI System docs, audit evidence, and changelog evidence; `npm run verify:answerlattice-runtime-truth` source-gates the Answerlattice route usage and shared helper. Follow-up gates passed after this checkpoint patch: `node --check scripts/verification/verify-menulist-api-tenant-safety.js`, `npm run verify:menulist-api-tenant-safety`, `node --check scripts/verification/verify-answerlattice-runtime-truth.js`, `npm run verify:answerlattice-runtime-truth`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes authenticated operation-history query admission and source-gate coverage only; it does not change valid transaction-history reads, valid browser date-range requests, Firestore writes/deletes, Storage operations, AI accounting, provider calls, Cloud Functions, Firebase deployment, Vercel deployment, browser/device QA, production-host smoke, launch approval, or release certification.

Master job status project ID boundary checkpoint: fixed in source. The server-backed outlet editor master-job polling route keeps the same `withAuth()` admission, multi-outlet feature flag, shared `DATA_READ` limiter, tenant access check, `MANAGE_MENU` permission check, linked outlet project validation, capped active-job query, bounded diagnostics, and bounded browser response contract, but malformed `masterProjectId` and `outletProjectId` values can no longer reach project-scope parsing, linked outlet project reads, or active master-job queries. `/api/projects/master-job-status` now validates those IDs with the existing multi-outlet project-ID character rule plus `src/lib/firebase/firestoreDocumentId.ts` before downstream work. `npm run verify:menulist-api-tenant-safety` source-gates the shared document-ID helper usage, regex-only schema removal, multi-outlet docs, audit evidence, and changelog evidence. Follow-up gates passed after this checkpoint patch: `node --check scripts/verification/verify-menulist-api-tenant-safety.js`, `npm run verify:menulist-api-tenant-safety`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, and focused `git diff --check`. This changes authenticated master-job status query admission and source-gate coverage only; it does not change valid master-store polling, valid linked-outlet polling, store-link authorization, job status semantics, Firestore read counts for valid requests, Firestore writes/deletes, Storage operations, Cloud Functions, Firebase deployment, Vercel deployment, browser/device QA, production-host smoke, launch approval, or release certification.

Master job status store-link boundary checkpoint: fixed in source. The server-backed outlet editor master-job polling route keeps the same `withAuth()` admission, multi-outlet feature flag, shared `DATA_READ` limiter, tenant access check, `MANAGE_MENU` permission check, linked outlet project validation, capped active-job query, bounded diagnostics, and bounded browser response contract, but a store marked `isMaster` can no longer bypass outlet-link validation when the requested `masterProjectId` belongs to a different store. `src/app/api/projects/master-job-status/route.ts` now lets only the store encoded in `masterProjectId` query that master job directly; all other stores must present an `outletProjectId` linked to the requested master project before the job query runs. `npm run verify:menulist-api-tenant-safety` source-gates the explicit `sessionStoreId !== masterStoreId` branch, the old `sessionStore?.isMaster` bypass exclusion, multi-outlet docs, audit evidence, and changelog evidence. Follow-up gates passed after this checkpoint patch: `node --check scripts/verification/verify-menulist-api-tenant-safety.js`, `npm run verify:menulist-api-tenant-safety`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes authenticated master-job status route admission and source-gate coverage only; it does not change valid master-store polling, valid linked-outlet polling, job status semantics, Firestore writes/deletes, Storage operations, Cloud Functions, Firebase deployment, Vercel deployment, browser/device QA, production-host smoke, launch approval, or release certification.

ROI metrics query-parameter boundary checkpoint: fixed in source. The protected `/api/analytics/roi-metrics` route keeps the same `withAuth()` admission, `tId/sId` scope check, shared analytics read-rate limit, optimized chat-statistics read path, ROI calculation response shape, bounded route diagnostics, and browser response parser contract, but arbitrary numeric query strings can no longer become non-finite or extreme owner-facing ROI inputs. `src/app/api/analytics/roi-metrics/route.ts` now parses `days` with a strict digit guard capped to 1-90 days, ignores malformed `hourlyCost`, `clv`, and `platformCost` overrides instead of partially parsing them, and clamps valid money overrides to finite server caps before calling `calculateROI()`; money overrides use a strict decimal guard. `npm run verify:menulist-api-tenant-safety` source-gates the ROI days parser, money parser, route usage, old permissive `parseFloat()` / `parseInt()` exclusions, Answerlattice chat-monitoring docs, audit evidence, and changelog evidence. Follow-up gates passed after this checkpoint patch: `node --check scripts/verification/verify-menulist-api-tenant-safety.js`, `npm run verify:menulist-api-tenant-safety`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes authenticated ROI query-parameter admission and source-gate coverage only; it does not change valid ROI defaults, valid bounded overrides, chat analytics reads, Firestore writes/deletes, Storage operations, provider calls, Cloud Functions, Firebase deployment, Vercel deployment, browser/device QA, production-host smoke, launch approval, or release certification.

Google Analytics configured-store scope boundary checkpoint: fixed in source. The authenticated legacy Google Analytics dashboard routes keep the same `withAuth()`, `VIEW_ANALYTICS` permission, shared analytics read-rate limit, configured-property authorization, generic owner-facing errors, and report/realtime/menu/location response shapes for valid sessions, but malformed, reserved, path-shaped, whitespace-mutated, decimal, zero, negative, unsafe, or nonnumeric session tenant/store IDs can no longer reach the configured `stores/{storeId}` authorization read or provider calls. `src/lib/analytics/googlePropertyAccess.ts` now exposes `normalizeGoogleAnalyticsScopeDocumentId()`, validates session tenant/store scope before the store read, uses the normalized store document ID for `stores/{storeId}`, and keeps the normalized numeric IDs for tenant ownership comparison and bounded diagnostics. `npm run verify:menulist-api-tenant-safety` source-gates the helper, exact numeric document-ID guard, normalized store ref, raw `doc(String(storeId))` exclusion, Client Menu analytics docs, audit evidence, and changelog evidence. This changes authenticated owner Google Analytics configured-store scope admission only; it does not change valid configured-property access, Google Analytics property/date query admission, dashboard card rendering, owner analytics settings, internal MenuList analytics writes, public menu/OBP rendering, Firestore reads/writes/deletes for valid requests, Storage operations, Cloud Functions, Firebase deployment, Vercel deployment, browser/device QA, provider smoke, production-host smoke, launch approval, or release certification.

Owner Google Analytics provider-query boundary checkpoint: fixed in source. The authenticated legacy Google Analytics dashboard routes keep the same `withAuth()`, `VIEW_ANALYTICS` permission, shared analytics read-rate limit, configured-property authorization, generic owner-facing errors, and report/realtime/menu/location response shapes, but raw query date ranges and malformed property resources can no longer pass directly to Google Analytics Data API calls. `src/lib/analytics/googlePropertyAccess.ts` now accepts only numeric GA Data API property IDs, including `properties/{id}` inputs after suffix normalization. `src/lib/analytics/googleReportQuery.ts` normalizes saved dashboard quick preferences like `7days` to `7daysAgo`, accepts only `today`, `yesterday`, bounded `NdaysAgo`, and valid non-future `YYYY-MM-DD` dates, and rejects missing, reversed, future, or wider-than-366-day report ranges before provider calls. `npm run verify:menulist-api-tenant-safety` source-gates the property shape guard, date-range normalizer, menu/location/report route usage, raw date-range exclusion, Client Menu analytics docs, audit evidence, and changelog evidence. Follow-up gates passed after this checkpoint patch: `node --check scripts/verification/verify-menulist-api-tenant-safety.js`, `npm run verify:menulist-api-tenant-safety`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes authenticated owner Google Analytics provider-query admission and source-gate coverage only; it does not change valid configured-property access, dashboard card rendering, owner analytics settings, internal MenuList analytics writes, public menu/OBP rendering, Firestore reads/writes/deletes, Storage operations, Cloud Functions, Firebase deployment, Vercel deployment, browser/device QA, production-host smoke, launch approval, or release certification.

OBP aggregation map-key boundary checkpoint: fixed in source. Official Business Page analytics keeps the same live page-view, action-click, menu-click, external-link, share, language, UTM, dashboard-summary, weekly/monthly, and lifetime rollup behavior, but late-correction aggregation can no longer use recovered raw or legacy dotted map keys as Firestore field-path suffixes. `functions/src/analytics/obpAnalyticsAggregation.ts` now normalizes recovered daily and cached dashboard map keys to the same lowercase `[a-z0-9_-]` shape used by live analytics writes, sums duplicate normalized numeric keys, skips empty normalized keys, and re-checks the suffix immediately before writing `lifetime.*` dotted update paths, including `obpLanguageNames`. `npm run verify:public-business-truth` source-gates the OBP aggregation key normalizer, numeric duplicate folding, current/previous map normalization, old raw suffix assignment exclusions, Client Menu analytics docs, OBP docs, audit evidence, and changelog evidence. Follow-up gates passed after this checkpoint patch: `node --check scripts/verification/verify-public-business-truth.js`, `npm run verify:public-business-truth`, `npm --prefix functions run build`, `npm run verify:functions-deploy-preflight`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. The scoped QA deploy command `firebase deploy --project menulist-qa --config firebase.json --only functions:computeDecisionBlocksScores,functions:triggerDecisionBlocksScoring,functions:triggerStoreNightlyScheduler --non-interactive` completed predeploy lint/build and remains blocked before upload by Cloud Resource Manager HTTP 403: caller lacks permission for `menulist-qa`. This changes OBP late-correction Cloud Function aggregation behavior and source-gate coverage only; it does not change valid live OBP analytics writes, customer menu rendering, OBP rendering, owner settings, public cache behavior, Firestore collections/indexes/rules, Storage operations, Vercel deployment, browser/device QA, production-host smoke, launch approval, or release certification.

Customer App shortcut handoff URL boundary checkpoint: fixed in source. Customer App shortcut handoff pages keep the same valid call, directions, WhatsApp, reservation, order, shortcut source-attribution, store lookup, analytics preference, and same-origin handoff route behavior, but malformed serialized handoff props can no longer drive browser/OS navigation or noscript links from the client layer. `src/app/client/pwa/shortcutHandoffUrl.ts` now re-checks reservation/order targets as HTTPS URLs, directions targets as Google Maps HTTPS URLs, WhatsApp targets as `https://wa.me/{digits}`, and call targets as `tel:+{digits}` before any shortcut analytics write, `window.location.replace()`, or noscript fallback link. Invalid handoff props show the existing minimal handoff surface with a shortcut-unavailable message and create no redirect or shortcut analytics write. `npm run verify:customer-app-pwa` source-gates the shared handoff guard, each client-side safe URL admission check, old raw redirect/href exclusions, server-side URL normalization, Customer App implementation/Firebase docs, audit evidence, and changelog evidence. Follow-up gates passed after this checkpoint patch: `node --check scripts/verification/verify-customer-app-pwa.js`, `npm run verify:customer-app-pwa`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes Customer App browser-local handoff URL admission and source-gate coverage only; it does not change valid shortcut URLs, manifest shortcut construction, tenant/store lookup, owner settings, Customer App install identity, public menu/OBP rendering, Firestore reads/writes/deletes, Storage operations, Cloud Functions, cache invalidations, Firebase deployment, Vercel deployment, browser/device QA, production-host smoke, launch approval, or release certification.

MyCodex client navigation path boundary checkpoint: fixed in source. The private MyCodex reader keeps the same document tree, favorites, queue, recent-doc, continue-reading, previous/next, local-dev `/__mycodex`, document API, logout, and same-origin document-navigation behavior, but malformed browser-local reader targets can no longer produce protocol-relative `//...` hrefs or `window.location.href` handoffs. `MyCodexClientContainer.buildUrl()` now trims incoming targets, collapses empty values and protocol-relative targets to `/`, then applies the normal relative-path and local-dev prefix handling. `npm run verify:mycodex-pwa-assets` source-gates the client route-builder guard, old raw target-path shortcut exclusion, MyCodex implementation/Firebase docs, audit evidence, and changelog evidence. Follow-up gates passed after this checkpoint patch: `node --check scripts/verification/verify-mycodex-pwa-assets.js`, `npm run verify:mycodex-pwa-assets`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes MyCodex browser-local navigation path normalization and source-gate coverage only; it does not change valid reader paths, MyCodex document loading, login/session cookies, service-worker behavior, PWA assets, Firebase reads/writes/deletes, Storage operations, Cloud Functions, billing/product state, Firebase deployment, Vercel deployment, browser/device QA, production-host smoke, launch approval, or release certification.

Login callback redirect boundary checkpoint: fixed in source. The shared login page keeps the same supported same-origin `callbackUrl` handoff for MenuList, Answerlattice, CampaignCue, SignalDesk aliases, create-menu claim flows, and normal dashboard redirects, but protocol-relative `//...` and cross-origin callback targets can no longer pass through the post-login redirect path. `src/components/templates/loginPage/index.tsx` now parses the submitted `callbackUrl` against `window.location.origin`, requires the parsed origin to match the current origin, and returns only path/search/hash for `router.push()` or the claim-flow hard navigation; rejected targets fall back to the existing dashboard or Answerlattice subscription redirect logic. `npm run verify:auth-security-failure-matrix` source-gates the same-origin parser, path/search/hash return shape, old raw leading-slash shortcut exclusion, auth docs, audit evidence, and changelog evidence. Follow-up gates passed after this checkpoint patch: `node --check scripts/verification/verify-auth-security-failure-matrix.js`, `npm run verify:auth-security-failure-matrix`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes browser-local post-login redirect admission and source-gate coverage only; it does not change valid same-origin callback URLs, Google login, credential login, phone OTP login, claim-account setup, Firebase custom-token sync, Answerlattice custom-token sync, route auth/rate limits, Firestore reads/writes/deletes, Firebase Auth operations, Firestore rules, Storage rules, Cloud Functions, Firebase deployment, Vercel deployment, browser/device QA, production-host smoke, launch approval, or release certification.

Phone OTP challenge ID boundary checkpoint: fixed in source. The public `/api/auth/phone-otp/verify` route keeps the same feature flag, IP throttle, 1KB bounded body reader, fixed invalid-code copy, challenge-specific throttle, OTP transaction behavior, login-token creation, and NextAuth handoff, but malformed or whitespace-mutated `challengeId` values can no longer reach challenge-specific throttle keys, `authPhoneOtpChallenges/{challengeId}` document refs, OTP hash comparison, or login-token writes. `src/lib/auth/phoneOtp.ts` now exposes `normalizePhoneOtpChallengeId()`, accepts only raw Firestore auto-ID shaped challenge IDs generated by `createPhoneOtpChallenge()`, and rejects whitespace-mutated, path-shaped, or reserved Firestore document IDs through exact raw-value comparison plus the shared document-ID helper. `npm run verify:auth-security-failure-matrix` source-gates the route schema, helper normalizer, strict raw challenge-ID equality, raw `.doc(params.challengeId)` exclusion, Phone OTP docs, audit evidence, and changelog evidence. Follow-up gates passed after this checkpoint patch: `node --check scripts/verification/verify-auth-security-failure-matrix.js`, `npm run verify:auth-security-failure-matrix`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes public Phone OTP challenge-ID admission and source-gate coverage only; it does not change valid challenge creation, valid OTP verification, valid login-token consumption, WhatsApp delivery, NextAuth credentials login, Firebase Auth claim sync, Firestore rules/indexes, Cloud Functions, Firebase deployment, Vercel deployment, browser/device QA, production-host smoke, launch approval, or release certification.

Phone OTP user document-ID boundary checkpoint: fixed in source. Phone OTP existing-user profile updates, login-token `userId` writes, and consumed-token user comparisons keep the same feature flag, phone lookup order, WhatsApp OTP verification, deterministic first-time phone user creation, login-token creation, NextAuth handoff, and Firebase Auth claim sync for valid users, but malformed, reserved, empty, whitespace-mutated, path-shaped, or oversized resolved user IDs can no longer reach `users/{userId}` update refs or login-token trust comparisons. `src/lib/auth/phoneOtp.ts` now exposes `normalizePhoneOtpUserDocumentId()`, uses it before `.doc(existingUserId)`, stores normalized `dbUserId` into phone login tokens, and requires normalized token/resolved user IDs to match during token consumption. `npm run verify:auth-security-failure-matrix` source-gates the helper normalizer, normalized profile update ref, normalized token write, normalized consumed-token comparison, raw `.doc(String(dbUser.id))` exclusion, Phone OTP docs, audit evidence, and changelog evidence. This changes Phone OTP user document-ID admission only; it does not change valid challenge creation, valid OTP verification, valid login-token consumption, WhatsApp delivery, NextAuth credentials login, Firebase Auth claim sync, Firestore rules/indexes, Cloud Functions, Firebase deployment, Vercel deployment, browser/device QA, production-host smoke, launch approval, or release certification.

Set-claims workspace scope boundary checkpoint: fixed in source. `/api/auth/set-claims` keeps the same `withAuth()` admission, 2KB bounded optional body, product-specific user lookup, target-store membership check, UID/email match, Firebase custom-token creation, and Answerlattice custom-token sync for valid sessions, but malformed, reserved, whitespace-mutated, path-shaped, decimal, zero, negative, unsafe, or nonnumeric tenant/store IDs in an auth profile can no longer be minted into Firebase custom claims or used for Answerlattice store-role permission reads. `src/app/api/auth/set-claims/route.ts` now reuses `normalizeStorePermissionScopeDocumentId()`, resolves the selected claim store through `resolveClaimStoreScope()`, validates `claimTenantScope`, reads Answerlattice role permissions through `.doc(params.storeScope.documentId)`, writes `tenantId: claimTenantScope.documentId` and `storeId: claimStoreScope.documentId` into custom claims, and fails closed with bounded `set_claims_invalid_workspace_scope_rejected` diagnostics before custom-token creation when scope is invalid. `npm run verify:auth-security-failure-matrix` and `npm run verify:menulist-api-tenant-safety` source-gate the shared normalizer import, claim-store resolver, normalized tenant/store claim values, normalized Answerlattice store read, raw `.doc(String(params.storeId))` and `tenantId: String(dbUser.tenantId)` exclusions, auth docs, audit evidence, and changelog evidence. This changes set-claims workspace-scope admission only; it does not change valid Google login, credential login, phone OTP login, valid Firebase custom-token sync, valid Answerlattice custom-token sync, store-switch permission semantics, Firestore rules/indexes, Cloud Functions, Firebase deployment, Vercel deployment, browser/device QA, provider smoke, production-host smoke, launch approval, or release certification.

Customer menu UTM map-key boundary checkpoint: fixed in source. Customer menu and OBP analytics keep the same intentional `utm_source`, `utm_medium`, `utm_campaign`, and `utm_content` campaign attribution on the existing menu-view and OBP-view analytics writes, but public URL query values can no longer become raw Firestore map-key suffixes for `viewsBySource`, `viewsByMedium`, or `viewsByCampaign`. `src/lib/analytics/unified.ts` now normalizes all four UTM fields through the existing analytics map-key guard before writing `viewsBySource`, `viewsByMedium`, `viewsByCampaign`, or `viewsByContent`, and skips values that normalize to an empty key. `npm run verify:public-business-truth` source-gates the menu and OBP UTM map-key normalizers, old raw UTM map-key suffix exclusions, Client Menu analytics docs, OBP docs, audit evidence, and changelog evidence. Follow-up gates passed after this checkpoint patch: `node --check scripts/verification/verify-public-business-truth.js`, `npm run verify:public-business-truth`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes UTM analytics field-key normalization and source-gate coverage only; it does not change intentional UTM capture, `entry_source` attribution, analytics write counts, owner analytics settings, third-party pixel loading, customer menu rendering, OBP rendering, public cache behavior, Firestore collections, Storage operations, Cloud Functions, Firebase deployment, Vercel deployment, browser/device QA, production-host smoke, launch approval, or release certification.

Customer menu third-party analytics ID boundary checkpoint: fixed in source. Customer menu rendering keeps the same owner-provided Google Analytics and Meta Pixel integration points, lazy client-only script loading, `window.gaId` handoff for ecommerce events, `fbq('track', 'PageView')` behavior, and intentional campaign-parameter visibility to third-party tools, but malformed saved analytics IDs can no longer be interpolated into public customer-page script URLs or inline bootstrap scripts. `GoogleAnalytics.tsx` now loads only GA4 measurement IDs matching `G-[A-Z0-9]+`, and `FacebookPixel.tsx` now loads only numeric Meta Pixel IDs between 5 and 32 digits; malformed saved values behave like absent IDs and load no third-party script. `npm run verify:public-business-truth` source-gates the GA4 and Meta ID shape guards, old raw owner-ID assignment exclusions, Client Menu analytics docs, Firebase cost docs, audit evidence, and changelog evidence. Follow-up gates passed after this checkpoint patch: `node --check scripts/verification/verify-public-business-truth.js`, `npm run verify:public-business-truth`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes customer menu third-party analytics script admission and source-gate coverage only; it does not change owner analytics settings saves, valid GA4/Meta script loading, internal MenuList analytics writes, customer menu rendering, OBP rendering, public cache behavior, Firestore reads/writes, Storage operations, Cloud Functions, Firebase deployment, Vercel deployment, browser/device QA, production-host smoke, launch approval, or release certification.

Answerlattice website analytics URL minimization boundary: fixed in source. The Answerlattice public website keeps the same shared public cookie banner, accepted-consent analytics mount, Plausible event names, optional GA script, `NEXT_PUBLIC_ANSWERLATTICE_FIREBASE_MEASUREMENT_ID` / `NEXT_PUBLIC_GA_MEASUREMENT_ID` env contract, delegated `data-answerlattice-event` conversion tracking, and resource/referrer GA4 compatibility events, but GA4 URL payloads no longer send raw full URLs with query strings or hash fragments. `AnswerlatticeAnalytics.tsx` now fails closed unless the measurement id matches the GA4 `G-...` shape and sends page-location plus click-link values through origin-and-path-only URL handling. `AnswerlatticeResourceAnalytics.tsx` now uses `answerlatticeAnalyticsUtils.ts` to bound analytics text fields, strip query/hash from resource target, referrer, and entry-page values, and keep UTM fields bounded. `npm run verify:answerlattice-runtime-truth` source-gates the GA id shape guard, shared analytics URL/text minimizer, raw full-URL exclusions, Answerlattice website docs, audit evidence, and changelog evidence. Follow-up gates passed after this checkpoint patch: `node --check scripts/verification/verify-answerlattice-runtime-truth.js`, `npm run verify:answerlattice-runtime-truth`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes Answerlattice public website analytics URL minimization and source-gate coverage only; it does not change Plausible event names, analytics consent choices, valid GA4 script loading, public website copy, Answerlattice dashboard analytics, MenuList website analytics, Firestore reads/writes, Storage operations, Cloud Functions, Firebase deployment, Vercel deployment, browser/device QA, production-host smoke, launch approval, or release certification.

Report Leads source timestamp metadata boundary checkpoint: fixed in source. The consented report follow-up path keeps the same existing `/api/public/contact` enquiry write, Turnstile/contact validation, bounded `sourceContext` metadata, top-level report-lead routing fields, `/api/ops/report-leads` manual-refresh read path, platform-role admission, and setup-job triage behavior, but direct report follow-up submissions can no longer persist arbitrary `sourceContext.reportGeneratedAt` strings. `/api/public/contact` now stores `reportGeneratedAt` only when the submitted value matches the canonical ISO timestamp shape produced by source tools and round-trips through `Date.parse()` / `toISOString()` unchanged; malformed values store `null`. `/api/ops/report-leads` applies the same canonical timestamp normalizer before returning lead rows, so legacy malformed values return as `null` instead of platform-visible arbitrary text. `npm run verify:report-leads-boundary` source-gates the public contact timestamp normalizer, ops route timestamp normalizer, old arbitrary string-cleaning exclusions, Shareable Tool Reports docs, audit evidence, and changelog evidence. Follow-up gates passed after this checkpoint patch: `node --check scripts/verification/verify-report-leads-boundary.js`, `npm run verify:report-leads-boundary`, `npm run verify:shareable-tool-reports`, `npm run verify:public-truth-tools`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes report-lead source timestamp metadata normalization and source-gate coverage only; it does not change valid report links, report hash decoding, follow-up contact write admission, contact notification behavior, setup job derivation, Firestore collection shape, Firestore read/write counts, Storage operations, Cloud Functions, external URL fetches, DNS lookups, provider calls, saved reports, report API routes, Firebase deployment, Vercel deployment, browser/device QA, production-host smoke, launch approval, or release certification.

Website Google Analytics page-location boundary checkpoint: fixed in source. The consent-gated public website Google Analytics mount keeps the same `NEXT_PUBLIC_GA_MEASUREMENT_ID` env key, GA script source, denied ad storage defaults, ads data redaction, accepted-consent mount path, and page-view behavior, but default GA page views no longer send raw query strings or hash fragments as `page_location`. `GoogleAnalytics.tsx` now fails closed unless the configured id matches the GA4 `G-...` shape, builds `page_location` through `getMenulistAnalyticsPageLocation()`, strips `search` and `hash`, and falls back to origin plus pathname if URL parsing fails. `npm run verify:website-public-copy-boundary` source-gates the GA ID shape guard, sanitized page-location helper, raw `page_location: window.location.href` exclusion, implementation/content docs, audit evidence, and changelog evidence. Follow-up gates passed after this checkpoint patch: `node --check scripts/verification/verify-website-public-copy-boundary.js`, `npm run verify:website-public-copy-boundary`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes public website GA page-view URL minimization and source-gate coverage only; it does not change Plausible event names, consent gating, Google Analytics script source, resource custom-event payloads, Microsoft Clarity configuration, owner dashboard analytics, customer menu/OBP analytics, Firestore reads/writes, Storage operations, Cloud Functions, Firebase deployment, Vercel deployment, browser/device QA, production-host smoke, launch approval, or release certification.

Website resource analytics custom-event payload boundary checkpoint: fixed in source. `/resources` and `/resources/[slug]` keep the same property-free Plausible event names, GA4 compatibility events, resource CTA tracking, AI/referrer detection, create-menu/pricing click tracking, and checklist-copy tracking, but resource GA4 custom-event payload strings no longer pass raw public URL/search/referrer/UTM values directly to `gtag`. `src/lib/website/plausible.ts` now bounds custom Google marketing event params, strips control characters from string params, caps URL/referrer/UTM-style fields, and drops empty/non-finite values before calling GA4. `ResourceAnalytics`, `ResourceTrackedLink`, and `ArticleSection` now route resource GA4 events through `trackGoogleMarketingEvent` instead of direct `analyticsWindow.gtag` calls. `npm run verify:website-public-copy-boundary` source-gates the shared payload normalizer, direct resource `gtag` call removal, implementation/content docs, audit evidence, and changelog evidence. Follow-up gates passed after this checkpoint patch: `node --check scripts/verification/verify-website-public-copy-boundary.js`, `npm run verify:website-public-copy-boundary`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes public website resource analytics payload minimization and source-gate coverage only; it does not change Plausible event names, analytics consent gating, Google Analytics script configuration, Microsoft Clarity configuration, owner dashboard analytics, customer menu/OBP analytics, Firestore reads/writes, Storage operations, Cloud Functions, Firebase deployment, Vercel deployment, browser/device QA, production-host smoke, launch approval, or release certification.

Shareable Tool Reports timestamp/display-string payload boundary checkpoint: fixed in source. `/tools/reports` keeps the same public hash-fragment viewer, no-storage report behavior, invalid-report fallback, copy/download actions, and optional consented `/api/public/contact` handoff, but decoded report payloads no longer accept arbitrary generated-time strings. `shareableToolReport.ts` now strips control characters from decoded display strings and rejects report payloads whose `generatedAt` value is not the canonical ISO timestamp shape produced by source tools, so tampered links cannot render `Invalid Date` or arbitrary generated-time text. `npm run verify:shareable-tool-reports` source-gates the timestamp normalizer, control-character stripping, old loose `generatedAt` guard exclusion, Shareable Tool Reports docs, Firebase cost note, audit evidence, changelog evidence, and system ledger evidence. Follow-up gates passed after this checkpoint patch: `node --check scripts/verification/verify-shareable-tool-reports.js`, `node --check scripts/verification/verify-public-truth-tools.js`, `npm run verify:shareable-tool-reports`, `npm run verify:public-truth-tools`, `npm run verify:report-leads-boundary`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes public report hash decoding and verifier coverage only; it does not change valid report links, source-tool report generation, follow-up contact writes, Firestore reads/writes, Storage operations, Cloud Functions, external URL fetches, DNS lookups, provider calls, saved reports, report API routes, Firebase deployment, Vercel deployment, browser/device QA, production-host smoke, launch approval, or release certification.

Public create-menu success business-name query display boundary checkpoint: fixed in source. `/create-menu/success` keeps the same normalized `menuUrl` / `officialPageUrl` handling, copy/share handoffs, starter activation signal recording, QR hint, Google Maps hint, and dashboard navigation, but no longer renders the raw `name` query parameter directly into the success subtitle. `CreateMenuSuccessClient` now normalizes the query value, removes control characters, bounds displayed business-name text, falls back to localized default copy when no safe display text remains, and logs only invalid-reason plus length metadata. `npm run verify:menu-extraction-pipeline` source-gates the business-name normalization helper, bounded invalid-name diagnostics, the old direct `searchParams.get('name')` rendering exclusion, Menu Extraction Pipeline docs, Firebase cost note, audit evidence, changelog evidence, and system ledger evidence. Follow-up gates passed after this checkpoint patch: `node --check scripts/verification/verify-menu-extraction-pipeline.js`, `npm run verify:menu-extraction-pipeline`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes public success-page display normalization and verifier coverage only; it does not change public create-menu uploads, link import, preview polling reads, claim/publish behavior, public draft storage, Firestore reads/writes, Storage operations, Cloud Functions, Firebase deployment, Vercel deployment, browser/device QA, production-host smoke, launch approval, or release certification.

Answerlattice transactions raw load-reason diagnostics boundary checkpoint: fixed in source. `/answerlattice/transactions` keeps the same billing-history fetch, support-credit usage fetch, pagination, owner-facing fixed load failure copy, and transaction/usage display behavior, but the billing-history rejected Promise reason, support-credit rejected Promise reason, and load-more exception are no longer passed to browser logger diagnostics. `AnswerlatticeTransactions` now logs `answerlattice_billing_history_load_failed`, `answerlattice_support_credit_usage_load_failed`, and `answerlattice_support_credit_usage_more_load_failed` through bounded runtime diagnostics with tenant/store presence-length metadata, page-state counts, cursor presence, and source error name/code/status only. `npm run verify:answerlattice-runtime-truth` source-gates the diagnostic codes, bounded tenant/store context, old raw `logger.error(..., reason/error)` exclusions, billing docs, Firebase cost note, audit evidence, and changelog evidence. Follow-up gates passed after this checkpoint patch: `node --check scripts/verification/verify-answerlattice-runtime-truth.js`, `npm run verify:answerlattice-runtime-truth`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes browser-local Answerlattice transactions diagnostics and source-gate coverage only; it does not change billing history reads, support-credit usage reads, pagination behavior, owner copy, Firestore reads/writes, provider calls, Cloud Functions, Firebase deployment, Vercel deployment, browser/device QA, production-host smoke, launch approval, or release certification.

Public create-menu preview stored-error display boundary checkpoint: fixed in source. `/create-menu/preview/[draftId]` keeps the same status-only polling, completed full-draft fetch, failed/expired/success states, and claim handoff, but the failed preview state no longer renders stored `publicMenuDrafts.error` / `draft.error` text into the public UI. `PreviewClient` now uses the existing fixed localized `CreateMenu.previewFailedFallback` copy for failed drafts, so legacy or raw stored draft-error strings cannot become visible through the public preview page. `npm run verify:menu-extraction-pipeline` source-gates the fixed failed-preview copy, direct stored draft-error rendering exclusion, Menu Extraction Pipeline docs, Firebase cost note, audit evidence, changelog evidence, and system ledger evidence. Follow-up gates passed after this checkpoint patch: `node --check scripts/verification/verify-menu-extraction-pipeline.js`, `npm run verify:menu-extraction-pipeline`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes public preview failed-state copy and verifier coverage only; it does not change public create-menu uploads, link import, preview polling reads, claim/publish behavior, public draft storage, Firestore reads/writes, Storage operations, Cloud Functions, Firebase deployment, Vercel deployment, browser/device QA, production-host smoke, launch approval, or release certification.

Batch image result stored-error display boundary checkpoint: fixed in source. The batch image result screen keeps the same queued, processing, completed, failed, cancel, upload, discard, and retry flows, but failed job copy no longer renders stored `imageBatchProcessingJobs.error` / `activeJobData.error` text into the owner UI. `BatchImageGenerationResultView` now uses fixed owner recovery copy for failed jobs, so legacy or raw stored job-error strings cannot become owner-visible through this result card. `npm run verify:ai-accounting` source-gates the fixed failed-job copy, direct stored-error rendering exclusion, AI image docs, Firebase cost note, audit evidence, changelog evidence, and system ledger evidence. Follow-up gates passed after this checkpoint patch: `node --check scripts/verification/verify-ai-accounting-hardening.js`, `npm run verify:ai-accounting`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes owner-visible failed batch result copy and verifier coverage only; it does not change batch trigger behavior, worker behavior, result actions, job listener reads, Firestore reads/writes, Storage operations, AI accounting, Cloud Functions, Firebase deployment, Vercel deployment, browser/device QA, production-host smoke, launch approval, or release certification.

Answerlattice context bundle rebuild reason boundary checkpoint: fixed in source. The compiled context manual rebuild route keeps the same Answerlattice session scope resolution, rate limit before permission/build work, `REBUILD_CONTEXT` permission gate, bounded body reader, owner response shape, lock/manifest writes, immutable Storage bundle paths, and generic failure copy, but arbitrary request reason text and raw owner ids/emails no longer enter durable build metadata. `src/app/api/answerlattice/bundles/rebuild/route.ts` now accepts only fixed rebuild reason codes (`manual` or `activation_manual_rebuild`), returns `400 Invalid rebuild request.` for invalid reason payloads, and passes the fixed requester label `owner`. `src/lib/answerlattice/contextBundleBuilderServer.ts` normalizes fixed reason/requester metadata again before writing `platformSummary/bundleBuildLock_*` or `platformSummary/bundleManifest_*`. The product-surface summary rebuild route now also rejects arbitrary request reason text and accepts only the existing fixed `manual` code. `npm run verify:answerlattice-runtime-truth` source-gates the route reason allowlists, safe body parsing, fixed requester label, builder reason/requester normalizers, old arbitrary reason schema exclusions, and compiled-context docs. Follow-up gates passed after this checkpoint patch: `node --check scripts/verification/verify-answerlattice-runtime-truth.js`, `npm run verify:answerlattice-runtime-truth`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes Answerlattice rebuild metadata admission and source-gate coverage only; it does not change valid activation rebuild behavior, bundle source reads, bundle Storage writes, product-surface summary contents, Firestore rules/indexes, Cloud Functions, Firebase deployment, Vercel deployment, browser/device QA, production-host smoke, launch approval, or release certification.

Support Board nightly derived-card source-text duplication boundary checkpoint: fixed in source. The Answerlattice nightly Support Board sync keeps the same feature flag, source queries, entity grouping, deterministic card ids, resolved/unchanged skip behavior, compact summary write, scheduler diagnostics, and owner/staff private board semantics, but derived recurring-miss and signal-cluster cards no longer copy raw search queries, prompts, ticket subjects, reasons, or messages into durable card descriptions. `functions-answerlattice/src/answerlattice/supportBoardSync.ts` now tracks only source example counts plus bounded context keys for those derived cards and tells reviewers to inspect the original search/signal history when source text is needed. `npm run verify:answerlattice-runtime-truth` source-gates the count-only `exampleCount` path, blocks the old `samples` / `Examples:` rendering path, and checks Support Board docs, audit evidence, and changelog evidence. Follow-up gates passed after this checkpoint patch: `node --check scripts/verification/verify-answerlattice-runtime-truth.js`, `npx tsc --noEmit --incremental false --pretty false` from `functions-answerlattice/`, `npm run verify:answerlattice-runtime-truth`, root `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. The Answerlattice QA Functions deploy attempt `firebase deploy --only functions:answerlattice --project answerlattice-qa --config firebase-answerlattice.json --non-interactive` completed predeploy build and then failed before upload with Cloud Resource Manager HTTP 403 caller permission for `answerlattice-qa`; live Function effect remains blocked until an account with project access can deploy the changed Functions codebase. This changes nightly derived Support Board card descriptions and source-gate coverage only; it does not change manual cards, owner-entered notes, ticket/signal source rows, original search history rows, Firestore rules/indexes, successful Cloud Functions deployment, Vercel deployment, browser/device QA, production-host smoke, launch approval, or release certification.

Shared client DAL toast trust boundary checkpoint: fixed in source. The shared client DAL wrappers keep the same active-session guard, loader behavior, bounded secure diagnostics, fallback message, and fallback return value behavior, but generic DAL exceptions no longer display arbitrary short plain `Error.message` text in owner toasts. `src/lib/errors/uiErrorMessages.ts` now defaults to fixed fallback copy after technical-shape filtering unless a caller explicitly passes `{ allowTrustedPlainText: true }`. The two current trusted validation-copy call sites are local-only: custom drag-and-drop validators and MCE Publish-Gate validation messages. `npm run verify:auth-security-failure-matrix` source-gates the default-fallback helper behavior, blocks DAL composers from opting into trusted plain text, and requires explicit trust for drag-and-drop validation copy. `npm run verify:menu-project-editor-boundary` and `npm run verify:public-business-truth` source-gate the explicit MCE Publish-Gate trust boundary. Follow-up gates passed after this checkpoint patch: `node --check scripts/verification/verify-auth-security-failure-matrix.js`, `node --check scripts/verification/verify-menu-project-editor-boundary.js`, `node --check scripts/verification/verify-public-business-truth.js`, `npm run verify:auth-security-failure-matrix`, `npm run verify:menu-project-editor-boundary`, `npm run verify:public-business-truth`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes owner-visible generic DAL failure copy selection and source-gate coverage only; it does not change local trusted validation messages, MCE rules, upload validation behavior, Firestore reads/writes, Storage operations, API routes, Cloud Functions, Firebase deployment, Vercel deployment, browser/device QA, production-host smoke, launch approval, or release certification.

MenuList stale menu notification reason boundary checkpoint: fixed in source. The `menulist.menu_stale` owner-notification template keeps the same stale-detection trigger, owner-notification event, delivery channels, delivery rows, and owner review copy, but no longer renders arbitrary `metadata.reason` strings into owner email/text bodies. `src/lib/owner-notifications/templates/menulist.ts` now maps stale-menu reason metadata to fixed owner copy before rendering. `npm run verify:owner-notifications-boundary` source-gates the `MENU_STALE_REASON_OWNER_COPY` mapper, fixed `menuStaleReasonText()` path, raw reason rendering exclusion, Owner Notifications docs, audit evidence, and changelog evidence. Follow-up gates passed after this checkpoint patch: `node --check scripts/verification/verify-owner-notifications-boundary.js`, `npm run verify:owner-notifications-boundary`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes MenuList owner-notification email/text rendering only; it does not change stale detection, notification enqueueing, delivery behavior, delivery logs, Firestore reads/writes, Storage operations, Cloud Functions, Firebase deployment, Vercel deployment, browser/device QA, production-host smoke, launch approval, or release certification.

Razorpay past-due grace-period display fallback checkpoint: fixed in source. Desktop Billing, Mobile Billing, and authenticated pricing subscription-management keep the same valid `past_due` behavior when `pastDueSinceAt` is present: the owner sees the normal grace countdown, payment-failed status, retry-payment handoff, and existing cancellation/support recovery paths. The shared `src/utils/razorpay.ts` helper now exposes `getGracePeriodDisplayInfo()` so malformed or legacy `past_due` docs without `pastDueSinceAt` render fixed "Grace period details unavailable." recovery copy instead of a false zero-day countdown. Stale top-of-file TODO comments in `ActiveSubscriptionCard.tsx` were removed because `past_due`, `cancelled`, and `expired` branches already exist. `npm run verify:billing-entitlement-boundary` source-gates the helper, desktop/mobile/website surface usage, old raw countdown exclusions, Razorpay docs, audit evidence, and changelog evidence. Follow-up gates passed after this checkpoint patch: `node --check scripts/verification/verify-billing-entitlement-boundary.js`, `npm run verify:billing-entitlement-boundary`, `npm run verify:menulist-api-tenant-safety`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes owner-visible billing fallback copy and source-gate coverage only; it does not change Razorpay webhook handling, subscription status writes, DAL access rules, reconciliation, provider calls, billing route calls, Firestore reads/writes, Cloud Functions, Firebase deployment, Vercel deployment, browser/device QA, production-host smoke, launch approval, or release certification.

Answerlattice owner notification template output boundary checkpoint: fixed in source. The Answerlattice owner-notification template renderer keeps the same trigger registration, recipient resolution, delivery channels, queue-first owner-notification flow, delivery rows, and app-side template selection, but owner-visible metadata is now normalized and capped before rendering. `src/lib/owner-notifications/templates/answerlattice.ts` validates action links as `http:`/`https:` before rendering email buttons, maps widget/source-sync/high-priority failure metadata to fixed owner copy, and no longer prints arbitrary `metadata.failureReason` or `metadata.reason` strings into owner email/text bodies. `npm run verify:owner-notifications-boundary` source-gates the Answerlattice template constants, URL validation, fixed failure-copy mappers, raw failure/reason rendering exclusions, Owner Notifications docs, audit evidence, and changelog evidence. Follow-up gates passed after this checkpoint patch: `node --check scripts/verification/verify-owner-notifications-boundary.js`, `npm run verify:owner-notifications-boundary`, `npm run verify:answerlattice-runtime-truth`, `npm run verify:billing-entitlement-boundary`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes Answerlattice owner-notification email/text rendering only; it does not change delivery behavior, delivery logs, Firestore reads/writes, Storage operations, Cloud Functions, Firebase deployment, Vercel deployment, browser/device QA, production-host smoke, launch approval, or release certification.

CampaignCue workspace template Storage cleanup diagnostics checkpoint: fixed in source. Workspace saved-template save/delete keeps the same explicit owner action model, `campaigncueWorkspaces/{workspaceId}/packTemplateIndexes/default` index write, bounded summary array, Storage payload/editor/preview paths, and best-effort cleanup after failed saves or after successful metadata deletes. Unexpected Storage cleanup failures are no longer swallowed: `src/lib/campaigncue/pack-templates/workspaceTemplates.ts` now logs fixed `campaigncue_workspace_template_storage_cleanup_failed` diagnostics with cleanup target plus storage path, template id, and workspace id presence-length metadata only. Missing Storage objects remain expected no-ops through the `storage/object-not-found` branch. `npm run verify:campaigncue-pack-templates` source-gates the diagnostic code, bounded context, expected missing-object branch, payload/editor/preview cleanup targets, old silent catch exclusion, Campaign Pack Template docs, audit evidence, and changelog evidence. Follow-up gates passed after this checkpoint patch: `node --check scripts/verification/verify-campaigncue-pack-templates.js`, `npm run verify:campaigncue-pack-templates`, `npm run verify:campaigncue`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes CampaignCue workspace saved-template cleanup observability only; it does not change template catalog reads, workspace index writes, payload/editor/preview upload paths, owner save/delete copy, Firestore rules, Storage rules, Cloud Functions, Firebase deployment, Vercel deployment, browser/device QA, production-host smoke, launch approval, or release certification.

Extraction Cost Monitor load diagnostics checkpoint: fixed in source. The main Extraction Monitor still gets cost metrics through the deduped dashboard snapshot, so normal page loads keep the same read pattern. The standalone `CostMonitor.tsx` compatibility path now logs fixed `extraction_cost_monitor_load_failed` diagnostics through the shared ops logger when its direct `getExtractionCostMetrics()` call rejects, with bounded refresh-trigger metadata and normalized source error metadata only. The panel also renders fixed "Cost metrics unavailable" copy instead of collapsing the failure into the "No extraction calls today" empty state. `npm run verify:menu-extraction-pipeline` source-gates the diagnostic code, fixed unavailable state, old silent catch exclusion, AI Extraction Monitoring docs, audit evidence, and changelog evidence. Follow-up gates passed after this checkpoint patch: `node --check scripts/verification/verify-menu-extraction-pipeline.js`, `npm run verify:menu-extraction-pipeline`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes Extraction Monitor cost-panel observability only; it does not change the main dashboard snapshot path, valid cost metrics, Firestore reads beyond the already-attempted compatibility read, Firestore writes, Storage operations, Cloud Functions, Firebase deployment, Vercel deployment, provider calls, browser/device QA, production-host smoke, launch approval, or release certification.

Analytics export service CSV spreadsheet formula boundary checkpoint: fixed in source. `src/lib/export/exportService.ts` has no current external call site in the repo scan, but it remains a maintained analytics export helper and `npm run verify:menu-export` already guards it. Its CSV generator keeps the same summary/top-question/knowledge-gap/feedback rows, browser-local Blob download, JSON export, Markdown export, clipboard copy, Web Share handling, security audit log, filename behavior, and diagnostics, but row values now route through a local `csvRow()` helper backed by shared `escapeCSVValue()`. The old hand-rolled quoted top-question and knowledge-gap template rows are source-gated out. `npm run verify:menu-export` source-gates the sanitizer import, `csvRow()` helper, top-question/knowledge-gap sanitizer use, old template exclusions, audit evidence, and changelog evidence. Follow-up gates passed after this checkpoint patch: `node --check scripts/verification/verify-menu-export.js`, `npm run verify:menu-export`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes browser-local analytics CSV cell text in the maintained export service only; it does not add call sites, change dashboard reads, alter JSON/Markdown exports, change clipboard/share behavior, touch Firestore reads/writes, Storage operations, API routes, Cloud Functions, Firebase deployment, Vercel deployment, production builds, browser/device QA, production-host smoke, launch approval, or release certification.

Answerlattice conversation CSV spreadsheet formula boundary checkpoint: fixed in source. The platform Chat Monitoring conversation export keeps the same filtered conversation set, browser-local CSV Blob download, message counts, feedback counts, satisfaction percentage, timestamps, filename, and success copy, but `src/components/templates/platform/chatManagement/ConversationsList.tsx` now routes header and row cells through the shared `escapeCSVValue()` helper. This removes the old private `escapeCSV` helper so customer names, emails, conversation titles, and other string cells receive the same spreadsheet-formula protection as other CSV exports. `npm run verify:answerlattice-runtime-truth` source-gates the shared sanitizer import, header/row sanitizer use, private escape exclusion, Chat Monitoring docs, audit evidence, and changelog evidence. Follow-up gates passed after this checkpoint patch: `node --check scripts/verification/verify-answerlattice-runtime-truth.js`, `npm run verify:answerlattice-runtime-truth`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes browser-local conversation CSV cell text only; it does not change chat session reads, filters, pagination, batch metadata writes, transcript export, Firestore reads/writes, Firestore rules/indexes, Storage operations, Cloud Functions, Firebase deployment, Vercel deployment, production builds, browser/device QA, production-host smoke, launch approval, or release certification.

Extraction review MOL diagnostics checkpoint: fixed in source. The review-apply path keeps the existing acknowledged project/menu write, linked-outlet save route, job completion update, public cache invalidation, owner success state, and fire-and-forget MOL `EXTRACTION_APPLIED` success behavior, but a failed non-blocking MOL event write no longer disappears behind a comment-only catch. `src/lib/extraction/applyChanges.ts` now logs fixed `menu_review_apply_mol_event_log_failed` diagnostics through the shared menu-processing logger with project/job presence-length context, actor/tenant/store presence-length context, applied-change count, review mode, MOL version, and normalized source error metadata only. `npm run verify:menu-extraction-pipeline` source-gates the diagnostic code, bounded context, non-blocking `void logMOLEvent` branch, old silent catch exclusion, Menu Extraction Pipeline docs, AI Data Extraction docs, audit evidence, and changelog evidence. Follow-up gates passed after this checkpoint patch: `node --check scripts/verification/verify-menu-extraction-pipeline.js`, `npm run verify:menu-extraction-pipeline`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes extraction review observability only; it does not change valid review apply writes, linked-outlet save behavior, job completion, MOL success writes, owner success copy, public cache invalidation, Firestore rules/indexes, Storage operations, Cloud Functions, Firebase deployment, Vercel deployment, browser/device QA, production-host smoke, launch approval, or release certification.

CSV export spreadsheet formula boundary checkpoint: fixed in source. Shared CSV/Excel exports and the analytics `ExportButton` keep the same browser-local Blob download behavior, row selection, JSON export path, failure diagnostics, and owner copy, but CSV cells now pass through the shared `escapeCSVValue()` helper. String cells that begin with `=`, `+`, `-`, `@`, tab, carriage return, newline, or those prefixes after leading whitespace now receive a leading apostrophe before CSV quoting so spreadsheet apps do not preserve them as executable formulas. Numeric metrics remain numeric because the formula guard applies only to string values. `npm run verify:auth-security-failure-matrix` source-gates `CSV_FORMULA_INJECTION_PREFIXES`, `shouldEscapeCSVFormula`, `value.trimStart()`, exported `escapeCSVValue`, analytics `ExportButton` usage, header sanitization, old private CSV escaping exclusion, security docs, audit evidence, and changelog evidence. Follow-up gates passed after this checkpoint patch: `node --check scripts/verification/verify-auth-security-failure-matrix.js`, `npm run verify:auth-security-failure-matrix`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes browser-local CSV/Excel export cell text only; it does not change analytics reads, export row selection, JSON export output, PDF unavailable copy, Firestore reads/writes, Storage operations, API routes, Cloud Functions, Firebase deployment, Vercel deployment, production builds, browser/device QA, production-host smoke, launch approval, or release certification.

Vercel domain provider response parse diagnostics checkpoint: fixed in source. The shared Vercel domain helper keeps the existing fixed provider host, URL-encoded project/domain path segments, manual redirect handling, provider timeout, timeout cleanup, and 64KB bounded response parser. If the bounded parser rejects a provider response, the helper now logs fixed `vercel_domain_provider_response_parse_failed` diagnostics with method, path presence/length, query presence, response status, response OK state, max-byte cap, and source error type only, then preserves the existing `{}` compatibility fallback so `/api/domain` and `/api/answerlattice/hosted-help-settings` continue to own generic browser responses and route-level provider failure handling. `npm run verify:public-business-truth` and `npm run verify:answerlattice-runtime-truth` source-gate the diagnostic code, bounded context, guarded parser helper, old silent bounded-parser fallback exclusion, audit evidence, and changelog evidence. Follow-up gates passed after this checkpoint patch: `node --check scripts/verification/verify-public-business-truth.js`, `node --check scripts/verification/verify-answerlattice-runtime-truth.js`, `npm run verify:public-business-truth`, `npm run verify:answerlattice-runtime-truth`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes Vercel provider response observability only; it does not change valid custom-domain add/status/remove behavior, hosted-help domain save/status behavior, provider request count, provider request paths, Firestore reads/writes, hosted-help registry writes, public cache invalidation, rules, indexes, Cloud Functions, Firebase deployment, Vercel deployment, browser/device QA, production-host smoke, launch approval, or release certification.

Menu Kit ZIP filename boundary checkpoint: fixed in source. Desktop Use MenuList, mobile Share, project Share Modal, and printable-template complete Menu Kit downloads keep the same client-side `generateMenuKit()` rendering, ZIP contents, internal asset filenames, print instructions, success/failure copy, diagnostics, analytics tracking, and starter-signal behavior, but the browser download filename now comes from the shared generator result as `result.zipFilename`. The filename is built by `buildMenuKitZipFilename(input.storeName, input.templateFamilyId)` inside `src/lib/menu-kit/menuKitGenerator.ts` instead of hand-rolled store-name filename derivation in each caller. `npm run verify:menu-card-export` source-gates the generator contract, desktop/mobile/project-share/printable-template caller usage, old hand-rolled filename exclusions, Menu Kit docs, audit evidence, and changelog evidence. Follow-up gates passed after this checkpoint patch: `node --check scripts/verification/verify-menu-card-export.js`, `npm run verify:menu-card-export`, `npm run verify:printable-asset-templates`, `npm run verify:menu-export`, `npm run verify:public-business-truth`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes complete Menu Kit ZIP browser-local filename generation only; it does not change ZIP contents, individual asset filenames, QR payloads, Canvas/jsPDF generation, feedback QR downloads, Firestore reads/writes, Storage operations, API routes, provider calls, Cloud Functions, Firebase rules/indexes, Firebase deploys, Vercel deploys, production builds, browser/device QA, production-host smoke, launch approval, or release certification.

Feedback QR download filename boundary checkpoint: fixed in source. Desktop Use MenuList and mobile Share keep the same branded Feedback QR generation, feedback URL, logo/brand coloring, browser download behavior, success/failure copy, and bounded diagnostics, but the download filename is now produced through `getQrCodeFilename(data.storeName)` from the shared feedback QR utility instead of raw store-name whitespace replacement. This keeps browser-visible filenames ASCII-safe and suffix-stable for arbitrary store names. `npm run verify:menu-card-export` source-gates the desktop/mobile helper usage, rejects the old raw store-name whitespace replacement, and checks Internal Feedback docs, audit evidence, and changelog evidence. Follow-up gates passed after this checkpoint patch: `node --check scripts/verification/verify-menu-card-export.js`, `npm run verify:menu-card-export`, `npm run verify:public-business-truth`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes feedback QR browser-local filename generation only; it does not change QR payload URLs, QR image rendering, feedback submission, feedback inbox reads/writes, Firestore reads/writes, Storage operations, API routes, provider calls, Cloud Functions, Firebase rules/indexes, Firebase deploys, Vercel deploys, production builds, browser/device QA, production-host smoke, launch approval, or release certification.

MOL no-session diagnostics checkpoint: fixed in source. The Menu Observation Layer remains fire-and-forget and non-blocking, but enabled `logMenuChange()` calls without active tenant/store session, explicit-scope calls without complete scope, and pending flush handoffs without session no longer disappear silently. `src/database/menuChangeLog/index.ts` now logs bounded `menu_change_log_session_missing`, `menu_change_log_scope_missing`, and `menu_change_log_flush_session_missing` diagnostics with entry shape and identifier presence/length metadata only. `npm run verify:public-business-truth` source-gates the diagnostic codes, bounded logger, Data Editor docs parity, audit evidence, changelog evidence, and old silent-return exclusions. Follow-up gates passed after this checkpoint patch: `node --check scripts/verification/verify-public-business-truth.js`, `npm run verify:public-business-truth`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes MOL observability only; it does not change valid editor saves, valid MOL writes, debouncing, feature-flag disabled no-ops, Firestore reads/writes, Storage operations, Cloud Functions, Firebase deployment, Vercel deployment, browser/device QA, production-host smoke, launch approval, or release certification.

Answerlattice KB session lookup diagnostics checkpoint: fixed in source. The Answerlattice KB article, category, and KB-generation job DAL compatibility paths keep their valid tenant/store-scoped reads and platform-admin list behavior, but thrown `getActiveSession()` failures no longer disappear into anonymous or global fallback scope. Article lookups log `answerlattice_kb_articles_session_lookup_failed`, category lookups log `answerlattice_kb_categories_session_lookup_failed`, job lookups log `answerlattice_kb_generation_session_lookup_failed`, and each uses bounded operation metadata only. `getCategories()` now returns `null` when session lookup itself failed so it does not fall through to the legacy categories doc. `npm run verify:answerlattice-runtime-truth` source-gates the runtime helpers, silent fallback exclusions, Knowledge Base docs parity, KB Generation docs parity, Help Center docs parity, audit evidence, and changelog evidence. Follow-up gates passed after this checkpoint patch: `node --check scripts/verification/verify-answerlattice-runtime-truth.js`, `npm run verify:answerlattice-runtime-truth`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes Answerlattice KB observability and scope admission only; it does not change valid scoped reads/writes, entity extraction, cache versioning, Firestore rules/indexes, Cloud Functions, Firebase deployment, Vercel deployment, browser/device QA, production-host smoke, launch approval, or release certification.

Weekly narrative output boundary checkpoint: fixed in source. The protected local weekly narrative generator keeps the same authentication, permission gate, SAFE_MODE check, rate limit, chat-analytics reads, Gemini call, deterministic fallback copy, `insights/{tId}/stores/{sId}/ai/weekly` write, best-effort AI operation accounting, and count/length success response. Parsed provider output is now normalized before persistence: `narrative`, `highlights`, and `recommendations` strip control/template characters, collapse whitespace, cap narrative/list-item lengths, cap list counts, and fall back to deterministic copy when generated text is unusable after cleaning. `npm run verify:menulist-api-tenant-safety` source-gates the output caps, cleaning helper, parser usage, old trim-only generated-output exclusions, AI System docs parity, audit evidence, and changelog evidence. Follow-up gates passed after this checkpoint patch: `node --check scripts/verification/verify-menulist-api-tenant-safety.js`, `npm run verify:menulist-api-tenant-safety`, `npm run verify:ai-accounting`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes generated weekly narrative output normalization only; it does not change Firestore read/write counts, provider calls, AI accounting writes, credit consumption, Cloud Functions, Firebase rules/indexes, Storage rules, owner settings, public routes, Firebase deploys, Vercel deploys, production builds, provider smoke, browser/device QA, production-host smoke, launch approval, or release certification. The app-side continuation requires the normal Next.js release path; no Vercel deploy was run.

Maps Place Check raw provider output boundary checkpoint: fixed in source. The guarded owner/admin `mapsPlaceCheck` callable keeps the same authentication, tenant/store access, SAFE_MODE, rate-limit, Google Maps grounding, attribution, candidate, and no-write behavior, but it no longer returns truncated raw provider response text when grounding is missing or the provider response cannot be parsed as JSON. `functions/src/logic/mapsPlaceCheck.ts` now returns only status, attribution, model, checked time, candidate facts, and source metadata, while diagnostics retain bounded response length and parse-state metadata. `npm run verify:public-truth-tools` source-gates the no-`rawText` callable contract, runtime return branches, and Maps Place Check docs parity. Follow-up gates passed after this checkpoint patch: `node --check scripts/verification/verify-public-truth-tools.js`, `npm run verify:public-truth-tools`, `npm run verify:functions-deploy-preflight`, `npm --prefix functions run build`, `npm --prefix functions run lint`, and focused `git diff --check`. A scoped MenuList QA Functions deploy was attempted for `functions:mapsPlaceCheck`; it completed predeploy lint/build and then failed before upload with Cloud Resource Manager HTTP 403 caller permission for `menulist-qa`. Live Function effect remains blocked until an account with project access can deploy the changed callable. This changes Maps Place Check callable output only; it does not enable the feature flag, add Firestore writes, change canonical store truth, alter Maps source attribution fields, add public UI, change public tool reports, change Storage rules, run Vercel deploys, complete provider smoke, production-host smoke, launch approval, or release certification.

Legacy lifecycle event/status diagnostics checkpoint: fixed. The legacy Functions lifecycle messaging sender keeps the same send, idempotency, rate-limit, retry, digest, and owner-notification migration behavior, but Cloud Functions diagnostics no longer include raw lifecycle event type or message status strings. `functions/src/messaging/messagingEngine.ts` now reports event type and delivery status through bounded presence/length/type metadata, and the feature-disabled and no-template branches use the same bounded log context. Stored `messageLogs.eventType` and `messageLogs.status` are unchanged because they remain part of the idempotency, retry, and digest contracts. `npm run verify:owner-notifications-boundary` source-gates the bounded lifecycle messaging context, old raw `eventType`/`status` log-context exclusions, direct raw feature-disabled/no-template log exclusions, Lifecycle Messaging docs parity, and changelog evidence. Follow-up gates passed after this checkpoint patch: `node --check scripts/verification/verify-owner-notifications-boundary.js`, `npm run verify:owner-notifications-boundary`, `npm run verify:functions-deploy-preflight`, `cd functions && npm run build`, `cd functions && npm run lint`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. A scoped MenuList QA Functions deploy was attempted for `functions:verifyMenuPublish,functions:computeDecisionBlocksScores,functions:triggerDecisionBlocksScoring,functions:triggerStoreNightlyScheduler`; it completed predeploy lint/build and then failed before upload with Cloud Resource Manager HTTP 403 caller permission for `menulist-qa`. Live Function effect remains blocked until an account with project access can deploy the changed Functions bundle. This changes legacy lifecycle messaging observability only; it does not change valid sends, owner-notification migration delivery, idempotency queries, retry/digest queries, stored message-log fields, Firestore reads/writes, Firestore rules/indexes, Storage rules, Vercel deploys, production builds, provider smoke, browser/device QA, production-host smoke, launch approval, or release certification.

Owner-notification flag and trigger diagnostics checkpoint: fixed. The queue-first owner-notification processor keeps the existing fail-closed behavior: if `ops_config/system.ENABLE_LIFECYCLE_MESSAGING` cannot be read, delivery is skipped before owner-notification event creation; if an incoming MenuList trigger has no registry entry, delivery is skipped before event creation; if an already-stored event has an unknown trigger, the existing skipped `unknown_trigger` merge is retained. Those degraded paths are no longer silent or raw. `functions/src/ownerNotifications/processor.ts` now logs bounded `owner_notification_lifecycle_flag_check_failed` and `owner_notification_unknown_menulist_trigger` diagnostics with trigger presence/length/type metadata, fixed fallback policies, event-id presence/length metadata for stored events, and normalized source error metadata only. Raw trigger strings, event IDs, tenant IDs, store IDs, recipients, provider responses, and exception text are not logged. `npm run verify:owner-notifications-boundary` source-gates the diagnostic codes, fallback policies, raw-trigger logging exclusions, Owner Notifications docs parity, Lifecycle Messaging docs parity, and changelog evidence. Follow-up gates passed after this checkpoint patch: `node --check scripts/verification/verify-owner-notifications-boundary.js`, `npm run verify:owner-notifications-boundary`, `npm run verify:functions-deploy-preflight`, `cd functions && npm run build`, `cd functions && npm run lint`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. A scoped MenuList QA Functions deploy was attempted for `functions:verifyMenuPublish,functions:computeDecisionBlocksScores,functions:triggerDecisionBlocksScoring,functions:triggerStoreNightlyScheduler`; it completed predeploy lint/build and then failed before upload with Cloud Resource Manager HTTP 403 caller permission for `menulist-qa`. Live Function effect remains blocked until an account with project access can deploy the changed Functions bundle. This changes owner-notification Functions observability only; it does not change valid lifecycle delivery, event creation for known triggers, provider sends, delivery rows, rate limits, Firestore rules/indexes, Storage rules, Vercel deploys, production builds, provider smoke, browser/device QA, production-host smoke, launch approval, or release certification.

Owner notification template output boundary checkpoint: fixed in source. Queue-first MenuList owner notification templates and the legacy/app lifecycle template mirrors now normalize subject/text metadata, escape HTML metadata before rendering, validate rendered email links as `http:`/`https:`, and map publish-health failure codes to fixed owner copy before displaying `MENU_PUBLISH_FAILED` / `menulist.menu_publish_failed` messages. Arbitrary stored or caller-provided `failureReason` text is no longer printed into owner-facing email or manual handoff copy. `npm run verify:owner-notifications-boundary` source-gates the MenuList owner template guard, app lifecycle mirror guard, Functions lifecycle mirror guard, old raw interpolation exclusions, Owner Notifications docs parity, Lifecycle Messaging docs parity, and audit/changelog evidence. Follow-up gates passed after this checkpoint patch: `node --check scripts/verification/verify-owner-notifications-boundary.js`, `npm run verify:owner-notifications-boundary`, `npx tsc --noEmit --incremental false --pretty false`, `npm run verify:functions-deploy-preflight`, `cd functions && npm run build`, `cd functions && npm run lint`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. A scoped MenuList QA Functions deploy was attempted with `firebase deploy --only functions:verifyMenuPublish,functions:computeDecisionBlocksScores,functions:triggerDecisionBlocksScoring,functions:triggerStoreNightlyScheduler --project menulist-qa`; it completed predeploy lint/build and then failed before upload with Cloud Resource Manager HTTP 403 caller permission for `menulist-qa`. Live Functions effect remains blocked until an account with project access can deploy the changed Functions bundle. This changes email/manual-message rendering only; it does not change valid SMTP sends, owner-notification migration delivery, idempotency, rate limits, message-log schema, Firestore reads/writes, Firestore rules/indexes, Storage rules, cache invalidation, Vercel deploys, production builds, provider smoke, browser/device QA, production-host smoke, launch approval, or release certification. The app-side continuation requires the normal Next.js release path; no Vercel deploy was run.

Owner Dashboard OBP summary read diagnostics checkpoint: fixed. `src/database/ownerDashboard/index.ts` still builds the OBP overview status, WTD, MTD, and historical weeks from the already-read OBP daily docs, while the optional `{tId}_{sId}_obp_overall_summary` read supplies only `viewsChange`. If that optional summary read fails, the dashboard now logs bounded `owner_dashboard_obp_summary_read_failed` diagnostics with tenant/store/project/summary-doc presence-length metadata, fixed `use_daily_obp_docs_without_views_change` fallback policy, and normalized source error metadata only; raw tenant IDs, store IDs, summary document IDs, analytics payloads, and exception text are not logged. The fallback keeps `viewsChange` as `null` and does not add a fallback Firestore read/write. `npm run verify:owner-dashboard-today-boundary` source-gates the diagnostic code, cap, fallback policy, old silent catch exclusion, Owner Dashboard docs parity, Official Business Page docs parity, and changelog evidence. This changes owner-dashboard observability only; it does not change OBP daily-doc reads, settled dashboard summary reads, owner dashboard status copy, Firestore writes, analytics writes, Cloud Functions, cache invalidation, Firebase/Vercel deploys, production builds, browser/device QA, production-host smoke, launch approval, or release certification.

Pricing Integrity PDF failure reason persistence checkpoint: fixed. The Pricing Integrity engine remains dormant in the current editor save path, but its exported `markPDFFailed()` helper no longer persists arbitrary caller text into `pricingIntegrity.pdf.lastFailureReason`. `src/lib/pricing/integrityEngine.ts` now normalizes PDF failure reasons to a bounded local-code pattern and collapses arbitrary text to `pricing_pdf_generation_failed`; diagnostics keep the stored code plus raw input presence/length metadata only. `npm run verify:pricing-integrity-boundary` source-gates the normalized failure reason, direct raw-error persistence exclusion, Pricing Integrity docs parity, and audit/changelog evidence. `npm run verify:menulist-api-tenant-safety` also source-gates the normalized persisted field through the shared pricing diagnostic boundary. Follow-up gates passed after this checkpoint patch: `node --check scripts/verification/verify-pricing-integrity-boundary.js`, `node --check scripts/verification/verify-menulist-api-tenant-safety.js`, `npm run verify:pricing-integrity-boundary`, `npm run verify:menulist-api-tenant-safety`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes dormant Pricing Integrity PDF failure metadata only; it does not wire `runPricingIntegrity()` into editor saves, enable background PDF regeneration, change current on-demand PDF generation, change project save behavior, change public menu output, change Digital Screens invalidation, add Firestore reads/writes beyond the dormant write if explicitly called, change Firestore rules/indexes, require Firebase/Vercel deploys, run production builds, complete browser/device QA, or certify launch.

Scheduler-hour timezone diagnostics checkpoint: fixed. The app and Functions scheduler-hour helpers keep the existing fallback behavior: valid store timezones still compute the UTC settlement hour for `businessDayEndTime + buffer`, while missing or malformed timezones use the UTC settlement hour derived from the business-day cutoff. The malformed-timezone path is no longer silent. `src/lib/utils/schedulerHour.ts` now logs bounded `scheduler_hour_timezone_validation_failed` diagnostics, and `functions/src/utils/schedulerHour.ts` logs bounded `SCHEDULER_HOUR_TIMEZONE_VALIDATION_FAILED` diagnostics. Diagnostics include only timezone presence-length metadata, target local hour, Intl availability, fixed `use_utc_settlement_hour` fallback policy, and normalized source error metadata; raw timezone values, tenant IDs, store IDs, project IDs, scheduler payloads, and exception text are not logged. `npm run verify:menulist-api-tenant-safety` source-gates the app and Functions scheduler-hour diagnostic guards, old silent catch exclusion, analytics docs parity, and changelog evidence. Follow-up gates passed after this checkpoint patch: `node --check scripts/verification/verify-menulist-api-tenant-safety.js`, `npm run verify:menulist-api-tenant-safety`, `npm run verify:functions-deploy-preflight`, `cd functions && npm run build`, `cd functions && npm run lint`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. A scoped MenuList QA Functions deploy was attempted for `functions:messagingOnboarding,functions:backfillStoresSummary`; it completed predeploy lint/build and then failed before upload with Cloud Resource Manager HTTP 403 caller permission for `menulist-qa`. Live Function effect remains blocked until an account with project access can deploy the changed Functions bundle. The app-side continuation requires the normal Next.js release path; no Vercel deploy was run. This changes scheduler-hour fallback observability only; it does not change valid scheduler-hour calculation, valid store-local analytics settlement, stored `schedulerHour` fallback semantics, public analytics event admission, Firestore reads/writes, analytics writes, Cloud Function schedules, cache invalidations, Firestore rules/indexes, Storage rules, Vercel deploys, production builds, provider smoke, browser/device QA, production-host smoke, launch approval, or release certification.

Deleted-project slug reservation fail-closed checkpoint: fixed. Project create, rename, duplicate, and no-slug backfill flows keep the same normal slug generation and confirmed 90-day deleted-slug reservation behavior, but unknown reservation state no longer defaults to available. `src/database/projects/index.ts` logs bounded `deleted_project_slug_reservation_check_failed` diagnostics and treats the proposed slug as reserved when the reservation query fails. Create and duplicate flows append a unique suffix; rename and no-slug backfill flows refuse the new slug through the existing reserved-slug owner-facing error path. `npm run verify:public-business-truth` source-gates the conservative return, old fail-open wording exclusion, audit/changelog evidence, and URL-routing docs parity. Follow-up gates passed after this checkpoint patch: `node --check scripts/verification/verify-public-business-truth.js`, `npm run verify:public-business-truth`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes deleted-project slug reservation lookup failure handling only; it does not change normal valid slug generation, confirmed reservation handling, previous-slug redirects, public resolver behavior, project summary schema, public cache invalidation, Firestore rules/indexes, Storage rules, Firebase/Vercel deploys, production builds, browser/device QA, production-host smoke, launch approval, or release certification.

Staleness lifecycle delivery diagnostics checkpoint: fixed. The nightly staleness detector keeps the existing behavior: it writes the staleness cooldown/detection row before attempting `MENU_STALE` lifecycle delivery, and a failed delivery attempt does not remove that cooldown or stop the rest of the nightly scan. The delivery exception path is no longer silent. `functions/src/analytics/stalenessCheck.ts` now logs bounded `STALENESS_LIFECYCLE_DELIVERY_FAILED` diagnostics with store, tenant, and reference presence-length metadata, fixed `keep_detection_cooldown_and_continue` fallback policy, and normalized source error metadata only. Raw store IDs, tenant IDs, reference IDs, owner recipients, provider responses, and exception text are not logged. `npm run verify:owner-notifications-boundary` source-gates the diagnostic code, fallback policy, old silent catch exclusion, and lifecycle docs parity. Follow-up gates passed after this checkpoint patch: `node --check scripts/verification/verify-owner-notifications-boundary.js`, `npm run verify:owner-notifications-boundary`, `npm run verify:functions-deploy-preflight`, `cd functions && npm run build`, `cd functions && npm run lint`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. A scoped MenuList QA Functions deploy was attempted for `functions:computeDecisionBlocksScores,functions:triggerDecisionBlocksScoring,functions:triggerStoreNightlyScheduler`; it completed predeploy lint/build and then failed before upload with Cloud Resource Manager HTTP 403 caller permission for `menulist-qa`. Live effect remains blocked until an account with project access can deploy the changed Functions bundle. This changes staleness lifecycle delivery observability only; it does not change valid lifecycle delivery, staleness detection writes, cooldown semantics, owner-notification migration delivery, valid SMTP sends, message-log schema, Firestore rules/indexes, Storage rules, Vercel deploys, production builds, provider smoke, browser/device QA, production-host smoke, launch approval, or release certification.

SMTP port configuration fail-closed checkpoint: fixed. The Functions SMTP provider and root app SMTP senders no longer default missing `SMTP_PORT` to port `587`. `functions/src/messaging/providers/resend.ts` and shared app helper `src/lib/notifications/smtpConfig.ts` now require an explicit numeric SMTP port in the `1..65535` range before a nodemailer transporter is created. Missing, malformed, or out-of-range port values return the existing local not-configured result without attempting delivery; the Functions provider logs only bounded `hasPort` / `smtpPortValid` metadata. The app-side senders in `src/lib/messaging/index.ts`, `src/lib/notifications/index.ts`, and `src/lib/owner-notifications/channels/email.ts` use that shared helper, and app-side duplicate/rate-limit read failures now log bounded diagnostics and skip sends instead of sending optimistically. `npm run verify:owner-notifications-boundary` source-gates the Functions provider code and lifecycle docs parity; `npm run verify:menulist-api-tenant-safety` source-gates the shared app SMTP helper, the three app senders, the old fallback exclusions, and the app-side fail-closed duplicate/rate-limit control flow. Follow-up gates passed after this expanded checkpoint patch: `node --check scripts/verification/verify-menulist-api-tenant-safety.js`, `node --check scripts/verification/verify-owner-notifications-boundary.js`, `npm run verify:menulist-api-tenant-safety`, `npm run verify:owner-notifications-boundary`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. The prior scoped MenuList QA Functions deploy attempt for `functions:verifyMenuPublish,functions:gcpBudgetAlertWebhook,functions:computeDecisionBlocksScores,functions:triggerDecisionBlocksScoring,functions:triggerStoreNightlyScheduler,functions:menulistMaintenanceScheduler,functions:messagingOnboarding,functions:msgExtractionWatcher` completed predeploy lint/build and then failed before upload with Cloud Resource Manager HTTP 403 caller permission for `menulist-qa`; live Functions effect remains blocked until an account with project access can deploy the changed Functions bundle. The app-side continuation requires the normal Next.js release path; no Vercel deploy was run. This changes SMTP configuration failure handling and app-side safety-read failure handling only; it does not change valid SMTP sends with explicit valid ports, lifecycle message templates, owner-notification migration delivery, normal duplicate skips, normal rate-limit skips, message-log schema, Firestore rules/indexes, Storage rules, Vercel deploys, production builds, provider smoke, browser/device QA, production-host smoke, launch approval, or release certification.

Lifecycle messaging fail-closed checkpoint: fixed. The legacy MenuList Functions lifecycle email sender keeps the owner-notification migration path and valid SMTP behavior unchanged, but message-log safety reads now bias toward silence instead of duplicate sends. If the feature flag read fails, sending is skipped with bounded `MESSAGING_FLAG_CHECK_FAILED` diagnostics. If idempotency or daily rate-limit reads fail, the sender logs `MESSAGING_IDEMPOTENCY_CHECK_FAILED` or `MESSAGING_RATE_LIMIT_CHECK_FAILED` with store/reference presence-length metadata and skips the legacy email send instead of sending optimistically. Retry-send exceptions now log `MESSAGING_RETRY_MARK_FAILED` before marking the original failed message retried. `npm run verify:owner-notifications-boundary` source-gates the Functions source and lifecycle docs parity. Follow-up gates passed after this checkpoint patch: `node --check scripts/verification/verify-owner-notifications-boundary.js`, `npm run verify:owner-notifications-boundary`, `npm run verify:functions-deploy-preflight`, `cd functions && npm run build`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. A scoped MenuList QA Functions deploy was attempted for `functions:verifyMenuPublish,functions:computeDecisionBlocksScores,functions:triggerDecisionBlocksScoring,functions:triggerStoreNightlyScheduler`; it completed predeploy lint/build and then failed before upload with Cloud Resource Manager HTTP 403 caller permission for `menulist-qa`. Live effect remains blocked until an account with project access can deploy the changed Functions bundle. This changes legacy lifecycle email failure handling only; it does not change valid owner-notification migration delivery, valid SMTP sends, duplicate skips, normal rate-limit skips, templates, message-log schema, Firestore rules/indexes, Storage rules, Vercel deploys, production builds, provider smoke, browser/device QA, production-host smoke, launch approval, or release certification.

Root app Sentry DSN fail-closed checkpoint: fixed. The Next.js client, server, and edge Sentry setup keeps the same sanitizer and expected-error filtering behavior, but `src/lib/monitoring/sentryShared.ts` no longer embeds dev or production Sentry DSN fallbacks in source. Browser Sentry now reads only `NEXT_PUBLIC_SENTRY_DSN` / `NEXT_PUBLIC_SENTRY_DEV_DSN`; server and edge Sentry read `SENTRY_DSN` or local `SENTRY_DEV_DSN`, with the same app public DSN as fallback. If no matching DSN is configured, `isSentryMonitoringEnabled` remains false and the Sentry SDK is not initialized by `instrumentation-client.ts`, `sentry.server.config.ts`, or `sentry.edge.config.ts`. The platform Sentry diagnostics page now says the flag and DSN config are both required. `npm run verify:auth-security-failure-matrix` source-gates the explicit DSN readers, hardcoded root app DSN exclusion, diagnostics copy, and docs parity. Follow-up gates passed after this checkpoint patch: `node --check scripts/verification/verify-auth-security-failure-matrix.js`, `npm run verify:auth-security-failure-matrix`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes root app Sentry configuration only; it does not change monitoring sanitization, Functions Sentry setup, logger console output, owner/customer UI, Firestore reads/writes, Storage operations, Cloud Functions, Firebase/Vercel deploys, production builds, browser/device QA, Sentry project setup, production-host smoke, launch approval, or release certification.

Customer App service-worker degraded fallback diagnostics checkpoint: fixed. Customer App and owner PWA service-worker selection keeps the same valid behavior: known owner/customer/MyCodex worker URLs still map to bounded worker labels, malformed registered worker script URLs still degrade to the safe `unknown` label, and public-route stale-worker cleanup still uses the existing reload fallback when the session guard cannot be stored. Those degraded paths are no longer silent. `src/components/ServiceWorkerRegister.tsx` now logs capped `service_worker_script_url_label_parse_failed` diagnostics with script URL presence/length and shape metadata, and logs `service_worker_public_cleanup_reload_storage_failed` when the public cleanup reload session guard cannot read or write. Raw hostnames, raw script URLs, route paths, store IDs, tenant IDs, customer identity, storage values, and browser exception text are not logged. `npm run verify:customer-app-pwa` and `npm run verify:auth-security-failure-matrix` source-gate the diagnostic codes, caps, bounded metadata, fallback policies, docs parity, and old silent-catch exclusions. Follow-up gates passed after this checkpoint patch: `node --check scripts/verification/verify-customer-app-pwa.js`, `npm run verify:customer-app-pwa`, `node --check scripts/verification/verify-auth-security-failure-matrix.js`, `npm run verify:auth-security-failure-matrix`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes service-worker fallback observability only; it does not change valid worker selection, worker scripts, worker cache policy, install prompt behavior, manifest behavior, Customer App analytics, Firestore reads/writes, Storage operations, Cloud Functions, cache invalidations, Firebase/Vercel deploys, production builds, browser/device QA, production-host smoke, launch approval, or release certification.

Website analytics consent storage diagnostics checkpoint: fixed. The public website analytics gate keeps the same consent behavior: analytics children still render only after an accepted public cookie choice, Plausible still skips events before accepted consent, and failed banner writes still keep the runtime choice for the current page view. LocalStorage read/write failures are no longer silent. `PublicCookieConsentBanner` logs bounded `public_cookie_consent_storage_failed` diagnostics with operation, storage-key presence/length, and fixed read/write fallback policy; the Plausible helper logs bounded `public_website_plausible_consent_read_failed` diagnostics and keeps the fail-closed `skip_plausible_event_until_consent_known` policy. Raw storage keys, consent event names, page URLs, referrers, user identifiers, tenant/store/project identifiers, and browser exception text are not logged. `npm run verify:website-public-copy-boundary` source-gates the diagnostics, fallback policies, bounded metadata, silent-catch exclusions, and website docs parity. Follow-up gates passed after this checkpoint patch: `node --check scripts/verification/verify-website-public-copy-boundary.js`, `npm run verify:website-public-copy-boundary`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes public website consent-storage observability only; it does not change analytics consent choices, valid Plausible/GA4/Clarity loading, public website copy, owner/customer analytics, Firestore reads/writes, Storage operations, Cloud Functions, Firebase/Vercel deploys, production builds, browser/device QA, production-host smoke, launch approval, or release certification.

Website analytics Clarity configuration checkpoint: fixed. The main MenuList marketing website keeps the same analytics consent behavior: `src/app/(website)/layout.tsx` mounts `WebsiteAnalyticsConsent`, and `PublicCookieConsentBanner` renders analytics children only after an accepted analytics choice. `src/components/website/ClarityAnalytics.tsx` no longer falls back to a committed Microsoft Clarity project ID; Clarity now requires an explicit alphanumeric `NEXT_PUBLIC_CLARITY_ID` and returns `null` when the value is absent or malformed. `npm run verify:website-public-copy-boundary` source-gates the consent mount, accepted-only child rendering, Clarity env/shape gate, hardcoded project fallback exclusion, and docs parity. Follow-up gates passed after this checkpoint patch: `node --check scripts/verification/verify-website-public-copy-boundary.js`, `npm run verify:website-public-copy-boundary`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes public website Clarity configuration only; it does not change Plausible loading, Google Analytics loading, website marketing events, owner dashboard analytics, customer menu/OBP analytics, Firestore reads/writes, Storage operations, Cloud Functions, Firebase/Vercel deploys, production builds, browser/device QA, production-host smoke, launch approval, or release certification.

CORS blocked-origin diagnostics checkpoint: fixed. `src/lib/security/corsValidation.ts` keeps the same CORS allow/deny behavior: same-origin requests still pass, configured HTTPS origins and HTTPS subdomains still pass, development localhost origins stay development-only, and rejected origins still return the existing CORS policy failure. Blocked-origin logging no longer sends the raw Origin header value or the full allowed-origin list to the logger; it now emits `cors_origin_blocked` through bounded security diagnostics with origin presence/length, parse/protocol/credential booleans, and allowed-origin count only. Raw Origin values, allowed origins, domains, IP addresses, user agents, request URLs, and customer/owner paths are not logged by this helper. `npm run verify:auth-security-failure-matrix` source-gates the bounded diagnostic helper, raw-origin exclusion, CORS docs parity, and existing production-localhost allowlist boundary. Follow-up gates passed after this checkpoint patch: `node --check scripts/verification/verify-auth-security-failure-matrix.js`, `npm run verify:auth-security-failure-matrix`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes CORS blocked-origin observability only; it does not change valid origin matching, rejected-origin responses, public or owner route behavior, Firestore reads/writes, Storage operations, Cloud Functions, Firebase/Vercel deploys, production builds, browser/device QA, production-host smoke, launch approval, or release certification.

CSP report malformed JSON diagnostics checkpoint: fixed. `/api/csp-report` keeps the existing anonymous report contract: rate limiting still happens before the 32KB bounded text body read, valid CSP reports still log production security events with stable directive/URI categories, malformed JSON reports still return the standard non-blocking 204 response, and unexpected route failures still use `csp_report_processing_failed`. The malformed JSON path no longer drops parse failures silently; it now logs capped `csp_report_json_parse_failed` diagnostics with body shape, body length, content-type/referrer/user-agent/request-IP-hash presence-length metadata, fixed `ignore_malformed_report` fallback policy, and source error name only. Raw report bodies, blocked URIs, source files, referrer values, user agents, IP addresses, full request URLs, and parser exception messages are not logged. `npm run verify:menulist-api-tenant-safety` source-gates the diagnostic code, capped guard, fallback policy, CSP docs parity, and raw body preview exclusion. Follow-up gates passed after this checkpoint patch: `npm run verify:menulist-api-tenant-safety`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes malformed CSP report observability only; it does not change valid CSP report handling, CSP headers, customer/owner UI, Firestore reads/writes, Storage operations, Cloud Functions, cache invalidations, Firebase/Vercel deploys, production builds, browser/device QA, production-host smoke, launch approval, or release certification.

Razorpay normal-path debug breadcrumb checkpoint: fixed. Payment plan lookup and webhook handling keep the same valid behavior: `getOrCreateRazorpayPlan()` still deduplicates plans by product/user/plan/interval/currency/price lookup keys, returns existing provider plans, creates missing plans, logs bounded found/create/failure diagnostics, and throws the same fixed local failure text; `/api/razorpay/webhook` still validates declared body size, rate limits by webhook IP, reads a bounded raw body, validates the Razorpay signature, claims idempotency, processes known payment/subscription events, writes local payment audits, and marks webhook status. Redundant normal-path `logger.debug()` breadcrumbs for plan lookup start and unhandled webhook event types were removed. Unhandled webhook events still write the existing `RAZORPAY_WEBHOOK_UNHANDLED_EVENT` local audit row with the bounded audit summary. `npm run verify:menulist-api-tenant-safety` source-gates this by rejecting the removed Razorpay debug labels and requiring the unhandled-event audit row. Follow-up gates passed after this checkpoint patch: `node --check scripts/verification/verify-menulist-api-tenant-safety.js`, `node --check scripts/verification/verify-billing-entitlement-boundary.js`, `npm run verify:menulist-api-tenant-safety`, `npm run verify:billing-entitlement-boundary`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes payment debug diagnostics only; it does not change Razorpay provider calls, webhook signature validation, webhook idempotency, subscription/top-up writes, entitlement sync, local webhook audit rows, Firestore rules/indexes, Cloud Functions, cache invalidations, Firebase/Vercel deploys, production builds, browser/device QA, Razorpay sandbox smoke, launch approval, or release certification.

Public language parameter parse fallback checkpoint: fixed. Public menu and OBP language-preserving links keep the same valid behavior: valid relative and absolute public URLs still receive a normalized `?lang=xx` value while preserving existing query strings and fragments. `appendPublicLanguageParam()` no longer falls back to raw string concatenation when URL parsing fails; malformed inputs now log bounded `public_language_param_url_parse_failed` diagnostics with URL/language presence-length metadata, relative/query/hash shape, fixed `return_original_url` fallback policy, and normalized source error metadata only, then return the original URL unchanged. Raw URLs, query strings, fragments, custom domains, subdomains, store IDs, tenant IDs, project slugs, language labels, and exception text are not logged. `npm run verify:url-routing-boundary` source-gates this by requiring the diagnostic, capped guard, fallback policy, URL-routing docs parity, and rejecting the old raw string-concat fallback. Follow-up gates passed after this checkpoint patch: `npm run verify:url-routing-boundary` and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes public-routing language-link fallback behavior only on malformed URL inputs; it does not change valid public language links, menu rendering, OBP rendering, canonical routing, Firestore reads/writes, analytics writes, Storage operations, Cloud Functions, API routes, cache invalidations, Firebase/Vercel deploys, production builds, browser/device QA, production-host smoke, launch approval, or release certification.

AI image route normal-path debug breadcrumb checkpoint: fixed. `/api/image-generation` and `/api/image-editing` keep the same valid owner image behavior: valid single-image generation still computes prompts, checks capacity before provider work, calls the provider, writes AI accounting through the shared finalizer, records bounded local summaries, and returns base64 image previews; valid image editing still generates the edit prompt, calls the provider with the reference image and optional prompt images, writes AI accounting through the shared finalizer, records bounded local summaries, and returns edited image previews. Redundant normal-path `logger.debug()` breadcrumbs for prompt counts, recorded transaction IDs, provider start/finish counts, and generated edit-prompt length were removed. Security and failure paths still use the existing bounded route diagnostics. `npm run verify:ai-accounting` source-gates this by rejecting the removed single-image and edit-image debug labels. Follow-up gates passed after this checkpoint patch: `node --check scripts/verification/verify-ai-accounting-hardening.js`, `npm run verify:ai-accounting`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes route debug diagnostics only; it does not change prompt generation, provider calls, generated image previews, image editing output, AI capacity checks, AI accounting write count/order/credit behavior, owner acceptance/upload flow, Firestore writes, Storage operations, Cloud Functions, cache invalidations, Firebase/Vercel deploys, production builds, browser/device QA, provider smoke, launch approval, or release certification.

Create Menu success URL normalization checkpoint: fixed. `/create-menu/success` keeps the same valid post-claim owner handoff behavior: valid published menu and official business page URLs still render, Copy Link still copies the menu URL, and WhatsApp share still composes the localized share text. The browser client now normalizes raw `menuUrl` and `officialPageUrl` query-string values before any `href`, copy, or share use. Invalid, oversized, whitespace-containing, non-HTTPS, credentialed, or malformed query values are treated as absent and log bounded `public_create_menu_success_url_invalid` diagnostics with URL kind, invalid reason, presence/length, protocol/whitespace shape, a capped per-shape guard, and normalized source error metadata only. Raw query-string URLs, generated WhatsApp messages, browser exception text, session-storage claim payloads, and store IDs are not logged. `npm run verify:menu-extraction-pipeline` source-gates this by requiring the success URL normalizer and rejecting raw query URL assignments in the success page. Follow-up gates passed after this checkpoint patch: `npm run verify:production-readiness-local` with 93/93 checks, including `npm run verify:menu-extraction-pipeline`, 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes browser-local success-page URL handling only; it does not change valid claim/publish URLs, preview polling, public menu rendering, QR rendering, copy/open behavior for valid URLs, starter activation writes, successful-path Firestore reads/writes, Storage operations, Cloud Functions, API routes, cache invalidations, Firebase/Vercel deploys, production builds, browser/device QA, production-host smoke, launch approval, or release certification.

Batch image worker normal-path debug breadcrumb checkpoint: fixed. `/api/image-generation/batch-generation` keeps the same Cloud Tasks worker behavior: valid tasks still validate the worker secret and bounded body, read the scoped batch job, skip terminal or already-generated items, check capacity before provider work, generate/upload images, finalize AI accounting, append the item result, write bounded local start/success/job-updated logs, and return the same fixed worker success copy. Redundant normal-path `logger.debug()` breadcrumbs for fetched job data, uploaded images, recorded transaction IDs, remaining capacity balance, and batch job updates were removed. Failure and security paths still use the existing bounded runtime/security diagnostics, while durable local logs continue to use summarized item/config/image/job fields through the local-log sanitizer. `npm run verify:ai-accounting` source-gates this by rejecting the removed worker debug labels. Follow-up gates passed after this checkpoint patch: `node --check scripts/verification/verify-ai-accounting-hardening.js`, `npm run verify:ai-accounting`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes worker debug diagnostics only; it does not change valid task admission, idempotent skips, capacity checks, provider calls, generated images, Storage writes, AI accounting write count/order/credit behavior, batch job writes, owner review flow, Firestore rules/indexes, Cloud Functions, cache invalidations, Firebase/Vercel deploys, production builds, browser/device QA, provider smoke, launch approval, or release certification.

Public menu external link normalization checkpoint: fixed. Customer-facing menu output keeps the same valid contact/action behavior: valid calls, WhatsApp, Maps, reservation, order, social, and review links still render where owners have enabled them. The public menu footer, unavailable-item recovery actions, and feedback nudge now reuse the shared OBP public-link policy before emitting owner-managed public URLs. Invalid, non-HTTPS, wrong-host Maps/review/social, credentialed, oversized, or malformed publicPresence/social/review URLs are suppressed or fall back to internal feedback instead of becoming customer `href` values. `npm run verify:public-business-truth` source-gates the normalized public menu external link paths and rejects raw reservation/order URLs, raw Maps URLs, raw social URL filtering, and raw feedback review URLs in customer-facing menu output. Follow-up gates passed: `npm run verify:public-business-truth` and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes public menu link normalization only; it does not change valid public links, owner public-link saves, menu rendering data reads, analytics event shapes, Firestore reads/writes, Storage operations, Cloud Functions, API routes, cache invalidations, Firebase/Vercel deploys, production builds, browser/device QA, launch approval, or release certification.

Desktop menu upload job-start debug breadcrumb checkpoint: fixed. `getProcessedFile.ts` keeps the same owner upload job flow: existing active-job reuse still returns the existing job ID, new uploads still call `createMenuProcessingJob()`, valid jobs still return the job ID to the caller, and failed job creation still throws fixed owner retry copy after logging `desktop_menu_upload_job_create_failed` through bounded menu-processing diagnostics. The normal start/reuse/success path no longer imports the raw logger or writes debug breadcrumbs containing raw project IDs, job IDs, existing job IDs, or file counts. Server-side route breadcrumbs remain the bounded authority for owner job creation/reuse. `npm run verify:menu-extraction-pipeline` source-gates this by rejecting raw `logger.debug()` breadcrumbs in the desktop processing helper. Follow-up gates passed after this checkpoint patch: `node --check scripts/verification/verify-menu-extraction-pipeline.js`, `npm run verify:menu-extraction-pipeline`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes browser-local debug diagnostics only; it does not change upload admission, active-job reuse, job creation, extraction worker behavior, provider calls, owner copy, Firestore reads/writes, Storage operations, Cloud Functions, cache invalidations, Firebase/Vercel deploys, production builds, browser/device QA, provider smoke, launch approval, or release certification.

Menu editor description-generation returned-error diagnostics checkpoint: fixed. `descriptionGeneration.shared.ts` keeps the same generated-description flow, owner-visible failure behavior, direct-save fallback, project acknowledgement guard, and generated-description persistence for valid output. The returned-error branch now logs `menu_editor_description_generation_returned_error_message` through bounded menu-editor diagnostics with project/file/result-message/message-type presence-length metadata only. Raw returned error messages, file IDs, project IDs, generated descriptions, item text, source language data, target language data, and exception text are not logged by that branch. `npm run verify:public-business-truth` source-gates this by requiring the bounded diagnostic and rejecting the old raw logger warning, raw `resultMessage`, and raw `file.uid` fields in the editor helper. Follow-up gates passed after this checkpoint patch: `node --check scripts/verification/verify-public-business-truth.js`, `node --check scripts/verification/verify-ai-accounting-hardening.js`, `npm run verify:public-business-truth`, `npm run verify:ai-accounting`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes editor-local diagnostics only; it does not change provider calls, description output, owner review/save behavior, project write count/order, Firestore reads/writes, Storage operations, Cloud Functions, cache invalidations, Firebase/Vercel deploys, production builds, browser/device QA, provider smoke, launch approval, or release certification.

AI transaction DB local error diagnostics checkpoint: fixed. `/api/business-copy`, `/api/descriptions`, `/api/image-editing`, `/api/new-item-metadata`, `/api/seo`, and `/api/translations` keep the same valid provider-call, generated-output, owner review/save, AI accounting finalizer, and credit-consumption behavior. Their local `TRANSACTION_DB_ERROR` logs now store `getAIErrorDiagnostics(transactionError)` source-error metadata instead of raw accounting exception objects, while keeping the existing bounded transaction summaries. Raw accounting exception messages and stacks are not written by those local transaction DB log paths. `npm run verify:ai-accounting` source-gates this by requiring bounded transaction DB error diagnostics and rejecting `error: transactionError` in the affected routes. Follow-up gates passed after this checkpoint patch: `node --check scripts/verification/verify-ai-accounting-hardening.js`, `npm run verify:ai-accounting`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes local accounting-error diagnostics only; it does not change successful AI operation writes, credit consumption, provider calls, generated output, owner review/save flows, Firestore writes, Storage operations, Cloud Functions, cache invalidations, Firebase/Vercel deploys, production builds, browser/device QA, provider smoke, launch approval, or release certification.

Text AI operation response summaries checkpoint: fixed. `/api/descriptions`, `/api/translations`, `/api/new-item-metadata`, `/api/business-copy`, `/api/seo`, `/api/reviews/suggest`, `/api/campaigns/caption`, and `/api/menu-card-export/design-advisor` keep their valid owner response contracts: generated descriptions, translations, metadata, copy, review replies, captions, and design advice still return to the authenticated caller for review/save/copy. Successful AI operation input now passes count/shape `clientResponse` summaries with `responseSummaryKind` markers instead of generated owner-facing text objects, and `src/lib/ai/operationLog.ts` preserves those pre-summarized payloads in `accounting_only` mode. `npm run verify:ai-accounting` source-gates the summary helpers, summary markers, raw generated `clientResponse` exclusions, and shared pre-summary preservation path. This changes AI operation ledger payload shape only; it does not change owner API responses, plan/permission/capacity checks, provider calls, AI accounting write count/order/credit behavior, Firestore rules/indexes, Storage operations, Cloud Functions, cache invalidations, Firebase/Vercel deploys, production builds, browser/device QA, provider smoke, launch approval, or release certification.

Item Photo Capture camera-start diagnostics checkpoint: fixed. `src/components/shared/media/ItemPhotoCaptureAssist.tsx` keeps the existing owner fallback behavior: when browser camera startup fails, the component stops any partial stream, shows the fixed blocked-camera copy, and leaves upload available. The startup failure path now logs bounded `item_photo_camera_start_failed` diagnostics with capture-mode/item-name presence-length metadata, camera API availability, video element presence, and normalized source error metadata only. Camera frames, photo bytes, filenames, raw item text, media IDs, tenant/store/project IDs, and browser exception text are not logged. `npm run verify:auth-security-failure-matrix` source-gates the diagnostic code and rejects the old silent startup catch. Follow-up gates passed after this checkpoint patch: `node --check scripts/verification/verify-auth-security-failure-matrix.js`, `node --check scripts/verification/verify-ai-accounting-hardening.js`, `npm run verify:auth-security-failure-matrix`, `npm run verify:ai-accounting`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes browser-local capture observability only; it does not change valid camera capture, blocked-camera upload fallback, readiness scoring, accepted-image upload/save behavior, Firestore reads/writes, Storage operations, Cloud Functions, API routes, Firebase/Vercel deploys, production builds, browser/device QA, launch approval, or release certification.

AI image route local log and accounting input summaries checkpoint: fixed. `/api/image-generation`, `/api/image-editing`, and `/api/image-generation/batch-generation` keep the existing generation/editing behavior: valid provider calls still return base64 image previews or uploaded batch images, successful AI accounting still uses the existing operation finalizer with the same write count/order and credit consumption, and owner acceptance/upload flow is unchanged. Local success, validation, no-image, accounting-error, and batch-start logs now use bounded local request, attempted-data, response, and transaction summaries with prompt/config shape, item field presence/length, provider response counts, MIME types, token/cost totals, transaction IDs, and invalid-request payload shape only. AI operation rows use `itemSummary` and `generationConfigSummary` instead of raw item details or generation config payloads. Raw prompt text, raw item names/descriptions/categories, reference image URLs, generated base64 images, full provider responses, full transaction objects, raw image accounting input payloads, project IDs, file IDs, job IDs, user IDs, and exception text are not written by those route logging/accounting paths. `npm run verify:ai-accounting` source-gates the summary helpers, request/response/transaction/validation summary fields, operation-row summary fields, full transaction-object exclusions, and raw config/item/ID payload exclusions. Follow-up gates passed after this checkpoint patch: `node --check scripts/verification/verify-ai-accounting-hardening.js`, `npm run verify:ai-accounting`, `npm run verify:menulist-api-tenant-safety`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes image route local logging and AI operation ledger payload shape only; it does not change valid provider calls, generated image previews, image editing output, owner acceptance/upload flow, batch generation behavior, AI accounting finalizer/count/order/credit behavior, Storage operations, Cloud Functions, cache invalidations, Firebase/Vercel deploys, production builds, browser/device QA, provider smoke, launch approval, or release certification.

AI image batch-trigger prompt-block/log summaries checkpoint: fixed. `/api/image-generation/batch-trigger` keeps the same bounded body admission, permission/outlet policy, prompt-count capacity check, Cloud Tasks config preflight, requested-item registration, and task enqueue behavior for valid batches. Task-start local logs now write `generationConfigSummary`, `itemCount`, `itemsWithIdCount`, job ID, and prompt quantity instead of sanitized generation config payloads or raw item ID arrays. Prompt-block failures now return `itemsWithoutPromptsCount` instead of raw item IDs or names. `npm run verify:ai-accounting` source-gates the summary helper, summary start-log field, count-only prompt-block response, and raw config/item-array exclusions. This changes local batch-trigger logging and one owner-safe failure response shape only; it does not change provider calls, Cloud Tasks fanout for valid batches, batch job writes, AI capacity checks, Storage operations, Cloud Functions, cache invalidations, Firebase/Vercel deploys, production builds, browser/device QA, provider smoke, launch approval, or release certification.

Campaign Caption provider response parse diagnostics checkpoint: fixed. `/api/campaigns/caption` keeps the valid-output contract: successful caption responses still return `data`, `meta`, `remainingBalance`, and transaction summary after phrase guarding and AI accounting. Fenced JSON and extractable object-fragment JSON now recover before the failure path, while empty, malformed, and non-object provider responses return the generic caption failure without a usable AI operation row or credit consumption. Unrecoverable provider-response parse failures log capped `campaign_caption_provider_response_parse_failed` diagnostics with action/model/project/store/tenant/user/campaign/surface/language presence-length metadata, response length, trimmed length, candidate length, parse stage, fenced-response/object-fragment booleans, fixed `return_caption_generation_failed` policy, and normalized source error metadata only. AI accounting input carries bounded prompt summaries and caption response summaries instead of raw prompt item/business fields or generated caption objects. Raw provider response text, prompt item/business copy, generated captions, project IDs, store IDs, tenant IDs, user IDs, response preview text, raw prompt item/business fields, and exception text are not logged. `npm run verify:ai-accounting` source-gates the parser helper, capped guard, fallback policy, bounded metadata, non-object fail-closed path, docs parity, no raw response preview, no raw prompt item/business fields, and no generated captions in transaction input. Follow-up gates passed after this checkpoint patch: `node --check scripts/verification/verify-ai-accounting-hardening.js`, `npm run verify:ai-accounting`, `npm run verify:menulist-api-tenant-safety`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes Campaign Caption provider-response parse resilience, observability, and AI operation ledger payload shape only; it does not change valid caption output, phrase guard behavior, owner review/copy flow, plan/permission/capacity checks, successful AI accounting write count/order/credit behavior, Firestore writes, Storage operations, Cloud Functions, cache invalidations, Firebase/Vercel deploys, production builds, browser/device QA, provider smoke, launch approval, or release certification.

New item metadata provider response parse diagnostics checkpoint: fixed. `/api/new-item-metadata` keeps the existing fail-closed contract: unusable Gemini JSON returns the existing generic metadata failure without a usable AI operation row or credit consumption. Fenced JSON and extractable object-fragment JSON now recover before the failure path. Unrecoverable provider-response parse failures log capped `new_item_metadata_provider_response_parse_failed` diagnostics with action/content/model/project/file/store/tenant/user/source/target/tone/business/attribute presence-length/count metadata, response length, trimmed length, candidate length, parse stage, fenced-response/object-fragment booleans, fixed `return_metadata_generation_failed` policy, and normalized source error metadata only. The route also records local `API_RESPONSE` logs as response presence/length/usage metadata instead of handing full provider response objects to the local logger, and local success/error logs use bounded request/response/transaction summaries. Raw provider response text, prompt item/language copy, generated metadata, project IDs, file IDs, store IDs, tenant IDs, user IDs, response preview text, full provider response objects, raw prompt item/language payloads, and exception text are not logged. `npm run verify:ai-accounting` source-gates the parser helper, capped guard, fallback policy, bounded metadata, docs parity, no raw response preview, no full provider response object handoff, and no raw prompt item/language payloads in transaction input. Follow-up gates passed after this checkpoint patch: `node --check scripts/verification/verify-ai-accounting-hardening.js`, `npm run verify:ai-accounting`, `npm run verify:menulist-api-tenant-safety`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes New Item Metadata provider-response parse resilience and observability only; it does not change valid generated metadata normalization, owner review/save flow, plan/permission/capacity checks, forbidden-field stripping, successful AI accounting behavior, client save behavior, Firestore writes, Storage operations, Cloud Functions, cache invalidations, Firebase/Vercel deploys, production builds, browser/device QA, provider smoke, launch approval, or release certification.

SEO provider response parse diagnostics checkpoint: fixed. `/api/seo` keeps the existing fail-closed contract: unusable Gemini JSON returns the existing generic SEO failure without a usable AI operation row or credit consumption. Fenced JSON and extractable object-fragment JSON now recover before the failure path. Unrecoverable provider-response parse failures log capped `seo_provider_response_parse_failed` diagnostics with action/request/model/store/tenant/user/source/category/item presence-length/count metadata, response length, trimmed length, candidate length, parse stage, fenced-response/object-fragment booleans, fixed `return_seo_generation_failed` policy, and normalized source error metadata only. Local accounting-error logs now store bounded transaction/result summaries instead of full transaction objects. Raw provider response text, prompt/menu/store copy, generated metadata, store IDs, tenant IDs, user IDs, response preview text, full transaction objects, and exception text are not logged. `npm run verify:ai-accounting` source-gates the parser helper, capped guard, fallback policy, bounded metadata, docs parity, no raw response preview, and local accounting-error logs. Follow-up gates passed after this checkpoint patch: `node --check scripts/verification/verify-ai-accounting-hardening.js`, `npm run verify:ai-accounting`, `npm run verify:menulist-api-tenant-safety`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes SEO provider-response parse resilience and observability only; it does not change valid generated SEO metadata normalization, owner review/save flow, plan/permission/capacity checks, successful AI accounting behavior, client save behavior, Firestore writes, Storage operations, Cloud Functions, cache invalidations, Firebase/Vercel deploys, production builds, browser/device QA, provider smoke, launch approval, or release certification.

Description provider response parse diagnostics checkpoint: fixed. `/api/descriptions` keeps the existing fail-closed contract: unusable Gemini JSON returns the existing generic Description failure without a usable AI operation row or credit consumption. Fenced JSON and extractable object-fragment JSON now recover before the failure path. Unrecoverable provider-response parse failures log capped `description_provider_response_parse_failed` diagnostics with action/content/model/project/file/store/tenant/user/source/target/tone/item-count presence-length/count metadata, response length, trimmed length, candidate length, parse stage, fenced-response/object-fragment booleans, fixed `return_description_generation_failed` policy, and normalized source error metadata only. The route also records local `API_RESPONSE` logs as response presence/length/usage metadata instead of handing full provider response objects to the local logger, and local success/error logs use bounded request/response/transaction summaries. Raw provider response text, prompt/menu/item copy, generated descriptions, project IDs, file IDs, store IDs, tenant IDs, user IDs, response preview text, full provider response objects, raw prompt item/language payloads, and exception text are not logged. `npm run verify:ai-accounting` source-gates the parser helper, capped guard, fallback policy, bounded metadata, docs parity, no raw response preview, no full provider response object handoff, no raw prompt item/language payloads in transaction input, and local success/error logs. Follow-up gates passed after this checkpoint patch: `node --check scripts/verification/verify-ai-accounting-hardening.js`, `npm run verify:ai-accounting`, `npm run verify:menulist-api-tenant-safety`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes Description provider-response parse resilience and observability only; it does not change valid generated description normalization, owner review/save flow, linked-outlet policy, plan/permission/capacity checks, successful AI accounting behavior, client save behavior, Firestore writes, Storage operations, Cloud Functions, cache invalidations, Firebase/Vercel deploys, production builds, browser/device QA, provider smoke, launch approval, or release certification.

Business Copy provider response parse diagnostics checkpoint: fixed. `/api/business-copy` keeps the existing retry/fail-closed contract: the first unrecoverable Gemini JSON parse failure retries once, and an unrecoverable retry parse failure returns the existing generic Business Copy failure without a usable AI operation row or credit consumption. Fenced JSON and extractable object-fragment JSON still recover before the retry call. Unrecoverable provider-response parse failures now log capped `business_copy_provider_response_parse_failed` diagnostics with action/request/model/store/tenant/user/source/category/item presence-length/count metadata, attempt, response length, trimmed length, candidate length, parse stage, fenced-response/object-fragment booleans, fixed `retry_once_then_return_business_copy_failed` policy, and normalized source error metadata only. Local accounting-error logs now store bounded transaction/result summaries instead of full transaction objects. Raw provider response text, prompt/menu/store copy, generated copy, store IDs, tenant IDs, user IDs, response preview text, full transaction objects, and exception text are not logged. `npm run verify:ai-accounting` source-gates the parser helper, capped guard, fallback policy, bounded metadata, docs parity, no raw response preview, local accounting-error logs, and absence of the old ad hoc invalid-JSON retry warning. Follow-up gates passed after this checkpoint patch: `node --check scripts/verification/verify-ai-accounting-hardening.js`, `npm run verify:ai-accounting`, `npm run verify:menulist-api-tenant-safety`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes Business Copy provider-response parse observability only; it does not change valid generated copy normalization, owner review/save flow, plan/permission/capacity checks, successful AI accounting behavior, client save behavior, Firestore writes, Storage operations, Cloud Functions, cache invalidations, Firebase/Vercel deploys, production builds, browser/device QA, provider smoke, launch approval, or release certification.

Translation provider response parse diagnostics checkpoint: fixed. `/api/translations` keeps the existing retry/fail-closed contract: the first unrecoverable Gemini JSON parse failure retries once, and an unrecoverable retry parse failure returns the existing generic translation failure without a usable translation, AI operation row, or credit consumption. Fenced JSON and extractable object-fragment JSON can now recover before the retry call, reducing avoidable second provider calls for recoverable formatting. Unrecoverable provider-response parse failures log capped `translation_provider_response_parse_failed` diagnostics with action/file/project/store/tenant/user/source/target/input-key presence-length metadata, attempt, response length, trimmed length, candidate length, parse stage, fenced-response/object-fragment booleans, fixed `retry_once_then_return_translation_failed` policy, and normalized source error metadata only. Raw provider response text, prompt/input JSON, menu text, translated strings, language names, project IDs, file IDs, store IDs, tenant IDs, user IDs, response preview text, raw prompt input/language payloads, and exception text are not logged. AI accounting input and local success/error logs now store bounded input, language, coverage, request, response, and transaction summaries instead of raw prompt input/language payloads, raw coverage arrays, or normalized translation output. `npm run verify:ai-accounting` source-gates the parser helper, capped guard, fallback policy, bounded metadata, docs parity, no raw response preview, no raw prompt input/language payloads in transaction input, local success/error logs, and absence of the old ad hoc invalid-JSON retry warning. Follow-up gates passed after this checkpoint patch: `node --check scripts/verification/verify-ai-accounting-hardening.js`, `npm run verify:ai-accounting`, `npm run verify:menulist-api-tenant-safety`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes translation provider-response parse resilience and observability only; it does not change valid translation normalization, owner review/publish flow, linked-outlet policy, plan/permission/capacity checks, successful AI accounting behavior, client save behavior, Firestore writes, Storage operations, Cloud Functions, cache invalidations, Firebase/Vercel deploys, production builds, browser/device QA, provider smoke, launch approval, or release certification.

Menu Card Export AI advisor provider response parse diagnostics checkpoint: fixed. The Pro/Premium layout suggestion route keeps the existing fail-closed behavior: empty, malformed non-object, or malformed object-fragment Gemini JSON returns the owner-safe suggestion failure, records no usable recommendation, writes no AI operation row, and consumes no credits. Provider-response parse failures now log capped `menu_card_design_advisor_provider_response_parse_failed` diagnostics with project/store/tenant/user/action/model/request presence-length metadata, response length, trimmed length, candidate length, parse stage, fenced-response/object-fragment booleans, fixed `return_layout_suggestion_failed` policy, and normalized source error metadata only. Raw provider response text, menu/source content, preflight warnings, project IDs, store IDs, tenant IDs, user IDs, source hashes, response preview text, and exception text are not logged. `npm run verify:menu-card-export` source-gates the diagnostic code, capped guard, fallback policy, bounded metadata, docs parity, no raw response preview, and absence of the old silent provider JSON parse catch. Follow-up gates passed: `node --check scripts/verification/verify-menu-card-export.js`, `node --check scripts/verification/verify-ai-accounting-hardening.js`, `npm run verify:menu-card-export`, `npm run verify:ai-accounting`, `npm run verify:menulist-api-tenant-safety`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes Menu Card Export AI advisor provider-response parse observability only; it does not change valid layout suggestions, extractable-object JSON fallback success, plan gating, permission checks, capacity checks, AI accounting success behavior, export rendering, PDF/packet output, Firestore reads/writes, Storage operations, Cloud Functions, cache invalidations, Firebase/Vercel deploys, production builds, browser/device QA, provider smoke, launch approval, or release certification.

Menu-intake identity provider response parse diagnostics checkpoint: fixed. Menu-intake identity keeps the existing non-blocking preflight behavior: if Gemini returns malformed fenced JSON, malformed object-fragment JSON, or no parseable object for the identity response, the helper still uses the existing low-confidence fallback analysis so owner upload/job creation can continue through the current path. The provider-response parse fallback now logs capped `menu_intake_identity_provider_response_parse_failed` diagnostics with project/tenant/store/user/source/billing presence-length metadata, response length, trimmed length, candidate length, parse stage, fenced-response/object-fragment booleans, fixed `use_low_confidence_identity_fallback` policy, and normalized source error metadata only. Raw provider response text, extracted menu text, file content, file names, Storage URLs, project IDs, tenant IDs, store IDs, user IDs, decision reasons, and exception text are not logged. `npm run verify:menu-extraction-pipeline` source-gates the parser diagnostic, capped guard, fallback policy, bounded metadata, docs parity, and absence of the old silent provider JSON parse catches. Follow-up gates passed: `node --check scripts/verification/verify-menu-extraction-pipeline.js`, `npm run verify:menu-extraction-pipeline`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes menu-intake identity provider-response parse observability only; it does not change valid identity parsing, invalid-response fallback analysis, owner upload admission, job creation, Storage URL allowlisting, Firestore reads/writes, Storage operations, Cloud Functions, AI provider call rules, cache invalidations, Firebase/Vercel deploys, production builds, browser/device QA, production-host smoke, launch approval, or release certification.

Legacy project QR download diagnostics checkpoint: fixed. The tracked legacy project QR component keeps the existing browser-local behavior: failed branded QR generation or download still shows the fixed owner-facing failed-download message and does not create durable output. The failure path now logs bounded `project_share_legacy_qr_download_failed` diagnostics with share URL, QR URL, store name, logo URL, active plan, color length, QR size, logo-toggle state, fixed `show_qr_download_failed_message` fallback policy, and normalized source error metadata only. Raw URLs, store/business names, logo URLs, color values, QR data URLs, tenant/store/project IDs, and exception text are not logged. `npm run verify:public-business-truth` source-gates the diagnostic code, bounded metadata, docs parity, raw console exclusion, and absence of the old silent legacy QR catch. Follow-up gates passed: `node --check scripts/verification/verify-public-business-truth.js`, `npm run verify:public-business-truth`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes legacy project QR download failure observability only; it does not change successful QR generation, QR file contents, share modal primary QR behavior, public menu routing, public cache behavior, Firestore reads/writes, analytics writes, Storage operations, Cloud Functions, API routes, cache invalidations, Firebase/Vercel deploys, production builds, browser/device QA, launch approval, or release certification.

Customer App manifest start-url diagnostics checkpoint: fixed. Tenant manifest generation keeps the existing store-level launch policy: `/menu` is used when the cached project-summary read confirms an active customer menu, and `/` is used when no active customer menu is known. If that cached summary read fails, `getStoreLevelStartUrl()` still returns `/` so the manifest stays valid, but it now logs capped `customer_app_manifest_start_url_lookup_failed` diagnostics with store-id and project-summary-doc presence/length metadata, fixed `use_root_manifest_start_url` fallback policy, root-start-url fallback state, and normalized source error metadata only. Raw store IDs, tenant IDs, project IDs, project names, project slugs, hostnames, domains, manifest URLs, summary document contents, and exception text are not logged. `npm run verify:customer-app-pwa` source-gates the diagnostic code, capped shape guard, fallback policy, bounded metadata, docs parity, raw console exclusion, and absence of the old silent start-url catch. Follow-up gates passed: `node --check scripts/verification/verify-customer-app-pwa.js`, `npm run verify:customer-app-pwa`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes Customer App manifest start-url degradation observability only; it does not change valid `/menu` start-url selection, root fallback behavior, manifest eligibility, PWA install identity, service-worker behavior, Customer App analytics, Firestore reads/writes, Storage operations, Cloud Functions, API routes, cache invalidations, Firebase/Vercel deploys, production builds, browser/device QA, launch approval, or release certification.

Public menu gradient parser diagnostics checkpoint: fixed. Owner Menu Design keeps the existing gradient parser fallback behavior: malformed saved `linear-gradient(...)` strings still fall back to the current parser result or `null` instead of blocking design editing or public menu rendering. Parser exceptions now log capped `public_menu_gradient_parse_failed` diagnostics with value kind, gradient string length, linear-gradient/angle token shape, approximate stop count, fixed `use_existing_gradient_fallback` policy, and normalized source error metadata only. Raw gradient CSS, color values, owner design payloads, tenant/store/project IDs, customer URLs, and parser exception text are not logged. `npm run verify:public-business-truth` source-gates the diagnostic code, capped shape guard, docs parity, raw console exclusion, and absence of the old silent parser catch. Follow-up gates passed: `node --check scripts/verification/verify-public-business-truth.js`, `npm run verify:public-business-truth`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes owner Menu Design gradient parser observability only; it does not change valid gradient rendering, design save behavior, public menu output for valid gradients, Firestore reads/writes, analytics writes, Storage operations, Cloud Functions, API routes, cache invalidations, Firebase/Vercel deploys, production builds, browser/device QA, launch approval, or release certification.

WhatsApp Action Link malformed-link diagnostics checkpoint: fixed. WhatsApp Action Link Check keeps the existing V0 browser-local behavior: malformed or unsupported owner-entered WhatsApp links are treated as invalid local evidence, the report may still use a separate valid phone number when present, and the tool still does not open links, send messages, verify phone numbers, fetch external targets, store reports, or call providers. Malformed WhatsApp URL parser failures now log capped `whatsapp_action_link_url_parse_failed` diagnostics with value/candidate length, protocol presence, WhatsApp-scheme/host shape booleans, fixed `treat_as_invalid_whatsapp_link` fallback policy, and normalized source error metadata only. Raw WhatsApp links, phone numbers, suggested messages, customer links, generated preview links, report rows, tenant/store/project IDs, and parser exception text are not logged. `npm run verify:whatsapp-action-link-check` source-gates the diagnostic code, capped shape guard, docs parity, V0 no-send/no-open/no-fetch/no-storage boundaries, and locale/discovery coverage. Follow-up gates passed: `node --check scripts/verification/verify-whatsapp-action-link-check.js`, `npm run verify:whatsapp-action-link-check`, `npm run verify:public-truth-tools`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes WhatsApp Action Link Check malformed-link observability only; it does not change valid link recognition, phone-shape checks, report status rules, contact handoff writes, Firestore reads/writes, Storage operations, Cloud Functions, external URL fetches, WhatsApp API calls, provider calls, Firebase/Vercel deploys, production builds, browser/device QA, launch approval, or release certification.

Tenant sitemap diagnostic cap checkpoint: fixed. Tenant sitemap generation keeps the existing fail-closed sitemap behavior: if the master-store lookup fails, the sitemap returns empty; if project-summary lookup fails, affected project entries are omitted; and if outlet lookup fails, outlet entries are omitted. Those degraded paths already logged `tenant_sitemap_master_store_lookup_failed`, `tenant_sitemap_projects_lookup_failed`, and `tenant_sitemap_outlets_lookup_failed`; they now add fixed fallback-policy labels and a capped unique-shape guard so repeated crawler requests cannot flood logs with the same failure shape. Diagnostics include only subdomain/custom-domain/store/tenant presence-length metadata and source error name. Raw hostnames, domains, tenant IDs, store IDs, project slugs, sitemap URLs, and exception text are not logged. `npm run verify:url-routing-boundary` source-gates the diagnostic cap, fallback policies, docs parity, safe public path-segment boundary, and raw-route exclusion boundary. Follow-up gates passed: `node --check scripts/verification/verify-url-routing-boundary.js`, `npm run verify:url-routing-boundary`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes tenant sitemap observability only; it does not change valid sitemap entries, indexability decisions, middleware rewrites, public menu/OBP rendering, Firestore reads/writes, Storage operations, Cloud Functions, cache invalidations, Firebase/Vercel deploys, production builds, browser/device QA, launch approval, or release certification.

Public Truth owner menu URL diagnostics checkpoint: fixed. Public Truth owner readiness keeps the existing best-effort menu URL behavior: if the current store/project summary cannot produce a MenuList customer link, `publicLinks.menuUrl` is omitted and the rest of the owner readiness report still renders from current MenuList truth. Failed `generateProjectUrl(...)` calls now log bounded `public_truth_owner_menu_url_generation_failed` diagnostics with subdomain/custom-domain presence and length, project slug/name/project-id shape metadata, default-project state, fixed `omit_menu_url` fallback policy, a capped shape guard, and normalized source error metadata only. Raw domains, generated URLs, project names, project slugs, project IDs, tenant/store IDs, and exception text are not logged. `npm run verify:public-truth-tools` source-gates the diagnostic code, capped guard, bounded metadata, docs parity, and raw domain/project exclusion boundary. Follow-up gates passed: `node --check scripts/verification/verify-public-truth-tools.js`, `npm run verify:public-truth-tools`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes Public Truth owner readiness menu URL observability only; it does not change valid customer links, owner readiness module status, setup job lists, Business Health UI, Firestore reads/writes, Storage operations, Cloud Functions, cache invalidations, external URL fetches, DNS lookups, provider calls, Firebase/Vercel deploys, production builds, browser/device QA, launch approval, or release certification.

Shareable Tool Reports decode diagnostics checkpoint: fixed. Shareable Tool Reports keep the existing public hash-fragment report behavior: valid links decode and render locally, missing hashes show the empty state, and invalid, oversized, malformed, or wrong-shape payloads show the invalid-report state without report storage. Decode failures now log bounded `shareable_tool_report_payload_decode_failed` diagnostics with failure stage, hash input length, encoded payload length, decoded payload length when available, hash/key shape booleans, max-length booleans, fixed `show_invalid_report_state` fallback policy, a capped shape guard, and normalized source error metadata only. Raw hash payloads, decoded JSON, business names, business context, evidence rows, setup jobs, contact details, tenant/store/project IDs, and exception text are not logged. `npm run verify:shareable-tool-reports` source-gates the diagnostic code, capped guard, failure-stage coverage, docs parity, and report-content logging exclusion boundary. Follow-up gates passed: `node --check scripts/verification/verify-shareable-tool-reports.js`, `npm run verify:shareable-tool-reports`, `npm run verify:public-truth-tools`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes Shareable Tool Reports hash decode observability only; it does not change valid report links, invalid-link UI behavior, report payload schema, follow-up contact writes, Firestore reads/writes, Storage operations, Cloud Functions, external URL fetches, DNS lookups, provider calls, saved reports, report API routes, Firebase/Vercel deploys, production builds, browser/device QA, launch approval, or release certification.

Public Truth Tools URL parse diagnostics checkpoint: fixed. Public Truth Tools keep the existing browser-local public HTTPS URL boundary: malformed owner-entered public/customer links are treated as missing or invalid local hints, local/private/insecure/raw-IP/credentialed links are not counted as ready, and report builders still do not fetch, open, resolve, crawl, inspect, store, or mutate URL targets. Malformed URL parser failures now log bounded `public_truth_tool_url_parse_failed` diagnostics with stable source labels, value kind, value string length, candidate length, explicit-protocol shape, fixed `treat_as_missing_public_url` fallback policy, a capped shape guard, and normalized source error metadata only. Raw entered URLs, hostnames, paths, query strings, fragments, report payloads, tenant/store/project IDs, and parser exception text are not logged. `npm run verify:public-truth-tools` source-gates the diagnostic code, capped guard, source-labelled report calls, docs parity, and raw-URL exclusion boundary. Follow-up gates passed: `node --check scripts/verification/verify-public-truth-tools.js`, `npm run verify:public-truth-tools`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes Public Truth Tools URL parse observability only; it does not change public HTTPS validation results, URL evidence text, report output, follow-up contact writes, Firestore reads/writes, Storage operations, Cloud Functions, external URL fetches, DNS lookups, provider calls, saved reports, Firebase/Vercel deploys, production builds, browser/device QA, launch approval, or release certification.

Guest Feedback review URL parse diagnostics checkpoint: fixed. Guest Feedback keeps the existing safe review URL behavior: configured store review URLs and public-presence Google review URLs are normalized before guest-facing output, and invalid, unsafe, non-Google, non-HTTPS, oversized, or malformed values are treated as absent. Malformed URL parser failures now log bounded `guest_feedback_review_url_parse_failed` diagnostics with fixed `omit_review_url` fallback policy, source label, value type, value presence/length, max-length metadata, a capped shape guard, and normalized source error metadata only. Raw review URLs, query strings, store IDs, tenant IDs, project IDs, guest details, feedback text, and parser exception text are not logged. `npm run verify:guest-feedback-boundary` source-gates the diagnostic code, capped shape guard, source-labelled API/browser call sites, docs parity, and safe review URL runtime behavior. Follow-up gates passed: `node --check scripts/verification/verify-guest-feedback-boundary.js`, `npm run verify:guest-feedback-boundary`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes Guest Feedback review URL observability only; it does not change valid review links, invalid-link omission, feedback submission, feedback writes, owner inbox behavior, Firestore reads/writes, Storage operations, Cloud Functions, cache invalidations, Firebase/Vercel deploys, production builds, browser/device QA, launch approval, or release certification.

Menu Link Import render fallback diagnostics checkpoint: fixed. Menu Link Import keeps the existing optional Chrome-rendered HTML fallback: if the normal fetched source is not usable and the render fallback fails after target validation, the rendered source is skipped and the import continues through the existing source-rejection path when no other usable candidate exists. Render failures now log bounded `menu_link_import_render_fallback_failed` diagnostics with fixed `skip_rendered_html` fallback policy, render host presence-length metadata, timeout metadata, temporary-directory-created state, business category/type presence-length metadata, and normalized source error metadata only. Raw source URLs, rendered URLs, page text, rendered HTML, temporary paths, tenant/store/project IDs, owner-provided URLs, and Chrome exception text are not logged. `npm run verify:menu-extraction-pipeline` source-gates the render fallback diagnostic, bounded render metadata, docs parity, and absence of the old silent render fallback catch. Follow-up gates passed: `node --check scripts/verification/verify-menu-extraction-pipeline.js`, `npm run verify:menu-extraction-pipeline`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes Menu Link Import render fallback observability only; it does not change valid link fetching, SSRF validation, Chrome network isolation, rendered-source acceptance, artifact/job creation, owner-facing import responses, Firestore reads/writes, Storage operations, Cloud Functions, provider calls, cache invalidations, Firebase/Vercel deploys, production builds, browser/device QA, launch approval, or release certification.

Platform Cost Posture timestamp diagnostics checkpoint: fixed. Cost Posture keeps the existing read-model fallback for malformed or unreadable source timestamps: invalid values are omitted from generated timestamps rather than blocking the platform read. Timestamp conversion failures now log bounded `platform_cost_posture_timestamp_parse_failed` diagnostics with source/value-shape metadata, fixed `omit_timestamp` fallback policy, a capped per-shape reporting guard, and normalized source error metadata only. Raw timestamp values, source documents, alert text, tenant/store/project IDs, user IDs, Firestore paths beyond bounded collection/order-field context, and exception text are not logged. `npm run verify:platform-cost-posture-boundary` source-gates the timestamp parser diagnostics, bounded shape guard, docs parity, source-only platform API admission, bounded Admin SDK reads, browser response guard, desktop/mobile navigation, and docs gate. Follow-up gates passed: `node --check scripts/verification/verify-platform-cost-posture-boundary.js`, `npm run verify:platform-cost-posture-boundary`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes Platform Cost Posture timestamp observability only; it does not change valid source reads, valid timestamp display, platform admission, read limits, browser response guards, Firestore reads/writes, Storage operations, Cloud Functions, provider calls, cache tags, Firebase/Vercel deploys, production builds, browser/device QA, launch approval, or release certification.

Business Health packet-cache diagnostics checkpoint: fixed. The Owner Business Assistant server packet cache remains an optimization only: Redis read failures still behave as cache misses, index-read failures still behave as an empty index, write failures still skip cache persistence, and invalidation failures still return the best-effort deletion result. Those degraded paths now log bounded `owner_business_assistant_packet_cache_index_read_failed`, `owner_business_assistant_packet_cache_read_failed`, `owner_business_assistant_packet_cache_write_failed`, and `owner_business_assistant_packet_cache_invalidate_failed` diagnostics with cache-key/index-key and tenant/store/project/profile presence-length metadata, counts, fixed fallback policies, and normalized source error metadata only. Raw Upstash keys, tenant IDs, store IDs, project IDs, context packets, answers, and Redis exception text are not logged. `npm run verify:owner-business-assistant` and `npm run verify:owner-business-health-boundary` source-gate the bounded cache diagnostics, fallback policies, docs parity, and absence of the old silent cache read/invalidation catches. Follow-up gates passed: `npm run verify:owner-business-assistant`, `npm run verify:owner-business-health-boundary`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, and `git diff --check`. This changes Business Health packet-cache observability only; it does not change valid cache hits, Firestore-backed packet generation, answer behavior, Business Health UI, public truth writes, Firestore reads/writes, Storage operations, Cloud Functions, provider calls, cache tags, Firebase/Vercel deploys, production builds, browser/device QA, launch approval, or release certification.

Menu intake preflight file diagnostics checkpoint: fixed. Menu-intake identity keeps the existing non-blocking behavior: if an allowed preflight file passes Storage bucket/prefix and app-server network-target validation but fetch, redirect handling, or bounded response reading throws, that individual file is skipped for the identity prompt and the full extraction path can still proceed. The degraded skip now logs bounded `menu_intake_identity_preflight_file_unreadable` diagnostics with project/tenant/store/user/source/billing/file-type presence-length metadata, file index, file size, fixed `skip_file` fallback policy, and normalized source error metadata only. Raw filenames, URLs, Storage paths, project IDs, tenant IDs, store IDs, user IDs, file content, provider payloads, and exception text are not logged. `npm run verify:menu-extraction-pipeline` source-gates the unreadable-file diagnostic, bounded file context, docs parity, and absence of the old silent preflight-file catch. Follow-up gates passed: `node --check scripts/verification/verify-menu-extraction-pipeline.js`, `npm run verify:menu-extraction-pipeline`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, and `git diff --check`. This changes menu-intake preflight observability only; it does not change valid preflight analysis, owner upload admission, Storage URL allowlisting, full extraction behavior, Firestore reads/writes, Storage operations, Cloud Functions, AI provider call rules, cache invalidations, Firebase/Vercel deploys, production builds, browser/device QA, production-host smoke, launch approval, or release certification.

Compliance Pages owner store-lookup diagnostics checkpoint: fixed. `GET /api/compliance` now distinguishes a missing/inactive store or missing contact inputs from a Firestore store lookup failure. The route still uses the authenticated session store, still generates owner previews from pure templates when the store is readable, and still returns `missingData` when contact inputs are unavailable. Store lookup failures now log bounded `compliance_store_lookup_failed` diagnostics with tenant/store presence-length metadata, fixed failure policy, and normalized source error metadata only, then return a fixed 500 response. Raw tenant IDs, store IDs, store data, contact details, policy text, and exception text are not logged. `npm run verify:compliance-pages-boundary` source-gates the typed store lookup result, bounded diagnostics, fixed failure response, docs parity, and absence of the old silent `catch { return null; }` store lookup fallback. Follow-up gates passed: `node --check scripts/verification/verify-compliance-pages-boundary.js`, `npm run verify:compliance-pages-boundary`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, and `git diff --check`. This changes owner compliance load failure handling and observability only; it does not change valid compliance previews, override saves/resets, public compliance rendering, Firestore reads/writes beyond the existing owner load read, Storage operations, Cloud Functions, cache invalidations, Firebase/Vercel deploys, production builds, browser/device QA, production-host smoke, launch approval, or release certification.

Customer App service-worker domain diagnostics checkpoint: fixed. Customer App and owner PWA service-worker selection keeps the existing fail-closed behavior: if tenant-domain resolution fails in the browser, `src/components/ServiceWorkerRegister.tsx` registers no worker rather than attaching the owner, customer, or MyCodex worker to an unknown origin. That degraded path now logs bounded `service_worker_domain_resolution_failed` diagnostics once per browser session. Diagnostics include only host presence/length metadata, window/location availability, fixed failure policy, and normalized source error metadata. Raw hostnames, route paths, service-worker script URLs, store IDs, tenant IDs, customer identity, and exception text are not logged. `npm run verify:customer-app-pwa` source-gates the diagnostic code, one-per-path guard, bounded host context, Customer App docs parity, and absence of the old silent domain-resolution catch. Follow-up gates passed: `node --check scripts/verification/verify-customer-app-pwa.js`, `npm run verify:customer-app-pwa`, `node --check scripts/verification/verify-auth-security-failure-matrix.js`, `npm run verify:auth-security-failure-matrix`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes service-worker selection observability only; it does not change valid owner/customer/MyCodex worker selection, worker scripts, install prompt behavior, manifest behavior, Customer App analytics, Firestore reads/writes, Storage operations, Cloud Functions, cache invalidations, Firebase/Vercel deploys, production builds, browser/device QA, production-host smoke, launch approval, or release certification.

Communication Kit today-hours diagnostics checkpoint: fixed. Customer Communication Kit message generation keeps the existing browser-local, zero-Firebase-cost behavior: copied templates use already-loaded store data and omit unavailable address, phone, or hours lines. `src/lib/communication/messageTemplates.ts` now logs bounded `communication_kit_today_hours_timezone_fallback_failed` diagnostics when invalid timezone resolution falls back to the browser-local day, and logs bounded `communication_kit_today_hours_range_invalid` diagnostics when malformed current-day time ranges are suppressed. Diagnostics include only timezone presence-length metadata, fixed fallback policy, Intl availability, day-key metadata, today-hours value length, range-separator presence, capped per-shape guards, and normalized source error metadata. Raw generated messages, raw public URLs, raw working-hours payloads, store IDs, tenant IDs, customer phone numbers, customer routes, and exception text are not logged. Malformed `HH:mm-HH:mm` values now omit today-hours copy instead of generating unsafe customer-facing text. Follow-up gates passed: `node --check scripts/verification/verify-communication-kit-boundary.js`, `npm run verify:communication-kit-boundary`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes Customer Communication Kit message-generation resilience and observability only; it does not change valid templates, valid hours copy, copy/share/WhatsApp behavior, Firestore reads/writes, Storage operations, Cloud Functions, API routes, Menu Kit generation, printable output generation, Firebase/Vercel deploys, production builds, browser/device QA, launch approval, or release certification.

OBP public link parse diagnostics checkpoint: fixed. Public OBP link normalization keeps the existing fail-closed behavior: invalid, non-HTTPS, wrong-host, credentialed, oversized, or malformed owner-entered public links are hidden from customer `href`, manifest, redirect, and JSON-LD output instead of being rewritten or emitted. `src/lib/obp/publicLinks.ts` now logs bounded `obp_public_link_url_parse_failed` diagnostics when the URL parser rejects an attempted HTTPS candidate. Diagnostics include only value-kind presence-length metadata, candidate length, allowed-host count, fallback-base presence, protocol presence, a capped per-shape reporting guard, and normalized source error metadata. Raw URLs, query strings, domains, paths, social handles, store IDs, tenant IDs, customer routes, and exception text are not logged. Follow-up gates passed: `node --check scripts/verification/verify-official-business-page-boundary.js`, `npm run verify:official-business-page-boundary`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes OBP public-link observability only; it does not change valid public links, invalid-link hiding policy, owner public-link saves, analytics writes, Firestore reads/writes, Storage operations, Cloud Functions, API routes, public cache behavior, Firebase/Vercel deploys, production builds, browser/device QA, launch approval, or release certification.

Hours status fallback diagnostics checkpoint: fixed. Public hours status keeps the existing valid-data behavior across `src/lib/hours/hoursEngine.ts` and the OBP-specific `src/lib/obp/hoursStatus.ts`: valid saved working hours and valid store timezones still compute Open/Closed status and next-change copy. Invalid timezone resolution still falls back to local/browser time instead of breaking public rendering, but that degraded path now logs bounded `hours_status_timezone_fallback_failed` diagnostics through `src/lib/hours/hoursDiagnostics.ts`. Diagnostics include only timezone presence-length metadata, stable source/fallback-policy labels, Intl availability, a capped per-shape reporting guard, and normalized source error metadata. Malformed current-day time ranges now degrade to `Hours not available`, and invalid future ranges are skipped when calculating next opening copy; invalid range diagnostics log only stable source/day-key labels, value presence/length, separator presence, and capped per-shape metadata. Raw working-hours payloads, raw timezone values, store IDs, tenant IDs, project IDs, customer URLs, dates, and exception text are not logged. Follow-up gates passed: `node --check scripts/verification/verify-working-hours-boundary.js`, `npm run verify:working-hours-boundary`, `npx tsc --noEmit --incremental false --pretty false`, `npm run verify:public-business-truth`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes public hours-status resilience and observability only; it does not change valid working-hours display, valid OBP status display, owner working-hours saves, Today quick-hours saves, time-slot presets, public cache behavior, Firestore reads/writes, Storage operations, Cloud Functions, API routes, Firebase/Vercel deploys, production builds, browser/device QA, launch approval, or release certification.

Hours confidence timestamp diagnostics checkpoint: fixed. Output Control hours-confidence rendering keeps the existing safety policy: missing or unparseable freshness metadata degrades to `Check with store`, while valid `hoursLastUpdatedAt` values still drive trusted/risky/broken display decisions when `ENABLE_OUTPUT_CONTROL` is enabled. `src/lib/outputControl/hoursConfidence.ts` now validates Date, epoch, and serialized timestamp values before stale-day comparison so malformed numeric freshness cannot bypass stale/broken checks through an invalid `Date`. Unexpected timestamp parser failures now log bounded `hours_confidence_timestamp_parse_failed` diagnostics with timestamp value-kind presence-length metadata, string length, finite-number, Date, `toDate`, and `seconds` shape booleans, a capped per-shape reporting guard, and normalized source error metadata. Raw working-hours payloads, timestamps, store IDs, tenant IDs, project IDs, customer URLs, timezone values, and exception text are not logged. Follow-up gates passed: `node --check scripts/verification/verify-public-business-truth.js`, `npm run verify:public-business-truth`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes Output Control hours-confidence resilience and observability only; it does not change valid working-hours display, valid freshness parsing, current `ENABLE_OUTPUT_CONTROL` flag state, owner working-hours saves, public menu fallback policy, OBP fallback policy, Firestore reads/writes, Storage operations, Cloud Functions, API routes, cache invalidations, Firebase/Vercel deploys, production builds, browser/device QA, launch approval, or release certification.

OBP Menu CTA entry-source diagnostics checkpoint: fixed. Official Business Page menu CTA links keep the existing behavior: valid menu URLs are attributed through the shared `withAnalyticsSource(url, 'obp')` helper, and if that helper unexpectedly fails the CTA still falls back to the manual `entry_source=obp` URL instead of blocking menu navigation. `src/app/client/obp/OBPMenuCTA.tsx` now logs bounded `obp_menu_cta_entry_source_fallback_failed` diagnostics for that outer fallback path. Diagnostics include only menu URL presence-length metadata, relative/absolute URL shape, query/hash booleans, a capped per-shape reporting guard, and normalized source error metadata. Raw menu URLs, custom domains, subdomains, store IDs, tenant IDs, project slugs, UTM values, source labels, and exception text are not logged. `npm run verify:public-business-truth` source-gates the diagnostic code, bounded metadata, diagnostic cap, OBP docs parity, and absence of the old silent CTA fallback catch. Follow-up gates passed: `node --check scripts/verification/verify-public-business-truth.js`, `npm run verify:public-business-truth`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes OBP menu CTA attribution observability only; it does not change valid CTA URLs, menu navigation, OBP menu-click analytics, project-switch analytics, Firestore reads/writes, analytics writes, Storage operations, Cloud Functions, API routes, cache invalidations, Firebase/Vercel deploys, production builds, browser/device QA, launch approval, or release certification.

Analytics entry-source inference diagnostics checkpoint: fixed. Shared analytics source-quality inference keeps the existing attribution order: explicit event `entrySource` wins, then browser `entry_source` query/referrer inference, then the existing safe `direct` fallback when browser URL/referrer parsing fails. `src/lib/analytics/unified.ts` now logs bounded `analytics_entry_source_inference_failed` diagnostics when inference has to use that degraded `direct` fallback. Diagnostics include only browser query/referrer presence-length metadata, browser API availability booleans, a capped per-shape reporting guard, and normalized source error metadata. Raw query strings, referrer URLs, UTM values, tenant IDs, store IDs, project IDs, customer routes, source labels, and exception text are not logged. `npm run verify:menulist-api-tenant-safety` source-gates the diagnostic code, bounded metadata, cap, analytics docs parity, and absence of the old silent `direct` downgrade catch. Follow-up gates passed: `node --check scripts/verification/verify-menulist-api-tenant-safety.js`, `npm run verify:menulist-api-tenant-safety`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes shared analytics source-attribution observability only; it does not change valid source attribution, accepted entry-source labels, event admission, analytics event shapes, Firestore reads/writes, analytics writes, GA4 forwarding, Cloud Functions, API routes, cache invalidations, Firebase/Vercel deploys, production builds, browser/device QA, launch approval, or release certification.

Analytics session ID storage diagnostics checkpoint: fixed. Shared public analytics session management keeps the existing non-blocking behavior: anonymous session IDs stay browser-local in `sessionStorage`, and failed session lookup falls back to a fresh anonymous ID for the current runtime call instead of blocking menu analytics or customer rendering. `src/lib/analytics/session.ts` now logs bounded `analytics_session_get_failed`, `analytics_session_refresh_failed`, and `analytics_session_clear_failed` diagnostics for failed sessionStorage get/refresh/clear paths. Diagnostics include only operation labels, storage-key presence-length metadata, session-value presence-length metadata, timeout, browser API availability, fixed fallback policy, and normalized source error metadata. Raw session IDs, timestamps, tenant IDs, store IDs, project IDs, customer routes, and exception text are not logged. `npm run verify:menulist-api-tenant-safety` source-gates the capped session diagnostics, bounded session context, analytics docs parity, and absence of the old unbounded session failure logging. Follow-up gates passed: `node --check scripts/verification/verify-menulist-api-tenant-safety.js`, `npm run verify:menulist-api-tenant-safety`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes browser-local analytics session observability only; it does not change accepted analytics events, session milestone counters, search de-duplication, local analytics queueing, Firestore analytics writes, GA4 forwarding, owner analytics reads, Cloud Functions, API routes, cache behavior, Firebase/Vercel deploys, production builds, browser/device QA, launch approval, or release certification.

Analytics location fallback diagnostics checkpoint: fixed. Shared public analytics location lookup keeps the existing non-blocking behavior: opt-in browser geolocation still rounds successful coordinates, permission denial remains quiet as an expected privacy outcome, non-permission position failures still fall back to timezone, and unexpected lookup failures still return `unknown`. `src/lib/analytics/geo.ts` now logs bounded `analytics_location_lookup_failed` diagnostics for that outer fallback path with geolocation/Intl availability, timezone presence-length metadata, fixed fallback policy, and normalized source error metadata only. Raw coordinates, raw timezones, tenant IDs, store IDs, project IDs, customer routes, session IDs, and exception text are not logged. `npm run verify:menulist-api-tenant-safety` source-gates the bounded location lookup diagnostics, analytics docs parity, and absence of the old unbounded location lookup failure logging. Follow-up gates passed: `node --check scripts/verification/verify-menulist-api-tenant-safety.js`, `npm run verify:menulist-api-tenant-safety`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes analytics location fallback observability only; it does not change location opt-in rules, coordinate rounding, location key format, event admission, Firestore analytics writes, GA4 forwarding, owner analytics reads, Cloud Functions, API routes, cache behavior, Firebase/Vercel deploys, production builds, browser/device QA, launch approval, or release certification.

Analytics timezone validation diagnostics checkpoint: fixed. Shared analytics date-key and business-day helpers keep the existing safe fallback behavior: valid store timezones still drive owner-facing daily docs, hourly buckets, scheduler cache keys, and business-day cutoff logic, while malformed configured timezones fall back to UTC instead of blocking public/customer analytics or owner dashboard reads. `src/lib/analytics/timeZoneDiagnostics.ts` now logs bounded `analytics_timezone_validation_failed` diagnostics when timezone validation falls back. Diagnostics include only timezone presence-length metadata, a fixed source label, fallback policy, window availability, and normalized source error metadata. Raw timezones, tenant IDs, store IDs, project IDs, analytics document IDs, customer routes, dashboard ranges, and exception text are not logged. `npm run verify:menulist-api-tenant-safety` source-gates the shared helper, bounded metadata, diagnostic cap, date-key/business-day caller wiring, analytics docs parity, and absence of the old silent invalid-timezone catch. Follow-up gates passed: `node --check scripts/verification/verify-menulist-api-tenant-safety.js`, `npm run verify:menulist-api-tenant-safety`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes shared analytics fallback observability only; it does not change valid store-local date keys, business-day cutoff behavior, hourly buckets, event admission, analytics event shapes, Firestore reads/writes, analytics writes, GA4 forwarding, Cloud Functions, API routes, cache invalidations, Firebase/Vercel deploys, production builds, browser/device QA, launch approval, or release certification.

Public menu Decision Blocks timezone diagnostics checkpoint: fixed. Public menu Decision Blocks keep the existing availability-first behavior: runtime filtering uses the store timezone when valid, hides blocks that customers cannot act on, and falls back to browser time if timezone formatting fails rather than rendering an error state. `src/components/templates/main-app/projects/b2cView/output/DecisionBlocks.tsx` now logs bounded `public_menu_decision_blocks_timezone_failed` diagnostics when store-time parsing falls back. Diagnostics include only timezone presence-length metadata, window availability, and normalized source error metadata. Raw timezones, store IDs, tenant IDs, project IDs, item IDs, menu names, customer URLs, and exception text are not logged. `npm run verify:public-business-truth` source-gates the diagnostic code, bounded context, one-per-session guard, client-menu docs parity, and absence of the old silent timezone fallback. Follow-up gates passed: `node --check scripts/verification/verify-public-business-truth.js`, `npm run verify:public-business-truth`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes public menu Decision Blocks observability only; it does not change valid store-time availability filtering, block labels, block ordering, click analytics, Firestore reads/writes, analytics writes, Storage operations, Cloud Functions, API routes, cache invalidations, Firebase/Vercel deploys, production builds, browser/device QA, launch approval, or release certification.

Public menu footer freshness diagnostics checkpoint: fixed. Public menu footer update metadata keeps the existing customer-safe policy: valid `lastPublishedAt` values render relative freshness text and machine-readable ISO metadata, while invalid or malformed values omit unverified freshness text instead of displaying a wrong date. `src/components/templates/main-app/projects/b2cView/output/MenuFooter.tsx` now guards the footer timestamp parser and `data-last-updated` ISO conversion so malformed `toDate()` output cannot throw during render. Failed relative-date or ISO metadata parsing logs bounded `public_menu_footer_freshness_relative_failed` and `public_menu_footer_freshness_iso_failed` diagnostics. Diagnostics include only timestamp type, toDate/date booleans, window availability, failure type, and normalized source error metadata. Raw timestamps, project IDs, store IDs, tenant IDs, menu names, dates, route URLs, and exception text are not logged. `npm run verify:public-business-truth` source-gates the diagnostic codes, guarded timestamp helpers, safe freshness rendering, client-menu docs parity, and absence of the old unguarded `data-last-updated` expression and silent timestamp catch. Follow-up gates passed: `node --check scripts/verification/verify-public-business-truth.js`, `npm run verify:public-business-truth`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes public menu footer freshness resilience and observability only; it does not change valid update-date display, menu version display, footer actions, feedback links, analytics event shapes, Firestore reads/writes, analytics writes, Storage operations, Cloud Functions, API routes, cache invalidations, Firebase/Vercel deploys, production builds, browser/device QA, launch approval, or release certification.

Public menu feedback nudge storage diagnostics checkpoint: fixed. Public menu feedback nudges keep the existing behavior: `FeedbackNudge` appears after the existing 18-second timer or 55% scroll-depth trigger, uses a browser-local session guard to avoid repeating in the same tab session when sessionStorage is available, and still keeps feedback/review links usable when sessionStorage is blocked, unavailable, full, or malformed. `src/components/templates/main-app/projects/b2cView/output/FeedbackNudge.tsx` now logs bounded `public_menu_feedback_nudge_storage_read_failed` and `public_menu_feedback_nudge_storage_write_failed` diagnostics for those storage fallback paths. Diagnostics include only session-key and project-id presence-length metadata, window availability, operation, and normalized source error metadata. Raw project IDs, session keys, feedback URLs, review URLs, store IDs, tenant IDs, customer input, route URLs, and exception text are not logged. `npm run verify:public-business-truth` source-gates the diagnostic codes, bounded context, one-per-operation guard, client-menu and Guest Feedback docs parity, and absence of the old silent sessionStorage catches. Follow-up gates passed: `node --check scripts/verification/verify-public-business-truth.js`, `npm run verify:public-business-truth`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes public menu feedback-nudge observability only; it does not change nudge timing, scroll-depth behavior, feedback links, review links, submitted feedback payloads, analytics event shapes, Firestore reads/writes, analytics writes, Storage operations, Cloud Functions, API routes, cache invalidations, Firebase/Vercel deploys, production builds, browser/device QA, launch approval, or release certification.

Public menu language preference storage diagnostics checkpoint: fixed. Public menu language selection keeps the existing behavior: `MenuLanguageSwitcher` restores the project-scoped preferred language from browser storage when available, saves later language selections to that project-specific key, and keeps the current in-memory menu language usable when localStorage is blocked, unavailable, or malformed. `src/components/templates/main-app/projects/b2cView/output/MenuLanguageSwitcher.tsx` now logs bounded `public_menu_language_storage_read_failed` and `public_menu_language_storage_write_failed` diagnostics for those storage fallback paths. Diagnostics include only storage-key and active-language presence-length metadata, project language count, restore setting, window availability, and normalized source error metadata. Raw project IDs, storage keys, language values, menu names, store IDs, tenant IDs, custom domains, subdomains, route URLs, and exception text are not logged. `npm run verify:public-business-truth` source-gates the diagnostic codes, bounded context, one-per-operation guard, client-menu docs parity, and absence of the old silent localStorage catches. Follow-up gates passed: `node --check scripts/verification/verify-public-business-truth.js`, `npm run verify:public-business-truth`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes public menu language-preference observability only; it does not change valid language selection, route `?lang=` behavior, language dropdown rendering, breadcrumb language preservation, analytics event shapes, Firestore reads/writes, analytics writes, Storage operations, Cloud Functions, API routes, cache invalidations, Firebase/Vercel deploys, production builds, browser/device QA, launch approval, or release certification.

Public menu search focus diagnostics checkpoint: fixed. Public menu search keeps the existing first-tap focus behavior: tapping the search row attempts `focus({ preventScroll: true })` so the sticky command row stays stable, then falls back to normal focus if the browser rejects the prevent-scroll option. `src/components/templates/main-app/projects/b2cView/output/MenuSearchBar.tsx` now logs bounded `public_menu_search_focus_prevent_scroll_failed` and `public_menu_search_focus_fallback_failed` diagnostics for those degraded focus paths. Diagnostics include only focus-state, layout-state, browser API availability, and active-element booleans. Raw search terms, menu names, store IDs, tenant IDs, project IDs, route URLs, and exception text are not logged. `npm run verify:public-business-truth` source-gates the diagnostic codes, one-per-path guard, bounded focus context, client-menu docs parity, and absence of the old silent focus fallback catch. Follow-up gates passed: `node --check scripts/verification/verify-public-business-truth.js`, `npm run verify:public-business-truth`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes public menu search focus resilience and observability only; it does not change valid search input behavior, search filtering, suggestions, result scrolling, analytics writes, Firestore reads/writes, Storage operations, Cloud Functions, API routes, cache invalidations, Firebase/Vercel deploys, production builds, browser/device QA, launch approval, or release certification.

Public menu breadcrumb language preservation diagnostics checkpoint: fixed. Public menu breadcrumbs keep the existing behavior: when a customer is on a localized `?lang=` menu URL, `MenuBreadcrumb` attempts to preserve that language on Business/Outlet breadcrumb navigation, and falls back to the original breadcrumb href if URL parsing fails rather than blocking navigation. `src/app/client/[[...slug]]/MenuBreadcrumb.tsx` now logs bounded `public_menu_breadcrumb_language_preserve_failed` diagnostics for that fallback path. Diagnostics include only breadcrumb href and current URL presence-length metadata, window availability, and normalized source error metadata. Raw URLs, query strings, custom domains, subdomains, store IDs, tenant IDs, project slugs, language labels, and exception text are not logged. `npm run verify:public-business-truth` source-gates the diagnostic code, bounded context, one-per-session guard, client-menu docs parity, and absence of the old silent breadcrumb fallback. Follow-up gates passed: `node --check scripts/verification/verify-public-business-truth.js`, `npm run verify:public-business-truth`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes public menu breadcrumb observability only; it does not change valid breadcrumb URLs, selected-language persistence, menu rendering, OBP routing, analytics event shapes, Firestore reads/writes, analytics writes, Storage operations, Cloud Functions, API routes, cache invalidations, Firebase/Vercel deploys, production builds, browser/device QA, launch approval, or release certification.

OBP resolved surface fallback diagnostics checkpoint: public OBP rendering keeps the existing safe fallback behavior for render-time helper failures: invalid timezone/day-key resolution falls back to the host day key, invalid Google Maps embed URLs are hidden, and invalid modified-on freshness timestamps omit freshness text. `src/app/client/obp/OBPResolvedSurface.tsx` now logs bounded `public_obp_today_day_key_timezone_failed`, `public_obp_google_maps_embed_url_parse_failed`, and `public_obp_freshness_timestamp_parse_failed` diagnostics for those fallback paths. Diagnostics include only time-zone, Google Maps URL, and modified-on value-type presence-length metadata plus normalized source error metadata. Raw map URLs, addresses, store IDs, tenant IDs, custom domains, subdomains, working-hours payloads, freshness values, and exception text are not logged. `npm run verify:public-business-truth` source-gates the diagnostic codes, bounded context, one-per-code guard, OBP docs parity, invalid-date handling, and absence of the old silent render fallbacks. Follow-up gates passed: `node --check scripts/verification/verify-public-business-truth.js`, `npm run verify:public-business-truth`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes public OBP render observability only; it does not change valid hours display, valid map embed output, valid freshness text, fallback display behavior, OBP analytics event shapes, Firestore reads/writes, analytics writes, Storage operations, Cloud Functions, API routes, cache invalidations, Firebase/Vercel deploys, production builds, browser/device QA, launch approval, or release certification.

OBP language switch attribution diagnostics checkpoint: public OBP language links keep the existing behavior: `OBPLanguageSwitcher` preserves `entry_source`, `utm_source`, `utm_medium`, `utm_campaign`, and `utm_content` when URL parsing succeeds, and falls back to the generated language URL if attribution preservation fails rather than blocking language switching. `src/app/client/obp/OBPLanguageSwitcher.tsx` now logs bounded `obp_language_switcher_attribution_preserve_failed` diagnostics for that fallback path. Diagnostics include only base URL, language code, and generated language URL presence-length metadata, attribution parameter count, search-param presence, and normalized source error metadata. Raw URLs, UTM values, entry-source values, language labels, custom domains, subdomains, store IDs, tenant IDs, and exception text are not logged. `npm run verify:public-business-truth` source-gates the diagnostic code, bounded context, one-per-session guard, OBP docs parity, and absence of the old silent language-link fallback. Follow-up gates passed: `node --check scripts/verification/verify-public-business-truth.js`, `npm run verify:public-business-truth`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes public OBP language-link observability only; it does not change valid language switch URLs, attribution parameter preservation on valid URLs, language rendering, OBP analytics event shapes, Firestore reads/writes, analytics writes, Storage operations, Cloud Functions, API routes, cache invalidations, Firebase/Vercel deploys, production builds, browser/device QA, launch approval, or release certification.

Analytics source attribution URL diagnostics checkpoint: shared public/owner link attribution keeps the existing safe fallback policy: if `withAnalyticsSource()` cannot parse a URL, it still appends the encoded `entry_source` value manually and returns the link rather than blocking menu, OBP, QR, share, mobile, or communication-kit flows. `src/lib/analytics/sourceAttribution.ts` now logs bounded `analytics_source_attribution_url_parse_failed` diagnostics for that parse-fallback path. Diagnostics include only source URL and entry-source presence-length metadata, absolute/relative URL shape, query/hash booleans, a capped per-shape reporting guard, and normalized source error metadata. Raw URLs, custom domains, subdomains, store IDs, tenant IDs, campaign values, share messages, QR payloads, and exception text are not logged. `npm run verify:public-business-truth` source-gates the diagnostic code, bounded context, diagnostic cap, analytics docs parity, and absence of the old silent parse fallback. Follow-up gates passed: `node --check scripts/verification/verify-public-business-truth.js`, `npm run verify:public-business-truth`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes shared source-attribution observability only; it does not change generated share URLs on valid inputs, manual `entry_source` fallback behavior on malformed inputs, analytics event shapes, Firestore reads/writes, analytics writes, Storage operations, Cloud Functions, API routes, cache invalidations, Firebase/Vercel deploys, production builds, browser/device QA, launch approval, or release certification.

Tenant sitemap read diagnostics checkpoint: tenant sitemap generation keeps the existing fail-closed discovery policy: if master-store lookup, project-summary parsing, or outlet-summary/read lookup fails, sitemap generation returns the existing safe empty/null fallback rather than exposing unverified public entries. `src/app/client/sitemap.ts` now logs bounded `tenant_sitemap_master_store_lookup_failed`, `tenant_sitemap_projects_lookup_failed`, and `tenant_sitemap_outlets_lookup_failed` diagnostics for those degraded lookup paths. Diagnostics include only failure code, failure type, subdomain/custom-domain/store/master-store/tenant presence-length metadata, and normalized source error name. Raw subdomains, custom domains, store IDs, tenant IDs, outlet slugs, project slugs, provider payloads, and exception text are not logged. `npm run verify:public-business-truth` and `npm run verify:url-routing-boundary` source-gate the diagnostic codes, bounded context, docs parity, path-segment boundary, and absence of the old silent lookup catches. Follow-up gates passed: `node --check scripts/verification/verify-public-business-truth.js`, `node --check scripts/verification/verify-url-routing-boundary.js`, `npm run verify:public-business-truth`, `npm run verify:url-routing-boundary`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes tenant sitemap observability only; it does not change sitemap inclusion rules, public truth indexability gates, tenant routing, slug normalization, Firestore reads/writes, analytics writes, Storage operations, Cloud Functions, API routes, cache invalidations, Firebase/Vercel deploys, production builds, browser custom-domain smoke, production-host smoke, launch approval, or release certification.

Public menu/OBP canonical URL diagnostics checkpoint: public metadata still preserves the existing canonical safety policy: malformed stored OBP canonical URLs fall back to the generated current public URL, and valid stored canonical URLs are accepted only when their host matches the generated current URL or tenant canonical base. `src/app/client/[[...slug]]/page.tsx` now logs bounded `public_menu_resolution_canonical_url_parse_failed` diagnostics when stored canonical URL parsing fails. Diagnostics include only tenant/store/slug/canonical-url presence-length metadata plus normalized source error metadata. Raw stored canonical URLs, custom domains, subdomains, query strings, store IDs, tenant IDs, menu names, and browser/provider exception text are not logged. `npm run verify:public-business-truth` source-gates the failure code, bounded canonical metadata, metadata call-site context, docs parity, and direct-console bans. Follow-up gates passed: `node --check scripts/verification/verify-public-business-truth.js`, `npm run verify:public-business-truth`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes public metadata observability only; it does not change canonical URL output for valid stored URLs, generated fallback canonical URLs, public routing, Firestore reads/writes, analytics writes, Storage operations, Cloud Functions, cache invalidations, Firebase/Vercel deploys, production builds, browser custom-domain smoke, production-host smoke, launch approval, or release certification.

Customer App install detection and URL intent diagnostics checkpoint: Customer App install-mode detection, shortcut `entry_source` parsing, and direct-install `pwa` intent parsing keep their existing fail-closed behavior: install detection returns false when browser APIs fail, shortcut parsing returns null for invalid source values, and direct install intent returns false for invalid query values. `src/lib/pwa/installDetection.ts`, `src/lib/pwa/shortcutSourceDetector.ts`, and `src/lib/pwa/visitCounter.ts` now log bounded `customer_app_install_detection_failed`, `customer_app_shortcut_source_parse_failed`, and `customer_app_direct_install_intent_parse_failed` diagnostics once per failure path. Diagnostics include only browser capability booleans or URL search presence-length metadata plus normalized source error metadata. Raw query strings, store IDs, tenant IDs, customer identity, shortcut URLs, and browser exception text are not logged. `npm run verify:customer-app-pwa` source-gates the diagnostic codes, one-per-path guards, bounded context, direct-console bans, docs parity, and absence of the old silent fail-closed catches. Follow-up gates passed: `node --check scripts/verification/verify-customer-app-pwa.js`, `npm run verify:customer-app-pwa`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes browser-local Customer App PWA diagnostics only; it does not change install prompt eligibility, shortcut event tracking on valid inputs, direct-install intent behavior on valid links, analytics event shapes, Firestore analytics counters, public analytics route validation, Storage operations, Cloud Functions, cache invalidations, Firebase/Vercel deploys, production builds, browser/device QA, production-host smoke, launch approval, or release certification.

Official Business Page server fallback diagnostics checkpoint: public OBP rendering keeps the same customer-safe fallback behavior when menu-summary lookup, menu-resolution timeout, or tenant-store-count lookup fails: menu CTA data falls back to a non-menu state and master-brand outlet count falls back to single-store rendering. `src/app/client/obp/OBPContent.tsx` now logs bounded `public_obp_menu_info_lookup_failed`, `public_obp_menu_info_resolution_failed`, and `public_obp_store_count_lookup_failed` diagnostics for those fallback paths. Diagnostics include only store, tenant, tenant-type, active-special-menu, and operation presence-length metadata plus normalized source error metadata. Raw store IDs, tenant IDs, special menu IDs, domains, menu names, project slugs, provider payloads, and exception text are not logged. `npm run verify:official-business-page-boundary` and `npm run verify:public-business-truth` source-gate the diagnostic codes, bounded context, direct-console bans, docs parity, and absence of the old silent fallback shapes. Follow-up gates passed: `node --check scripts/verification/verify-official-business-page-boundary.js`, `npm run verify:official-business-page-boundary`, `node --check scripts/verification/verify-public-business-truth.js`, `npm run verify:public-business-truth`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes public OBP server observability only; it does not change menu CTA behavior on valid reads, brand/outlet routing, tenant lookup, public cache TTL, Firestore reads/writes, analytics writes, Storage operations, Cloud Functions, cache invalidations, Firebase/Vercel deploys, production builds, browser custom-domain smoke, production-host smoke, launch approval, or release certification.

Compliance Pages public override-read diagnostics checkpoint: public `/privacy`, `/terms`, and `/refund` rendering no longer hides compliance override lookup failures. `src/app/client/compliance/CompliancePageContent.tsx` still generates system policy text first, still reads `compliancePages/{sId}` only to compose optional owner override content, and still falls back to generated policy text if that read fails. Failed override reads now log bounded `public_compliance_override_read_failed` diagnostics with store/page/tenant-type presence-length metadata, subdomain/custom-domain presence booleans, and normalized source error metadata only. Raw store IDs, custom domains, subdomains, generated policy text, owner override text, and provider exception payloads are not logged. `npm run verify:compliance-pages-boundary` and `npm run verify:public-business-truth` source-gate the diagnostic, bounded context, direct-console bans, docs parity, and absence of the old silent Firestore fallback. Follow-up gates passed: `node --check scripts/verification/verify-compliance-pages-boundary.js`, `npm run verify:compliance-pages-boundary`, `node --check scripts/verification/verify-public-business-truth.js`, `npm run verify:public-business-truth`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes public compliance page observability only; it does not change generated policy text, owner override behavior on valid reads, compliance API behavior, owner editor behavior, Firestore writes, Storage operations, Cloud Functions, cache invalidations, Firebase/Vercel deploys, production builds, browser custom-domain smoke, owner/legal approval, production-host smoke, launch approval, or release certification.

Customer App install de-dupe storage diagnostics checkpoint: Customer App per-device install-fired de-dupe no longer hides browser-local storage failures inside a broad install-tracking catch. `src/lib/pwa/installTracker.ts` still keeps install-fired de-dupe browser-local, still uses the existing `CUSTOMER_APP_INSTALLED` analytics path, still records no customer identity or device fingerprint, and still creates no fallback write when storage is unavailable or rejects reads/writes. Failed install de-dupe localStorage availability, read, and write paths now log bounded `customer_app_install_dedupe_storage_unavailable`, `customer_app_install_dedupe_read_failed`, and `customer_app_install_dedupe_write_failed` diagnostics once per operation. Diagnostics include only operation plus store/storage-key presence-length metadata and normalized source error metadata; raw store IDs, storage keys, timestamp values, and browser exception text are not logged. If the de-dupe read fails after storage availability passed, the existing no-storage policy applies: install tracking remains non-blocking and may fire through the existing accepted event path rather than creating fallback storage/server state. `npm run verify:customer-app-pwa` source-gates the failure codes, bounded metadata, one-per-operation guard, docs parity, and absence of the old broad storage-write tracking catch. Follow-up gates passed: `node --check scripts/verification/verify-customer-app-pwa.js`, `npm run verify:customer-app-pwa`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes browser-local Customer App install de-dupe diagnostics only; it does not change prompt eligibility rules, prompt-shown analytics, accepted install analytics shape, Firestore analytics counters, public analytics route validation, customer identity policy, Storage operations, Cloud Functions, cache invalidations, Firebase/Vercel deploys, production builds, browser/device QA, production-host smoke, launch approval, or release certification.

Customer App prompt-shown timestamp diagnostics checkpoint: Customer App iOS install-inference timestamp storage no longer hides browser-local storage failures. `src/lib/pwa/installTracker.ts` still keeps the prompt-shown timestamp browser-local, still uses it only as an input to iOS install inference, and still creates no fallback write when storage is unavailable or rejects writes. Failed prompt-shown timestamp localStorage availability and write paths now log bounded `customer_app_prompt_shown_storage_unavailable` and `customer_app_prompt_shown_storage_write_failed` diagnostics once per operation. Diagnostics include only operation plus store/storage-key presence-length metadata and normalized source error metadata; raw store IDs, storage keys, timestamp values, and browser exception text are not logged. `CUSTOMER_APP_PROMPT_SHOWN` event behavior is unchanged and remains owned by the existing prompt analytics path. `npm run verify:customer-app-pwa` source-gates the failure codes, bounded metadata, one-per-operation guard, docs parity, and absence of the old silent prompt-shown timestamp catch. Follow-up gates passed: `node --check scripts/verification/verify-customer-app-pwa.js`, `npm run verify:customer-app-pwa`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes browser-local Customer App prompt-shown timestamp diagnostics only; it does not change prompt eligibility rules, prompt-shown analytics, install detection, standalone launch detection, iOS install inference success behavior, analytics event shapes, Firestore analytics counters, public analytics route validation, Storage operations, Cloud Functions, cache invalidations, Firebase/Vercel deploys, production builds, browser/device QA, production-host smoke, launch approval, or release certification.

Customer App prompt storage diagnostics checkpoint: Customer App install prompt visit counting and dismissal suppression no longer hide browser-local storage failures. `src/lib/pwa/visitCounter.ts` still keeps prompt gating browser-local, still returns `0` when visit-count storage cannot be trusted, still treats dismissal storage failures as not suppressed, and still avoids server calls or fallback writes. Failed localStorage availability checks, visit increment/read paths, and dismissal write/read paths now log bounded `customer_app_prompt_storage_unavailable`, `customer_app_prompt_visit_increment_failed`, `customer_app_prompt_visit_read_failed`, `customer_app_prompt_dismissal_write_failed`, and `customer_app_prompt_dismissal_read_failed` diagnostics once per operation. Diagnostics include only operation plus store/storage-key presence-length metadata and normalized source error metadata; raw store IDs, storage keys, values, and browser exception text are not logged. `npm run verify:customer-app-pwa` source-gates the failure codes, bounded metadata, one-per-operation guard, docs parity, and absence of the old silent visit-counter catches. Follow-up gates passed: `node --check scripts/verification/verify-customer-app-pwa.js`, `npm run verify:customer-app-pwa`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes browser-local Customer App prompt-gate diagnostics only; it does not change prompt eligibility rules, dismissal duration, install detection, standalone launch detection, analytics event shapes, Firestore analytics counters, public analytics route validation, Storage operations, Cloud Functions, cache invalidations, Firebase/Vercel deploys, production builds, browser/device QA, production-host smoke, launch approval, or release certification.

Customer App open storage diagnostics checkpoint: Customer App standalone-open tracking no longer hides browser-local storage failures. `src/lib/pwa/standaloneDetector.ts` still fires `CUSTOMER_APP_OPENED` only for installed/standalone launches, still keeps tracking non-blocking, still uses `sessionStorage` to avoid repeat standalone-open writes inside the same tab session when browser storage is available, and still keeps iOS install inference best-effort. Failed sessionStorage availability checks, per-session guard reads/writes, and iOS prompt-storage reads now log bounded `customer_app_open_session_storage_unavailable`, `customer_app_open_session_guard_failed`, and `customer_app_ios_install_inference_storage_failed` diagnostics once per operation. Diagnostics include only operation plus store/tenant/storage-key presence-length metadata and normalized source error metadata; raw store IDs, tenant IDs, storage keys, and browser exception text are not logged. `npm run verify:customer-app-pwa` source-gates the failure codes, bounded metadata, one-per-operation guard, and absence of the old silent standalone-detector catches. Follow-up gates passed: `node --check scripts/verification/verify-customer-app-pwa.js`, `npm run verify:customer-app-pwa`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes browser-local Customer App PWA diagnostics only; it does not change standalone launch detection, install detection, shortcut routing, analytics event shapes, Firestore analytics counters, queue coalescing, public analytics route validation, Storage operations, Cloud Functions, cache invalidations, Firebase/Vercel deploys, production builds, browser/device QA, production-host smoke, launch approval, or release certification.

OBP theme preference storage diagnostics checkpoint: Official Business Page customer display preference persistence no longer hides browser-local storage failures. `src/app/client/obp/OBPThemeToggle.tsx` still applies the selected light/dark display preference immediately, still falls back to the system preference when no valid stored preference exists, and still keeps the page usable when localStorage is unavailable, full, blocked, or malformed. Failed read/write paths now log bounded `obp_theme_storage_read_failed` and `obp_theme_storage_write_failed` diagnostics once per operation. Diagnostics include only operation, storage-key presence-length metadata, theme presence-length metadata, and normalized error name; raw storage values and raw browser exceptions are not logged. `npm run verify:public-business-truth` source-gates the failure codes, bounded metadata, one-per-operation guard, and absence of the old silent theme storage catches. Follow-up gates passed: `node --check scripts/verification/verify-public-business-truth.js`, `npm run verify:public-business-truth`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes browser-local OBP theme preference diagnostics only; it does not add owner theme customization, page-builder behavior, Firestore reads/writes, analytics writes, Storage operations, Cloud Functions, cache invalidations, Firebase/Vercel deploys, production builds, browser/device QA, production-host smoke, launch approval, or release certification.

OBP language analytics storage diagnostics checkpoint: Official Business Page language-adoption tracking no longer hides browser-local storage failures. `src/app/client/obp/OBPAnalytics.tsx` still keeps OBP rendering non-blocking, still records OBP views through the existing analytics path, still waits for the switched language to remain active before calling `trackOBPLanguageAdoption()`, and still skips unsafe adoption de-dupe when `sessionStorage` is unavailable, full, blocked, or malformed. Failed language storage read/write paths now log bounded `obp_analytics_language_storage` diagnostics. Diagnostics include only failure type, storage operation, tenant/store/language/previous-language/storage-key presence-length metadata, location/timezone booleans, and normalized error name; raw language values and raw storage keys are not logged. `npm run verify:public-business-truth` source-gates the failure type, bounded metadata, operation context, and absence of the old silent previous-language clear. Follow-up gates passed: `node --check scripts/verification/verify-public-business-truth.js`, `npm run verify:public-business-truth`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes browser-local OBP language analytics diagnostics only; it does not change OBP rendering, language switching, dwell timing, OBP view analytics, OBP action analytics, Firestore analytics counters, public analytics route validation, queue coalescing, owner analytics reads, cache behavior, Firebase/Vercel deploys, production builds, browser/device QA, production-host smoke, launch approval, or release certification.

Search analytics de-dupe diagnostics checkpoint: public/customer search analytics no longer hides browser-local search de-dupe storage failures. `src/lib/analytics/searchDedup.ts` still keeps menu search rendering non-blocking, still normalizes and de-dupes search terms per store/project/tab session when `sessionStorage` is available, and still falls back to normal accepted search tracking when browser storage is unavailable, full, blocked, or malformed. Failed availability/read/write paths now log bounded `analytics_search_dedup_storage_unavailable`, `analytics_search_dedup_read_failed`, and `analytics_search_dedup_write_failed` diagnostics. Diagnostics include only operation, storage-key presence-length metadata, stored/serialized payload presence-length metadata, normalized-search-term presence-length metadata, and normalized source error metadata; raw search terms and raw storage keys are not logged. `npm run verify:menulist-api-tenant-safety` source-gates the failure codes, bounded metadata, one-per-operation availability guard, and absence of the old silent search de-dupe catches. Follow-up gates passed: `node --check scripts/verification/verify-menulist-api-tenant-safety.js`, `npm run verify:menulist-api-tenant-safety`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes browser-local public search analytics de-dupe diagnostics only; it does not change public search UI, search debounce timing, search result counts, analytics event admission, Firestore analytics counters, queue coalescing, public analytics route validation, GA4 forwarding, owner analytics reads, cache behavior, Firebase/Vercel deploys, production builds, browser/device QA, production-host smoke, launch approval, or release certification.

Analytics sessionStorage diagnostics checkpoint: public/customer analytics no longer hides browser-local session milestone, entry-source, or active-filter storage failures. `src/lib/analytics/unified.ts` still keeps menu rendering non-blocking, still attaches milestone/source/filter counters only to existing accepted analytics writes, and still skips unsafe tab-scoped de-duplication or attribution context when `sessionStorage` is unavailable, full, blocked, or malformed. Failed read/write/remove paths now log bounded `analytics_session_milestones_read_failed`, `analytics_session_milestones_write_failed`, `analytics_session_source_read_failed`, `analytics_session_source_write_failed`, `analytics_active_filter_read_failed`, and `analytics_active_filter_write_failed` diagnostics. Diagnostics include only storage-key presence-length metadata, stored/serialized payload presence-length metadata, small milestone counts/booleans, entry-source/filter presence-length metadata, operation, and normalized source error metadata. `npm run verify:menulist-api-tenant-safety` source-gates the failure codes, bounded metadata, and absence of the old silent sessionStorage catches/comments. Follow-up gates passed: `node --check scripts/verification/verify-menulist-api-tenant-safety.js`, `npm run verify:menulist-api-tenant-safety`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes browser-local analytics sessionStorage diagnostics only; it does not change analytics event admission, counters, Firestore writes, queue coalescing, public analytics route validation, GA4 forwarding, owner analytics reads, location consent, cache behavior, Firebase/Vercel deploys, production builds, browser/device QA, production-host smoke, launch approval, or release certification.

Active store context load fallback checkpoint: owner session-provider store-context switching no longer leaves a failed target-store load as a quiet stale-store state. `src/providers/sessionProvider.tsx` already validates active store context against tenant storesList, active-state, and mapped access before loading a target store. If the target store or active subscription load now fails after that admission, the provider logs bounded `session_provider_active_store_context_load_failed` diagnostics with session tenant/store/user presence-length metadata, target/previous store presence-length metadata, and target-summary booleans only, clears stale subscription state, resets the persisted active store context, and falls back to the login store. `npm run verify:auth-security-failure-matrix` source-gates the failure code, bounded metadata, and absence of the old silent `loadTargetStore()` catch. Follow-up gates passed: `node --check scripts/verification/verify-auth-security-failure-matrix.js`, `npm run verify:auth-security-failure-matrix`, `npm run verify:multi-location-boundary`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes owner session-provider fallback behavior for failed active store-context loading only; it does not change `/api/auth/switch-store`, successful store switching, Firebase Auth claim refresh, tenant/store writes, subscription reads on valid paths, public output, cache invalidation, Firestore rules/indexes, Cloud Function logic, Firebase/Vercel deploys, production builds, browser/device QA, production-host smoke, launch approval, or release certification.

Image generation preferences diagnostics checkpoint: owner image-generation preference persistence no longer hides browser-local save, load, or clear failures. `src/lib/imageGenPreferences.ts` still stores visual preference defaults per tenant/store in localStorage only, still returns defaults/null when browser storage is unavailable or malformed, and still creates no Firestore, Storage, provider, or accounting work, but failed persistence paths now log bounded `image_generation_preferences_save_failed`, `image_generation_preferences_load_failed`, and `image_generation_preferences_clear_failed` diagnostics through shared hook diagnostics. Diagnostics include only phase, bounded tenant/store/storage-key presence-length metadata, preference count/boolean shape metadata, serialized/stored payload presence-length metadata, and normalized source error metadata. `npm run verify:auth-security-failure-matrix` source-gates the helper, bounded metadata, diagnostic codes, and absence of the old silent localStorage comments. Follow-up gates passed: `npm run verify:auth-security-failure-matrix`, targeted silent localStorage comment scan, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes browser-local owner image-generation preference diagnostics only; it does not change generated-image prompts/provider calls, preference values, defaults, Firestore reads/writes, Storage operations, image generation jobs, AI accounting, public menu output, Firebase/Vercel deploys, production builds, browser/device QA, production-host smoke, launch approval, or release certification.

Analytics queue persistence diagnostics checkpoint: public/customer analytics no longer hides browser-local queue persistence failures when `localStorage` is unavailable, full, blocked, or rejects writes. `src/database/analytics/index.ts` still keeps analytics non-blocking for public menu rendering, still coalesces events locally, still flushes through `/api/public/analytics/track`, and still recovers persisted queue entries when available, but failed persistence now logs bounded `analytics_queue_persist_failed` diagnostics once per failing browser session. Diagnostics include only persistence phase, queue entry count, queued event count, serialized-payload presence/length, and normalized source error metadata. `npm run verify:menulist-api-tenant-safety` source-gates the persistence failure code, one-per-session guard, bounded serialized-payload metadata, and absence of the old silent localStorage persistence catch. Follow-up gates passed: `node --check scripts/verification/verify-menulist-api-tenant-safety.js`, `npm run verify:menulist-api-tenant-safety`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes browser-local analytics queue persistence diagnostics only; it does not change analytics event admission, coalescing, public analytics route validation, Firestore analytics writes, GA4 forwarding, owner analytics reads, location consent, cache behavior, Firebase/Vercel deploys, production builds, browser/device QA, production-host smoke, launch approval, or release certification.

Error page theme fallback diagnostics checkpoint: root global-error rendering and wrapped error pages no longer keep duplicated silent Redux Persist theme readers. `src/lib/runtime/errorPageTheme.ts` now owns the default theme fallback and persisted-theme reader, `src/app/global-error.tsx` and `src/components/atoms/ErrorPageThemeWrapper/index.tsx` both use it, and malformed or blocked browser storage/JSON reads log bounded `error_page_theme_persisted_state_read_failed` diagnostics with stable caller source, read phase, persisted-state presence/length, client-theme-config presence/length, and normalized source error metadata only. Missing browser storage or absent `clientThemeConfig` still falls back quietly to the default dark theme. `npm run verify:auth-security-failure-matrix` source-gates the shared reader, bounded metadata, caller wiring, and absence of duplicate silent persisted-theme parsers. Follow-up gates passed: `node --check scripts/verification/verify-auth-security-failure-matrix.js`, `npm run verify:auth-security-failure-matrix`, targeted persisted-theme silent-parser scan, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes browser-local error-page theme diagnostics only; it does not change crash rendering, reset behavior, explicit error reports, normal theme settings, Redux persistence, auth, Firestore reads/writes, Storage operations, Cloud Function logic, Firebase/Vercel deploys, production builds, browser/device QA, production-host smoke, launch approval, or release certification.

Menu extraction owner-upload metadata diagnostics checkpoint: `POST /api/menu-extraction/jobs` no longer hides Firebase Storage metadata lookup failures while building the server-trusted owner-upload `sourceFingerprint`. The route still allows only tenant/store-owned `projects/files/{tId}/{sId}/` Storage paths, still treats metadata lookup as a dedupe optimization, and still lets valid owner uploads continue without a completed-job reuse fingerprint when metadata cannot be read. Failures now log bounded `menu_extraction_owner_upload_metadata_lookup_failed` diagnostics with tenant/store, file UID/type, Storage-path presence/length, file size, and normalized source error metadata only. `npm run verify:menu-extraction-pipeline` source-gates the bounded diagnostic and rejects the old silent `getMetadata().catch(() => [null as any])` fallback. Follow-up gates passed: `node --check scripts/verification/verify-menu-extraction-pipeline.js`, `npm run verify:menu-extraction-pipeline`, and `npx tsc --noEmit --incremental false --pretty false`. This changes owner-upload dedupe metadata diagnostics only; it does not change upload admission, Storage URL allowlisting, project ownership checks, file fingerprints when metadata is available, active-job reuse, completed-job reuse success behavior, extraction job writes, AI/provider behavior, owner-facing copy, Firestore reads/writes, Storage operation counts, Firebase/Vercel deploys, production builds, browser/device QA, production-host smoke, launch approval, or release certification.

Analytics geolocation fallback diagnostics checkpoint: opt-in location analytics no longer collapses browser geolocation timeout or position-unavailable failures into an unobserved timezone fallback. `src/lib/analytics/geo.ts` still keeps normal permission denial quiet as an expected privacy outcome, still falls back to timezone/unknown without blocking the event, and now logs bounded `analytics_geolocation_position_failed` diagnostics for non-permission geolocation failures with API support, timeout, maximum-age, fallback-policy, and normalized source error metadata only. `npm run verify:menulist-api-tenant-safety` source-gates the permission-denial exception, bounded fallback diagnostic context, and absence of the old silent `.catch(() => null)` position lookup. Follow-up gates passed: `node --check scripts/verification/verify-menulist-api-tenant-safety.js`, `npm run verify:menulist-api-tenant-safety`, targeted geolocation silent-catch scan, `npm run verify:public-business-truth`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes analytics location diagnostics only; it does not change location opt-in rules, analytics preferences, event admission, location key format, Firestore analytics writes, GA4 forwarding, public analytics API behavior, cache behavior, Firebase/Vercel deploys, production builds, browser/device QA, production-host smoke, launch approval, or release certification.

Create Menu success starter-signal diagnostics checkpoint: `/create-menu/success` still attempts `MENU_LINK_COPIED` and `WHATSAPP_SHARE_STARTED` only after the acknowledged Copy Link or WhatsApp open handoff succeeds, but failures around the non-blocking telemetry path are no longer silent. `src/app/(website)/create-menu/success/CreateMenuSuccessClient.tsx` now logs `public_create_menu_success_starter_signal_claim_read_failed` when the session-storage claim context cannot be read/parsed and `public_create_menu_success_starter_signal_write_failed` when the existing starter activation write attempt fails. Diagnostics include only bounded signal, raw-claim presence/length, store-presence, and normalized source error metadata; they do not log raw query-string URLs, session-storage claim payloads, store IDs, generated messages, or browser exception text. `npm run verify:menu-extraction-pipeline` source-gates these failure codes and rejects the old silent starter-signal catch patterns. Follow-up gates passed: `node --check scripts/verification/verify-menu-extraction-pipeline.js`, `npm run verify:menu-extraction-pipeline`, targeted starter-signal silence scan, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes public Create Menu success-page telemetry diagnostics only; it does not change valid upload/link import, preview polling, claim/publish, public URLs, QR rendering, copy/open handoff behavior, starter activation write timing, successful-path Firestore reads/writes, Storage operations, Firebase/Vercel deploys, production builds, browser/device QA, production-host smoke, launch approval, or release certification.

Creative Editor template cleanup diagnostics checkpoint: user and platform template deletes no longer hide non-missing Storage cleanup failures after metadata deletion succeeds. `src/lib/creative-editor/templateRegistryDal.ts` still removes visible template metadata first and still treats `storage/object-not-found` as an expected cleanup no-op, but other document/preview object delete failures now log the bounded `creative_editor_template_storage_cleanup_failed` diagnostic with path presence/length, template/product/source/asset/business-category presence/length, template origin, cleanup target, and normalized source error metadata. `scripts/verification/verify-auth-security-failure-matrix.js` and `scripts/verification/verify-printable-asset-templates.js` source-gate the stable cleanup failure code, bounded path context, missing-object exception, and absence of the old silent `]).catch(() => undefined);` swallow. Follow-up gates passed: `node --check scripts/verification/verify-auth-security-failure-matrix.js`, `node --check scripts/verification/verify-printable-asset-templates.js`, `node scripts/verification/verify-printable-asset-templates.js`, `npm run verify:auth-security-failure-matrix`, targeted cleanup-silence scan, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes Creative Editor template-registry cleanup diagnostics only; it does not change template list/open/save/update/delete metadata behavior, Firestore reads/writes, Storage upload/delete attempts, owner-visible failure copy, platform manager access, feature flags, Firebase/Vercel deploys, production builds, browser/device QA, production-host smoke, launch approval, or release certification.

Server DAL session lookup diagnostics checkpoint: protected `apiCallComposerServer` calls no longer collapse thrown `getActiveSession()` failures into silent unauthenticated fallbacks. The helper now logs a bounded `dal_server_session_lookup_failed` diagnostic with normalized error name, bounded argument summary, function name, and ignored-session metadata before preserving the existing `null` unauthenticated return. `npm run verify:auth-security-failure-matrix` source-gates the normalized diagnostic and rejects the old silent `getActiveSession().catch(() => null)` pattern. Follow-up gates passed: `node --check scripts/verification/verify-auth-security-failure-matrix.js`, `npm run verify:auth-security-failure-matrix`, `npm run verify:menulist-api-tenant-safety`, targeted silent-session-lookup scan, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes server DAL diagnostics only; it does not change route authorization, session semantics, ignored webhook/helper function behavior, DAL return shapes, Firestore reads/writes, Firebase Auth behavior, Firebase/Vercel deploys, production builds, browser/device QA, production-host smoke, launch approval, or release certification.

System ledger pending-validation cleanup checkpoint: `__docs__/system-strengthening/menulist-system-data-flow-audit-2026-06-20.md` no longer preserves the stale Reseller Operations Browser Request Boundary row that said scoped `git diff --check` was pending. The row now records the diff check as passed, and `npm run verify:agent-readiness` rejects the old pending validation wording so the audit ledger cannot imply an unresolved local source gate after the aggregate has passed. Follow-up gates passed: `node --check scripts/verification/verify-agent-readiness.js`, `npm run verify:agent-readiness`, stale pending validation scan over the active audit/changelog docs, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes audit-ledger evidence and source gates only; it does not change runtime behavior, reseller APIs, browser request policy, feature flags, Firebase rules/indexes, Cloud Function logic, provider behavior, Firebase/Vercel deploys, production builds, browser/device QA, production-host smoke, launch approval, or release certification.

Production audit prompt launch-authority checkpoint: the active master production audit governance prompt and final production-readiness verdict prompt now state that source-confidence scores and local source gates are not launch approval when External Certification Runbook evidence is missing. The final verdict scoring now separates 9-10 with every required external certification gate evidenced from 9-10 with missing external gates, which must be reported as source-ready but not launch certified. The website screenshot/asset production prompt also now asks for source-backed visual asset workflows and prompts instead of "production-ready" asset wording. `npm run verify:agent-readiness` source-gates these active prompt-template boundaries. Follow-up gates passed: `node --check scripts/verification/verify-agent-readiness.js`, `npm run verify:agent-readiness`, targeted stale prompt wording scan, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes audit/process documentation and source gates only; it does not change runtime behavior, feature flags, Firebase rules/indexes, Cloud Function logic, provider behavior, Firebase/Vercel deploys, production builds, browser/device QA, production-host smoke, launch approval, or release certification.

Archive launch-certification boundary checkpoint: archived docs under `__docs__/archive/` that contain old production-ready, ready-for-testing, ship-approved, or deploy-all wording now state at the top that they are historical archive evidence only, not current MenuList source of truth, production approval, deploy approval, launch approval, or release certification. `__docs__/README.md` now labels the top-level archive folder as not current launch certification, and `npm run verify:agent-readiness` source-gates the generic archive scan so future archived launch/certification wording must carry the same top-of-file boundary. Follow-up gates passed: `node --check scripts/verification/verify-agent-readiness.js`, `npm run verify:agent-readiness`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes archive documentation and source gates only; it does not change runtime behavior, feature flags, Firebase rules/indexes, Cloud Function logic, provider behavior, Firebase/Vercel deploys, production builds, browser/device QA, production-host smoke, launch approval, or release certification.

Public Truth Tools public HTTPS evidence-text checkpoint: manifest-backed public V0 tool report builders now disclose the public HTTPS URL/customer-link boundary in URL-derived evidence text instead of the older generic "URL format was checked" wording. `scripts/verification/verify-public-truth-tools.js` now requires public HTTPS evidence text in each manifest-backed public tool report, rejects the old generic URL evidence return strings, and source-gates `PTT-024` in the family test matrix. Follow-up gates passed: `node --check scripts/verification/verify-public-truth-tools.js`, `npm run verify:public-truth-tools`, `npm run docs:check-links`, `npx tsc --noEmit --incremental false --pretty false`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes browser-local report evidence text, docs, and source gates only; it does not fetch, open, resolve DNS, crawl, inspect, store, or mutate URL targets; add API routes; add Firebase state; call providers; deploy Firebase/Vercel; run production builds; perform browser/device QA; provider smoke; launch approval; or release certification.

Public Truth Tools shared HTTPS URL checkpoint: manifest-backed public V0 tool report builders now share `src/lib/public-truth-tools/publicUrlValidation.ts` for owner-entered public/customer link readiness. The helper accepts bare public domains by normalizing them to HTTPS, then requires public HTTPS and rejects explicit `http://`, localhost, `.localhost`, `.local`, private IPv4, raw IPv4, loopback IPv6, and credentialed URLs. Booking Inquiry keeps explicit `tel:`, `mailto:`, `whatsapp://`, and likely phone-number action destinations outside the public URL branch; WhatsApp Action Link still recognizes WhatsApp deep links for click-to-chat format while current customer/menu links use the shared public HTTPS validator. `npm run verify:public-truth-tools` source-gates the helper, family docs/test case, manifest inventory including `publicUrlValidation.ts`, and rejection of the old local/HTTP helper pattern across public tool reports. Follow-up gates passed: `node --check scripts/verification/verify-public-truth-tools.js`, `npm run verify:public-truth-tools`, `npm run docs:check-links`, `npx tsc --noEmit --incremental false --pretty false`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes browser-local public URL readiness validation, family docs, and source gates only; it does not fetch, open, resolve DNS, crawl, inspect, store, or mutate URL targets; add API routes; add Firebase state; call providers; enable external adapters; deploy Firebase/Vercel; run production builds; perform browser/device QA; provider smoke; launch approval; or release certification.

QR Link Health public HTTPS target checkpoint: public QR Link Health Check no longer treats local or insecure pasted QR targets as valid customer-openable URLs. `src/lib/public-truth-tools/qrLinkHealthReport.ts` now accepts only public HTTPS target URLs, normalizes bare public domains to HTTPS, rejects explicit `http://`, localhost, `.local`, private IPv4, raw IPv4, loopback IPv6, and credentialed URLs, and keeps the existing no-fetch/no-decoder V0 boundary. The UI helper copy and `__docs__/menulist-tools/qr-link-health-check/` docs now describe the public HTTPS target rule. `npm run verify:qr-link-health-check` source-gates the parser, rejection of the old HTTP/localhost acceptance path, and public HTTPS evidence text. Follow-up gates passed: `node --check scripts/verification/verify-qr-link-health-check.js`, `npm run verify:qr-link-health-check`, `npm run verify:public-truth-tools`, `npm run docs:check-links`, `npx tsc --noEmit --incremental false --pretty false`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes browser-local QR target validation, docs, locale copy, and source gates only; it does not decode QR images, fetch target pages, inspect Google/social/WhatsApp profiles, call AI/search providers, store reports, add API routes, mutate MenuList truth, change Firebase rules/indexes, change Cloud Function logic, deploy Firebase/Vercel, run production builds, perform browser/device QA, provider smoke, launch approval, or release certification.

Print & Share public HTTPS link checkpoint: public Print & Share asset tools no longer treat insecure or local owner-entered customer links as ready QR targets. `src/lib/public-asset-tools/printShareToolReport.ts` now accepts only public HTTPS customer-link targets, normalizes bare public domains to HTTPS, rejects explicit `http://`, localhost, `.local`, private IPv4, raw IPv4, loopback IPv6, and credentialed URLs, and leaves invalid links out of `report.customerLink` so generated QR assets fall back instead of encoding an unsafe/local target. The UI helper copy and `__docs__/menulist-tools/print-share-tools/` docs now describe the public HTTPS boundary. `npm run verify:print-share-tools` source-gates the parser, invalid-link evidence text, rejection of the old HTTP/localhost acceptance path, and the QR fallback contract. Follow-up gates passed: `node --check scripts/verification/verify-print-share-tools.js`, `npm run verify:print-share-tools`, `npm run verify:public-truth-tools`, `npm run docs:check-links`, `npx tsc --noEmit --incremental false --pretty false`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes browser-local public asset validation, docs, locale copy, and source gates only; it does not fetch destination pages, call providers, store files or reports, add API routes, mutate MenuList truth, change Firebase rules/indexes, change Cloud Function logic, deploy Firebase/Vercel, run production builds, perform browser/device QA, provider smoke, launch approval, or release certification.

Answerlattice KB article list scope checkpoint: deprecated `src/database/knowledgeBase/articles.ts#getArticles()` no longer queries `kb_articles` globally for every caller. The helper now resolves `resolveReadableArticleScope()`, returns an empty list when a non-platform caller lacks tenant/store scope, applies `getReadableScopeFilters(scope)` before the Firestore query, and retains `readableScopeAllowsArticle(scope, article)` as a final guard while preserving platform-admin global read behavior. Active UI call sites already use scoped category/section/id helpers; this hardens the compatibility helper for future imports. `npm run verify:answerlattice-runtime-truth` source-gates the old unscoped query and the KB doc boundary. Follow-up gates passed: `node --check scripts/verification/verify-answerlattice-runtime-truth.js`, `npm run verify:answerlattice-runtime-truth`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 92/92 checks, including 88 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes Answerlattice KB article read-scope behavior and docs only; KB article writes, KB category writes, public content cache shape, Firestore rules/indexes, Cloud Function logic, Firebase deploys, Vercel deploys, production builds, browser/device QA, provider smoke, launch approval, and release certification remain unchanged.

Answerlattice KB generation job list scope checkpoint: deprecated `src/database/kb-generation/jobs.ts#getIngestionJobs()` no longer queries `kb_generation_jobs` globally for every caller. The helper now resolves `resolveReadableIngestionJobScope()`, returns an empty list when a non-platform caller lacks tenant/store scope, applies `getReadableIngestionJobFilters(scope)` before the Firestore query, and retains `readableIngestionJobScopeAllowsJob(scope, job)` as a final guard while preserving platform-admin global read behavior. Active UI call sites already use scoped active/previous job helpers; this hardens the compatibility helper for future imports. `npm run verify:answerlattice-runtime-truth` source-gates the old unscoped query and the KB generation doc boundary. Follow-up gates passed: `node --check scripts/verification/verify-answerlattice-runtime-truth.js`, `npm run verify:answerlattice-runtime-truth`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 92/92 checks, including 88 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes Answerlattice KB generation job read-scope behavior and docs only; KB job writes, job deletion cleanup, Storage cleanup, KB article/category writes, Firestore rules/indexes, Cloud Function logic, Firebase deploys, Vercel deploys, production builds, browser/device QA, provider smoke, launch approval, and release certification remain unchanged.

Answerlattice KB tenant-scope documentation checkpoint: active Answerlattice Knowledge Base, Help Center, AI QnA, and decoupling docs no longer describe workspace KB as platform-wide or globally shared for owners. The docs now state that workspace KB articles, categories, jobs, and related search/history paths are tenant/store scoped for non-platform callers, with platform-admin global reads kept administrative-only. `npm run verify:answerlattice-runtime-truth` source-gates these active-doc claims against stale platform-wide wording. Follow-up gates passed: `node --check scripts/verification/verify-answerlattice-runtime-truth.js`, `npm run verify:answerlattice-runtime-truth`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes Answerlattice documentation parity only; runtime tenant/store scoping was already enforced by the scoped helper and retrieval changes in this pass. KB writes, public content cache shape, Firestore rules/indexes, Cloud Function logic, Firebase deploys, Vercel deploys, production builds, browser/device QA, provider smoke, launch approval, and release certification remain unchanged.

Social Bio Link mobile-support boundary checkpoint: `__docs__/menulist-tools/social-bio-link-check/social-bio-link-check_mobile-support.md` again states that V1 maps to existing owner mobile Share, Public Discovery, and Business Health surfaces, preserving the existing mobile-owner surface boundary and blocking a separate Social Bio Link mobile dashboard interpretation. Follow-up gates passed: `npm run verify:social-bio-link-check`, `npm run verify:public-truth-tools`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 92/92 checks, including 88 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes documentation boundary wording only; Social Bio Link runtime behavior, public tool behavior, owner Business Health modules, mobile routing, report storage, external URL fetching, AI/provider calls, Firestore rules/indexes, Cloud Function logic, Firebase deploys, Vercel deploys, production builds, browser/device QA, launch approval, and release certification remain unchanged.

Public Truth Monitor runtime checkpoint: the V2 paid add-on is now implemented as entitlement-gated saved Public Truth history inside Business Health. The runtime adds authenticated summary and refresh APIs, paid plan entitlement checks, bounded JSON/body parsing, tenant access checks, existing Business Health `VIEW_ANALYTICS` store permission checks, capped `platformSummary/publicTruthMonitor_{storeId}` latest/history storage, desktop Business Health panel placement, mobile Business Health card placement inside `MobileShell`, and browser-local text report download. `npm run verify:public-truth-monitor-addon` source-gates the package script, aggregate verifier registration, entitlement boundary, server/API/DAL boundaries, permission boundary, capped storage, desktop/mobile placement, and no external-fetch/provider path; `npm run verify:menulist-api-tenant-safety` also guards the monitor API admission order. Follow-up gates passed: `node --check scripts/verification/verify-public-truth-monitor-addon.js`, `npm run verify:public-truth-monitor-addon`, `npm run verify:public-truth-tools`, `npm run verify:doc-npm-scripts`, `npm run verify:menulist-api-tenant-safety`, `npm run docs:check-links`, `npx tsc --noEmit --incremental false --pretty false`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes MenuList owner runtime, docs, and source gates only; background scheduling, multi-location comparison runtime, email delivery, external adapters, AI/search sampling, public V2 routes, managed repair workflow, Firestore rules/indexes, Cloud Function logic, Firebase deploys, Vercel deploys, production builds, live Firestore writes, browser/device QA, provider smoke, launch approval, and release certification remain unchanged.

Answerlattice widget frame host checkpoint: `src/middleware.ts` no longer relaxes `X-Frame-Options` and CSP `frame-ancestors` for `/widget/*` by path alone. The widget iframe frame policy now uses `isAnswerlatticeWidgetFrameRoute()` and allows the embeddable header path only on Answerlattice product hosts resolved through `resolveKnownProductIdByHostname(hostname) === 'answerlattice'`, or on local development hosts outside Vercel. `__docs__/answerlattice/README.md` and `__docs__/answerlattice/help-widget/help-widget_impl.md` now carry the same host-boundary wording. `npm run verify:answerlattice-runtime-truth` source-gates this middleware and doc boundary. Follow-up gates passed: `node --check scripts/verification/verify-answerlattice-runtime-truth.js`, `npm run verify:answerlattice-runtime-truth`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 92/92 checks, including 88 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes middleware security headers and Answerlattice widget docs only; widget API key validation, widget search/config/feedback routes, allowed widget origins, public API scope checks, Answerlattice Firestore data, Cloud Function logic, Firebase deploys, Vercel deploys, production builds, browser/device QA, provider smoke, launch approval, and release certification remain unchanged.

MenuList Storage bucket fallback checkpoint: active MenuList bucket-resolution helpers no longer use `menulist-qa.appspot.com` as a runtime fallback. `src/lib/firebase/firebaseClient.ts` now fails closed with an empty `firebaseStorageUrl` when public bucket config is absent; `src/lib/apiUtils/index.ts`, `src/lib/menu-extraction/menuIntakeIdentityServer.ts`, `functions/src/logic/processMenuImages.ts`, `functions/src/logic/processMenuImagesJob.ts`, and `functions/src/messagingOnboarding/assetIntelligence.ts` now resolve the bucket from `FIREBASE_STORAGE_BUCKET`, `NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET`, or the active project id (`FIREBASE_PROJECT_ID`, `GCLOUD_PROJECT`, `GCP_PROJECT`, `NEXT_PUBLIC_FIREBASE_PROJECT_ID`) instead of crossing to the QA bucket. Follow-up gates passed: `node --check scripts/verification/verify-agent-readiness.js`, `npm run verify:agent-readiness`, `npm run verify:menu-extraction-pipeline`, `npm run verify:ai-accounting`, `npm run verify:functions-deploy-preflight`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, the targeted hardcoded-fallback scan over the six active helper files, and `npm run verify:production-readiness-local` with 92/92 checks, including 88 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. The required scoped MenuList Functions deploy gate was attempted with `firebase deploy --project menulist-qa --config firebase.json --only functions:processMenuImages,functions:processMenuImagesJob,functions:menulistMaintenanceScheduler --non-interactive`; predeploy lint/build completed, then Google Cloud blocked deployment before upload with `HTTP Error: 403, The caller does not have permission` on `https://cloudresourcemanager.googleapis.com/v1/projects/menulist-qa`. This changes app/server source and MenuList Functions source only; valid upload paths, Storage rules, Firestore reads/writes, extraction request shape, AI provider behavior, messaging onboarding flow shape, public menu output, cache invalidation, Vercel deploys, production builds, browser/device QA, provider smoke, launch approval, and release certification remain unchanged. Function-side live effect remains pending until the scoped Firebase deploy succeeds with project access.

Static asset preview cleanup bucket checkpoint: `src/database/static/static.ts` no longer keys replacement-preview deletion to `menulist-qa.appspot.com`. Replacement-preview cleanup now detects Firebase Storage references generically (`gs://`, `firebasestorage.googleapis.com`, `storage.googleapis.com`, and Firebase Storage app hosts) before calling the existing `deleteFileByUrl()` helper, so production bucket previews are not skipped by a QA-bucket-only guard. `npm run verify:storage-paths` source-gates this boundary. Follow-up gates passed: `node --check scripts/verification/verify-storage-path-hardening.js`, `npm run verify:storage-paths`, targeted QA-bucket scan over the active storage helper files, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 92/92 checks, including 88 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes platform static-asset cleanup logic only; upload paths, Storage rules, platform asset Firestore document shape, public menu output, customer-facing assets, Cloud Function logic, Firebase deploys, Vercel deploys, production builds, browser/device QA, live Storage writes/deletes, launch approval, and release certification remain unchanged.

CORS production localhost allowlist checkpoint: `src/lib/security/corsValidation.ts` no longer includes `http://localhost:3000` in the production-capable `ALLOWED_ORIGINS` list. Localhost and `127.0.0.1` remain allowed only through the development-origin list, and the production allowlist filters local origins out even if `NEXT_PUBLIC_APP_URL` is misconfigured; same-origin local requests still pass through the existing same-origin request check. `npm run verify:auth-security-failure-matrix` source-gates this boundary. Follow-up gates passed: `node --check scripts/verification/verify-auth-security-failure-matrix.js`, `npm run verify:auth-security-failure-matrix`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 92/92 checks, including 88 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes CORS allowlist behavior only; route authentication, public rate limits, Turnstile validation, request body validation, Firestore writes, Firebase rules, Cloud Function logic, Firebase deploys, Vercel deploys, production builds, browser/device QA, provider smoke, launch approval, and release certification remain unchanged.

Shared axios PATCH verb checkpoint: `src/lib/axios/axiosClient.ts` no longer routes the shared `PATCH` helper through `axios.post()`. The helper now delegates to `axios.patch()`, and `npm run verify:agent-readiness` source-gates the helper so future PATCH callers cannot silently send POST requests through the shared client. Follow-up gates passed: `node --check scripts/verification/verify-agent-readiness.js`, `npm run verify:agent-readiness`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 92/92 checks, including 88 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. No active MenuList caller currently uses `axiosClient.PATCH()`, so this changes no current owner/customer flow, API route behavior, Firestore read/write, Storage operation, Firebase rule, Cloud Function logic, Firebase deploy, Vercel deploy, production build, browser/device QA, provider smoke, launch approval, or release certification.

Store switch response envelope checkpoint: desktop header, desktop Billing, desktop Locations, mobile More, mobile Billing, and mobile Locations no longer accept `/api/auth/switch-store` success from HTTP status alone. Each caller now uses the shared no-store, same-origin, manual-redirect auth request policy and parses the response through `readAuthAccountResponse(res, 'switch_store')` before refreshing Firebase claims or updating active-store context. The shared parser validates a bounded `success: true` envelope with an integer `targetStoreId`, maps rejected bodies to fixed auth-account codes, and rejects malformed success bodies. `npm run verify:menulist-api-tenant-safety` and `npm run verify:auth-security-failure-matrix` now source-gate the switch-store parser boundary. No switch-store route authorization, tenant/store access check, Firebase claim refresh behavior, active-store context semantics, Firestore read behavior, Firestore rule, Storage rule, Cloud Function logic, Firebase deploy, Vercel deploy, production build, provider call, browser/device QA, Firestore write, Storage write, launch approval, or release certification changed for this checkpoint.

Switch-store scope document ID boundary checkpoint: fixed in source. The authenticated `/api/auth/switch-store` route keeps the same feature flag, `withAuth()` admission, tenant access verification, hashed per-user limiter, 1KB bounded body reader, `SWITCH_STORES` permission check, tenant storesList check, canonical target-store eligibility read, mapped access check, fixed failure copy, and success envelope, but raw session/body IDs can no longer reach store or tenant document refs. The route now normalizes the session tenant ID, session current-store ID, and requested target-store ID through the shared store-permission document ID guard before tenant access checks, caller-store permission reads, tenant storesList reads, target-store reads, access mapping, or success acknowledgement. `npm run verify:auth-security-failure-matrix` and `npm run verify:menulist-api-tenant-safety` source-gate the shared normalizer usage, normalized access check, normalized Firestore refs, old raw `db.doc` path exclusions, Auth docs, Multi-Outlet docs, audit evidence, and changelog evidence. Follow-up gates passed after this checkpoint patch: `node --check scripts/verification/verify-auth-security-failure-matrix.js`, `node --check scripts/verification/verify-menulist-api-tenant-safety.js`, `npm run verify:auth-security-failure-matrix`, `npm run verify:menulist-api-tenant-safety`, `npm run verify:multi-location-boundary`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 93/93 checks, including 89 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes authenticated switch-store scope admission and source-gate coverage only; it does not change valid store switching, Firebase Auth claim refresh behavior, active-store context semantics, Firestore read counts for valid switches, Firestore writes/deletes, cache invalidation, Firestore rules/indexes, Cloud Functions, Firebase deployment, Vercel deployment, production builds, browser/device QA, provider smoke, production-host smoke, launch approval, or release certification.

Presence Dominance public-source freshness checkpoint: active Presence Dominance website, marketing, and spec docs no longer tell sales/public audiences that the official link is always updated, always accurate, live everywhere, correct everywhere, or that customers see every change within 60 seconds everywhere. The docs now describe the Official Business Page/public link as an owner-approved public source, state that public menu and Official Business Page output can take up to 60 seconds to refresh, and keep Google/provider surfaces plus old downloaded PDFs behind separate evidence or replacement guidance. `npm run verify:public-business-truth` now source-gates those Presence Dominance docs against stale blanket freshness/correctness phrases and requires the owner-approved public-source/cache-window/provider-artifact boundary wording. Follow-up gates passed: `node --check scripts/verification/verify-public-business-truth.js`, `npm run verify:public-business-truth`, targeted Presence Dominance stale freshness/correctness scan, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 91/91 checks, including 87 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. No Presence Dominance runtime behavior, OBP runtime behavior, public menu runtime behavior, Google/provider behavior, PDF behavior, public cache behavior, Firestore rule, Storage rule, Cloud Function logic, Firebase deploy, Vercel deploy, production build, provider call, browser/device QA, Firestore write, Storage write, launch approval, or release certification changed for this checkpoint.

Presence Dominance behavior-link freshness checkpoint: the post-publish preview WhatsApp handoff, preview helper copy, active Presence Dominance README/implementation docs, and share-nudge locale overrides no longer say the menu link is "always updated", "latest", or that customers will always see the latest menu. The runtime WhatsApp handoff now says "Here is our menu link", the post-publish helper says the link opens the approved menu, and Menu Kit/behavior nudge locale text now points to the approved menu/menu link without blanket freshness guarantees. `npm run verify:public-business-truth` now source-gates the post-publish preview page, Presence Dominance README/implementation docs, and the affected locale overrides against the stale behavior-link phrases while requiring the bounded menu-link and approved-menu copy. Follow-up gates passed: `node --check scripts/verification/verify-public-business-truth.js`, `npm run verify:public-business-truth`, targeted behavior-link stale-copy scan, JSON parse over the edited locale files, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local`. No publish behavior, public menu rendering behavior, OBP runtime behavior, public cache behavior, PDF behavior, Google/provider behavior, Firestore rule, Storage rule, Cloud Function logic, Firebase deploy, Vercel deploy, production build, provider call, browser/device QA, Firestore write, Storage write, launch approval, or release certification changed for this checkpoint.

Working Hours public claim boundary checkpoint: active Hours + Holiday Accuracy README, spec, website, and marketing docs no longer tell sales/public audiences that hours are always correct, correct everywhere, always current, automatically handled on holidays, backed by shipped holiday calendars, or managed through date-specific exception screens. The docs now limit the current claim to owner-set weekly working hours, public open/closed status, Today quick-hours edits, time-slot presets, Temporary Status, and today's-hours edits for one-off changes. `npm run verify:working-hours-boundary` now source-gates the active Hours docs against stale always-correct/holiday/exception claims and requires the shipped source-boundary wording. Follow-up gates passed: `node --check scripts/verification/verify-working-hours-boundary.js`, `npm run verify:working-hours-boundary`, targeted Hours stale always-correct/holiday/exception scan, `npm run verify:public-business-truth`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 91/91 checks, including 87 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. No Working Hours runtime behavior, Time Slot runtime behavior, public menu rendering, public cache behavior, Firestore rule, Storage rule, Cloud Function logic, Firebase deploy, Vercel deploy, production build, provider call, browser/device QA, live working-hours write, live time-slot write, Firestore write, Storage write, launch approval, or release certification changed for this checkpoint.

Menu Correctness Engine public claim boundary checkpoint: active Menu Correctness Engine README, spec, and marketing docs no longer turn save-time validation into blanket claims that every surface is always correct, correct everywhere, guaranteed validated, instantly updated, or covered by downloaded/generated artifacts after later edits. The docs now state that MCE validates project data at save time, stamps `_mce` metadata on the existing project document, and keeps supported surfaces behind their own refresh, download, device, provider, and target-specific QA evidence. `npm run verify:public-business-truth` now source-gates those MCE README/spec/marketing claim boundaries. Follow-up gates passed: `node --check scripts/verification/verify-public-business-truth.js`, `npm run verify:public-business-truth`, targeted MCE stale blanket surface/correctness scan, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 91/91 checks, including 87 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. No MCE runtime behavior, project save behavior, publish-gate behavior, customer-facing output, public cache behavior, Digital Screens behavior, PDF generation behavior, POS/provider behavior, Firestore rule, Storage rule, Cloud Function logic, Firebase deploy, Vercel deploy, production build, provider call, browser/device QA, Firestore write, Storage write, launch approval, or release certification changed for this checkpoint.

Client Menu availability refresh-copy checkpoint: active Client Menu README, spec, marketing, and AutoSell docs no longer describe sold-out availability changes as instant customer freshness, 100% instantly reflected updates, or "Instant Availability." The docs now use "Availability State," state that sold-out items fade after public menu refresh, and keep customer-visible availability freshness tied to the current public cache path with the existing up-to-60-second window. `npm run verify:customer-app-pwa` now source-gates those active Client Menu and AutoSell docs against the stale instant-availability wording and requires the approved-source/cache-refresh replacement copy. Follow-up gates passed: `node --check scripts/verification/verify-customer-app-pwa.js`, `npm run verify:customer-app-pwa`, targeted active Client Menu stale instant-availability scan, `npm run verify:public-business-truth`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 91/91 checks, including 87 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. No Client Menu runtime behavior, item availability behavior, owner save behavior, public menu rendering, public cache behavior, customer app service worker behavior, Digital Screens behavior, Firestore rule, Storage rule, Cloud Function logic, Firebase deploy, Vercel deploy, production build, provider call, browser/device QA, Firestore write, Storage write, launch approval, or release certification changed for this checkpoint.

Client Menu marketing freshness-copy checkpoint: active Client Menu marketing copy no longer uses a standalone "always live" hero line, one-minute price-update testimonial/campaign placeholder, live-menu CTA, or "runs itself" campaign framing that implies immediate customer freshness. The marketing doc now says the menu link is backed by the approved source, customer-visible changes appear after refresh, and the CTA asks for an approved menu link. `npm run verify:customer-app-pwa` now source-gates these active Client Menu marketing lines against stale immediate-freshness copy. Follow-up gates passed: `node --check scripts/verification/verify-customer-app-pwa.js`, `npm run verify:customer-app-pwa`, targeted active Client Menu stale always-live/one-minute/live-menu CTA scan, `npm run verify:public-business-truth`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 91/91 checks, including 87 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. No Client Menu runtime behavior, item availability behavior, owner save behavior, public menu rendering, public cache behavior, customer app service worker behavior, Digital Screens behavior, Firestore rule, Storage rule, Cloud Function logic, Firebase deploy, Vercel deploy, production build, provider call, browser/device QA, Firestore write, Storage write, launch approval, or release certification changed for this checkpoint.

Client Menu performance and realtime-doc boundary checkpoint: active Client Menu spec, implementation, website, and help docs no longer certify fixed load-time/FCP/LCP/TTI numbers, fixed extraction timing, real-time owner-change freshness, or offline cached menu access as current release facts. The docs now route performance claims to target-release browser/device evidence, keep customer-visible freshness on the public cache path, and preserve the service-worker boundary that offline mode shows a reconnect screen instead of cached menu content. `npm run verify:customer-app-pwa` now rejects stale fixed-speed, real-time, auto-update, and offline-cache wording across the Client Menu doc set. Follow-up gates passed: `node --check scripts/verification/verify-customer-app-pwa.js`, `npm run verify:customer-app-pwa`, targeted active Client Menu fixed-speed/realtime/offline-cache scan, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local`. No Client Menu runtime behavior, item availability behavior, extraction behavior, public menu rendering, public cache behavior, customer app service worker behavior, analytics behavior, Firestore rule, Storage rule, Cloud Function logic, Firebase deploy, Vercel deploy, production build, provider call, browser/device QA, Firestore write, Storage write, launch approval, or release certification changed for this checkpoint.

Customer App freshness public-copy boundary checkpoint: active Customer App website and marketing docs no longer tell public/sales audiences that the installed app updates automatically, reflects menu edits immediately, always shows the current menu, opens instantly, has fixed save timing, or has unverified repeat-visit/testimonial/stat proof. The docs now describe the Customer App as opening the current public menu path, with approved menu changes appearing after the supported public cache or return-to-app refresh path, and with owner quotes or analytics stats requiring live evidence before publication. `npm run verify:customer-app-pwa` now source-gates this Customer App public-copy boundary against stale immediate freshness, fixed timing, and unsupported social-proof phrasing. Follow-up gates passed: `node --check scripts/verification/verify-customer-app-pwa.js`, targeted Customer App stale immediate-freshness/fixed-timing/social-proof scan, and `npm run verify:customer-app-pwa`. No Customer App runtime behavior, manifest behavior, service-worker behavior, public menu rendering, public cache behavior, visibility refresh behavior, analytics behavior, Firestore rule, Storage rule, Cloud Function logic, Firebase deploy, Vercel deploy, production build, provider call, browser/device QA, Firestore write, Storage write, launch approval, or release certification changed for this checkpoint.

Customer App companion evidence-bound docs checkpoint: active Customer App help, mobile-support, spec, and marketing docs no longer preserve fixed `<5 seconds` mobile admission tests, immediate/latest-menu help guidance, fixed conversion/open/activation/retention targets, future-phase push-notification wording, unsupported native-app cost comparisons, or broad every-phone/device-coverage phrasing as current release claims. The docs now require save acknowledgement, supported public cache/return-to-app refresh, release-specific KPI targets, cohort evidence before retention claims, real-device QA for browser coverage, and current market evidence before exact cost comparisons. `npm run verify:customer-app-pwa` now source-gates these Customer App companion evidence boundaries. Follow-up gates passed: `node --check scripts/verification/verify-customer-app-pwa.js`, targeted Customer App stale fixed-speed/immediate-freshness/KPI/cost/device scan, and `npm run verify:customer-app-pwa`. No Customer App runtime behavior, manifest behavior, service-worker behavior, public menu rendering, public cache behavior, visibility refresh behavior, analytics behavior, mobile routing behavior, Firestore rule, Storage rule, Cloud Function logic, Firebase deploy, Vercel deploy, production build, provider call, browser/device QA, Firestore write, Storage write, launch approval, or release certification changed for this checkpoint.

Main website publish-surface copy checkpoint: `__docs__/main-website/main-website_content.md` no longer says "Atomic publishing — all surfaces update together." The Publish section now states that MenuList-controlled live surfaces refresh through their supported cache, listener, or device paths and that fresh PDF downloads should replace older downloaded or printed copies. `npm run verify:website-public-copy-boundary` now source-gates this main website content boundary against the stale atomic/all-surface publishing line. Follow-up gates passed: `node --check scripts/verification/verify-website-public-copy-boundary.js`, `npm run verify:website-public-copy-boundary`, targeted main website stale atomic/all-surface publishing scan, `npm run verify:public-business-truth`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 91/91 checks, including 87 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. No main website runtime behavior, homepage mounted component behavior, public menu runtime behavior, OBP runtime behavior, Digital Screens behavior, PDF generation behavior, public cache behavior, Firestore rule, Storage rule, Cloud Function logic, Firebase deploy, Vercel deploy, production build, provider call, browser/device QA, Firestore write, Storage write, launch approval, or release certification changed for this checkpoint.

Main website marketing distribution-copy checkpoint: `__docs__/main-website/main-website_marketing.md` and the communication-doctrine section in `__docs__/main-website/main-website_content.md` no longer use lifecycle/ad/source-copy that frames the menu as always correct everywhere after one update. The docs now describe one approved source, approved-link sharing, old-link/PDF replacement, and replacement of external links that customers may still find. `npm run verify:website-public-copy-boundary` now source-gates these main website distribution lines against stale universal-correctness copy. Follow-up gates passed: `node --check scripts/verification/verify-website-public-copy-boundary.js`, `npm run verify:website-public-copy-boundary`, targeted active main website stale always-correct/correct-everywhere distribution scan, `npm run verify:public-business-truth`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 91/91 checks, including 87 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. No main website runtime behavior, website mounted component behavior, lifecycle notification runtime, Menu Presence Monitor behavior, public menu runtime behavior, OBP runtime behavior, PDF behavior, external-provider behavior, public cache behavior, Firestore rule, Storage rule, Cloud Function logic, Firebase deploy, Vercel deploy, production build, provider call, browser/device QA, Firestore write, Storage write, launch approval, or release certification changed for this checkpoint.

Main website where-it-lives surface-copy checkpoint: `__docs__/main-website/main-website_content.md` and active Website locale packs no longer say the published menu appears across every customer surface automatically. The Where It Lives section now separates MenuList-controlled links/assets from owner-placed external profiles and older printed/downloaded copies, and it routes Digital Screens through configured screen refresh paths. `npm run verify:website-public-copy-boundary` now source-gates this active main website section and locale strings against stale every-surface publish copy. Follow-up gates passed: `node --check scripts/verification/verify-website-public-copy-boundary.js`, `npm run verify:website-public-copy-boundary`, `npm run verify:website-resource-locales`, targeted active main website and locale stale every-surface publish scan, `npm run verify:public-business-truth`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 91/91 checks, including 87 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. No main website runtime behavior, website mounted component behavior, locale routing behavior, public menu runtime behavior, OBP runtime behavior, Digital Screens behavior, PDF generation behavior, external-profile placement, public cache behavior, Firestore rule, Storage rule, Cloud Function logic, Firebase deploy, Vercel deploy, production build, provider call, browser/device QA, Firestore write, Storage write, launch approval, or release certification changed for this checkpoint.

Silent Correction supported-surface freshness checkpoint: `__docs__/silent-correction-systems/silent-correction-systems_spec.md` no longer says Silent Correction Systems ensure customers never see wrong hours/prices/menus/data, no longer says all surfaces share guaranteed 60-second propagation, and no longer says a price typo propagates instantly to all surfaces. The spec now frames Silent Correction as guardrails for supported surfaces, states that public menu and Official Business Page output can take up to 60 seconds to refresh, and keeps generated/provider-backed artifacts behind separate evidence. `npm run verify:public-business-truth` now source-gates those supported-surface and artifact/provider boundaries. Follow-up gates passed: `node --check scripts/verification/verify-public-business-truth.js`, `npm run verify:public-business-truth`, targeted Silent Correction stale blanket freshness/correctness scan, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 91/91 checks, including 87 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. No Silent Correction runtime behavior, MCE behavior, public menu runtime behavior, OBP runtime behavior, Digital Screens behavior, PDF/provider behavior, public cache behavior, Firestore rule, Storage rule, Cloud Function logic, Firebase deploy, Vercel deploy, production build, provider call, browser/device QA, Firestore write, Storage write, launch approval, or release certification changed for this checkpoint.

Multi-Outlet public claim sync/cache checkpoint: active Multi-Outlet README, spec, marketing, help, and website docs no longer claim every store sees master updates instantly, all stores update instantly, changes propagate instantly, instant sync, default guaranteed menu consistency, or guaranteed brand consistency. The public/business-facing claim now says saved master updates flow to linked stores through the outlet sync/cache path, while customer menus can take up to 60 seconds to refresh. `npm run verify:multi-location-boundary` now source-gates the active Multi-Outlet README/spec/marketing/website docs against those stale instant/guarantee phrases and requires the saved-update/cache-path replacement wording; `npm run verify:public-business-truth` also guards the website/helpdoc go-live copy. Follow-up gates passed: `node --check scripts/verification/verify-multi-location-boundary.js`, `npm run verify:multi-location-boundary`, `npm run verify:public-business-truth`, targeted active Multi-Outlet stale instant/guarantee scan, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 91/91 checks, including 87 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. No Multi-Outlet runtime behavior, linked outlet save behavior, outlet policy behavior, public menu runtime behavior, public cache behavior, Firestore rule, Storage rule, Cloud Function logic, Firebase deploy, Vercel deploy, production build, provider call, browser/device QA, Firestore write, Storage write, launch approval, or release certification changed for this checkpoint.

Multi-Outlet marketing update-everywhere checkpoint: active Multi-Outlet marketing copy no longer uses the shorter "update everywhere" and "instant updates" slogans. The marketing doc now uses saved master changes flowing through the outlet sync/cache path, with linked outlets inheriting saved master prices through that path. `npm run verify:multi-location-boundary` now source-gates the active Multi-Outlet marketing doc against those remaining stale slogans. Follow-up gates passed: `node --check scripts/verification/verify-multi-location-boundary.js`, `npm run verify:multi-location-boundary`, targeted active Multi-Outlet stale update-everywhere slogan scan, `npm run verify:public-business-truth`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 91/91 checks, including 87 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. No Multi-Outlet runtime behavior, linked outlet save behavior, outlet policy behavior, public menu runtime behavior, public cache behavior, Firestore rule, Storage rule, Cloud Function logic, Firebase deploy, Vercel deploy, production build, provider call, browser/device QA, Firestore write, Storage write, launch approval, or release certification changed for this checkpoint.

Menu Command Center public claim boundary checkpoint: active Menu Command Center spec, website, and marketing docs no longer claim bulk edits update every surface automatically, update POS/PDF/screen/QR outputs instantly, make connected systems follow automatically, or complete full-menu changes within seconds as a public promise. The docs now match the source-backed Command Center path: bulk edits update editor state, save through the single `updateProject()` project DAL path, MenuList-controlled public links and configured screens refresh through supported paths, downloaded PDFs/printed copies need replacement, and POS/external systems require connected integration evidence. `npm run verify:menu-project-editor-boundary` now source-gates these Command Center docs against the stale all-surface/instant/POS/PDF propagation phrases and requires the bounded editor-save, supported-refresh, artifact-replacement, and connected-integration wording. Follow-up gates passed: `node --check scripts/verification/verify-menu-project-editor-boundary.js`, `npm run verify:menu-project-editor-boundary`, targeted active Menu Command Center stale all-surface/POS/PDF/instant scan, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 91/91 checks, including 87 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. No Menu Command Center runtime behavior, editor persistence behavior, project DAL behavior, public menu runtime behavior, Digital Screens behavior, PDF generation behavior, POS/provider behavior, public cache behavior, Firestore rule, Storage rule, Cloud Function logic, Firebase deploy, Vercel deploy, production build, provider call, browser/device QA, Firestore write, Storage write, launch approval, or release certification changed for this checkpoint.

Special Menu Switching surface boundary checkpoint: active Special Menu Switching README, spec, implementation, validation, website, marketing, and help docs no longer claim special-menu activation updates every surface automatically, resolves PDF/POS outputs automatically, or guarantees that every surface/customer always sees the right menu without target-specific refresh or replacement evidence. The docs now match the source-backed runtime: active special menus resolve through `activeSpecialMenuId` on the public menu/QR link, OBP menu entry, and configured Digital Screen data/version paths; downloaded PDFs/printed copies require regeneration or replacement; POS/provider targets require connected integration evidence. `npm run verify:public-business-truth` now source-gates these Special Menu Switching docs against stale all-surface/PDF/POS/everywhere phrases and requires the public-link, OBP, configured-screen, artifact-replacement, and provider-integration boundary wording. Follow-up gates passed: `node --check scripts/verification/verify-public-business-truth.js`, `npm run verify:public-business-truth`, targeted active Special Menu Switching stale all-surface/PDF/POS scan, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 91/91 checks, including 87 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. No Special Menu Switching runtime behavior, public route resolver behavior, OBP behavior, Digital Screens behavior, scheduler behavior, cache behavior, PDF generation behavior, POS/provider behavior, Firestore rule, Storage rule, Cloud Function logic, Firebase deploy, Vercel deploy, production build, provider call, browser/device QA, Firestore write, Storage write, launch approval, or release certification changed for this checkpoint.

B2C View marketing performance/device-coverage copy checkpoint: `__docs__/projects/b2c-view/b2c-view_marketing.md` no longer preserves instant access, fixed under-2-second load timing, every-phone support, offline support, Google-discoverable, one-click sharing, fake testimonials, QR adoption stats, or customer-behavior claims as usable current collateral. The marketing draft now frames B2C View as a mobile-friendly customer menu link for supported browsers, controlled brand presentation, preview before publish, supported public cache/share paths, and release-specific evidence before performance, device-coverage, QR adoption, indexing, sharing, or customer-behavior claims. `npm run verify:menu-design-presentation-boundary` now rejects stale B2C marketing performance/device/share claims while requiring the evidence-bound customer-menu copy. Follow-up gates passed: `node --check scripts/verification/verify-menu-design-presentation-boundary.js`, `npm run verify:menu-design-presentation-boundary`, targeted active B2C View marketing stale performance/device scan, `npm run verify:agent-readiness`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 91/91 checks, including 87 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. No B2C View runtime behavior, customer menu runtime, design settings behavior, publish behavior, public cache behavior, share modal behavior, QR generation behavior, public route behavior, Firestore rule, Storage rule, Cloud Function logic, Firebase deploy, Vercel deploy, production build, browser/device QA, production-host smoke, Firestore write, Storage write, launch approval, or release certification changed for this checkpoint.

AI Image Generation website/marketing claim boundary checkpoint: `__docs__/projects/ai-image-generation/ai-image-generation_website.md` and `__docs__/projects/ai-image-generation/ai-image-generation_marketing.md` no longer preserve one-click, instant, every-item, full-menu, professional-quality, fixed-timing, photoshoot-cost, perfect-style, full-rights third-party-platform, fake testimonial, customer-behavior, or guaranteed quality claims as usable current collateral. The docs now frame image generation as menu item image drafts prepared where plan, credits, provider, feature flags, and safety checks allow, with owner review before publishing and only approved images going customer-facing. `npm run verify:public-business-truth` now reads both docs and rejects stale AI Image Generation public-collateral claims while requiring the current launch boundary and review/plan/credit/source-gate wording. Follow-up gates passed: `node --check scripts/verification/verify-public-business-truth.js`, `npm run verify:public-business-truth`, targeted active AI Image Generation website/marketing stale public-claim scan, `npm run verify:ai-accounting`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 91/91 checks, including 87 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. No AI Image Generation runtime behavior, image generation route behavior, batch worker behavior, image editing behavior, AI accounting behavior, public menu image-rendering behavior, Storage behavior, Firestore rule, Storage rule, Cloud Function logic, Firebase deploy, Vercel deploy, production build, browser/device QA, provider smoke, Firestore write, Storage write, launch approval, or release certification changed for this checkpoint.

AI Image Generation helpdoc claim boundary checkpoint: `__docs__/projects/ai-image-generation/ai-image-generation_helpdoc.md` no longer tells owners that MenuList automatically generates professional-looking photos, handles the entire menu at once, finishes single images in 5-10 seconds, finishes 50-item menus in about 5 minutes, allows five images per minute as owner-facing policy, saves time on large menus without evidence, or makes rejected drafts cost nothing extra. The help doc now frames image generation as selected-item or supported-batch drafts where feature, plan, credits, provider, and safety checks allow, with owner review before public-menu use. `npm run verify:public-business-truth` now reads the help doc with the existing website/marketing AI Image Generation boundary and rejects stale help copy while requiring draft, review, credit/provider, bounded-batch, and public-menu review wording. Follow-up gates passed for this checkpoint: `node --check scripts/verification/verify-public-business-truth.js`, `npm run verify:public-business-truth`, targeted active AI Image Generation helpdoc stale public-claim scan, `npm run verify:ai-accounting`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 91/91 checks, including 87 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. No AI Image Generation runtime behavior, image generation route behavior, batch worker behavior, image editing behavior, AI accounting behavior, public menu image-rendering behavior, Storage behavior, Firestore rule, Storage rule, Cloud Function logic, Firebase deploy, Vercel deploy, production build, browser/device QA, provider smoke, Firestore write, Storage write, launch approval, or release certification changed for this checkpoint.

AI Data Extraction website speed/provider claim boundary checkpoint: `__docs__/projects/ai-data-extraction/ai-data-extraction_website.md` no longer presents photo/PDF extraction as a live digital menu in seconds, under-a-minute completion, all-format handling, or every-field automatic extraction without target-run evidence. The website draft now identifies itself as source-backed website-copy evidence only, requires production-readiness audit evidence, External Certification Runbook evidence, `npm run verify:menu-extraction-pipeline`, `npm run verify:ai-accounting`, provider smoke, authenticated desktop/mobile upload and extraction-review QA, Firebase Functions/Storage deploy evidence where the release depends on worker or Storage behavior, target deploy evidence, production-host smoke, and release-specific evidence for numeric speed, accuracy, page-count, language-count, or volume claims. `npm run verify:agent-readiness` now source-gates that website boundary and rejects stale speed/provider/all-format claims. Follow-up gates passed: `node --check scripts/verification/verify-agent-readiness.js`, `npm run verify:agent-readiness`, targeted active AI Data Extraction website stale speed/provider scan, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 91/91 checks, including 87 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. No AI Data Extraction runtime behavior, upload behavior, extraction worker behavior, review behavior, publish behavior, public menu runtime, provider call, AI accounting behavior, Firestore rule, Storage rule, Cloud Function logic, Firebase deploy, Vercel deploy, production build, browser/device QA, Firestore write, Storage write, launch approval, or release certification changed for this checkpoint.

AI Data Extraction marketing speed/every-field copy checkpoint: `__docs__/projects/ai-data-extraction/ai-data-extraction_marketing.md` no longer preserves January 2026 speed, provider, every-field, fixed accuracy, fixed timing, fake testimonial, no-typo, no-data-entry, or fixed-demo-stat claims as usable current collateral. The marketing draft now frames extraction as a review draft from a clear photo or supported PDF, owner review of extracted fields, and publishing only the approved menu. `npm run verify:agent-readiness` now rejects stale AI Data Extraction marketing speed, provider, every-field, accuracy, and demo-stat wording while requiring the current review-draft and evidence-bound copy. Follow-up gates passed: `node --check scripts/verification/verify-agent-readiness.js`, `npm run verify:agent-readiness`, targeted active AI Data Extraction marketing stale speed/every-field scan, `npm run verify:menu-extraction-pipeline`, `npm run verify:ai-accounting`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 91/91 checks, including 87 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. No AI Data Extraction runtime behavior, upload behavior, extraction worker behavior, review behavior, publish behavior, public menu runtime, provider call, AI accounting behavior, Firestore rule, Storage rule, Cloud Function logic, Firebase deploy, Vercel deploy, production build, browser/device QA, provider smoke, Firestore write, Storage write, launch approval, or release certification changed for this checkpoint.

AI System Layer website reference claim boundary checkpoint: `__docs__/ai-system-layer/ai-system-layer_website.md` no longer provides reusable website copy that says photo/PDF upload creates a digital menu in seconds or reads every item, price, and category automatically. The infrastructure doc now identifies itself as a source-backed internal website reference only, requires the active production-readiness audit, External Certification Runbook, `npm run verify:menu-extraction-pipeline`, `npm run verify:ai-accounting`, provider smoke, authenticated desktop/mobile upload and extraction-review QA, target deploy evidence, production-host smoke, and release-specific evidence before numeric speed, accuracy, page-count, language-count, or volume claims. Its indirect website references now describe a review draft after processing, owner review/editing, and publishing only after approval. `npm run verify:agent-readiness` now source-gates the AI System Layer website reference against stale speed/all-field/publish-without-review claims. Follow-up gates passed: `node --check scripts/verification/verify-agent-readiness.js`, `npm run verify:agent-readiness`, targeted active AI System Layer website stale speed/all-field scan, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 91/91 checks, including 87 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. No AI System Layer runtime behavior, upload behavior, extraction worker behavior, review behavior, publish behavior, public menu runtime, provider call, AI accounting behavior, Firestore rule, Storage rule, Cloud Function logic, Firebase deploy, Vercel deploy, production build, browser/device QA, Firestore write, Storage write, launch approval, or release certification changed for this checkpoint.

Main website transformation-demo speed-copy checkpoint: `__docs__/main-website/main-website_marketing.md` no longer scripts the demo overlay as "Business online in under a minute." The transformation demo now shows a review draft, owner approval/publish, QR/customer link, Official Business Page, and the bounded overlay "Review first. Publish the approved menu link." `npm run verify:website-public-copy-boundary` now source-gates this active marketing playbook against the stale under-a-minute business-online speed claim and requires the review-before-publish replacement copy. Follow-up gates passed: `node --check scripts/verification/verify-website-public-copy-boundary.js`, `npm run verify:website-public-copy-boundary`, targeted active main website speed-claim scan, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 91/91 checks, including 87 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. No main website runtime behavior, upload behavior, extraction worker behavior, review behavior, publish behavior, public menu runtime, OBP runtime, QR generation, PDF behavior, public cache behavior, Firestore rule, Storage rule, Cloud Function logic, Firebase deploy, Vercel deploy, production build, provider call, browser/device QA, Firestore write, Storage write, launch approval, or release certification changed for this checkpoint.

Mobile Hours/Status Temporary Status docs checkpoint: `__docs__/mobile-operational-support/03-mobile-screens-spec.md` no longer says "Close for today" or "Reopen today" auto-publishes to all surfaces, and it no longer sketches unimplemented `todayOverride` / `tempCloseUntil` Firestore fields. The mobile screen spec now matches the source-backed path: owner UI updates optimistically, Temporary Status syncs through `POST /api/store/temp-status`, optimistic state remains only after `{ success: true }`, failures roll back, and customer output refreshes through supported public paths from the existing store `tempStatus` field. `npm run verify:temporary-status-boundary` now rejects the stale all-surface auto-publish wording and old field sketch while requiring the store `tempStatus` field and `/api/store/temp-status` response-confirmation boundary. Follow-up gates passed: `node --check scripts/verification/verify-temporary-status-boundary.js`, `npm run verify:temporary-status-boundary`, `npm run verify:mobile-shell-route-map`, targeted active mobile Hours/Status stale auto-publish/field scan, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 91/91 checks, including 87 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. No mobile runtime behavior, MobileShell behavior, Temporary Status API behavior, Today/Hours runtime behavior, public menu runtime, OBP runtime, Digital Screens behavior, public pull API behavior, public cache behavior, Firestore rule, Storage rule, Cloud Function logic, Firebase deploy, Vercel deploy, production build, provider call, browser/device QA, Firestore write, Storage write, launch approval, or release certification changed for this checkpoint.

Future roadmap supported-surface correctness checkpoint: `__docs__/strategy/menulist-future-roadmap-ssot.md` no longer states the 10-year thesis as "What customers see is always correct," no longer says customers trust MenuList pages are "always accurate," and no longer uses "Owner updates once -> correct everywhere" as active doctrine. The roadmap now preserves the strategy as supported MenuList surfaces sharing one owner-approved source, consistency where MenuList owns the surface, verified refresh paths for supported surfaces, and placement/replacement needs for external platforms plus printed/downloaded artifacts. `npm run verify:agent-readiness` now rejects stale blanket correctness doctrine and requires the supported-surface/external-artifact boundary wording. Follow-up gates passed: `node --check scripts/verification/verify-agent-readiness.js`, `npm run verify:agent-readiness`, targeted active Future Roadmap stale correctness scan, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 91/91 checks, including 87 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. No roadmap strategy approval, runtime behavior, public menu runtime, OBP runtime, Digital Screens behavior, PDF generation, external-platform integration, public cache behavior, Firestore rule, Storage rule, Cloud Function logic, Firebase deploy, Vercel deploy, production build, provider call, browser/device QA, Firestore write, Storage write, launch approval, or release certification changed for this checkpoint.

Main website current-source copy checkpoint: `__docs__/main-website/main-website_content.md` no longer says "One menu becomes every customer surface," no longer says "Your menu stays correct. You never check again," and no longer describes hours as real-time customer-visible truth. The Source-To-Public and Current Source sections now say one approved source becomes customer links/assets, supported public paths refresh through their own cache/device/replacement rules, customer-facing pages refresh through configured paths, external profiles and older print/download files need placement or replacement, and hours display from weekly hours, today's-hours edits, or Temporary Status when owners set it. `npm run verify:website-public-copy-boundary` now source-gates those main website content lines against stale every-surface, never-check-again, broad currentness, and real-time hours claims. Follow-up gates passed: `node --check scripts/verification/verify-website-public-copy-boundary.js`, `npm run verify:website-public-copy-boundary`, targeted active main website current-source stale-claim scan, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 91/91 checks, including 87 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. No main website runtime behavior, homepage mounted component behavior, public menu runtime, OBP runtime, Digital Screens behavior, PDF generation behavior, hours/Temporary Status runtime, public cache behavior, Firestore rule, Storage rule, Cloud Function logic, Firebase deploy, Vercel deploy, production build, provider call, browser/device QA, Firestore write, Storage write, launch approval, or release certification changed for this checkpoint.

PDF Surface website speed/all-field copy checkpoint: `__docs__/pdf-surface/pdf-surface_website.md` no longer says owners can download a PDF "in seconds" or that every item, every price, and every category is automatically formatted without owner review. The website doc now says the PDF is generated from current project data at download time, uses the approved source, is ready after the owner reviews the file, and that older downloads and printed copies need replacement after changes. `npm run verify:menu-export` now rejects the stale PDF speed/all-field wording while keeping the generation-time/versioned-output boundary. Follow-up gates passed: `node --check scripts/verification/verify-menu-export.js`, `npm run verify:menu-export`, targeted active PDF Surface website stale speed/all-field scan, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 91/91 checks, including 87 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. No PDF generation runtime behavior, Menu Kit behavior, print template behavior, generated artifact metadata, share modal behavior, public menu routing, cache behavior, Firebase rule, Storage rule, Cloud Function logic, Firebase deploy, Vercel deploy, production build, browser/device QA, visual print artifact review, provider call, Firestore write, Storage write, launch approval, or release certification changed for this checkpoint.

PDF Surface marketing/help fixed-speed print-quality copy checkpoint: `__docs__/pdf-surface/pdf-surface_marketing.md` and `__docs__/pdf-surface/pdf-surface_helpdoc.md` no longer present PDF output as professional-print-shop quality automatically, always-professional output, fixed under-5-second generation, few-seconds download timing, or no-review/no-action behavior without release-specific evidence. The docs now frame PDF Surface as a versioned generated artifact from current project data, with owner review before printing or sharing and replacement of older downloads or printed copies after menu changes. `npm run verify:menu-export` now rejects stale PDF Surface marketing/help fixed-speed and print-quality wording while requiring generation-time, versioned-output, and review-before-use copy. Follow-up gates passed for this checkpoint: `node --check scripts/verification/verify-menu-export.js`, `npm run verify:menu-export`, targeted active PDF Surface marketing/help stale speed/quality scan, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 91/91 checks, including 87 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. No PDF generation runtime behavior, Menu Kit behavior, print template behavior, generated artifact metadata, share modal behavior, public menu routing, cache behavior, Firebase rule, Storage rule, Cloud Function logic, Firebase deploy, Vercel deploy, production build, browser/device QA, visual print artifact review, provider call, Firestore write, Storage write, launch approval, or release certification changed for this checkpoint.

Upload File Processing website speed/all-field copy checkpoint: `__docs__/projects/upload-file-processing/upload-file-processing_website.md` and `__docs__/projects/upload-file-processing/upload-file-processing_marketing.md` no longer present upload as a 60-second/under-a-minute digital-menu conversion, all-format automatic conversion, every-field extraction, or a fixed multi-page demo result without release-specific evidence. The docs now frame upload as source processing into a structured review draft, owner review/editing, and publishing only the approved menu. `npm run verify:agent-readiness` now reads the upload website doc and rejects stale upload speed, all-field, page-count, and provider-demo wording while requiring the current website launch boundary. Follow-up gates passed: `node --check scripts/verification/verify-agent-readiness.js`, `npm run verify:agent-readiness`, targeted active Upload File Processing stale speed/all-field scan, `npm run verify:menu-extraction-pipeline`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 91/91 checks, including 87 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. No upload runtime behavior, extraction worker behavior, provider behavior, review behavior, publish behavior, public menu runtime, Storage rule, Firestore rule, Cloud Function logic, Firebase deploy, Vercel deploy, production build, browser/device QA, Storage write, Firestore write, launch approval, or release certification changed for this checkpoint.

Description Generation website speed/every-item copy checkpoint: `__docs__/projects/description-generation/description-generation_website.md` and `__docs__/projects/description-generation/description-generation_marketing.md` no longer present description generation as instant, every-item, all-language, copywriter-quality, no-editing, or fixed-duration demo copy without release-specific evidence. The docs now frame Description Generation as description suggestions for selected items, owner review/editing, and saving approved descriptions before publishing. `npm run verify:agent-readiness` now reads the Description Generation website doc and rejects stale speed, every-item, all-language, quality, no-editing, and demo timing wording while requiring the current website launch boundary. Follow-up gates passed: `node --check scripts/verification/verify-agent-readiness.js`, `npm run verify:agent-readiness`, targeted active Description Generation stale speed/every-item scan, `npm run verify:ai-accounting`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 91/91 checks, including 87 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. No description generation runtime behavior, provider behavior, AI accounting behavior, editor behavior, save behavior, publish behavior, public menu runtime, Firebase rule, Storage rule, Cloud Function logic, Firebase deploy, Vercel deploy, production build, browser/device QA, provider smoke, Firestore write, Storage write, launch approval, or release certification changed for this checkpoint.

Description Generation helpdoc speed/language/full-rewrite copy checkpoint: `__docs__/projects/description-generation/description-generation_helpdoc.md` no longer tells owners that MenuList automatically generates professional descriptions, rewrites all descriptions at once as a blanket capability, generates all languages at once, needs no extra review steps, exposes a fixed five-requests-per-minute policy, creates languages simultaneously, or translates the entire menu through this help flow. The help doc now frames description generation as selected-item drafts where enabled and credited, with processing time depending on provider status, item count, and current system conditions; owners review/edit drafts and save approved descriptions before publishing. `npm run verify:agent-readiness` now reads the help doc with the existing Description Generation website/marketing boundary and rejects stale speed, language, all-description, no-review, and fixed-policy wording while requiring draft, approval, timing-boundary, language-boundary, and credit/provider wording. Follow-up gates passed for this checkpoint: `node --check scripts/verification/verify-agent-readiness.js`, `npm run verify:agent-readiness`, targeted active Description Generation helpdoc stale speed/language/full-rewrite scan, `npm run verify:ai-accounting`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 92/92 checks, including 88 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. No description generation runtime behavior, provider behavior, AI accounting behavior, editor behavior, save behavior, publish behavior, public menu runtime, Firebase rule, Storage rule, Cloud Function logic, Firebase deploy, Vercel deploy, production build, browser/device QA, provider smoke, Firestore write, Storage write, launch approval, or release certification changed for this checkpoint.

Public print/share tool discovery and safe-print checkpoint: existing concrete Print & Share tool routes for QR Poster Maker, WhatsApp Menu Status Maker, Holiday Hours Poster Maker, Customer Link Card Maker, and Feedback QR Card Maker are now registered in `src/lib/seo/discoveryPolicy.ts`, `__docs__/main-website/main-website_impl.md`, `public/sitemap.xml`, `public/llms.txt`, and `public/llms-full.txt`. The browser-local print action in `src/lib/public-asset-tools/printShareToolRender.ts` now uses an owned temporary iframe with `no-referrer` instead of a writable `_blank` popup, and `npm run verify:print-share-tools` now gates that iframe/no-popup contract while `npm run verify:auth-security-failure-matrix` gates unsafe `_blank` opens. Follow-up gates passed: `npm run verify:agent-readiness`, `npm run verify:auth-security-failure-matrix`, `npm run verify:print-share-tools`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 92/92 checks, including 88 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. This changes only browser-local print handoff and discovery metadata for existing routes; it does not change report storage, template storage, external fetch behavior, AI/provider behavior, Firestore rules, Storage rules, Cloud Function logic, Firebase deploy, Vercel deploy, production build, browser/device QA, provider smoke, Firestore writes, Storage writes, launch approval, or release certification.

Multi-Language Translation website speed/language-count copy checkpoint: `__docs__/projects/multi-language-translation/multi-language-translation_website.md` and `__docs__/projects/multi-language-translation/multi-language-translation_marketing.md` no longer present customer-language output as instant, one-click, any-language, full-menu, every-field, fixed language-count, fixed timing, uniform-quality, customer-behavior, or external-platform automatic copy without release-specific evidence. The docs now frame customer-language output as translated drafts from the approved menu source, owner review of localized fields, and publishing only approved localized output for supported project languages. `npm run verify:agent-readiness` now reads the Multi-Language Translation website doc and rejects stale speed, language-count, one-click, full-menu, quality, customer-behavior, and demo timing wording while requiring the current website launch boundary. Follow-up gates passed: `node --check scripts/verification/verify-agent-readiness.js`, `npm run verify:agent-readiness`, targeted active Multi-Language Translation stale speed/language-count scan, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 91/91 checks, including 87 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. No translation runtime behavior, provider behavior, editor behavior, public renderer behavior, RTL behavior, customer menu runtime, external-platform behavior, Firebase rule, Storage rule, Cloud Function logic, Firebase deploy, Vercel deploy, production build, browser/device QA, provider smoke, Firestore write, Storage write, launch approval, or release certification changed for this checkpoint.

Support automation manual-template freshness-copy checkpoint: `__docs__/support-automation/README.md` no longer tells owners that a shared menu link is "always up to date." The deferred manual WhatsApp template now says the link opens the owner-published menu and repeats the existing customer-menu refresh window. `npm run verify:public-business-truth` now source-gates this support template against the stale always-up-to-date live-menu phrase while requiring the owner-published/cache-window replacement copy. Follow-up gates passed: `node --check scripts/verification/verify-public-business-truth.js`, `npm run verify:public-business-truth`, targeted support-template stale freshness-copy scan, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 91/91 checks, including 87 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. No support automation runtime, WhatsApp Business API integration, public menu routing, publish behavior, customer-facing output, public cache tag behavior, Firestore rule, Storage rule, Cloud Function logic, Firebase deploy, Vercel deploy, production build, provider call, browser/device QA, Firestore write, Storage write, launch approval, or release certification changed for this checkpoint.

Master update awareness Firestore-safe snapshot checkpoint: `src/lib/multiOutlet/masterUpdateDiff.ts` now normalizes master-update diff and snapshot payloads before `useMasterUpdateAwareness` persists `masterSnapshot.lastDiff`, so desktop and mobile "Got it" acknowledgements do not fail on nested `undefined` optional values such as missing outlet override prices. `npm run verify:multi-location-boundary` now source-gates the Firestore-safe diff/snapshot builders, the acknowledge write ordering, and the mobile/Firebase docs boundary. Follow-up gates passed: `node --check scripts/verification/verify-multi-location-boundary.js`, `npm run verify:multi-location-boundary`, targeted master update snapshot undefined scan, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 91/91 checks, including 87 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. No master/outlet inheritance behavior, linked outlet save behavior, menu publish behavior, customer-facing menu output, public cache tag behavior, Firestore rule, Storage rule, Cloud Function logic, Firebase deploy, Vercel deploy, production build, provider call, browser/device QA, extra Firestore read, extra Firestore write, Storage write, launch approval, or release certification changed for this checkpoint.

Historical readiness-score wording checkpoint: `__docs__/social-content/social-content_logic-verification.md` no longer presents its January 2026 logic-audit summary as `PRODUCTION READINESS: SAFE`, and `__docs__/projects/ai-data-extraction/ai-data-extraction_impl.md` no longer presents the March 13 audit score table with a standalone `Production Readiness | 10/10` row. Both docs now preserve those rows as historical code-readiness evidence only, with current release approval still controlled by this audit, the External Certification Runbook, current source gates, browser/mobile/provider/deploy evidence where relevant, and production-host smoke. `npm run verify:agent-readiness` now rejects the stale Social Content production-readiness line and the stale AI Data Extraction production-readiness score row. Follow-up gates passed: `node --check scripts/verification/verify-agent-readiness.js`, targeted Social Content and AI Data Extraction stale readiness-score scan, `npm run verify:agent-readiness`, `npm run docs:check-links`, focused `git diff --check`, broader active-doc readiness scan, and `npm run verify:production-readiness-local` with 91/91 checks, including 87 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. No runtime behavior, AI extraction behavior, Social Content Today behavior, provider call, Cloud Function logic, Firebase deploy, Vercel deploy, production build, Firestore write, Storage write, browser/device QA, or launch certification changed for this checkpoint.

AI Data Extraction retired deploy-command boundary checkpoint: `__docs__/projects/ai-data-extraction/ai-data-extraction_impl.md` no longer embeds the exact retired `ecomsai` `processMenuImages` deploy command in active implementation guidance. It preserves the June 11 Secret Manager/billing blocker as historical evidence only, blocks reuse of that retired target or command shape, and routes current callable retry evidence through `npm run verify:functions-deploy-preflight` plus External Certification Runbook Gate 1 against `menulist-qa`, with production still requiring QA evidence and explicit production deploy approval. `npm run verify:agent-readiness` now source-gates this AI Data Extraction implementation boundary. Follow-up gates passed: `node --check scripts/verification/verify-agent-readiness.js`, `node --check scripts/verification/verify-owner-notifications-boundary.js`, targeted AI Data Extraction and Owner Notifications stale deploy-command scan, `npm run verify:agent-readiness`, `npm run verify:owner-notifications-boundary`, `npm run docs:check-links`, focused `git diff --check`, and `npm run verify:production-readiness-local` with 91/91 checks, including 87 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. No runtime behavior, Cloud Function logic, Firebase deploy, Cloud Function upload, deployed revision, extraction behavior, provider call, Firestore write, Storage write, Vercel deploy, production build, browser/device QA, or launch certification changed for this checkpoint.

Owner Notifications deploy retry doc-boundary checkpoint: `__docs__/owner-notifications/owner-notifications_firebase.md` no longer repeats older `firebase deploy --only functions:... --project menulist-qa` command shapes or PATH-wrapped deploy commands in historical blocker notes. It now preserves the June 28-29 predeploy-lint/build attempts as historical blocker evidence only, routes current owner-notification retry evidence through `npm run verify:functions-deploy-preflight` plus External Certification Runbook Gate 1 against `menulist-qa`, requires any target-list deviation to be recorded in this audit before retry, and keeps production deploys behind QA evidence plus explicit production deploy approval. `npm run verify:owner-notifications-boundary` now source-gates that Firebase doc boundary. Follow-up gates passed: `node --check scripts/verification/verify-owner-notifications-boundary.js`, `npm run verify:owner-notifications-boundary`, targeted Owner Notifications stale deploy-command scan, `npm run docs:check-links`, focused `git diff --check`, and `npm run verify:production-readiness-local` with 91/91 checks, including 87 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. No runtime behavior, Cloud Function logic, Firebase deploy, Cloud Function upload, deployed revision, owner notification delivery behavior, provider call, Firestore write, Storage write, Vercel deploy, production build, browser/device QA, or launch certification changed for this checkpoint.

System Strengthening audit deploy-evidence boundary checkpoint: `__docs__/system-strengthening/menulist-system-data-flow-audit-2026-06-20.md` now states that its many deploy command blocks are append-only historical evidence, not current deploy guidance. Current MenuList Functions retries from that audit must start with `npm run verify:functions-deploy-preflight`, use External Certification Runbook Gate 1 against `menulist-qa`, record any target-list deviation and reason in this production-readiness audit before retry, and keep production deploys behind QA evidence plus explicit production deploy approval. `npm run verify:system-strengthening` now source-gates that boundary. Follow-up gates passed: `node --check scripts/verification/verify-system-strengthening-boundary.js`, `npm run verify:system-strengthening`, targeted System Strengthening deploy-boundary scan, `npm run docs:check-links`, focused `git diff --check`, and `npm run verify:production-readiness-local` with 91/91 checks, including 87 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. No runtime behavior, Cloud Function logic, Firebase deploy, Cloud Function upload, deployed revision, historical deploy evidence, Firestore write, Storage write, Vercel deploy, production build, browser/device QA, or launch certification changed for this checkpoint.

Menu Link Import deploy-target validation checkpoint: `__docs__/menu-link-import/menu-link-import_validation.md` no longer embeds the exact retired `ecomsai` `processMenuImagesJob` deploy command in active validation evidence. It now keeps the successful retired-target deploy as historical evidence only, blocks reuse of that target or command shape, and routes current retry evidence through `npm run verify:functions-deploy-preflight` plus External Certification Runbook Gate 1 against `menulist-qa`, with production still requiring QA evidence and explicit production deploy approval. `npm run verify:agent-readiness` now source-gates this Menu Link Import validation boundary. Follow-up gates passed: `node --check scripts/verification/verify-agent-readiness.js`, `npm run verify:agent-readiness`, targeted Menu Link Import retired-command scan, `npm run docs:check-links`, focused `git diff --check`, and `npm run verify:production-readiness-local` with 91/91 checks, including 87 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. No runtime behavior, Cloud Function logic, Firebase deploy, Cloud Function upload, deployed revision, menu link import job behavior, source acquisition behavior, Firestore write, Storage write, Vercel deploy, production build, browser/device QA, or launch certification changed for this checkpoint.

Maps Place Check deploy retry doc boundary checkpoint: `__docs__/menulist-tools/maps-place-check/maps-place-check_validation.md` no longer exposes the July 3 deploy attempt as a reusable `firebase deploy --only functions:mapsPlaceCheck --project menulist-qa` command. It now preserves the Cloud Resource Manager 403 as historical blocker evidence only, routes current retry evidence through `npm run verify:functions-deploy-preflight`, uses the scoped `firebase deploy --project menulist-qa --config firebase.json --only functions:mapsPlaceCheck --non-interactive` command after audit evidence, and keeps production deploys behind QA evidence plus explicit production deploy approval. `npm run verify:agent-readiness` now source-gates this Maps Place Check validation boundary. Follow-up gates passed: `node --check scripts/verification/verify-agent-readiness.js`, `npm run verify:agent-readiness`, targeted Maps Place Check stale deploy-command scan, `npm run docs:check-links`, focused `git diff --check`, and `npm run verify:production-readiness-local` with 91/91 checks, including 87 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. No runtime behavior, Cloud Function logic, Firebase deploy, Cloud Function upload, deployed revision, Google Maps/Gemini provider call, Firestore write, Storage write, Vercel deploy, production build, browser/device QA, or launch certification changed for this checkpoint.

Menu Health Monitor deploy retry doc boundary checkpoint: `__docs__/menu-health-monitor/menu-health-monitor_firebase.md` no longer exposes the older shared network-target/menu-health deploy attempt as a reusable `firebase deploy --only functions:... --project menulist-qa` command. It now preserves the June 28 predeploy-lint/build attempt as historical blocker evidence only, routes current retry evidence through `npm run verify:functions-deploy-preflight`, and requires any menu-health-specific Functions subset to be recorded with its exact scoped `menulist-qa` target list and reason in this audit before deploy retry. `npm run verify:agent-readiness` now source-gates this Firebase doc boundary. Follow-up gates passed: `node --check scripts/verification/verify-agent-readiness.js`, `npm run verify:agent-readiness`, targeted stale active-doc deploy-command scan, `npm run docs:check-links`, focused `git diff --check`, and `npm run verify:production-readiness-local` with 91/91 checks, including 87 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. No runtime behavior, Cloud Function logic, Firebase deploy, Cloud Function upload, deployed revision, menu health fetch behavior, Firestore write, Storage write, Vercel deploy, production build, browser/device QA, or launch certification changed for this checkpoint.

MenuList setup-guide deploy-command boundary checkpoint: `__docs__/deployment/three-product-environment-setup.md` no longer repeats the older MenuList QA `firebase deploy --only functions:menulistMaintenanceScheduler --project menulist-qa --config firebase.json` command shape inside the historical health-check attempt block. The guide now preserves that attempt as predeploy-lint/build blocker evidence only and points current retry work to the owner action register's scoped `firebase deploy --project menulist-qa --config firebase.json --only functions:menulistMaintenanceScheduler --non-interactive` command. `npm run verify:agent-readiness` now source-gates that setup-guide boundary. Follow-up gates passed: `node --check scripts/verification/verify-agent-readiness.js`, `npm run verify:agent-readiness`, targeted stale setup-guide deploy-command scan, `npm run docs:check-links`, focused `git diff --check`, and `npm run verify:production-readiness-local` with 91/91 checks, including 87 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. No runtime behavior, Cloud Function logic, Firebase deploy, Cloud Function upload, deployed revision, Secret Manager value, Firestore write, Storage write, Vercel deploy, production build, browser/device QA, or launch certification changed for this checkpoint.

Menu Extraction deploy retry doc boundary checkpoint: `__docs__/menu-extraction-pipeline/menu-extraction-pipeline_firebase.md` no longer exposes retired `ecomsai` deploy commands or older `firebase deploy --only functions:... --project menulist-qa` command shapes as reusable retry instructions. Historical June 11 `ecomsai` evidence and June 28-29 extraction deploy attempts are now preserved as blocker evidence only, while current retry evidence must start with `npm run verify:functions-deploy-preflight`, use External Certification Runbook Gate 1 against `menulist-qa`, and record any smaller extraction-specific target list plus reason in this audit before deploy retry. The existing July 2 SAFE_MODE worker attempt remains documented with the current scoped `--project menulist-qa --config firebase.json --only functions:processMenuImagesJob --non-interactive` command. `npm run verify:agent-readiness` now source-gates this Firebase doc boundary. Follow-up gates passed: `node --check scripts/verification/verify-agent-readiness.js`, `npm run verify:agent-readiness`, targeted stale active-doc deploy-command scan, `npm run docs:check-links`, focused `git diff --check`, and `npm run verify:production-readiness-local` with 91/91 checks, including 87 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. No runtime behavior, Cloud Function logic, Firebase deploy, Cloud Function upload, deployed revision, Gemini provider call, Firestore write, Storage write, Vercel deploy, production build, browser/device QA, or launch certification changed for this checkpoint.

Messaging Onboarding deploy retry doc boundary checkpoint: `__docs__/messaging-onboarding/messaging-onboarding_firebase.md` no longer repeats older `firebase deploy --only functions:... --project menulist-qa` command shapes in active retry prose. It now preserves June 28-29 Asset Intelligence, session-cleanup, WhatsApp media, response-size, endpoint-ID, lifecycle-event, upload-target, and network-target deploy attempts as historical predeploy-lint/build blocker evidence only, routes current retry evidence through `npm run verify:functions-deploy-preflight`, and requires any messaging-specific Functions subset to be recorded with its exact scoped `menulist-qa` target list and reason in this audit before deploy retry. `npm run verify:messaging-onboarding-monitor-boundary` now source-gates that Firebase doc boundary. Follow-up gates passed: `node --check scripts/verification/verify-messaging-onboarding-monitor-boundary.js`, `npm run verify:messaging-onboarding-monitor-boundary`, targeted stale active-doc deploy-command scan, `npm run docs:check-links`, focused `git diff --check`, and `npm run verify:production-readiness-local` with 91/91 checks, including 87 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. No runtime behavior, Cloud Function logic, Firebase deploy, Cloud Function upload, deployed revision, Meta/WhatsApp provider call, Firestore write, Storage write, Vercel deploy, production build, browser/device QA, or launch certification changed for this checkpoint.

Active Firebase deploy retry doc boundary checkpoint: active Ops Alerting Delivery and AI System Layer Firebase docs no longer expose older `firebase deploy --only functions:... --project menulist-qa` command shapes as reusable retry instructions. Ops Alerting now preserves June 28-29 deploy attempts as historical predeploy-lint/build blocker evidence only, routes current retry evidence through `npm run verify:functions-deploy-preflight`, and uses the scoped MenuList QA platform-alert command with `--config firebase.json` and `--non-interactive`. AI System Layer now routes gateway structured-classification retry evidence through External Certification Gate 1 and requires any additional Gemini consumer Function beyond documented Gate 1/source-file subsets to be listed in this audit before retry. The External Certification Runbook latest local boundary now reflects the current `npm run verify:production-readiness-local` pass with 91/91 checks, including 87 child root `verify:*` scripts. `npm run verify:agent-readiness` and `npm run verify:ai-accounting` now source-gate these docs. Follow-up gates passed: `node --check scripts/verification/verify-agent-readiness.js`, `node --check scripts/verification/verify-ai-accounting-hardening.js`, `npm run verify:agent-readiness`, `npm run verify:ai-accounting`, targeted stale active-doc deploy-command scan, `npm run docs:check-links`, focused `git diff --check`, and `npm run verify:production-readiness-local` with 91/91 checks, including 87 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. No runtime behavior, Cloud Function logic, Firebase deploy, Cloud Function upload, deployed revision, provider call, Firestore write, Storage write, Vercel deploy, production build, browser/device QA, or launch certification changed for this checkpoint.

Feature deploy-command boundary checkpoint: active Platform Founder Monitor deploy docs now use the scoped `menulist-qa` Functions command with `--config firebase.json` and `--non-interactive`, while the Customer App go-live checklist routes Cloud Function changes through External Certification Gate 1 after `npm run verify:functions-deploy-preflight` instead of naming a stale standalone `aggregateCustomerAnalytics` deploy target. `npm run verify:platform-founder-monitor-boundary` and `npm run verify:customer-app-pwa` now source-gate those feature-local deploy docs. Follow-up gates passed: `node --check scripts/verification/verify-platform-founder-monitor-boundary.js`, `node --check scripts/verification/verify-customer-app-pwa.js`, `npm run verify:platform-founder-monitor-boundary`, `npm run verify:customer-app-pwa`, `npm run docs:check-links`, focused `git diff --check`, and `npm run verify:production-readiness-local` with 91/91 checks, including 87 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. No runtime behavior, Cloud Function logic, Firebase deploy, Cloud Function upload, deployed revision, scheduler behavior, customer analytics behavior, Firestore write, Storage write, Vercel deploy, production build, browser/device QA, or launch certification changed for this checkpoint.

Functions package deploy-script boundary checkpoint: `functions/package.json` no longer exposes a default-project broad `firebase deploy --only functions` script. The default `deploy` script now fails closed with the scoped QA deploy instruction, while `deploy:menulist-qa` mirrors the External Certification Runbook Gate 1 target set with `--project menulist-qa`, `--config ../firebase.json`, and `--non-interactive`. `npm run verify:agent-readiness` and `npm run verify:functions-deploy-preflight` now source-gate that package-script boundary. Follow-up gates passed: `node --check scripts/verification/verify-agent-readiness.js`, `node --check scripts/verification/verify-functions-deploy-preflight.js`, Functions package JSON parse, `npm run verify:agent-readiness`, `npm run verify:functions-deploy-preflight`, `npm run docs:check-links`, focused `git diff --check`, and `npm run verify:production-readiness-local` with 91/91 checks, including 87 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. No Firebase deploy, Cloud Function upload, deployed revision, scheduler/callable behavior, production approval, provider call, Firestore write, Storage write, Vercel deploy, production build, browser/device QA, or launch certification changed for this checkpoint.

Local log filename hardening checkpoint: `src/lib/logs/utils.ts` and `functions/src/utils/log-utils.ts` now sanitize the local log filename before joining it with the `logs/` directory, normalize slash/backslash and parent-directory dot runs, and fall back to `local.log` for empty or dot-only filenames. `npm run verify:auth-security-failure-matrix` now source-gates the app and Functions helpers against raw filename joins. Follow-up gates passed: `node --check scripts/verification/verify-auth-security-failure-matrix.js`, `npm run verify:auth-security-failure-matrix`, `npm --prefix functions run build`, focused `git diff --check`, and `npm run verify:production-readiness-local` with 91/91 checks, including 87 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. No production logging behavior, API route behavior, exported Function trigger behavior, provider call, Firebase rule, Storage rule, Firebase deploy, Vercel deploy, production build, browser/device QA, live log write, launch approval, or release certification changed for this checkpoint.

Generated artifact tracking boundary checkpoint: the stale tracked root `tsconfig.tsbuildinfo` cache is being removed now that `tsconfig.json` writes build info to ignored `.next/cache/tsconfig.tsbuildinfo`; `.gitignore` also ignores local `tmp/` QA output and root `routes-manifest.json` route-inspection output. `npm run verify:agent-readiness` now fails if `tsconfig.tsbuildinfo`, `routes-manifest.json`, or anything under `tmp/` is present as a tracked source file. No TypeScript compiler settings, typecheck behavior, route behavior, screenshot/browser QA behavior, Firebase rule, Storage rule, Cloud Function, Firebase deploy, Vercel deploy, production build, provider smoke, launch approval, or release certification changed for this checkpoint.

Runtime local-log tracking boundary checkpoint: tracked `logs/*.log` runtime captures are being removed from source, `.gitignore` now ignores local runtime log files, the secure logging guide documents that local log files are development artifacts that must not be committed, and `npm run verify:agent-readiness` now fails if any tracked `logs/*.log` file is present in the working tree. The current deleted files previously held historical local API/provider captures, so this checkpoint treats their removal as source hygiene instead of feature behavior. No logging runtime behavior, secure logger sanitizer behavior, API route behavior, provider call, Firebase rule, Storage rule, Cloud Function, Firebase deploy, Vercel deploy, production build, browser/device QA, live log write, launch approval, or release certification changed for this checkpoint.

Customer FAQ Reply Pack source-gate checkpoint: `/tools/customer-faq-reply-pack` now has a feature-flagged public route, browser-local component, deterministic report/types, focused verifier, en-US/hi-IN locale namespaces, Tools Hub card, shareable report integration, discovery policy entry, static sitemap entry, LLM context entries, and package script. `npm run verify:public-truth-tools` now checks `src/lib/public-truth-tools/` and fails if a report/type module exists without a matching public tool manifest entry, except for the explicit shared helpers `shareableToolReport.ts` and `ownerPublicTruthReadiness.ts`. `npm run verify:tools-hub` now compares the component's literal `/tools/*` card routes and locale keys against the expected sixteen public tools, so stale cards for unimplemented routes fail. Follow-up gates passed: `node --check scripts/verification/verify-customer-faq-reply-pack.js`, `node --check scripts/verification/verify-public-truth-tools.js`, `node --check scripts/verification/verify-tools-hub.js`, `npm run verify:customer-faq-reply-pack`, `npm run verify:tools-hub`, `npm run verify:shareable-tool-reports`, `npm run verify:public-truth-tools` with `Public Truth Tools inventory verification passed (16 public tools)`, and `npm run verify:agent-readiness`. No customer conversation read, chatbot creation, automation configuration, message sending, entered-link fetch, report document storage, AI/search provider call, external platform mutation, owner dashboard runtime, customer menu/OBP runtime, Firebase rule, Storage rule, Cloud Function, Firebase deploy, Vercel deploy, production build, browser/device QA, provider smoke, launch approval, or release certification changed for this checkpoint.

Main website route inventory checkpoint: `npm run verify:agent-readiness` now derives concrete `/tools/*` and `/industries/*` website routes from `src/app/(website)/tools/*/page.tsx` and `src/app/(website)/industries/*/page.tsx`, then fails if those routes are missing from `PLATFORM_DISCOVERY_PAGES` or the main website implementation route table. `__docs__/main-website/main-website_impl.md` now records 184 implemented website routes: 57 concrete route files, 15 generated English resource articles, 7 reviewed localized resource hubs, and 105 reviewed localized resource articles. The route table now includes `/tools/reports`, all sixteen public tool pages, and all seven industry pages, and the content doc no longer describes the current industry set as four pages. Follow-up gates passed: `node --check scripts/verification/verify-agent-readiness.js`, `npm run verify:agent-readiness`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 91/91 checks, including 87 child root `verify:*` scripts, `typecheck`, `lint`, and `git diff --check`. No website runtime behavior, public tool runtime behavior, industry page runtime behavior, generated resource route, locale copy, Firebase rule, Storage rule, Cloud Function, Firebase deploy, Vercel deploy, production build, browser/device QA, external crawler behavior, sitemap submission, launch approval, or release certification changed for this checkpoint.

Public Truth Tools inventory gate checkpoint: `npm run verify:public-truth-tools` now derives the expected sixteen public `/tools/*` routes from a single manifest and fails if an implemented public tool route is missing its child verifier, package script, route page, website component, report builder, type contract, doc set, feature flag, public discovery policy entry, sitemap entry, LLM context entry, Tools Hub card, shareable-report source integration, or en-US/hi-IN locale namespace. `npm run verify:tools-hub` now includes the Business Facts Copy Pack, WhatsApp Reply Pack, and Customer FAQ Reply Pack routes/keys in the hub manifest, and Public Truth Tools Firebase docs now include their firebase docs in the current-tool cost coverage list. Follow-up gates passed: `node --check scripts/verification/verify-public-truth-tools.js`, `node --check scripts/verification/verify-tools-hub.js`, `npm run verify:tools-hub`, `npm run verify:public-truth-tools` with `Public Truth Tools inventory verification passed (16 public tools)`, and `npm run verify:production-readiness-local` with 91/91 checks, including 87 child root `verify:*` scripts, `docs:check-links`, `typecheck`, `lint`, and `git diff --check`. No public tool runtime behavior, Tools Hub runtime behavior, shareable report payload shape, `/api/public/contact` behavior, report lead ops behavior, Firebase rule, Storage rule, Cloud Function, Firebase deploy, Vercel deploy, production build, browser/device QA, provider call, report storage, external profile inspection, external platform update, launch approval, or release certification changed for this checkpoint.

Business Facts Copy Pack source-gate checkpoint: Business Facts Copy Pack now has `npm run verify:business-facts-copy-pack`, and `npm run verify:public-truth-tools` includes it with the rest of the public tool family. The focused verifier checks the route, component, report builder, type contract, docs, feature flag, localized copy, Tools Hub entry, public discovery policy, sitemap, LLM context, shareable-report integration, contact handoff guard, and V0 no-external-fetch/no-provider/no-report-storage boundaries. Follow-up gates passed: `node --check scripts/verification/verify-business-facts-copy-pack.js`, `node --check scripts/verification/verify-public-truth-tools.js`, `npm run verify:business-facts-copy-pack`, `npm run verify:doc-npm-scripts`, `npm run verify:public-truth-tools`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 91/91 checks, including 87 child root `verify:*` scripts, `typecheck`, `lint`, and `git diff --check`. No Business Facts Copy Pack runtime behavior, report generation, shareable report payload shape, `/api/public/contact` behavior, Firebase rule, Storage rule, Cloud Function, Firebase deploy, Vercel deploy, production build, browser/device QA, provider call, report storage, external profile inspection, external platform update, launch approval, or release certification changed for this checkpoint.

Realtime Database logger boundary checkpoint: `npm run verify:auth-security-failure-matrix` now scans active `src` app, component, hook, config, database, and lib files for direct `firebase/database` and `@firebase/database` imports. The bounded logger/client allowlist is limited to `src/lib/firebase/firebaseClient.ts` for `getDatabase`, plus `src/database/loggers/applicationLogger.ts` and `src/database/loggers/errorLogger.ts` for the existing bounded logger helpers `child`, `get`, `onValue`, `ref`, and `set`; new direct Realtime Database read, write, or listener paths must be intentionally reviewed and added to the source gate. Follow-up gates passed: `node --check scripts/verification/verify-auth-security-failure-matrix.js`, `npm run verify:auth-security-failure-matrix`, focused Realtime Database direct-import boundary scan, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 88/88 checks, including 84 child root `verify:*` scripts, `typecheck`, `lint`, and `git diff --check`. No Realtime Database logging behavior, logger read/write/listener behavior, Firebase client initialization, Firestore rule, Storage rule, Realtime Database rule, Cloud Function, Firebase deploy, Vercel deploy, production build, browser/device QA, provider call, Firestore write, Storage write, Realtime Database write, launch approval, or release certification changed for this checkpoint.

Browser surface Firestore read/listener boundary checkpoint: `npm run verify:public-business-truth` now scans active MenuList browser surfaces for direct Firestore read/listener helper imports and fails if UI code imports new read helpers outside the approved UI allowlist. The allowlist is limited to Digital Screens public state listeners, the POS Sync delivery-log read, and the platform Weekly Digest read; new owner, public, platform, and screen UI reads should use existing DALs, API routes, or shared helpers unless a direct listener/read path is intentionally reviewed and source-gated. Follow-up gates passed: `node --check scripts/verification/verify-public-business-truth.js`, `npm run verify:public-business-truth`, focused browser Firestore read/listener boundary scan, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 88/88 checks, including 84 child root `verify:*` scripts, `typecheck`, `lint`, and `git diff --check`. No Digital Screens listener, POS Sync delivery-log read, Weekly Digest read, browser behavior, Firestore rule, Storage rule, Cloud Function, Firebase deploy, Vercel deploy, production build, browser/device QA, provider call, Firestore write, Storage write, launch approval, or release certification changed for this checkpoint.

Browser Firebase Functions callable boundary checkpoint: `npm run verify:menulist-api-tenant-safety` now scans active MenuList browser surfaces for direct `firebase/functions` and `@firebase/functions` imports. The allowlist is limited to existing platform recovery controls: desktop/mobile Ops Control Room may call `forceRepublish`, and desktop/mobile Scheduler Monitor may call `triggerStoreNightlyScheduler`. Normal UI must continue to use approved API routes and shared helpers such as `@lib/firebase/functions`; new direct `httpsCallable` browser imports fail unless they are reviewed and added to the source gate intentionally. Follow-up gates passed: `node --check scripts/verification/verify-menulist-api-tenant-safety.js`, `npm run verify:menulist-api-tenant-safety`, focused browser Firebase Functions callable boundary scan, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 88/88 checks, including 84 child root `verify:*` scripts, `typecheck`, `lint`, and `git diff --check`. No platform recovery control, callable payload, callable permission, API route behavior, shared Firebase Functions helper, Cloud Function logic, Firebase deploy, Vercel deploy, production build, browser/device QA, provider call, Firestore write, Storage write, launch approval, or release certification changed for this checkpoint.

Browser Firebase Auth direct-method boundary checkpoint: `npm run verify:auth-security-failure-matrix` now scans active MenuList browser surfaces for direct Firebase Auth imports and fails if UI code imports new client auth methods outside the existing auth-entry allowlist. The gate covers `src/app` and `src/components` MenuList browser surfaces while excluding API routes and Answerlattice surfaces. The login page may continue to use `signInWithEmailAndPassword` and `signInWithCustomToken` for the established NextAuth-to-Firebase client sync handoff, and the forgot-password page may continue to use `sendPasswordResetEmail`; `NextAuth signOut` imports from `next-auth/react` remain allowed because the gate targets only `firebase/auth` and `@firebase/auth` direct browser imports. Follow-up gates passed: `node --check scripts/verification/verify-auth-security-failure-matrix.js`, `npm run verify:auth-security-failure-matrix`, focused browser Firebase Auth direct-method boundary scan, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 88/88 checks, including 84 child root `verify:*` scripts, `typecheck`, `lint`, and `git diff --check`. No login behavior, password-reset behavior, Firebase Auth sync behavior, NextAuth session behavior, sign-out behavior, API route behavior, auth middleware, Firestore rule, Storage rule, Cloud Function, Firebase deploy, Vercel deploy, production build, browser/device QA, provider call, Firestore write, Storage write, launch approval, or release certification changed for this checkpoint.

Browser surface Storage mutation boundary checkpoint: `npm run verify:storage-paths` now scans active MenuList browser surfaces for direct Firebase Storage mutation helper imports and fails if UI code imports `getStorage`, `ref`, `uploadBytes`, `uploadBytesResumable`, `uploadString`, `getDownloadURL`, or `deleteObject` from Firebase Storage. The source gate covers `src/app` and `src/components` MenuList browser surfaces while excluding API routes and Answerlattice surfaces, so owner, platform, public, and screen UI keep Storage writes/deletes behind existing storage DAL helpers, project upload helpers, media helpers, or server/API routes. Type-only `firebase/storage` imports remain allowed when they do not expose browser write/delete helpers. Follow-up gates passed: `node --check scripts/verification/verify-storage-path-hardening.js`, `npm run verify:storage-paths`, focused browser Storage mutation boundary scan, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 88/88 checks, including 84 child root `verify:*` scripts, `typecheck`, `lint`, and `git diff --check`. No browser behavior, Storage read, Storage upload, Storage delete, project upload helper, media helper, API route behavior, public menu, Firebase rule, Storage rule, Cloud Function, Firebase deploy, Vercel deploy, production build, browser/device QA, provider call, Firestore write, Storage write, launch approval, or release certification changed for this checkpoint.

Browser surface Firestore mutation boundary checkpoint: `npm run verify:public-business-truth` now scans active MenuList browser surfaces for direct Firestore mutation helper imports and fails if UI code imports `addDoc`, `setDoc`, `updateDoc`, `deleteDoc`, `writeBatch`, or `runTransaction` from Firestore. The source gate covers `src/app` and `src/components` MenuList browser surfaces while excluding API routes and Answerlattice surfaces, so owner, platform, public, and screen UI keep reads/listeners/timestamps where established but write mutations stay behind DAL helpers or server/API routes. Follow-up gates passed: `node --check scripts/verification/verify-public-business-truth.js`, `npm run verify:public-business-truth`, focused browser Firestore mutation boundary scan, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 88/88 checks, including 84 child root `verify:*` scripts, `typecheck`, `lint`, and `git diff --check`. No browser behavior, Firestore read, Firestore listener, DAL behavior, API route behavior, public menu, public cache, Firebase rule, Storage rule, Cloud Function, Firebase deploy, Vercel deploy, production build, browser/device QA, provider call, Firestore write, Storage write, launch approval, or release certification changed for this checkpoint.

Tenant and platform-user acknowledgement source-gate checkpoint: `npm run verify:public-business-truth` now scans every active `src` tenant write/stores-list call and platform-user write call and fails unless the matching acknowledgement guard appears in the nearby follow-up block before local tenant, tenant-store-list, or platform-user state can be trusted. The generic gate covers `addTenant()`, `updateTenant()`, `updateTenantsStoreslist()`, `addPlatformUser()`, `updatePlatformUser()`, and `addStoreToUser()` outside their DAL implementations; tenant create/update uses `assertTenantUpdateSucceeded()`, tenant stores-list writes use `assertTenantsStoresListUpdateSucceeded()`, and platform-user writes use `assertUserUpdateSucceeded()`. Follow-up gates passed: `node --check scripts/verification/verify-public-business-truth.js`, `npm run verify:public-business-truth`, focused tenant/platform-user acknowledgement source-gate scans, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 88/88 checks, including 84 child root `verify:*` scripts, `typecheck`, `lint`, and `git diff --check`. No tenant write, tenant stores-list write, platform-user write, store write, project write, staff creation, auth behavior, public menu, public cache, Firebase rule, Storage rule, Cloud Function, Firebase deploy, Vercel deploy, production build, browser/device QA, provider call, Firestore write, Storage write, launch approval, or release certification changed for this checkpoint.

Project lifecycle mutation acknowledgement source-gate checkpoint: `npm run verify:public-business-truth` now scans every active `src` project delete/activate/duplicate/restore call and fails unless the matching project acknowledgement guard appears in the nearby follow-up block before local deleted, active/inactive, duplicated, or restored state can be trusted. The generic gate covers `deleteProject()`, `setProjectActive()`, `duplicateProject()`, and `restoreProject()` outside the project DAL implementation; deletes require `assertProjectDeleteSucceeded()`, while activation, duplication, and restoration require `assertProjectUpdateSucceeded()`. Follow-up gates passed: `node --check scripts/verification/verify-public-business-truth.js`, `npm run verify:public-business-truth`, focused project lifecycle acknowledgement source-gate scan, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 88/88 checks, including 84 child root `verify:*` scripts, `typecheck`, `lint`, and `git diff --check`. No project deletion, activation, duplication, restoration, save, publish, metadata, editor, mobile menu, public menu, public cache, Digital Screens, Firebase rule, Storage rule, Cloud Function, Firebase deploy, Vercel deploy, production build, browser/device QA, provider call, Firestore write, Storage write, launch approval, or release certification changed for this checkpoint.

Project write acknowledgement source-gate checkpoint: `npm run verify:public-business-truth` now scans every active `src` project write/create/publish call and fails unless `assertProjectUpdateSucceeded()` appears in the nearby follow-up block before local saved, created, published, metadata, or background-persisted project state can be trusted. The generic gate covers `addProject()`, `publishProject()`, `updateProject()`, `updateProjectMetadata()`, and `updateProjectWithoutLoader()` outside the project DAL implementation, turning the existing hand-picked project acknowledgement assertions into a repo-wide regression check for active source. Follow-up gates passed: `node --check scripts/verification/verify-public-business-truth.js`, `npm run verify:public-business-truth`, focused project write acknowledgement source-gate scan, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 88/88 checks, including 84 child root `verify:*` scripts, `typecheck`, `lint`, and `git diff --check`. No project save, create, publish, metadata, deletion, active-state, duplicate, editor, mobile menu, public menu, public cache, Digital Screens, Firebase rule, Storage rule, Cloud Function, Firebase deploy, Vercel deploy, production build, browser/device QA, provider call, Firestore write, Storage write, launch approval, or release certification changed for this checkpoint.

PDF and physical-surface freshness-copy checkpoint: active PDF Surface and Physical Surfaces website/help/marketing/spec docs no longer claim generated or printed artifacts are always current, always matching, or guaranteed to match online prices after later edits. They now describe PDFs and print assets as generated from current project data or the current approved source at download time, with version ID/generation timestamp evidence and fresh-download guidance after menu changes. `npm run verify:menu-export` now rejects stale PDF/physical-surface always-current artifact claims and requires generation-time/versioned-output boundary wording. Follow-up gates passed: `node --check scripts/verification/verify-menu-export.js`, `npm run verify:menu-export`, targeted PDF/physical-surface stale freshness-copy scan, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 88/88 checks, including 84 child root `verify:*` scripts, `typecheck`, `lint`, and `git diff --check`. No PDF generation, Menu Kit generation, print template behavior, generated artifact metadata, share modal behavior, public menu routing, cache behavior, Firebase rule, Storage rule, Cloud Function, Firebase deploy, Vercel deploy, production build, browser/device QA, visual print artifact review, provider call, Firestore write, Storage write, launch approval, or release certification changed for this checkpoint.

Locale freshness public-copy checkpoint: active locale strings no longer promise that QR materials always open the current menu or that PDF exports always reflect the current menu. QR locale copy now says materials open the current published menu, and Bengali, Marathi, Tamil, and Telugu PDF feature copy now describes a print-ready PDF generated from menu data. `npm run verify:website-public-copy-boundary` now rejects stale locale QR/PDF freshness claims across active locale JSON files. Follow-up gates passed: `node --check scripts/verification/verify-website-public-copy-boundary.js`, `npm run verify:website-public-copy-boundary`, targeted locale freshness public-copy scan, `npm run verify:website-resource-locales`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 88/88 checks, including 84 child root `verify:*` scripts, `typecheck`, `lint`, and `git diff --check`. No QR generation, public menu routing, PDF generation, print template behavior, cache behavior, website route behavior, Firebase rule, Storage rule, Cloud Function, Firebase deploy, Vercel deploy, production build, browser/device QA, provider call, Firestore write, Storage write, launch approval, or release certification changed for this checkpoint.

Public FAQ schema freshness-copy checkpoint: `src/lib/schema/index.ts` no longer uses a fallback FAQ answer that tells search/AI consumers the menu is always up to date. The fallback now says customers can view the full published menu at the catalog URL, and active `publicFaqMenuAnswer` translations in English (GB), Arabic, Bengali, Spanish, Gujarati, Marathi, Tamil, Telugu, and Simplified Chinese no longer carry always-current menu claims. `npm run verify:public-business-truth` now parses every active locale JSON file and source-gates `publicFaqMenuAnswer` values against stale freshness promises while also guarding the schema fallback string. Follow-up gates passed: `node --check scripts/verification/verify-public-business-truth.js`, `npm run verify:public-business-truth`, targeted public FAQ schema freshness-copy scan, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 88/88 checks, including 84 child root `verify:*` scripts, `typecheck`, `lint`, and `git diff --check`. No schema shape, route behavior, public menu data selection, OBP rendering, cache behavior, sitemap/robots behavior, Firebase rule, Storage rule, Cloud Function, Firebase deploy, Vercel deploy, production build, browser/device QA, provider call, Firestore write, Storage write, launch approval, or release certification changed for this checkpoint.

Pricing public-copy freshness-boundary checkpoint: active pricing/plan-feature copy no longer promises real-time updates across all surfaces, real-time OBP status, or real-time webhook notifications. `src/components/website/pricing-pages/PricingFaq.tsx` now says approved updates flow through the public menu and business page refresh paths, and `src/data/PlatformFeaturesList.ts` now describes current status plus supported event notifications. `npm run verify:website-public-copy-boundary` now source-gates pricing FAQ and shared plan-feature copy against stale real-time/all-surface public claims and requires the bounded replacement copy. Follow-up gates passed: `node --check scripts/verification/verify-website-public-copy-boundary.js`, `npm run verify:website-public-copy-boundary`, targeted pricing/feature stale real-time public-claim scan, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 88/88 checks, including 84 child root `verify:*` scripts, `typecheck`, `lint`, and `git diff --check`. No pricing plan, entitlement, billing behavior, public menu behavior, OBP behavior, webhook behavior, notification behavior, Firebase rule, Storage rule, Cloud Function, Firebase deploy, Vercel deploy, production build, browser/device QA, provider call, Firestore write, Storage write, launch approval, or release certification changed for this checkpoint.

Truth & Accuracy Dominance source-boundary checkpoint: active `__docs__/truth-accuracy-dominance/` docs no longer position MenuList as universally current, instantly propagated, synchronized across every surface, or covered by blanket correctness/freshness promises. They now describe supported MenuList customer-facing surfaces reading saved and verified project/store data after normal refresh, publish, cache, download, or provider flows complete; public menu/OBP using the current public cache boundary; Digital Screens using their own `screen-data` cache/listener path; mobile approval requiring mobile QA evidence; and downloaded/provider/external targets requiring separate artifact or provider evidence. `npm run verify:public-business-truth` now source-gates the Truth & Accuracy Dominance docs against stale blanket correctness and freshness claims and requires the production audit, External Certification Runbook, public-output QA, public cache, Digital Screens, mobile, provider/artifact, and `ENABLE_MCE: true` source boundaries. Follow-up gates passed: `node --check scripts/verification/verify-public-business-truth.js`, `npm run verify:public-business-truth`, targeted active Truth & Accuracy Dominance stale blanket-claim scan, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 88/88 checks, including 84 child root `verify:*` scripts, `typecheck`, `lint`, and `git diff --check`. No MCE runtime behavior, project save behavior, publish behavior, public cache behavior, Digital Screens cache/listener behavior, provider behavior, generated artifact behavior, Firebase rule, Storage rule, Cloud Function, Firebase deploy, Vercel deploy, production build, browser/device QA, provider call, Firestore write, Storage write, launch approval, or release certification changed for this checkpoint.

Product Universe SSOT launch-boundary checkpoint: `__docs__/strategy/product-universe-ssot.md` no longer presents February 2026 product-universe source-state strategy as current production status, universal correctness, external-platform freshness, or launch certification. It now identifies itself as historical product-universe/source-state reference, routes current readiness to this audit, the External Certification Runbook, current source verifiers, browser/device QA, provider smoke, target deploy evidence, and production-host smoke, points live feature-flag truth to `src/config/features.ts`, and bounds MenuList positioning to supported customer-facing business truth surfaces while external platforms, printed artifacts, and cached third-party surfaces require their own integration or refresh evidence. `npm run verify:agent-readiness` now rejects the stale Product Universe production table rows, core-product-done wording, universal-correctness positioning, automatic all-surface correction claim, and built-status quick-card wording. Follow-up gates passed: `node --check scripts/verification/verify-agent-readiness.js`, `npm run verify:agent-readiness`, targeted Product Universe stale launch/correctness scan, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 88/88 checks, including 84 child root `verify:*` scripts, `typecheck`, `lint`, and `git diff --check`. No runtime behavior, feature flag value, public output, cache behavior, Firebase rule, Storage rule, Cloud Function, Firebase deploy, Vercel deploy, production build, browser/device QA, provider call, Firestore write, Storage write, launch approval, or release certification changed for this checkpoint.

Digital Screens freshness-copy boundary checkpoint: active Digital Screens README, spec, implementation, website, marketing, and help docs no longer position connected TVs as instant, always-current, real-time, or independently self-updating displays. They now match the source-backed screen runtime: `/screen/[token]` uses the 60-second `screen-data` server cache, display clients render cache-first from localStorage, public-output writes refresh public cache and bump `screen.contentVersion` plus the public-safe `platformSummary/screen_{storeId}` listener mirror, connected screens reload when the listener sees a newer version, and manual browser refresh remains the owner-facing fallback when a TV misses the listener path. `npm run verify:digital-screens-boundary` now source-gates those docs against stale freshness phrases and requires the matching cache/listener/manual-refresh wording. Follow-up gates passed: `node --check scripts/verification/verify-digital-screens-boundary.js`, `npm run verify:digital-screens-boundary`, targeted active Digital Screens stale freshness-copy scan, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 88/88 checks, including 84 child root `verify:*` scripts, `typecheck`, `lint`, and `git diff --check`. No Digital Screens runtime behavior, public screen route behavior, screen rendering behavior, screen cache behavior, listener behavior, owner settings behavior, Firestore rule, Storage rule, Cloud Function, Firebase deploy, Vercel deploy, production build, browser TV smoke, physical-device QA, Firestore write, Storage write, launch approval, or release certification changed for this checkpoint.

Behavior Engineering official-link freshness checkpoint: active Behavior Engineering README, spec, implementation, website, helpdoc, and marketing docs no longer position the official customer link as an instant, always-latest, or automatically-current freshness guarantee. The live share-copy sources in `src/lib/menu-kit/businessTypeLabels.ts`, desktop Use MenuList, Project Share Modal, and Menu Kit WhatsApp sharing now use current-approved-source wording and share the stable customer link without appending stale freshness promises. This matches the existing source contract: public customer output is current after owner-approved save/publish paths and the public cache refresh path, with explicit Publish actions preserved where the UI requires them. `npm run verify:public-business-truth` now source-gates the Behavior Engineering docs, menu-kit label source, desktop Use MenuList copy, share modal WhatsApp message, and Menu Kit WhatsApp message against stale official-link freshness phrases. Follow-up gates passed: `node --check scripts/verification/verify-public-business-truth.js`, `npm run verify:public-business-truth`, targeted Behavior Engineering stale official-link freshness scan, `npm run docs:check-links`, `npx tsc --noEmit --pretty false --incremental false`, `git diff --check`, and `npm run verify:production-readiness-local` with 88/88 checks, including 84 child root `verify:*` scripts, `typecheck`, `lint`, and `git diff --check`. No menu save behavior, publish behavior, public cache tag name, Digital Screen touch ownership, Firebase rule, Storage rule, Cloud Function, Firebase deploy, Vercel deploy, production build, browser/device QA, provider call, Firestore write, or Storage write changed for this checkpoint.

Menu revalidation explicit-tag assistant-cache checkpoint: `/api/revalidate/menu` now derives a normalized single store id from valid explicit `menu-store-{id}` / `store-{id}` tag arrays before clearing the Owner Business Assistant packet cache, so platform or `x-revalidate-secret` callers using a one-store explicit tag array do not revalidate Vercel cache tags while leaving the assistant read-model cache stale. The route still rejects explicit tag arrays from non-platform authenticated app sessions, still accepts only numeric per-store tags plus `client-stores` and `screen-data`, and live Digital Screen content-version touches remain owned by the browser, server, and Functions public-truth caller helpers after writes. `npm run verify:public-business-truth` and `npm run verify:menulist-api-tenant-safety` now source-gate the explicit-tag store-id derivation, bounded route diagnostics, docs boundary, valid tag allowlist, and authenticated store-access behavior. Follow-up gates passed: `node --check scripts/verification/verify-public-business-truth.js`, `node --check scripts/verification/verify-menulist-api-tenant-safety.js`, `npm run verify:public-business-truth`, `npm run verify:menulist-api-tenant-safety`, `npm run docs:check-links`, `npx tsc --noEmit`, and `npm run verify:production-readiness-local` with 88/88 checks, including 84 child root `verify:*` scripts, `typecheck`, `lint`, and `git diff --check`. No cache tag name, project/store write behavior, live Digital Screen touch ownership, Firebase rule, Storage rule, Cloud Function, Firebase deploy, Vercel deploy, production build, browser/device QA, provider call, Firestore write, or Storage write changed for this checkpoint.

Agent Readiness external-system claim-boundary checkpoint: active Agent Readiness README, spec, website, marketing, helpdoc, and `public/llms-full.txt` no longer claim MenuList guarantees AI assistant accuracy, instant external readability, external ranking/citation, universal AI/search visibility, or real-time external freshness. They now match the source-backed discovery contract: MenuList exposes owner-approved public pages, schema.org JSON-LD, sitemap/robots policy, `llms.txt`, `llms-full.txt`, and gated public API/POS surfaces where enabled, while external AI/search systems decide what they crawl, cite, show, summarize, rank, or refresh. `npm run verify:agent-readiness` now source-gates these active Agent Readiness docs and public LLM context files against stale guarantee phrases and requires the matching reserved-disabled `ENABLE_AGENT_DISCOVERY`, static discovery-layer, no dynamic agent endpoint, schema/source, and external-system boundary evidence. Follow-up gates passed: `node --check scripts/verification/verify-agent-readiness.js`, `npm run verify:agent-readiness`, targeted active Agent Readiness stale external-claim scan, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 88/88 checks, including 84 child root `verify:*` scripts. No Agent Readiness runtime behavior, public website runtime behavior, schema generation behavior, public menu/OBP runtime behavior, public API behavior, POS webhook behavior, Firebase rule, Storage rule, Cloud Function, Firebase deploy, Vercel deploy, production build, browser/device QA, provider call, Firestore write, Storage write, external crawler behavior, launch approval, or release certification changed for this checkpoint.

Official Business Page freshness-copy boundary checkpoint: active OBP README, website, marketing, helpdoc, and implementation docs no longer claim store/menu/page changes update instantly, always show the latest info, update themselves without an acknowledged save, or bypass the current public cache window. They now match the source-backed freshness contract: public OBP store/menu lookup uses `unstable_cache` with a 60-second `client-stores` tag window, owner-side store saves call `revalidatePublicClientCache()` after acknowledged store/summary propagation, and `/api/revalidate/menu` invalidates `menu-store-{storeId}`, `store-{storeId}`, and `client-stores`. `npm run verify:official-business-page-boundary` now source-gates the active OBP freshness copy against stale instant/latest/self-update phrases and requires the matching cache-tag, store-DAL, shared lookup, OBP content, README, website, marketing, helpdoc, and implementation evidence. Follow-up gates passed: `node --check scripts/verification/verify-official-business-page-boundary.js`, `npm run verify:official-business-page-boundary`, targeted active OBP stale freshness scan, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 88/88 checks, including 84 child root `verify:*` scripts. No OBP runtime behavior, public menu runtime behavior, store-DAL behavior, public cache behavior, Firebase rule, Storage rule, Cloud Function, Firebase deploy, Vercel deploy, production build, browser/device QA, provider call, Firestore write, Storage write, launch approval, or release certification changed for this checkpoint.

Custom domain SSL/verification boundary checkpoint: active Client Menu custom-domain docs no longer claim custom domains are fully supported with automatic SSL, require manual Vercel Dashboard setup, or keep the Vercel Domains API as future automation. They now match the source-backed route contract: `/api/domain` adds the domain to the Vercel project, stores it as `domainVerified: false`, verifies Vercel config before writing `domainVerified: true`, invalidates public cache after routing-state changes, and public menu lookup only serves custom domains where `customDomain`, `domainVerified: true`, and `active` all match. Certificate handling is documented as provider-managed after the domain is accepted and configured. `npm run verify:public-business-truth` now source-gates the Client Menu website and multi-tenant architecture docs against stale custom-domain/SSL phrases and requires the matching `/api/domain`, Vercel helper, public lookup, and client page evidence. Follow-up gates passed: `node --check scripts/verification/verify-public-business-truth.js`, `npm run verify:public-business-truth`, targeted active custom-domain stale-claim scan, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 88/88 checks, including 84 child root `verify:*` scripts. No custom-domain runtime behavior, Vercel behavior, public menu runtime behavior, DNS behavior, certificate behavior, public cache behavior, Firebase rule, Storage rule, Cloud Function, Firebase deploy, Vercel deploy, production build, browser/device QA, provider call, Firestore write, Storage write, launch approval, or release certification changed for this checkpoint.

Client Menu offline/freshness copy boundary checkpoint: active Client Menu docs no longer claim cached menu browsing, offline item interaction, Workbox menu caching, instant customer visibility, WhatsApp-based menu editing, or self-updating menu behavior. They now match the frozen customer service-worker contract: tenant/customer origins use `public/sw-customer.js`, navigation stays network-first with a bounded timeout, `/offline` is the only cached fallback, and menu HTML/data/images/Firestore responses are never served from a stale offline cache. Public update copy now routes owner changes through the approved source and current public cache window. `npm run verify:customer-app-pwa` now source-gates the Client Menu README, spec, implementation, marketing, website, helpdoc, and mobile-support docs against stale offline/instant-update phrases and requires the matching service-worker/offline-page evidence. Follow-up gates passed: `node --check scripts/verification/verify-customer-app-pwa.js`, `npm run verify:customer-app-pwa`, targeted active Client Menu stale offline/freshness scan, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 88/88 checks, including 84 child root `verify:*` scripts. No customer menu runtime behavior, service-worker behavior, public cache behavior, Firebase rule, Storage rule, Cloud Function, Firebase deploy, Vercel deploy, production build, browser/device QA, provider call, Firestore write, Storage write, launch approval, or release certification changed for this checkpoint.

Public menu/editor go-live copy boundary checkpoint: active Public Menu Entry, Data Editor, mobile doctrine, support templates, Client Menu, Multi-Outlet, POS webhook, Behavior Engineering, and AI Image Generation docs no longer claim saved edits go live instantly, skip publishing universally, appear immediately, or become customer-visible within seconds. They now route menu data edits through the shared `updateProject()` public-truth/cache path, keep explicit Publish actions where the UI provides them, and bound customer-menu refresh to the current 60-second public cache window. This matches the source-backed `updateProject()` cache invalidation path, explicit `publishProject()` design/page path, `menuVersion`/`lastPublishedAt` updates, desktop/mobile publish controls, review-before-publish upload copy, and client `revalidate: 60` menu metadata. `npm run verify:public-business-truth` now source-gates those active docs against the stale instant-live phrases and requires the matching save/cache/publish source evidence. Follow-up gates passed: `node --check scripts/verification/verify-public-business-truth.js`, `npm run verify:public-business-truth`, targeted active MenuList stale go-live scan, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 88/88 checks, including 84 child root `verify:*` scripts. No public menu runtime behavior, editor behavior, publish behavior, cache behavior, Firebase rule, Storage rule, Cloud Function, Firebase deploy, Vercel deploy, production build, browser/device QA, provider call, Firestore write, Storage write, launch approval, or release certification changed for this checkpoint.

Future roadmap SSOT launch-boundary checkpoint: `__docs__/strategy/menulist-future-roadmap-ssot.md` no longer presents its February 2026 roadmap/source-state snapshot as current codebase truth, production-ready status, testing approval, activation approval, or launch certification. It now identifies itself as historical roadmap/source-state evidence, points live feature-flag truth to `src/config/features.ts`, routes current readiness to this audit and the External Certification Runbook, and labels the old implemented/flag-off tables as historical source evidence rather than launch/testing approval. `npm run verify:agent-readiness` now source-gates this roadmap boundary and rejects the old production-ready heading, testing/activation heading, and ready-to-test action statuses. Follow-up gates passed: `node --check scripts/verification/verify-agent-readiness.js`, `npm run verify:agent-readiness`, targeted Future Roadmap stale readiness scan, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 88/88 checks, including 84 child root `verify:*` scripts. No runtime behavior, feature flag value, Firebase rule, Storage rule, Cloud Function, Firebase deploy, Vercel deploy, production build, browser/device QA, provider call, Firestore write, Storage write, testing approval, activation approval, launch approval, or release certification changed for this checkpoint.

Local readiness list-mode checkpoint: `npm run verify:production-readiness-local -- --list` now prints the aggregate gate inventory, check count, child `verify:*` count, and local-only proof boundary without running any child checks. This keeps the aggregate source-gate surface inspectable before a full run and prevents a no-op inventory request from accidentally launching the full suite. `npm run verify:agent-readiness` now guards the list-only mode, command output shape, README usage, and External Certification Runbook wording. Follow-up gates passed: `node --check scripts/verification/run-production-readiness-local.js`, `node --check scripts/verification/verify-agent-readiness.js`, `npm run verify:production-readiness-local -- --list`, `npm run verify:agent-readiness`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 88/88 checks, including 84 child root `verify:*` scripts. No runtime behavior, Next.js production build, Firebase deploy, Vercel deploy, provider smoke, browser/device QA, Firestore write, Storage write, launch approval, or release certification changed for this checkpoint.

Local readiness typecheck-script checkpoint: the root package now exposes `npm run typecheck` as the incremental-safe TypeScript gate (`tsc --noEmit --incremental false --pretty false`), `npm run build:verify` delegates to that script, `tsconfig.json` sends incremental build-info to ignored `.next/cache/tsconfig.tsbuildinfo`, and `npm run verify:production-readiness-local` now calls that script instead of a raw `npx tsc` invocation. This keeps the local source gate on one TypeScript command while reducing stale tracked build-info churn during production-readiness runs. `npm run verify:agent-readiness` now guards the root script contract, the `build:verify` delegation, the build-info cache location, and the aggregate runner call path. Follow-up gates passed: `node --check scripts/verification/run-production-readiness-local.js`, `node --check scripts/verification/verify-agent-readiness.js`, `npm run typecheck`, `npm run build:verify`, `npm run verify:agent-readiness`, `npm run verify:doc-npm-scripts`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 88/88 checks, including 84 child root `verify:*` scripts. No runtime behavior, Next.js production build, Firebase deploy, Vercel deploy, provider smoke, browser/device QA, Firestore write, Storage write, launch approval, or release certification changed for this checkpoint.

Social Content code-review launch-boundary checkpoint: `__docs__/social-content/social-content_code-review.md` no longer ends with a standalone production-ready signoff for the January 2026 Today Tab review. It now identifies itself as historical source-review evidence only and routes current release approval to this audit, the External Certification Runbook, `npm run verify:agent-readiness`, `npm run verify:public-business-truth`, Today desktop/mobile/browser QA, campaign AI/provider smoke where enabled, target deploy evidence, and production-host smoke. `npm run verify:agent-readiness` now reads the Social Content code-review doc and rejects the old production-ready signoff. Follow-up gates passed: `node --check scripts/verification/verify-agent-readiness.js`, `npm run verify:agent-readiness`, `npm run verify:public-business-truth`, targeted Social Content code-review stale readiness scan, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 88/88 checks, including 84 child root `verify:*` scripts. No Social Content runtime behavior, Today behavior, GrowthOS summary reads, campaign provider behavior, Firebase rule, Storage rule, Cloud Function, Firebase deploy, Vercel deploy, production build, provider smoke, browser/device QA, Firestore write, Storage write, launch approval, or release certification changed for this checkpoint.

Help Chat source-doc boundary checkpoint: `src/components/templates/main-app/helpChat/README.md` and `src/components/templates/main-app/helpChat/IMPLEMENTATION_SUMMARY.md` no longer present the Help Chat UI slice as current production approval or user-testing signoff while backend integration remains separately listed. The README now identifies Help Chat as a source-gated owner help interface, the summary identifies itself as a source-gated UI slice, and both route backend integration, provider behavior, browser/device QA, launch certification, target deploy evidence, and production-host smoke through the active Help Center verifier, this audit, and the External Certification Runbook. `npm run verify:help-center-boundary` now source-gates both source-adjacent docs and rejects the old production-ready and ready-for-user-testing phrases. Follow-up gates passed: `node --check scripts/verification/verify-help-center-boundary.js`, `npm run verify:help-center-boundary`, targeted Help Chat source-doc readiness wording scan, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 88/88 checks, including 84 child root `verify:*` scripts. No Help Chat runtime behavior, Help Center search behavior, Answerlattice support behavior, mobile help behavior, ticket behavior, public route behavior, Firestore rule, Storage rule, Cloud Function, Firebase deploy, Vercel deploy, production build, browser/device QA, provider call, Firestore write, Storage write, launch approval, or release certification changed for this checkpoint.

SignalDesk feedback-boundary wording checkpoint: `__docs__/menulist-signaldesk/menulist-signaldesk_feature-map.md` and `__docs__/menulist-signaldesk/menulist-signaldesk_chatgpt-feedback-review-2026-06-24.md` no longer describe ChatGPT feedback as proof of SignalDesk readiness. They now preserve the same operating-layer roadmap boundary while saying the feedback is not SignalDesk launch certification. `npm run verify:signaldesk` now source-gates both active feedback-boundary docs and rejects the old proof-of-readiness phrase. Follow-up gates passed: `node --check scripts/verification/verify-signaldesk-runtime.js`, `npm run verify:signaldesk`, targeted active SignalDesk readiness wording scan, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 88/88 checks, including 84 child root `verify:*` scripts. No SignalDesk runtime behavior, provider-send behavior, paid campaign behavior, public route behavior, MenuList truth-write behavior, Firestore rule, Storage rule, Cloud Function, Firebase deploy, Vercel deploy, production build, browser/device QA, provider call, Firestore write, Storage write, launch approval, or release certification changed for this checkpoint.

Customer App production-signoff wording checkpoint: `__docs__/customer-app/customer-app_test.md` no longer uses direct "production-ready" wording for the freeze gate, KPI truth table, or analytics signoff rule. The active checklist now says the feature must clear the 3-year freeze production gate, each KPI must clear production analytics signoff, and analytics must clear production signoff only after the listed real-device, event-write, rollup, dashboard, deploy, and eligibility checks pass. `npm run verify:customer-app-pwa` now source-gates those checklist phrases and rejects the old production-ready phrases. Follow-up gates passed: `node --check scripts/verification/verify-customer-app-pwa.js`, `npm run verify:customer-app-pwa`, targeted Customer App test readiness wording scan, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 88/88 checks, including 84 child root `verify:*` scripts. No Customer App runtime behavior, PWA behavior, analytics behavior, dashboard behavior, public route behavior, customer/menu output behavior, Firestore rule, Storage rule, Cloud Function, Firebase deploy, Vercel deploy, production build, browser/device QA, provider call, Firestore write, Storage write, launch approval, or release certification changed for this checkpoint.

AI Enhancement Pack billing-copy readiness checkpoint: `__docs__/ai-enhancement-packs/ai-billing-explainer.md` and `__docs__/ai-enhancement-packs/ai-enhancement-packs_marketing.md` no longer use "ready to use" wording for subscription credit initialization or post-purchase activation copy. The billing explainer now states that `monthlyCredits` are the full starting balance after subscription activation, and the marketing confirmation copy states that additional AI capacity is available after the pack is activated. `npm run verify:billing-entitlement-boundary` now reads the billing explainer, source-gates the activation timing wording, source-gates the marketing activation copy, and rejects the old ready-to-use phrases. Follow-up gates passed: `node --check scripts/verification/verify-billing-entitlement-boundary.js`, `npm run verify:billing-entitlement-boundary`, targeted active AI Enhancement Pack ready-to-use scan, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 88/88 checks, including 84 child root `verify:*` scripts. No billing runtime behavior, Razorpay behavior, AI capacity accounting behavior, entitlement behavior, customer/menu output behavior, public route behavior, Firestore rule, Storage rule, Cloud Function, Firebase deploy, Vercel deploy, production build, browser/device QA, provider call, Firestore write, Storage write, launch approval, or release certification changed for this checkpoint.

AI Menu Manager website claim-boundary checkpoint: `__docs__/ai-menu-manager/ai-menu-manager_website.md` and `__docs__/ai-menu-manager/ai-menu-manager_spec.md` no longer use unqualified launch-readiness wording for full speech-to-command claims. They now preserve the same public-copy boundary while requiring separate launch-use verification for full speech-to-command, external posting, rule execution, provider-backed image/import/publish execution, durable rollback, and every checklist action. `npm run verify:ai-menu-manager` now source-gates the AMM website/spec claim boundary, the allowed public claim scope, and the speech-to-command verification wording. Follow-up gates passed: `node --check scripts/verification/verify-ai-menu-manager.js`, `npm run verify:ai-menu-manager`, targeted AMM readiness wording scan, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 88/88 checks, including 84 child root `verify:*` scripts. No AMM runtime behavior, AI behavior, speech input behavior, provider behavior, customer/menu output behavior, public route behavior, Firestore rule, Storage rule, Cloud Function, Firebase deploy, Vercel deploy, production build, browser/device QA, Firestore write, Storage write, launch approval, or release certification changed for this checkpoint.

Changelog historical entry boundary checkpoint: `__docs__/CHANGELOG.md` now states that chronological release entries are historical notes, not current MenuList launch approval. This keeps older "production ready" or readiness wording as historical record only while routing current production readiness to this audit, the External Certification Runbook, current source verifiers, browser/device QA, provider smoke, target deploy evidence, and production-host smoke. `npm run verify:agent-readiness` now requires that changelog-level release certification boundary. Follow-up gates passed: `node --check scripts/verification/verify-agent-readiness.js`, `npm run verify:agent-readiness`, targeted changelog release-certification boundary scan, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 88/88 checks, including 84 child root `verify:*` scripts. No product runtime behavior, public route behavior, billing behavior, AI behavior, Firestore rule, Storage rule, Cloud Function, Firebase deploy, Vercel deploy, production build, browser/device QA, provider call, Firestore write, Storage write, launch approval, or release certification changed for this checkpoint.

Main website Stage 7 output launch-boundary checkpoint: `__docs__/main-website/website-prep-codex-prompts/stage-07-output-final-launch-polish-production-readiness.md` no longer presents the May 2026 final launch polish output as current controlled-launch, founder-review, launch-good-enough, or V2 deferral approval. It now preserves historical Stage 7 planning evidence only while routing current website launch or founder-review approval to this audit, the External Certification Runbook, `npm run verify:website-public-copy-boundary`, `npm run verify:website-resource-locales`, browser desktop/mobile route smoke, supporting page claim-hardening, founder-approved demo tenant screenshots, target Vercel deploy evidence, and production-host smoke. `npm run verify:website-public-copy-boundary` now rejects the stale Stage 7 controlled-launch sentence, stale launch-good-enough wording, and stale V2 waiting label in addition to the existing main website README launch-boundary guard. Follow-up gates passed: `node --check scripts/verification/verify-website-public-copy-boundary.js`, `npm run verify:website-public-copy-boundary`, `npm run verify:website-resource-locales`, `npm run verify:agent-readiness`, targeted stale Stage 7 website launch scan, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 88/88 checks, including 84 child root `verify:*` scripts. No website runtime behavior, public route behavior, locale copy, LLM context, public image asset, analytics behavior, Firebase rule, Storage rule, Cloud Function, Firebase deploy, Vercel deploy, production build, browser/mobile route smoke, founder screenshot approval, Firestore write, Storage write, launch approval, founder-review approval, or release certification changed for this checkpoint.

GBP critical review implementation-boundary checkpoint: `__docs__/gbp-sync/GBP-CHATGPT-CRITICAL-REVIEW.md` no longer presents the January 2026 ChatGPT review as ready to implement, ready to proceed, or current GBP launch approval. It now preserves historical external-suggestion analysis only while routing current GBP Sync implementation or release approval to this audit, the External Certification Runbook, `npm run verify:public-business-truth`, Google Business Profile API access, OAuth and secret setup, provider smoke, scoped deploy evidence where routes/rules/functions change, browser/device QA, and production-host smoke. `npm run verify:public-business-truth` now source-gates this companion review alongside the active GBP docs and rejects the stale ready-to-implement heading, stale implementation-ready verdict row, stale ready-to-proceed prerequisite note, and stale schema/DAL dev-action instruction. Follow-up gates passed: `node --check scripts/verification/verify-public-business-truth.js`, `npm run verify:public-business-truth`, targeted stale GBP critical-review implementation scan, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 88/88 checks, including 84 child root `verify:*` scripts. No GBP runtime behavior, Google provider behavior, token storage behavior, Integrations tab runtime behavior, Firestore rule, Storage rule, Cloud Function, Firebase deploy, Vercel deploy, production build, Google API call, browser/device QA, Firestore write, Storage write, implementation approval, launch approval, or release certification changed for this checkpoint.

Upload cross-check testing-phase boundary checkpoint: `__docs__/projects/development_done/1-CROSS-CHECK-UPLOAD.md` no longer presents the November 2025 upload cross-check confidence note as ready for testing. It now preserves historical upload cross-check evidence only while routing current upload testing, deploy, or release approval to this audit, the External Certification Runbook, `npm run verify:agent-readiness`, `npm run verify:menu-extraction-pipeline`, upload/browser/mobile/Storage results, browser/mobile upload QA, Storage quota/rules evidence, target deploy evidence where upload or Storage behavior changes, and production-host smoke. `npm run verify:agent-readiness` now rejects the stale ready-for-testing-phase confidence line in addition to the existing Projects upload implementation and testing guide boundaries. Follow-up gates passed: `node --check scripts/verification/verify-agent-readiness.js`, `npm run verify:agent-readiness`, `npm run verify:menu-extraction-pipeline`, targeted stale upload cross-check scan, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 88/88 checks, including 84 child root `verify:*` scripts. No upload runtime behavior, file validation behavior, PDF processing behavior, project editor behavior, Storage rule, Firestore rule, Cloud Function, Firebase deploy, Vercel deploy, production build, browser/mobile QA, Storage write, Firestore write, testing approval, deploy approval, or release certification changed for this checkpoint.

MOL v0 footer testing/deploy boundary checkpoint: `__docs__/internal-tracking/mol-v0-implementation-plan.md` no longer presents the historical Menu Observation Layer Sprint 1/2 implementation note as ready for testing and deployment. It now preserves source evidence only while routing current MOL testing, scheduler deploy, or release approval to this audit, the External Certification Runbook, `npm run verify:agent-readiness`, `npm run verify:functions-deploy-preflight`, Firestore/cost evidence, scoped `menulist-qa` scheduler deploy evidence where Function logic changes, target-environment smoke, and explicit production deploy approval. `npm run verify:agent-readiness` now rejects the stale footer in addition to the existing MOL source-gate and scoped deploy-command checks. Follow-up gates passed: `node --check scripts/verification/verify-agent-readiness.js`, `npm run verify:agent-readiness`, `npm run verify:functions-deploy-preflight`, targeted stale MOL footer scan, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 88/88 checks, including 84 child root `verify:*` scripts. No MOL runtime behavior, menu change log behavior, scheduler behavior, Firestore rule, Storage rule, Cloud Function, Firebase deploy, Vercel deploy, production build, browser/API smoke, Firestore write, Storage write, testing approval, deploy approval, or release certification changed for this checkpoint.

Payment security analysis boundary checkpoint: `__docs__/security/payment-security/payment-security-analysis.md` no longer presents the November 2025 payment/onboarding security analysis as ready for implementation, ready for testing, or current payment launch approval. It now preserves the historical security analysis and source-evidence context only while routing current payment/onboarding release approval to this audit, the External Certification Runbook, `npm run verify:agent-readiness`, `npm run verify:billing-entitlement-boundary`, `npm run verify:auth-security-failure-matrix`, authenticated onboarding browser/API smoke, Razorpay sandbox subscription/top-up/webhook smoke, Firebase Auth custom-claims/token smoke where onboarding is in scope, provider-failure compensation evidence for tenant/store creation and subscription creation, target deploy evidence where auth/payment/rules/functions/routes change, and production-host smoke. `npm run verify:agent-readiness` now rejects the stale ready-for-implementation status, stale Phase 2 proceed instruction, and stale Phase 1 ready-for-testing footer while checking the current server-owned onboarding and billing source-gate boundary. Follow-up gates passed: `node --check scripts/verification/verify-agent-readiness.js`, `npm run verify:agent-readiness`, `npm run verify:billing-entitlement-boundary`, `npm run verify:auth-security-failure-matrix`, targeted stale payment-security status scan, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 88/88 checks, including 84 child root `verify:*` scripts. No auth runtime behavior, onboarding runtime behavior, Razorpay runtime behavior, billing entitlement behavior, Firestore rule, Storage rule, Cloud Function, Firebase deploy, Vercel deploy, production build, browser/API smoke, provider smoke, Firebase Auth token smoke, Firestore write, Storage write, or release certification changed for this checkpoint.

Main website README launch-boundary checkpoint: `__docs__/main-website/README.md` no longer presents Stage 7 homepage evidence as ready for controlled launch or founder review. It now preserves the local source-gated homepage evidence only while routing current website launch or founder-review approval to this audit, the External Certification Runbook, `npm run verify:website-public-copy-boundary`, `npm run verify:website-resource-locales`, browser desktop/mobile route smoke, supporting page claim-hardening, founder-approved demo tenant screenshots, target Vercel deploy evidence, and production-host smoke. `npm run verify:website-public-copy-boundary` now rejects the stale controlled-launch sentence in addition to the mounted homepage copy, Website locale namespace, LLM context, and public-copy claim boundaries. Follow-up gates passed: `node --check scripts/verification/verify-website-public-copy-boundary.js`, `npm run verify:website-public-copy-boundary`, `npm run verify:website-resource-locales`, `npm run verify:agent-readiness`, targeted stale website controlled-launch scan, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 88/88 checks, including 84 child root `verify:*` scripts. No website runtime behavior, public route behavior, locale copy, LLM context, public image asset, analytics behavior, Firebase rule, Storage rule, Cloud Function, Firebase deploy, Vercel deploy, production build, browser/mobile route smoke, founder screenshot approval, Firestore write, Storage write, or release certification changed for this checkpoint.

Multi-Outlet companion footer-boundary checkpoint: `__docs__/multi-outlet-consistency/multi-outlet-consistency_spec.md`, `multi-outlet-consistency_impl.md`, `multi-outlet-consistency_marketing.md`, `store-onboarding/store-onboarding_spec.md`, `store-onboarding/store-onboarding_impl.md`, and `store-onboarding/store-onboarding-billing_impl.md` no longer present old implementation-ready, implementation-plan-ready, analysis-complete, or marketing-collateral-ready footers as current approval. They now preserve source evidence only while routing current Multi-Outlet release, billing, sales, and publication approval to this audit, the External Certification Runbook, `npm run verify:multi-location-boundary`, desktop/mobile Locations browser QA, linked outlet save QA, Razorpay sandbox evidence where billing is involved, approved pricing/package copy where collateral is used, target deploy evidence where rules/functions/routes change, and production-host smoke. `npm run verify:multi-location-boundary` now rejects the stale companion footer labels in addition to the existing outlet lifecycle route, linked outlet save, bounded response, MobileShell, cost, and docs-parity checks. Follow-up gates passed: `node --check scripts/verification/verify-multi-location-boundary.js`, `npm run verify:multi-location-boundary`, targeted stale Multi-Outlet footer scan, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 79/79 checks, including 75 child root `verify:*` scripts. No Multi-Outlet runtime behavior, outlet lifecycle behavior, linked outlet save behavior, Razorpay behavior, collateral publication behavior, pricing/package behavior, Firestore rule, Storage rule, Cloud Function, Firebase deploy, Vercel deploy, production build, browser/mobile QA, provider smoke, Firestore write, Storage write, or release certification changed for this checkpoint.

Customer Question Coverage Check doc-set checkpoint: the rooted `verify:customer-question-coverage-check` gate no longer points at missing required docs. The required doc set now lives under `__docs__/menulist-tools/customer-question-coverage-check/`, matching the existing MenuList Tools namespace and Public Truth Tools family docs. The docs preserve the V0 runtime boundary: browser-local self-report, explicit `evidenceText`, no external URL fetch, no customer-chat read, no AI/provider call, no chatbot answer generation, no external platform update, no report storage, and optional consented follow-up only through the existing bounded public contact route. Follow-up gates passed: `npm run verify:customer-question-coverage-check`, `npm run verify:agent-readiness`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 79/79 checks, including 75 child root `verify:*` scripts. No Customer Question Coverage Check runtime behavior, website route behavior, public contact route behavior, Business Health owner module behavior, Firebase rule, Storage rule, Cloud Function, Firebase deploy, Vercel deploy, production build, browser/device QA, provider call, Firestore write, Storage write, or release certification changed for this checkpoint.

Onboarding Centralization status-boundary checkpoint: `__docs__/onboarding-centralization/README.md` no longer presents the tenant/store transaction helper as implementation-ready planning status. It now preserves implemented source evidence only while routing current onboarding release approval to this audit, the External Certification Runbook, `npm run verify:agent-readiness`, `npm run verify:auth-security-failure-matrix`, authenticated browser/API smoke for the target onboarding flow, Razorpay sandbox evidence where subscription creation is in scope, Firebase Auth custom-claims/token smoke where account/session handoff is in scope, provider-failure compensation evidence for create-subscription and public claim flows, target deploy evidence where rules/indexes/storage/functions change, target Vercel deploy evidence where app routes or middleware change, and production-host smoke for the target tenant/store creation path. `npm run verify:agent-readiness` now source-gates the doc boundary alongside the current onboarding route/helper checks. Follow-up gates passed: `node --check scripts/verification/verify-agent-readiness.js`, `npm run verify:agent-readiness`, narrowed active stale-readiness scan, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 79/79 checks, including 75 child root `verify:*` scripts. No onboarding runtime behavior, tenant/store transaction behavior, auth behavior, Razorpay behavior, public claim behavior, compensation behavior, Answerlattice onboarding behavior, Firestore rule, Storage rule, Cloud Function, Firebase deploy, Vercel deploy, production build, browser/API smoke, provider smoke, Firestore write, Storage write, or release certification changed for this checkpoint.

Compliance Pages spec launch-boundary checkpoint: `__docs__/compliance-pages/compliance-pages_spec.md` no longer presents the feature as implementation-ready planning status. It now preserves runtime source evidence only while routing current Compliance Pages release approval to this audit, the External Certification Runbook, `npm run verify:compliance-pages-boundary`, browser custom-domain smoke for `/privacy`, `/terms`, and `/refund`, authenticated desktop/mobile owner save/reset QA, owner/legal review of final generated or custom policy text, target Firebase deploy evidence where rules/indexes/storage/functions change, target Vercel deploy evidence where routes/middleware/public renderers change, and production-host smoke for the target tenant/domain. `npm run verify:compliance-pages-boundary` now source-gates the active spec boundary in addition to the sanitizer, template, API, public route, owner editor, rules, and docs parity checks. Follow-up gates passed: `node --check scripts/verification/verify-compliance-pages-boundary.js`, `npm run verify:compliance-pages-boundary`, targeted stale Compliance Pages spec-status scan, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 78/78 checks, including 74 child root `verify:*` scripts. No Compliance Pages runtime behavior, public route behavior, owner editor behavior, compliance text generation behavior, middleware behavior, Firestore rule, Storage rule, Cloud Function, Firebase deploy, Vercel deploy, production build, browser custom-domain smoke, owner/legal copy approval, Firestore write, Storage write, or release certification changed for this checkpoint.

Item Photo Capture Assist mobile-support boundary checkpoint: `__docs__/item-photo-capture-assist/item-photo-capture-assist_mobile-support.md` no longer presents mobile support as an implementation approval status. It now preserves implemented mobile support evidence only while routing current mobile release approval to this audit, the External Certification Runbook, `npm run verify:agent-readiness`, `npm run verify:auth-security-failure-matrix`, authenticated desktop owner browser QA for the image upload modal, authenticated mobile owner-shell QA inside `MobileShell`, real-device camera QA on iOS Safari and mid-range Android Chrome, media preparation/upload QA through `prepareMediaImage(file, 'menuItem')`, target deploy evidence where routes/storage rules/upload behavior change, and production-host smoke. `npm run verify:agent-readiness` now source-gates the mobile-support boundary and rejects the old approved-for-mobile-implementation status. Follow-up gates passed: `node --check scripts/verification/verify-agent-readiness.js`, `npm run verify:agent-readiness`, targeted stale Item Photo Capture Assist mobile-status scan, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 78/78 checks, including 74 child root `verify:*` scripts. No Item Photo Capture Assist runtime behavior, camera behavior, upload behavior, media preparation behavior, MobileShell behavior, route behavior, Storage rule, Firestore rule, Firebase deploy, Vercel deploy, production build, browser/device QA, Firestore write, Storage write, mobile release approval, or release certification changed for this checkpoint.

Auth guide production-active boundary checkpoint: `__docs__/auth/authentication-complete-guide.md`, `__docs__/auth/README.md`, and `__docs__/auth/firebase-auth-sync.md` no longer present auth architecture, the auth hub, or Firebase Auth sync as current "Production Active" launch approval. They now preserve source evidence only while routing current auth release approval to this audit, the External Certification Runbook, `npm run verify:agent-readiness`, `npm run verify:auth-security-failure-matrix`, auth browser/API smoke, Firebase Auth custom-claims/token smoke, App Check/session-cookie review, login/logout/OAuth/password/staff-passcode QA, target deploy evidence where auth-adjacent routes or rules change, and production-host smoke. `npm run verify:agent-readiness` now rejects the old production-active auth guide/footer labels in addition to the existing auth source-doc production-ready guards. Follow-up gates passed: `node --check scripts/verification/verify-agent-readiness.js`, `npm run verify:agent-readiness`, targeted stale auth production-active scan, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 78/78 checks, including 74 child root `verify:*` scripts. No auth runtime behavior, session behavior, Firebase Auth sync behavior, middleware, API route, Firestore rule, security logging behavior, Firebase deploy, Vercel deploy, production build, browser/API smoke, provider smoke, Firestore write, Storage write, or release certification changed for this checkpoint.

Digital Screens spec launch-boundary checkpoint: `__docs__/digital-screens/digital-screens_spec.md` no longer presents the locked screen runtime as "Production complete" or current launch certification. It now preserves source-bounded runtime evidence only while routing current Digital Screens release approval to this audit, the External Certification Runbook, `npm run verify:digital-screens-boundary`, browser TV smoke for Menu Board and Highlights modes, authenticated desktop/mobile owner settings QA, physical-device TV/tablet/browser QA, target Firebase deploy evidence where rules/storage/indexes/functions change, target Vercel deploy evidence where app routes or display clients change, and production-host smoke for the target tenant and screen URL. `npm run verify:digital-screens-boundary` now source-gates the active spec boundary in addition to the existing route, listener mirror, seen-signal, invalidation, owner acknowledgement, and docs-parity checks. Follow-up gates passed: `node --check scripts/verification/verify-digital-screens-boundary.js`, `npm run verify:digital-screens-boundary`, targeted stale Digital Screens spec-status scan, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 78/78 checks, including 74 child root `verify:*` scripts. No Digital Screens runtime behavior, public screen route behavior, owner settings behavior, cache invalidation behavior, Firestore rule, Storage rule, Cloud Function, Firebase deploy, Vercel deploy, production build, browser TV smoke, physical-device QA, Firestore write, Storage write, or release certification changed for this checkpoint.

Mobile, strategy, and multi-outlet reference-boundary checkpoint: `__docs__/mobile-operational-support/03-mobile-screens-spec.md`, `__docs__/mobile-operational-support/05-mobile-navigation-spec.md`, `__docs__/strategy/future-ideas-bucket-list.md`, and `__docs__/multi-outlet-consistency/multi-outlet-consistency_ai-extraction.md` no longer present mobile screen/navigation references, future idea candidates, or Multi-Outlet AI extraction analysis as current implementation approval, launch approval, or production certification. Mobile screen and navigation approval now routes to this audit, the External Certification Runbook, `npm run verify:mobile-shell-route-map`, relevant feature-specific mobile/source gates, authenticated owner-shell mobile QA, real-device QA where camera/share/PWA behavior is in scope, target deploy evidence, and production-host smoke. Future strategy candidates now stay historical until a current feature doc/source gate exists. Multi-Outlet AI extraction approval routes to this audit, the External Certification Runbook, `npm run verify:multi-location-boundary`, `npm run verify:menu-extraction-pipeline`, linked outlet extraction QA, provider smoke, deploy evidence where rules/functions change, and target-environment smoke. Follow-up gates passed: `node --check scripts/verification/verify-mobile-shell-route-map.js`, `node --check scripts/verification/verify-multi-location-boundary.js`, `node --check scripts/verification/verify-agent-readiness.js`, `npm run verify:mobile-shell-route-map`, `npm run verify:multi-location-boundary`, `npm run verify:agent-readiness`, targeted stale reference-readiness scan, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 78/78 checks, including 74 child root `verify:*` scripts. No mobile runtime behavior, MobileShell behavior, multi-outlet runtime behavior, extraction runtime behavior, strategy/runtime scope, Firebase rules, Cloud Functions, Firebase deploy, Vercel deploy, production build, browser/device QA, provider smoke, Firestore write, Storage write, implementation approval, launch approval, or release certification changed for this checkpoint.

Owner Notifications doc-boundary checkpoint: `__docs__/owner-notifications/owner-notifications_spec.md`, `owner-notifications_mobile-support.md`, and `owner-notifications_helpdoc.md` no longer present the owner-notification architecture, mobile setup/status scope, or owner help draft as implementation-planning status, future-implementation help, or current launch/support-publication approval. They now preserve source evidence only while routing current owner-notification release or publication approval to this audit, the External Certification Runbook, `npm run verify:owner-notifications-boundary`, SMTP/WhatsApp provider smoke where enabled, authenticated owner settings/status QA for the target owner surface, platform recovery monitor browser QA, target Firebase deploy evidence where Functions logic changes, target Vercel deploy evidence where app routes change, and production-host smoke. `npm run verify:owner-notifications-boundary` now source-gates the spec/mobile/help boundaries in addition to the existing registry/API/processor/monitor/docs gate and rejects the old planning/help status lines. Follow-up gates passed: `node --check scripts/verification/verify-owner-notifications-boundary.js`, `npm run verify:owner-notifications-boundary`, targeted stale Owner Notifications doc-status scan, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 78/78 checks, including 74 child root `verify:*` scripts. No owner-notification runtime behavior, recipient resolution behavior, SMTP behavior, WhatsApp behavior, platform recovery monitor runtime behavior, owner settings UI, Firebase rules, Cloud Functions, Firebase deploy, Vercel deploy, production build, provider smoke, browser/mobile QA, Firestore write, Storage write, support-publication approval, or release certification changed for this checkpoint.

Owner Business Health boundary checkpoint: active Business Health docs now match the current source truth that `/business-health` is a gated read-only owner page, `/api/owner-business-assistant/*` routes enforce feature flags, bounded admission, selected-store scope, `VIEW_ANALYTICS` permissions, and rate limits, answer requests use a 32KB body cap with deterministic/provider-backed answer boundaries, feedback uses an 8KB body cap, browser hooks parse read models through bounded response helpers, MobileShell Business Health remains read-only, and removed Business Health action surfaces stay absent. `npm run verify:owner-business-health-boundary` now source-gates the route, desktop page, mobile screen, MobileShell mapping, current/analytics/locations/answer/feedback routes, client response helpers, schemas, context packets, domain capability matrix, thread store, platform monitor, docs parity, and ledger coverage. Follow-up gates passed: `npm run verify:owner-business-health-boundary` and `npm run verify:production-readiness-local` with 78/78 checks, including 74 child root `verify:*` scripts. No Business Health runtime behavior, answer behavior, scheduler behavior, public truth behavior, project/menu/store write behavior, Firebase rules, Cloud Functions, Firebase deploy, Vercel deploy, production build, browser/mobile QA, provider smoke, live Firestore write, Storage write, or release certification changed for this checkpoint.

Menu Card Export collateral boundary checkpoint: `__docs__/menu-card-export/menu-card-export_helpdoc.md`, `menu-card-export_marketing.md`, and `menu-card-export_website.md` no longer present help, support, founder-demo, sales, or website-copy notes as current collateral approval. They now preserve source evidence only while routing current collateral approval to this audit, the External Certification Runbook, Digital Menu Output Constitution checks, `npm run verify:menu-card-export`, authenticated desktop/mobile browser QA, visual PDF and print-shop artifact review, provider smoke for the AI advisor where enabled, target deploy evidence, and production-host smoke. `npm run verify:menu-card-export` now source-gates these active collateral docs in addition to the core/validation/test-case launch boundaries and rejects the old ready-for-support, ready-for-founder-demo, and active-publication status lines. Follow-up gates passed: `node --check scripts/verification/verify-menu-card-export.js`, `npm run verify:menu-card-export`, targeted stale Menu Card Export collateral status scan, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 77/77 checks, including 73 child root `verify:*` scripts. No Menu Card Export runtime behavior, public website runtime behavior, support workflow, sales/demo approval, PDF renderer behavior, print-shop packet behavior, AI advisor behavior, mobile shell behavior, Firebase rules, Cloud Functions, Firebase deploy, Vercel deploy, production build, provider smoke, browser/device QA, Firestore write, Storage write, collateral publication approval, or release certification changed for this checkpoint.

PONR, Physical Surfaces, and Image Editing historical-readiness checkpoint: `__docs__/onboarding/ponr-onboarding_spec.md`, `__docs__/physical-surfaces/physical-surfaces_spec.md`, and `__docs__/projects/Assessments/assessment-08-image-editing.md` no longer present January/November 2025-2026 planning and assessment labels as current implementation approval, deploy approval, production-ready approval, or launch certification. PONR now stays a historical strategy draft until a current implementation document, onboarding/auth/payment source gates, authenticated desktop/mobile onboarding QA, public-surface cache/deploy evidence where relevant, target deploy evidence, and production-host smoke exist. Physical Surfaces now stays a historical legacy spec superseded by Menu Kit/Menu Card Export until Digital Menu Output Constitution checks, `npm run verify:menu-card-export`, browser/mobile output QA, visual print artifact review, target deploy evidence, and production-host smoke exist. Image Editing now preserves its November 2025 assessment grade as historical evidence only and routes current approval to browser/mobile editor QA, Storage rules/deploy evidence, image provider smoke where enabled, target-environment smoke, this audit, and External Certification Runbook evidence. `npm run verify:agent-readiness` now rejects the stale implementation-ready and production-ready labels for these docs. Follow-up gates passed: `node --check scripts/verification/verify-agent-readiness.js`, targeted stale-readiness scan over the three docs, `npm run verify:agent-readiness`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 77/77 checks, including 73 child root `verify:*` scripts. No onboarding runtime behavior, auth/payment behavior, public-surface cache behavior, Physical Surfaces runtime behavior, Menu Kit runtime behavior, print renderer behavior, image upload/editing runtime behavior, Storage rule, Firestore rule, Cloud Function, Firebase deploy, Vercel deploy, production build, browser/device QA, Firestore write, Storage write, implementation approval, deploy approval, or release certification changed for this checkpoint.

Menu Card Export core-doc launch-boundary checkpoint: `__docs__/menu-card-export/README.md`, `menu-card-export_spec.md`, `menu-card-export_impl.md`, and `menu-card-export_mobile-support.md` no longer present the routed owner workflow, implementation plan, or mobile route evidence as production-ready status. They now preserve source/runtime evidence only while routing current Menu Card Export approval to this audit, the External Certification Runbook, Digital Menu Output Constitution checks, `npm run verify:menu-card-export`, authenticated desktop/mobile browser QA, visual PDF and print-shop artifact review, provider smoke for the AI advisor where enabled, target deploy evidence, and production-host smoke. `npm run verify:menu-card-export` now source-gates those four core docs in addition to the existing validation/test-case launch boundaries and rejects the old production-ready status lines. Follow-up gates passed: `node --check scripts/verification/verify-menu-card-export.js`, `npm run verify:menu-card-export`, targeted stale Menu Card Export core status scan, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 77/77 checks, including 73 child root `verify:*` scripts. No Menu Card Export runtime behavior, PDF renderer behavior, print-shop packet behavior, AI advisor behavior, mobile shell behavior, Firebase rules, Cloud Functions, Firebase deploy, Vercel deploy, production build, provider smoke, browser/device QA, Firestore write, Storage write, or release certification changed for this checkpoint.

Projects marketing collateral boundary checkpoint: `__docs__/projects/ai-data-extraction/ai-data-extraction_marketing.md`, `__docs__/projects/upload-file-processing/upload-file-processing_marketing.md`, `__docs__/projects/description-generation/description-generation_marketing.md`, and `__docs__/projects/multi-language-translation/multi-language-translation_marketing.md` no longer present January/February 2026 positioning drafts as ready for use, current sales approval, publication approval, or launch certification. They now preserve historical marketing context while routing current collateral approval to this audit, the External Certification Runbook, `npm run verify:menu-extraction-pipeline`, `npm run verify:agent-readiness`, `npm run verify:ai-accounting` where relevant, target feature-flag/provider review, provider smoke, authenticated desktop/mobile upload, extraction-review, editor, and translated-menu QA, public renderer fallback/RTL evidence, customer-menu browser/device QA for translated output, Storage quota/rules/deploy evidence, target deploy evidence, production-host smoke, and release-specific evidence for numeric speed, accuracy, language-count, quality, or customer-behavior claims. `npm run verify:agent-readiness` now rejects the stale ready-for-use footers and source-gates these marketing boundaries. Follow-up gates passed: `node --check scripts/verification/verify-agent-readiness.js`, targeted ready-for-use scan over the four marketing docs, `npm run verify:agent-readiness`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 77/77 checks, including 73 child root `verify:*` scripts. No upload runtime behavior, extraction runtime behavior, description-generation runtime behavior, translation runtime behavior, provider behavior, public renderer behavior, customer-menu behavior, Storage rule, Firestore rule, Cloud Function, Firebase deploy, Vercel deploy, production build, browser/device QA, Firestore write, Storage write, publication approval, sales approval, or release certification changed for this checkpoint.

Menu Project Editor boundary checkpoint: active Projects editor and Menu Command Center docs now match the current source truth that `/projects` renders the Projects template, desktop editor saves route through `syncChanges()` and `updateProject()`, public-output project writes revalidate the public client cache and touch Digital Screens content version through the project DAL, publish increments `menuVersion` and sets `lastPublishedAt`, mobile menu persistence uses `updateProjectWithoutLoader` with `assertProjectUpdateSucceeded`, and mobile project selector/bulk action mutations require explicit acknowledgements. `npm run verify:menu-project-editor-boundary` now source-gates `/projects`, desktop Projects page mutations, desktop Editor persistence, Command Center handoff, project DAL update/publish/cache behavior, public cache helper behavior, Digital Screens invalidation, MobileMenu, MobileProjectSelectorSheet, BulkActionsSheet, docs parity, and ledger coverage. Follow-up gates passed: `npm run verify:menu-project-editor-boundary` and `npm run verify:production-readiness-local` with 77/77 checks, including 73 child root `verify:*` scripts. No project-editor runtime behavior, menu edit behavior, publish behavior, public menu rendering behavior, project DAL behavior, Firebase rules, Cloud Functions, Firebase deploy, Vercel deploy, production build, desktop/mobile browser editor QA, live Firestore write, Storage write, or release certification changed for this checkpoint.

Owner Dashboard Today boundary checkpoint: active owner-dashboard and mobile-support docs now match the current source truth that the dashboard is Today-first, desktop/mobile dashboard callers lazy-load settled historical data through `loadHistorical: showHistorical`, `/today/history` remains disabled unless `ENABLE_PAST_ACTIVITY_HISTORY` is enabled, and campaign completion/skip flows require shaped acknowledgements before local Today state advances. `npm run verify:owner-dashboard-today-boundary` now source-gates `/dashboard`, `/today`, `/today/history`, `useOwnerDashboard()`, `useOBPDashboard()`, desktop OwnerDashboard, desktop Today, MobileShell route parity, MobileHours, MobileDashboard, MobileTodayHistory, Past Activity filtering, docs parity, and ledger coverage. Follow-up gates passed: `npm run verify:owner-dashboard-today-boundary` and `npm run verify:production-readiness-local` with 76/76 checks, including 72 child root `verify:*` scripts. No owner-dashboard runtime behavior, Today campaign behavior, analytics read model behavior, Firebase rules, Cloud Functions, Firebase deploy, Vercel deploy, production build, desktop/mobile browser QA, live Firestore write, Storage write, or release certification changed for this checkpoint.

Decision Intelligence and Staff Prompt logic-verification launch-boundary checkpoint: `__docs__/decision-intelligence/decision-intelligence_logic-verification.md` and `__docs__/staff-prompt/staff-prompt_logic-verification.md` no longer present January/May 2026 source checks as current release approval, deployable status, production-readiness certification, or zero-issue launch verdicts. They now preserve historical source evidence only. Decision Intelligence current approval routes to this audit, the External Certification Runbook, `npm run verify:agent-readiness`, `npm run verify:functions-deploy-preflight`, scoped scheduler deploy evidence, current decision-block scoring checks, browser/mobile customer-menu QA, public cache/deploy evidence, target deploy evidence, and production-host smoke. Staff Prompt current approval routes to this audit, the External Certification Runbook, `npm run verify:staff-prompt-runtime`, authenticated desktop/mobile Today QA with an eligible target-store `staffPrompt`, target deploy evidence, production-host smoke, and upstream summary-writer evidence if release scope claims generated prompts end-to-end. Follow-up gates passed: `node --check scripts/verification/verify-agent-readiness.js`, `node --check scripts/verification/verify-staff-prompt-runtime.js`, `npm run verify:agent-readiness`, and `npm run verify:staff-prompt-runtime`. No Decision Intelligence runtime behavior, Staff Prompt runtime behavior, decision-block scoring behavior, Today/mobile rendering behavior, upstream summary writer, scheduler behavior, Firebase rules, Cloud Functions, Firebase deploy, Vercel deploy, production build, provider smoke, browser/device QA, Firestore write, Storage write, or release certification changed for this checkpoint.

CMI and Physical Surfaces logic-verification launch-boundary checkpoint: `__docs__/continuous-menu-intelligence/continuous-menu-intelligence_logic-verification.md` and `__docs__/physical-surfaces/physical-surfaces_logic-verification.md` no longer present January 2026 source checks as current release approval, deployable status, production-readiness certification, or zero-issue launch verdicts. They now preserve historical source evidence only. CMI current approval routes to this audit, the External Certification Runbook, `npm run verify:agent-readiness`, `npm run verify:functions-deploy-preflight`, scoped `menulist-qa` deploy evidence where Cloud Functions logic changes, current scheduler behavior checks, browser/device QA where CMI surfaces are in scope, target deploy evidence, and production-host smoke. Active physical/print output approval routes to this audit, the External Certification Runbook, `npm run verify:menu-card-export`, Digital Menu Output Constitution checks, browser/mobile output QA, visual print artifact review, target deploy evidence, and production-host smoke. Follow-up gates passed: `node --check scripts/verification/verify-agent-readiness.js`, `node --check scripts/verification/verify-menu-card-export.js`, `npm run verify:agent-readiness`, `npm run verify:menu-card-export`, and a targeted CMI/Physical Surfaces stale logic-verification scan. No CMI runtime behavior, Physical Surfaces runtime behavior, Menu Kit runtime behavior, Today/mobile output behavior, print renderer behavior, scheduler behavior, Firebase rules, Cloud Functions, Firebase deploy, Vercel deploy, production build, provider smoke, browser/device QA, Firestore write, Storage write, or release certification changed for this checkpoint.

Item Photo Capture Assist validation boundary checkpoint: `__docs__/item-photo-capture-assist/item-photo-capture-assist_validation.md` no longer presents its June 25, 2026 implementation validation as current owner-side browser-smoke clearance. The validation report now preserves historical source evidence while routing current approval to this audit, the External Certification Runbook, `npm run verify:agent-readiness`, `npm run verify:auth-security-failure-matrix`, authenticated desktop owner browser QA, authenticated mobile owner-shell QA inside `MobileShell`, real-device camera QA on iOS Safari and mid-range Android Chrome, media preparation/upload QA through `prepareMediaImage(file, 'menuItem')`, target deploy evidence, and production-host smoke. `npm run verify:agent-readiness` now rejects the stale owner-side browser-smoke verdict and source-gates the current release boundary. Follow-up gates passed: `node --check scripts/verification/verify-agent-readiness.js` and `npm run verify:agent-readiness`. No Item Photo Capture Assist runtime behavior, camera behavior, media preparation behavior, upload behavior, project save behavior, Storage path, Firestore rule, Cloud Function, Firebase deploy, Vercel deploy, production build, browser/device QA, live Firestore write, Storage write, or release certification changed for this checkpoint.

AI Enhancement Pack public-copy boundary checkpoint: `__docs__/ai-enhancement-packs/ai-enhancement-packs_helpdoc.md`, `ai-enhancement-packs_website.md`, and `ai-enhancement-packs_marketing.md` no longer present February 2026 customer copy as ready for use or current launch certification. They now route publication and sales use to this audit, the External Certification Runbook, `npm run verify:billing-entitlement-boundary`, Razorpay sandbox top-up smoke, desktop/mobile Billing browser QA, website/pricing copy review, target deploy evidence, and production-host smoke. Draft pricing must come from an approved launch pricing source, and activation copy is bounded to payment-confirmed availability. `npm run verify:billing-entitlement-boundary` now source-gates those help/website/marketing boundaries in addition to the existing AI Enhancement Pack spec/implementation billing evidence. Follow-up gates passed: `node --check scripts/verification/verify-billing-entitlement-boundary.js` and `npm run verify:billing-entitlement-boundary`. No AI Enhancement Pack runtime behavior, billing runtime behavior, Razorpay route behavior, provider behavior, Firebase rules, Cloud Functions, Firebase deploy, Vercel deploy, production build, browser/device QA, Firestore write, Storage write, or release certification changed for this checkpoint.

Menu Design Presentation boundary checkpoint: active B2C/customer-menu docs now route release approval through this audit, the External Certification Runbook, Digital Menu Output Constitution checks, `npm run verify:menu-design-presentation-boundary`, public cache/deploy evidence, browser/mobile customer-menu QA, and target production smoke. The boundary source gate checks controlled owner-selectable moods/layouts, desktop/mobile design publish acknowledgement, public menu output formatting, `verifyMenuPublish` fail-soft behavior, public cache invalidation, and B2C doc parity. Follow-up gates passed: `npm run verify:menu-design-presentation-boundary` and `npm run verify:production-readiness-local` with 75/75 checks, including 71 child root `verify:*` scripts. No B2C runtime behavior, customer menu rendering behavior, project publish behavior, Firebase rules, Cloud Functions, Firebase deploy, Vercel deploy, production build, browser/device QA, live Firestore write, Storage write, or release certification changed for this checkpoint.

Social Content strategy and logic-verification launch-boundary checkpoint: `__docs__/social-content/social-content-product-strategy.md` and `social-content_logic-verification.md` no longer present January 2026 strategy or logic evidence as locked implementation approval, deployable release approval, or zero-issue certification. They now preserve historical source evidence while routing current approval to this audit, the External Certification Runbook, `npm run verify:agent-readiness`, `npm run verify:public-business-truth`, Today desktop/mobile/browser QA, campaign AI/provider smoke where enabled, target deploy evidence, and production-host smoke. `npm run verify:agent-readiness` now source-gates the strategy and logic-verification docs in addition to the existing Social Content implementation and validation boundaries. Follow-up gates passed: `node --check scripts/verification/verify-agent-readiness.js`, `npm run verify:agent-readiness`, `npm run verify:public-business-truth`, targeted Social Content stale strategy/logic launch scan, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 75/75 checks, including 71 child root `verify:*` scripts. No Social Content runtime behavior, Today behavior, GrowthOS summary reads, campaign provider behavior, Firebase rules, Cloud Functions, Firebase deploy, Vercel deploy, production build, provider smoke, browser/device QA, Firestore write, Storage write, or release certification changed for this checkpoint.

Staff Prompt active-doc runtime-boundary checkpoint: `__docs__/staff-prompt/staff-prompt_spec.md`, `staff-prompt_impl.md`, and `staff-prompt_marketing.md` no longer present January 2026 planning and positioning notes as current implementation approval, dev-ready work, sales-team approval, standalone product scope, or staff/customer behavior guarantees. They now preserve historical evidence while routing current release approval to this audit, the External Certification Runbook, `npm run verify:staff-prompt-runtime`, authenticated desktop/mobile Today QA with an eligible target-store `staffPrompt`, target deploy evidence, and production-host smoke. If release or sales scope claims generated prompts end-to-end, upstream summary-writer evidence for `platformSummary/campaigns_{sId}.staffPrompt` is also required. `npm run verify:staff-prompt-runtime` now source-gates the spec, implementation plan, and marketing docs in addition to the existing read-only Today runtime, README, Firebase, website, mobile, validation/code-review, audit, readiness, and changelog boundaries. Follow-up gates passed: `node --check scripts/verification/verify-staff-prompt-runtime.js`, `npm run verify:staff-prompt-runtime`, targeted Staff Prompt active-doc stale implementation/sales scan, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 74/74 checks, including 70 child root `verify:*` scripts. No Staff Prompt runtime behavior, Today/mobile rendering behavior, upstream summary writer, Firebase rules, Cloud Functions, Firebase deploy, Vercel deploy, production build, provider smoke, browser/device QA, Firestore write, Storage write, or release certification changed for this checkpoint.

Menu Correctness Engine public-copy boundary checkpoint: active MCE website, help, and marketing docs now describe source-gated runtime evidence rather than current launch certification. They route approval to this audit, the External Certification Runbook, `npm run verify:public-business-truth`, browser/mobile save and publish-gate QA, customer-facing surface smoke for validated project data, public menu/device QA where copy references QR menus or screens, PDF artifact review where PDF copy is used, POS/provider smoke where POS copy is used, target deploy evidence, and production-host smoke. The docs no longer claim instant all-surface updates, universal surface certification, automatic POS/PDF/device behavior, or blanket "correct everywhere" release guarantees. `npm run verify:public-business-truth` now source-gates those website/help/marketing boundaries in addition to the existing MCE runtime flag, `updateProject()` hook, same-write `_mce` metadata path, README, implementation, and spec boundary. Follow-up gates passed: `node --check scripts/verification/verify-public-business-truth.js`, `npm run verify:public-business-truth`, targeted MCE stale public-copy scan, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 74/74 checks, including 70 child root `verify:*` scripts. No MCE runtime behavior, project save behavior, publish-gate behavior, public/customer rendering behavior, public cache behavior, POS/provider behavior, PDF generation, Firebase rules, Cloud Functions, Firebase deploy, Vercel deploy, production build, browser/device QA, live Firestore write, Storage write, or release certification changed for this checkpoint.

Pricing Integrity current-runtime public-claim checkpoint: active `__docs__/pricing-integrity-system/` docs now match source truth that project saves run through `updateProject()`, public menu/OBP cache revalidation and configured Digital Screens content-version touches are the current source-backed propagation paths, PDF downloads are generated on demand from current menu data, `runPricingIntegrity()` is dormant scaffold with no current caller, and `ENABLE_BACKGROUND_PDF_REGEN` is false. Website/help/marketing/mobile/spec/implementation/Firebase/validation docs no longer claim automatic background PDF regeneration, all-surface certification, ready-for-implementation posture, or a wired standalone Pricing Integrity engine. `npm run verify:pricing-integrity-boundary` and `npm run verify:agent-readiness` now source-gate the current save/cache/screen/PDF path and reject stale Pricing Integrity active-doc claims. Follow-up gates passed: `node --check scripts/verification/verify-pricing-integrity-boundary.js`, `npm run verify:pricing-integrity-boundary`, `node --check scripts/verification/verify-agent-readiness.js`, `npm run verify:env-targets`, `npm run verify:agent-readiness`, `npm run verify:menulist-api-tenant-safety`, targeted Pricing Integrity stale readiness/automatic-PDF scan, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 74/74 checks, including 70 child root `verify:*` scripts. No Pricing Integrity runtime behavior, editor save behavior, PDF queue behavior, public menu output, cache behavior, Digital Screens behavior, Firestore rules, Cloud Functions, Firebase deploy, Vercel deploy, production build, browser/device QA, live Firestore write, Storage write, or release certification changed for this checkpoint.

GBP Sync disabled-runtime public-claim checkpoint: active `__docs__/gbp-sync/` docs now match source truth that `ENABLE_GBP_SYNC` is false, token operations fail closed with `GBP_TOKEN_STORE_DISABLED`, current Integrations tab exposes Google Business Profile controls only behind `gbpEnabled`, no Google OAuth/callback/connect/disconnect/apply-hours route or GBP sync worker is active, and current owner behavior is manual Google handoff through OBP/menu links. Public/help/marketing docs no longer claim automatic Google updates, connect-once setup, one-click Google fixes, or launch/testing readiness. `npm run verify:public-business-truth` now source-gates this disabled-runtime boundary. Follow-up gates passed: `node --check scripts/verification/verify-public-business-truth.js`, `npm run verify:public-business-truth`, targeted active GBP stale automatic-sync/readiness scan, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 73/73 checks, including 69 child root `verify:*` scripts. No Google provider behavior, token storage behavior, Integrations tab runtime behavior, Firestore rules, Cloud Functions, Firebase deploy, Vercel deploy, production build, Google API call, browser/device QA, Firestore write, Storage write, or release certification changed for this checkpoint.

Google listing guide owner-copy boundary checkpoint: the active Business Settings Google Listing guide no longer tells owners that customers always see the correct menu and information on Google, and it no longer claims the manual Google edit takes less than 30 seconds or makes Google show the latest published MenuList menu/info. The current copy now says Google should point customers to the MenuList-approved page and that Google controls when profile edits appear, matching the disabled GBP runtime and manual Google handoff contract. `npm run verify:public-business-truth` now source-gates the guide against the stale Google correctness, fixed-time, and latest-menu overclaims and requires the owner-managed MenuList-approved page copy. Follow-up gates passed: `node --check scripts/verification/verify-public-business-truth.js`, `npm run verify:public-business-truth`, targeted Google listing stale-copy scan, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local`. No Google provider behavior, Google Business Profile update behavior, token storage behavior, Business Settings save behavior, public menu/OBP runtime behavior, public cache behavior, Firestore rule, Storage rule, Cloud Function logic, Firebase deploy, Vercel deploy, production build, Google API call, browser/device QA, Firestore write, Storage write, launch approval, or release certification changed for this checkpoint.

Localized OBP correctness public-copy checkpoint: active `Website.HowItWorks` locale strings no longer present the Official Business Page as "Always Correct", "validated and verified", or equivalent localized blanket correctness wording. The affected locale packs now use current-page/current-source cue wording like the bounded `en-US` and `hi-IN` source copy, and the Arabic/Spanish About and Contact public copy no longer claims the menu is always correct. `npm run verify:website-public-copy-boundary` now rejects stale localized OBP blanket-correctness claims. Follow-up gates passed: `node --check scripts/verification/verify-website-public-copy-boundary.js`, `npm run verify:website-public-copy-boundary`, targeted localized OBP stale-copy scan, locale JSON parse, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local`. No public menu/OBP runtime behavior, website routing behavior, locale loading behavior, public cache behavior, Firebase rule, Storage rule, Cloud Function logic, Firebase deploy, Vercel deploy, production build, browser/device QA, provider smoke, Firestore write, Storage write, launch approval, or release certification changed for this checkpoint.

Client menu marketing launch-boundary checkpoint: `__docs__/client-menu/_marketing.md` no longer presents customer-facing digital menu sales collateral as ready for use. The doc now identifies itself as marketing evidence only, routes release approval to this audit, External Certification Runbook evidence, Digital Menu Output Constitution checks, public cache/deploy evidence, browser/mobile customer-menu QA, physical QR/menu device QA, low-bandwidth/offline/back-button tests, analytics delivery checks where relevant, target deployment evidence, and production-host smoke, and replaces stale "Smart recommendations" / "Our AI extracts..." sales phrasing with owner-safe featured-choice and review wording. `npm run verify:agent-readiness` now guards the client-menu marketing boundary and rejects the stale ready-for-use labels and stale public-copy phrasing. Follow-up gates passed: `node --check scripts/verification/verify-agent-readiness.js`, `npm run verify:agent-readiness`, targeted client-menu marketing stale-label/copy scan, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 73/73 checks, including 69 child root `verify:*` scripts. No customer-menu runtime behavior, public route behavior, public website copy, public cache behavior, Firebase deploy, Vercel deploy, production build, provider smoke, browser/device QA, Firestore write, Storage write, or external certification gate changed for this checkpoint.

Platform Pull API boundary checkpoint: `npm run verify:platform-pull-api-boundary` now source-gates API-key generation/revocation, the Business Settings Integrations tab key lifecycle UI, one-time raw key reveal, bounded desktop response parsing, authenticated key-management admission, hashed key storage, business/menu pull-route live key and target revalidation, private response headers, menu summary selection, active temporary-status output, bounded diagnostics, and docs parity. Follow-up gates passed: `npm run verify:platform-pull-api-boundary`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 73/73 checks, including 69 child root `verify:*` scripts. This is a source/docs boundary only; live key fixture/manual testing, provider smoke, browser/device QA, Firebase deploy, Vercel deploy, production build, live Firestore writes, Storage writes, and production-host smoke remain outside this checkpoint.

Customer App companion-doc status boundary checkpoint: active Customer App spec, Firebase, mobile-support, helpdoc, marketing, and website docs no longer present April 2026 "Ready for Implementation" labels. The docs now identify the current state as source-gated runtime evidence or source-backed draft content, while preserving that manual real-device QA, external certification evidence, and production-host smoke remain required before launch certification. `npm run verify:customer-app-pwa` now rejects the stale ready-for-implementation headers/footers across those docs. Follow-up gates passed: `node --check scripts/verification/verify-customer-app-pwa.js`, `npm run verify:customer-app-pwa`, targeted Customer App stale-label scan, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 72/72 checks, including 68 child root `verify:*` scripts. No Customer App runtime behavior, manifest behavior, service-worker behavior, analytics behavior, public route behavior, Firestore rules, Cloud Functions, Firebase deploy, Vercel deploy, production build, browser/device QA, live Firestore write, Storage write, or external certification gate changed for this checkpoint.

AI Menu Manager technical-team handoff launch-boundary checkpoint: `__docs__/ai-menu-manager/ai-menu-manager_technical-team-flow.md` no longer presents the team handoff as ready for controlled launch. The safe claim now says Menu Manager has source-gated support for verified daily menu operations and local export cards, but not current launch certification, and it routes current release approval to this audit, the External Certification Runbook, `npm run verify:ai-menu-manager`, authenticated desktop/mobile Menu Manager QA, supported-adapter smoke behind AMM feature flags, public website/help copy review, target deploy evidence, and production-host smoke. The same handoff now labels non-runtime checklist rows as discovery/reserved adapter rows instead of future adapter rows. `npm run verify:ai-menu-manager` now rejects stale launch-readiness wording in the technical handoff in addition to the README boundary. Follow-up gates passed: `node --check scripts/verification/verify-ai-menu-manager.js`, `npm run verify:ai-menu-manager`, targeted active AMM stale launch-label scan, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 72/72 checks, including 68 child root `verify:*` scripts. No AMM runtime behavior, action registry behavior, resolver behavior, route behavior, desktop/mobile UI behavior, public website copy, help copy, Firestore rules, Cloud Functions, Firebase deploy, Vercel deploy, production build, provider smoke, browser/device QA, live Firestore write, Storage write, or external certification gate changed for this checkpoint.

Reviews/Reputation validation residual implementation-boundary checkpoint: `__docs__/reviews-reputation/reviews-reputation_validation.md` no longer presents the February 2026 documentation feedback loop as ready for team implementation. The footer now identifies the validation as historical docs-alignment evidence only and keeps implementation blocked until GBP API access, ingestion evidence, owner mount-point review, active source gates, and target-environment smoke exist. `npm run verify:menulist-api-tenant-safety` now rejects the stale ready-for-implementation footer while preserving the dormant Reviews/Reputation flag and mount-point boundary. Follow-up gates passed: `node --check scripts/verification/verify-menulist-api-tenant-safety.js`, `npm run verify:menulist-api-tenant-safety`, targeted Reviews/Reputation validation stale-label scan, `npm run docs:check-links`, `git diff --check`, the broader readiness-label scan with only rubric/negative matches remaining, and `npm run verify:production-readiness-local` with 71/71 checks, including 67 child root `verify:*` scripts. No Reviews/Reputation runtime behavior, feature flag value, API route behavior, owner mount point, GBP ingestion path, Firestore rules, Cloud Functions, Firebase deploy, Vercel deploy, production build, provider smoke, browser/device QA, live Firestore write, Storage write, or external certification gate changed for this checkpoint.

Description Generation assessment residual production-testing checkpoint: `__docs__/projects/Assessments/assessment-09-description-generation.md` no longer presents its November 2025 user-testing row as ready for production testing. The row now preserves historical assessment evidence while routing current production testing to this audit, the External Certification Runbook, target deploy evidence, provider smoke, and browser/mobile editor QA. `npm run verify:agent-readiness` now rejects the stale production-testing label. Follow-up gates passed: `node --check scripts/verification/verify-agent-readiness.js`, `npm run verify:agent-readiness`, targeted Description Generation assessment stale-label scan, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 71/71 checks, including 67 child root `verify:*` scripts. No Description Generation runtime behavior, AI provider behavior, AI accounting path, editor save behavior, public menu output, Firestore rules, Cloud Functions, Firebase deploy, Vercel deploy, production build, provider smoke, browser/device QA, live Firestore write, Storage write, or external certification gate changed for this checkpoint.

Messaging Onboarding active-provider source-contract checkpoint: active `__docs__/messaging-onboarding/` docs now match source truth that `functions/src/messagingOnboarding/providers/providerRegistry.ts` registers WhatsApp only, checked-in Functions env templates keep `ENABLE_MESSAGING_ONBOARDING=false` with `MESSAGING_ONBOARDING_PROVIDERS=whatsapp`, and Telegram/LINE/Viber references are reserved extension candidates only. The implementation status table now reflects completed source files instead of unchecked phase tasks, and active docs no longer present provider expansion, phase sections, or optimization candidates as current roadmap/runtime scope. `npm run verify:messaging-onboarding-monitor-boundary` now source-gates the provider registry, Functions flag defaults, staging/production env defaults, active messaging docs, validation boundary, and this audit evidence. Follow-up gates passed: `node --check scripts/verification/verify-messaging-onboarding-monitor-boundary.js`, `npm run verify:messaging-onboarding-monitor-boundary`, targeted active Messaging Onboarding stale-label scan, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 71/71 checks, including 67 child root `verify:*` scripts. No Messaging Onboarding Cloud Function logic, webhook processing behavior, provider registry runtime behavior, env values, Firestore rules/indexes, Firebase deploy, Vercel deploy, production build, real WhatsApp provider smoke, browser/device QA, Firestore write, Storage write, or external certification gate changed for this checkpoint.

AI Menu Manager README launch-boundary checkpoint: `__docs__/ai-menu-manager/README.md` no longer presents the initial AMM implementation as controlled-launch-ready. The hub now identifies the implementation as source-gated but not current launch certification, and routes current approval to this audit, the External Certification Runbook, `npm run verify:ai-menu-manager`, authenticated desktop/mobile Menu Manager QA, supported-adapter smoke behind AMM feature flags, public website/help copy review, target deploy evidence, and production-host smoke. `npm run verify:ai-menu-manager` now rejects the stale controlled-launch-ready wording. Follow-up gates passed: `node --check scripts/verification/verify-ai-menu-manager.js`, `npm run verify:ai-menu-manager`, targeted AMM README stale-label scan, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 71/71 checks, including 67 child root `verify:*` scripts. No AMM runtime behavior, action registry behavior, public website copy, help copy, Firestore rules, Cloud Functions, Firebase deploy, Vercel deploy, production build, provider smoke, browser/device QA, live Firestore write, Storage write, or external certification gate changed for this checkpoint.

Menu editor Phase 4 residual launch-boundary checkpoint: `__docs__/projects/menu-editor/phase-4-advanced.md` no longer presents its December 2025 advanced-feature phase note as current ship-ready launch approval. The note now preserves the implementation evidence while routing current customer-facing menu/editor approval to this audit, the External Certification Runbook, Digital Menu Output Constitution checks, physical/mobile browser QA, low-bandwidth/offline/back-button tests, public cache/deploy evidence, analytics delivery checks where relevant, and target production smoke. `npm run verify:agent-readiness` now rejects the stale Phase 4 ship-ready label. Follow-up gates passed: `node --check scripts/verification/verify-agent-readiness.js`, `npm run verify:agent-readiness`, targeted menu-editor stale-label scan, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 70/70 checks, including 66 child root `verify:*` scripts. No menu/editor runtime behavior, public route/cache behavior, analytics runtime behavior, Firestore rules, Cloud Functions, Firebase deploy, Vercel deploy, production build, provider smoke, browser/device QA, live Firestore write, Storage write, or external certification gate changed for this checkpoint.

POS Sync live-delivery failure-threshold checkpoint: `/api/pos-sync/deliver` now tracks `posSync.consecutiveFailures` on the existing store status write. Failed live deliveries one and two are logged with `lastStatus: failed` while keeping the owner-facing warning quiet; the third failed live delivery in a row marks `connection_issue` and stores the owner-safe connection message. Invalid or blocked provider URL configuration and explicit connection-test failures still mark `connection_issue` immediately, while successful delivery, successful connection test, owner URL save, owner secret rotation, and enable/disable changes reset the counter. Active POS Webhook Sync docs no longer present Cloud Function delivery workers, retry schedulers, `pos_delivery_queue`, or phase/deferred worker language as current runtime scope. `npm run verify:pos-sync-boundary` now source-gates the failure threshold, store/shared types, desktop/mobile reset paths, and active-doc stale wording. Follow-up gates passed: `node --check scripts/verification/verify-pos-sync-boundary.js`, `npm run verify:pos-sync-boundary`, targeted active POS stale-label scan, `npm run verify:menulist-api-tenant-safety`, `npm run docs:check-links`, `npx tsc --noEmit --incremental false --pretty false`, `git diff --check`, and `npm run verify:production-readiness-local` with 70/70 checks, including 66 child root `verify:*` scripts. No POS payload shape, signing scheme, provider request target validation, route auth/tenant/rate-limit order, Firestore rules/indexes, Cloud Functions, Firebase deploy, Vercel deploy, production build, real external webhook provider smoke, browser/device QA, Storage write, or external certification gate changed for this checkpoint.

POS Sync diagnostics verifier precision checkpoint: `scripts/verification/verify-menulist-api-tenant-safety.js` and `scripts/verification/verify-pos-sync-boundary.js` now match the current POS Sync desktop/mobile source contract. Mobile POS Sync saves preserve the owner-safe `POS_SYNC_CONNECTION_ISSUE_MESSAGE` only when the connection has not changed, avoiding stale error carryover after a new provider URL or secret; desktop POS Sync URL saves are verified through the normalized URL plus status, last-error, and consecutive-failure reset fields. The POS Sync boundary verifier order helper now searches forward from the previous token so repeated field names do not cause false order failures. Follow-up gates passed: `node --check scripts/verification/verify-menulist-api-tenant-safety.js`, `node --check scripts/verification/verify-pos-sync-boundary.js`, `npm run verify:menulist-api-tenant-safety`, `npm run verify:pos-sync-boundary`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 70/70 checks, including 66 child root `verify:*` scripts. No POS Sync runtime behavior, mobile POS Sync behavior, desktop POS Sync behavior, API route behavior, Firestore rules, Cloud Functions, Firebase deploy, Vercel deploy, production build, provider smoke, browser/device QA, live Firestore write, Storage write, or external certification gate changed for this checkpoint.

Working Hours and time-slot boundary checkpoint: `npm run verify:working-hours-boundary` now source-gates desktop Business Settings `workingHours` acknowledgement and `hoursLastUpdatedAt`, mobile weekly-hours and Today quick-hours acknowledgement/restoration, desktop/mobile time-slot preset write and project cascade acknowledgement, store/project public cache revalidation, current public hours badge behavior, Mobile More route wiring, and docs parity for the shipped working-hours boundary. The sweep inventory now records the working-hours boundary source gate. Follow-up gates passed: `node --check scripts/verification/verify-working-hours-boundary.js`, `npm run verify:working-hours-boundary`, `npm run verify:public-business-truth`, `npm run verify:menulist-api-tenant-safety`, `npm run verify:mobile-shell-route-map`, `npm run verify:doc-npm-scripts`, `npm run docs:check-links`, `npx tsc --noEmit --incremental false --pretty false`, `git diff --check`, and `npm run verify:production-readiness-local` with 71/71 checks, including 67 child root `verify:*` scripts. No Working Hours runtime behavior, Time Slot runtime behavior, public menu rendering, public cache behavior, Firestore rules, Cloud Functions, Firebase deploy, Vercel deploy, production build, provider smoke, browser/device QA, live working-hours write, live time-slot write, Storage write, or production-host behavior changed.

Temporary Status boundary checkpoint: `npm run verify:temporary-status-boundary` now source-gates the authenticated and permissioned `/api/store/temp-status` set/clear route, hashed write limiter, 4KB bounded body, future-expiry validation, public cache revalidation, Digital Screens content-version touch, Owner Business Assistant packet-cache invalidation, desktop/mobile authenticated request policy, 8KB bounded response parser, optimistic rollback, Mobile Today shortcuts, OBP/menu/feedback banner expiry behavior, public pull API expired-status hiding, and active docs parity. The OBP page now renders the server-safe `TempStatusBanner` behind `ENABLE_TEMP_STATUS`, and the public pull API returns `null` for expired temporary statuses. Follow-up gates passed: `node --check scripts/verification/verify-temporary-status-boundary.js`, `npm run verify:temporary-status-boundary`, `npm run verify:public-business-truth`, `npm run verify:menulist-api-tenant-safety`, `npm run verify:mobile-shell-route-map`, `npm run verify:doc-npm-scripts`, `npm run docs:check-links`, `npx tsc --noEmit --incremental false --pretty false`, `git diff --check`, and `npm run verify:production-readiness-local` with 72/72 checks, including 68 child root `verify:*` scripts. No Firestore rules, Cloud Functions, Firebase deploy, Vercel deploy, production build, provider smoke, browser/device QA, live temporary-status write, public API credential smoke, Storage write, or production-host behavior changed.

Platform Pull API boundary checkpoint: `npm run verify:platform-pull-api-boundary` now source-gates API-key generate/regenerate/revoke admission, `MANAGE_INTEGRATIONS` permission, hashed key storage, 1KB key-action body cap, desktop Business Settings Integrations tab key UI, 8KB bounded key-action response parsing, fixed failure copy, business/menu pull route `ml_` key admission, hashed public rate-limit keys, live key/target revalidation, private response headers with `Vary: X-API-Key`, menu summary project selection, active temporary-status business output, bounded diagnostics, and active docs parity. The Business Settings Integrations tab now exposes the desktop key lifecycle and shows the raw key only once after generation. Follow-up gates passed: `node --check scripts/verification/verify-platform-pull-api-boundary.js`, `npm run verify:platform-pull-api-boundary`, `npm run verify:menulist-api-tenant-safety`, `npm run verify:public-business-truth`, `npm run verify:doc-npm-scripts`, `npm run docs:check-links`, `npx tsc --noEmit --incremental false --pretty false`, `npm run lint`, `git diff --check`, and `npm run verify:production-readiness-local` with 73/73 checks, including 69 child root `verify:*` scripts. No public pull response schema beyond active temporary-status hiding from the Temporary Status checkpoint, key hash format, route auth/target validation, Firestore rules, Cloud Functions, Firebase deploy, Vercel deploy, production build, provider smoke, browser/device QA, live API-key write, live pull-key fixture smoke, Storage write, or production-host behavior changed.

GBP Sync disabled-runtime public-claim checkpoint: `ENABLE_GBP_SYNC` remains false, token storage operations fail closed with `GBP_TOKEN_STORE_DISABLED`, and active GBP docs continue to describe manual Google handoff rather than a live connection feature. The Business Settings Integrations tab may now show the Platform Pull API card when public API is enabled, but the GBP card itself remains gated by `ENABLE_GBP_SYNC`. Follow-up gates passed: `npm run verify:public-business-truth`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 73/73 checks, including 69 child root `verify:*` scripts. No GBP provider route, Google OAuth flow, token storage, Firestore rule/index, Cloud Function, Firebase deploy, Vercel deploy, production build, provider smoke, browser/device QA, live Firestore write, Storage write, or production-host behavior changed.

Development-done security summary residual deploy-boundary checkpoint: `__docs__/projects/development_done/5-implementation-summary.md` no longer presents its lower deployment block as ready to deploy. The section now identifies itself as historical deployment notes with source evidence only, while the existing top launch boundary continues to route current security/API release approval to this audit, the External Certification Runbook, current Projects API/security verifiers, browser/API smoke, target deploy evidence, and production-host evidence. `npm run verify:agent-readiness` now rejects the stale `Ready to Deploy` status in this historical summary. Follow-up gates passed: `node --check scripts/verification/verify-agent-readiness.js`, `npm run verify:agent-readiness`, targeted development-done stale-label scan, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 69/69 checks, including 65 child root `verify:*` scripts. No Projects runtime behavior, security runtime behavior, API route behavior, Firestore rules, Cloud Functions, Firebase deploy, Vercel deploy, production build, provider smoke, browser/API QA, live Firestore write, Storage write, or external certification gate changed for this checkpoint.

Menu Command Center validation launch-boundary checkpoint: `__docs__/menu-command-center/menu-command-center_validation.md` no longer presents its February 2026 implementation validation as a current ready-for-testing or final-ready verdict. It now preserves the source evidence while routing current release approval to this audit, the External Certification Runbook, `npm run verify:agent-readiness`, authenticated desktop editor QA for bulk pricing, availability, category move, show/hide, preview, undo, and save behavior, mobile bulk availability/show-hide parity QA, public menu output QA after publish, cache/deploy evidence for changed menu truth, target deploy evidence, and production-host smoke. `npm run verify:agent-readiness` now rejects the stale Menu Command Center ready-for-testing verdict and final ready status. Follow-up gates passed: `node --check scripts/verification/verify-agent-readiness.js`, `npm run verify:agent-readiness`, targeted Menu Command Center stale-label scan, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 69/69 checks, including 65 child root `verify:*` scripts. No Menu Command Center runtime behavior, editor save behavior, mobile bulk-action behavior, public menu rendering, cache invalidation behavior, Firebase rules, Cloud Functions, Firebase deploy, Vercel deploy, production build, provider smoke, browser/device QA, live Firestore write, Storage write, or external certification gate changed for this checkpoint.

Agent Readiness reserved-flag source-contract checkpoint: `src/config/features.ts` now matches its own source comment and the active `__docs__/agent-readiness-strategy/` docs: `ENABLE_AGENT_DISCOVERY` is a disabled reserved flag, and no current MenuList route, API, Cloud Function, or client workflow reads it. The active Agent Readiness docs no longer present the static `llms.txt` / `llms-full.txt` discovery layer as a phase roadmap, feature-flag placeholder, deferred endpoint lane, or current dynamic agent API. They now separate current runtime behavior (static public discovery files plus schema.org JSON-LD on OBP/menu pages) from conditional endpoint candidates that require a separate source-backed implementation decision. `npm run verify:agent-readiness` now source-gates the disabled flag value, reserved source comment, absence of unapproved agent API route files, active-doc stale roadmap wording, and static discovery source contract. Follow-up gates passed: `node --check scripts/verification/verify-agent-readiness.js`, `npm run verify:agent-readiness`, targeted Agent Readiness stale-label scans, `npm run docs:check-links`, focused `git diff --check`, and `npm run verify:production-readiness-local` with 69/69 checks, including 65 child root `verify:*` scripts. No public discovery file content, sitemap behavior, robots behavior, schema.org runtime behavior, API route behavior, Cloud Function logic, Firestore rules, Firebase deploy, Vercel deploy, production build, provider smoke, browser/device QA, Firestore write, Storage write, or external certification gate changed for this checkpoint; the only source value change is an unused reserved feature flag now fail-closed.

Razorpay webhook diagnostic verifier precision checkpoint: `scripts/verification/verify-menulist-api-tenant-safety.js` now scopes the raw `eventKey` rejection to the duplicate-webhook breadcrumb block instead of rejecting every runtime idempotency argument named `eventKey`. The verifier still requires `getBoundedRazorpayStringContext('eventKey', webhookClaim.eventKey)` inside the duplicate-skip log and rejects `eventKey: webhookClaim.eventKey` in that logger block, while preserving the raw event key for Founder Monitor revenue movement idempotency where it is not logged. Follow-up gates passed: `node --check scripts/verification/verify-menulist-api-tenant-safety.js`, `npm run verify:menulist-api-tenant-safety`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 68/68 checks, including 64 child root `verify:*` scripts. No Razorpay webhook runtime behavior, payment mutation behavior, Founder Monitor revenue movement behavior, Firestore write shape, Firestore rules, Cloud Functions, Firebase deploy, Vercel deploy, production build, provider smoke, browser/device QA, live Firestore write, Storage write, or external certification gate changed for this checkpoint.

Menu Readability Check validation launch-boundary checkpoint: `__docs__/menulist-tools/menu-readability-check/menu-readability-check_validation.md` no longer presents the browser-local V0 validation report as ready for testing. It now identifies itself as source-gate evidence only and routes current release approval to this audit, the External Certification Runbook where applicable, `npm run verify:menu-readability-check`, public website route QA, contact handoff QA, target deploy evidence, and production-host smoke. `npm run verify:menu-readability-check` now reads the validation report and rejects the stale ready-for-testing status/verdict. Follow-up gates passed: `node --check scripts/verification/verify-menu-readability-check.js`, `npm run verify:menu-readability-check`, `npm run docs:check-links`, focused `git diff --check`, and `npm run verify:production-readiness-local` with 68/68 checks, including 64 child root `verify:*` scripts. No Menu Readability Check runtime behavior, public route behavior, contact route behavior, Firebase rules, Cloud Functions, Firebase deploy, Vercel deploy, production build, provider smoke, browser/device QA, Firestore write, Storage write, or external certification gate changed for this checkpoint.

AI System Layer operation-ledger source-contract checkpoint: active `__docs__/ai-system-layer/` and `__docs__/ai-extraction-monitoring/` docs no longer describe cross-feature AI cost visibility through phase-labeled `aiUsageLog`, `AI_TASK_TYPES`, `AI_GLOBAL_RATE_LIMIT`, task-queue, caching, or knowledge-reuse roadmap claims. The docs now route current AI accounting truth to `MENULIST_AI_OPERATIONS` for extraction rows and `menulistAiOperations/{tId}/{sId}` for billable app-route operation rows, while marking universal AI queues, request fingerprint caching, provider fallbacks, and knowledge reuse as conditional candidates only, not current runtime behavior or production savings. `npm run verify:ai-accounting` now source-gates those doc boundaries, the absence of stale Phase 2/Phase 3/future/deferred labels in the selected active docs, and the extraction-monitoring statement that no separate `aiUsageLog` collection is read. Follow-up gates passed: `node --check scripts/verification/verify-ai-accounting-hardening.js`, `npm run verify:ai-accounting`, targeted AI System Layer / AI Extraction Monitoring stale-label scan, `npm run docs:check-links`, focused `git diff --check`, and `npm run verify:production-readiness-local` with 69/69 checks, including 65 child root `verify:*` scripts. No AI runtime behavior, AI accounting write path, operation ledger schema, provider behavior, route rate-limit behavior, feature flag value, Firestore rules, Cloud Functions logic, Firebase deploy, Vercel deploy, production build, provider smoke, browser/device QA, Firestore write, Storage write, or external certification gate changed for this checkpoint.

Help Center Answerlattice support boundary checkpoint: `npm run verify:help-center-boundary` now source-gates MenuList Help Center search admission, Answerlattice scoped session helpers, bounded Help Center browser response parsing, MobileShell Help Center route mapping, scoped support-ticket reads/mutations, Answerlattice support-ticket rules, and docs parity. The sweep inventory now records the scoped ticket mutation gate. Follow-up gate passed: `npm run verify:help-center-boundary`. No Help Center runtime behavior, Answerlattice ticket runtime behavior, Firestore rules, Cloud Functions, Firebase deploy, Vercel deploy, production build, provider smoke, browser/device QA, live Firestore write, Storage write, or production-host behavior changed for this checkpoint.

Communication Kit and physical-surface output boundary checkpoint: `npm run verify:communication-kit-boundary` now source-gates Customer Communication Kit desktop/mobile template wiring, acknowledged clipboard fallbacks, bounded copy/share/WhatsApp diagnostics, active-project filtering on mobile message links, browser-local Menu Kit and printable output generation, the Menu Kit/Physical Surfaces ownership boundary, and the legacy Physical Surfaces launch-certification boundary. The sweep inventory now records the communication kit boundary source gate. Follow-up gates passed: `node --check scripts/verification/verify-communication-kit-boundary.js`, `npm run verify:communication-kit-boundary`, `npm run verify:menu-export`, `npm run verify:menu-card-export`, `npm run verify:printable-asset-templates`, `npm run verify:doc-npm-scripts`, `npm run docs:check-links`, `npx tsc --noEmit --incremental false --pretty false`, `git diff --check`, and `npm run verify:production-readiness-local` with 69/69 checks, including 65 child root `verify:*` scripts. No Customer Communication Kit runtime behavior, Customer Communication Kit template behavior, Menu Kit generation behavior, printable renderer behavior, Physical Surfaces generation behavior, Firestore rules, Cloud Functions, Firebase deploy, Vercel deploy, production build, provider smoke, browser/device QA, visual print artifact review, live Firestore write, Storage write, or production-host behavior changed.

Menu Presence Monitor boundary checkpoint: `npm run verify:menu-presence-monitor-boundary` now source-gates active-session store guards for `updateMenuPresence()` and `recordStarterActivationSignal()`, typed write acknowledgement before desktop/mobile local success state, bounded desktop/mobile diagnostics, Use MenuList and Business Settings embedding, Mobile More Search & Discovery routing, starter activation proof counts, and docs parity for the current mobile List + bottom-sheet flow. The sweep inventory now records the menu presence boundary source gate. Follow-up gates passed: `node --check scripts/verification/verify-menu-presence-monitor-boundary.js`, `npm run verify:menu-presence-monitor-boundary`, `npm run verify:public-business-truth`, `npm run verify:menulist-api-tenant-safety`, `npm run verify:pos-sync-boundary`, `npm run verify:mobile-shell-route-map`, `npm run verify:doc-npm-scripts`, `npm run docs:check-links`, `npx tsc --noEmit --incremental false --pretty false`, `git diff --check`, and `npm run verify:production-readiness-local` with 70/70 checks, including 66 child root `verify:*` scripts. No Menu Presence Monitor runtime behavior, menuPresence write behavior, starterActivationSignals write behavior, Firestore rules, Cloud Functions, Firebase deploy, Vercel deploy, production build, provider smoke, browser/device QA, live confirm/remove Firestore write, Storage write, or production-host behavior changed.

Menu Presence Monitor copy claim boundary checkpoint: `__docs__/menu-presence-monitor/menu-presence-monitor_marketing.md`, `__docs__/menu-presence-monitor/menu-presence-monitor_website.md`, `__docs__/menu-presence-monitor/menu-presence-monitor_helpdoc.md`, `__docs__/menu-presence-monitor/menu-presence-monitor_spec.md`, and `__docs__/menu-presence-monitor/menu-presence-monitor_mobile-support.md` no longer present the checklist as instant external-platform visibility, zero-setup external placement, automatic Google/Instagram/WhatsApp verification, every-surface deployment proof, one-tap external deployment, checked external surfaces, QR print placement proof, or fixed mobile completion speed. The docs now separate MenuList-recorded readiness/actions from owner-confirmed external placements and state that external platform placement is not verified by crawling or provider APIs. `npm run verify:menu-presence-monitor-boundary` now source-gates those active Menu Presence docs against stale claim wording while preserving the existing active-session store guard, typed acknowledgement, diagnostics, desktop/mobile routing, and starter activation proof checks. Follow-up gates passed for this checkpoint: `node --check scripts/verification/verify-menu-presence-monitor-boundary.js`, `npm run verify:menu-presence-monitor-boundary`, targeted active Menu Presence stale external-verification/speed/setup scan, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 91/91 checks, including 87 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. No Menu Presence Monitor runtime behavior, menuPresence write behavior, starterActivationSignals write behavior, Use MenuList runtime behavior, Business Settings runtime behavior, MobileShell routing behavior, starter activation logic, public menu runtime behavior, external-provider behavior, crawler behavior, Firestore rule, Storage rule, Cloud Function logic, Firebase deploy, Vercel deploy, production build, browser/device QA, live confirm/remove Firestore write, Storage write, launch approval, or release certification changed for this checkpoint.

Physical Surfaces validation residual launch-boundary checkpoint: `__docs__/physical-surfaces/physical-surfaces_validation.md` no longer presents its lower final-verdict, production-quality-gate, or final status blocks as ship-ready/current production approval. The validation report now keeps those sections as historical implementation evidence only, uses `npm run verify:menu-card-export` as the source gate, and warns that production builds/deploys are release-certification steps only. `npm run verify:menu-card-export` now rejects the residual ship-ready and production-quality-gate labels in addition to the prior Physical Surfaces legacy-doc boundary checks. Follow-up gates passed: `node --check scripts/verification/verify-menu-card-export.js`, `npm run verify:menu-card-export`, `npm run docs:check-links`, focused `git diff --check`, and `npm run verify:production-readiness-local` with 67/67 checks, including 63 child root `verify:*` scripts. No Physical Surfaces runtime behavior, Menu Kit runtime behavior, print renderer behavior, Today/mobile output behavior, Firebase rules, Cloud Functions, Firebase deploy, Vercel deploy, production build, provider smoke, browser/device QA, Firestore write, Storage write, or external certification gate changed for this checkpoint.

Multi-Outlet test-case residual launch-boundary checkpoint: `__docs__/multi-outlet-consistency/multi-outlet-consistency_test-cases.md` no longer presents its final-assessment feature-completeness row as production ready. The row now matches the document header: historical QA evidence only, not current launch certification. `npm run verify:multi-location-boundary` now rejects the stale final-assessment production-ready row in addition to the prior Multi-Outlet doc-boundary checks. Follow-up gates passed: `node --check scripts/verification/verify-multi-location-boundary.js`, `npm run verify:multi-location-boundary`, `npm run docs:check-links`, focused `git diff --check`, and `npm run verify:production-readiness-local` with 67/67 checks, including 63 child root `verify:*` scripts. No multi-outlet runtime behavior, outlet billing behavior, linked outlet save behavior, permissions behavior, roles behavior, Firebase rules, Cloud Functions, Firebase deploy, Vercel deploy, production build, provider smoke, browser/device QA, Firestore write, Storage write, or external certification gate changed for this checkpoint.

Discovery Infrastructure source-contract checkpoint: `__docs__/client-menu/seo-implementation-guide.md` and the active `__docs__/discovery-infrastructure/` source-contract docs now describe the current public discovery boundary instead of a dated phase roadmap. The docs now separate shipped public runtime behavior (client metadata, schema.org JSON-LD, platform/tenant sitemaps, robots, LLM files, Public API v1, POS push, and public truth indexability gates) from conditional work such as business-entity index scheduler/query wiring, generic webhooks, API v2/OpenAPI, standard feeds, review/event schema, and Core Web Vitals certification. `src/config/features.ts`, `functions/src/constants/features.ts`, `src/lib/infrastructure/discovery/indexBuilder.ts`, and `src/lib/infrastructure/taxonomy/adapter.ts` now clarify that the discovery taxonomy/provenance/semantic/index utilities are disabled builder utilities and that no discovery-index writer or query API is active while `ENABLE_INFRASTRUCTURE_DISCOVERY_INDEX` is false. `npm run verify:public-business-truth` now source-gates the runtime files, disabled flags, active docs, and stale discovery launch labels. Follow-up gates passed: `node --check scripts/verification/verify-public-business-truth.js`, `npm run verify:public-business-truth`, targeted active discovery stale-label scan, `npm run docs:check-links`, focused `git diff --check`, and `npm run verify:production-readiness-local` with 67/67 checks, including 63 child root `verify:*` scripts. No public menu/OBP/schema runtime behavior, sitemap runtime behavior, robots runtime behavior, Public API behavior, POS behavior, feature-flag value, Firebase rules, Cloud Functions logic, Firebase deploy, Vercel deploy, production build, provider smoke, browser/device QA, Firestore write, Storage write, or external certification gate changed for this checkpoint.

Hours Status Display validation launch-boundary checkpoint: `__docs__/hours-holiday-accuracy/hours-holiday-accuracy_validation.md` no longer presents its January 2026 ready-for-testing status, final ready-for-testing verdict, or manual-QA readiness line as current release approval. It now preserves the historical source evidence while routing current release approval to this audit, the External Certification Runbook, current source gates including `npm run verify:public-business-truth` and `npm run verify:menulist-api-tenant-safety`, authenticated desktop/mobile working-hours save QA, customer-facing public menu/OBP hours output QA across timezone/open/closed/temporary-status cases, cache/deploy evidence for store-output writes, target deploy evidence, and production-host smoke. `npm run verify:agent-readiness` now rejects the stale Hours validation launch labels. Follow-up gates passed: `node --check scripts/verification/verify-agent-readiness.js`, `npm run verify:agent-readiness`, `npm run docs:check-links`, focused `git diff --check`, and `npm run verify:production-readiness-local` with 67/67 checks, including 63 child root `verify:*` scripts. No hours runtime behavior, temporary-status behavior, public menu/OBP rendering, cache invalidation behavior, Firebase rules, Cloud Functions, Firebase deploy, Vercel deploy, production build, provider smoke, browser/device QA, Firestore write, Storage write, or external certification gate changed for this checkpoint.

Social Content validation launch-boundary checkpoint: `__docs__/social-content/social-content_validation.md` no longer presents its January 2026 final production validation title, ship-ready statuses, production-quality-gate label, or Vercel deploy plus SMB-testing line as current release approval. It now preserves the historical source evidence while routing current release approval to this audit, the External Certification Runbook, current source gates including `npm run verify:public-business-truth`, Today desktop/mobile/browser QA, campaign AI/provider smoke where enabled, target deploy evidence, and production-host smoke. `npm run verify:agent-readiness` now rejects the stale Social Content validation launch labels. Follow-up gates passed: `node --check scripts/verification/verify-agent-readiness.js`, `npm run verify:agent-readiness`, `npm run docs:check-links`, focused `git diff --check`, and `npm run verify:production-readiness-local` with 67/67 checks, including 63 child root `verify:*` scripts. No Social Content runtime behavior, Today behavior, GrowthOS summary read behavior, campaign provider behavior, Firebase rules, Cloud Functions, Firebase deploy, Vercel deploy, production build, provider smoke, browser/device QA, Firestore write, Storage write, or external certification gate changed for this checkpoint.

Pricing Integrity validation launch-boundary checkpoint: `__docs__/pricing-integrity-system/pricing-integrity-system_validation.md` no longer presents its January 2026 ready-for-testing status, final ready-for-testing verdict, or external-review "lock for 3 years" line as current release approval. It now preserves the historical source evidence while routing current release approval to this audit, the External Certification Runbook, current source gates including `npm run verify:menulist-api-tenant-safety`, authenticated desktop/mobile editor price-change QA, public menu and PDF artifact QA, cache/deploy evidence for published price changes, target deploy evidence, and production-host smoke. If a release scope enables background PDF regeneration, queue/job/worker evidence and scoped Firebase deploy evidence are also required when Cloud Function logic changes. `npm run verify:agent-readiness` now rejects the stale ready-for-testing and external-review approval labels. Follow-up gates passed: `node --check scripts/verification/verify-agent-readiness.js`, `npm run verify:agent-readiness`, `npm run docs:check-links`, focused `git diff --check`, and `npm run verify:production-readiness-local` with 67/67 checks, including 63 child root `verify:*` scripts. No pricing runtime behavior, price formatter behavior, PDF queue behavior, public menu rendering, Firebase rules, Cloud Functions, Firebase deploy, Vercel deploy, production build, provider smoke, browser/device QA, Firestore write, Storage write, or external certification gate changed for this checkpoint.

Business Type Data Model source-contract checkpoint: `__docs__/business-type-data-model/README.md`, `__docs__/auth-onboarding/auth-onboarding_spec.md`, and `auth-onboarding_impl.md` now match the current onboarding business identity contract: `businessType` is the owner-selected actual business type, `businessIndustry` is the B2C/B2B plan-type marker, and `businessCategory` is derived through shared business-type data. `scripts/migrate-business-type-swap.ts` now derives categories from `src/data/shared/businessTypes.ts`, defaults to dry-run, requires an explicit project id, and refuses live writes unless `--write`, matching `--confirm-project`, and `--all-stores-and-tenants` are present. `npm run verify:agent-readiness` now source-gates the docs, onboarding route values, centralized `createTenantStoreInTransaction()` behavior, `StoreDataType`, byte-for-byte shared frontend/Functions business-type data parity, and migration live-write guards. Follow-up gates passed: `node --check scripts/verification/verify-agent-readiness.js`, focused TypeScript check for `scripts/migrate-business-type-swap.ts`, `npm run verify:agent-readiness`, targeted stale business-type/auth-onboarding scan, missing-project and missing-confirm migration fail-fast guard checks, `npm run docs:check-links`, focused `git diff --check`, and `npm run verify:production-readiness-local` with 67/67 checks, including 63 child root `verify:*` scripts. No onboarding runtime behavior, Razorpay behavior, auth behavior, public menu/OBP/schema runtime behavior, Firestore rules, Cloud Functions, Firebase deploy, Vercel deploy, production build, provider smoke, browser/device QA, Firestore write, Storage write, live migration, or external certification gate changed for this checkpoint.

Staff Prompt validation/code-review launch-boundary checkpoint: `__docs__/staff-prompt/staff-prompt_validation.md` and `__docs__/staff-prompt/staff-prompt_code-review.md` no longer present January 2026 source-validation and review notes as current testing, deploy, SMB-testing, ship-ready, or production approval. They now preserve the historical source evidence while routing current release approval to this audit, the External Certification Runbook, `npm run verify:staff-prompt-runtime`, authenticated desktop/mobile Today QA with an eligible target-store `staffPrompt`, target deploy evidence, and production-host smoke. If release scope claims generated prompts end-to-end, upstream summary-writer evidence for `platformSummary/campaigns_{sId}.staffPrompt` is also required. No Staff Prompt runtime behavior, Today/mobile rendering behavior, upstream summary writer, Firebase rules, Cloud Functions, Firebase deploy, Vercel deploy, production build, provider smoke, browser/device QA, Firestore write, Storage write, or external certification gate changed for this checkpoint.

Platform Pull API live key-revalidation checkpoint: `/api/public/v1/business` and `/api/public/v1/menu` no longer pass the shared `cacheTtlMs` validation-cache option, so MenuList pull requests re-run API-key lookup and active/deleted/platform-blocked store plus tenant eligibility checks on every request before returning business or menu data. The shared validation cache remains opt-in for routes that deliberately choose it, but MenuList pull endpoints now match the Platform Pull API spec boundary that key revocation and target blocking should take effect on the next request from each server process. Follow-up gates passed: `node --check scripts/verification/verify-menulist-api-tenant-safety.js`, `node --check scripts/verification/verify-public-business-truth.js`, `npm run verify:menulist-api-tenant-safety`, `npm run verify:public-business-truth`, `npm run docs:check-links`, focused `git diff --check`, and `npm run verify:production-readiness-local` with 67/67 checks, including 63 child root `verify:*` scripts. No key generation/revocation behavior, API-key hashing, ETag/304 behavior, private response-cache headers, response schema, menu project selection, Firestore writes/deletes, rules/indexes, Cloud Functions, Firebase deploy, Vercel deploy, production build, provider smoke, browser/device QA, Storage write, or external certification gate changed for this checkpoint.

POS Sync debounced-delivery admission checkpoint: `triggerPosSyncDebounced()` now requires both a provider connection URL and signing secret before calling `/api/pos-sync/deliver`, so stores with incomplete POS Sync config do not create repeated invalid delivery-route calls after menu saves. `npm run verify:pos-sync-boundary` now owns that debounced delivery URL+secret admission alongside the public-HTTPS/DNS guard, route auth/tenant/rate-limit order, desktop/mobile shared test policy, MobileShell More routing, and docs parity. Follow-up gates passed: `node --check scripts/verification/verify-pos-sync-boundary.js`, `npm run verify:pos-sync-boundary`, `npm run verify:menulist-api-tenant-safety`, `npm run docs:check-links`, `npx tsc --noEmit --incremental false --pretty false`, focused `git diff --check`, and `npm run verify:production-readiness-local` with 67/67 checks, including 63 child root `verify:*` scripts. No valid POS settings save, secret generation/rotation, server-side route auth, URL/DNS target validation, debounce timing, payload shape, signing, delivery logs, Firestore rules/indexes, Cloud Functions, Firebase deploy, Vercel deploy, production build, real external webhook provider smoke, browser/device QA, Storage write, or external certification gate changed for this checkpoint.

Multi-location active-cap source checkpoint: `POST /api/outlets/create` now enforces `MAX_OUTLETS_PER_TENANT` against active non-master outlets only. Deactivated outlets still preserve history and data, but no longer consume replacement-location capacity after the owner deactivates a location. `npm run verify:multi-location-boundary` now owns this source contract alongside outlet lifecycle route admission, linked outlet save acknowledgement, desktop/mobile Locations guards, and docs parity. Follow-up gates passed: `node --check scripts/verification/verify-multi-location-boundary.js`, `npm run verify:multi-location-boundary`, `npm run verify:menulist-api-tenant-safety`, `npm run verify:public-business-truth`, `npm run docs:check-links`, `npx tsc --noEmit --incremental false --pretty false`, `git diff --check`, and `npm run verify:production-readiness-local` with 67/67 checks, including 63 child root `verify:*` scripts. No Firestore read/write shape, billing provider sequence, cache invalidation, Firebase rules/indexes, Cloud Functions, Firebase deploy, Vercel deploy, production build, browser/device QA, provider smoke, Storage write, or external certification gate changed for this checkpoint.

Help Center Answerlattice support boundary checkpoint: `src/database/tickets/index.ts` now requires non-platform partial support-ticket mutations to carry selected ticket `tId/sId` matching the active session before merge writes, and strips composer-injected `tId/sId` from platform partial updates when no explicit selected-ticket scope is supplied. `TicketDetailView`, `ConversationTimeline`, and `PlatformTicketsView` now pass selected ticket scope for close/edit/reply/soft-delete/restore actions. `npm run verify:help-center-boundary` now owns the source boundary across Help Center search API auth/body caps, bounded browser response parsing, MobileShell Help Center routing, client ticket detail mode, Firestore stable-scope rules, and scoped ticket mutation callers. Follow-up gates passed: `node --check scripts/verification/verify-help-center-boundary.js`, `npm run verify:help-center-boundary`, `node --check scripts/verification/verify-answerlattice-runtime-truth.js`, `npm run verify:answerlattice-runtime-truth`, `npm run verify:mobile-shell-route-map`, `npm run verify:menulist-api-tenant-safety`, `npm run verify:auth-security-failure-matrix`, `npm run verify:doc-npm-scripts`, `npm run docs:check-links`, `npx tsc --noEmit --incremental false --pretty false`, `git diff --check`, and `npm run verify:production-readiness-local` with 68/68 checks, including 64 child root `verify:*` scripts. No Help Center search behavior, provider behavior, ticket schema, Firestore rules/indexes, Cloud Functions, Firebase deploy, Vercel deploy, production build, browser/device QA, Firestore write, Storage write, or external certification gate changed for this checkpoint.

URL routing architecture launch-boundary checkpoint: `__docs__/url-routing-architecture/README.md`, `url-routing-architecture_spec.md`, `url-routing-architecture_impl.md`, `url-routing-architecture_firebase.md`, and `url-routing-architecture_adr.md` no longer present active public routing work as phase-labeled launch scope. They now describe the implemented slug, canonical, product-domain, and path-segment guardrails as current milestones/capabilities tied to `src/app/client/[[...slug]]/page.tsx`, `src/lib/publicRouting/pathSegments.ts`, middleware routing, and the product-domain resolver. `npm run verify:url-routing-boundary` now reads the active spec and ADR, requires the corrected milestone/status wording, and rejects stale phase/post-launch/production-readiness labels across active URL routing docs. Follow-up gates passed: `node --check scripts/verification/verify-url-routing-boundary.js`, `npm run verify:url-routing-boundary`, a targeted active URL routing stale phase/readiness scan, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 67/67 checks, including 63 child root `verify:*` scripts. No URL routing runtime behavior, middleware behavior, public path-segment behavior, product-domain behavior, Firebase rules, Cloud Functions, Firebase deploy, Vercel deploy, production build, provider smoke, browser/device QA, Firestore write, Storage write, or external certification gate changed for this checkpoint.

Customer App analytics source-chain checkpoint: `npm run verify:customer-app-pwa` now guards the Customer App analytics contract beyond event-name presence. The verifier checks `CUSTOMER_APP_*` event field increments in `src/lib/analytics/unified.ts`, reserved `projectId='customerApp'` routing, public analytics rate-limit/body/preference gates, client/server daily analytics write shape, Cloud Function summary rollups, dashboard-summary generation, timezone scheduler inclusion of the virtual `customerApp` project, the owner-dashboard DAL/hook read path, and desktop/mobile KPI card rendering for installed customers, 30-day opens, 30-day installs, conversion, shortcuts, platform breakdown, iOS manual/inferred installs, and app stickiness. The Customer App docs now describe this as a source-chain gate only; live event writes, nightly rollup, dashboard-value parity, real-device install flows, Firebase Function deployment, Vercel deployment, provider/browser/device QA, and production-host evidence remain external certification gates. Follow-up gates passed: `node --check scripts/verification/verify-customer-app-pwa.js`, `npm run verify:customer-app-pwa`, `npm run docs:check-links`, focused `git diff --check`, and `npm run verify:production-readiness-local` with 67/67 checks, including 63 child root `verify:*` scripts. No Customer App runtime behavior, analytics runtime behavior, Firebase rules, Cloud Functions, Firebase deploy, Vercel deploy, production build, provider smoke, browser/device QA, Firestore write, Storage write, or external certification gate changed for this checkpoint.

Reseller dashboard launch-boundary checkpoint: `__docs__/reseller-dashboard/reseller-dashboard_spec.md`, `reseller-dashboard_impl.md`, and `reseller-dashboard_marketing.md` no longer present reseller scale policy, implementation order, commission, referral payout, or multi-currency behavior as "Phase 2", "Phase 3", or deferred future work. The active spec now treats reseller tier and offline-payment availability as current source-controlled configuration in `src/config/resellerPricing.ts`, states that commission/referral payouts are not part of the current runtime, and requires a separate audited billing/revenue-share feature before any commission system exists. `npm run verify:reseller-dashboard-boundary` now reads the spec and marketing doc and rejects stale phase/deferred commission wording. Follow-up gates passed: `node --check scripts/verification/verify-reseller-dashboard-boundary.js`, `npm run verify:reseller-dashboard-boundary`, a targeted stale Reseller Dashboard phase/deferred-commission scan, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 67/67 checks, including 63 child root `verify:*` scripts. No reseller runtime behavior, billing behavior, pricing constants, feature flags, API behavior, mobile behavior, Firebase rules, Cloud Functions, Firebase deploy, Vercel deploy, production build, provider smoke, browser/device QA, Firestore write, Storage write, or external certification gate changed for this checkpoint.

Reseller dashboard account/link handoff checkpoint: active Reseller Dashboard README, spec, implementation, marketing, website, helpdoc, and mobile-support docs no longer say the reseller onboarding flow uploads menus, runs AI extraction, creates a fully working/live digital menu in minutes, makes the menu live before the reseller leaves, or includes photo-upload mobile UI in the reseller onboarding path. The docs now match the current source path: `/api/reseller/onboard` creates the tenant/store account, owner access, subscription state, dashboard link, and public customer link handoff; menu content is added later through normal owner dashboard and import/review flows. `npm run verify:reseller-dashboard-boundary` now reads the website and help docs in addition to the existing reseller docs, requires the account/link/no-upload boundary, and rejects stale live-menu/upload/extraction/AI-powered copy. Follow-up gates passed for this checkpoint: `node --check scripts/verification/verify-reseller-dashboard-boundary.js`, `npm run verify:reseller-dashboard-boundary`, targeted active Reseller Dashboard stale live-menu/upload/extraction scan, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 92/92 checks, including 88 child root `verify:*` scripts, docs links, typecheck, lint, and `git diff --check`. No reseller runtime behavior, billing behavior, pricing constants, feature flags, API behavior, mobile behavior, menu import behavior, AI extraction behavior, Firebase rules, Cloud Functions, Firebase deploy, Vercel deploy, production build, provider smoke, browser/device QA, Firestore write, Storage write, or external certification gate changed for this checkpoint.

Projects miscellaneous backlog launch-boundary checkpoint: `__docs__/projects/miscellaneous-task.md` no longer presents historical Projects backlog notes as a "next phase", "Phase 2", "post-launch", or current launch-blocker deferral bucket. It now identifies itself as historical backlog evidence, routes current release approval to this audit, the production-readiness checklist, and the External Certification Runbook, and treats remaining ideas as conditional items that require scoped audit evidence rather than automatic later work. `npm run verify:agent-readiness` now rejects stale "Phase 2", "Phase 3", "post-launch", "Deferred To", and "Still Deferred" wording in this active Projects backlog note. Follow-up gates passed: `node --check scripts/verification/verify-agent-readiness.js`, `npm run verify:agent-readiness`, a targeted stale Projects miscellaneous phase/post-launch scan, focused `git diff --check`, `npm run docs:check-links`, and `npm run verify:production-readiness-local` with 67/67 checks, including 63 child root `verify:*` scripts. No Projects runtime behavior, extraction behavior, AI capacity behavior, translation behavior, security logging behavior, Firebase deploy, Vercel deploy, production build, provider smoke, browser/device QA, Firestore write, Storage write, or external certification gate changed for this checkpoint.

Menu Card Export validation launch-boundary checkpoint: `__docs__/menu-card-export/menu-card-export_validation.md` and `__docs__/menu-card-export/menu-card-export_test-cases.md` no longer present the June 2026 route, cost, authenticated demo-runtime, artifact validation, or test-case evidence as current production-release approval. They now preserve that evidence while routing current release approval to this audit, the External Certification Runbook, Digital Menu Output Constitution checks for print/menu outputs, authenticated desktop/mobile browser QA, visual PDF and print-shop artifact review, provider smoke for the AI advisor where enabled, target deploy evidence, and production-host smoke. `npm run verify:menu-card-export` now rejects the stale production-ready status, final ready-for-production verdict, production-ready test baseline, and release-ready test-gate sentence while requiring the active audit/runbook boundary. Follow-up gates passed: `node --check scripts/verification/verify-menu-card-export.js`, `npm run verify:menu-card-export`, a targeted stale Menu Card Export validation/test-case scan, focused `git diff --check`, `npm run docs:check-links`, and `npm run verify:production-readiness-local` with 67/67 checks, including 63 child root `verify:*` scripts. No Menu Card Export runtime behavior, PDF renderer behavior, print-shop packet behavior, AI advisor behavior, mobile shell behavior, Firebase rules, Cloud Functions, Firebase deploy, Vercel deploy, production build, provider smoke, browser/device QA, Firestore write, Storage write, or external certification gate changed for this checkpoint.

Menu Correctness Engine spec launch-boundary checkpoint: `__docs__/menu-correctness-engine/menu-correctness-engine_spec.md` no longer says the runtime flag is off or carries the February 2026 ChatGPT launch verdict as active release evidence. It now matches current source truth (`ENABLE_MCE: true`, `updateProject()` stamps `_mce` metadata in the same write), keeps only historical score context, and routes release approval to this audit, the External Certification Runbook, `npm run verify:public-business-truth`, browser/mobile save and publish-gate QA, customer-facing surface smoke for validated project data, Firestore rules/deploy evidence if rules change, target deploy evidence, and production-host smoke. `npm run verify:public-business-truth` now source-gates the runtime flag, `updateProject()` MCE hook, same-write `_mce` metadata path, README/implementation status, and the spec launch boundary. Follow-up gates passed: `node --check scripts/verification/verify-public-business-truth.js`, `npm run verify:public-business-truth`, a targeted stale MCE readiness/flag scan, focused `git diff --check`, `npm run docs:check-links`, and `npm run verify:production-readiness-local` with 67/67 checks, including 63 child root `verify:*` scripts. No MCE runtime behavior, project save behavior, publish-gate behavior, public/customer rendering behavior, Firebase rules, Cloud Functions, Firebase deploy, Vercel deploy, production build, browser/device QA, Firestore write, Storage write, or external certification gate changed for this checkpoint.

Physical Surfaces legacy-doc launch-boundary checkpoint: `__docs__/physical-surfaces/physical-surfaces_code-review.md`, `physical-surfaces_validation.md`, and `physical-surfaces_marketing.md` no longer present January 2026 campaign-based recommendation cards as current production-ready, deploy-ready, or launch-ready approval. They now preserve historical implementation, code-review, and positioning evidence while repeating the existing README boundary that Menu Kit is the canonical physical surface system for identity surfaces. Current approval for active physical/print output routes to this audit, the External Certification Runbook, `npm run verify:menu-card-export`, Digital Menu Output Constitution checks, browser/mobile output QA, visual print artifact review, target deploy evidence, and production-host smoke. `npm run verify:menu-card-export` now source-gates the legacy README boundary, the three active Physical Surfaces doc boundaries, and rejects the stale production-ready / Vercel-ready / launch-ready status lines. Follow-up gates passed: `node --check scripts/verification/verify-menu-card-export.js`, `npm run verify:menu-card-export`, a targeted stale Physical Surfaces readiness scan, focused `git diff --check`, `npm run docs:check-links`, and `npm run verify:production-readiness-local` with 67/67 checks, including 63 child root `verify:*` scripts. No Physical Surfaces runtime behavior, Menu Kit runtime behavior, print renderer behavior, Today/mobile output behavior, Firebase rules, Cloud Functions, Firebase deploy, Vercel deploy, production build, browser/device QA, Firestore write, Storage write, or external certification gate changed for this checkpoint.

Active strategy and legacy implementation launch-boundary checkpoint: `__docs__/strategy/five-year-vision-2026-complete.md`, `__docs__/strategy/menulist-complete-feature-spec.md`, `__docs__/strategy/product-strategy-2026.md`, `__docs__/strategy/product-strategy-market-research.md`, `__docs__/internal-tracking/MOL-V0-IMPLEMENTATION-PLAN.md`, `__docs__/projects/development_done/1-IMPLEMENTATION-UPLOAD-COMPLETE.md`, `1-TESTING-GUIDE-UPLOAD.md`, and `5-IMPLEMENTATION-SUMMARY.md` no longer present strategic feature maps, founder strategy notes, market-research scope filters, MOL v0 history, upload implementation notes, upload testing checklist, or security implementation summaries as current production certification. They now preserve historical/source evidence while routing current release approval to this audit, External Certification Runbook evidence, current source verifiers, `npm run verify:menu-extraction-pipeline`, browser/mobile upload QA, Storage quota/rules evidence, current Projects API/security verifiers, browser/API smoke, Firestore/cost evidence, scoped scheduler/deploy evidence where relevant, target deploy evidence, and production-host smoke. `npm run verify:agent-readiness` now rejects the stale strategy and legacy implementation production-ready statuses, table values, current-launch wording, public-beta strict rule, achievement rows, and upload checklist ready-for-production checkbox. Follow-up gates passed: `node --check scripts/verification/verify-agent-readiness.js`, `npm run verify:agent-readiness`, targeted stale-status scans for the selected docs, focused `git diff --check`, `npm run docs:check-links`, and `npm run verify:production-readiness-local` with 67/67 checks, including 63 child root `verify:*` scripts. No strategy runtime, MOL runtime, upload runtime, security runtime, Firebase rules, Cloud Functions, Firebase deploy, Vercel deploy, production build, provider smoke, browser QA, Firestore write, Storage write, or external certification gate changed for this checkpoint.

Mobile PWA analysis launch-boundary checkpoint: `__docs__/mobile-operational-support/08-full-pwa-mobile-analysis.md` no longer presents its February 2026 PWA-only analysis as phased launch/post-launch scope or current launch certification. It now keeps the implemented-source analysis, identifies the current operational baseline and implemented core mobile gap closure, treats additional mobile setup/analytics/camera flows as conditional additions requiring a separate scoped audit, and keeps complex editor, AI image, multi-outlet, POS webhook, translation, user/role, and chat-management surfaces as permanent mobile rejections unless a later audited feature loop changes that. `npm run verify:agent-readiness` now rejects the stale "Phase 3", "3 months post-launch", "After launch", "Keep Phase 2", "phased approach", and old implemented-launch badge wording. Follow-up gates passed: `node --check scripts/verification/verify-agent-readiness.js`, `npm run verify:agent-readiness`, targeted stale mobile phase/post-launch scan, focused `git diff --check`, `npm run verify:mobile-shell-route-map`, `npm run docs:check-links`, and `npm run verify:production-readiness-local` with 67/67 checks, including 63 child root `verify:*` scripts. No mobile runtime behavior, MobileShell behavior, owner PWA route behavior, Firebase deploy, Vercel deploy, production build, provider smoke, browser/device QA, Firestore write, Storage write, or external certification gate changed for this checkpoint.

Active cost/source doc launch-boundary checkpoint: `__docs__/projects/upload-file-processing/README.md`, `upload-file-processing_firebase.md`, `__docs__/projects/b2b-view/b2b-view_firebase.md`, `__docs__/projects/multi-language-translation/README.md`, `__docs__/decision-intelligence/decision-intelligence_spec.md`, `__docs__/auth/auth_firebase.md`, `firebase-auth-sync.md`, `firebase-auth-null-fix.md`, `__docs__/stores-management/stores-management_firebase.md`, and `__docs__/system-strengthening/system-strengthening_firebase.md` no longer present upload, B2B, translation, auth, store, system-strengthening, or Decision Intelligence source/cost evidence as current production certification. They now keep source, Firebase-cost, and historical fix evidence while routing release approval to this audit, External Certification Runbook evidence, `npm run verify:agent-readiness`, `npm run verify:menu-extraction-pipeline`, browser/mobile upload QA, Storage quota/rules evidence, API/export security review, translated-menu renderer/RTL evidence, auth browser/API smoke, Firebase Auth custom-claims evidence, store CRUD/browser QA, public cache evidence for store-output writes, scoped scheduler deploy evidence, `npm run verify:system-strengthening`, target deploy evidence, and production-host smoke. `npm run verify:agent-readiness` now rejects stale production-ready statuses in those active docs and requires their launch-boundary wording. Follow-up gates passed: `node --check scripts/verification/verify-agent-readiness.js`, `npm run verify:agent-readiness`, a targeted stale-status scan for the selected docs, focused `git diff --check`, `npm run docs:check-links`, and `npm run verify:production-readiness-local` with 67/67 checks, including 63 child root `verify:*` scripts. No upload, B2B, translation, auth, store, system-strengthening, Decision Intelligence, Firebase rules, Cloud Functions, Firebase deploy, Vercel deploy, production build, provider smoke, browser QA, Firestore write, Storage write, or external certification gate changed for this checkpoint.

Active-doc stale readiness wording checkpoint: `__docs__/projects/production-readiness-assessment.md`, `__docs__/projects/Editor/07-AUTO-SAVE.md`, `__docs__/projects/testing-checklist.md`, `__docs__/projects/internal-feedback-system/README.md`, `__docs__/social-content/social-content-product-strategy.md`, `__docs__/pos-webhook-sync/pos-webhook-sync_impl.md`, and `__docs__/main-website/README.md` no longer present historical project assessment scope, editor auto-save evidence, Guest Feedback scalability notes, social content scope, POS webhook scope, or Menu Link Import website history as post-launch exceptions or current production certification. They now keep source or strategy evidence while routing release approval to this audit, External Certification Runbook evidence, active feature/source gates, browser/mobile QA where relevant, load/provider/deploy evidence where relevant, and target-environment smoke. `npm run verify:agent-readiness` now rejects stale auto-save production-ready wording, Projects assessment post-launch language, Projects testing checklist post-launch deferral, Guest Feedback hardening deferral, Social Content QR/TV post-launch language, POS webhook WhatsApp post-launch language, and the main website's stale Menu Link Import production-ready claim. Follow-up gates passed: `node --check scripts/verification/verify-agent-readiness.js`, `npm run verify:agent-readiness`, a targeted stale-claim scan for the selected docs, focused `git diff --check`, `npm run docs:check-links`, and `npm run verify:production-readiness-local` with 67/67 checks, including 63 child root `verify:*` scripts. No editor, Projects, Guest Feedback, Social Content, POS webhook, Menu Link Import, website runtime behavior, Firebase deploy, Vercel deploy, production build, provider smoke, browser QA, Firestore write, Storage write, or external certification gate changed for this checkpoint.

Active cost/source doc launch-boundary checkpoint: `__docs__/projects/upload-file-processing/upload-file-processing_firebase.md`, `__docs__/projects/b2b-view/b2b-view_firebase.md`, `__docs__/projects/multi-language-translation/README.md`, `__docs__/auth/auth_firebase.md`, `__docs__/stores-management/stores-management_firebase.md`, `__docs__/system-strengthening/system-strengthening_firebase.md`, and `__docs__/decision-intelligence/decision-intelligence_spec.md` no longer present upload, B2B, translation, auth, store, system-strengthening, or Decision Intelligence source/cost evidence as current production certification. They now keep source, Firebase-cost, or locked-spec evidence, but route release approval to this audit, External Certification Runbook evidence, browser/mobile upload QA, Storage quota/rules evidence, API/export security review, auth browser/API smoke, store CRUD/browser QA, scoped scheduler deploy evidence, and `npm run verify:system-strengthening`. `npm run verify:agent-readiness` now rejects stale production-ready statuses across those active docs and checks the current external-gate routing. Follow-up gates passed: `node --check scripts/verification/verify-agent-readiness.js`, `npm run verify:agent-readiness`, `npm run docs:check-links`, full `git diff --check`, and `npm run verify:production-readiness-local` with 67/67 checks, including 63 child root `verify:*` scripts. No upload, B2B, translation, auth, store, system-strengthening, Decision Intelligence, Firebase rules, Cloud Functions, Firebase deploy, Vercel deploy, production build, provider smoke, browser QA, Firestore write, Storage write, or external certification gate changed for this checkpoint.

Billing and pricing doc launch-boundary checkpoint: `__docs__/razorpay/README.md`, `__docs__/strategy/pricing-strategy.md`, `__docs__/ai-enhancement-packs/ai-enhancement-packs_impl.md`, `ai-usage-audit.md`, and `ai-enhancement-packs_spec.md` no longer present Razorpay, pricing, or AI Enhancement Pack billing evidence as current production certification. They now keep the frozen-provider, source-gated route, historical pricing, and AI top-up reuse evidence, but route current release approval to this audit, External Certification Runbook evidence, `npm run verify:billing-entitlement-boundary`, Razorpay sandbox subscription/top-up/reseller/webhook smoke, desktop/mobile Billing browser QA, website/pricing copy review, target deploy evidence, and production-host smoke. `npm run verify:billing-entitlement-boundary` now rejects the stale Razorpay hub production-ready status, historical pricing production-ready status, AI Enhancement Pack "fully built / production-ready" billing wording, and the old top-up production-ready adaptation claim. Follow-up gates passed: `node --check scripts/verification/verify-billing-entitlement-boundary.js`, `npm run verify:billing-entitlement-boundary`, a targeted stale-status scan for the selected active docs, focused `git diff --check`, `npm run docs:check-links`, and `npm run verify:production-readiness-local` with 67/67 checks, including 63 child root `verify:*` scripts. No Razorpay route behavior, billing runtime behavior, pricing runtime behavior, AI Enhancement Pack runtime behavior, Firebase rules, Cloud Functions, Firebase deploy, Vercel deploy, production build, provider smoke, browser QA, Firestore write, Storage write, or external certification gate changed for this checkpoint.

Projects editor and historical ledger launch-boundary checkpoint: `__docs__/README.md`, `__docs__/projects/Editor/README.md`, `__docs__/projects/Editor/07-AUTO-SAVE.md`, `__docs__/projects/development_done/README.md`, `__docs__/projects/development_done/2-implementation-ai-extraction-complete.md`, and `__docs__/projects/development_done/5-security-implementation-complete.md` no longer present the root docs index, legacy editor source notes, editor auto-save source note, November 2025 AI extraction completion note, or November 2025 security completion note as current MenuList launch certification. They now keep source and historical implementation evidence, but route current release approval to this audit, External Certification Runbook evidence, browser/mobile editor QA, publish/cache evidence for edited public truth, Firestore write observation for the target editor environment, upload/extraction/provider smoke, current API/security source review, Firebase deploy evidence where rules/functions change, and target-environment smoke. `npm run verify:agent-readiness` now rejects stale root-docs production-ready status, stale editor production-ready status, stale auto-save production-ready wording, stale development-done production-ready ledger rows, stale AI extraction completion production-ready status, and stale security completion deployment-ready wording while source-gating current editor entry points, editor view wiring, shortcuts, auto-save debounce/throttle references, before-unload warning, editor logic, and image preview usage. Follow-up gates passed: `node --check scripts/verification/verify-agent-readiness.js`, `npm run verify:agent-readiness`, a targeted stale-status scan for the selected docs, focused `git diff --check`, `npm run verify:doc-npm-scripts`, `npm run docs:check-links`, full `git diff --check`, and `npm run verify:production-readiness-local` with 67/67 checks, including 63 child root `verify:*` scripts. No editor runtime behavior, auto-save behavior, image preview behavior, AI extraction runtime behavior, security runtime behavior, Firebase deploy, Vercel deploy, production build, provider smoke, browser QA, Firestore write, Storage write, or external certification gate changed for this checkpoint.

Multi-location and permission docs launch-boundary checkpoint: `__docs__/multi-outlet-consistency/README.md`, `multi-outlet-consistency_spec.md`, `multi-outlet-consistency_firebase.md`, `multi-outlet-consistency_test-cases.md`, `multi-outlet-consistency_ai-extraction.md`, `multi-outlet-consistency_verification.md`, `store-onboarding/store-onboarding_spec.md`, `store-onboarding/store-onboarding_impl.md`, `store-onboarding/store-onboarding-billing_impl.md`, `__docs__/multi-chain-permissions/multi-chain-permissions_firebase.md`, `multi-chain-permissions_verification.md`, `multi-chain-permissions_marketing.md`, and `__docs__/roles-permissions/roles-permissions_firebase.md` no longer present their implementation, cost, QA, marketing, billing, or permission evidence as current production certification. They now keep the source and historical QA evidence, but route current release approval to this audit, External Certification Runbook evidence, `npm run verify:multi-location-boundary`, desktop/mobile Locations browser QA, linked outlet save QA, permission-policy browser QA, staff role CRUD QA, Razorpay sandbox evidence for quantity update/replacement subscription paths, Firebase deploy evidence where rules/functions change, and target-environment smoke. `npm run verify:multi-location-boundary` now rejects stale Multi-Outlet, store-onboarding, Multi-Chain Permissions, and Roles/Permissions production-ready statuses, plus the old test-matrix production-ready claim. Follow-up gates passed: `node --check scripts/verification/verify-multi-location-boundary.js`, `npm run verify:multi-location-boundary`, a targeted stale-status scan for the selected active docs, focused `git diff --check`, `npm run docs:check-links`, and `npm run verify:production-readiness-local` with 67/67 checks, including 63 child root `verify:*` scripts. No multi-outlet, outlet billing, linked outlet save, permissions, roles, Firebase rules, Cloud Functions, Firebase deploy, Vercel deploy, production build, provider smoke, browser QA, Firestore write, Storage write, or external certification gate changed for this checkpoint.

Final active stale-readiness wording checkpoint: `__docs__/features/profile-modal-redesign.md`, `__docs__/features/trust-security-page.md`, `__docs__/projects/development_done/3-implementation-editor-complete.md`, and `__docs__/projects/development_done/2-testing-guide-ai-extraction.md` no longer surface direct production-ready or ready-for-production wording in the active stale-claim scan. The profile modal doc now routes release approval to this audit, External Certification Runbook evidence, auth/profile API source review, desktop/mobile profile QA, permission-boundary checks, and target-environment smoke. The trust/security page boundary now avoids treating a historical page-build note as full MenuList launch readiness. The editor completion note now marks its November 2025 P0/P1 fixes as historical editor UX implementation evidence and routes release approval to browser/mobile editor QA, publish/cache evidence for edited public truth, and target-environment smoke. The AI extraction testing guide now uses launch-certification wording instead of "marking as production-ready." `npm run verify:agent-readiness` now rejects the stale profile modal footer, trust/security full-production wording, editor production-ready statuses, editor post-launch wording, and AI extraction testing-guide production-ready checklist intro. Follow-up gates passed: `node --check scripts/verification/verify-agent-readiness.js`, `npm run verify:agent-readiness`, a targeted active stale-claim scan for feature/development/strategy/assessment docs, and focused `git diff --check`. No profile, auth, editor, extraction, public website, Firebase deploy, Vercel deploy, production build, provider smoke, browser QA, Firestore write, Storage write, or external certification gate changed for this checkpoint.

Auth onboarding launch-boundary checkpoint: `__docs__/auth-onboarding/README.md`, `auth-onboarding_spec.md`, `auth-onboarding_impl.md`, `auth-onboarding_firebase.md`, and `auth-onboarding_mobile-support.md` no longer present auth, signup, first-payment, Firebase cost, or desktop-first mobile support evidence as current production certification. They now keep the implemented source and cost evidence, but route release approval to this audit, External Certification Runbook evidence, auth browser/API smoke across login, claim-account, sign-out, store-switch, and session-refresh flows, Razorpay sandbox onboarding and payment-verification evidence, Firebase Auth custom-claims evidence, mobile browser onboarding/payment QA, App Check/session-cookie review, provider-failure compensation evidence, cost monitoring, target deploy evidence, and production-host smoke. `npm run verify:agent-readiness` now rejects stale auth-onboarding "Production Ready" statuses and source-gates the current NextAuth, onboarding API, set-claims, payment handler, and Razorpay subscription verification wiring. Follow-up gates passed: `node --check scripts/verification/verify-agent-readiness.js`, `npm run verify:agent-readiness`, a targeted stale-status scan for the selected auth-onboarding docs, focused `git diff --check`, `npm run verify:doc-npm-scripts`, `npm run docs:check-links`, full `git diff --check`, and `npm run verify:production-readiness-local` with 67/67 checks, including 63 child root `verify:*` scripts. No auth runtime behavior, middleware, Firestore rules, Firebase Auth user/token creation, Razorpay provider call, Firebase deploy, Vercel deploy, production build, browser QA, Firestore write, Storage write, or external certification gate changed for this checkpoint.

Projects assessment-doc launch-boundary checkpoint: the active Projects assessment docs under `__docs__/projects/Assessments/` for upload, AI extraction, Data Editor, performance, security, UX, AI image generation, image editing, description generation, B2B view, B2C view, project management, Multi-Language Translation, menu job queue implementation, and the server-side data processing ADR no longer present their November/December 2025 assessment metadata, section statuses, footer verdicts, or architecture recommendations as current production certification. They now keep the historical assessment and architecture evidence, but route current release approval to this audit, External Certification Runbook evidence, upload/job/browser/mobile QA, AI accounting/source gates, Digital Menu Output Constitution checks where relevant, provider smoke, Storage rules/deploy evidence, API/export security review, CRUD/tenant-isolation QA, public cache evidence for publish-state writes, Cloud Function/deploy evidence where relevant, and target-environment smoke. `npm run verify:agent-readiness` now rejects the stale Projects assessment "Production Ready" metadata, launch-immediate footer, post-deploy production-ready claim, queue implementation status, and architecture ADR production-ready implementation claim. Follow-up gates passed: `node --check scripts/verification/verify-agent-readiness.js`, `npm run verify:agent-readiness`, a targeted stale-status scan for the selected active docs, focused `git diff --check`, `npm run docs:check-links`, and `npm run verify:production-readiness-local` with 67/67 checks, including 63 child root `verify:*` scripts. No upload, extraction, editor, performance, security, UX, image-generation, image-editing, description-generation, B2B, B2C, project-management, translation, queue, or server-side data-processing runtime behavior, Firebase deploy, Vercel deploy, production build, provider smoke, browser QA, Firestore write, Storage write, or external certification gate changed for this checkpoint.

Security perimeter guide launch-boundary checkpoint: `__docs__/security/app-check/complete-guide.md`, `__docs__/security/file-upload/file-upload-security.md`, `__docs__/security/webhook/webhook-security.md`, `__docs__/security/cors/cors-implementation.md`, `__docs__/security/cors/cors-implementation-complete.md`, `__docs__/security/monitoring/complete-guide.md`, `__docs__/security/api-security/api-security-status.md`, `__docs__/security/owasp/owasp-security-implementation.md`, and `__docs__/security/comprehensive-security-audit.md` no longer present implementation, checklist, CORS completion, API-security, OWASP, or historical audit notes as current production certification. They now route current release approval to this audit, External Certification Runbook evidence, current App Check/environment setup verification, provider token smoke, upload/browser/mobile QA, Storage rules/deploy evidence where changed, provider webhook smoke, secret setup evidence, replay/idempotency evidence for payment-affecting handlers, allowed-origin review, target-domain browser/API smoke, CORS failure-mode evidence, target Sentry/alert destination verification, security-event smoke, current API route inventory, OWASP gap review for touched routes, and current source review against mandatory security rules. `npm run verify:agent-readiness` now rejects stale security perimeter guide production-ready headers, fully-implemented headers, App Check launch progression, direct "don't launch" instruction, monitoring 100% coverage claim, CORS production-ready signoff, API "enterprise-grade" signoff, OWASP production-ready footer, and the comprehensive audit's production-ready heading/current-ready status. Follow-up gates passed: `node --check scripts/verification/verify-agent-readiness.js`, `npm run verify:agent-readiness`, a targeted stale-status scan for the selected active security docs, and focused `git diff --check`. No auth/security runtime behavior, middleware, Firestore rules, Storage rules, Firebase deploy, Vercel deploy, production build, provider smoke, browser QA, Firestore write, Storage write, or external certification gate changed for this checkpoint.

Security guide launch-boundary checkpoint: `__docs__/security/authentication/complete-guide.md`, `__docs__/security/email-validation/complete-guide.md`, `__docs__/security/object-sanitization-pattern.md`, `__docs__/security/security_firebase.md`, and `__docs__/security/login-source-tracking.md` no longer present their November 2025 or February 2026 implementation/cost notes as current production certification. They now keep the security-source and Firebase cost evidence, but route current release approval to this audit, External Certification Runbook evidence, current auth/security source review against mandatory security rules, QA/staging index or deploy evidence where relevant, auth browser/API smoke, privacy-safe logging review, source usage review for sanitization, and cost monitoring for any security path that writes Firebase data. `npm run verify:agent-readiness` now rejects stale security-guide production-ready headers, deployment-ready summary bullets, zero-gap claims, production-standard footers, and production-active footers on those active docs. Follow-up gates passed: `node --check scripts/verification/verify-agent-readiness.js`, `npm run verify:agent-readiness`, a targeted stale-status scan for the selected active docs, and focused `git diff --check`. No auth/security runtime behavior, middleware, Firestore rules, Firebase deploy, Vercel deploy, production build, provider smoke, browser QA, Firestore write, Storage write, or external certification gate changed for this checkpoint.

Network status monitoring launch-boundary checkpoint: `__docs__/features/network-status-monitoring.md` no longer presents its November 2025 implementation note as current production certification. The doc now keeps its source evidence for `useNetworkStatus`, `NetworkStatusProvider`, the main MenuList owner layout, and the CampaignCue protected layout, but routes release approval to this audit, External Certification Runbook evidence, browser/device QA for offline and slow-network behavior, target-shell smoke for every product that mounts the provider, and deploy evidence for the target environment. `npm run verify:agent-readiness` now rejects the stale Network Status "Production Ready" status and launch-certification footer, and source-gates the current hook/provider/layout wiring. Follow-up gates passed: `node --check scripts/verification/verify-agent-readiness.js`, `npm run verify:agent-readiness`, `npm run verify:doc-npm-scripts`, `npm run docs:check-links`, focused `git diff --check`, and `npm run verify:production-readiness-local` with 67/67 checks, including 63 child root `verify:*` scripts. No network-status runtime behavior, owner-app shell behavior, CampaignCue shell behavior, Firebase deploy, Vercel deploy, production build, provider smoke, browser QA, Firestore write, Storage write, or external certification gate changed for this checkpoint.

Menu editing, translated output, and recommendation/campaign doc-boundary checkpoint: `__docs__/projects/b2c-view/README.md`, `b2c-view_spec.md`, `b2c-view_impl.md`, `b2c-view_marketing.md`, `__docs__/projects/Assessments/assessment-11-b2c-view.md`, `__docs__/projects/data-editor/README.md`, `data-editor_spec.md`, `data-editor_impl.md`, `data-editor_firebase.md`, `data-editor_marketing.md`, `__docs__/projects/Assessments/assessment-03-editor.md`, `__docs__/projects/multi-language-translation/multi-language-translation_spec.md`, `multi-language-translation_impl.md`, `__docs__/social-content/social-content_impl.md`, and `__docs__/decision-intelligence/decision-intelligence_marketing.md` no longer present their source, marketing, cost, or historical assessment evidence as current production certification. They now route release approval to this audit, External Certification Runbook evidence, Digital Menu Output Constitution checks where relevant, browser/mobile customer-menu and editor QA, translated-menu fallback/RTL evidence, publish/cache evidence for edited public truth, public cache/deploy evidence, campaign/provider smoke where enabled, scheduler evidence for Decision Intelligence, and target production smoke. `npm run verify:agent-readiness` now rejects stale production-ready statuses and ready-for-use footers across these active docs. Follow-up gates passed: `node --check scripts/verification/verify-agent-readiness.js`, `npm run verify:agent-readiness`, a targeted stale-status scan for the active docs, and focused `git diff --check`. No runtime behavior, Firebase deploy, Vercel deploy, production build, provider smoke, browser QA, Firestore write, Storage write, or external certification gate changed for this checkpoint.

Description generation launch-boundary checkpoint: `__docs__/projects/description-generation/README.md`, `description-generation_spec.md`, `description-generation_impl.md`, `description-generation_firebase.md`, `description-generation_production-audit.md`, and `__docs__/projects/Assessments/assessment-09-description-generation.md` no longer present historical source, cost, audit, or assessment evidence as current production deployment approval. They now route release approval to this audit, External Certification Runbook evidence, target feature-flag/provider review, AI accounting/source gates, provider smoke, browser/mobile editor QA, and target-environment deploy evidence. `npm run verify:agent-readiness` now rejects stale description-generation "Production Ready" headers, footer verdicts, production-audit GO verdicts, the residual production-ready score-table verdict, and assessment signoffs. Follow-up gates passed: `node --check scripts/verification/verify-agent-readiness.js`, `npm run verify:agent-readiness`, a targeted stale description-generation production-audit scan, focused `git diff --check`, `npm run docs:check-links`, and `npm run verify:production-readiness-local` with 67/67 checks, including 63 child root `verify:*` scripts. No description-generation runtime behavior, Firebase deploy, Vercel deploy, production build, provider smoke, browser QA, Firestore write, Storage write, or external certification gate changed for this checkpoint.

Upload and AI extraction source-doc boundary checkpoint: `__docs__/projects/upload-file-processing/upload-file-processing_spec.md`, `__docs__/projects/upload-file-processing/upload-file-processing_impl.md`, `__docs__/projects/ai-data-extraction/ai-data-extraction_spec.md`, `__docs__/projects/ai-data-extraction/ai-data-extraction_impl.md`, and `__docs__/projects/ai-data-extraction/chaos-failure-simulation-audit.md` no longer present historical upload/extraction documentation as current production certification. Upload processing now routes release approval to this audit, External Certification Runbook evidence, target deploy evidence, browser/mobile upload QA, Storage quota/rules evidence, and extraction-job evidence. AI extraction source docs and the chaos audit now route release approval to this audit, External Certification Runbook evidence, resolved QA Firebase Functions/Storage deploy blockers, provider smoke, authenticated browser/mobile QA, and production-host smoke. `npm run verify:menu-extraction-pipeline` now rejects the stale upload/extraction "production ready" statuses and the old chaos-audit production-ready verdict. Follow-up gates passed: `node --check scripts/verification/verify-menu-extraction-pipeline.js`, `npm run verify:menu-extraction-pipeline` with 169/169 checks, `npm run verify:agent-readiness`, and focused `git diff --check`. No runtime behavior, Firebase deploy, Vercel deploy, production build, provider smoke, browser QA, Firestore write, Storage write, or external certification gate changed for this checkpoint.

Client menu launch-boundary checkpoint: `__docs__/client-menu/README.md`, `__docs__/client-menu/_spec.md`, `__docs__/client-menu/_impl.md`, `__docs__/client-menu/client-menu_firebase.md`, `__docs__/client-menu/autosell-features/autosell-features_firebase.md`, `__docs__/client-menu/analytics-tracking/analytics-tracking_firebase.md`, and `__docs__/projects/menu-editor/b2c-menu-layout-constitution-implementation.md` no longer present their customer-facing menu documentation or Firebase cost notes as current production certification. The docs now keep their menu-output/cost evidence, but route current client-menu launch approval to this audit, External Certification Runbook evidence, Digital Menu Output Constitution checks, physical/mobile browser QA, low-bandwidth/offline/back-button tests, public cache/deploy evidence, analytics delivery checks where relevant, and target production smoke. `npm run verify:agent-readiness` now rejects stale client-menu "Production Ready" headers, old document-status verdicts, and the old "ONLY verification criteria before declaring production-ready" sentence. Follow-up gates passed: `node --check scripts/verification/verify-agent-readiness.js`, `npm run verify:agent-readiness`, `npm run verify:doc-npm-scripts`, `npm run docs:check-links`, `git diff --check`, and `npm run verify:production-readiness-local` with 67/67 checks, including 63 child root `verify:*` scripts. No customer-menu runtime behavior, public route behavior, public cache behavior, Firebase deploy, Vercel deploy, production build, provider smoke, browser QA, Firestore write, Storage write, or external certification gate changed for this checkpoint.

Projects documentation hub launch-boundary checkpoint: `__docs__/projects/README.md`, `__docs__/projects/00-overview.md`, `__docs__/projects/14-utilities.md`, and `__docs__/projects/owner-dashboard.md` no longer label the Projects documentation hub/root references as current production-ready launch approval. The hub now marks feature folders as implemented source documentation, treats assessment docs as historical readiness evidence, and routes release readiness to this audit, the active production-readiness checklist, and the External Certification Runbook. `npm run verify:agent-readiness` now rejects the stale Projects hub/root "Production Ready" labels and assessment-table launch verdicts. Follow-up gates passed: `node --check scripts/verification/verify-agent-readiness.js`, `npm run verify:agent-readiness`, and focused `git diff --check`. No runtime behavior, Firebase deploy, Vercel deploy, production build, provider smoke, browser QA, Firestore write, or Storage write changed for this checkpoint.

Multi-Language translation launch-boundary checkpoint: `__docs__/projects/multi-language-translation/multi-language-translation_verification.md` and `__docs__/projects/Assessments/assessment-13-multi-language-translation.md` no longer present their 2025/2026 completed-fix evidence as current production deployment approval. They now state that translated menu release approval still requires this audit, External Certification Runbook evidence, target feature-flag/provider review, deploy evidence, and browser/mobile QA for translated menu flows. `npm run verify:agent-readiness` now rejects the stale Multi-Language "production-ready" conclusions. Follow-up gates passed: `node --check scripts/verification/verify-agent-readiness.js`, `npm run verify:agent-readiness`, and focused `git diff --check`. No runtime behavior, Firebase deploy, Vercel deploy, production build, provider smoke, browser QA, Firestore write, or Storage write changed for this checkpoint.

Internal Feedback verification launch-boundary checkpoint: `__docs__/projects/internal-feedback-system/internal-feedback-system_verification.md` no longer presents its February 2026 verification report as standalone production deployment approval. The report now states that it combines the original verification with later runtime audit addenda and is source-verified Guest Feedback evidence, while current release approval still requires this audit, External Certification Runbook evidence, target feature-flag review, deploy evidence, and browser/mobile QA for the release. `npm run verify:agent-readiness` now rejects the stale "feature production-ready", "Ready for Production", and live deployment approval wording. Follow-up gates passed: `node --check scripts/verification/verify-agent-readiness.js`, `npm run verify:agent-readiness`, and focused `git diff --check`. No runtime behavior, Firebase deploy, Vercel deploy, production build, provider smoke, browser QA, Firestore write, or Storage write changed for this checkpoint.

Continuous Menu Intelligence validation launch-boundary checkpoint: `__docs__/continuous-menu-intelligence/continuous-menu-intelligence_validation.md` no longer treats the January 2026 ChatGPT feedback pass, ready-for-testing status, ship-ready status, production-quality-gate label, or "Cloud Functions deploy + SMB testing" line as current production approval. The report now identifies itself as historical implementation-validation evidence, keeps the architecture/spec-alignment result, and routes release approval to this audit, External Certification Runbook evidence, target feature-flag review, scoped `menulist-qa` deploy evidence, current scheduler behavior checks, and browser/device QA where the release uses CMI surfaces. `npm run verify:agent-readiness` now rejects the stale "ChatGPT feedback validated the implementation as production-ready" sentence, old "FINAL STATUS: READY FOR PRODUCTION" heading, ready-for-testing verdict, ship-ready statuses, production-quality-gate label, and broad deploy/SMB-testing readiness line. Follow-up gates passed: `node --check scripts/verification/verify-agent-readiness.js`, `npm run verify:agent-readiness`, `npm run docs:check-links`, focused `git diff --check`, and `npm run verify:production-readiness-local` with 67/67 checks, including 63 child root `verify:*` scripts. No scheduler code, Decision Blocks behavior, Firestore write, Firebase deploy, Vercel deploy, production build, provider smoke, browser/device QA, Storage write, or external certification gate changed for this checkpoint.

Projects historical-assessment launch-boundary checkpoint: `__docs__/projects/production-readiness-assessment.md` no longer presents its November 2025 Projects feature review as current production deployment approval. It now identifies itself as a historical assessment snapshot and routes current release approval to this audit, the active production-readiness checklist, and the External Certification Runbook. `npm run verify:agent-readiness` now rejects the stale Projects assessment "production ready", "ready for production deployment", and live-user deployment approval wording. Follow-up gates passed: `node --check scripts/verification/verify-agent-readiness.js`, `npm run verify:agent-readiness`, and focused `git diff --check`. No runtime behavior, Firebase deploy, Vercel deploy, production build, provider smoke, browser QA, Firestore write, or Storage write changed for this checkpoint.

AI extraction monitoring launch-boundary checkpoint: `__docs__/ai-extraction-monitoring/ai-extraction-monitoring_spec.md` and `__docs__/ai-extraction-monitoring/ai-extraction-monitoring_impl.md` no longer treat "feature flag OFF" as production readiness. They now state that the internal monitoring dashboard is implemented and source-verified for controlled internal testing, while release enablement still requires target feature-flag review, platform-role access verification, current extraction data, applicable External Certification Runbook evidence, and this production-readiness audit. `npm run verify:menu-extraction-pipeline` now rejects the stale flag-off production-ready wording, and `npm run verify:agent-readiness` guards the same boundary in the aggregate readiness gate. Follow-up gates passed: `node --check scripts/verification/verify-menu-extraction-pipeline.js`, `node --check scripts/verification/verify-agent-readiness.js`, `npm run verify:menu-extraction-pipeline`, `npm run verify:agent-readiness`, and focused `git diff --check`. No runtime behavior, Firebase deploy, Vercel deploy, production build, provider smoke, browser QA, Firestore write, or Storage write changed for this checkpoint.

AI image-generation verification boundary checkpoint: `__docs__/projects/ai-image-generation/ai-image-generation_verification.md` no longer says the feature is production-ready as a current launch verdict. The report now states that code-side hardening is complete for the reviewed scope and controlled-owner-testing ready, while full certification remains gated by provider smoke, browser/device QA, app/Vercel deployment for Next API changes, Firebase scheduler deployment for retention cleanup, live Storage cleanup, production-host evidence, the External Certification Runbook, and this audit. `npm run verify:ai-accounting` now source-gates that wording and rejects the stale production-ready claims. Follow-up gates passed: `node --check scripts/verification/verify-ai-accounting-hardening.js`, `npm run verify:ai-accounting`, and focused `git diff --check`. No runtime behavior, Firebase deploy, Vercel deploy, production build, provider smoke, browser QA, Firestore write, or Storage write changed for this checkpoint.

AI extraction historical-audit boundary checkpoint: the March 13 AI Data Extraction audit files under `__docs__/projects/ai-data-extraction/` now explicitly identify themselves as historical code-readiness evidence, not current MenuList launch certification. The old unconditional "ready for production" verdicts were replaced with current-gate language that points launch authority to the External Certification Runbook and this audit, including blocked QA Firebase Functions/Storage deploys, provider smoke, authenticated browser/device QA, and production-host evidence. `npm run verify:menu-extraction-pipeline` now source-gates those historical-audit boundaries and passed with 156/156 checks. Follow-up gates passed: `node --check scripts/verification/verify-menu-extraction-pipeline.js`, `npm run verify:menu-extraction-pipeline`, and focused `git diff --check`. No runtime behavior, Firebase deploy, Vercel deploy, production build, provider smoke, browser QA, Firestore write, or Storage write changed for this checkpoint.

Gate 1 Functions handoff alignment checkpoint: the External Certification Runbook now includes narrow retry commands for the July 2 source-file path hardening subset, menu extraction SAFE_MODE worker guard, and Founder Monitor scheduler slice when the full Functions blocker set is not being retried. The production readiness checklist, launch prerequisites, and owner action tracker now summarize the latest documented `menulist-qa` source-file path hardening subset, `processMenuImagesJob`, and scheduler deploy attempts together: each completed predeploy lint/build and then failed before upload with Cloud Resource Manager HTTP 403 caller permission. `npm run verify:agent-readiness` now guards the subset notes, scoped retry commands, and current deploy-blocker wording. Follow-up gates passed: `node --check scripts/verification/verify-agent-readiness.js`, `npm run verify:agent-readiness`, `npm run docs:check-links`, focused `git diff --check`, and `npm run verify:production-readiness-local` with 67/67 checks, including 63 child root `verify:*` scripts. No deploy or runtime behavior changed.

Feature flag launch-boundary checkpoint: `__docs__/production-readiness/dev-prod-environment-guide.md` and `__docs__/ops-infrastructure-guide.md` no longer present operational flags as blanket dev/prod toggles or a direct go-live ritual. They now treat `src/config/features.ts` as the code-level source of truth, require target-specific provider/setup/deploy evidence before production use, and keep launch approval tied to External Certification Runbook evidence instead of the environment or ops guide alone. `npm run verify:agent-readiness` now rejects the stale "turn these flags true" instruction, the old production go-live heading, the direct go-live approval wording, and the ops guide's stale all-flags-off source-state claim. Follow-up gates passed: `npm run verify:agent-readiness`, `npm run verify:doc-npm-scripts`, `npm run docs:check-links`, focused `git diff --check`, and `npm run verify:production-readiness-local` with 67/67 checks, including 63 child root `verify:*` scripts. No runtime feature flag change, provider smoke, browser QA, Firebase deploy, Vercel deploy, production build, production-host smoke, or external certification gate was performed for this checkpoint.

Environment handoff launch-boundary checkpoint: the active dev/prod environment guide no longer tells operators to copy `.env.local` or legacy `.env.prod` wholesale into Vercel, and no longer lists unscoped `firebase functions:secrets:set ...` commands as production-ready handoff steps. It now points Vercel setup to `.env.staging.example` and `.env.production.example`, treats `.env.prod` as a legacy local file until rebuilt from canonical templates, routes MenuList Functions secret setup to QA-first commands with `--project menulist-qa`, and requires QA evidence plus explicit production approval before production secrets or production infrastructure deploys. `npm run verify:agent-readiness` now rejects the stale `.env.prod` copy instructions and unscoped secret commands from this active guide. Follow-up gates passed: `npm run verify:agent-readiness`, `npm run verify:doc-npm-scripts`, `npm run docs:check-links`, focused `git diff --check`, and `npm run verify:production-readiness-local` with 67/67 checks, including 63 child root `verify:*` scripts. No env values, secrets, Firebase project state, Vercel settings, Firebase deploy, Vercel deploy, production build, provider smoke, browser QA, or external certification gate was changed for this checkpoint.

Launch prerequisites secret-scope checkpoint: the top-level Telegram and SMTP setup steps in `__docs__/production-readiness/launch-prerequisites.md` now match the later platform-alert QA-first contract instead of showing unscoped `firebase functions:secrets:set` commands. Telegram and SMTP secrets are documented as QA-first with `--project menulist-qa`; production values require QA delivery evidence and explicit production secret approval. `npm run verify:agent-readiness` now rejects unscoped Telegram and SMTP secret commands in this active launch guide while preserving the scoped QA commands. Follow-up gates passed: `npm run verify:agent-readiness`, `npm run verify:doc-npm-scripts`, `npm run docs:check-links`, focused `git diff --check`, and `npm run verify:production-readiness-local` with 67/67 checks, including 63 child root `verify:*` scripts. No secrets, Firebase project state, provider delivery, Firebase deploy, Vercel deploy, production build, browser QA, or external certification gate was changed for this checkpoint.

Feature flag checklist evidence checkpoint: `__docs__/production-readiness/launch-prerequisites.md` and `__docs__/owner-action-items.md` no longer list "Enable feature flags", "Enable monitoring feature flags", "Enable production feature flags in order", or "3 lines in features.ts" as active launch tasks. The active rows now require current `src/config/features.ts` source-state review plus target secrets/provider setup, scoped deploy evidence, provider smoke where applicable, External Certification Runbook records, and explicit production approval. `npm run verify:agent-readiness` guards those active checklist rows and rejects the stale blanket activation wording. Follow-up gates passed: `npm run verify:agent-readiness`, `npm run verify:doc-npm-scripts`, `npm run docs:check-links`, focused `git diff --check`, and `npm run verify:production-readiness-local` with 67/67 checks, including 63 child root `verify:*` scripts. No runtime flag values, secrets, Firebase project state, provider smoke, Firebase deploy, Vercel deploy, production build, browser QA, or external certification gate was changed for this checkpoint.

Owner Gemini rotation handoff checkpoint: `__docs__/owner-action-items.md` no longer shows unscoped `firebase functions:secrets:set GEMINI_AI_KEY_2/3/4` commands or a broad "Redeploy CF + Vercel" instruction. Optional rotation keys now route to QA first with `--project menulist-qa`, Vercel Preview values, `npm run verify:functions-deploy-preflight`, and External Certification Gate 1 before any production repeat. Production rotation values require QA evidence plus explicit production secret/deploy approval. `npm run verify:agent-readiness` now guards the scoped QA commands, production approval boundary, and stale unscoped Gemini/deploy wording. Follow-up gates passed: `npm run verify:agent-readiness`, `npm run verify:doc-npm-scripts`, `npm run docs:check-links`, focused `git diff --check`, and `npm run verify:production-readiness-local` with 67/67 checks, including 63 child root `verify:*` scripts. No Gemini key, secret, Vercel env value, Firebase project state, provider smoke, Firebase deploy, Vercel deploy, production build, browser QA, or external certification gate was changed for this checkpoint.

Ops and messaging secret-scope checkpoint: active Telegram alert setup docs (`__docs__/ops-alerting-delivery/ops-alerting-delivery_impl.md` and `__docs__/ops-infrastructure-guide.md`) and the WhatsApp messaging onboarding implementation doc now use QA-first Firebase Functions secret commands with `--project menulist-qa`. Production repeats require QA alert/provider-smoke evidence and explicit production secret approval instead of unscoped or direct production handoff commands. `npm run verify:agent-readiness` now guards the scoped QA commands, production approval wording, and stale unscoped Telegram/WhatsApp secret commands across those active docs. Focused gate passed: `npm run verify:agent-readiness`; a stale-command scan found no active unscoped Telegram or WhatsApp setup commands in the patched docs. No Telegram bot value, WhatsApp credential, Secret Manager value, Firebase project state, provider smoke, Firebase deploy, Vercel deploy, production build, browser QA, or external certification gate was changed for this checkpoint.

Security/trust historical-audit boundary checkpoint: `__docs__/features/trust-security-page.md` and `__docs__/security/comprehensive-security-audit.md` no longer present November 2025 page-build or security-audit notes as current MenuList production-launch certification. The active wording now identifies those docs as historical implementation/security evidence and routes current approval to this audit, the External Certification Runbook, current App Check/provider/deploy evidence, and browser/device QA. `npm run verify:agent-readiness` now guards the historical-evidence wording and rejects the stale "COMPLETE & PRODUCTION READY", "Ready for Production: YES", "production-ready React/TypeScript", and "APPROVED FOR PRODUCTION" claims in those active docs. Focused gates passed: `npm run verify:agent-readiness`, `npm run docs:check-links`, and focused `git diff --check`. No security code, auth behavior, App Check config, provider smoke, Firebase deploy, Vercel deploy, production build, browser QA, or external certification gate was changed for this checkpoint.

Menu extraction SAFE_MODE worker checkpoint: `processMenuImagesJob` now checks `isSafeModeActive()` after winning the pending-job idempotency transaction and before public draft processing, deterministic link parsing, or Gemini extraction. When SAFE_MODE is active, the worker logs stable `PROCESS_MENU_IMAGES_JOB_SAFE_MODE_ACTIVE` diagnostics, marks the job failed with retryable `SAFE_MODE_ACTIVE`, persists fixed owner-safe pause copy with `retryAfterSeconds: 60`, and updates public draft extraction state with fixed retry copy instead of spending provider budget or leaving the job stuck in `processing`. `npm run verify:menu-extraction-pipeline` now guards the import, failed-job shape, and ordering before provider work. Follow-up gates passed: `node --check scripts/verification/verify-menu-extraction-pipeline.js`, `npm run verify:menu-extraction-pipeline` (150/150 checks), `npm --prefix functions run build`, `npm run verify:functions-deploy-preflight`, `npx tsc --noEmit --incremental false --pretty false`, `npm run verify:agent-readiness`, `npm run docs:check-links`, focused `git diff --check`, and `npm run verify:production-readiness-local` with 67/67 checks, including 63 child root `verify:*` scripts. The scoped MenuList QA Functions deploy for `functions:processMenuImagesJob` reached predeploy lint/build and then failed before upload with Cloud Resource Manager HTTP 403 caller permission for `menulist-qa`. Without a successful deploy, live Firebase Functions behavior is unchanged. No live SAFE_MODE toggle, budget webhook test, provider smoke, Firestore write, Storage write, Vercel deploy, Next.js production build, or production-host certification was performed for this checkpoint.

Aggregate local readiness output boundary checkpoint: `npm run verify:production-readiness-local` now prints an explicit local-only boundary at start and summary: it is a local source gate and does not run a Next.js production build, Firebase deploy, Vercel deploy, provider smoke, browser/device QA, live Firestore/Storage writes, or production-host behavior. `npm run verify:agent-readiness` guards this runner output text, the verification README, and the feature-sweep report recycle-bin row so aggregate green output cannot be mistaken for production certification or manual/browser QA evidence.

Store update acknowledgement source-gate checkpoint: `npm run verify:public-business-truth` now scans every active `src` `updateStore()` call and fails unless `assertStoreUpdateSucceeded()` appears in the nearby follow-up block before local success state can be trusted. This turns the existing owner/public-truth acknowledgement discipline into a generic regression gate across desktop and mobile store writes, instead of relying only on hand-picked call-site assertions. Follow-up gates passed: `node --check scripts/verification/verify-public-business-truth.js`, `npm run verify:public-business-truth`, and focused `git diff --check`. No app runtime behavior, Firestore write, Storage write, Firebase deploy, Vercel deploy, browser QA, or production-host behavior changed for this checkpoint.

Production testing guide boundary checkpoint: `__docs__/production-testing-guide.md` no longer presents the January manual checklist as production-launch approval. It now identifies itself as a companion manual spot-check guide, points active launch authority to the External Certification Runbook and this audit, requires `npm run verify:production-readiness-local` before manual evidence collection, and replaces the stale instruction to force all feature flags on with target-environment feature-flag review and audit evidence. `npm run verify:agent-readiness` now rejects the old all-flags instruction, final production checklist heading, and production-ready sign-off language. Follow-up gates passed: `npm run verify:agent-readiness`, `npm run verify:doc-npm-scripts`, `npm run docs:check-links`, focused `git diff --check`, and `npm run verify:production-readiness-local` with 67/67 checks, including 63 child root `verify:*` scripts. No manual browser QA, provider smoke, Firebase deploy, Vercel deploy, production build, physical-device QA, or external certification gate was performed for this checkpoint.

Public menu rate-limit fail-closed checkpoint: the shared app rate limiter now supports an opt-in `failClosedOnProviderError` mode while preserving the default fail-open behavior for the wider app. Public menu setup and draft claim routes use the strict mode, so `/api/public/create-menu` and `/api/public/create-menu/claim` return fixed temporary-unavailable copy with `503` and `Retry-After` instead of continuing into AI extraction, Firestore writes, Storage writes, or tenant/project claim work when the Upstash provider is unavailable. `npm run verify:auth-security-failure-matrix` now guards the strict provider-unavailable result shape, and `npm run verify:menu-extraction-pipeline` guards the public menu setup/claim opt-in. Follow-up gates passed: `npm run verify:auth-security-failure-matrix`, `npm run verify:menu-extraction-pipeline` (149/149 checks), `npx tsc --noEmit --incremental false --pretty false`, focused `git diff --check`, and `npm run verify:production-readiness-local` with 67/67 checks, including 63 child root `verify:*` scripts. No Upstash live call, public upload browser QA, Firestore write, Storage write, Firebase deploy, Vercel deploy, Next.js production build, provider call, or external certification gate was performed for this checkpoint.

Dependency freeze source-gate checkpoint: root app, MenuList Functions, and Answerlattice Functions `package.json` declarations are now pinned to their existing lockfile-resolved versions instead of floating semver ranges. `npm run verify:dependency-freeze` guards exact package declarations, root lockfile metadata parity, resolved `node_modules/*` package versions, the current core runtime set, and the current mobile UI dependency boundary where no `antd-mobile` runtime imports may exist unless the dependency is present. The same verifier now rejects stale frozen-stack strings in active rule/memory surfaces including `AGENTS.md`, `.windsurfrules`, `.codex/rules/*`, and the project-memory docs, so repo instructions no longer point at retired Next.js, Ant Design, Firebase, or OpenAI package versions. This preserves the tested current runtime instead of silently downgrading packages to older governance text. Focused gates passed: `npm run verify:dependency-freeze`, `npm run verify:agent-readiness`, and `npm run verify:doc-npm-scripts`. The full local aggregate then passed with 67/67 checks, including 63 child root `verify:*` scripts. No dependency was added, removed, or upgraded; no `npm install`, Next.js production build, Firebase deploy, Vercel deploy, provider smoke, browser smoke, Firestore write, or Storage write was performed for this checkpoint.

Recycle-bin verifier output boundary checkpoint: `npm run verify:recycle-bin` no longer prints "Ready for manual testing" or dev-server next steps inside the aggregate local-readiness path. It now reports a source-gate pass and explicitly states that authenticated browser smoke, visual QA, live Firestore reads/writes, Firebase deploy, Vercel deploy, and production-host behavior are not covered. `npm run verify:agent-readiness` guards that output boundary so aggregate verifier text cannot be mistaken for a manual/browser certification pass. No runtime support-ticket behavior, Firestore write, Firebase deploy, Vercel deploy, browser smoke, production build, or production-host behavior changed for this checkpoint.

System Strengthening source-gate checkpoint: `removePresetFromAllCategories()` now stages modified project documents in Firestore batches and revalidates public cache after commit, closing the remaining SS-8 sequential-write gap. The client subscription grace-expiry comment now matches current runtime behavior: browser reads do not mutate billing docs, while server-owned expiry readers perform the authoritative status transition and entitlement sync. `npm run verify:system-strengthening` now source-gates SS-1 through SS-9, covering analytics route auth/property scoping/rate limiting, stale DAL session-cache removal, batch image worker admission and accounting, `/api/screen/seen` hashed limiter ordering, AI route throttles/body bounds/permissions/capacity/accounting, accepted chat feedback write shape, billing read/write boundaries, preset cascade batching, and the no-console API/DAL boundary. Follow-up gates passed: `npm run verify:system-strengthening`, `npm run verify:doc-npm-scripts`, `npm run docs:check-links`, `npx tsc --noEmit --incremental false --pretty false`, `git diff --check`, and `npm run verify:production-readiness-local` with 67/67 checks, including 63 child root `verify:*` scripts. No Firebase deploy, Vercel deploy, Next.js production build, browser/device QA, provider call, Cloud Tasks enqueue, live Firestore write, Storage write, or external certification gate was performed for this checkpoint.

External-gate preflight alignment checkpoint: External Certification Gate 5 now requires `npm run verify:messaging-onboarding-monitor-boundary` and `npm run verify:menu-extraction-pipeline`, so local WhatsApp preflight covers platform-only ops visibility, preview/fix/publish source gates, messaging extraction destination routing, public cache-tag writes, and bounded diagnostics before live Meta testing. External Certification Gate 6 now uses the maintained `npm run verify:pos-sync-boundary` source gate instead of an ad hoc inline POS URL probe. This keeps public-HTTPS validation, DNS/private-target blocking, signed payload source coverage, desktop/mobile owner-safe failure copy, MobileShell routing, and acknowledgement guards tied to the same verifier that protects the POS feature docs and audit slice. The same pass restored Gate 1 checklist wording in the production readiness checklist and launch prerequisites: run `npm run verify:functions-deploy-preflight` before the scoped Firebase deploy retry, restore `menulist-qa` access, then retry the documented scoped target set. `npm run verify:agent-readiness` now requires the Gate 5 messaging source gates, rejects the old inline POS probe from the runbook, and requires the maintained Gate 6 preflight wording. Follow-up gates passed: `npm run verify:messaging-onboarding-monitor-boundary`, `npm run verify:menu-extraction-pipeline` (149/149 checks), `npm run verify:pos-sync-boundary`, `npm run verify:agent-readiness`, `npm run docs:check-links`, and `npm run verify:production-readiness-local` with 65/65 checks, including 61 child root `verify:*` scripts. No Meta/WhatsApp provider call, external POS provider call, webhook delivery, Firestore write, Firebase deploy, Vercel deploy, production build, or Gate 1/Gate 5/Gate 6 certification pass was performed for this checkpoint.

Subscription status type parity checkpoint: `/api/razorpay/verify-subscription` already returns for subscriptions that are active before the activation write path. The later payment-verified path is therefore only the non-active-to-active transition path, so the redundant `internalSub.status !== 'active'` guard around founder new-MRR recording was removed instead of weakening the shared `PaymentStatus` model. Follow-up gates passed: `npx tsc --noEmit --incremental false --pretty false`, `npm run verify:billing-entitlement-boundary`, `npm run verify:menulist-api-tenant-safety`, `npm run verify:auth-security-failure-matrix`, and `npm run verify:production-readiness-local` with 65/65 checks, including 61 child root `verify:*` scripts. No Razorpay provider call, real payment verification, Firestore write, Firebase deploy, Vercel deploy, production build, or provider configuration change was performed for this checkpoint.

External certification harness syntax-gate checkpoint: `npm run verify:agent-readiness` now runs `node --check` for the three external-only harnesses that remain outside the default local aggregate because their live runs require authenticated browser fixtures, upload media, live Firebase/Admin access, target data review, or backfill-specific Firestore reads: `verify-mobile-owner-menu.mjs`, `verify-mobile-upload-extraction.mjs`, and `verify-public-routing-summary-backfill.mjs`. The External Certification Runbook now lists the same syntax preflight before live gate execution. Focused source gates passed: `node --check scripts/verification/verify-mobile-owner-menu.mjs`, `node --check scripts/verification/verify-mobile-upload-extraction.mjs`, and `node --check scripts/verification/verify-public-routing-summary-backfill.mjs`. This is a parse/source gate only; it does not launch Chrome, authenticate an owner, read Firestore, upload media, review target data, run a provider smoke, deploy Firebase, deploy Vercel, run a production build, or certify any external gate.

Functions source-file path hardening checkpoint: the MenuList Functions package now has a shared `safeTempFile` helper for local `/tmp` paths. The helper strips traversal tokens, path separators, unsafe basename characters, and leading dots before creating unique temp paths. `processMenuImages` and the shared KB source upload path now use this helper instead of placing owner/source filenames directly in `/tmp`; the shared KB upload path also attempts cleanup in `finally` so failed Gemini uploads do not leave the local temp file unattempted. Shared KB generation now passes the job `tId` / `sId` into the source upload helper, and the helper rejects persisted source paths outside `ingestion_source_files/{tId}/{sId}/` before any Storage download. The source prompt metadata now uses the typed `downloadURL` field instead of an untyped `url` field. The deterministic menu-link text extractor now parses Firebase Storage URLs from both supported host shapes, rejects mismatched URL/metadata paths, and requires the resolved object path to stay under `menuLinkImports/{tId}/{sId}/{projectId}/` before Storage download. The agent-readiness stale public menu noindex source gate now checks the current `hasUnsafeProjectPath` / unresolved-project guard shape. Follow-up source gates: `node --check scripts/verification/verify-answerlattice-runtime-truth.js`, `node --check scripts/verification/verify-menu-extraction-pipeline.js`, `npm run verify:answerlattice-runtime-truth`, `npm run verify:menu-extraction-pipeline` (149/149 checks), `npm run verify:agent-readiness`, `npm run verify:functions-deploy-preflight`, `npx tsc --noEmit --incremental false`, focused `git diff --check`, and `npm run verify:production-readiness-local` passed; the aggregate passed with 65/65 checks, including 61 child root `verify:*` scripts. The scoped MenuList QA Functions deploy for `functions:processMenuImages,functions:processMenuImagesJob,functions:startGeneration,functions:embedArticleWorker,functions:regenerateEmbedding` was retried after local build verification; the final retry completed predeploy lint/build and then failed before upload with Cloud Resource Manager HTTP 403 caller permission for `menulist-qa`. Live effect remains blocked until an account with project access can pass External Certification Gate 1. No provider smoke, live Storage write, Firestore read/write, Firestore rule/index change, Vercel deploy, production build, successful Firebase deploy, owner/customer UI, public route, or production-host behavior changed for this checkpoint.

Force republish URL fail-closed checkpoint: the MenuList Functions force-republish callable no longer derives subdomain public URLs from stale defaults such as `app.menulist.online` or parse-fallback `menulist.online`. It now requires configured HTTPS `NEXT_PUBLIC_APP_URL` for subdomain stores, rejects missing/non-HTTPS/localhost/malformed values before touching the project document, and no longer reports `success: true` with `verification: 'skipped'` when public URL verification cannot run. Follow-up source gates: `npm run verify:menulist-api-tenant-safety`, `npm run verify:functions-deploy-preflight`, and `npm run verify:production-readiness-local` passed; the aggregate passed with 64/64 checks, including 60 child root `verify:*` scripts. Scoped MenuList QA Functions deploys were attempted after validation for the current Gate 1 target list and the changed `functions:forceRepublish` target; both reached predeploy lint/build and then failed before upload with Cloud Resource Manager HTTP 403 caller permission for `menulist-qa`. Live effect remains blocked until an account with project access can pass External Certification Gate 1. No provider smoke, Storage write, Firestore rule/index change, Vercel deploy, production build, successful Firebase deploy, or production-host behavior changed for this checkpoint.

Functions test config secret-hygiene checkpoint: `functions/functions-test.html` is now placeholder-only and no longer contains committed Firebase web-config-shaped values. `npm run verify:auth-security-failure-matrix` now rejects Firebase API-key-shaped literals in that local test page, and a focused key-pattern scan across `functions/functions-test.html`, `functions/src`, `src`, and `scripts/verification` returned no matches. No runtime Function logic, Firebase read/write, Storage operation, secret name, API route, owner/customer UI, public route, Firebase deploy, Vercel deploy, production build, provider call, or production-host behavior changed for this checkpoint.

Functions Sentry DSN fail-closed checkpoint: Firebase Functions Sentry initialization no longer embeds dev or production Sentry DSN fallbacks in source. When `ENABLE_SENTRY` is true, deployed Functions read `SENTRY_DSN`; local emulators may use `SENTRY_DEV_DSN`; if no DSN is configured, Sentry initialization is skipped with a bounded Firebase warning and Firebase logs remain active. Follow-up source gates: `npm run verify:auth-security-failure-matrix`, `npm run verify:env-targets`, and `npm run verify:functions-deploy-preflight` passed. The scoped `menulist-qa` Functions deploy was attempted after validation, reached predeploy lint/build, and then failed before upload with Cloud Resource Manager HTTP 403 caller permission for `menulist-qa`; live effect remains blocked until an account with project access can pass External Certification Gate 1. No provider smoke, Storage write, Firestore write, Vercel deploy, production build, successful Firebase deploy, or production-host behavior changed for this checkpoint.

Staff Prompt audit boundary checkpoint: the Staff Prompt audit wording now classifies upstream `staffPrompt` summary writing as outside the audited display slice instead of an unresolved runtime implementation gap. Current active Staff Prompt behavior is a read-only Today summary consumer of `platformSummary/campaigns_{sId}`, with no standalone generator, provider call, owner setting, mobile-only write, staff-facing route, or public Staff Prompt landing page. Follow-up source gate: `npm run verify:staff-prompt-runtime` passed. No runtime code, Firebase read/write, Cloud Function logic, provider call, Firebase deploy, Vercel deploy, production build, owner/customer UI, public route, or production-host behavior changed for this checkpoint.

Menu extraction deploy-target checkpoint: the active direct-callable hardening risk now points to the current External Certification Gate 1 `menulist-qa` deploy path instead of describing the retired `ecomsai` project as the active blocker. Historical `ecomsai` deploy attempts remain preserved as past evidence only. Current source truth remains that `processMenuImages` is disabled in code and returns `failed-precondition`; the live-effect blocker is the current scoped Functions deploy, whose latest staging attempt against `menulist-qa` reached predeploy lint/build and then failed with Cloud Resource Manager HTTP 403 caller permission before upload. Follow-up source gate: `npm run verify:menu-extraction-pipeline` passed. No runtime code, Firebase read/write, Cloud Function logic, provider call, Firebase deploy, Vercel deploy, production build, owner/customer UI, public route, or production-host behavior changed for this checkpoint.

Decision Intelligence deploy-target checkpoint: the Decision Intelligence/CMI/MCE audit slice now labels older `ecomsai` Function deploy output as historical evidence and points active retry work to External Certification Gate 1 against `menulist-qa`. Current feature docs already require `npm run verify:functions-deploy-preflight`, scoped QA deploy evidence, and explicit production deploy approval before production. Follow-up source gate: `npm run verify:agent-readiness` passed. No runtime code, Firebase read/write, Cloud Function logic, provider call, Firebase deploy, Vercel deploy, production build, owner/customer UI, public route, or production-host behavior changed for this checkpoint.

Messaging/notification deploy-target checkpoint: the Public Create-Menu/Messaging/Notifications audit slice now treats its older `ecomsai` Function deploy attempt as historical evidence and routes active retry work to the scoped `menulist-qa` deploy path in the External Certification Runbook and launch prerequisites. If the messaging/notification/platform-alert target list differs from Gate 1, the exact widened target list and reason must be recorded before deploy evidence is accepted. Follow-up source gate: `npm run verify:agent-readiness` passed. No runtime code, Firebase read/write, Cloud Function logic, provider call, Firebase deploy, Vercel deploy, production build, owner/customer UI, public route, or production-host behavior changed for this checkpoint.

Runtime production-marker checkpoint: the active runtime scan found one stale TODO marker in `src/config/features.ts` inside the Ops Alert Delivery feature-flag comment. The comment now states the disabled behavior without TODO language, and `npm run verify:auth-security-failure-matrix` now includes `src/config` in its stale TODO/FIXME/STUB production-marker scan. Follow-up source gate: `npm run verify:auth-security-failure-matrix` passed. No feature flag value, alert delivery behavior, Firebase read/write, Cloud Function logic, provider call, Firebase deploy, Vercel deploy, production build, owner/customer UI, or production-host behavior changed for this checkpoint.

AI provider configuration fail-closed checkpoint: the app-route and MenuList Cloud Functions Gemini key managers no longer create an empty-key `GoogleGenAI` client when `GEMINI_AI_KEY` is missing. The shared AI gateways now throw stable `AI_PROVIDER_CONFIG_MISSING` before provider I/O, while preserving startup safety, bounded diagnostics, key rotation, and retry behavior when real keys exist. Follow-up source gates: `npm run verify:ai-accounting`, `npm run verify:functions-deploy-preflight`, and `npm run verify:production-readiness-local` passed; the aggregate passed with 63/63 checks, including 59 child root `verify:*` scripts. The scoped `menulist-qa` Functions deploy was attempted after validation, reached predeploy lint/build, and then failed before upload with Cloud Resource Manager HTTP 403 caller permission for `menulist-qa`; live effect remains blocked until an account with project access can pass External Certification Gate 1. No provider smoke, Storage write, Firestore write, Vercel deploy, production build, successful Firebase deploy, or production-host behavior changed for this checkpoint.

Aggregate verifier coverage checkpoint: `verify:mycodex-pwa-assets`, `verify:signaldesk-security-rules`, `verify:compliance-pages-boundary`, `verify:digital-screens-boundary`, `verify:guest-feedback-boundary`, `verify:official-business-page-boundary`, `verify:platform-founder-monitor-boundary`, `verify:website-public-copy-boundary`, `verify:url-routing-boundary`, `verify:dependency-freeze`, `verify:owner-dashboard-today-boundary`, `verify:menu-project-editor-boundary`, and `verify:owner-business-health-boundary` are now exposed as root `verify:*` scripts, so `npm run verify:production-readiness-local` covers MyCodex static PWA assets, SignalDesk security-rule static checks, compliance page boundaries, Digital Screens boundary checks, Guest Feedback submission boundaries, Official Business Page public-link boundaries, Founder Monitor platform-only boundary checks, website public-copy claim boundaries, URL routing boundary checks, dependency/package freeze parity, Owner Dashboard Today route/read/docs parity, Menu Project Editor route/save/cache/mobile/docs parity by default, and Owner Business Health route/API/mobile/docs parity by default. `npm run verify:agent-readiness` guards the MyCodex and SignalDesk root aliases, the verification registry allowlist, and current aggregate evidence; `npm run verify:compliance-pages-boundary` guards the compliance page runtime/docs boundary; `npm run verify:digital-screens-boundary` guards the Digital Screens public display/owner settings/cache boundary; `npm run verify:guest-feedback-boundary` guards Guest Feedback public submit, owner inbox, Firestore, retention, and docs parity; `npm run verify:official-business-page-boundary` guards OBP action links, social links, review links, schema targets, manifest shortcuts, PWA directions/reservation/order handoffs, and docs parity; `npm run verify:platform-founder-monitor-boundary` guards the Founder Monitor route/API/DAL/UI/mobile-wrapper boundary; `npm run verify:website-public-copy-boundary` guards the mounted homepage copy, Website locale namespace blocked-claim scan, LLM context files, and documented unmounted `SmartFeaturesSection` exception; `npm run verify:url-routing-boundary` guards the MenuList routing boundary; `npm run verify:dependency-freeze` guards root and Functions dependency declarations against lockfile drift; `npm run verify:owner-dashboard-today-boundary` guards Today-first dashboard route/read/mobile/docs parity; `npm run verify:menu-project-editor-boundary` guards Projects route, editor save/publish, project DAL cache/screen invalidation, mobile persistence, mobile project mutations, and docs parity; `npm run verify:owner-business-health-boundary` guards Business Health route, API read/answer/feedback admission, bounded response parsing, MobileShell read-only behavior, removed action surfaces, and docs parity. `verify-mobile-owner-menu.mjs`, `verify-mobile-upload-extraction.mjs`, and `verify-public-routing-summary-backfill.mjs` remain external/manual gates outside the default local aggregate because they require authenticated browser fixtures, live Firebase/Admin access, target data review, or backfill-specific Firestore reads. Latest aggregate source gate: `npm run verify:production-readiness-local` passed with 78/78 checks, including 74 child root `verify:*` scripts. No Firebase deploy, Vercel deploy, Next.js production build, browser smoke, provider smoke, Razorpay call, SMTP call, WhatsApp send, external POS call, Cloud Tasks enqueue, Storage write, live Firestore write, secret change, or production-host smoke was run for this checkpoint.

Storage rules legacy-project cutover checkpoint: active project fallback uploads already write tenant-scoped Storage paths through `generateStoragePath()` as `projects/{fileType}/{tId}/{sId}/{fileId}`. The legacy `MenuListAi/project/files`, `generated`, `edited`, and `custom` rule blocks are now authenticated-read-only and explicitly deny writes/deletes, so old references can still render while new legacy project Storage growth is blocked. `npm run verify:storage-paths` now guards this source boundary by checking each legacy rule block directly instead of counting deny statements. Focused gates passed: `node --check scripts/verification/verify-storage-path-hardening.js`, `npm run verify:storage-paths`, and focused `git diff --check`. This changes `storage.rules`, so live effect requires a scoped Firebase Storage rules deploy to `menulist-qa` first, with production only after QA evidence and explicit production approval. The scoped QA deploy was attempted with `firebase deploy --project menulist-qa --config firebase.json --only storage --non-interactive`; Firebase CLI failed before rules upload while checking/enabling `firebasestorage.googleapis.com` with Service Usage HTTP 403: project `menulist-qa` not found or permission denied.

Razorpay top-up and Founder Monitor source-gate checkpoint: the top-up verification route now records founder top-up revenue movement with a hashed movement key and without copying the raw payment id into the platform read model payload. The Founder Monitor API now returns hashed movement row identifiers instead of raw movement document ids, while the billing-owned top-up ledger continues to retain provider identifiers for reconciliation. `verify:menulist-api-tenant-safety` guards the bounded top-up diagnostic shape, and `verify:platform-founder-monitor-boundary` now matches the current precomputed summary architecture instead of the retired direct collection fan-out. The consolidated scheduler wrapper also returns the Founder Monitor snapshot details in the `MaintenanceTaskResult` shape and keeps only one `founder_monitor_snapshot` task registration. Follow-up gates passed: `npm run verify:menulist-api-tenant-safety`, `npm --prefix functions run build`, `npm run verify:catalog-analytics`, `npm run verify:platform-founder-monitor-boundary`, and `npm run verify:production-readiness-local` with 65/65 checks, including 61 child root `verify:*` scripts. The scoped QA deploy was attempted with `firebase deploy --project menulist-qa --config firebase.json --only functions:menulistMaintenanceScheduler --non-interactive`; Firebase CLI completed predeploy lint/build and then failed before upload with Cloud Resource Manager HTTP 403: the caller does not have permission. No Razorpay provider call, Firestore write, Storage write, successful Firebase deploy, Vercel deploy, production build, owner/customer UI, public route, or production-host behavior changed for this checkpoint.

Compliance pages boundary source-gate checkpoint: `npm run verify:compliance-pages-boundary` now locks sanitizer script/style stripping, template composition, API admission, bounded mutation parsing, public compliance route intercepts, desktop/mobile acknowledgement guards, Firestore rule shape, and compliance docs parity. This is a local source gate only; it does not perform browser custom-domain smoke, DNS/Vercel domain verification, provider calls, Firebase deploys, Vercel deploys, production builds, live Firestore reads/writes beyond local source checks, or production-host behavior verification.

Digital Screens boundary source-gate checkpoint: `npm run verify:digital-screens-boundary` now locks the screen-token SSR route, projection/fallback menu data path, public-safe `platformSummary/screen_{storeId}` listener mirror and Firestore rule shape, `/api/screen/seen` cheap-fail ordering and hashed limiter keys, browser/server screen content-version touches, desktop/mobile owner acknowledgement guards, and Digital Screens docs parity. Follow-up aggregate evidence: `npm run verify:production-readiness-local` passed with 64/64 checks, including 60 child root `verify:*` scripts. This is a local source gate only; it does not perform TV/browser display smoke, physical-device QA, Firebase deploys, Vercel deploys, production builds, live Firestore reads/writes, provider calls, custom-domain verification, CDN behavior verification, or production-host behavior verification.

Guest Feedback boundary source-gate checkpoint: `npm run verify:guest-feedback-boundary` now locks anonymous public feedback submission admission, safe Google review URL return/rendering, Turnstile/honeypot/rate-limit/body caps, project/store/tenant eligibility, API-only guestFeedback creates, owner desktop/mobile acknowledgement guards, QR/link copy acknowledgement, retention wiring, and required feature docs parity. Follow-up aggregate evidence: `npm run verify:production-readiness-local` passed with 64/64 checks, including 60 child root `verify:*` scripts. This is a local source gate only; it does not perform browser feedback-form smoke, physical-device QA, Firebase deploys, Vercel deploys, production builds, live Firestore writes, provider calls, Turnstile provider smoke, custom-domain verification, CDN behavior verification, or production-host behavior verification.

Official Business Page boundary source-gate checkpoint: `npm run verify:official-business-page-boundary` now locks OBP customer-facing action links, external social links, Google review links, Schema.org `sameAs`/ReserveAction/OrderAction targets, customer app manifest shortcuts, and PWA directions/reservation/order handoffs to normalized HTTPS public URLs. Google Maps and Google review URLs are constrained to Google-owned hosts/paths, social links are constrained to matching platform hosts, and invalid/stale stored strings are hidden instead of emitted as public `href`, manifest, redirect, or JSON-LD targets. Follow-up aggregate evidence: `npm run verify:production-readiness-local` passed with 64/64 checks, including 60 child root `verify:*` scripts. This is a local source gate only; it does not perform browser OBP smoke, physical-device QA, Firebase deploys, Vercel deploys, production builds, live Firestore reads/writes, custom-domain verification, CDN behavior verification, search crawler validation, provider calls, or production-host behavior verification.

Website public copy boundary source-gate checkpoint: `npm run verify:website-public-copy-boundary` now locks the mounted homepage public-copy boundary, Website locale namespace blocked-claim scan, LLM context files, documented AI Menu Manager shorthand ban, and the known unmounted `SmartFeaturesSection`/`Smart Picks` exception so old supporting/future material cannot be mistaken for current first-viewport public copy. Follow-up aggregate evidence: `npm run verify:production-readiness-local` passed with 64/64 checks, including 60 child root `verify:*` scripts. This is a local source gate only; it does not perform browser visual QA, Search Console/Bing verification, IndexNow submission, web crawler behavior verification, Vercel deploys, production builds, custom-domain verification, CDN behavior verification, provider calls, or production-host behavior verification.

URL routing boundary source-gate checkpoint: `npm run verify:url-routing-boundary` now locks product-domain precedence, SignalDesk private-host separation, tenant subdomain/custom-domain classification, reserved-subdomain safety, Vercel alias safety, middleware `/client` rewrite order, public client cache headers, tenant headers, robots/sitemap middleware handling, safe outlet path segments for brand OBP cards, outlet OBP links, client menu canonical redirects, sitemap outlet URLs, safe project path segments for menu lookup, previous-slug redirects, canonical menu URLs, metadata lookup, sitemap project URLs, OBP menu CTA links, and active URL-routing docs parity. Follow-up aggregate evidence: `npm run verify:production-readiness-local` passed with 65/65 checks, including 61 child root `verify:*` scripts. This is a local source gate only; it does not perform browser custom-domain smoke, DNS/Vercel domain verification, CDN behavior verification, Firebase deploys, Vercel deploys, production builds, live Firestore reads/writes, or provider calls.

Source-gate baseline checkpoint: POS sync, multi-location/outlet lifecycle, owner notifications, platform notifications, messaging onboarding monitor, scheduler monitor, platform cost posture, platform entity blocks, ops control room, and reseller dashboard boundaries now have first-class local source gates. `npm run verify:pos-sync-boundary`, `npm run verify:multi-location-boundary`, `npm run verify:owner-notifications-boundary`, `npm run verify:platform-notifications-boundary`, `npm run verify:messaging-onboarding-monitor-boundary`, `npm run verify:scheduler-monitor-boundary`, `npm run verify:platform-cost-posture-boundary`, `npm run verify:platform-entity-blocks-boundary`, `npm run verify:ops-control-room-boundary`, `npm run verify:reseller-dashboard-boundary`, `npm run verify:public-business-truth`, `npm run verify:menulist-api-tenant-safety`, `npm run verify:auth-security-failure-matrix`, `npm run verify:mobile-shell-route-map`, `npm run verify:doc-npm-scripts`, `npm run docs:check-links`, `npx tsc --noEmit --incremental false --pretty false`, `npm run lint`, `git diff --check`, and `npm run verify:production-readiness-local` passed. At that baseline checkpoint, the aggregate local boundary passed 56/56 checks across 52 child root `verify:*` scripts plus docs links, root typecheck, lint, and `git diff --check`; the newer aggregate verifier coverage checkpoint above is the current local aggregate evidence. No Firebase deploy, Vercel deploy, production build, browser smoke, provider smoke, Razorpay call, SMTP call, WhatsApp send, external POS call, Cloud Tasks enqueue, Storage write, live Firestore write, secret change, or production-host smoke was run for this checkpoint.

Reseller dashboard boundary source-gate checkpoint: `npm run verify:reseller-dashboard-boundary` passed and now locks reseller route admission order, platform/reseller role separation, hashed read/write rate-limit keys, bounded request/response parsing, offline/manual entitlement sync, online-provider failure compensation, desktop/mobile shell parity, and docs parity. Focused adjacent checks passed: `npm run verify:billing-entitlement-boundary`, `npm run verify:menulist-api-tenant-safety`, `npm run verify:auth-security-failure-matrix`, `npm run verify:mobile-shell-route-map`, `npm run verify:doc-npm-scripts`, and `npm run docs:check-links`. The full local bundle passed through `npm run verify:production-readiness-local` with 56/56 checks and 52 child root `verify:*` scripts. This is a local source gate for the reseller dashboard; it does not perform Razorpay sandbox payment smoke, authenticated browser QA, physical-device mobile QA, Firebase deploys, Vercel deploys, production builds, live Firestore writes, or provider calls.

Environment target source-gate checkpoint: the active owner action tracker and lower sections of the dev/prod environment guide now use the current MenuList contract: local/preview Firebase targets stay on `menulist-qa`, production uses `menulist`, and Answerlattice remains separated as `answerlattice-qa`/`answerlattice`. `npm run verify:agent-readiness` now blocks stale active-task wording for `menulist-dev` project creation or `--project menulist-dev` deploy commands. No Firebase project, deploy, secret, Vercel deploy, production build, or runtime behavior changed for this checkpoint.

External-gate preflight source-gate checkpoint: the external certification runbook now requires `npm run verify:billing-entitlement-boundary` before Razorpay sandbox smoke and `npm run verify:agent-readiness` before batch image worker Cloud Tasks smoke. `npm run verify:agent-readiness` now guards those preflight commands and their local-only proof boundaries so local source checks are not mistaken for provider or worker certification. No Firebase deploy, Vercel deploy, production build, Razorpay call, Cloud Task enqueue, worker invocation, Storage write, Firestore write, or provider smoke was run for this checkpoint.

Mobile/browser external-gate preflight source-gate checkpoint: Gate 3 in the external certification runbook now requires `node --check scripts/verification/verify-mobile-owner-menu.mjs`, `npm run verify:mobile-shell-route-map`, `npm run verify:staff-roles-route-parity`, `npm run verify:customer-app-pwa`, `npm run verify:public-business-truth`, `npm run verify:menu-extraction-pipeline`, and `npm run verify:public-truth-tools` before physical-device or authenticated owner-shell QA. `npm run verify:agent-readiness` now guards that preflight list and the local-only proof boundary. No browser smoke, physical-device QA, authenticated owner-shell run, Firebase deploy, Vercel deploy, production build, provider call, Storage write, or Firestore write was run for this checkpoint.

Production-host external-gate preflight source-gate checkpoint: Gate 8 in the external certification runbook now requires `npm run verify:production-readiness-local` immediately before any explicitly approved Vercel deploy or production-host smoke. The runbook and `npm run verify:agent-readiness` now guard that local readiness does not prove a Vercel build, deployed artifact, production environment variables, custom-domain routing, CDN behavior, Firebase production access, or production-host runtime behavior. No Vercel deploy, preview deploy, production deploy, production build, production-host smoke, Firebase deploy, provider call, Storage write, or Firestore write was run for this checkpoint.

Deployment checklist guard checkpoint: the active deployment checklist now serves as a guarded handoff map to the External Certification Runbook instead of a direct deployment recipe. It repeats the active-session Vercel deploy approval requirement, routes local proof through `npm run verify:production-readiness-local`, points Function deploy work to the scoped `menulist-qa` Gate 1 target, and removes stale `vercel --prod`, push-to-main, old dashboard-path, stale auth-index config, and broad Functions deploy instructions. `npm run verify:agent-readiness` now guards those checklist boundaries. No Firebase deploy, Vercel deploy, production build, provider call, Cloud Task enqueue, Storage write, Firestore write, secret write, or production-host smoke was run for this checkpoint.

Firestore/ops deploy scope checkpoint: the production readiness checklist, launch prerequisites, and owner action tracker now route Firestore rules/index deploys through scoped `menulist-qa` commands with `firebase.json` instead of broad default-project commands. The ops infrastructure guide now routes Function deploy work through `npm run verify:functions-deploy-preflight` and the External Certification Runbook instead of `firebase deploy --only functions`. `npm run verify:agent-readiness` now rejects the old broad Firestore rules/index commands, `cd functions && npm run deploy`, and broad Functions deploy instructions on those active guidance surfaces. Follow-up source gate: `npm run verify:production-readiness-local` passed with 55/55 checks, including 51 child root `verify:*` scripts. No Firebase deploy, Vercel deploy, production build, provider call, Cloud Task enqueue, Storage write, Firestore write, secret write, or production-host smoke was run for this checkpoint.

Active feature deploy runbook scope checkpoint: active feature and maintenance docs now align with the same scoped deploy model. Nightly scheduler, Continuous Menu Intelligence, MOL, Customer App, Messaging Onboarding, AI data extraction, internal feedback, and menu job queue instructions now require `npm run verify:functions-deploy-preflight`, scoped `menulist-qa` Firebase commands, or the External Certification Runbook instead of broad/default deploy commands. Maintenance and email-validation docs now repeat the active-session Vercel deploy approval guard instead of `vercel --prod`. Authentication, AI data extraction, and internal-feedback docs now use `menulist-qa` plus `firebase.json` for Firestore index/rule examples. `npm run verify:agent-readiness` now rejects the old broad/default deploy instructions on these active surfaces. Follow-up source gate: `npm run verify:production-readiness-local` passed with 55/55 checks, including 51 child root `verify:*` scripts. No Firebase deploy, Vercel deploy, production build, provider call, Cloud Task enqueue, Storage write, Firestore write, secret write, or production-host smoke was run for this checkpoint.

Stale target guidance checkpoint: URL routing docs now use `https://menulist.online` as the MenuList preview URL instead of the retired `menulist-ai.vercel.app` value. SignalDesk validation now treats `ecomsai` in local `.env` files as legacy local state instead of setup truth and points current local/preview guidance to `menulist-qa`. Creative Editor, AI Menu Manager, GrowthOS add-on, and cost posture docs now label old `ecomsai` deploy references as historical evidence only; current evidence must use `menulist-qa` first, with production only after QA evidence and explicit production approval. App Check setup examples now list current `menulist-qa` and `menulist` projects. `npm run verify:agent-readiness` now guards these stale-target boundaries. Follow-up source gate: `npm run verify:production-readiness-local` passed with 56/56 checks, including 52 child root `verify:*` scripts. No Firebase deploy, Vercel deploy, production build, provider call, Cloud Task enqueue, Storage write, Firestore write, secret write, or production-host smoke was run for this checkpoint.

Tenant-block backfill external-gate preflight source-gate checkpoint: Gate 2 in the external certification runbook now requires `npm run verify:tenant-block-backfill-safety`, `npm run verify:public-business-truth`, and `npm run verify:menulist-api-tenant-safety` before a target Firestore dry-run or write-mode backfill. The new verifier proves missing-project, mismatched-`--confirm-project`, and unscoped write attempts fail before Firebase initialization, while `npm run verify:agent-readiness` guards the runbook command and local-only proof boundary. No Firestore read, Firestore write, Firebase deploy, Vercel deploy, production build, provider call, Storage write, or production-host smoke was run for this checkpoint.

Multi-location boundary source-gate checkpoint: `npm run verify:production-readiness-local` surfaced a failing `verify:multi-location-boundary` source gate. The runtime outlet create/policy routes already invalidated `client-stores` and `screen-data`, so the verifier now checks cache invalidation behavior without depending on single-quote versus double-quote style. The same gate also exposed an overly narrow desktop rename assertion; the runtime already uses the `currentOutletSlug` prop to block same-slug submissions, so the verifier now checks that actual contract. No runtime route, Firestore read, Firestore write, Firebase deploy, Vercel deploy, production build, provider call, Storage write, or production-host smoke was run for this checkpoint.

Firebase Functions external-gate preflight source-gate checkpoint: Gate 1 in the external certification runbook now requires `npm run verify:functions-deploy-preflight` before the scoped `menulist-qa` Functions deploy retry. The new verifier runs the Functions package lint/build and guards the current blocked deploy target set plus the boundary that local preflight does not prove Firebase CLI authentication, project IAM, enabled Google Cloud APIs, Secret Manager access, function upload, deployed revisions, scheduler execution, callable behavior, trigger delivery, or live production effect. No Firebase deploy, Vercel deploy, production build, provider call, Storage write, Firestore write, or production-host smoke was run for this checkpoint.

Functions deploy checklist scope checkpoint: the production readiness checklist and launch prerequisites summary no longer present `firebase deploy --only functions` as the active manual step. They now route MenuList launch certification through External Certification Gate 1, the production readiness checklist launch verdict requires every External Certification Runbook gate to have audit evidence, and the deployment setup guide requires `npm run verify:functions-deploy-preflight` before the explicit MenuList Functions target list. `npm run verify:agent-readiness` now guards the scoped wording and rejects the old broad MenuList Functions deploy checklist/setup commands. No Firebase deploy, Vercel deploy, production build, provider call, Cloud Task enqueue, Storage write, Firestore write, or production-host smoke was run for this checkpoint.

Platform alert deploy gate checkpoint: Step 7B in the launch prerequisites now treats the June 2 platform-alert deployment state as historical state to reconfirm, writes Telegram/SMTP Secret Manager values to `menulist-qa` first, requires QA alert-delivery evidence plus explicit production secret approval before writing production channel secrets, requires `npm run verify:functions-deploy-preflight`, deploys affected platform-alert Functions to `menulist-qa` first, and allows the `menulist` production deploy only after QA evidence and explicit production deploy approval. If this widened Step 7B target list differs from External Certification Gate 1, the exact function list and reason must be recorded in this audit. `npm run verify:agent-readiness` now guards that QA-first wording and rejects the old direct production secret/deploy commands. No Firebase deploy, Vercel deploy, production build, provider call, Cloud Task enqueue, Storage write, Firestore write, secret write, or production-host smoke was run for this checkpoint.

Active broad Functions deploy doc cleanup checkpoint: the five-year vision strategy doc and active Answerlattice doctrine/product-separation docs no longer instruct `firebase deploy --only functions --project menulist` or the QA equivalent for MenuList. They now route MenuList Functions work through `npm run verify:functions-deploy-preflight`, the scoped Gate 1 target set, and explicit QA-evidence/production-approval boundaries while leaving separate Answerlattice Functions commands product-scoped. `npm run verify:agent-readiness` now rejects the old broad MenuList Functions deploy commands in those active docs. No Firebase deploy, Vercel deploy, production build, provider call, Cloud Task enqueue, Storage write, Firestore write, secret write, or production-host smoke was run for this checkpoint.

Legacy Firebase target doc boundary checkpoint: Menu extraction pipeline, AI data extraction, and menu-link import docs now preserve `ecomsai` deploy lines as historical evidence only, not current setup truth. Current retry evidence routes through External Certification Gate 1 with `npm run verify:functions-deploy-preflight` and the current `menulist-qa` target; production retry remains gated on QA evidence plus explicit production deploy approval. The `.codex` and `.cascade` Answerlattice rule sources now describe MenuList separation against current `menulist-qa` local/preview and `menulist` production targets instead of the retired `ecomsai` project. `npm run verify:agent-readiness` now guards those boundaries and rejects the stale instruction to retry the old `ecomsai` deploy after billing/project access is restored. No Firebase deploy, Vercel deploy, production build, provider call, Cloud Task enqueue, Storage write, Firestore write, secret write, or production-host smoke was run for this checkpoint.

---

## Feature Inventory

Inventory sources used:

- `__docs__/index.md`
- `src/app/(main)/`
- `src/app/client/[[...slug]]/`
- `src/app/(website)/`
- `src/app/api/`
- `src/components/templates/main-app/`
- `src/components/templates/website/`
- `src/lib/`
- `src/constants/database.ts`
- `src/config/features.ts`

MenuList feature families queued for audit:

1. Client Menu (QR), public menu rendering, analytics tracking, AutoSell/decision blocks
2. Official Business Page (OBP), public metadata, sitemap, robots, discovery policy
3. URL Routing Architecture, tenant/domain routing, custom domains, public cache
4. Projects/Menu Builder parent flow
5. AI data extraction, upload/file processing, extraction review, public `/create-menu`
6. Data editor, B2B/B2C views, description generation, multi-language translation
7. AI image generation and media/image systems
8. Continuous Menu Intelligence, Decision Intelligence, MCE, output control
9. Digital Screens
10. Stores Management, Business Settings, PWA/customer app settings, compliance pages, temporary status
11. Multi-Outlet Consistency, store onboarding, outlet policy, master-update awareness
12. Multi-Chain Permissions, Roles & Permissions, Staff/user management
13. Authentication and auth onboarding
14. Billing, subscriptions, Razorpay, AI capacity/top-ups, reseller dashboard
15. Analytics dashboards, ROI metrics, owner dashboard, Today, business health/Owner Business Assistant
16. Reviews & Reputation, guest feedback/customer feedback loop
17. GBP Sync, Hours & Holiday Accuracy, Pricing Integrity System, POS Webhook Sync
18. Physical Surfaces, print assets, menu card export, Use MenuList kit
19. Staff Prompt, messaging onboarding, owner notifications, lifecycle messaging
20. Main Website (menulist.ai), resources, pricing/get-started/create-menu/trust/legal pages
21. Security, system strengthening, ops/safe mode, platform monitoring dashboards

Non-MenuList surfaces found and intentionally excluded unless separation hardening is required:

- Answerlattice routes, APIs, Firebase clients, and docs
- GrowthOS/Growth products unless they affect MenuList boundaries
- MyCodex private reader
- Website Asset Operating System internal package

---

## Follow-Up Audit: July 1 Static And Source-Gate Verifier Sweep

**Audit date:** July 1, 2026

**Scope:** MenuList protected owner AI routes, public-business-truth/cache source gates, customer/public truth tools, extraction and AI accounting, Menu Card/export surfaces, owner business assistant, customer app PWA, activation, printable assets, environment targets, and catalog-aware analytics.

Confirmed source/runtime alignment:

- Protected owner AI/capacity/status routes now enforce route-specific store-role permissions before provider calls, capacity/accounting checks, analytics reads, insight writes, or menu/output AI work.
- Review reply suggestions require feedback-management permission before AI generation.
- Public business truth and public cache invalidation source gates still cover owner/public write paths, digital screen refresh, client-store lookup tags, and public menu/OBP cache tags.
- Public Truth Check, Hours Check, QR Link Health Check, WhatsApp Action Link Check, Menu Readability Check, and Photo Gap Check remain V0 browser-local/static-report tools with no report API route or Firebase write path. Photo Gap Check now has the same package-level source verifier as the sibling tools, including no upload, no image analysis, no external fetch, no Google/Instagram inspection, no report storage, and no provider/AI/ranking claims.
- POS webhook test and delivery routes use public HTTPS validation plus server-side DNS target validation before outbound provider fetches.
- Catalog-aware analytics still builds in Functions and verifies Pro gating plus scheduler catalog reuse.
- Reseller manual license expiry moved out of the legacy decision-block nightly scheduler and into `menulistMaintenanceScheduler`, with a capped query, per-task lease, reseller count decrement, store/platform-summary entitlement cleanup, public cache revalidation, digital-screen touch, and owner-business-assistant context invalidation.
- Self-serve and reseller online onboarding now compensate local tenant/store/user creation if Razorpay plan lookup or subscription creation fails after the local transaction: created tenant/store scope is marked inactive, `storesSummary` is updated to hide the store from summary-backed public reads, failed user mappings are cleared when they match the just-created scope, public cache is revalidated, and reseller owner custom claims are cleared back to owner identity without tenant/store scope.
- Platform tenant block/unblock now denormalizes inherited `tenantBlocked` state onto existing affected store docs in 450-write batches and into `storesSummary`, matching both `tenantId` and `tId` store shapes. Public store lookup uses the store-level flag when present and keeps the tenant-doc fallback for legacy stores.
- `scripts/backfill-store-tenant-block-state.ts` now provides a dry-run-by-default legacy backfill for `stores/{storeId}.tenantBlocked`; it checks `tenantId` and `tId`, updates only existing store docs, and requires `--write` for Firestore mutation.
- The tenant-block backfill now requires an explicit Firebase project via `--project-id` or `FIREBASE_PROJECT_ID`, refuses writes unless `--confirm-project` matches the target and the write is scoped with `--tenant-id`, `--store-id`, or explicit `--all-stores`, prints project/mode before Firestore reads, and logs bounded `tenant_block_backfill_failed` diagnostics instead of raw stacks.
- Batch image generation now checks Cloud Tasks worker URL, queue id, project location, project id, and worker secret readiness before AI capacity checks or task fanout. Missing config marks the existing batch job failed with owner-safe copy and a stable diagnostic instead of discovering the issue after enqueue attempts.
- WhatsApp provider setup docs and env validation now use the current `WHATSAPP_ACCESS_TOKEN` / `WHATSAPP_PHONE_NUMBER_ID` naming from the Functions secret registry. The stale `WHATSAPP_API_TOKEN` name was removed from active messaging-onboarding setup instructions and locked by `verify:agent-readiness`.
- Messaging onboarding provider processing now fails closed in checked-in env files/templates: root env examples and MenuList Functions dotenv files/templates set `ENABLE_MESSAGING_ONBOARDING=false`, while docs instruct enabling only the target with real WhatsApp secrets and Meta webhook registration. `verify:agent-readiness` locks the env files, setup docs, messaging-onboarding docs, owner action item, and external certification runbook. This is setup parity only; real Gate 5 WhatsApp provider smoke remains pending.
- The deployment setup guide's environment-variable table now stays contiguous through the optional provider rows, so Email/WhatsApp/widget/menu-import/Vercel/webhook prerequisites render as table rows instead of being split by diagnostic notes.
- Reviews/Reputation dormant status is now source-gated: both flags remain off, the review APIs return disabled 404s while off, `ReviewReplyTool` requires the parent and child flags, no owner mount imports exist, and active code comments no longer describe the disabled reply assist as launchable without GBP-backed ingestion.
- GrowthOS and Digital Screen paths are now source-gated as non-consumers of CMI/menuIntelligence. GrowthOS continues to build from project/store facts, and screen rendering continues to use screen state, campaigns, owner uploads, and project-extracted menu items until a dedicated CMI consumer feature loop certifies otherwise.

Validation performed in the July 1 continuation:

- `npm run verify:menulist-api-tenant-safety` - passed.
- `npm run verify:menulist-api-tenant-safety` - passed after adding the Reviews/Reputation dormant GBP/mount-point source gate.
- `npm run verify:auth-security-failure-matrix`, `npm run verify:ai-accounting`, `npx tsc --noEmit --incremental false --pretty false`, full `git diff --check`, and scoped touched-file `git diff --check` - passed after the Reviews/Reputation dormant-boundary changes.
- `npm run verify:growthos`, `npm run verify:customer-app-pwa`, `npx tsc --noEmit --incremental false --pretty false`, full `git diff --check`, and scoped touched-file `git diff --check` - passed after adding no-CMI-consumer source gates for GrowthOS and Digital Screens.
- Browser/mobile QA tooling check: Chrome extension backend discovery returned `[]`, and local `playwright` / `@playwright/test` packages are not installed. No browser-emulated mobile certification was claimed from this continuation.
- `npm run verify:auth-security-failure-matrix` - passed.
- `npm run verify:ai-accounting` - passed.
- `npm run verify:menu-card-export` - passed.
- `npm run verify:public-business-truth` - passed.
- `npm run verify:menu-extraction-pipeline` - passed.
- `npm run verify:menu-extraction-pipeline:dry-run` - passed.
- `npm run verify:public-truth-check` - passed.
- `npm run verify:hours-check` - passed.
- `npm run verify:photo-gap-check` - passed.
- `npm run verify:qr-link-health-check` - passed.
- `npm run verify:whatsapp-action-link-check` - passed.
- `npm run verify:menu-readability-check` - passed.
- `npm run verify:public-truth-tools` - passed after adding the aggregate family verifier for all six Public Truth Tools checks.
- `npm run verify:ai-menu-manager` - passed.
- `npm run verify:customer-app-pwa` - passed.
- `npm run verify:menu-export` - passed.
- `npm run verify:owner-business-assistant` - passed.
- Latest Gate 5 WhatsApp provider fail-closed setup continuation: root env examples and MenuList Functions dotenv files/templates now default `ENABLE_MESSAGING_ONBOARDING=false`; active messaging-onboarding docs, deployment setup, owner action items, Functions env setup, external certification runbook, changelog, and `verify:agent-readiness` now lock enabling only the target with real Meta secrets and webhook registration. `node --check scripts/verification/verify-agent-readiness.js`, stale enabled-by-default doc scan, `npm run verify:agent-readiness`, `npm run verify:env-targets`, `npm run verify:doc-npm-scripts`, `npm run docs:check-links`, scoped `git diff --check`, and `npm run verify:production-readiness-local` passed. The aggregate local boundary passed 42/42 checks across 38 child root `verify:*` scripts plus docs links, root typecheck, lint, and `git diff --check`. No Next.js production build, Firebase deploy, Vercel deploy, provider smoke, Cloud Tasks enqueue, Storage write, live Firestore write, secret write, or webhook registration was performed.
- `npm run verify:no-free-product-plans` - passed.
- `npm run verify:staff-prompt-runtime` - passed after adding the source gate and correcting active Staff Prompt public-copy boundaries.
- `npm run verify:menulist-activation-concierge` - passed.
- `npm run verify:printable-asset-templates` - passed.
- `npm run verify:website-resource-locales` - passed.
- `npm run verify:doc-npm-scripts` - passed after correcting active documentation references to real root and nested package commands.
- `npm run docs:check-links` - passed after exposing the package script, tightening checker archive/inline-link/parser handling, repairing stale active-doc links, adding missing active-folder README indexes, and normalizing legacy uppercase/space active doc filenames; latest focused baseline is 0 broken links and 0 naming violations.
- Latest docs-link-health continuation after active-doc filename normalization: `npm run docs:check-links`, `npm run verify:doc-npm-scripts`, `npx tsc --noEmit --incremental false --pretty false`, `npm run lint`, and full `git diff --check` - passed.
- Latest mobile-width browser smoke on local Next dev server `http://localhost:3050` passed for `/`, `/features`, `/pricing`, `/create-menu`, `/trust-security`, and `/contact` at 390px width: all routes rendered with no horizontal overflow, no visible runtime error text, and no captured browser console errors.
- Latest local tenant-host mobile smoke on `http://mysalon.menulist.ai:3050/` and `/menu` rendered the safe `Menu not found` fallback with no horizontal overflow, no visible runtime error text, and no captured browser console errors. This proves fallback rendering for the local tenant host, not an active customer-menu fixture.
- Latest active public tenant/customer fixture mobile smoke on `http://habibis.192-168-10-239.sslip.io:3050` passed for `/`, `/menu`, `/bar-menu`, `/spa-menus`, `/privacy`, `/terms`, `/refund`, and `/feedback/14-mn8d5jbz-15?source=menu_footer` at 390px width: all routes rendered the expected active tenant content with no page-level horizontal overflow, no visible runtime error/not-found/sign-in state, and no captured browser console errors.
- Latest authenticated owner mobile Menu-tab verifier reached a valid NextAuth/Firebase-claims session but was blocked by the current QA account's subscription state: the selected store rendered the mobile `Subscribe to Get Started` gate instead of `MobileShell`. The verifier now fails fast with explicit `fixture_blocked`, `auth_blocked`, or `runtime_blocked` initial-state classification and captures a screenshot plus bounded debug payload for blocked states.
- The owner mobile Menu-tab verifier now honors `MOBILE_QA_ENV_FILE`, `MOBILE_QA_CDP_TIMEOUT_MS`, creates `MOBILE_QA_OUTPUT_DIR` before screenshot capture, and supports `MOBILE_QA_REQUIRE_EXPLICIT_FIXTURE=1` so certification runs cannot accidentally pass from stale default owner/store/project values.
- Latest owner-mobile certification harness hardening: `node --check scripts/verification/verify-mobile-owner-menu.mjs`, `node --check scripts/verification/verify-agent-readiness.js`, explicit-fixture negative-path preflight, `npm run verify:agent-readiness`, `npm run docs:check-links`, scoped `git diff --check`, and `npm run verify:production-readiness-local` passed. The aggregate local boundary passed 41/41 checks across 37 child root `verify:*` scripts plus docs links, root typecheck, lint, and `git diff --check`. No Next.js production build, Firebase deploy, Vercel deploy, provider smoke, Cloud Tasks enqueue, Storage write, or live Firestore write was performed.
- Latest local production-readiness boundary refresh after Gate 1/Gate 2 hardening: `npm run verify:production-readiness-local` passed 40/40 checks. The command ran 36 child root `verify:*` scripts, `npm run docs:check-links`, `npx tsc --noEmit --incremental false --pretty false`, `npm run lint`, and `git diff --check`. This includes `verify:catalog-analytics`, which ran its targeted Functions TypeScript build; no Next.js production build, Firebase deploy, Vercel deploy, provider smoke, Cloud Tasks enqueue, or Firestore write was performed.
- Latest project document-size gate continuation: `node --check scripts/verification/verify-menu-extraction-pipeline.js`, `npm run verify:menu-extraction-pipeline` (136/136 checks), `npx tsc --noEmit --incremental false --pretty false`, Functions `npx tsc --noEmit --pretty false`, `npm --prefix functions run build`, and `npm run verify:production-readiness-local` passed. The aggregate run again passed 40/40 checks across 36 child root `verify:*` scripts, docs links, root typecheck, lint, and `git diff --check`; no Next.js production build, Firebase deploy, Vercel deploy, provider smoke, Cloud Tasks enqueue, or Firestore write was performed.
- Latest DS-1 client upload cap continuation: desktop owner upload and the mobile upload sheet now derive their existing-pending plus new file/page cap from `MENU_EXTRACTION_JOB_LIMITS.MAX_FILES`, block over-limit image/PDF batches before Storage upload, reject PDFs above the same 15-page cap during browser conversion, and document the current 15-page extraction contract instead of the stale 30/50-page claims. Mobile also passes the remaining available page slots into shared PDF conversion before canvas rendering. `npm run verify:menu-extraction-pipeline` passed 145/145 checks after adding this source gate. `npm run verify:production-readiness-local` then passed 40/40 checks across 36 child root `verify:*` scripts, docs links, root typecheck, lint, and `git diff --check`. No Next.js production build, Firebase deploy, Vercel deploy, provider smoke, Storage write, Cloud Tasks enqueue, or live Firestore write was performed.
- Latest DS-1 replacement-policy continuation: project extraction remains incremental by design because `files[].extractedData` is live editor data, while full replacement is explicit through reset/create-new. The source gate now verifies oversized append attempts return reset/create-new copy before AI work, owner project reset writes `files: []` and clears linked overrides when relevant, and the reset confirmation states that uploaded files and extracted data are cleared. `npm run verify:menu-extraction-pipeline` passed 147/147 checks after adding this source gate. `npm run verify:production-readiness-local` then passed 40/40 checks across 36 child root `verify:*` scripts, docs links, root typecheck, lint, and `git diff --check`. No Next.js production build, Firebase deploy, Vercel deploy, provider smoke, Storage write, Cloud Tasks enqueue, or live Firestore write was performed.
- Storage path hardening parity: the local boundary initially exposed stale docs for `verify:storage-paths`. The infrastructure tracker, upload-file processing docs, and AI extraction security audit now state that active project fallback uploads route through `generateStoragePath()` to `projects/files/{tId}/{sId}/{fileId}`, while legacy `MenuListAi/project/files/`, `generated/`, `edited/`, and `custom/` project rules are authenticated-read-only and deny new writes/deletes.
- AI Menu Manager diagnostic intent ordering: the local boundary initially exposed that "Why is print menu wrong?" resolved to the `print_menu_open` manual task. The resolver now keeps explicit design/action commands ahead of broad context answers while letting diagnostic print/QR/state questions resolve to read-only `system_context_answer` cards before generic More/manual screen tasks.
- Infrastructure risk tracker parity: CG-1 now reflects the current AI-operation detail-retention implementation (`AI_OPERATION_LOG_MODE`, `detailExpiresAt`, and daily `ai_operation_detail_cleanup`) instead of the stale "no cleanup" status. App-route AI operation rows also compact `clientResponse` into count/shape summaries in `accounting_only` mode while keeping owner transaction summaries readable from those compact fields. No Cloud Function logic changed in this continuation.
- Latest CG-1 compact-ledger retention continuation: AI operation ledgers now document compact-not-delete retention as the source-level policy. `MENULIST_AI_OPERATIONS` and nested `menulistAiOperations` retain compact accounting/audit rows for platform cost review and owner transaction history, while accounting-only mode avoids raw provider/output payload retention and detailed-mode rows are pruned by `ai_operation_detail_cleanup` when the updated scheduler is deployed. `npm run verify:ai-accounting` passed after adding source gates for this policy. `npm run verify:production-readiness-local` passed 40/40 checks across 36 child root `verify:*` scripts, docs links, root typecheck, lint, and `git diff --check`. No Next.js production build, Firebase deploy, Vercel deploy, provider smoke, Cloud Tasks enqueue, Storage write, or live Firestore write was performed.
- Existing-user subscription identity hardening: `/api/razorpay/create-subscription` now uses only the authenticated session name/email for pending subscription records and Razorpay notes, and ignores unvalidated `body.name` / `body.email` fallbacks after the bounded body parse and Zod validation. The tenant-safety verifier now asserts this session-only contract. This changes no Firestore operation counts, provider call counts, cache invalidation, rules, indexes, Cloud Functions, Firebase deploy requirement, or Vercel deploy action.
- Razorpay sandbox certification setup is now source-gated: the external certification runbook requires `RAZORPAY_KEY_ID` and `NEXT_PUBLIC_RAZORPAY_KEY_ID` to use the `rzp_test_` prefix for Gate 4, keeps `RAZORPAY_KEY_SECRET` and `RAZORPAY_WEBHOOK_SECRET` scoped to the same test account/endpoint, and `npm run verify:agent-readiness` checks those runbook and env-template anchors. This is still local setup parity only; real Razorpay sandbox subscription/top-up/reseller/webhook smoke remains pending.
- Billing entitlement boundary source gate: `npm run verify:billing-entitlement-boundary` locks Razorpay subscription/top-up route sequencing, checkout signature/payment/order truth checks, server-computed upgrade carry-forward, browser payment response acknowledgement, webhook cheap-fail/idempotency ordering, and MenuList entitlement/cache sync anchors. This is local source/docs parity only; real Razorpay sandbox subscription/top-up/reseller/webhook smoke remains pending.
- Latest billing entitlement boundary continuation: `node --check scripts/verification/verify-billing-entitlement-boundary.js`, `npm run verify:billing-entitlement-boundary`, `npm run verify:menulist-api-tenant-safety`, `npm run verify:auth-security-failure-matrix`, `npm run verify:no-free-product-plans`, `npm run verify:doc-npm-scripts`, `npm run docs:check-links`, `npx tsc --noEmit --incremental false --pretty false`, `npm run lint`, `git diff --check`, and `npm run verify:production-readiness-local` passed. The aggregate local boundary passed 44/44 checks across 40 child root `verify:*` scripts plus docs links, root typecheck, lint, and `git diff --check`. No Next.js production build, Firebase deploy, Vercel deploy, Razorpay sandbox call, webhook delivery, provider smoke, Cloud Tasks enqueue, Storage write, live Firestore write, or browser smoke was performed.
- Staff Prompt runtime/source gate: `npm run verify:staff-prompt-runtime` now locks the current read-only Today summary contract. The verifier checks that active code has no standalone `src/lib/staff-prompt` generator, desktop/mobile Today render `staffPrompt` only when eligible, Weekly Growth Pack copy receives the staff line only when eligible, docs state zero incremental Firebase cost, and active website copy has no separate staff-facing route, helper generator, provider call, owner setting, mobile-only write, or public Staff Prompt landing page.
- Latest Staff Prompt source-gate continuation: `npm run verify:staff-prompt-runtime`, `node --check scripts/verification/verify-staff-prompt-runtime.js`, `npm run verify:doc-npm-scripts`, `npm run docs:check-links`, `npx tsc --noEmit --incremental false --pretty false`, `npm run lint`, focused `git diff --check`, and `npm run verify:production-readiness-local` passed. The aggregate local boundary passed 41/41 checks across 37 child root `verify:*` scripts plus docs links, root typecheck, lint, and `git diff --check`. No Next.js production build, Firebase deploy, Vercel deploy, provider smoke, Cloud Tasks enqueue, Storage write, live Firestore write, new Function, new index, new listener, or public cache mutation was performed.
- Latest project default-handoff continuation: desktop and mobile project create/edit now pass previous-default or replacement-default ids through the shared `addProject()` / `updateProjectMetadata()` default-handoff option, and `verify:public-business-truth` locks the DAL-level summary payload plus caller/doc parity. `node --check scripts/verification/verify-public-business-truth.js`, `npm run verify:public-business-truth`, `npm run verify:doc-npm-scripts`, `npm run docs:check-links`, `npx tsc --noEmit --incremental false --pretty false`, `npm run lint`, `git diff --check`, and `npm run verify:production-readiness-local` passed. The aggregate local boundary passed 41/41 checks across 37 child root `verify:*` scripts plus docs links, root typecheck, lint, and `git diff --check`. No Next.js production build, Firebase deploy, Vercel deploy, provider smoke, Cloud Tasks enqueue, Storage write, live Firestore write, new Function, new index, new listener, or live public-cache mutation was performed.
- Mobile shell route-map source gate: `npm run verify:mobile-shell-route-map` now parses `MobileShell.tsx` and `MobileMoreScreen.tsx` to lock canonical owner, platform, ops, reseller, and help-center route targets against declared/rendered `MoreSubScreen` values, while the mobile route docs now match the runtime `/dashboard`, Today, Share, More, platform, ops, and reseller mappings. This is source/docs parity only; it does not replace authenticated owner-shell visual QA or physical-device QA.
- Staff/Roles route parity source gate: `npm run verify:staff-roles-route-parity` locks `/users`, `/users/list`, and `/users/permissions` desktop aliases, their mobile `MobileShell` More sub-screen targets, `MobileMoreScreen` permission gates, mobile Staff/Roles shared client usage, and roles-permissions docs/audit parity. This is source/docs parity only; browser smoke for `/users/list`, `/users/permissions`, and authenticated mobile Staff/Roles sub-screens remains pending.
- Latest Staff/Roles route parity continuation: `node --check scripts/verification/verify-staff-roles-route-parity.js`, `npm run verify:staff-roles-route-parity`, `npm run verify:mobile-shell-route-map`, `npm run verify:auth-security-failure-matrix`, `npm run verify:menulist-api-tenant-safety`, `npm run verify:doc-npm-scripts`, `npm run docs:check-links`, `npx tsc --noEmit --incremental false --pretty false`, `npm run lint`, `git diff --check`, and `npm run verify:production-readiness-local` passed. The aggregate local boundary passed 43/43 checks across 39 child root `verify:*` scripts plus docs links, root typecheck, lint, and `git diff --check`. No Next.js production build, Firebase deploy, Vercel deploy, provider smoke, Cloud Tasks enqueue, Storage write, live Firestore write, or authenticated browser smoke was performed.
- Staff mutation user-ID boundary checkpoint: fixed in source. Staff update, remove, reset-passcode, and force-sign-out keep the same `withAuth()` admission, session eligibility checks, hashed mutation rate limits, 16KB bounded body readers, tenant/store authority checks, target-store eligibility checks, last-owner protection, Admin SDK user writes, session revocation, Firebase Auth disabled-state sync, fixed owner copy, and mutation acknowledgements, but malformed `userId` values can no longer reach `users/{userId}` document reads or staff mutation response IDs. `src/lib/staffManagement/server.ts` validates these IDs through `StaffUserIdSchema`, repeats `normalizeStaffUserId(input.userId)` into local `targetUserId`, uses `.doc(targetUserId)` for user reads, and excludes `.doc(input.userId)` and `sanitizeStaffUserForAuthority(input.userId, ...)`. `npm run verify:menulist-api-tenant-safety` and `npm run verify:auth-security-failure-matrix` source-gate the schema, helper-level normalization, raw-ref exclusions, Roles/Permissions docs, audit evidence, and changelog evidence. This changes Staff/Roles target-user ID admission and source-gate coverage only; it does not change valid staff create/update/remove/reset/sign-out behavior, permission semantics, Firestore read/write counts for valid requests, Firebase Auth operations for valid requests, Firestore rules/indexes, Cloud Functions, Firebase deployment, Vercel deployment, browser/device QA, production-host smoke, launch approval, or release certification.
- Staff store scope document-ID boundary checkpoint: fixed in source. Staff list target-store reads, legacy default-role repair writes, and role save/delete writes keep the same `withAuth()` admission, session eligibility checks, hashed staff/role rate limits, bounded body readers, tenant/store authority checks, target-store eligibility checks, default-role repair behavior, `roles[]` mutation semantics, fixed owner copy, and mutation acknowledgements for valid store scope, but malformed, reserved, empty, whitespace-mutated, path-shaped, decimal, zero, negative, unsafe, or nonnumeric store IDs can no longer reach `stores/{storeId}` reads or role/default-role writes. `src/lib/staffManagement/server.ts` now exposes `normalizeStaffStoreScopeDocumentId()`, validates store scope with `src/lib/firebase/firestoreDocumentId.ts` plus exact positive numeric admission, and uses `.doc(storeScope.documentId)` for staff store refs instead of raw `.doc(String(storeId))`, `.doc(String(store.storeId))`, or `.doc(String(input.storeId))`. `npm run verify:menulist-api-tenant-safety` and `npm run verify:auth-security-failure-matrix` source-gate the store-scope normalizer, exact numeric guard, normalized refs, raw-ref exclusions, Roles/Permissions docs, audit evidence, and changelog evidence. This changes Staff/Roles store document-ID admission only; it does not change valid staff list, valid default-role repair, valid role save/delete behavior, permission semantics, Firestore read/write counts for valid requests, Firebase Auth operations for valid requests, Firestore rules/indexes, Cloud Functions, Firebase deployment, Vercel deployment, browser/device QA, production-host smoke, launch approval, or release certification.
- Image batch retention hardening: CG-2 and SG-4 are code-resolved by terminal batch job retention markers (`itemsExpiresAt`, `expiresAt`) plus a daily `image_batch_job_retention_cleanup` task inside `menulistMaintenanceScheduler`. The task prunes `itemsList` after seven days, deletes terminal job docs after 30 days, and attempts same-tenant/store `media/menuItem/{tId}/{sId}/` Storage cleanup for old `completed`, `failed`, and `discarded` generated-image jobs. This changes Firebase Function logic and remains pending live effect until the updated scheduler can be deployed.
- `npm run verify:agent-readiness` - passed.
- `npm run verify:env-targets` - passed.
- `npm run verify:recycle-bin` - passed.
- `npm run verify:creative-editor-smoke` - passed.
- `npm run verify:answerlattice-pwa` - passed.
- `npm run verify:answerlattice-runtime-truth` - passed.
- `npm run verify:campaigncue` - passed.
- `npm run verify:growthos` - passed.
- `npm run verify:signaldesk` - passed.
- `npm run verify:catalog-analytics` - passed, including the targeted Functions TypeScript build.
- `npx tsc --noEmit --incremental false --pretty false` - passed after the full source-gate sweep.
- `npm run verify:agent-readiness`, `npm run verify:env-targets`, and `npx tsc --noEmit --incremental false --pretty false` - passed after aligning active WhatsApp provider setup docs, env templates, Functions secret registry checks, and app env validation warnings.
- `npm run verify:agent-readiness` and `npm run verify:env-targets` - passed after preserving the deployment setup guide's env-table structure in the source gate.
- `npm --prefix functions run build` - passed for the reseller expiry scheduler changes.
- `npm run verify:menulist-api-tenant-safety` - passed after adding reseller expiry and onboarding compensation source gates.
- `npx tsc --noEmit --incremental false --pretty false` - passed after the reseller expiry and onboarding compensation changes.
- Focused `git diff --check` for touched reseller expiry and onboarding compensation files - passed.
- `npm run verify:menulist-api-tenant-safety` - passed after the tenant-block store-doc denormalization source gate.
- `npm run verify:public-business-truth` - passed after the public store lookup denormalized tenant-block source gate.
- `npx tsc --noEmit --incremental false --pretty false` - passed after the tenant-block denormalization changes.
- Focused `git diff --check` for tenant-block denormalization files - passed.
- `npm run verify:public-business-truth` - passed after adding the dry-run legacy tenant-block backfill source gate.
- `npx tsc --noEmit --incremental false --pretty false` - passed after adding the tenant-block backfill utility.
- `npm run verify:public-business-truth`, `npm run verify:menulist-api-tenant-safety`, and `npx tsc --noEmit --incremental false --pretty false` - passed after adding dual-field `tenantId` / `tId` tenant-block matching.
- `npm run verify:public-business-truth` and `npx tsc --noEmit --incremental false --pretty false` - passed after hardening tenant-block backfill project targeting, write confirmation, and bounded failure diagnostics.
- Tenant-block backfill refusal smokes passed: missing project target, mismatched `--confirm-project`, and unscoped write mode fail before Firestore initialization with bounded `tenant_block_backfill_failed` output.
- Bounded tenant-block dry-run command `npx ts-node --compiler-options '{"module":"CommonJS"}' scripts/backfill-store-tenant-block-state.ts --project-id menulist-qa --limit 1` reached Firestore in `DRY RUN` mode and failed with `PERMISSION_DENIED` / `CONSUMER_INVALID` for `menulist-qa`, so current credentials cannot review the target dataset.
- `npm run verify:ai-accounting` - passed after adding the batch image Cloud Tasks config preflight source gate.
- Latest AI image prompt-cache continuation: `npm run verify:ai-accounting`, `npx tsc --noEmit --incremental false --pretty false`, `npm run lint`, `npm run docs:check-links`, `npm run verify:doc-npm-scripts`, `git diff --check`, and `npm run verify:production-readiness-local` passed after adding the server-only batch non-reference prompt cache. The aggregate passed 40/40 checks across 36 child root `verify:*` scripts, docs links, root typecheck, lint, and `git diff --check`. No provider smoke, Storage write, Next.js production build, Firebase deploy, or Vercel deploy was performed.
- Latest DS-2 extraction job payload continuation: first-extraction project auto-save jobs now keep `result.summary` and stamp `project_auto_saved_immediate` instead of writing `result.combinedData`; public draft, messaging onboarding, and review jobs still retain required payloads. `npm run verify:menu-extraction-pipeline` passed 141/141 checks, `npx tsc --noEmit --incremental false --pretty false` passed, `npm --prefix functions run build` passed, and `npm run verify:production-readiness-local` passed 40/40 checks across 36 child root `verify:*` scripts, docs links, root typecheck, lint, and `git diff --check`. The updated `processMenuImagesJob` Function still requires a scoped Firebase deploy for live effect, but the known `menulist-qa` Cloud Resource Manager HTTP 403 permission blocker was not retried blindly.
- `npx tsc --noEmit --incremental false --pretty false` - passed after the batch image Cloud Tasks config preflight changes.
- `npm run verify:auth-security-failure-matrix` - passed after aligning the reseller expiry source gate with `menulistMaintenanceScheduler`.
- `npm run verify:ai-accounting`, `npm run verify:auth-security-failure-matrix`, `npm run verify:menulist-api-tenant-safety`, `npx tsc --noEmit --incremental false --pretty false`, `npm --prefix functions run build`, `npm run docs:check-links`, `npm run verify:doc-npm-scripts`, `npm run lint`, and `git diff --check` - passed after adding image batch retention markers and the `image_batch_job_retention_cleanup` scheduler task.
- `npm run verify:ai-accounting` - passed after adding server-side generated-image media preparation and verifier coverage for SG-3.
- `npx tsc --noEmit --incremental false --pretty false` - passed after adding `prepareMediaImageAdmin()` for batch worker uploads.
- `npm run verify:ai-accounting`, `npm run verify:menulist-api-tenant-safety`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `npm run verify:doc-npm-scripts`, `git diff --check`, and `npm run lint` - passed after the SG-3 docs and verifier alignment.
- `npx ts-node --compiler-options '{"module":"CommonJS"}' -r tsconfig-paths/register -e "<prepareMediaImageAdmin runtime smoke>"` - passed; an in-memory 2000x1400 PNG prepared to a 1200x900 `image/webp` under the 500KB `menuItem` target.
- `npm run verify:storage-lifecycle` - passed after adding the SG-1 Cloud Storage lifecycle artifact, launch runbook commands, and no-delete source gate.
- `npm run verify:menu-extraction-pipeline` - passed after adding the QP-1 extraction monitor SWR dedupe source gate.
- AI image prompt-cache source retention: CS-2 is code-resolved for the batch prompt-cache boundary. Cache source docs keep `expiresAt`, cache source objects stay under `system/aiImagePromptCache/`, cache hits copy bytes into the requesting store's own `media/menuItem/{tId}/{sId}/...` path, and `menulistMaintenanceScheduler` now includes bounded daily `ai_image_prompt_cache_cleanup` for expired cache docs/source objects. Single-image browser previews remain intentionally uncached because that route does not write Storage until owner acceptance. This changes Firebase Function logic and remains pending live effect until the updated scheduler can be deployed; the scoped `firebase deploy --project menulist-qa --config firebase.json --only functions:menulistMaintenanceScheduler --non-interactive` attempt passed predeploy lint/build, then failed with Cloud Resource Manager HTTP 403 caller permission.
- Batch image worker setup-contract parity: the Cloud Tasks readiness set is now locked across `.env.staging.example`, `.env.production.example`, the three-product environment setup guide, the external certification runbook, and `verify:agent-readiness` / `verify:env-targets`. The exact required values are `FIREBASE_PROJECT_ID`, `FIREBASE_PROJECT_LOCATION`, `BATCH_IMAGE_GENERATION_WORKER_URL`, `BATCH_IMAGE_GENERATION_QUEUE_ID`, and `BATCH_IMAGE_GENERATION_WORKER_SECRET`. This is docs/verifier hardening only; no queue was created, no Cloud Task was enqueued, and no provider smoke was run.
- `npm run verify:menu-extraction-pipeline`, `npm run verify:storage-lifecycle`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, `npm run verify:doc-npm-scripts`, `git diff --check`, and `npm run lint` - passed after the SG-1/QP-1 docs and verifier alignment.
- Latest browser-control attempt found no available in-app browser backends in this session, so true browser/mobile QA remains unverified rather than inferred.
- Latest Chrome-control attempt returned `Browser is not available: extension`; the bundled troubleshooting lookup also pointed at a stale plugin cache path, and local Playwright packages are not installed. Browser/mobile QA remains a real pending gate, not a source-level pass.
- `firebase deploy --project menulist-qa --config firebase.json --only functions:menulistMaintenanceScheduler,functions:computeDecisionBlocksScores --non-interactive` - predeploy lint/build passed, then deployment failed with Cloud Resource Manager HTTP 403: caller does not have permission.
- `firebase deploy --project menulist-qa --config firebase.json --only functions:menulistMaintenanceScheduler --non-interactive` - predeploy lint/build passed after the image batch retention scheduler change, then deployment failed with Cloud Resource Manager HTTP 403: caller does not have permission.
- `firebase deploy --project menulist-qa --config firebase.json --only functions:menulistMaintenanceScheduler --non-interactive` - predeploy lint/build passed after the Founder Monitor scheduler wrapper/build fix, then deployment failed with Cloud Resource Manager HTTP 403: caller does not have permission.

Remaining certification boundaries are unchanged: no Vercel deploy or production-host smoke was run, so the SG-3 app/API media-preparation change is not live; the SG-1 bucket lifecycle artifact was not applied to Cloud Storage and still requires the QA-first `gcloud storage buckets update` command in the launch prerequisite guide; real mobile-device QA, Razorpay sandbox smoke, WhatsApp/provider smoke, external POS provider smoke, Firebase Function deploy retry, and batch worker secret verification still require their owner-controlled or provider-controlled environments. The external certification runbook now separates local preflight evidence from external pass evidence so local verifier success cannot be mistaken for provider, cloud, device, or production-host certification.

The current external gate order, stop rules, commands, and evidence format are centralized in `__docs__/production-readiness/external-certification-runbook.md`.

---

## Follow-Up Audit: MenuList API Tenant-Safety Source Gate

**Audit date:** June 26, 2026

**Scope:** High-risk MenuList owner API routes that accept or derive tenant/store/project scope, plus shared auth, permission, staff, outlet, public-cache invalidation helpers, and public operational-signal cheap-fail ordering.

Added `npm run verify:menulist-api-tenant-safety` as a source-level regression gate for:

- AI Menu Manager selected-store scope and proposal/session ownership checks.
- AI Menu Manager command/proposal server fallbacks now use bounded JSON parsing before schema validation, selected-project/proposal reads, proposal writes, or completion writes.
- Caller-supplied tenant/store POS sync and menu extraction job routes.
- Multi-outlet create, deactivate, rename, linked outlet save, and master job status boundaries.
- Custom domain, compliance override, temporary status, set-claims store switching, and menu cache revalidation boundaries.
- Public pull API key generate/revoke now requires `MANAGE_INTEGRATIONS` before writing `stores/{storeId}.publicApi`.
- Public API key, temp-status, compliance, custom-domain, menu revalidation, POS sync, outlet lifecycle/policy, and linked outlet save mutations now use bounded JSON parsing before validation, provider calls, public cache tag work, project reads, outbound POS fetches, or Firestore writes.
- Claim-account, change-password, switch-store, and phone OTP auth/session mutations now use bounded JSON parsing; phone OTP applies IP throttles before parsing.
- Staff list/create/update/remove and role authority boundaries.
- `/api/screen/seen` now rejects oversized anonymous bodies, including no-length streamed bodies, and applies the `SCREEN_SEEN_SIGNAL` IP rate limit before Firestore lookup/write work.
- Public analytics tracking and guest feedback submission now use bounded JSON parsing before target validation, Turnstile checks, Firestore reads, analytics writes, or feedback writes.
- `/api/razorpay/webhook` now rejects malformed/oversized raw bodies, applies the `WEBHOOK` IP rate limit, and validates signatures before JSON parsing, idempotency claims, or billing mutations.
- Authenticated Razorpay payment actions, onboarding subscription setup, and reseller write mutations now use bounded JSON parsing before validation/provider/Firestore work.
- Reseller confirm-payment, renewal, and platform management POSTs now use the existing `DATA_WRITE` write limiter before body parsing or mutation work.

During the same public write-path review, `POST /api/auth/claim-account` was hardened so Mode 1 Google OAuth and Mode 2 email/password claims revalidate public menu, Official Business Page, store, and client-store cache after syncing the claimed store email. `npm run verify:public-business-truth` now locks that claim-account cache-invalidation contract.

Validation:

- `npm run verify:auth-security-failure-matrix` - passed.
- `npm run verify:public-business-truth` - passed.
- `npm run verify:menulist-api-tenant-safety` - passed.
- `npx tsc --noEmit --incremental false --pretty false` - passed.
- Targeted raw `request.json()` sweep for public API key, temp status, compliance, domain, menu revalidation, POS sync, outlet lifecycle/policy, and linked outlet save routes - passed with no matches.
- Targeted raw `request.json()` sweep for claim-account, change-password, switch-store, and phone OTP start/verify routes - passed with no matches.
- Targeted raw `request.json()` sweep for AI Menu Manager command/proposal fallback routes - passed with no matches.
- `git diff --check` - passed.

Verdict update:

The protected MenuList owner API family and adjacent public customer signal routes now have repeatable source-level guard coverage for auth, selected-store scope, tenant/store checks, permission helpers, rate limits, public-cache invalidation, staff/outlet scoping, public customer signal body admission, screen seen cheap-fail ordering, Razorpay webhook bounded raw-body ordering, authenticated billing/reseller bounded-body ordering, public-truth/session mutation bounded-body ordering, auth/session admission ordering, and AI Menu Manager fallback admission ordering. This does not complete live wrong-tenant/wrong-store authenticated fixture testing; that still requires a controlled second-tenant runtime dataset.

---

## Follow-Up Audit: Owner Dashboard And Transactions Route And Language Support

**Audit date:** June 11, 2026  
**Scope:** Owner desktop dashboard, forced owner mobile dashboard, desktop Transactions, forced mobile Transactions, shared AI-operation transaction presentation, route-level localhost behavior, and locale-key coverage.

### Feature Identification

Relevant runtime files inspected and changed:

- `src/components/templates/main-app/dashboard/OwnerDashboard/index.tsx`
- `src/components/templates/main-app/dashboard/OwnerDashboard/ViewModeTabs.tsx`
- `src/components/templates/main-app/dashboard/OwnerDashboard/TodaySoFarCard.tsx`
- `src/components/templates/main-app/dashboard/OwnerDashboard/DailyView.tsx`
- `src/components/templates/main-app/dashboard/OwnerDashboard/WeeklyView.tsx`
- `src/components/templates/main-app/dashboard/OwnerDashboard/MonthlyView.tsx`
- `src/components/templates/main-app/dashboard/OwnerDashboard/OverviewView.tsx`
- `src/components/templates/main-app/dashboard/OwnerDashboard/OverallFooter.tsx`
- `src/components/templates/main-app/dashboard/OwnerDashboard/OBPMetricsCard.tsx`
- `src/components/templates/main-app/dashboard/OwnerDashboard/MenuAnalyticsDetailsCard.tsx`
- `src/components/templates/main-app/dashboard/OwnerDashboard/OwnerActionPlanCard.tsx`
- `src/components/templates/main-app/dashboard/OwnerDashboard/DashboardProjectSelector.tsx`
- `src/components/templates/main-app/dashboard/OwnerDashboard/AISummaryCard.tsx`
- `src/components/templates/main-app/dashboard/OwnerDashboard/TopItemsList.tsx`
- `src/components/templates/main-app/dashboard/OwnerDashboard/HealthSignalCards.tsx`
- `src/components/templates/main-app/dashboard/OwnerDashboard/GoogleListingCard.tsx`
- `src/components/templates/main-app/dashboard/OwnerDashboard/BehaviorNudgeCard.tsx`
- `src/components/templates/main-app/dashboard/OwnerDashboard/HoursFreshnessNudge.tsx`
- `src/components/templates/main-app/dashboard/MenuQualitySignals.tsx`
- `src/components/templates/main-app/dashboard/AnalyticsDashboard/CustomerAppMetrics.tsx`
- `src/components/mobile/screens/MobileDashboardScreen.tsx`
- `src/components/mobile/screens/dashboardSections/MobileMenuAnalyticsDetailsCard.tsx`
- `src/components/mobile/screens/dashboardSections/MobileOBPMetricsCard.tsx`
- `src/components/mobile/screens/dashboardSections/MobileCustomerAppMetrics.tsx`
- `src/components/mobile/screens/dashboardSections/MobileOwnerActionPlanCard.tsx`
- `src/components/templates/main-app/transactions/index.tsx`
- `src/components/templates/main-app/transactions/TransactionDetailsModal.tsx`
- `src/components/templates/main-app/transactions/transaction-details/DescriptionDetailsView.tsx`
- `src/components/templates/main-app/transactions/transaction-details/ImageProcessingDetailsView.tsx`
- `src/components/templates/main-app/transactions/transaction-details/LanguageDetailsView.tsx`
- `src/components/mobile/screens/MobileTransactionsScreen.tsx`
- `src/lib/ai/operationPresentation.ts`
- `src/lib/analytics/ownerDashboardDetails.ts`
- `public/locales/menulist.ai/en-US.json`
- `public/locales/menulist.ai/ar-SA.json`
- `public/locales/menulist.ai/bn-IN.json`
- `public/locales/menulist.ai/en-GB.json`
- `public/locales/menulist.ai/es-ES.json`
- `public/locales/menulist.ai/gu-IN.json`
- `public/locales/menulist.ai/hi-IN.json`
- `public/locales/menulist.ai/mr-IN.json`
- `public/locales/menulist.ai/ta-IN.json`
- `public/locales/menulist.ai/te-IN.json`
- `public/locales/menulist.ai/zh-CN.json`
- `src/i18n/request.ts`

Surfaces covered:

- Owner dashboard: yes, desktop route, dashboard mode tabs, analytics/detail cards, status cards, project selector, no-action states, and forced mobile dashboard route.
- Mobile owner flow: yes, `?mobileAudit=1` dashboard and `/transactions` mapped through `MobileShell`, plus mobile dashboard section cards used by that shell.
- Public/customer page: no runtime change.
- Official Business Page: no runtime change.
- Website/marketing claim: no runtime change.
- Admin/internal operations: yes, platform-owner-visible transaction debug labels were localized.
- Billing/entitlement: no runtime change.
- Analytics/feedback/reviews: dashboard analytics presentation only; no analytics data model change.
- Multi-location behavior: no runtime change.
- Cache/public truth: no runtime change.

### End-To-End Request/Data Flow

Dashboard:

1. Authenticated owner opens `/dashboard`.
2. Main layout resolves session and wraps the route in `LocalisationProvider`.
3. `OwnerDashboard` reads selected store/project state and calls existing dashboard hooks.
4. Desktop dashboard shell now reads headings, tab labels, helper copy, and error state from `Dashboard.owner`.
5. Dashboard subcomponents build detail rows, OBP metrics, customer-app metrics, health signals, action plans, no-action states, and quick stats from `Dashboard.owner`.
6. Forced mobile shell opens `/dashboard?mobileAudit=1` and routes to the mobile Today/Dashboard screen through `MobileShell`.
7. `MobileDashboardScreen` and mobile dashboard section cards now read view-mode labels, metric labels, helper copy, cards, empty states, action-plan copy, and OBP/customer-app detail labels from `MobileDashboard` or shared `Dashboard.owner` keys.

Transactions:

1. Owner opens `/transactions` or mobile `/transactions?mobileAudit=1`.
2. Desktop/mobile screens call existing `getPaginatedAiOperations()` pagination.
3. Firestore/API read behavior is unchanged: paginated, 15 records per page, no listener.
4. Shared action labels, credit text, and owner summaries now flow through `src/lib/ai/operationPresentation.ts` with an optional `Transactions` translator and English fallbacks.
5. Desktop table, details modal, nested language/description/image panels, mobile list, mobile detail sheet, and filter sheet use `Transactions` keys.
6. Missing locale keys remain safe because `src/i18n/request.ts` deep-merges every selected locale over `en-US`.

### Correctness Findings And Fixes

Fixed:

- Desktop Transactions had hard-coded `Result`, `Credits Used`, page counters, paging buttons, filter state, and detail-modal labels.
- Mobile Transactions had hard-coded filter validation messages, detail sheet labels, empty state, platform debug labels, and transaction summaries.
- Shared transaction presentation helpers returned English-only action labels, owner summaries, and credit labels, which affected both desktop and mobile.
- Desktop dashboard shell had hard-coded view headings, tab labels, helper copy, and error state.
- Mobile dashboard top-level labels still depended on hard-coded `VIEW_MODE_CONFIG` labels and local string literals for key metric and helper copy.
- Deeper desktop dashboard cards still had hard-coded owner copy in Today, Daily, Weekly, Monthly, Overview, Overall, OBP, customer-app metrics, AI summary, top-items, quality signals, health signals, behavior nudges, Google listing, and hours freshness surfaces.
- Deeper mobile dashboard cards still had hard-coded owner copy in menu analytics details, OBP metrics, customer-app metrics, and owner action-plan sections.
- The monthly dashboard used a non-pluralized activity-days label that produced `1 days of activity recorded`.

Implementation notes:

- Existing transaction data, raw model output, IDs, language names, item names, and platform debug JSON remain unmodified.
- No entitlement, auth, billing, Firebase, cache, or public-route behavior was changed.
- Answerlattice action labels remain only ledger labels inside the shared AI operation transaction history; no Answerlattice route, collection, or behavior was changed.
- Non-English locale files were deep-filled with the new `Dashboard.owner` and `MobileDashboard` key structure from `en-US` to prevent runtime missing-key failures. This is runtime-safe fallback coverage, not a completed human translation pass.

### Firebase Cost Audit

Transactions:

- Reads per first page: unchanged existing paginated AI-operation read, page size 15.
- Writes per owner action: unchanged, none from this pass.
- Listener usage: unchanged, no new realtime listener.
- N+1 risk: unchanged; project names still load only when the current page includes project IDs.
- Cache behavior: unchanged; this feature is owner ledger UI and does not invalidate public truth.

Dashboard:

- Reads per page load: unchanged existing dashboard/OBP/business-health hooks.
- Writes per owner action: unchanged, none from this pass.
- Listener usage: unchanged.
- Public cache: no public truth write path touched.

Cost verdict for this follow-up: no new Firebase reads, writes, listeners, indexes, Cloud Functions, cache tags, or public revalidation paths.

### UI/UX And Mobile Parity

Verified on `npm run dev` localhost:

- Desktop `/dashboard?e-locale=hi-IN` returned `200 OK` and rendered for the authenticated platform-owner test user.
- Desktop dashboard mode tabs were visually checked in Chrome for `Overview`, `Yesterday`, `Last 7 Days`, `This Month`, `Overall`, and `Today` with no missing translation-key text.
- Desktop `/transactions?e-locale=hi-IN` returned `200 OK` and rendered the transaction table/list without missing transaction-key text.
- Forced mobile `/dashboard?mobileAudit=1&e-locale=hi-IN#mobile/today/dashboard` returned `200 OK` and rendered the mobile dashboard route through `MobileShell` without missing translation-key text.
- Forced mobile `/transactions?mobileAudit=1&e-locale=hi-IN` returned `200 OK` and rendered the mobile Transactions route without missing translation-key text.

Remaining UI/language parity risks:

- Supported-language translations are now present for the newly added `Dashboard.owner` and `MobileDashboard` keys across `ar-SA`, `bn-IN`, `es-ES`, `gu-IN`, `hi-IN`, `mr-IN`, `ta-IN`, `te-IN`, and `zh-CN`, with `en-GB` using British-English wording where it differs. Native-speaker review remains recommended before treating these strings as final copy.
- Chrome visual QA completed the desktop dashboard tab cycle before the Chrome native pipe dropped. In-app Browser fallback verified route render and missing-key absence for mobile routes, but a local offline modal blocked reliable interactive switching of every mobile dashboard sub-tab.
- Current in-app Browser clicks on the Transactions table action buttons did not reopen the detail modal during the final fallback pass. The transaction details modal had already been covered in the earlier Chrome pass, but it should be re-run once the Chrome pipe is reconnected.

### Documentation Alignment

Updated:

- `__docs__/audits/menulist-production-readiness-audit.md`
- `__docs__/changelog.md`

Docs were aligned only to verified runtime behavior. No future capability was documented as shipped.

### Validation

Commands run:

- `npx tsc --noEmit --incremental false --pretty false` - passed
- `npm run lint` - passed
- `git diff --check` - passed
- Locale coverage script for `Dashboard.owner` and `MobileDashboard` keys across all `public/locales/menulist.ai/*.json` files - passed
- Locale translation integrity script for `Dashboard.owner` and `MobileDashboard` - passed with zero missing keys, zero broken placeholders, zero leftover translation placeholder tokens, and zero ICU plural issues across all supported locale files.
- `npm run dev` - passed for localhost validation
- HTTP route probes with `e-locale=hi-IN` returned `200 OK` for `/dashboard`, `/transactions`, `/dashboard?mobileAudit=1`, and `/transactions?mobileAudit=1`.
- Chrome visual/DOM QA passed for the desktop dashboard route and mode tabs, desktop Transactions route, and earlier transaction details modal coverage. In-app Browser fallback verified desktop dashboard, mobile dashboard route, desktop Transactions route, and mobile Transactions route after a clean `.next` cache reset.
- `npm run build` was intentionally not run per owner instruction to validate on `npm run dev` localhost instead.

Known runtime notes:

- Local dev logs showed Upstash rate-limit provider timeouts during auth/access-status checks; route responses still completed. This is an environment/provider latency issue already isolated from this localization pass.
- A local aborted `set-claims` request appeared during mobile route switching; the authenticated session remained valid and the tested routes rendered. This pass did not modify claims behavior.

### Follow-Up Verdict

**Controlled owner testing ready for Dashboard and Transactions desktop/mobile route and supported-language coverage.**

This is not a full MenuList application certification and not a native-speaker translation certification. It covers the Dashboard and Transactions route family end to end for route availability, source-level string coverage, Firebase-cost preservation, runtime missing-key safety, and translated Dashboard/MobileDashboard locale values across the supported locale files.

---

## Follow-Up Audit: MenuList Route Inventory And Localhost Smoke

**Audit date:** June 11, 2026  
**Scope:** MenuList `src/app` page-route inventory, authenticated localhost page-route smoke, route-handler inventory, mobile shell route mapping, public/static discovery handlers, and product-boundary separation.

### Route Inventory

Source inventory from `src/app` found:

- 103 MenuList page routes.
- 115 MenuList API route handlers.
- 5 MenuList public/static route handlers.
- 103 Answerlattice page routes, intentionally excluded from MenuList QA.
- 2 CampaignCue page routes, intentionally excluded from MenuList QA.
- 5 MyCodex page routes, intentionally excluded from MenuList QA.

The MenuList mobile owner shell maps desktop owner routes into `MobileShell` through `OWNER_PATH_TO_MOBILE_ROUTE`, `PLATFORM_PATH_TO_MORE_SCREEN`, `OPS_PATH_TO_MORE_SCREEN`, and `RESELLER_PATH_TO_MORE_SCREEN` in `src/components/mobile/MobileShell.tsx`. Source inspection covered the mapped owner/mobile route families for Dashboard, Today, Projects/Menu, Share/Use MenuList, Assets/Print Assets, Print Menu, QR, Feedback, Business Settings, Transactions, Billing, Locations, Users, Roles, Platform, Ops, and Reseller screens.

### Localhost Page Smoke

Validation used `npm run dev` on localhost, not `npm run build`.

Authenticated smoke:

- A fresh in-memory NextAuth credentials session was established for the platform-owner account.
- 101 non-Sentry MenuList page routes were fetched with `?e-locale=hi-IN`.
- 100 routes returned `200`.
- The explicit `/404` page returned `404`, as expected for that route.
- 0 tested routes returned `5xx`, timed out, or rendered `MISSING_MESSAGE`, `IntlError`, or `application error` markers.
- 2 routes were skipped because they intentionally trigger monitoring errors: `/test-sentry` and `/platform/test-sentry`.

Route behavior observed:

- `/ops/extraction`, `/ops`, and `/ops/scheduler` redirect to their current platform monitor routes.
- `/qrCode` redirects to `/qr-code`.
- `/product` redirects to `/how-it-works`.
- Platform-host `/client` and `/client/pwa/*` placeholders redirect to `/` without crashing. This is not proof of tenant-menu rendering; tenant-host customer menu proof remains tracked under the public truth routing audit.

### API Route Inventory

Static API inventory found 115 MenuList API handlers:

- 83 handlers expose `POST`.
- 41 handlers expose `GET`.
- 3 handlers expose `DELETE`.
- 5 handlers expose `OPTIONS`.
- 2 handlers expose `PATCH`.

This pass did not bulk-call POST, PATCH, or DELETE route handlers because many are mutating billing, staff, outlet, public truth, image, extraction, POS, notification, or owner-assistant operations. GET API routes were covered only where already exercised by the route/page QA or targeted feature QA, such as auth/session/access-status, transactions AI operations, owner-business-assistant current state, and platform monitor reads. This preserves Firebase cost discipline and avoids destructive side effects.

### Public Static Route Findings

Safe public/static probes:

- `/sitemap.xml` returned `200`.
- `/manifest.webmanifest` returned `{}` with `404` on the platform host, which matches the route contract: the customer-app manifest is tenant-host only.
- `/client/robots.txt`, `/client/robots`, and `/client/sitemap.xml` redirected to `/` on the platform host.
- The active platform-owner store returned `{"hasDomain":false}` from `/api/domain`, so this pass did not have a configured current tenant subdomain for tenant-host manifest proof.

### Firebase Cost Impact

- Page-route smoke caused normal page-load reads for owner/platform/public routes and should not be treated as a production workload.
- No mutating API route was called in the all-routes smoke.
- No new reads, writes, listeners, indexes, Cloud Functions, revalidation paths, or cache tags were added by this route audit.
- Bulk API mutation smoke was explicitly avoided to prevent unnecessary Firestore writes and billing/provider side effects.

### Remaining Route QA Gaps

- Full visual QA of every mobile `MoreSubScreen` remains pending. The source route map is now locked by `verify:mobile-shell-route-map`, and Dashboard/Transactions mobile routes were visually checked, but every mobile sub-screen was not interactively opened because Chrome's native pipe dropped and the in-app browser showed a local offline overlay during mobile shell testing.
- Tenant-host manifest and tenant-host customer PWA shortcuts need a configured test store subdomain/custom domain. The active platform-owner test store had no domain configured.
- API handlers are inventoried, but not all are live-called. Mutating handlers require feature-by-feature tests with fixture data and explicit expected write/cost assertions.

### Follow-Up Verdict

**Controlled owner testing ready for MenuList page-route availability on localhost.**

This is still not a full production certification. The page routes are smoke-clean under `npm run dev`; API mutation routes and every mobile sub-screen still require feature-specific QA before claiming full MenuList route certification.

---

## Completed Audit 1: Public Truth Routing, Client Menu, OBP Cache

### A. Feature Identification

**Feature cluster:** Public client menu + OBP + tenant/domain routing + public cache invalidation.

Relevant routes and files inspected:

- `src/middleware.ts`
- `src/lib/multiTenant/domainResolver.ts`
- `src/constants/productDomains.ts`
- `src/constants/urls.ts`
- `src/app/client/[[...slug]]/page.tsx`
- `src/app/client/sitemap.ts`
- `src/app/client/robots.ts`
- `src/app/api/revalidate/menu/route.ts`
- `src/lib/cache/publicClientCache.ts`
- `src/lib/actions/revalidateMenuCache.ts`
- `src/lib/firestore/clientStoreLookup.ts`
- `src/lib/seo/publicTruthIndexing.ts`
- `src/components/templates/website/clientWebsite/index.tsx`
- `src/components/templates/website/mainContentRenderer/index.tsx`
- `__docs__/client-menu/public-routing-doctrine.md`
- `__docs__/client-menu/client-menu_firebase.md`
- `__docs__/url-routing-architecture/url-routing-architecture_firebase.md`

Affected surfaces:

- Owner dashboard: yes, via writes that call public cache revalidation.
- Mobile owner flow: yes, through shared project/store/multi-outlet DAL cache invalidation.
- Public menu/customer page: yes.
- Official Business Page: yes.
- Website/marketing claim: indirectly, through public truth and discovery promises.
- Admin/internal operations: yes, platform entity blocking and public cache revalidation.
- Billing/entitlement: no direct changes in this slice.
- Analytics/feedback/reviews: analytics write path noted; not fully audited yet.
- Multi-location behavior: yes, outlet slug and master project resolution.
- Cache/public truth: yes.

Feature flags inspected:

- `ENABLE_OBP`
- `ENABLE_MULTI_OUTLET`
- `ENABLE_SPECIAL_MENU_SWITCHING`
- `ENABLE_PUBLIC_MENU_RETRIEVAL_FOUNDATION`
- `ENABLE_CUSTOMER_APP_PWA`
- `ENABLE_COMPLIANCE_PAGES`

Firebase collections inspected for this slice:

- `stores`
- `tenants`
- `platformSummary`
- `projects`
- `analytics`
- owner-business-assistant packet cache invalidation path

### B. End-to-End Request/Data Flow

Tenant/public request flow:

1. Middleware classifies host using `resolveDomain()`.
2. Product domains are routed before tenant domains to preserve Answerlattice/MyCodex separation.
3. Tenant subdomain/custom domain requests rewrite into `/client`.
4. `/robots.txt` and `/sitemap.xml` stay tenant-aware instead of bypassing middleware.
5. Client page resolves store via `getStoreBySubdomain()` or `getStoreByCustomDomain()`.
6. Root path renders OBP when `ENABLE_OBP` is on.
7. Multi-outlet master paths check first segment with `getStoreByOutletSlug()`.
8. Project routing reads `platformSummary/projects_{storeId}` first, then reads the resolved project doc by immutable project ID.
9. `/menu` works as a two-layer route: owner-claimed slug first, default-project alias second.
10. Special-menu override performs no extra read unless `storeData.activeSpecialMenuId` exists.
11. Public payload is sanitized before reaching the client renderer.
12. Public renderer opens directly to `PageType.MENU`, not the retired intro screen.
13. Metadata and sitemap use public truth indexability gates.
14. Owner writes invalidate `menu-store-{storeId}`, `store-{storeId}`, `client-stores`, and screen/assistant cache paths where wired.

### C. Correctness Findings

1. **Fixed: public truth block-state drift.**  
   `publicTruthIndexing.ts` checked `blocked` and `tenantBlocked` manually and looked for `blockDetails.active`, while the platform block helper treats `blockDetails.blocked` as blocked. This could leave blocked entities indexable if a future or historical record relied on `blockDetails.blocked`.  
   Fix: `isStoreActiveForPublicTruth()` now uses `isPlatformEntityBlocked()`.

2. **Fixed: cache revalidation API accepted unvalidated body shapes.**  
   `/api/revalidate/menu` accepted arbitrary `storeId` values before building cache tags.  
   Fix: added Zod validation for primitive bounded `storeId` or bounded tag arrays.

3. **Fixed: authenticated cache revalidation lacked route wrapper and store scope.**  
   The route manually called `getServerSession()` and allowed any authenticated session to request store-id cache invalidation.  
   Fix: normal app callers now go through `withAuth()`. Server callers with `x-revalidate-secret` still bypass session auth. Authenticated app callers can revalidate only stores present in their session, unless they are platform admins. Explicit tag arrays are platform-only for authenticated app callers.

4. **Fixed: docs drifted from runtime.**  
   `client-menu_firebase.md` still described the removed project metadata subcollection scan, a separate decision-block document read, and `getStoreById()` as public menu reads.  
   Fix: updated the doc to the current summary-first, embedded-decision-block, shared-store-lookup model.

5. **Fixed: public menu lookup failures could disappear inside fail-open routing.**
   Metadata outlet lookup, metadata project lookup, and runtime outlet lookup used `withRetry(...).catch(() => null)`. Customer rendering intentionally degraded, but the lookup failure itself left no bounded diagnostic.
   Fix: these lookups now log `public_menu_resolution_metadata_outlet_lookup_failed`, `public_menu_resolution_metadata_project_lookup_failed`, and `public_menu_resolution_outlet_lookup_failed` with tenant/store/slug presence-length context only before preserving the existing fail-open behavior.

### D. Firebase Cost Audit

Current verified steady-state menu path:

- Store lookup: cached `stores` query by subdomain or verified custom domain.
- Project summary: cached `platformSummary/projects_{storeId}` doc read.
- Project data: cached direct project doc read.
- Decision blocks: 0 extra reads; embedded in loaded project doc.
- Store details: 0 extra reads after host lookup.
- Outlet lookup: conditional cached `stores` query only for master multi-outlet paths.
- Master project merge: conditional direct project read only for linked outlet projects.
- Special menu: conditional direct project read only when `activeSpecialMenuId` exists.
- Revalidation route: 0 Firestore reads/writes; cache-tag work only.

Remaining cost risk:

- Inherited tenant-block enforcement in `clientStoreLookup.ts` now uses denormalized store-level `tenantBlocked` when present. Legacy stores without that field still fall back to a tenant-doc read on public store-cache misses, preserving correctness until they are touched by a platform tenant block/unblock or a future backfill.

### E. UI/UX Audit

Public path checks:

- Customer menu opens directly to menu content; the retired intro screen is not in the public runtime.
- OBP remains the tenant root for single and multi-location stores.
- Unknown/unresolved project paths degrade to a menu-not-found fallback instead of a hard customer-facing crash.
- Root/outlet OBP keeps public business context before menu navigation.
- Skeleton exists for slow menu loads.

Mobile/public parity:

- The public renderer defaults to mobile device type and `PageType.MENU`.
- No separate mobile data path was introduced.
- Mobile owner write-path parity was not fully audited in this slice beyond shared cache invalidation call-site inventory.

### F. Product/Positioning Audit

The audited runtime supports MenuList as public-business truth infrastructure:

- Hostname resolves to store/project IDs before public rendering.
- Slugs remain lookup handles, not identity.
- OBP stays the public business record.
- `/menu` alias preserves customer utility without duplicating canonical indexable URLs.
- Public truth indexing suppresses weak, expired, blocked, or incomplete records.

No feature-clutter changes were added.

### G. Documentation Alignment

Updated:

- `__docs__/client-menu/client-menu_firebase.md`

Still to audit later:

- `__docs__/official-business-page/*`
- `__docs__/url-routing-architecture/*`
- Website/public claim pages that mention public discovery or QR/public menu behavior

### H. Fixes Made

Changed files:

- `src/lib/seo/publicTruthIndexing.ts`
- `src/app/api/revalidate/menu/route.ts`
- `__docs__/client-menu/client-menu_firebase.md`
- `__docs__/audits/menulist-production-readiness-audit.md`

### I. Validation Performed

Commands run:

- `npx tsc --noEmit --incremental false --pretty false` — passed
- `npm run lint` — passed
- `git diff --check` — passed

Manual/code review validation:

- Verified middleware product-domain priority before tenant routing.
- Verified client tenant rewrites preserve tenant-aware robots/sitemap.
- Verified client menu resolver uses summary-first slug/default lookup.
- Verified public renderer no longer opens to the retired intro screen.
- Verified cache revalidation call-site inventory includes projects, stores, PWA, multi-outlet, extraction, functions, outlet APIs, temp status, entity blocks, and messaging/public create-menu paths.

Browser/runtime validation:

- Not run in this slice. No local dev server was started because the first pass focused on static runtime tracing and code-level fixes. Runtime browser verification remains required before certifying the full public surface.

Build validation:

- `npm run build` not run because the full production-readiness audit is not complete. Build remains a global completion gate.

---

## Completed Audit 2: Stores Management, Business Settings, Public Store Writes

### A. Feature Identification

**Feature cluster:** Stores Management + owner Business Settings + custom domain routing + time-slot preset public-output cleanup.

Relevant routes and files inspected:

- `src/database/stores/index.tsx`
- `src/database/projects/index.ts`
- `src/database/platformSummary/index.ts`
- `src/app/api/domain/route.ts`
- `src/app/api/store/temp-status/route.ts`
- `src/app/api/store/public-api-key/route.ts`
- `src/app/api/platform/entity-blocks/route.ts`
- `src/components/templates/main-app/businessSettings/index.tsx`
- `src/components/templates/main-app/businessSettings/tabs/DomainSettingsTab.tsx`
- `src/components/templates/main-app/businessSettings/tabs/TimeSlotPresetsTab.tsx`
- `src/components/mobile/screens/MobileBasicSettingsScreen.tsx`
- `src/components/mobile/screens/MobileOfficialPageScreen.tsx`
- `src/components/mobile/screens/MobileBusinessAttributesScreen.tsx`
- `src/components/mobile/screens/MobileCustomerAppScreen.tsx`
- `src/components/mobile/screens/MobileDomainSettingsScreen.tsx`
- `src/components/mobile/screens/MobileLocaleSettingsScreen.tsx`
- `src/components/mobile/screens/MobileHoursScreen.tsx`
- `src/components/mobile/screens/MobileWorkingHoursEditScreen.tsx`
- `src/components/mobile/screens/MobileTempStatusScreen.tsx`
- `src/components/mobile/screens/MobileTimeSlotsScreen.tsx`
- `src/components/mobile/screens/MobileSeoAnalyticsScreen.tsx`
- `src/lib/firestore/clientStoreLookup.ts`
- `src/lib/actions/revalidateMenuCache.ts`
- `__docs__/stores-management/*`
- `__docs__/official-business-page/official-business-page_firebase.md`
- `__docs__/client-menu/autosell-features/_spec.md`
- `__docs__/client-menu/autosell-features/_impl.md`

Affected surfaces:

- Owner dashboard: yes, Business Settings, domain, hours, public profile, PWA/customer app, SEO, temp status, time-slot presets.
- Mobile owner flow: yes, mobile equivalents listed above.
- Public menu/customer page: yes, store identity, domain routing, hours/temp status, category time windows.
- Official Business Page: yes, publicPresence, custom domain, metadata, business attributes, media.
- Website/marketing claim: indirectly; docs only updated, no public website copy changed.
- Admin/internal operations: yes, Stores Dashboard and Entity Blocks.
- Billing/entitlement: only observed activePlanType summary sync; billing audit still queued.
- Analytics/feedback/reviews: feedback settings observed; analytics internals still queued.
- Multi-location behavior: yes, store summary, outlet slug, master propagation observed but not fully certified.
- Cache/public truth: yes.

Firebase collections inspected for this slice:

- `stores`
- `tenants`
- `platformSummary`
- `projects/{tId}/{sId}`
- `analytics` references for OBP/temp-status context

### B. End-to-End Request/Data Flow

Owner store-settings flow:

1. Desktop Business Settings builds normalized form state from `storeDetails`.
2. Mobile owner screens use the same `PlatformGlobalDataContext` store data and call the same DAL/API paths for their specific settings.
3. `updateStore()` normalizes language policy, resolves business category, protects published subdomains from mutation, recomputes scheduler hour when timezone/EOD changes, writes `stores/{storeId}`, and revalidates public cache through `/api/revalidate/menu`.
4. Summary-relevant store fields sync into `platformSummary/storesSummary`.
5. Master store propagation runs only for configured master-propagation fields and outlet-policy-allowed stores.
6. Public pages resolve the updated store through shared cached lookup helpers and render OBP/menu/customer app state from the store/project truth.

Custom-domain flow:

1. Owner checks availability from desktop or mobile.
2. `/api/domain` validates auth and `MANAGE_PUBLIC_PRESENCE`, applies the domain-management limiter, rejects bodies above 4KB before JSON parsing, validates the domain, checks existing active stores, and calls Vercel domain APIs.
3. The route writes `stores/{storeId}` domain fields only after Vercel add/remove or verification status is known.
4. The route now invalidates `menu-store-{storeId}`, `store-{storeId}`, `client-stores`, and owner-assistant packet cache.
5. Desktop and mobile screens update local state after the API succeeds without duplicating the Firestore write.

Time-slot preset flow:

1. Desktop/mobile owner creates, edits, or deletes a store-level time-slot preset.
2. `updateTimeSlotPresets()` writes `stores/{storeId}.timeSlotPresets` and stamps `modifiedOn`.
3. Editing an existing preset now calls `updatePresetInAllCategories()`.
4. That helper scans only the current store's project docs, updates category `timeSlots[]` entries with the matching `presetId`, writes only changed projects, and revalidates affected public project/store cache.
5. Deleting a preset continues to call `removePresetFromAllCategories()` and revalidates changed projects.

### C. Correctness Findings

1. **Fixed: custom-domain API invalidated only `client-stores`.**  
   Domain add/verify/remove writes affect public routing, canonical metadata, redirects, OBP, and menu lookup. The route now uses `revalidateMenuCache()` so all required public truth tags and owner-assistant cache are invalidated.

2. **Fixed: desktop custom-domain UI duplicated API-owned writes.**  
   After `/api/domain` wrote Firestore, the desktop tab called `onStoreUpdate()`, which called `updateStore()` for the same fields. This caused duplicate store writes, duplicate cache invalidation, and possible summary noise. The tab now uses a local state patch callback for API-owned domain changes while preserving `updateStore()` for subdomain saves.

3. **Fixed: edited time-slot presets did not update assigned category windows.**  
   Category visibility stores copied `startTime`/`endTime` values alongside `presetId` to avoid an extra public store read. Editing a preset changed only the store preset list, leaving already assigned categories on old windows. Desktop and mobile edit flows now cascade changed times into assigned categories and revalidate affected public cache.

4. **Fixed: time-slot preset store writes did not stamp `modifiedOn`.**  
   `updateTimeSlotPresets()` now writes `modifiedOn: serverTimestamp()` with the preset list.

5. **Fixed: TypeScript validation blocker from a separate product registry change.**  
   `DeploymentProductId` included `campaigncue`, but `PRODUCT_PROJECT_VARS` in `validateEnv.ts` did not. Added an empty CampaignCue requirement entry and display label. This does not change MenuList runtime behavior; it only restores exhaustive typing for the shared deployment map.

6. **Docs drift fixed.**  
   Stores, OBP, and time-slot docs now describe current runtime behavior: shared public store lookup helpers, full domain cache invalidation, desktop/mobile owner parity, and time-slot edit/delete cascades.

7. **Fixed: public-truth mutation routes parsed action bodies without shared route caps.**
   Custom-domain actions now reject bodies above 4KB before domain validation or Vercel calls. Temp status now uses a `DATA_WRITE` limiter and 4KB body cap before validation. Compliance override/reset now requires `MANAGE_PUBLIC_PRESENCE` or `MANAGE_STORE`, uses a `DATA_WRITE` limiter, and rejects bodies above 32KB before sanitization or writes. Public pull API key generate/revoke now requires `MANAGE_INTEGRATIONS` and a 1KB body cap. Menu cache revalidation now caps action bodies at 4KB before tag validation.

### D. Firebase Cost Audit

Verified owner store writes:

- `updateStore()` writes one store doc.
- Summary sync writes one `platformSummary/storesSummary` merge only when summary-relevant fields are present.
- `updateStore()` may read the current store only when needed for subdomain immutability, summary field resolution, scheduler recompute, or master propagation checks.
- Domain add/remove writes one store doc; verification writes only on false -> true transition.
- Time-slot preset create writes one store doc.
- Time-slot preset edit writes one store doc plus reads current-store project docs and writes only projects containing matching assigned categories.
- Time-slot preset delete writes one store doc plus existing 0-N changed project writes.
- Temp status writes one store doc and revalidates all public store tags.
- Entity block store flow writes the entity doc, summary patch, and revalidates affected store cache.
- Entity block tenant flow reads `storesSummary`, queries existing tenant stores, writes `tenantBlocked` to the summary and direct store docs for affected stores, and revalidates affected stores.

Cost fixes:

- Removed duplicate desktop custom-domain store writes after `/api/domain`.
- Kept time-slot edit cascade bounded to rare owner actions and changed project docs only.
- Tenant block state is now denormalized onto affected store docs during platform tenant block/unblock. Public lookup uses the store-level flag when present and keeps the tenant-doc fallback for legacy stores.
- Bounded body admission adds no Firestore writes for rejected oversized bodies. Routes that intentionally run auth, tenant, or permission checks first may still perform those existing guard reads before action parsing.

### E. UI/UX Audit

Desktop:

- Business Settings remains grouped by business profile, search/discovery, locale, hours, time slots, analytics, feedback, and POS sync.
- Subdomain locking remains visible after publish and enforced in `updateStore()`.
- Custom-domain add/remove/verify stays in the dedicated domain tab.
- Time-slot delete already had recoverable confirmation; edit now preserves public behavior without adding owner decisions.

Mobile:

- Owner mobile parity exists for business profile, public info, OBP, custom attributes, customer app, domains/subdomain, locale, hours/temp status, time slots, and SEO/analytics.
- Mobile time-slot edits now perform the same category cascade as desktop.
- Mobile domain screens already used `/api/domain` without duplicate `updateStore()` writes.
- Platform admin store CRUD remains desktop-only; this is acceptable because it is an internal admin workflow, not SMB owner operation.

Public/customer:

- Custom-domain changes now invalidate canonical/routing cache immediately.
- Category time-window edits now reach public menu output through changed project revalidation.
- Public pages still avoid extra reads for preset lookup because category time windows remain embedded in project data.

### F. Product/Positioning Audit

The fixes preserve MenuList's public-business truth role:

- Public routing changes are treated as truth-packet changes, not as cosmetic settings.
- Time-slot preset edits silently keep public category visibility correct without asking owners to manually revisit categories.
- No new owner-facing settings, toggles, or feature clutter were added.
- Store/admin separation remains intact: platform CRUD stays internal; SMB owner public truth controls stay mobile-capable.

### G. Documentation Alignment

Updated:

- `__docs__/stores-management/README.md`
- `__docs__/stores-management/stores-management_firebase.md`
- `__docs__/stores-management/stores-management_impl.md`
- `__docs__/stores-management/stores-management_mobile-support.md`
- `__docs__/stores-management/stores-management_spec.md`
- `__docs__/official-business-page/official-business-page_firebase.md`
- `__docs__/client-menu/autosell-features/_spec.md`
- `__docs__/client-menu/autosell-features/_impl.md`
- `__docs__/audits/menulist-production-readiness-audit.md`

### H. Fixes Made

Changed MenuList/runtime files:

- `src/app/api/domain/route.ts`
- `src/components/templates/main-app/businessSettings/index.tsx`
- `src/components/templates/main-app/businessSettings/tabs/DomainSettingsTab.tsx`
- `src/components/templates/main-app/businessSettings/tabs/TimeSlotPresetsTab.tsx`
- `src/components/mobile/screens/MobileTimeSlotsScreen.tsx`
- `src/database/projects/index.ts`
- `src/database/stores/index.tsx`

Changed shared validation file:

- `src/lib/env/validateEnv.ts`

### I. Validation Performed

Commands run:

- `npx tsc --noEmit --incremental false --pretty false` — passed after adding the missing `campaigncue` env-map key
- `npm run lint` — passed
- `git diff --check` — passed

Manual/code review validation:

- Verified desktop and mobile domain flows use `/api/domain`.
- Verified `/api/domain` is auth/permission guarded and now invalidates full public tags.
- Verified desktop custom-domain UI no longer performs duplicate store writes after API success.
- Verified `updateStore()` remains the shared cache invalidating path for owner store/publicPresence saves.
- Verified temp status and entity-block routes revalidate full public store tags.
- Verified time-slot edit/delete cascades write only changed current-store projects and revalidate affected project cache.
- Verified mobile owner parity for the audited store setting surfaces.

Browser/runtime validation:

- Not run in this slice. Local browser smoke remains required before full certification.

Build validation:

- `npm run build` not run because the full production-readiness audit is not complete. Build remains a global completion gate.

---

## Completed Audit 3: Projects/Menu Builder, Public Project Writes, Use MenuList Outputs

### A. Feature Identification

**Feature cluster:** Projects/Menu Builder lifecycle, project summary/public write paths, read-only output surfaces that consume project summaries.

Relevant routes, components, hooks, DAL, APIs, cache paths, docs, and mobile equivalents inspected:

- `src/database/projects/index.ts`
- `src/components/templates/main-app/projects/index.tsx`
- `src/components/templates/main-app/projects/editorView/Editor.tsx`
- `src/components/templates/main-app/projects/types/project.types.ts`
- `src/components/mobile/providers/MobileProjectsProvider.tsx`
- `src/components/mobile/components/MobileProjectSelectorSheet.tsx`
- `src/components/mobile/screens/MobileMenuScreen.tsx`
- `src/components/templates/main-app/useMenuList/index.tsx`
- `src/components/templates/main-app/businessSettings/OBPLinkCard.tsx`
- `src/components/templates/main-app/dashboard/OwnerDashboard/DashboardProjectSelector.tsx`
- `src/components/templates/main-app/today/PastActivity/index.tsx`
- `src/components/templates/main-app/growthos/index.tsx`
- `src/components/templates/main-app/printableAssetTemplates/PrintableAssetTemplatesRoute.tsx`
- `src/components/templates/main-app/transactions/index.tsx`
- `src/components/templates/main-app/ownerBusinessAssistant/BusinessHealthProjectScopeSelector.tsx`
- `src/hooks/useMenuCardExportController.ts`
- `src/lib/cache/publicClientCache.ts`
- `src/lib/actions/revalidateMenuCache.ts`
- `src/app/api/projects/outlet-save/route.ts` reviewed by call path from `updateProject()`
- `__docs__/projects/project-management/*`
- `__docs__/use-menulist/*`

Affected surfaces:

- Owner dashboard: yes, project selector and menu truth writes.
- Mobile owner flow: yes, mobile project provider and selector sheet use the same DAL.
- Public menu/customer page: yes, project summaries and full project docs drive public route resolution and menu rendering.
- Official Business Page: yes, default/direct project links and `/menu` alias behavior depend on project summary state.
- Website/marketing claim: no public website copy changed; Use MenuList docs aligned to runtime.
- Admin/internal operations: no direct admin change in this slice.
- Billing/entitlement: no direct billing changes; GrowthOS project selector was touched only to avoid hidden project creation.
- Analytics/feedback/reviews: Past Activity and feedback link generation were inspected as project-summary consumers; full analytics audit remains queued.
- Multi-location behavior: yes, `updateProject()` outlet-save handoff and inherited project delete/deactivate guard observed.
- Cache/public truth: yes.

Firebase collections inspected for this slice:

- `projects/{tId}/{sId}`
- `platformSummary/projects_{sId}`
- `platformSummary/campaigns_{sId}` for Use MenuList screen state
- `stores/{storeId}` only for linked feature calls and starter activation signal context

Feature flags observed:

- `ENABLE_MULTI_OUTLET`
- `ENABLE_PROJECT_PROPAGATION`
- `ENABLE_MASTER_UPDATE_AWARENESS`
- `ENABLE_MCE`
- `ENABLE_MENU_OBSERVATION`
- `ENABLE_MENU_SNAPSHOTS`
- `ENABLE_SPECIAL_MENU_SWITCHING`
- `ENABLE_USE_MENULIST`
- `ENABLE_MENU_CARD_EXPORT`
- `ENABLE_PRINTABLE_ASSET_TEMPLATES`
- `ENABLE_PRINT_ASSETS_ROUTE`
- `ENABLE_CUSTOMER_APP_PWA`
- `ENABLE_GROWTHOS_ADDON`

### B. End-to-End Request/Data Flow

Project list/menu-management flow:

1. Desktop Projects and the mobile project provider read `platformSummary/projects_{sId}` through `getProjectsList*()`.
2. If the owner enters a menu-management flow and no project exists, the DAL intentionally creates a default project and summary entry.
3. The selected project then loads full data from `projects/{tId}/{sId}/{projectId}`.
4. Editor/mobile changes call shared DAL functions for metadata, content, active status, publish, duplicate, delete, and restore.
5. Public-impacting writes call `revalidatePublicClientCacheForProject()`, which invalidates public cache, owner-assistant browser cache, and digital screen content version.

Read-only output/dashboard flow:

1. Dashboard selector, Use MenuList, OBP link card, Past Activity, GrowthOS, print/export, transactions, and business-health selectors read project summaries.
2. These surfaces now use `getExistingProjectsListWithoutLoader()` where an empty state is valid.
3. Empty stores remain empty on these surfaces instead of silently creating public menu truth.
4. Use MenuList reads screen state from `platformSummary/campaigns_{sId}` and full selected project data only for on-demand PDF/export fallback actions.

Delete/restore flow:

1. Owner deletes a project from desktop or mobile.
2. `deleteProject()` reads the current summary, stores a `deletedSummary` tombstone on the full project doc, removes the summary entry, and promotes a fallback default if needed.
3. Restore reads both the project doc and current summary.
4. `restoreProject()` rebuilds the summary from `deletedSummary` when available and clears the tombstone.
5. If another active non-special default already exists, the restored project is restored as non-default to preserve a single public default.
6. Summary sync revalidates the public truth packet.

### C. Correctness Findings

1. **Fixed: read-only project-summary consumers could create public menu truth.**  
   `getProjectsList()` and `getProjectsListWithoutLoader()` intentionally auto-create a default project when no project exists. That is correct for editor/onboarding flows but wrong for output/dashboard pages. Dashboard selector, Use MenuList, OBP link card, Past Activity, and GrowthOS now use `getExistingProjectsListWithoutLoader()`.

2. **Fixed: soft-delete restore could lose public summary fields.**  
   The active runtime stores name, slug, previous slugs, image, default state, and special-menu summary data in `platformSummary/projects_{sId}`. Deleting removed that summary, while restore rebuilt from the full project doc, which often lacks summary-only fields. `deleteProject()` now stores a `deletedSummary` tombstone and `restoreProject()` uses it.

3. **Fixed: restoring an old default could create duplicate defaults.**  
   If a deleted default project promoted a fallback default, restoring the deleted project could reintroduce a second default if old summary state was blindly restored. Restore now keeps the project non-default when another active non-special default already exists.

4. **Verified: public cache revalidation exists on high-volume project writes.**  
   `syncProjectToSummary()`, `removeProjectFromSummary()`, `updateProject()`, `publishProject()`, `setProjectActive()`, `deleteProject()`, `restoreProject()`, `duplicateProject()`, special-menu writes, and time-slot cascades invalidate public cache through the project/store cache path.

5. **Docs drift fixed.**  
   Project Management docs described retired `projectsMetadata` / `projectsData` collections and paginated list behavior. Use MenuList docs claimed mostly zero reads and older component structure. Both doc sets now match runtime.

6. **Fixed: project default switching is now DAL-coordinated.**
   Desktop and mobile project create/edit flows now pass previous-default or replacement-default ids through the `addProject()` / `updateProjectMetadata()` default-handoff option. The DAL folds the affected `isDefault` field changes into the same `platformSummary/projects_{sId}` summary write as the target project metadata, validates replacement defaults from the current summary map, and preserves the special-menu default guard.

### D. Firebase Cost Audit

Verified reads/writes:

- Project-management list load: 1 read from `platformSummary/projects_{sId}`.
- Auto-create default project: 1 full project write + 1 summary write; intentionally limited to editor/mobile management entry points.
- Read-only output/dashboard project list: 1 summary read, 0 writes.
- Full project load: 1 direct project doc read.
- Metadata update: 1 summary write and public cache invalidation. Default handoff for previous/replacement defaults is included in this same summary write instead of caller-level follow-up metadata writes.
- Full save: 1 project write, optional flag-gated reads/writes for MCE/MOL/master-awareness, and public cache invalidation.
- Publish: 1 project write, atomic version increment, optional flag-gated snapshot/event writes, and public cache invalidation.
- Delete: project doc write + summary write in one batch, no additional write for the restore tombstone because it is stored in the existing project delete write.
- Restore: 1 project write + 1 summary write + 2 reads.
- Use MenuList desktop load: 1 project-summary read + 1 screen-state summary read.
- Use MenuList full-project read: only on PDF/export fallback tap.

Cost fixes:

- Removed hidden writes from read-only owner/dashboard/output surfaces.
- Kept default auto-create only in menu-management entry points.
- Preserved restore correctness without adding a new collection or extra delete write.
- Moved project default switching into the shared DAL summary write path, removing separate desktop/mobile follow-up metadata writes for previous or replacement defaults.

Remaining cost risk:

- Time-slot preset cascade still scans all projects in the current store. This remains acceptable for low-frequency settings edits and typical SMB project counts, but it should not be used from high-frequency UI events.

### E. UI/UX Audit

Desktop:

- Project manager remains the intentional creation/editing surface.
- Dashboard project selector now shows `No catalogs` for an empty store instead of creating a project.
- Use MenuList now reaches its `no_menu` CTA state for empty stores.
- OBP link card falls back to `/menu` while loading or when no default project exists, without writing data.

Mobile:

- Mobile project-management still intentionally creates/loads first project inside `MobileProjectsProvider`.
- Mobile metadata, default, deactivate, duplicate, delete, and reset flows use shared DAL primitives.
- Mobile output docs now clarify that output actions should not create project truth.

Public/customer:

- Public route correctness remains summary-first and cache-invalidated.
- Delete/restore now preserves slug/name/image/default semantics for future restores.
- The restore default guard prevents public `/menu` ambiguity after restoring a previously default project.

### F. Product/Positioning Audit

The fixes reinforce MenuList's role as public-business truth infrastructure:

- Output and dashboard surfaces now read truth; they do not create truth.
- Restore keeps public identity fields stable without asking owners to reconstruct them.
- No owner-facing setting, toggle, or explanation was added.
- Project management remains simple: edit/create where expected, no hidden writes elsewhere.

### G. Documentation Alignment

Updated:

- `__docs__/projects/project-management/README.md`
- `__docs__/projects/project-management/project-management_firebase.md`
- `__docs__/projects/project-management/project-management_impl.md`
- `__docs__/projects/project-management/project-management_spec.md`
- `__docs__/use-menulist/README.md`
- `__docs__/use-menulist/use-menulist_firebase.md`
- `__docs__/use-menulist/use-menulist_impl.md`
- `__docs__/use-menulist/use-menulist_spec.md`
- `__docs__/use-menulist/use-menulist_mobile-support.md`
- `__docs__/audits/menulist-production-readiness-audit.md`

### H. Fixes Made

Changed runtime files:

- `src/components/templates/main-app/dashboard/OwnerDashboard/DashboardProjectSelector.tsx`
- `src/components/templates/main-app/today/PastActivity/index.tsx`
- `src/components/templates/main-app/businessSettings/OBPLinkCard.tsx`
- `src/components/templates/main-app/useMenuList/index.tsx`
- `src/components/templates/main-app/growthos/index.tsx`
- `src/database/projects/index.ts`
- `src/components/templates/main-app/projects/types/project.types.ts`

### I. Validation Performed

Commands run after this slice:

- `npx tsc --noEmit --incremental false --pretty false` - passed
- `npm run lint` - passed
- `git diff --check` - passed after mechanical trailing-whitespace cleanup in touched docs

Manual/code review validation:

- Verified remaining auto-create helper call sites are menu-management entry points (`Projects` page and mobile projects provider).
- Verified read-only project consumers now call `getExistingProjectsListWithoutLoader()`.
- Verified `restoreProject()` rebuilds summary from tombstone and avoids duplicate defaults.
- Verified cache invalidation still flows through `syncProjectToSummary()` on restore and metadata writes.
- Verified Use MenuList docs no longer claim zero-read page load or old split component structure.

Browser/runtime validation:

- Not run in this slice. Local browser smoke remains required before full certification.

Build validation:

- `npm run build` not run because the full production-readiness audit is not complete. Build remains a global completion gate.

---

## Completed Audit 4: AI Data Extraction, Upload Processing, Extraction Review

### A. Feature Identification

**Feature cluster:** AI data extraction + owner upload/file processing + menu-link import + public `/create-menu` draft extraction + extraction review apply/discard.

Relevant routes and files inspected:

- `src/app/api/menu-extraction/jobs/route.ts`
- `src/app/api/menu-link-imports/route.ts`
- `src/app/api/public/create-menu/route.ts`
- `src/app/api/projects/outlet-save/route.ts`
- `src/lib/firebase/menuProcessing.ts`
- `src/lib/menu-link-import/client.ts`
- `src/lib/menu-link-import/sourceAcquisition.ts`
- `src/lib/extraction/applyChanges.ts`
- `src/lib/extraction/comparisonEngine.ts`
- `src/components/templates/main-app/projects/getProcessedFile.ts`
- `src/components/templates/main-app/projects/index.tsx`
- `src/components/templates/main-app/projects/jobScreens/ExtractionJobReviewScreen.tsx`
- `src/components/mobile/sheets/MenuUploadSheet.tsx`
- `src/components/mobile/sheets/ExtractionReviewSheet.tsx`
- `functions/src/triggers/production.ts`
- `functions/src/triggers/shared.ts`
- `functions/src/dev-triggers.ts`
- `functions/src/logic/processMenuImagesJob.ts`
- `functions/src/logic/processMenuImages.ts`
- `functions/src/logic/saveFilesToProject.ts`
- `functions/src/logic/publicCacheRevalidation.ts`
- `functions/src/schedulers/menuJobCleanup.ts`
- `functions/src/schedulers/menulistMaintenanceScheduler.ts`
- `firestore.rules`
- `scripts/verification/verify-menu-extraction-pipeline.js`
- `__docs__/menu-extraction-pipeline/*`
- `__docs__/projects/ai-data-extraction/*`

Affected surfaces:

- Owner dashboard: yes, project upload/re-extraction/review.
- Mobile owner flow: yes, mobile upload and review sheets.
- Public menu/customer page: yes, accepted extraction changes mutate public menu truth.
- Official Business Page: indirectly, menu-derived business attributes can update store truth.
- Website/marketing claim: yes, public `/create-menu` draft path and docs.
- Admin/internal operations: yes, AI operations telemetry and extraction health alerts.
- Billing/entitlement: indirectly, AI cost accounting/rate limiting.
- Analytics/feedback/reviews: no direct customer feedback changes in this slice.
- Multi-location behavior: yes, linked-outlet extraction review.
- Cache/public truth: yes, project/store/screen/assistant cache invalidation after accepted changes.

Feature flags observed:

- `ENABLE_MENU_LINK_IMPORT`
- `ENABLE_MENU_LINK_IMPORT_RENDER_FALLBACK`
- `ENABLE_PUBLIC_MENU_ENTRY`
- `ENABLE_MENU_INTAKE_IDENTITY_CHECK`
- `ENABLE_MULTI_OUTLET`
- `ENABLE_MCE`
- `ENABLE_OWNER_BUSINESS_HEALTH`

Firebase collections/storage inspected:

- `menuImageProcessingJobs`
- `projects/{tId}/{sId}/{projectId}`
- `publicMenuDrafts`
- `menuLinkImportArtifacts`
- `MENULIST_AI_OPERATIONS`
- `platformSummary/projects_{sId}`
- Firebase Storage prefixes: `projects/files/{tId}/{sId}/`, `menuLinkImports/{tId}/{sId}/{projectId}/`, `publicMenuDrafts/{draftId}/`, `messagingOnboarding/{sessionId}/`

### B. End-to-End Request/Data Flow

Owner upload flow:

1. Desktop/mobile validate and upload files to Firebase Storage under the tenant/store prefix.
2. UI calls `createProcessingJob()` / `createMenuProcessingJob()`.
3. The client helper posts to `POST /api/menu-extraction/jobs`; it does not write job docs directly.
4. The API route uses `withAuth()`, SAFE_MODE, tenant/store verification, a `FILE_UPLOAD` request gate, a 128KB bounded JSON body, Zod request validation, project existence checks, source URL allowlists, MIME checks, active-job reuse, identity preflight, and `AI_EXPENSIVE` rate limiting for new extraction creation.
5. The route writes `menuImageProcessingJobs/{jobId}` with project destination metadata.
6. Production `processMenuImagesJob` Firestore trigger processes the job; development uses `dev_triggerProcessMenuImages`.
7. The worker validates tenant/project/source path again before AI, sets `processing`, runs Gemini extraction, applies hardening and deterministic category icons, and then branches.
8. First extraction saves directly to the project and revalidates public cache.
9. Re-extraction sets `preview_ready` with 24-hour TTL and waits for owner review.
10. Desktop and mobile review use the same `applyExtractionChanges()` / `discardExtractionChanges()` helpers.

Authenticated menu-link import:

1. Desktop/mobile call `createMenuLinkImportJob()`.
2. `POST /api/menu-link-imports` requires auth, SAFE_MODE, tenant/store verification, rate limit, an 8KB bounded JSON body, permission confirmation, project existence, active-job reuse, and feature flag.
3. The source acquisition helper normalizes URLs, blocks credentials/unsafe protocols/unsafe hostnames/private IPs, pins DNS lookup, caps response/rendered sizes, limits redirects, validates source confidence, and optionally uses render fallback behind flag.
4. The route stores a private artifact under `menuLinkImports/{tId}/{sId}/{projectId}/{jobId}/` and writes a forced-review extraction job.

Public `/create-menu` draft flow:

1. The route is authenticated and feature-flagged.
2. Uploads/links dedupe active drafts before expensive acquisition and dedupe by content hash after acquisition.
3. Public draft extraction jobs use platform tenant/store/user IDs, `skipProjectSave`, a public-draft destination, a 24-hour draft TTL, and public draft storage.
4. The worker writes completion/failure back to `publicMenuDrafts`.
5. Claiming a completed draft creates a normal renderer-parseable project and revalidates public cache tags.

Review apply/discard flow:

1. `applyExtractionChanges()` reads the current project and job once.
2. It now rejects missing jobs, non-`preview_ready` jobs, project mismatches, tenant/store mismatches, and user mismatches before any project mutation.
3. Single-store/master review applies mutate a cloned project `files` array and write one project update.
4. Linked-outlet review applies build an outlet-local project payload and call `POST /api/projects/outlet-save`; the server route enforces permissions, tenant membership, outlet policy, and local-only ID prefixes before Admin SDK persistence.
5. Accepted changes revalidate public client cache, digital screen content version, and owner-assistant cache through the shared project cache helper.
6. Discard uses the same job ownership/status validation before marking the review job cancelled.

### C. Correctness Findings

1. **Fixed: review apply/discard did not fail closed on job ownership/status.**  
   The helper read the job but did not reject missing jobs, wrong project, wrong tenant/store, wrong owner, or wrong status before applying changes. Firestore rules limited job updates, but project mutation could still proceed from an invalid review context. `assertOwnedPreviewJob()` now blocks these cases before project or job mutation.

2. **Fixed: linked-outlet extraction review bypassed the validated outlet save route.**  
   The review helper tried to write linked outlet `files` directly. Firestore rules intentionally block arbitrary `files` writes on linked projects, while the existing `/api/projects/outlet-save` route performs the required local-only ID and outlet-policy checks. Linked-outlet review apply now uses that route.

3. **Fixed: legacy direct AI callable remained an expensive parallel entry point.**  
   `processMenuImages` was still exported as a direct callable and could invoke Gemini with arbitrary HTTPS/data URLs for any store-scoped account. No current app caller was found; the product contract is the job queue. The callable now fails closed with `failed-precondition` and does not call AI processing.

4. **Fixed: verification drift.**  
   `verify:menu-extraction-pipeline` now asserts review ownership validation, linked-outlet `outlet-save`, disabled direct callable behavior, and absence of direct AI invocation from the legacy callable.

5. **Docs drift fixed.**  
   AI extraction docs still claimed full production-ready status and described the legacy callable as an active path. Docs now describe controlled owner testing readiness, queue-only production extraction, the linked-outlet review route, and the Firebase deploy blocker.

### D. Firebase Cost Audit

Verified reads/writes:

- Owner job creation: 1 project existence read, optional original failed-job read for retry, 1 active-job query, 1 job write, optional identity preflight reads/work.
- Client active-job check: 1 bounded query by `projectId`, `uId`, and active status.
- Job listener: one direct `menuImageProcessingJobs/{jobId}` listener while the extraction UI is active.
- Worker start: 1 job transaction and 1 project read.
- First extraction save: 1 project write, job status writes, 1 `MENULIST_AI_OPERATIONS` telemetry write, and public cache revalidation.
- Re-extraction: no project write until owner approval; writes `preview_ready` result to the job document.
- Review apply single-store/master: 1 project read + 1 job read + 1 project write + 1 job terminal update; optional 1 store read/write only when menu-derived business attributes change.
- Review apply linked outlet: 1 project read + 1 job read client-side, then `/api/projects/outlet-save` performs bounded store/tenant/project reads and 1 Admin SDK project write.
- Review discard: 1 job read + 1 terminal job update.
- Cleanup scheduler: consolidated in `menulistMaintenanceScheduler`; no new scheduled function added.

Cost fixes:

- Removed an exposed direct Gemini callable path from code.
- Preserved central job queue rate limiting and source validation.
- Added bounded JSON request parsing before owner extraction project reads, link source acquisition, and draft claim reads.
- Avoided weakening Firestore rules for linked project `files`; reused the existing validated server route.
- Kept cleanup under the consolidated maintenance scheduler.

Remaining cost/deploy risk:

- The direct callable hardening is code-resolved but remains pending live effect until the current Functions deploy set can be deployed through External Certification Gate 1. The historical June 11, 2026 attempt targeted retired `ecomsai` and failed on Secret Manager billing; current retry evidence must target `menulist-qa`, where the latest scoped Functions deploy blocker is Cloud Resource Manager HTTP 403 caller permission before upload. Until the scoped deploy succeeds, production may still have the older callable behavior.
- `MENULIST_AI_OPERATIONS` and nested `menulistAiOperations` rows now use compact-not-delete retention: accounting/audit rows remain for platform cost review and owner transaction history, while raw/generated response details are compacted by default or pruned by `ai_operation_detail_cleanup` when detailed-mode rows expire. The cleanup still requires the updated maintenance scheduler deploy for live effect.

### E. UI/UX Audit

Desktop:

- Desktop upload and review remain in the Projects surface.
- Existing active jobs are reused instead of creating duplicate work.
- Review save/discard now fails with a clear unavailable/not-owned error instead of silently applying from an invalid job context.

Mobile:

- Mobile upload and review sheets share the same job creation and review helpers as desktop.
- Mobile upload supports file upload and menu-link import behind the same feature flag.
- No mobile-only project mutation path was introduced.

Public/customer:

- Public `/create-menu` creates temporary drafts only; no imported data becomes public menu truth until an authenticated owner claims and approves it.
- Accepted owner extraction changes still invalidate public menu/OBP cache paths before customer refreshes.

### F. Product/Positioning Audit

The fixes reinforce MenuList as public-business truth infrastructure:

- There is one production extraction authority: the job queue.
- Linked outlet local truth is validated against outlet policy instead of bypassing governance.
- Public draft extraction stays temporary and owner-reviewed.
- No new owner settings or AI dashboard clutter were added.

### G. Documentation Alignment

Updated:

- `__docs__/menu-extraction-pipeline/menu-extraction-pipeline_impl.md`
- `__docs__/menu-extraction-pipeline/menu-extraction-pipeline_firebase.md`
- `__docs__/projects/ai-data-extraction/README.md`
- `__docs__/projects/ai-data-extraction/ai-data-extraction_firebase.md`
- `__docs__/projects/ai-data-extraction/ai-data-extraction_impl.md`
- `__docs__/projects/ai-data-extraction/ai-data-extraction_spec.md`
- `__docs__/audits/menulist-production-readiness-audit.md`

### H. Fixes Made

Changed runtime/verification files:

- `src/lib/extraction/applyChanges.ts`
- `functions/src/triggers/shared.ts`
- `scripts/verification/verify-menu-extraction-pipeline.js`

### I. Validation Performed

Commands run after this slice:

- `npx tsc --noEmit --incremental false --pretty false` - passed
- `npm --prefix functions run build` - passed
- `npm run verify:menu-extraction-pipeline` - passed after verifier correction
- `firebase deploy --only functions:processMenuImages --project ecomsai` - failed before deployment because Secret Manager validation returned HTTP 403 requiring billing on project `ecomsai`

Manual/code review validation:

- Verified app code has no current caller of the legacy direct `processMenuImages` callable.
- Verified owner job API rejects client-owned source metadata and validates Storage prefixes.
- Verified menu-link import acquisition blocks unsafe protocols, credentials, local/private/internal hostnames, unsafe DNS results, oversized responses, and excessive redirects.
- Verified public `/create-menu` queues jobs and does not run inline extraction.
- Verified Firestore rules keep browser job creation blocked and restrict review resolution job updates to `preview_ready` terminal status changes.
- Verified linked-outlet project file persistence is already handled by `/api/projects/outlet-save`, which validates local-only IDs and outlet policy.
- Verified cleanup and health monitoring run inside `menulistMaintenanceScheduler`, not a new standalone scheduler.

Browser/runtime validation:

- Not run in this slice. Desktop/mobile extraction review browser smoke remains required after Firebase deploy succeeds.

Build validation:

- `npm run build` not run because the full production-readiness audit is not complete. Build remains a global completion gate.

---

## Completed Audit 5: Data Editor, B2C Design Publish, Description Generation, Multi-Language Translation

### A. Feature Identification

**Feature cluster:** Owner data editor + desktop/mobile design publish + description generation + translation generation + linked-outlet editor governance.

Relevant routes and files inspected:

- `src/database/projects/index.ts`
- `src/app/api/projects/outlet-save/route.ts`
- `src/app/api/descriptions/route.ts`
- `src/app/api/translations/route.ts`
- `src/lib/multiOutlet/serverOutletPolicy.ts`
- `src/components/templates/main-app/projects/editorView/Editor.tsx`
- `src/components/templates/main-app/projects/editorView/editItemModal.tsx`
- `src/components/templates/main-app/projects/editorView/LanguageSelectorModal.tsx`
- `src/components/templates/main-app/projects/utils/translationsUtils.ts`
- `src/components/templates/main-app/projects/generateTranslations.ts`
- `src/components/templates/main-app/projects/b2cView/index.tsx`
- `src/components/templates/main-app/projects/b2bView.tsx`
- `src/components/mobile/screens/MobileMenuScreen.tsx`
- `src/components/mobile/screens/MobileDesignEditorScreen.tsx`
- `__docs__/projects/data-editor/*`
- `__docs__/projects/b2c-view/*`
- `__docs__/projects/b2b-view/*`
- `__docs__/projects/description-generation/*`
- `__docs__/projects/multi-language-translation/*`
- `__docs__/multi-outlet-consistency/*`

Affected surfaces:

- Owner dashboard: yes, editor and design publish.
- Mobile owner flow: yes, mobile menu editor and mobile design editor.
- Public menu/customer page: yes, accepted edits and design publishes mutate customer-visible truth.
- Official Business Page: yes, B2C view can edit public presence via store updates.
- Website/marketing claim: indirectly, feature docs and public claims about translation/design.
- Admin/internal operations: yes, AI accounting and linked-outlet policy enforcement.
- Billing/entitlement: yes, AI capacity checks for descriptions/translations.
- Analytics/feedback/reviews: not directly in this slice.
- Multi-location behavior: yes, linked outlet editor/design/translation policy.
- Cache/public truth: yes, `updateProject()`, `publishProject()`, `updateStore()`, and `outlet-save` invalidation paths.

Feature flags observed:

- `ENABLE_MULTI_OUTLET`
- `ENABLE_MCE`
- `ENABLE_MENU_OBSERVATION`
- `ENABLE_MASTER_UPDATE_AWARENESS`
- `ENABLE_BEHAVIOR_NUDGES`
- `ENABLE_OWNER_BUSINESS_HEALTH`

Firebase collections/storage inspected:

- `projects/{tId}/{sId}/{projectId}`
- `stores/{storeId}`
- `platformSummary/projects_{sId}`
- `masterOperationalState/{projectId}`
- AI operation/accounting collections through `finalizeAiOperationAccounting()`
- Storage assets uploaded through project asset helpers

### B. End-to-End Request/Data Flow

Editor save flow:

1. Desktop editor mutates local project state.
2. `Editor.syncChanges()` strips resolved linked-outlet display data before persistence.
3. Saves call `updateProject()`.
4. For normal/master projects, `updateProject()` normalizes localized project text, runs optional MCE/MOL/master-awareness checks, writes `projects/{tId}/{sId}/{projectId}`, and revalidates public cache.
5. For linked outlet projects, `updateProject()` posts to `/api/projects/outlet-save`, which validates tenant/store access, permissions, local-only IDs, outlet policy, and writes via Admin SDK.
6. Mobile menu editing uses `updateProjectWithoutLoader()`, which shares the same underlying `runUpdateProject()` behavior.

B2C/design publish flow:

1. Desktop B2C preview and mobile design editor stage design changes locally.
2. Publish calls `publishProject()`.
3. Background images are uploaded before persistence.
4. For normal/master projects, `publishProject()` validates linked master references when present, writes publish metadata, increments menu version, and revalidates public cache.
5. For linked outlet projects, `publishProject()` now posts to `/api/projects/outlet-save` with `publish: true`, so theme/brand/layout policy is enforced before publish metadata and public cache invalidation.
6. B2C official page changes call `updateStore()`, preserving store cache revalidation.

Description generation flow:

1. Desktop/mobile item description actions call `POST /api/descriptions`.
2. The route uses `withAuth()`, SAFE_MODE, AI operation rate limit, bounded JSON parsing, Zod validation, tenant/store check, linked-outlet description policy, AI capacity, Gemini generation, response ID validation, AI accounting, and structured logging.
3. The client merges accepted description text into project state and saves through `updateProject()`.

Translation flow:

1. Desktop/mobile language and item/category translation helpers call `POST /api/translations`.
2. The route uses `withAuth()`, SAFE_MODE, AI operation rate limit, bounded JSON parsing, Zod validation, AI capacity, Gemini generation, response-shape normalization, coverage logging, and AI accounting.
3. Project-scoped translation requests now validate tenant/store access, project existence, and linked-outlet translation governance before Gemini.
4. The client merges accepted translations into project state and saves through `updateProject()`.

### C. Correctness Findings

1. **Fixed: linked-outlet design publish bypassed outlet policy.**  
   `updateProject()` correctly routed linked outlet saves through `/api/projects/outlet-save`, but `publishProject()` wrote directly to Firestore after only checking that the master project existed. A linked outlet could bypass disabled theme, brand, or layout overrides from desktop B2C or mobile design publish. `publishProject()` now sends linked outlet publishes through `/api/projects/outlet-save` with `publish: true`.

2. **Fixed: outlet-save route did not support publish metadata.**  
   `/api/projects/outlet-save` only represented normal saves. It now accepts an optional `publish` flag and stamps `lastPublishedAt` and `menuVersion` in the same validated Admin SDK write.

3. **Fixed: translation API lacked project/linked-outlet server governance.**  
   The client translation utilities filter inherited linked-outlet content in the main translation flow, but the API itself could still translate arbitrary inherited IDs for a project-scoped request before Gemini. `/api/translations` now validates tenant/store/project context and rejects inherited linked-outlet item/category translation keys before AI capacity/provider work.

4. **Verified: description generation already has stronger project/policy checks.**  
   `/api/descriptions` already validates request shape, tenant context, linked-outlet description policy, AI capacity, and response IDs before returning text for client persistence.

5. **Fixed: high-cost text AI routes parsed unbounded JSON bodies.**
   Descriptions, translations, business copy, SEO copy, campaign captions, new-item metadata, and review reply suggestions now use explicit bounded JSON request caps after auth/rate limiting and before Zod validation, AI capacity, provider calls, or accounting.

6. **Verified: desktop/mobile editor mutation paths share cache-invalidating DALs.**
   Direct editor writes found in desktop and mobile route through `updateProject()` / `updateProjectWithoutLoader()` or `publishProject()`, not direct component Firestore writes.

### D. Firebase Cost Audit

Verified reads/writes:

- Normal editor save: optional 1 project read for MCE/MOL/master-awareness, 1 project write, optional signal/event writes, public cache invalidation.
- Linked outlet editor save: route reads caller/outlet/master stores, tenant, project, then writes 1 outlet project doc via Admin SDK.
- Normal publish: 1 project write with version increment, optional publish event/snapshot writes, public cache invalidation.
- Linked outlet publish: same bounded `/api/projects/outlet-save` reads as linked save, 1 outlet project write with publish metadata, public cache invalidation.
- Description API: 0 Firestore reads for single-store/master policy beyond AI capacity/accounting; linked outlet requests read project + master store before provider work.
- Translation API: project-scoped requests now add 1 project read, and linked outlet projects add a master-store policy read, before provider work; non-project translation requests remain 0 Firestore reads before provider/accounting.

Cost fixes:

- Added a small pre-provider read cost to project-scoped translation to prevent wrong/unauthorized linked-outlet translation work and wasted Gemini calls.
- Reused the existing linked-outlet save route instead of adding Firestore rule exceptions or new collections.
- Kept publish metadata on the existing outlet project write.

Remaining cost risk:

- Description/translation APIs still write AI operation/accounting telemetry per call. Those app-route rows store compact `clientResponse` summaries in accounting-only mode, owner transaction summaries read from the compact fields, and detailed provider fields are covered by `ai_operation_detail_cleanup` when detailed-mode rows expire.
- Translation has no translation memory/cache; this is documented as a non-goal/current tradeoff.

### E. UI/UX Audit

Desktop:

- Editor save behavior remains unchanged for owners.
- Linked-outlet policy failures now surface from the same `outlet-save` error path as normal editor saves.
- B2C publish still shows the existing publish success flow after valid writes.

Mobile:

- Mobile menu editor uses the same shared project DAL.
- Mobile design publish now inherits linked-outlet publish policy through `publishProject()`.
- Mobile category translation calls the same `/api/translations` route and now gets server-side linked-outlet protection.

Public/customer:

- Manual edits, design publishes, and accepted AI text changes still invalidate public menu/OBP cache paths.
- Linked outlets cannot publish local visual changes that the master policy disables.

### F. Product/Positioning Audit

The fixes preserve public-business truth authority:

- Master/outlet authority is enforced server-side for edits, translation, and design publish.
- Owner-facing workflow stays simple; no new setting or decision was added.
- Translation remains an owner aid, not an autonomous public publisher.
- Design changes are still public truth only after explicit owner publish/save paths.

### G. Documentation Alignment

Updated:

- `__docs__/projects/multi-language-translation/multi-language-translation_firebase.md`
- `__docs__/projects/multi-language-translation/multi-language-translation_impl.md`
- `__docs__/projects/b2c-view/b2c-view_firebase.md`
- `__docs__/multi-outlet-consistency/multi-outlet-consistency_firebase.md`
- `__docs__/audits/menulist-production-readiness-audit.md`

### H. Fixes Made

Changed runtime files:

- `src/app/api/translations/route.ts`
- `src/lib/multiOutlet/serverOutletPolicy.ts`
- `src/app/api/projects/outlet-save/route.ts`
- `src/database/projects/index.ts`

### I. Validation Performed

Commands run after this slice:

- `npx tsc --noEmit --incremental false --pretty false` - passed after runtime changes
- `npx tsc --noEmit --incremental false --pretty false` - passed after docs/report update
- `npm run lint` - passed
- `git diff --check` - passed

Manual/code review validation:

- Verified translation route now blocks linked-outlet inherited item/category keys before Gemini.
- Verified `getLinkedOutletPolicyBlockReason()` still preserves description/image policy behavior and adds translation without changing client UI.
- Verified `publishProject()` retains master existence validation before linked outlet publish handoff.
- Verified desktop B2C and mobile design both call `publishProject()`.
- Verified desktop/mobile editor saves use `updateProject()` / `updateProjectWithoutLoader()` shared DAL.

Browser/runtime validation:

- Not run in this slice. Editor and mobile design browser smoke remain required before full certification.

Build validation:

- `npm run build` not run because the full production-readiness audit is not complete. Build remains a global completion gate.

---

## Completed Audit 6: AI Image Generation and Media/Image Systems

### A. Feature Identification

**Feature cluster:** single menu image generation, image editing, batch image generation, image upload/storage, linked-outlet image policy, and generated-image owner acceptance.

Relevant routes and files inspected:

- `src/app/api/image-generation/route.ts`
- `src/app/api/image-generation/batch-trigger/route.ts`
- `src/app/api/image-generation/batch-generation/route.ts`
- `src/app/api/image-generation/generators.ts`
- `src/app/api/image-editing/route.ts`
- `src/lib/apiUtils/index.ts`
- `src/lib/google/cloudTask/index.ts`
- `src/lib/validation/apiSchemas.ts`
- `src/lib/multiOutlet/serverOutletPolicy.ts`
- `src/lib/media/*`
- `src/database/imageBatchProcessing/index.tsx`
- `src/database/imageBatchProcessing/server.ts`
- `src/database/storage/uploadBase64MediaImageAdmin.ts`
- `src/lib/media/prepareMediaImageAdmin.ts`
- `src/database/projects/index.ts`
- `src/services/ai/image/*`
- `src/hooks/useImageBatchJobListener.ts`
- `src/components/templates/main-app/projects/editorView/ImageUploadModal.tsx`
- `src/components/templates/main-app/projects/editorView/AiImageGenerator/*`
- `src/components/mobile/screens/MobileMenuScreen.tsx`
- `storage.rules`
- `firestore.rules`
- `__docs__/projects/ai-image-generation/*`

Affected surfaces:

- Owner dashboard: yes, editor image modal and batch generation.
- Mobile owner flow: yes, mobile menu image upload uses shared project/media upload primitives; AI generation modal remains desktop-oriented.
- Public menu/customer page: yes, accepted item images become customer-visible public truth.
- Official Business Page: indirectly through shared media profile and public image safety.
- Website/marketing claim: docs only; no public website copy changed in this slice.
- Admin/internal operations: yes, Cloud Tasks worker and AI operation/accounting records.
- Billing/entitlement: yes, AI capacity checks and operation accounting.
- Analytics/feedback/reviews: not directly.
- Multi-location behavior: yes, linked outlet image override policy.
- Cache/public truth: yes, accepted image writes go through project update paths that invalidate public menu/OBP cache.

### B. End-to-End Request/Data Flow

Single image generation:

1. Owner opens `ImageUploadModal`, selects an item, optionally adds prompt/reference image, and calls `generateImageViaApi()`.
2. Client now sends item id with item details so linked-outlet policy can identify inherited items.
3. `POST /api/image-generation` uses `withAuth()`, Safe Mode, expensive-AI rate limit, bounded JSON parsing, Zod payload validation, linked-outlet image policy, AI capacity, Gemini generation, summarized operation accounting, and safe response logging.
4. The route returns base64 image previews only; project data is not mutated until owner accepts/upload saves.
5. Accepted images upload through shared media/project helpers and then save through `updateProject()` with public cache invalidation.

Image editing:

1. Owner opens `EditImageModal` from an existing image.
2. Client sends only real prompt images, not empty placeholder image objects.
3. `POST /api/image-editing` applies expensive-AI rate limiting and bounded JSON parsing before validating source/reference images, linked-outlet policy, AI capacity, and provider output.
4. Provider failures and no-image responses now return errors instead of becoming false 200 OK responses.
5. Accepted edited images flow through the same owner acceptance/upload/save path as generated images.

Batch image generation:

1. Owner creates a visible batch job document, then calls `/api/image-generation/batch-trigger`.
2. Batch trigger applies the batch-operation rate limit and bounded JSON parsing before validating payload, linked-outlet image policy, total batch AI capacity, registering requested item ids on the job, and enqueuing one Cloud Task per item.
3. Cloud Tasks calls `/api/image-generation/batch-generation` with project header and shared worker secret.
4. Worker validates the secret/header, applies a bounded JSON body cap, then validates Zod payload, job/project match, requested item id, terminal status, duplicate item state, AI capacity, and provider output.
5. Worker prepares generated image bytes through the `menuItem` media profile, uploads the prepared WebP buffer through Admin SDK to public `media/menuItem/{tId}/{sId}/...` Storage paths, records summarized AI accounting, and updates the batch job.
6. The owner reviews batch results and either uploads selected images to the project or discards/cancels, using existing project/cache paths.

### C. Correctness Findings

1. **Fixed: image-editing schema did not validate the actual runtime payload.**  
   The client sent `referanceImage`, `feature`, and `promptImages`, but the server schema accepted only a generic prompt/mode shape. The schema now validates the real payload and bounds image URLs/MIME types.

2. **Fixed: image editing could return 200 after provider failure.**  
   `editImageViaFlash()` returned a `NextResponse` object that the route treated as a successful provider result. Provider errors and empty image results now return failure responses without accounting as success.

3. **Fixed: single image generation did not send item id.**  
   Linked-outlet image policy could not block inherited item image generation from the single-image client path. The client now includes `itemDetails.id`.

4. **Fixed: batch worker was protected only by a spoofable project header.**  
   Task enqueue now sends `x-menulist-task-secret`; worker verifies the shared secret and project id before parsing/processing the request.

5. **Fixed: batch worker used client Firestore/Storage helpers without an owner session.**  
   Worker job reads/writes now use Admin SDK helpers, and generated images upload through an explicit Admin SDK media upload path scoped by tenant/store.

6. **Fixed: batch trigger could show false success after enqueue failure.**  
   The client wrapper now throws on trigger failure, and the modal marks the pre-created job as failed when startup fails.

7. **Fixed: raw provider/image payloads could be logged or stored in operation records.**  
   Provider responses and reference images are now summarized before logs/accounting.

8. **Fixed: high-cost image AI routes parsed unbounded JSON bodies.**
   Single image generation and batch trigger/worker routes now reject bodies above 16MB before payload validation or provider work. Image editing uses a 64MB cap to preserve the existing multi-reference-image schema while preventing unbounded request parsing.

9. **Fixed: batch generated images could persist raw provider-sized bytes.**
   Browser accepted generated images already use the shared media profile upload path. Batch worker uploads now call `prepareMediaImageAdmin()` before Storage save, preserving the `menuItem` profile contract for generated images and recording original/prepared size metadata.

### D. Firebase Cost Audit

Verified reads/writes:

- Single image generation: project/outlet policy read, optional master-store read, subscription/capacity read, provider call, AI accounting write on success; no project write until owner accepts.
- Image editing: same policy/capacity/accounting pattern as single generation; no project write until owner accepts.
- Batch trigger: project/outlet policy read, optional master-store read, subscription/capacity read for total item count, one job update for requested item ids, one Cloud Task per item.
- Batch worker: one job read, one subscription/capacity read, one Admin Storage upload on successful provider output, AI accounting/subscription writes, and one batch job update per item.
- Batch listener: one scoped query with `projectId`, status `in`, and `limit(1)`; listener remains justified only while owner has an active batch job.

Cost fixes:

- Added total batch capacity preflight before task fanout.
- Added worker idempotency guard for duplicate item tasks.
- Removed raw image/provider responses from Firestore operation logs.
- Switched worker uploads to a single explicit media upload per generated image instead of failing/retrying through unauthenticated client Storage.

Remaining cost risk:

- Batch jobs still write one progress update per item. This is acceptable for owner-visible progress but should be monitored before allowing very large batch sizes.
- Batch image generation now uses a server-only prompt cache for one-prompt, one-image, non-reference generations, with store-owned media copies and bounded source retention. Single image generation/editing remains uncached because those routes return review previews and do not write Storage until owner acceptance.

### E. UI/UX Audit

Desktop:

- Image generation still uses the existing editor modal and owner acceptance model.
- Batch startup failures now surface as failed jobs instead of false success.
- Edit failures now surface as failures instead of silently returning empty results after a false success path.

Mobile:

- Mobile owner image upload uses the shared project/media upload path.
- Mobile does not expose the full AI generation modal as a separate mobile-native flow in this slice; this remains a parity gap for future product decision, not a regression.

Public/customer:

- Public images are only mutated after owner acceptance and project save.
- Worker-generated batch images are stored under public media profile paths so accepted item images can render on customer menus.

### F. Product/Positioning Audit

The hardening preserves MenuList's public-truth model:

- Generated images remain reviewed owner inputs, not autonomous public changes.
- Linked outlets cannot generate local inherited-item imagery when master policy blocks image overrides.
- No new owner settings or image-management dashboard were added.
- The feature stays bounded to menu truth preparation rather than expanding into a generic image tool.

### G. Documentation Alignment

Updated:

- `__docs__/projects/ai-image-generation/ai-image-generation_firebase.md`
- `__docs__/projects/ai-image-generation/ai-image-generation_impl.md`
- `__docs__/audits/menulist-production-readiness-audit.md`

### H. Fixes Made

Changed runtime files:

- `src/app/api/image-generation/route.ts`
- `src/app/api/image-generation/batch-trigger/route.ts`
- `src/app/api/image-generation/batch-generation/route.ts`
- `src/app/api/image-generation/generators.ts`
- `src/app/api/image-editing/route.ts`
- `src/lib/apiUtils/index.ts`
- `src/lib/google/cloudTask/index.ts`
- `src/lib/validation/apiSchemas.ts`
- `src/lib/env/validateEnv.ts`
- `src/lib/ai/imageOperationLogging.ts`
- `src/database/imageBatchProcessing/server.ts`
- `src/database/storage/uploadBase64MediaImageAdmin.ts`
- `src/services/ai/image/generateImageViaApi.ts`
- `src/services/ai/image/triggerBatchImageGenerationApi.ts`
- `src/components/templates/main-app/projects/editorView/ImageUploadModal.tsx`
- `src/components/templates/main-app/projects/editorView/AiImageGenerator/EditImageModal.tsx`
- `src/components/templates/main-app/projects/types/batchJob.types.ts`

### I. Validation Performed

Commands run after runtime changes:

- `npx tsc --noEmit --incremental false --pretty false` - passed
- `npx tsc --noEmit --incremental false --pretty false` - passed after docs/report update
- `npm run lint` - passed
- `git diff --check` - passed

Manual/code review validation:

- Verified server schemas now match generation/editing payloads.
- Verified worker uses Admin SDK job reads/writes and Admin SDK Storage upload.
- Verified task enqueue sends the shared worker secret and no longer logs raw task payload.
- Verified provider responses/reference images are summarized before logs/accounting.
- Verified linked-outlet image policy receives item ids from single, edit, and batch paths.
- Verified accepted images still depend on owner save/update paths for public cache invalidation.

Browser/runtime validation:

- Not run in this slice. Desktop image modal and batch job browser smoke remain required before full certification.

Build validation:

- `npm run build` not run because the full production-readiness audit is not complete. Build remains a global completion gate.

---

## Completed Audit 7: Digital Screens and Display Media

### A. Feature Identification

**Feature cluster:** Digital Screens public display, screen token routing, owner screen setup, display media upload, public live refresh, and daily seen signal.

Relevant routes and files inspected:

- `src/app/screen/[token]/page.tsx`
- `src/app/screen/[token]/ScreenDisplay.tsx`
- `src/app/screen/[token]/MenuBoardDisplay.tsx`
- `src/app/screen/[token]/ScreenAttribution.tsx`
- `src/app/api/screen/seen/route.ts`
- `src/database/campaigns/index.ts`
- `src/database/campaigns/serverScreen.ts`
- `src/lib/screen/screenContent.ts`
- `src/lib/screen/screenInvalidation.ts`
- `src/lib/screen/publicScreenState.ts`
- `src/lib/screen/utils.ts`
- `src/components/mobile/screens/MobileDigitalScreensScreen.tsx`
- `firestore.rules`
- `storage.rules`
- `__docs__/digital-screens/*`

Affected surfaces:

- Owner dashboard: yes, Digital Screen settings and owner uploads.
- Mobile owner flow: yes, shared mobile screen management surface.
- Public menu/customer page: indirectly, through menu edits triggering screen refresh.
- Official Business Page: no direct changes.
- Website/marketing claim: docs only; no website copy changed.
- Admin/internal operations: yes, Firestore rules and daily seen signal.
- Billing/entitlement: screen SSR blocks inactive/blocked stores.
- Analytics/feedback/reviews: daily seen operational signal only.
- Multi-location behavior: store-scoped screen token and store id.
- Cache/public truth: yes, public menu cache invalidation touches screen version and screen SSR cache tag.

### B. End-to-End Request/Data Flow

Public screen flow:

1. TV opens `/screen/[token]` or `/screen/[token]?mode=highlights`.
2. Server component resolves token through Admin SDK in `getScreenDataByTokenServer()`.
3. Resolver reads the matching `platformSummary/campaigns_{storeId}` summary, store doc, and automatic menu context.
4. Inactive or blocked stores return not found.
5. Menu Board uses generated `screen.menuProjection` when current; otherwise it falls back to project reads.
6. Highlights uses owner uploads only when override is enabled; otherwise it generates factual slides from menu/campaign data.
7. Client caches data in localStorage and sends at most one daily `/api/screen/seen` signal.
8. Client now listens to `platformSummary/screen_{storeId}` for content-version changes.
9. Owner menu/screen changes update canonical `campaigns_{storeId}.screen`, then sync the public-safe listener mirror.
10. Public display reloads with jitter when the mirror content version increases.

Owner flow:

1. Owner opens screen settings on desktop or mobile.
2. `getScreenState()` reads `platformSummary/campaigns_{sId}`.
3. First setup calls `initializeScreenState()`, generates a high-entropy token, writes canonical state, and writes the safe mirror.
4. Owner upload stores prepared media, adds a pinned slide, bumps content version, and syncs the safe mirror.
5. Owner delete/caption/override actions update canonical screen state, bump version when public output changes, and sync the safe mirror.

### C. Correctness Findings

1. **Fixed: public screen clients could read internal owner summary data.**  
   Firestore rules allowed unauthenticated reads of enabled `platformSummary/campaigns_{storeId}` documents. Those docs contain more than screen version data, including Today/campaign summaries, staff prompt, and physical-surface state. Public clients now listen to `platformSummary/screen_{storeId}`, a mirror with only `storeId`, `screenToken`, `enabled`, `contentVersion`, `lastContentChangeAt`, and `updatedAt`.

2. **Fixed: Firestore public rule was overbroad.**  
   Removed unauthenticated `campaigns_*` summary reads and replaced them with a strict `screen_*` state rule that enforces the exact safe key set, token format, matching store id, enabled state, timestamps, and positive integer content version.

3. **Fixed: override setting did not refresh live screens.**  
   `updateScreenSettings()` previously wrote only the override flag. It now reads existing screen state, bumps `contentVersion`, updates `lastContentChangeAt`, and syncs the safe mirror.

4. **Fixed: screen mutation paths could create partial screen state.**  
   Pinned slide add/remove/caption paths now require an initialized screen token before writing. They write full next screen state and mirror the public version.

5. **Fixed: seen endpoint accepted weak store path input and logged token fragments.**  
   `/api/screen/seen` now validates token and store id formats before direct doc lookup, rejects non-string/non-number store ids, and uses sanitized structured logging.

6. **Fixed: seen endpoint parsed anonymous JSON before the cheap-fail guard.**
   `/api/screen/seen` now rejects oversized requests by `content-length`, applies the shared `SCREEN_SEEN_SIGNAL` IP rate limit, and uses a 1KB bounded JSON reader before token/store validation. Token-level throttling still runs before Firestore lookup/write work.

### D. Firebase Cost Audit

Verified steady-state reads:

- Screen SSR cold path: token query + store doc + optional project summary/project fallback; cached by `unstable_cache` for 60 seconds.
- Connected public display: one `onSnapshot` read on `platformSummary/screen_{storeId}` at connect, then one read per content-version update.
- Daily seen signal: cheap-fail `content-length` and IP-rate checks first, then one direct Admin SDK doc read by `campaigns_{storeId}` when `storeId` is provided and one write only when the screen was not already seen today.
- Owner settings: one canonical `campaigns_{sId}` read.

Cost changes from fixes:

- Public listener read cost is unchanged: still one exact document listener, now on the small safe mirror.
- Owner screen setup/content mutations now add one tiny `platformSummary/screen_{sId}` write after the canonical screen write.
- Public menu cache invalidation for initialized screens now writes both canonical screen version and safe mirror version.

Cost verdict:

- The extra mirror write is justified because it removes unauthenticated public reads of the larger internal campaign summary document.
- No new collection, Cloud Function, scheduler, Storage path, or index was added.
- Daily seen remains one write per day per active screen, not a heartbeat.

### E. UI/UX Audit

Desktop:

- Existing screen setup cards, QR links, last-seen status, owner upload list, caption edit, delete, and override toggle remain intact.
- Override toggle now actually refreshes connected screens.

Mobile:

- Mobile screen management uses the same shared DAL functions, so mirror sync and version bumps apply to both desktop and mobile owner actions.
- No new mobile-only route or bypass was added.

Public/customer display:

- Menu Board and Highlights keep the same visual behavior and refresh pattern.
- Public clients continue to show cached/offline content if the listener fails.
- Public screen runtime receives less internal data after the fix.

### F. Product/Positioning Audit

The fix strengthens MenuList's public-business truth model:

- Public screens still follow menu truth automatically.
- Owners do not get new screen-management decisions.
- The runtime exposes only the public reload packet needed for correctness.
- Screen display remains a physical public truth surface, not a generic signage builder.

### G. Documentation Alignment

Updated:

- `__docs__/digital-screens/README.md`
- `__docs__/digital-screens/digital-screens_impl.md`
- `__docs__/digital-screens/digital-screens_firebase.md`
- `__docs__/audits/menulist-production-readiness-audit.md`

### H. Fixes Made

Changed runtime/infrastructure files:

- `src/lib/screen/publicScreenState.ts`
- `src/app/screen/[token]/ScreenDisplay.tsx`
- `src/app/screen/[token]/MenuBoardDisplay.tsx`
- `src/database/campaigns/index.ts`
- `src/lib/screen/screenInvalidation.ts`
- `src/app/api/screen/seen/route.ts`
- `firestore.rules`

### I. Validation Performed

Commands run after runtime changes:

- `npx tsc --noEmit --incremental false --pretty false` - failed at the time on unrelated CampaignCue TypeScript errors; later re-run during Audit 8 passed after the worktree changed.
- `npm run lint` - passed
- `git diff --check` - passed
- `firebase emulators:exec --only firestore --project ecomsai "echo firestore-rules-loaded"` - passed
- `firebase deploy --only firestore:rules --project ecomsai` - passed and released `firestore.rules`

Manual/code review validation:

- Verified public display listeners now use `platformSummary/screen_{storeId}`.
- Verified unauthenticated Firestore reads no longer allow `campaigns_*` summary docs.
- Verified owner setup/upload/delete/caption/override/version-bump paths sync the safe mirror.
- Verified public menu cache invalidation syncs the safe mirror after canonical screen version changes.
- Verified daily seen direct lookup still verifies token against canonical screen state before writing.

Browser/runtime validation:

- Not run in this slice. TV display browser smoke remains required before full certification.

Build validation:

- `npm run build` not run because the full production-readiness audit is not complete. Later global `npx tsc --noEmit --incremental false --pretty false` runs passed after the unrelated worktree drift was corrected.

---

## Completed Audit 8: Decision Intelligence, CMI, MCE, and Output Control

**Feature cluster:** Decision Blocks customer recommendations, Continuous Menu Intelligence observation state, Menu Correctness Engine save-time validation, and the dormant output-control rendering layer.

### A. Feature Identification

Routes and surfaces inspected:

- Public menu: `src/app/client/[[...slug]]/page.tsx`, `src/components/templates/main-app/projects/b2cView/menuPage/menuPageNew.tsx`, `src/components/templates/main-app/projects/b2cView/output/DecisionBlocks.tsx`
- Owner desktop: `src/components/templates/main-app/projects/editorView/DecisionBlocksSettingsModal.tsx`, `src/components/templates/main-app/projects/editorView/decisionBlocks.shared.ts`, `src/components/templates/main-app/projects/editorView/Editor.tsx`
- Owner mobile: `src/components/mobile/sheets/SmartRecommendationsSheet.tsx`, `src/components/mobile/screens/MobileHoursScreen.tsx`
- Backend/Functions: `functions/src/decisionBlocksScoring.ts`, `functions/src/intelligence/menuIntelligence.ts`, `functions/src/intelligence/shared/analyticsAggregator.ts`, `functions/src/intelligence/shared/itemExtractor.ts`, `functions/src/intelligence/shared/scoreNormalizer.ts`
- DAL/runtime libraries: `src/lib/intelligence/dal.ts`, `src/lib/mce/index.ts`, `src/lib/mce/utils.ts`, `src/lib/outputControl/index.ts`, `src/lib/outputControl/hoursConfidence.ts`
- Public output-control consumers: `src/app/client/obp/OBPResolvedSurface.tsx`, `src/app/client/obp/BrandOBPContent.tsx`, `src/components/atoms/TrustSignals.tsx`
- Write path: `src/database/projects/index.ts`
- Configuration: `src/config/features.ts`, `src/config/decisionBlocks.ts`
- Rules/docs: `firestore.rules`, `__docs__/decision-intelligence/*`, `__docs__/continuous-menu-intelligence/*`, `__docs__/menu-correctness-engine/*`

Affected areas:

- Owner dashboard: yes, recommendation controls and MCE publish gate.
- Mobile owner flow: yes, Smart Recommendations and hours confidence nudge path.
- Public menu/customer page: yes, Decision Blocks.
- Official Business Page: yes, output control is wired but flag-disabled.
- Website/marketing claim: docs only in this slice; public website copy not changed.
- Admin/internal operations: yes, scheduler monitor/manual scoring callable and run logs.
- Billing/entitlement: no direct entitlement change in this slice.
- Analytics/feedback/reviews: analytics snapshot consumption and Decision Block events inspected; broader analytics audit still queued.
- Multi-location behavior: store-local timezone behavior corrected; broader outlet intelligence behavior still queued.
- Cache/public truth: public menu reads embedded `project.publicDecisionBlocks`; owner saves continue to invalidate public menu/OBP cache through `updateProject()`.

### B. End-to-End Request/Data Flow

Decision Blocks scheduled flow:

1. `computeDecisionBlocksScores` runs hourly at `:30` UTC.
2. Function reads `platformSummary/storesSummary`, filters stores whose local settlement window is due, then reads `platformSummary/projects_{sId}` and active project docs.
3. Per project it reads `analytics/{tId}_{sId}_{projectId}_intelligence_7d`.
4. Missing/stale analytics snapshots score as empty analytics instead of falling back to daily range reads.
5. Scheduler extracts active items, computes Decision Blocks, and writes `project.publicDecisionBlocks`.
6. It also computes CMI state and writes `menuIntelligence/{tId}_{sId}_{projectId}`.
7. Public menu route loads project truth, extracts the embedded Decision Blocks projection, and passes it to `DecisionBlocks.tsx`.
8. Runtime filters candidates by TTL, malformed timestamp safety, lifecycle gates, business type, owner toggles/pins, item active/available state, category time slots, duplicate prevention, and hidden-price rules.

Manual recovery flow:

1. `triggerDecisionBlocksScoring` requires authenticated platform role.
2. Scoped project/store/all-store requests recompute Decision Blocks.
3. After this audit fix, manual recovery uses the same compact analytics snapshot path as scheduled scoring.

MCE flow:

1. Owner saves project data through `updateProject()`.
2. When `ENABLE_MCE` is true, `mceValidate()` runs client-side on the save payload.
3. `_mce` metadata is merged into the same project write.
4. Public/admin surfaces continue reading the same project document; MCE does not create a parallel truth store.

Output-control flow:

1. `ENABLE_OUTPUT_CONTROL` is currently false.
2. OBP, Brand OBP, TrustSignals, and mobile hours screens are wired to use confidence-gated hours rendering when the flag is enabled.
3. With the flag off, current public output follows the existing hours status behavior.

### C. Correctness Findings

1. **Fixed: public Decision Blocks used viewer timezone for category time-slot checks.**  
   A customer viewing a store menu from another timezone could see the wrong time-slot-gated recommendations.

2. **Fixed: malformed `computedAt` timestamps were not treated as stale.**  
   `NaN` comparisons could allow malformed precomputed data to avoid the hard-stale guard when `validUntil` still looked valid.

3. **Fixed: platform manual scoring used a hidden daily analytics range query when analytics were not prefetched.**  
   This diverged from the scheduler's compact snapshot contract and created a cost surprise during recovery/backfill.

4. **Fixed docs/runtime drift.**  
   Decision Intelligence, CMI, and MCE docs now describe the current controlled-testing checkpoint, active `ENABLE_MCE: true` runtime, embedded `publicDecisionBlocks` write model, compact analytics snapshot cost contract, and priority-only CMI DAL.

5. **Verified: output control is dormant.**  
   `ENABLE_OUTPUT_CONTROL` is false. OBP, TrustSignals, and mobile hours consumers are wired but not production-active.

6. **Verified in July 1 follow-up: GrowthOS and Digital Screens are not CMI consumers.**
   `verify:growthos` now locks GrowthOS away from `menuIntelligence`/CMI reads, and `verify:customer-app-pwa` locks Digital Screen rendering/settings/display paths away from CMI reads. A future CMI consumer must go through its own feature loop instead of silently changing these downstream surfaces.

### D. Firebase Cost Audit

Reads:

- Public Decision Blocks add zero customer Firestore reads because the projection is embedded in the already-loaded project document.
- Scheduled scoring reads per active project: project doc, compact analytics snapshot, and existing intelligence doc when items are present.
- Store/project iteration uses platform summary documents before nested project reads.
- Manual recovery now reads one compact analytics snapshot per scoped active project instead of opening a daily analytics range.

Writes:

- Scheduled scoring writes one project merge for `publicDecisionBlocks`.
- CMI writes one `menuIntelligence` doc per active project with items.
- MCE adds no extra write operation because `_mce` is included in the existing project write.

Cost verdict:

- Cost improved for manual recovery/backfill by removing the hidden daily analytics range query.
- Public page cost did not increase.
- No new collection, index, Storage path, or scheduler was added.
- A targeted Functions deploy remains pending under the current External Certification Gate 1 flow. The older `ecomsai` billing/Secret Manager blocker is historical evidence only; current retry evidence must target `menulist-qa`, where the latest scoped Functions deploy blocker is Cloud Resource Manager HTTP 403 caller permission before upload.

### E. UI/UX Audit

Desktop:

- Owner recommendation controls remain simple toggles and pins.
- MCE remains silent during normal saves and only affects the existing publish gate when project data is critically invalid.
- No new owner decision or dashboard setting was added.

Mobile:

- Smart Recommendations uses the same project settings model as desktop.
- Mobile hours confidence nudge remains behind the disabled output-control flag.
- No mobile route bypass was added.

Public/customer:

- Decision Blocks keep the same UI, but now evaluate category time slots in the store timezone.
- Stale or malformed automatic scoring degrades to owner-pinned-only behavior rather than unsafe automatic recommendations.
- Output control remains dormant; public hours rendering is unchanged while the flag is off.

### F. Product/Positioning Audit

The fixes support MenuList's public-business truth model:

- Scheduler remains the ranking authority; the client only filters for real-time safety.
- Owner pins remain explicit owner truth but cannot override active/available/time-slot/price-visibility safety.
- CMI remains an observation and priority layer, not a truth-hiding layer.
- MCE validates the existing project document instead of creating duplicate truth stores.

### G. Documentation Alignment

Updated:

- `__docs__/decision-intelligence/README.md`
- `__docs__/decision-intelligence/decision-intelligence_impl.md`
- `__docs__/decision-intelligence/decision-intelligence_firebase.md`
- `__docs__/continuous-menu-intelligence/README.md`
- `__docs__/continuous-menu-intelligence/continuous-menu-intelligence_impl.md`
- `__docs__/continuous-menu-intelligence/continuous-menu-intelligence_firebase.md`
- `__docs__/menu-correctness-engine/README.md`
- `__docs__/menu-correctness-engine/menu-correctness-engine_impl.md`
- `__docs__/menu-correctness-engine/menu-correctness-engine_firebase.md`
- `__docs__/audits/menulist-production-readiness-audit.md`

### H. Fixes Made

Changed runtime files:

- `src/components/templates/main-app/projects/b2cView/output/DecisionBlocks.tsx`
- `src/components/templates/main-app/projects/b2cView/menuPage/menuPageNew.tsx`
- `functions/src/decisionBlocksScoring.ts`
- `src/config/features.ts`

Runtime changes:

- Added robust timestamp parsing for Date, ISO/number, Firestore Timestamp, and serialized timestamp shapes.
- Treated invalid/malformed `computedAt` as hard-stale.
- Used store-local minutes for category time-slot checks.
- Passed `storeDetails.timeZone` into the public Decision Blocks renderer.
- Removed the manual scoring daily analytics range-query fallback; manual scoring now uses `fetch7DayAnalytics()`.
- Corrected stale feature-flag comments that implied client-side ranking.

### I. Validation Performed

Commands run after runtime/docs changes:

- `npm --prefix functions run build` - passed
- `npm run lint` - passed
- `git diff --check` - passed after stripping trailing spaces in edited docs
- `npx tsc --noEmit --incremental false --pretty false` - passed
- `npm run verify:growthos`, `npm run verify:customer-app-pwa`, `npx tsc --noEmit --incremental false --pretty false`, full `git diff --check`, and scoped touched-file `git diff --check` - passed in the July 1 follow-up after adding no-CMI-consumer source gates for GrowthOS and Digital Screens
- Historical deploy evidence: `firebase deploy --only functions:computeDecisionBlocksScores,functions:triggerDecisionBlocksScoring --project ecomsai --non-interactive` failed after predeploy lint/build due external Secret Manager billing-disabled HTTP 403 on project `ecomsai`. Current retry evidence must use External Certification Gate 1 against `menulist-qa`.

Deploy blocker details:

- Historical `ecomsai` Secret Manager validation failed for `GEMINI_AI_KEY`, `GEMINI_AI_KEY_2`, `GEMINI_AI_KEY_3`, `GEMINI_AI_KEY_4`, `RAZORPAY_KEY_ID`, `RAZORPAY_KEY_SECRET`, `SENTRY_DSN`, `WHATSAPP_PHONE_NUMBER_ID`, `WHATSAPP_ACCESS_TOKEN`, and `WHATSAPP_APP_SECRET`.
- Current retry evidence must use the scoped `menulist-qa` Gate 1 command; the latest `menulist-qa` Function deploy attempts pass predeploy lint/build and then stop at Cloud Resource Manager HTTP 403 caller permission before upload.

Manual/code review validation:

- Verified `DecisionBlocks` is only called from `MenuPageNew`.
- Verified `MenuPageNew` already has store timezone and now passes it explicitly.
- Verified `triggerDecisionBlocksScoring` is platform-role gated.
- Verified Firestore rules allow tenant/platform reads and deny client writes for `menuIntelligence`.
- Verified `getItemPresentation()` always returns `visible: true` and `getItemsByPriority()` sorts rather than hides.
- Verified output-control consumers are behind `ENABLE_OUTPUT_CONTROL`.

Browser/runtime validation:

- Not run in this slice. Public Decision Blocks browser smoke remains required before full certification.

Build validation:

- `npm run build` not run because the full production-readiness audit is not complete.

---

## Completed Audit 9: Business Health and Owner Business Assistant

**Feature cluster:** Business Health dashboard/page/mobile surface, owner question answering, context packet cache, Action Support, multi-location summary, workflow storage, and internal monitor primitives.

### A. Feature Identification

Routes and surfaces inspected:

- APIs: `src/app/api/owner-business-assistant/current/route.ts`, `analytics/route.ts`, `locations/route.ts`, `answer/route.ts`, `action/route.ts`, `feedback/route.ts`, `thread/[threadId]/route.ts`
- Platform monitor: `src/app/api/platform/owner-business-assistant/monitor/route.ts`
- Server helpers: `src/lib/ownerBusinessAssistant/server/*`
- Actions: `src/lib/ownerBusinessAssistant/actions/*`
- Types/schemas/constants: `src/lib/ownerBusinessAssistant/types.ts`, `schemas.ts`, `constants.ts`
- UI/hooks: `src/hooks/ownerBusinessAssistant/*`, `src/components/templates/main-app/ownerBusinessAssistant/*`, `src/components/mobile/screens/MobileBusinessHealthScreen.tsx`, `src/components/mobile/components/MobileBusinessHealthCard.tsx`
- Scheduler builders: `functions/src/ownerBusinessAssistant/*`, `functions/src/decisionBlocksScoring.ts`, `functions/src/schedulers/menulistMaintenanceScheduler.ts`
- Rules/docs: `firestore.rules`, `__docs__/owner-business-assistant/*`

Affected areas:

- Owner dashboard: yes, dashboard card, Business Health page, analytics strip, suggested questions, and action chips.
- Mobile owner flow: yes, MobileShell Business Health screen and mobile dashboard summary card.
- Public menu/customer page: indirectly, through action public-truth guardrails and cache invalidation expectations.
- Official Business Page: indirectly, through public-truth action blocking/open-screen handoff.
- Website/marketing claim: docs only in this slice.
- Admin/internal operations: yes, platform monitor and scheduler/cleanup docs.
- Billing/entitlement: no direct billing mutation; billing questions/actions stay navigate/open only.
- Analytics/feedback/reviews: yes, compact analytics index, feedback summary, answer events, feedback route.
- Multi-location behavior: yes, compact tenant summary route.
- Cache/public truth: yes, browser cache, server packet cache, and owner-assistant packet invalidation contracts.

### B. End-to-End Request/Data Flow

Scheduler/read-model flow:

1. Unified scheduler processes due stores after analytics settlement.
2. Business Health builder uses `platformSummary/projects_{sId}`, capped active project analytics docs, menu intelligence, stores summary facts, and capped guest feedback summaries.
3. Builder writes compact `platformSummary/ownerBusinessHealthCurrent_{tId}_{sId}`, optional `ownerBusinessAnalyticsIndex_{tId}_{sId}`, daily snapshot, and compact multi-location summary.
4. Function writer invalidates matching owner-assistant context packet cache keys after committing the read model.

Owner current/analytics flow:

1. Dashboard/page/mobile hooks read browser cache first.
2. API routes are protected by `withAuth`, feature flags, rate limits, store-scope checks, and `VIEW_ANALYTICS` permission checks.
3. Packet builder uses server packet cache when valid; on miss it reads only compact current/analytics docs.
4. `not_ready` fallback packets and missing analytics responses are not cached as durable business facts.

Answer flow:

1. `/answer` applies SAFE_MODE only when provider AI answers are enabled, rate-limits before work, rejects bodies above 32KB, and validates the request schema.
2. It builds a context packet cache-first, classifies the intent, refuses unsupported domains/periods without raw scans, and returns deterministic grounded answers while `ENABLE_OWNER_BUSINESS_HEALTH_AI_ANSWERS` is false.
3. Optional thread history writes one capped thread document, and optional answer-event logging writes one compact monitor event.

Action flow:

1. `/action` validates a registered action request.
2. It now requires both parent Business Health and Action Support flags.
3. Registry/permission/project-scope checks run before navigation, draft preparation, or check workflow writes.
4. Public-truth writes remain blocked by default and hand off to existing MenuList screens/services.

### C. Correctness Findings

1. **Fixed: Action Support endpoint did not require the parent Business Health flag.**  
   `/api/owner-business-assistant/action` and the executor now require both `ENABLE_OWNER_BUSINESS_HEALTH` and `ENABLE_OWNER_BUSINESS_ACTION_SUPPORT`.

2. **Fixed: multi-location summary endpoint ignored the multi-location flag.**  
   `/api/owner-business-assistant/locations` now requires `ENABLE_OWNER_BUSINESS_HEALTH_MULTI_LOCATION` in addition to the parent feature flag.

3. **Fixed: action payloads were not size-capped.**  
   `OwnerBusinessAssistantActionRequestSchema` now caps `payload` at 12,000 JSON characters before it can be stored in drafts or action audit docs.

Verified:

- Answer route remains deterministic while provider AI answers are disabled.
- Action registry and public-truth guard block direct public-truth mutation by default.
- Thread storage is capped at 20 embedded messages and expires with the thread doc.
- Feedback and answer-event writes are compact and flag-gated.
- Firestore rules deny client reads/writes for assistant workflow collections.

### D. Firebase Cost Audit

Reads:

- Current route: 0 reads on browser/server cache hit, 1 compact `platformSummary` read on miss.
- Analytics route: 0 reads on cache hit, 1 compact analytics-index read on miss.
- Answer route: 0 Firestore reads on server packet cache hit; current + analytics-index on miss.
- Locations route: 2 reads on miss, one tenant summary plus `storesSummary`.
- Scheduler analytics index: bounded to at most 10 active projects for dashboard/today docs.
- Guest feedback builder: capped scheduler-only read path, not owner question path.

Writes:

- Current/snapshot/index/multi-location docs are scheduler writes.
- Answer events, feedback, threads, drafts, and action audit docs are flag-gated and retention-backed.
- Successful low-risk navigation does not write an audit doc.
- New payload cap reduces oversized draft/action document risk.

Cost verdict:

- Hot owner question paths stay cache-first and summary-first.
- No new collection, Storage path, scheduler, or index was added in this audit slice.
- No Firebase deploy was required because only Next/API/schema/docs changed.

### E. UI/UX Audit

Desktop:

- Business Health card and page show stable state and explicitly render "No action needed." when current facts are stable.
- Analytics strip reuses compact period data and avoids raw dashboard clutter.
- Action chips remain registry-driven and open existing screens/drafts instead of direct mutation.

Mobile:

- Mobile dashboard uses the same Business Health current/analytics hooks.
- Mobile Business Health lives inside MobileShell sub-screen routing.
- Mobile action sheet uses the same Action Support API and flags as desktop.

Public/customer:

- No public/customer UI changed in this slice.
- Public-truth action writes remain disabled/guarded, so Business Health cannot silently mutate customer-facing truth.

### F. Product/Positioning Audit

The feature remains Business Health, not a generic assistant:

- It shows checked business state before chat.
- It refuses unsupported domains instead of scanning live collections.
- It presents stable "No action needed" states.
- It keeps mutations in existing owner screens and existing public-truth services.

### G. Documentation Alignment

Updated:

- `__docs__/owner-business-assistant/owner-business-assistant_impl.md`
- `__docs__/owner-business-assistant/_archive/owner-business-assistant_action-support-removed-2026-06-17.md`
- `__docs__/owner-business-assistant/owner-business-assistant_firebase.md`
- `__docs__/owner-business-assistant/owner-business-assistant_validation.md`
- `__docs__/audits/menulist-production-readiness-audit.md`

### H. Fixes Made

Changed runtime files:

- `src/app/api/owner-business-assistant/action/route.ts`
- `src/app/api/owner-business-assistant/locations/route.ts`
- `src/lib/ownerBusinessAssistant/actions/actionExecutor.ts`
- `src/lib/ownerBusinessAssistant/schemas.ts`

Runtime changes:

- Parent Business Health flag now gates Action Support endpoint and executor.
- Multi-location flag now gates the locations endpoint.
- Action payloads are capped before draft/audit storage.

### I. Validation Performed

Commands run after runtime/docs changes:

- `npx tsc --noEmit --incremental false --pretty false` - passed
- `npm run lint` - passed
- `git diff --check` - passed

Manual/code review validation:

- Verified current/analytics/answer routes use `withAuth`, feature flags, rate limits, scope resolution, and permission checks.
- Verified action execution validates action registry, permissions, project membership, and public-truth guardrails.
- Verified thread route checks tenant/store before returning one capped thread doc.
- Verified workflow collections are denied to client Firestore in `firestore.rules`.
- Verified maintenance scheduler has cleanup tasks for expired Business Health workflow docs.

Browser/runtime validation:

- Not run in this slice. Desktop/mobile Business Health browser smoke remains required before full certification.

Build validation:

- `npm run build` not run because the full production-readiness audit is not complete.

---

## Completed Audit 10: Analytics, Guest Feedback, Reviews, and Owner Analytics Surfaces

**Feature cluster:** public customer analytics writes, owner menu/OBP/customer-app analytics read models, legacy GA report routes, Guest Feedback inbox/public submission, Reviews & Reputation scaffolding, and weekly narrative/ROI analytics APIs.

### A. Feature Identification

Routes/surfaces inspected:

- Public writes: `src/app/api/public/analytics/track/route.ts`, `src/app/api/public/feedback/submit/route.ts`
- Owner analytics: `src/components/templates/main-app/dashboard/OwnerDashboard/*`, `src/components/templates/main-app/dashboard/AnalyticsDashboard/*`, `src/hooks/useOwnerDashboard.ts`, `src/hooks/useOBPDashboard.ts`, `src/hooks/useAnalyticsData.ts`, `src/hooks/useCustomerAppDashboard.ts`, `src/database/ownerDashboard/index.ts`, `src/database/analytics/index.ts`, `src/lib/analytics/*`
- Legacy GA routes: `src/app/api/analytics/route.ts`, `src/app/api/analytics/realtime/route.ts`, `src/app/api/analytics/reports/route.ts`, `src/app/api/analytics/menu/route.ts`, `src/app/api/analytics/locations/route.ts`
- Expensive analytics routes: `src/app/api/analytics/weekly-narrative/generate-local/route.ts`, `src/app/api/analytics/weekly-narrative/regenerate/route.ts`, `src/app/api/analytics/roi-metrics/route.ts`
- Feedback: `src/app/feedback/[projectId]/page.tsx`, `src/components/atoms/GuestFeedbackForm/index.tsx`, `src/components/templates/main-app/feedback/*`, `src/components/mobile/screens/MobileFeedbackScreen.tsx`, `src/components/mobile/screens/MobileFeedbackDetail.tsx`, `src/database/guestFeedback/*`
- Reviews: `src/app/api/reviews/states/route.ts`, `src/app/api/reviews/suggest/route.ts`, `src/components/templates/main-app/reviews/*`, `src/types/reviews.ts`
- Firebase contracts: `firestore.rules`, `firestore.indexes.json`, `src/constants/database.ts`, `src/config/features.ts`
- Docs: `__docs__/client-menu/analytics-tracking/analytics-tracking_firebase.md`, `__docs__/projects/internal-feedback-system/*`, `__docs__/reviews-reputation/README.md`, `__docs__/reputation-protection/reputation-protection_impl.md`

Affected surfaces:

- owner dashboard: yes
- mobile owner flow: yes, feedback inbox and Customer App metrics
- public menu/customer page: yes, analytics and feedback
- Official Business Page: yes, OBP analytics
- website/marketing claim: docs only, no public marketing copy changed
- admin/internal operations: yes, legacy GA routes and weekly narrative
- billing/entitlement: AI reply suggestion capacity/accounting inspected
- analytics/feedback/reviews: yes
- multi-location behavior: yes, guest feedback store-scoped update path
- cache/public truth: analytics writes do not invalidate public truth; feedback remains private and does not invalidate public packets

### B. End-to-End Request/Data Flow

Public analytics:

1. Public menu/OBP/customer-app trackers coalesce local counters in `src/database/analytics/index.ts`.
2. Browser flushes to `POST /api/public/analytics/track`.
3. Route rate-limits by IP, validates body shape, loads store/project target, enforces store analytics preferences, resolves the store-owned business-day date, strips Decision Blocks fields when disabled, and writes daily analytics via Admin SDK.
4. Owner dashboard reads `analytics/{tId}_{sId}_{projectId}_dashboard_summary` plus today's daily doc from client DAL with SWR/localStorage cache.
5. Nightly scheduler read models remain the cost boundary for settled tabs.

Guest Feedback:

1. Customer opens `/feedback/{projectId}` or feedback link from public surfaces.
2. Server page loads nested project and store with Admin SDK and blocks inactive/deleted/blocked/disabled targets.
3. Client form validates rating and visible fields, then posts to `POST /api/public/feedback/submit`.
4. API rate-limits, validates honeypot/body, re-verifies project/store, enforces store-owned field defaults, writes with Admin SDK, and returns `feedbackId` plus configured review URL.
5. Desktop and mobile owner inboxes read paginated tenant/store-scoped feedback through the shared client DAL and can mark `new`/`resolved`.

Reviews:

1. Product remains disabled by default until GBP ingestion is real.
2. `GET /api/reviews/states` is authenticated, parent-flag gated, rate-limited, and queries non-expired state docs when enabled.
3. `POST /api/reviews/suggest` is authenticated, parent+child flag gated, SAFE_MODE guarded, rate-limited, capacity/accounting checked, and returns owner-reviewed suggestions only.

### C. Correctness Findings

1. Fixed: public feedback route imported the browser Firestore DAL and relied on unauthenticated direct Firestore create rules. Added `src/database/guestFeedback/server.ts`, moved public writes to Admin SDK, and denied public Firestore creates.
2. Fixed: public feedback API accepted hidden contact fields when posted directly. The API now enforces store defaults, drops disabled fields, and validates required fields server-side.
3. Fixed: feedback single-doc read/update checked tenant but not store. Store-scoped managers now cannot update another store's feedback inside the same tenant.
4. Fixed: feedback rules allowed broad content mutation. Client updates are now limited to resolution fields.
5. Fixed: public analytics route trusted client timezone/business-day metadata and client-only tracking preferences. The route now uses store-owned settings and skips disabled surfaces server-side.
6. Fixed: legacy GA routes accepted arbitrary property IDs. Added `src/lib/analytics/googlePropertyAccess.ts` and wired store-property authorization into root, realtime, reports, menu, and locations routes.
7. Fixed: reviews feature flags were enabled despite GBP ingestion being blocked and the review UI not mounted. Both review flags are now disabled by default.
8. Fixed: review-state endpoint filtered expiration after `limit(1)`, which could miss an active warning behind an expired doc. It now queries `autoExpiresAt > now`.
9. Fixed: review suggestion route did not require the parent reviews flag or SAFE_MODE. Both guards are now enforced.
10. Fixed: weekly narrative manual generation had SAFE_MODE but no per-user throttle. Added a strict `BATCH_OPERATION` rate limit.
11. Hardened: `ReviewReplyTool` now mirrors the API's parent+child feature gate, and `verify:menulist-api-tenant-safety` locks the disabled flags, disabled API responses, missing owner mount imports, and audit blocker text until GBP ingestion and intentional mount points exist.

### D. Firebase Cost Audit

- Public analytics writes: one Admin SDK daily-doc merge per coalesced flush/action. Validation cache reads store/project target at most once per 5 minutes per target via `unstable_cache`.
- Public analytics now skips writes when the relevant store analytics category is disabled and strips Decision Blocks fields when that category is disabled.
- Owner dashboard reads: Today reads one daily doc per Menu/OBP surface with a 10-minute local cache; settled tabs read one dashboard summary per surface per scheduler cache window.
- Deep analytics range reads: read-model backed for recent ranges; range including today adds one daily read.
- Guest feedback public submit: one project read, one store read, one feedback write, plus non-blocking compact feedback event write.
- Guest feedback inbox: first desktop load reads one paginated list query plus one count query; mobile reads one paginated list query. No listeners.
- Review states: when enabled, two indexed, rate-limited queries capped to one doc each.
- Weekly narrative: expensive Gemini route is SAFE_MODE guarded and rate-limited to the existing batch-operation budget.
- Firestore indexes updated for non-expired `reviewsState` block/escalation queries.

### E. UI/UX Audit

- Owner dashboard remains read-model driven and uses stable "Today", "Overview", "Yesterday", "This Week", "This Month", and "Overall" tabs.
- Mobile Customer App analytics mirrors desktop card content and keeps compact touch-friendly cards.
- Guest Feedback public form remains mobile-first, private, and non-admin-like.
- Desktop/mobile feedback inboxes share the same DAL and status semantics.
- Reviews UI components are not mounted while the product is disabled, avoiding premature owner clutter.
- Review reply assist remains hidden unless both `ENABLE_REVIEWS_REPUTATION` and `ENABLE_AI_REPLY_ASSIST` are intentionally enabled.

### F. Product/Positioning Audit

- Analytics remains owner-visible operational evidence, not generic event tracking. Public writes are only accepted for owner dashboard/read-model surfaces.
- Guest Feedback remains a private correction inbox, not CRM, NPS, sentiment, or review gating.
- Reviews remains disabled infrastructure until GBP ingestion exists; AI reply assist is not positioned or surfaced as a generic review-management product.
- Active review-suggestion code comments now describe the endpoint/component as dormant until GBP-backed ingestion exists rather than a standalone without-GBP tool.

### G. Documentation Alignment

Updated docs:

- `__docs__/client-menu/analytics-tracking/analytics-tracking_firebase.md`
- `__docs__/projects/internal-feedback-system/README.md`
- `__docs__/projects/internal-feedback-system/internal-feedback-system_impl.md`
- `__docs__/projects/internal-feedback-system/internal-feedback-system_validation.md`
- `__docs__/reviews-reputation/README.md`
- `__docs__/reputation-protection/reputation-protection_impl.md`

Docs now distinguish live public analytics, live Guest Feedback, and disabled Reviews/Reputation scaffolding.

### H. Fixes Made

Runtime/files changed in this slice:

- Added `src/database/guestFeedback/server.ts`
- Added `src/lib/analytics/googlePropertyAccess.ts`
- Updated `src/app/api/public/feedback/submit/route.ts`
- Updated `src/app/api/public/analytics/track/route.ts`
- Updated `src/database/guestFeedback/index.ts`
- Updated `src/lib/validation/apiSchemas.ts`
- Updated `src/app/api/reviews/states/route.ts`
- Updated `src/app/api/reviews/suggest/route.ts`
- Updated `src/components/templates/main-app/reviews/ReviewReplyTool.tsx`
- Updated `scripts/verification/verify-menulist-api-tenant-safety.js`
- Updated `src/app/api/analytics/weekly-narrative/generate-local/route.ts`
- Updated `src/app/api/analytics/{route,realtime,reports,menu,locations}/route.ts`
- Updated `src/config/features.ts`
- Updated `firestore.rules`
- Updated `firestore.indexes.json`

### I. Validation Performed

- `npx tsc --noEmit --incremental false --pretty false` — passed after this slice's runtime changes.
- `npm run verify:menulist-api-tenant-safety` — passed after adding the Reviews/Reputation dormant GBP/mount-point source gate.
- `npm run verify:auth-security-failure-matrix` and `npm run verify:ai-accounting` — passed after the Reviews/Reputation dormant-boundary changes.
- Full `git diff --check` and scoped touched-file `git diff --check` — passed.
- `npm run lint` — passed.
- `git diff --check` — passed.
- `firebase deploy --only firestore:rules --project ecomsai` — passed.
- `firebase deploy --only firestore:rules,firestore:indexes --project ecomsai` — rules compiled/uploaded, but index deploy failed on an unrelated existing `kb_articles` index 409.
- Targeted `gcloud firestore indexes composite create` commands were issued for the two new `reviewsState` indexes. Both operations were accepted and later reported `done: true`:
  - `reviewsState`: `tId ASC`, `sId ASC`, `blockActive ASC`, `autoExpiresAt ASC`
  - `reviewsState`: `tId ASC`, `sId ASC`, `escalationActive ASC`, `autoExpiresAt ASC`
- Browser smoke for public feedback and owner feedback inbox remains pending.

### Remaining Risk For This Slice

- Legacy Google Analytics routes still depend on correct owner-provided GA property configuration and service-account access.
- Reviews/Reputation is not launched; GBP ingestion, owner mount points, and end-to-end review data flow remain blocked.
- Feedback QR/public form browser smoke was not run yet in this slice.

---

## Completed Audit 11: Roles, Permissions, Staff/User Management, Auth Session Guards, and Multi-Chain Access

**Feature cluster:** staff CRUD, role CRUD, owner/staff login access, session claims, store switching, route permission requirements, Firestore user rules, and multi-location staff payload isolation.

### A. Feature Identification

Routes/surfaces inspected:

- Staff APIs: `src/app/api/staff/route.ts`, `src/app/api/staff/password-reset/route.ts`, `src/app/api/staff/force-signout/route.ts`, `src/app/api/staff/roles/route.ts`, legacy wrapper `src/app/api/auth/create-staff/route.ts`
- Auth/session APIs: `src/app/api/auth/[...nextauth]/route.ts`, `src/app/api/auth/set-claims/route.ts`, `src/app/api/auth/access-status/route.ts`, `src/app/api/auth/switch-store/route.ts`, `src/app/api/auth/update-profile/route.ts`, `src/app/api/auth/change-password/route.ts`
- Runtime helpers: `src/lib/staffManagement/server.ts`, `src/lib/staffManagement/client.ts`, `src/lib/permissions/*`, `src/lib/auth/index.ts`, `src/lib/auth/firebaseAuthSync.ts`, `src/lib/multiOutlet/storeSwitchAccess.ts`, `src/middleware/auth.ts`
- Owner UI: `src/components/templates/main-app/users/usersList/*`, `src/components/templates/main-app/users/permissions/*`, `src/components/auth/SessionExpiryMonitor.tsx`
- Mobile UI: `src/components/mobile/screens/MobileUsersScreen.tsx`, `src/components/mobile/screens/MobileRolesScreen.tsx`, `src/components/mobile/screens/MobileMoreScreen.tsx`, `src/components/mobile/MobileShell.tsx`
- Firebase/rules/docs: `firestore.rules`, `src/constants/permissions.ts`, `src/data/shared/defaultRoles.ts`, `__docs__/roles-permissions/*`, `__docs__/multi-chain-permissions/*`, `__docs__/auth/*`

Affected surfaces:

- owner dashboard: yes
- mobile owner flow: yes
- public menu/customer page: no direct customer render, but role/session correctness protects public-truth write paths
- Official Business Page: indirect through permissions on public presence/domain/settings
- website/marketing claim: docs only
- admin/internal operations: yes, platform user management and Firestore rules
- billing/entitlement: indirect through `canAccessBilling`, `canManageSubscription`, and store switching
- analytics/feedback/reviews: indirect through route permission requirements
- multi-location behavior: yes
- cache/public truth: indirect through permissioned owner writes

### B. End-to-End Request/Data Flow

Staff list and management:

1. Desktop/mobile staff screens call `GET /api/staff?tenantId=&storeId=`.
2. `withAuth()` validates session/CORS and blocks inactive, deleted, unverified, or platform-blocked accounts.
3. Staff server resolves acting authority from the session store role and store document.
4. Staff list reads users assigned to the current store using `users.storeIds array-contains storeId` plus legacy `users.storeId == storeId` variants, validates tenant, loads role store options, and returns sanitized staff summaries.
5. Non-master managers receive only the current-store mapping for each staff member; master users can see/manage tenant store options.
6. Create/update/remove/reset/force-signout routes validate input with Zod, enforce `canManageUsers` and `canAssignRoles` where relevant, validate store/role mappings, update the user doc with Admin SDK, and mirror revocation/disabled state to Firebase Auth.
7. Desktop and mobile update their local staff list from the returned sanitized user. Removal from the current store removes the row even when the account remains assigned elsewhere.

Role management:

1. Desktop/mobile role screens use `POST/PATCH/DELETE /api/staff/roles`.
2. Server checks `canAssignRoles`, locks the owner role, validates role payload permissions against `ALL_PERMISSIONS`, prevents deactivating roles assigned to active current-store users, and writes the store `roles[]` array.
3. `sessionProvider` resolves permissions from the active store role and applies master/outlet policy where relevant.

Auth/session/store switching:

1. NextAuth loads a compact user context into JWT/session.
2. The session user now includes `storeIds` derived from user `storeIds`, `stores[]`, and active `storeId`.
3. Firebase claims sync uses `/api/auth/set-claims`, validates target store membership, and mints store-scoped Firebase custom claims.
4. Store switching uses `/api/auth/switch-store`, verifies the current tenant/store session, rate-limits the action, rejects bodies above 1KB before validation, checks `canSwitchStores`, validates tenant store membership, and refreshes Firebase claims.
5. `SessionExpiryMonitor` polls `/api/auth/access-status` while visible and signs out on revoked, inactive, deleted, blocked, tenant-blocked, or store-blocked access.

### C. Correctness Findings

1. Fixed: non-master store managers could receive staff summaries containing all of a staff member's store mappings inside the tenant. Staff payloads are now current-store scoped unless the acting user has master authority.
2. Fixed: the staff list used a tenant-wide user read even though the screen is current-store scoped. It now queries by `storeIds array-contains` and filters tenant.
3. Fixed: last-owner and role-in-use checks scanned the whole tenant. They now read only users assigned to the affected store.
4. Fixed: removing a multi-store staff member from the current store left the row visible in desktop/mobile lists when the account remained assigned elsewhere. Both UIs now remove the row when the returned user no longer has the current store.
5. Fixed: NextAuth compact session omitted `storeIds`, while several guarded helpers expected it. The session payload now includes a compact derived `storeIds` array.
6. Fixed: Firestore `users/{userId}` rules allowed broad direct self-writes for any account whose Firebase Auth UID matched the Firestore doc ID. Direct owner/staff user writes are now denied; platform admins remain allowed, and normal profile/staff access writes go through server APIs.
7. Verified: legacy `/api/auth/create-staff` is only a wrapper around the current staff server implementation, not a duplicate mutation path.
8. Verified: Answerlattice staff access code is separate and was not changed.
9. Fixed: auth/session mutations parsed JSON without shared body caps. Claim-account, change-password, switch-store, and phone OTP start/verify now use bounded JSON parsing; phone OTP start/verify run IP throttles before request parsing.

### D. Firebase Cost Audit

- Staff list: bounded `users` queries by numeric/string current `storeId` in `storeIds` and legacy `storeId`, tenant-filtered in memory, plus one current store read for non-master users or tenant store reads for master store options.
- Role deactivation/owner-protection checks: current-store user queries instead of tenant-wide scans.
- Staff create/update/remove/reset/force-signout: one target user read and one user write in the normal path, plus Firebase Auth Admin operations for account creation, disable/enable, password reset, or refresh-token revocation where applicable.
- Role save/delete: one store read and one store write; deactivation checks current-store users.
- Session access check: one user read, and tenant/store reads only when IDs are present.
- Store switching: rate-limits and rejects bodies above 1KB before reading current store/tenant summary; no Firestore write.
- Claim-account, change-password, and phone OTP body caps add no Firestore writes for rejected oversized bodies and prevent malformed requests from reaching claim-token queries, Firebase Auth verification, OTP writes, or login-token writes.
- Firestore rules now make owner/staff direct user writes zero-cost and impossible; server APIs own user writes.

### E. UI/UX Audit

- Desktop staff and roles screens are permission-aware and use explicit destructive confirmations for removal, role deactivation, passcode reset, and force sign-out.
- Mobile staff and roles screens are present inside `MobileShell` through More sub-screens, use the same APIs as desktop, and keep 44px-class primary/destructive actions.
- Phone-only owners can add staff, reset a passcode, force sign out staff, remove staff from a store, and edit roles.
- The remaining UX limitation is that complex multi-store staff remapping is still easier on desktop; server authority protects it, but the mobile UI intentionally keeps current-store operations simple.

### F. Product/Positioning Audit

- Staff/roles remain operational guardrails, not a generic HR/team-management product.
- Current-store payload isolation supports owner trust and avoids exposing hidden location assignments to local managers.
- Server-only user writes preserve MenuList's public-truth authority by preventing owner/staff clients from directly changing role/store/access fields.

### G. Documentation Alignment

Updated docs:

- `__docs__/roles-permissions/roles-permissions_impl.md`
- `__docs__/roles-permissions/roles-permissions_firebase.md`
- `__docs__/roles-permissions/roles-permissions_mobile-support.md`
- `__docs__/auth/auth_firebase.md`
- `__docs__/auth/auth_mobile-support.md`

Docs now reflect mobile staff support, current-store staff list reads, current-store payload isolation, `storeIds` session claims, and direct Firestore user-write restrictions.

### H. Fixes Made

Runtime/files changed in this slice:

- `src/lib/staffManagement/server.ts`
- `src/lib/auth/index.ts`
- `src/components/templates/main-app/users/usersList/index.tsx`
- `src/components/mobile/screens/MobileUsersScreen.tsx`
- `firestore.rules`

### I. Validation Performed

- `npx tsc --noEmit --incremental false --pretty false` — passed after this slice's runtime/rules changes.
- `npm run lint` — passed.
- `git diff --check` — passed after removing one trailing-space doc line.
- `firebase deploy --only firestore:rules --project ecomsai` — passed; rules compiled and were released to Cloud Firestore.
- Browser smoke for desktop/mobile staff and roles screens remains pending.

### Remaining Risk For This Slice

- Legacy user documents that only have `storeId` are included by the bounded legacy query. Older multi-store user documents missing both `storeIds` and the current `storeId` still need a migration or profile/staff write to be visible in store-scoped staff lists.
- Browser smoke has not yet been run for `/users/list`, `/users/permissions`, or the mobile Staff/Roles sub-screens.
- Full billing/entitlement audit is still pending; role flags that gate billing were inspected only as permissions, not as billing correctness.

---

## Completed Audit 12: Billing, Subscriptions, Razorpay, AI Capacity/Top-Ups, Entitlement Gates, and Reseller Dashboard

**Feature cluster:** owner billing, subscription lifecycle, Razorpay checkout/verification/webhooks, AI enhancement packs, subscription entitlement mirrors, billing history, reseller-assisted onboarding, reseller renewals, reseller monthly summary, and mobile billing/reseller parity.

### A. Feature Identification

Routes/surfaces inspected:

- Razorpay APIs: `src/app/api/razorpay/create-subscription/route.ts`, `verify-subscription/route.ts`, `webhook/route.ts`, `create-topup-order/route.ts`, `verify-topup/route.ts`, `upgrade-subscription/route.ts`, `cancel-subscription/route.ts`, `pause-subscription/route.ts`, `resume-subscription/route.ts`
- Onboarding billing: `src/app/api/onboarding/create-subscription/route.ts`
- Reseller APIs: `src/app/api/reseller/onboard/route.ts`, `confirm-payment/route.ts`, `renew/route.ts`, `add-location-capacity/route.ts`, `clients/route.ts`, `profile/route.ts`, `monthly-summary/route.ts`, `manage/route.ts`
- Billing helpers/DAL: `src/lib/billing/*`, `src/lib/razorpay/*`, `src/lib/ai/capacityCheck.ts`, `src/database/subscriptions/*`, `src/database/topups/index.ts`, `src/database/reseller/server.ts`
- Owner UI: `src/components/templates/main-app/billing/*`, `src/components/templates/main-app/transactions/*`, `src/hooks/usePaymentHandler.ts`
- Mobile UI: `src/components/mobile/screens/MobileBillingScreen.tsx`, `MobileTransactionsScreen.tsx`, `MobileResellerDashboardScreen.tsx`, `MobileResellerManagementScreen.tsx`, `MobileResellerOnboardingScreen.tsx`
- Firebase/rules/docs: `firestore.rules`, `firestore.indexes.json`, `src/constants/database.ts`, `src/config/features.ts`, `__docs__/razorpay/*`, `__docs__/ai-enhancement-packs/ai-billing-explainer.md`, `__docs__/reseller-dashboard/*`

Affected surfaces:

- owner dashboard: yes
- mobile owner flow: yes
- public menu/customer page: indirect through paid-access entitlement mirrors and public cache invalidation
- Official Business Page: indirect through plan entitlement cache
- website/marketing claim: pricing/top-up CTA behavior inspected, not fully website-certified
- admin/internal operations: yes, reseller management and monthly summaries
- billing/entitlement: yes
- analytics/feedback/reviews: indirect through plan entitlement and AI capacity
- multi-location behavior: yes, subscription quantity and inherited master billing
- cache/public truth: yes, subscription entitlement mirror revalidates public store/menu tags and assistant packet cache

### B. End-to-End Request/Data Flow

Subscription creation and activation:

1. Owner/mobile payment action calls the shared `usePaymentHandler`.
2. API validates plan/product/currency/quantity, resolves billing scope from session, verifies tenant/store access, checks `canManageSubscription`, rate-limits, and creates a Razorpay subscription.
3. Firestore `subscriptions/{providerSubscriptionId}` is created as `pending` with `topUpCredits: 0`.
4. Razorpay Checkout returns payment/subscription/signature.
5. `/api/razorpay/verify-subscription` validates input, verifies HMAC checkout signature, fetches Razorpay payment/subscription, requires captured payment and matching provider subscription id, verifies local tenant/store ownership, activates the subscription, syncs store entitlement, and sends non-blocking lifecycle/internal messages.
6. Webhooks reject missing signature/secret, declared bodies above 256KB, and `WEBHOOK` IP-rate-limited bursts before raw-body signature validation. The raw body is read through a bounded 256KB stream cap, then signed events use durable `razorpayWebhookEvents` idempotency locks, lean payment transaction audit writes, subscription state-machine transitions, entitlement sync, and non-blocking messaging/alerts.

Plan upgrade:

1. Owner/mobile creates and pays for a new subscription with zero browser-supplied carry-forward.
2. `/api/razorpay/upgrade-subscription` verifies old and new subscription docs belong to the same billing scope.
3. Server computes remaining credits from the old subscription, cancels/expires the old provider subscription, writes old subscription `expired`, writes server-computed carry-forward onto the new subscription, and stamps `carryForwardFromSubscriptionId` for idempotency.

Top-up:

1. Owner/mobile calls `create-topup-order`.
2. API validates auth/permission/rate limit, verifies an active subscription exists, then creates a Razorpay order and pending `topups/{orderId}`.
3. `verify-topup` validates checkout signature, fetches order/payment, verifies tenant/store/order/payment ownership, requires captured payment, finds active subscription, and transactionally increments `topUpCredits` and marks the top-up paid.

Reseller:

1. Reseller/platform APIs require `ENABLE_RESELLER_DASHBOARD` and `withAuth({ requiredPlatformRole: 'RESELLER' })` or platform-only management.
2. Onboard creates tenant/store/owner auth/user in a transaction, then creates either pending Razorpay subscription or active manual subscription.
3. Manual subscription activation/renewal/location-capacity updates use Admin SDK and sync store entitlement.
4. Client lists are bounded and merge current subscription state by subscription ID.
5. Monthly summary reads month-scoped transaction rows and only visible profile docs for non-platform resellers.

### C. Correctness Findings

1. Fixed: `create-subscription` accepted browser-supplied `rc` and wrote it to `topUpCredits`, allowing client-side credit minting during new subscription creation. New subscriptions now start with zero top-up credits.
2. Fixed: upgrade carry-forward depended on a browser-calculated value. The upgrade API now computes remaining credits server-side from the old subscription and applies it to the verified new subscription.
3. Fixed: subscription verification did not require the Razorpay checkout signature or explicitly prove the payment belonged to the submitted subscription. Verification now requires signature, captured payment status, and payment-subscription match.
4. Fixed: top-up checkout could be created before proving the store had an active subscription, risking paid orders that fail at verification. The route now verifies active subscription after rate limiting and before creating a Razorpay order.
5. Fixed: browser subscription DAL attempted to auto-expire subscriptions during a read, but Firestore rules deny subscription writes. Client reads now return no active access without writing; server-owned reads perform expiry writes.
6. Fixed: server-side grace-period auto-expiry updated subscription state but did not sync store entitlement/public cache. It now calls `safeSyncStorePlanEntitlementFromSubscription()`.
7. Fixed: owner billing history had no row limit. Desktop/mobile shared DAL now reads only the latest 50 successful payment events.
8. Fixed: reseller monthly summary read the full reseller profile collection even for normal reseller users. Non-platform users now read only their direct/email-matched profile docs.
9. Fixed: Razorpay webhook raw-body reads were not size-bounded before signature validation. The route now rejects malformed/oversized `content-length` headers, applies the existing `WEBHOOK` public IP limiter, and reads chunked/no-length bodies through a 256KB bounded raw-body reader before signature verification, JSON parsing, or Firestore idempotency work.
10. Fixed: authenticated billing and reseller mutation routes parsed JSON before shared route-size caps. Razorpay payment actions now use 8KB bounded JSON parsing; onboarding subscription setup and reseller write routes use 16KB caps before validation, provider calls, tenant/store reads, subscription writes, account creation, or entitlement sync.
11. Fixed: reseller confirm-payment, renew, and platform manage POSTs lacked a route-level write throttle. They now use the existing `DATA_WRITE` limiter before bounded body parsing and mutation work.
12. Fixed: existing-user subscription creation still fell back to unvalidated `body.name` and `body.email` for pending subscription documents and Razorpay notes after validation. It now uses only authenticated session identity values.

Verified:

- Firestore rules already deny client writes to `subscriptions`, `topups`, `payment_transactions`, `razorpayWebhookEvents`, `resellerTransactions`, and `resellerProfiles`.
- Pause/resume routes are feature-flag disabled by default before provider or Firestore mutation.
- Top-up verification is transactionally idempotent and rejects payment/order mismatches.
- Reseller client list and monthly summaries are bounded.
- Answerlattice billing product paths remain product-aware and were not modified for MenuList-only behavior.

### D. Firebase Cost Audit

- Subscription create: one subscription write after provider subscription creation.
- Subscription verify: one subscription read and one write, plus entitlement mirror writes when status changes.
- Webhook: missing signature/secret, oversized body, rate-limit, invalid signature, and malformed JSON failures perform zero Firestore reads/writes. Signed valid events perform one idempotency transaction, one lean payment transaction write, one subscription read/update when event type requires it, plus entitlement mirror writes on status changes.
- Upgrade: two subscription reads and up to two subscription writes; carry-forward is idempotent by marker.
- Top-up create: one active-subscription read after rate limit and one pending top-up write.
- Top-up verify: top-up and subscription reads inside transaction, subscription/top-up writes inside transaction.
- Billing history: capped at 50 successful payment transaction reads.
- Browser subscription lookup: zero writes; server paths own expiry writes.
- Reseller clients: capped 100 reseller / 200 platform transaction rows plus bounded subscription doc reads.
- Reseller monthly summary: capped 2000 monthly transaction rows; profile reads are 1-2 for reseller users and up to 50 for platform users.
- Rejected oversized/malformed billing or reseller action bodies perform zero Firestore reads/writes. Rate-limited reseller write actions also fail before Firestore/Firebase Auth work.

Cost increased by one read on top-up order creation, intentionally, to prevent paid orders for stores without active subscriptions. This is correctness-positive and bounded behind auth, permission, and rate limit.

### E. UI/UX Audit

- Desktop Billing and Mobile Billing share the same payment hook and DAL.
- Mobile sends Razorpay checkout signature for subscription verification and no longer sends carry-forward credit authority during subscription creation.
- Pending reseller-online subscriptions show Pay Now/copy/open paths.
- Manual reseller subscriptions are labeled as offline one-time prepaid and hide Razorpay-only pause/cancel/upgrade actions.
- Billing history remains user-triggered/lazy and now bounded.
- Exact enhancement balances remain visible in Billing. This is useful for paid feature transparency, but should not spread into public/customer surfaces or turn owner surfaces into generic credit dashboards.

### F. Product/Positioning Audit

- Billing remains infrastructure access and capacity control, not a generic payments dashboard.
- AI capacity stays tied to subscriptions/top-ups and server-side consumption, not owner-controlled toggles.
- Reseller flow remains assisted onboarding infrastructure; it does not create public pricing leakage or separate license objects.
- Entitlement mirrors continue to update public truth/cache when paid access changes.

### G. Documentation Alignment

Updated docs:

- `__docs__/razorpay/razorpay_impl.md`
- `__docs__/razorpay/razorpay_firebase.md`
- `__docs__/razorpay/README.md`
- `__docs__/razorpay/razorpay_mobile-support.md`
- `__docs__/ai-enhancement-packs/ai-billing-explainer.md`
- `__docs__/reseller-dashboard/reseller-dashboard_impl.md`
- `__docs__/reseller-dashboard/reseller-dashboard_firebase.md`
- `__docs__/reseller-dashboard/reseller-dashboard_mobile-support.md`

Docs now reflect server-side carry-forward, checkout-signature verification, top-up active-subscription gating, client read/no-write grace behavior, bounded billing history, reseller monthly-summary profile scoping, and Razorpay webhook cheap-fail/body-size/rate-limit ordering.

### H. Fixes Made

Runtime/files changed in this slice:

- `src/lib/validation/apiSchemas.ts`
- `src/app/api/razorpay/create-subscription/route.ts`
- `src/app/api/razorpay/verify-subscription/route.ts`
- `src/app/api/razorpay/webhook/route.ts`
- `src/app/api/razorpay/upgrade-subscription/route.ts`
- `src/app/api/razorpay/create-topup-order/route.ts`
- `src/hooks/usePaymentHandler.ts`
- `src/types/razorpay.ts`
- `src/database/subscriptions/index.ts`
- `src/database/subscriptions/server.ts`
- `src/database/subscriptions/paymentTransactions.ts`
- `src/app/api/reseller/monthly-summary/route.ts`

### I. Validation Performed

- `npx tsc --noEmit --incremental false --pretty false` — passed after this slice's runtime/doc changes.
- `npm run lint` — passed.
- `git diff --check` — passed after removing reseller-doc trailing spaces.

Pending validation for this slice:

- Browser smoke for desktop/mobile Billing and Reseller screens.
- Real Razorpay sandbox smoke was not run in this environment.

### Remaining Risk For This Slice

- Self-serve and reseller online onboarding now compensate local tenant/store/user creation when Razorpay plan lookup or subscription creation fails after the local transaction. Razorpay sandbox smoke remains pending.
- Reseller offline active-count decrement on expiry is now covered by the consolidated maintenance scheduler source path; production effect still depends on a Firebase Functions deploy.
- Billing UI still shows exact enhancement balances. That is acceptable on Billing, but should be watched so MenuList does not become a generic credits dashboard.
- No Vercel deploy or Razorpay sandbox test was run.

---

## Completed Audit 13: Multi-Outlet Consistency, Store Onboarding, Outlet Policy, Location Lifecycle, and Scheduler/Ops Maintenance

### A. Feature Identification

**Feature cluster:** Multi-outlet consistency, chain control panel, outlet lifecycle, outlet policy, linked outlet project persistence, project/brand propagation, and consolidated scheduler maintenance.

Relevant routes, components, hooks, DAL/helpers, collections, and docs inspected:

- API routes: `src/app/api/outlets/create/route.ts`, `src/app/api/outlets/deactivate/route.ts`, `src/app/api/outlets/rename/route.ts`, `src/app/api/outlets/policy/route.ts`, `src/app/api/auth/switch-store/route.ts`, `src/app/api/projects/outlet-save/route.ts`
- Owner UI: `src/app/(main)/locations/page.tsx`, `src/components/organisms/AddOutletModal/index.tsx`, `src/components/organisms/OutletPolicyEditor/index.tsx`, `src/components/organisms/OutletRenameModal/index.tsx`
- Mobile UI: `src/components/mobile/screens/MobileLocationsScreen.tsx`, `src/components/mobile/components/MobileProjectSelectorSheet.tsx`
- DAL/helpers: `src/database/multiOutlet/index.ts`, `src/database/multiOutlet/propagation.ts`, `src/database/multiOutlet/brandPropagation.ts`, `src/lib/multiOutlet/locationAccess.ts`, `src/lib/multiOutlet/storeSwitchAccess.ts`, `src/lib/multiOutlet/serverStoreAccess.ts`, `src/lib/multiOutlet/serverOutletPolicy.ts`, `src/lib/multiOutlet/outletProjectPersistence.ts`, `src/lib/multiOutlet/resolveProject.ts`
- Firebase/security: `firestore.rules`, `src/constants/database.ts`, `src/config/features.ts`
- Scheduler/ops: `functions/src/schedulers/menulistMaintenanceScheduler.ts`
- Docs: `__docs__/multi-outlet-consistency/*`, store onboarding docs, mobile support, Firebase contract docs

Affected surfaces:

- owner dashboard: yes
- mobile owner flow: yes
- public menu/customer page: yes, through outlet routing, linked project public output, and cache invalidation
- Official Business Page: yes, through outlet OBP routing and store summary propagation
- website/marketing claim: docs inspected, not full website-certified in this slice
- admin/internal operations: yes, through platform summary and scheduler patterns
- billing/entitlement: yes, through subscription quantity/prepaid capacity checks
- analytics/feedback/reviews: indirect through store switching and public cache
- multi-location behavior: yes
- cache/public truth: yes

### B. End-to-End Request/Data Flow

Outlet creation:

1. Desktop/mobile Locations validates owner input and billing capacity from active subscription state.
2. `POST /api/outlets/create` requires feature flag, auth, tenant/store access, `MANAGE_OUTLETS`, rate limit, an 8KB body cap before validation, active plan when outlet billing is enabled, and prepaid/provider capacity.
3. The route acquires a tenant creation lock, updates Razorpay/provider quantity only when needed and supported, writes internal subscription quantity only when outlet billing is enabled, creates the new store and inherited outlet projects in a transaction, grants creator access, syncs platform summaries, releases lock, and revalidates public/assistant cache paths.

Outlet deactivation/rename/policy:

1. Desktop/mobile actions call server APIs; inactive targets are blocked in UI where applicable.
2. Deactivation verifies caller master authority and validates the target against both tenant `storesList` and canonical `stores/{outletStoreId}` before Admin writes.
3. Deactivation atomically marks store inactive, updates `storesSummary`, and updates tenant `storesList`, then reduces Razorpay-backed quantity only when configured.
4. Rename requires master authority, target tenant match, non-master target, active target, reserved-slug checks, current/previous slug collision checks, transactionally updates store, storesSummary, and tenant storesList, then revalidates public cache.
5. Policy updates are server-owned, master-only, merge with default policy, repair legacy single-store master state when safe, and revalidate public/assistant cache.

Linked outlet project save and propagation:

1. `/api/projects/outlet-save` rejects bodies above 2MB, then validates outlet/master project IDs, tenant/store membership, active target stores, caller permission, local ID prefixes, and OutletPolicy before writing local outlet state.
2. Owner project writes invalidate public menu/store/client cache tags and assistant packet cache.
3. Master-created project propagation now writes only to active non-master outlets.
4. Master store brand/classification propagation now updates only active non-master outlets.
5. Master delete protection now scans only active non-master outlet project collections.

Scheduler/ops:

1. `menulistMaintenanceScheduler` remains the consolidated MenuList operational scheduler.
2. Each task has its own cadence, lease, state update, compact run details, and alert path.
3. No new standalone scheduler was added in this slice.

### C. Correctness Findings

1. Fixed: `POST /api/outlets/deactivate` trusted `tenant.storesList` as the only target boundary before Admin writes. It now validates the canonical target store doc and rechecks target/tenant state inside the transaction.
2. Fixed: outlet creation could update internal subscription quantity even when `ENABLE_OUTLET_BILLING` was disabled. Subscription quantity writes are now guarded by the outlet-billing feature flag.
3. Fixed: project and brand propagation included deactivated outlets. Both propagation loops now filter to active non-master outlets.
4. Fixed: master delete protection scanned inactive stores and could let inactive outlet data block master project cleanup while increasing reads. It now checks active non-master outlets only.
5. Fixed: desktop and mobile Locations counters labeled active outlets but counted inactive outlets. Both now count active outlets.
6. Fixed: outlet rename accepted inactive outlet targets through direct API calls. It now rejects inactive outlet rename attempts.
7. Fixed: outlet lifecycle, outlet policy, and linked outlet project-save routes parsed JSON without route-level body caps. Create, rename, deactivate, and policy actions now use 8KB caps; linked outlet save uses a 2MB cap before validation, policy enforcement, or project writes.
8. Fixed: outlet creation enforced `MAX_OUTLETS_PER_TENANT` against all historical non-master outlets. It now counts active non-master outlets only, matching billing capacity, public visibility, and deactivation-as-replacement-slot behavior.

Verified:

- Store switching rejects inactive targets and checks mapped `storeIds`.
- Firebase claims include `storeIds`; Locations refreshes claims before store-context switching.
- Linked outlet save is server-owned and enforces tenant/store membership, active stores, local IDs, policy flags, and public cache invalidation.
- Public sitemap and public analytics paths filter inactive stores.
- Firestore rules keep direct `platformSummary/storesSummary` writes scoped to the current store entry unless platform-admin/server-owned.

### D. Firebase Cost Audit

- Outlet creation reads caller store, tenant list, active subscription, lock tenant doc, master projects query, master project summary, platform summary count, and creator user doc; writes one store, one tenant update, one storesSummary update, one platform count update, N inherited project docs, N project-summary rows, and optionally one creator access update plus subscription quantity write.
- Deactivation now adds one canonical target-store read and transaction rechecks target/tenant state. This small cost increase prevents cross-tenant/stale-list Admin writes and is correctness-positive.
- Project propagation and brand propagation now skip inactive outlets, reducing writes, summary writes, and public cache revalidations after outlet deactivation.
- Master delete protection now avoids inactive stores, reducing N+1 project collection queries in tenants with old/deactivated outlets.
- The active-only max-outlet cap uses the already-loaded tenant `storesList`, so it adds no Firestore reads/writes/deletes, provider calls, cache invalidations, rules, indexes, Cloud Functions, Firebase deploy, or Vercel deploy requirement.
- Store switching remains one caller store read and one tenant list read.
- Linked outlet save uses bounded direct reads for caller/outlet/master stores, tenant, and existing project; no broad scans.
- Scheduler remains consolidated with per-task leases and compact logs.

### E. UI/UX Audit

- Desktop and mobile Locations show active outlet counts that match billing/public behavior.
- Outlet creation now uses the same active-only count, so deactivating a location frees a replacement slot instead of blocking the owner at the historical outlet cap.
- Inactive outlets remain visible for context but cannot be switched, renamed, or deactivated again from normal UI.
- Add outlet remains a single owner decision with clear billing/prepaid/UPI recovery states.
- Mobile owner flow uses the existing `MobileShell` screen, large touch buttons, bottom sheets, shared policy taxonomy, and direct Billing handoff when payment action is required.
- Public customer surfaces are unaffected except that inactive outlets no longer receive new propagated public truth.

### F. Product/Positioning Audit

The fixes preserve MenuList’s public-truth role:

- Active locations are the only locations receiving new public menu/project/brand truth.
- Admin routes no longer rely on stale denormalized tenant lists for cross-store writes.
- Billing quantity remains infrastructure capacity, not an owner-facing toggle system.
- Multi-location behavior stays simple: HQ controls chain policy; outlets get local exceptions within policy.

### G. Documentation Alignment

Updated docs:

- `__docs__/multi-outlet-consistency/multi-outlet-consistency_impl.md`
- `__docs__/multi-outlet-consistency/multi-outlet-consistency_firebase.md`
- `__docs__/multi-outlet-consistency/multi-outlet-consistency_mobile-support.md`
- `__docs__/audits/menulist-production-readiness-audit.md`

Docs now reflect active-only propagation/delete protection, canonical target-store validation for deactivation, billing-flag isolation for subscription quantity writes, active-outlet desktop/mobile counts, and active-only max-outlet cap enforcement.

### H. Fixes Made

Runtime/files changed in this slice:

- `src/app/api/outlets/create/route.ts`
- `scripts/verification/verify-multi-location-boundary.js`
- `src/app/api/outlets/deactivate/route.ts`
- `src/app/api/outlets/rename/route.ts`
- `src/database/multiOutlet/propagation.ts`
- `src/database/multiOutlet/brandPropagation.ts`
- `src/database/multiOutlet/index.ts`
- `src/app/(main)/locations/page.tsx`
- `src/components/mobile/screens/MobileLocationsScreen.tsx`

### I. Validation Performed

- Multi-location boundary source gate: `npm run verify:multi-location-boundary` — passed; source-only outlet lifecycle/project-save/mobile-shell gate.
- Active-cap focused gate: `node --check scripts/verification/verify-multi-location-boundary.js`, `npm run verify:multi-location-boundary`, `npm run verify:menulist-api-tenant-safety`, `npm run verify:public-business-truth`, `npm run docs:check-links`, `npx tsc --noEmit --incremental false --pretty false`, `git diff --check`, and `npm run verify:production-readiness-local` — passed; aggregate local readiness reported 67/67 checks, including 63 child root `verify:*` scripts.
- Focused multi-location boundary sweep: `npm run verify:public-business-truth`, `npm run verify:menulist-api-tenant-safety`, `npm run verify:auth-security-failure-matrix`, `npm run verify:mobile-shell-route-map`, `npm run verify:doc-npm-scripts`, and `npm run docs:check-links` — passed.
- Current aggregate local readiness after the multi-location source gate: `npm run verify:production-readiness-local` — passed; 47/47 checks, including 43 child root `verify:*` scripts.
- `npx tsc --noEmit --incremental false --pretty false` — passed after runtime changes.
- `npm run lint` — passed.
- `git diff --check` — passed.

Pending validation for this slice:

- Browser smoke for desktop/mobile Locations, Add Outlet, Rename Outlet, and Deactivate Outlet remains pending.
- No Razorpay sandbox or Firebase Function deploy was run for this slice; no Firebase rules/index/function files were changed.

### Remaining Risk For This Slice

- Reseller offline subscription expiry/count maintenance is source-covered by `menulistMaintenanceScheduler`; production effect still depends on a Firebase Functions deploy.
- Browser smoke for desktop/mobile Location flows was not run in this environment.
- Existing deactivated outlet slugs remain reusable in some creation paths when not present in previous-slug history; this may be intended replacement-location behavior, but it should be decided explicitly in the public-routing/outlet-policy docs if URL reuse becomes a support issue.

---

## Completed Audit 14: Public Create-Menu, Messaging Onboarding, Starter Activation, Owner Notifications, and Lifecycle Messaging

### A. Feature Identification

**Feature cluster:** public `/create-menu` intake and claim, messaging onboarding preview/approval, starter activation signals, owner notification delivery, lifecycle messaging, and related scheduler cleanup.

Relevant routes, components, DAL/helpers, functions, collections, and docs inspected:

- Public create-menu: `src/app/(website)/create-menu/*`, `src/app/api/public/create-menu/route.ts`, `src/app/api/public/create-menu/claim/route.ts`, `src/lib/menu-link-import/sourceAcquisition.ts`
- Messaging preview: `src/app/(global-pages)/msg-preview/[sessionId]/page.tsx`, `src/app/api/msg-preview/[sessionId]/route.ts`, `approve/route.ts`, `fix/route.ts`
- Functions messaging: `functions/src/messagingOnboarding/webhookHandler.ts`, `inboundQueue.ts`, `sessionEngine.ts`, `intakeProcessor.ts`, `extractionWatcher.ts`, `constants.ts`
- Scheduler: `functions/src/schedulers/messagingSessionCleanup.ts`, `functions/src/schedulers/menulistMaintenanceScheduler.ts`
- Publish/activation: `src/lib/messaging-onboarding/publish.ts`, `src/lib/onboarding/createTenantStore.ts`, `src/lib/onboarding/starterActivation.ts`, `src/app/(website)/create-menu/success/page.tsx`
- Notifications: `src/lib/owner-notifications/*`, `functions/src/ownerNotifications/processor.ts`, `functions/src/messaging/messagingEngine.ts`, `src/app/api/ops/owner-notifications/route.ts`, `src/components/templates/main-app/platform/ownerNotificationMonitor/index.tsx`
- Firebase/security/constants: `firestore.rules`, `firestore.indexes.json`, `src/constants/database.ts`, `functions/src/constants/database.ts`, `src/config/features.ts`
- Docs: `__docs__/public-menu-entry/*`, `__docs__/messaging-onboarding/*`, `__docs__/owner-notifications/*`, `__docs__/lifecycle-messaging/*`, onboarding docs

Affected surfaces:

- owner dashboard: yes, after claim/publish and ops notification recovery
- mobile owner flow: yes, public create-menu and msg-preview are phone-first web surfaces; starter activation signals also come from mobile Share/Presence paths
- public menu/customer page: yes, claimed drafts and messaging publishes create customer-facing menu truth
- Official Business Page: yes, new store/project/public identity writes feed OBP and `/menu`
- website/marketing claim: yes, `/create-menu` acquisition path and docs
- admin/internal operations: yes, owner-notification ops monitor and scheduler state
- billing/entitlement: indirect, starter activation and lifecycle messaging
- analytics/feedback/reviews: indirect, starter activation signals and notification observability
- multi-location behavior: no direct outlet write in this slice
- cache/public truth: yes, claim/publish paths revalidate public menu/store/client tags

### B. End-to-End Request/Data Flow

Public `/create-menu` flow:

1. Owner opens website `/create-menu`, signs in with phone OTP or Google, then uploads an image or submits a permission-confirmed public menu link.
2. `POST /api/public/create-menu` requires auth, feature flag, SAFE_MODE check, pre-parse body-size guard, user rate limit, active draft/source dedupe, storage/source validation, and writes a `publicMenuDrafts` doc plus a `menuImageProcessingJobs` job.
3. Preview page polls `GET /api/public/create-menu?draftId=...`; the route is owner-bound and now rate-limited before the draft read.
4. Claim route rate-limits publish attempts, rejects bodies above 8KB before draft reads, verifies owner/draft state, creates or updates tenant/store/user/project truth, writes project summary, invalidates `menu-store-{storeId}`, `store-{storeId}`, and `client-stores`, and now preserves a single default project for existing owners.
5. Success page records starter activation signals as non-blocking client-side owner-success metadata.

Messaging onboarding flow:

1. Provider webhook verifies provider/signature, writes a durable sanitized inbound queue doc, and acknowledges quickly.
2. Immediate or scheduled queue drain claims PENDING docs, calls the session engine, sends provider replies when needed, and records processed/failed status with retry/backoff.
3. Session engine checks existing live/existing-store/rate-limit state, downloads media to isolated Storage, deduplicates uploads, and advances intake windows.
4. Intake scheduler validates assets, creates an extraction job, and sends pending preview/fix/publish provider messages.
5. Extraction watcher writes extracted menu data, generates a tokenized preview URL, sends the preview link, and leaves failed sends for retry.
6. Token preview API validates token/state/expiry, now rate-limits reads, and writes at most one `PREVIEW_VIEWED` event per session.
7. Approval route rejects request bodies above 4KB, rate-limits by IP, transactionally moves `AWAITING_APPROVAL` to `PUBLISHING`, validates menu structure, calls the active Next publish library, revalidates public cache, and marks the session `LIVE`.
8. Fix route rejects request bodies above 4KB, limits correction issue choices to 5, validates token/state/correction count, and rate-limits mutation attempts before resetting the session for corrected uploads.

Owner notification/lifecycle flow:

1. Trigger points enqueue product-scoped owner notification events when owner-notification migration flags are on.
2. Shared app and Functions processors resolve registry policy, recipient, formatting context, channel flags, rate limits, template, delivery, and delivery logs.
3. MenuList recipient resolution now reads canonical top-level `stores/{storeId}` first, with nested tenant-store fallback only for legacy compatibility.
4. Platform-only `/ops/owner-notifications` uses bounded manual refresh, detail reads, retry, manual send, and handoff recording; no realtime listener.
5. Legacy lifecycle email remains a fallback when owner-notification migration is disabled or unavailable.

### C. Correctness Findings

1. **Fixed: public create-menu status polling had no backend throttle.**  
   The client polls every 2 seconds up to 30 times. Refresh loops could repeatedly read `publicMenuDrafts`. The GET route now rate-limits `public-menu-entry-status:{userId}:{draftId}` before reading the draft.

2. **Fixed: claiming a public draft for an existing owner could leave multiple default project summaries.**  
   Existing owners claiming a new draft could create a second `isDefault` summary, making `/menu` ambiguous. The claim transaction now demotes existing non-deleted defaults before writing the claimed project summary with the shared summary payload builder.

3. **Fixed: messaging preview reads and view events were unbounded for token links.**  
   The unauthenticated token route could be refreshed repeatedly, causing session reads and `PREVIEW_VIEWED` event writes. The route now rate-limits by session/IP before Firestore and logs preview view only once per session via `previewViewedAt`.

4. **Fixed: preview fix requests lacked an unauthenticated mutation throttle.**  
   The route enforced token/state/correction count but did not throttle before Firestore work. It now rate-limits per session/IP before reading or writing.

5. **Fixed: durable inbound webhook queue relied only on TTL cleanup.**  
   Queue docs had `expiresAt`, but no bounded scheduler fallback if TTL was not enabled or delayed. `messagingSessionCleanupLogic()` now deletes up to 100 expired inbound queue docs per daily run and reports `inboundCleaned`.

6. **Fixed: MenuList owner-notification recipients resolved from a non-canonical nested store path.**  
   The app resolver, Functions owner-notification processor, and legacy Functions lifecycle engine read `stores/{storeId}` first and fall back to `tenants/{tenantId}/stores/{storeId}` only for old compatibility.

7. **Fixed: create-menu upload body limit was enforced only after multipart parsing.**
   `POST /api/public/create-menu` now rejects requests above the 10MB image limit plus multipart overhead by `content-length` before multipart parsing, and the link-import JSON path uses an 8KB bounded body reader before source acquisition. The extraction pipeline verifier now locks both orderings.

8. **Fixed: token preview approve/fix actions parsed unbounded JSON bodies.**
   `/api/msg-preview/[sessionId]/approve` and `/api/msg-preview/[sessionId]/fix` now reject bodies above 4KB before JSON parsing. The fix route also caps `issues` at the 5 known issue types, preventing oversized correction arrays from reaching validation or Firestore work.

9. **Fixed: extraction control JSON routes lacked bounded parsing.**
   Owner extraction jobs now gate upload-control requests and reject bodies above 128KB before project reads or job work. Menu intake identity uses a 256KB cap, and menu-link import plus public draft claim use 8KB caps before source/draft reads.

Verified:

- `/api/public/create-menu` remains auth-first, feature-gated, SAFE_MODE checked, pre-parse body-size guarded, source-deduped, and SSRF-hardened for link import.
- Messaging onboarding collections are Admin SDK only in Firestore rules.
- Messaging indexes cover active-session, intake, cleanup, pending-message, inbound queue, and event queries already present in runtime.
- Messaging publish uses the active Next library, not the stale Functions publish copy.
- Owner notification registry is byte-for-byte mirrored between app and Functions.
- Ops owner-notification API is platform-role guarded and bounded.

### D. Firebase Cost Audit

- Public create-menu POST: pre-parse body-size guard, bounded draft reuse/dedupe reads, 1 draft write, 1 job write, 1 storage write for new source.
- Public create-menu GET: now 1 draft read per poll, client capped at 30 polls, backend capped at 90 status reads per 5 minutes per user/draft.
- Claim: oversized-body failures perform zero Firestore reads/writes; valid claims perform transactional tenant/store/user/project/project-summary writes. Existing-owner default demotion adds only summary row updates needed to preserve one public default.
- Messaging webhook: 1 queue doc create per unique provider message, duplicate provider retries do not write a second queue doc.
- Messaging preview approve/fix oversized-body failures perform zero Firestore reads/writes; valid approve remains the existing publish transaction, and valid fix remains one session read plus one session write.
- Inbound drain: capped 20 docs per intake run; stale PROCESSING reset capped 20.
- Messaging preview GET: 1 session read, now session/IP-limited; first view only writes one session merge and one event.
- Fix route: 1 session read and 1 session update only after rate-limit and token validation.
- Messaging cleanup: session expiry/reminders/expired media cleanup stay capped; inbound queue cleanup adds a capped 0-100 deletes/day fallback.
- Owner notification processing: direct event/delivery/rate-limit docs, direct store read, no realtime listener; ops dashboard list is bounded and manual refresh only.

Cost impact of fixes:

- Reduced unauthenticated token-preview event writes from unbounded per refresh to one per session.
- Added cheap rate-limit checks before public create-menu status reads and msg-preview read/mutation work.
- Added bounded inbound queue deletes to prevent document accumulation.
- Added one fallback read only when canonical top-level store lookup is missing for owner notifications.

### E. UI/UX Audit

- Public `/create-menu` remains sign-in-first and owner-bound; no anonymous AI utility behavior was introduced.
- Preview and success pages are phone-friendly web surfaces, appropriate for owners opening links from messaging apps.
- Fix requests remain simple: choose issue(s), optional short note, then send corrected photos.
- Owner notification recovery stays platform-only and does not add an owner-facing notification dashboard.
- Mobile owner parity is acceptable for this slice because the acquisition/preview surfaces are responsive public web routes, and post-activation owner tasks continue through existing mobile Share/Presence surfaces.

### F. Product/Positioning Audit

The fixes preserve MenuList as public-business truth infrastructure:

- Public starter flows create public truth only after authenticated owner claim or token approval.
- `/menu` authority remains singular after create-menu claim.
- Messaging onboarding remains a narrow acquisition/publish tunnel, not a generic chat/menu tool.
- Owner notifications remain account-critical infrastructure messages, not marketing automation or workflow chatter.
- Scheduler work remains consolidated with leases and bounded cleanup.

### G. Documentation Alignment

Updated docs:

- `__docs__/public-menu-entry/public-menu-entry_firebase.md`
- `__docs__/public-menu-entry/public-menu-entry_impl.md`
- `__docs__/messaging-onboarding/messaging-onboarding_impl.md`
- `__docs__/messaging-onboarding/messaging-onboarding_firebase.md`
- `__docs__/messaging-onboarding/messaging-onboarding_validation.md`
- `__docs__/owner-notifications/owner-notifications_impl.md`
- `__docs__/owner-notifications/owner-notifications_firebase.md`
- `__docs__/audits/menulist-production-readiness-audit.md`

### H. Fixes Made

Runtime files changed in this slice:

- `src/app/api/public/create-menu/route.ts`
- `src/app/api/public/create-menu/claim/route.ts`
- `src/app/api/msg-preview/[sessionId]/route.ts`
- `src/app/api/msg-preview/[sessionId]/approve/route.ts`
- `src/app/api/msg-preview/[sessionId]/fix/route.ts`
- `src/lib/security/boundedRequestBody.ts`
- `src/lib/owner-notifications/recipientResolver.ts`
- `functions/src/ownerNotifications/processor.ts`
- `functions/src/messaging/messagingEngine.ts`
- `functions/src/schedulers/messagingSessionCleanup.ts`
- `functions/src/schedulers/menulistMaintenanceScheduler.ts`

### I. Validation Performed

- Owner notification boundary source gate: `npm run verify:owner-notifications-boundary` — passed; source-only owner-notification registry/API/processor/monitor/docs gate.
- `node --check scripts/verification/verify-owner-notifications-boundary.js` — passed.
- Owner-notification focused sweep: `npm run verify:owner-notifications-boundary`, `npm run verify:menulist-api-tenant-safety`, `npm run verify:auth-security-failure-matrix`, `npm run verify:doc-npm-scripts`, and `npm run docs:check-links` — passed.
- Current aggregate local readiness after the platform cost posture source gate: `npm run verify:production-readiness-local` — passed; 55/55 checks, including 51 child root `verify:*` scripts.
- `npx tsc --noEmit --incremental false --pretty false` — passed after create-menu and messaging/notification fixes.
- `npm --prefix functions run build` — passed.
- `npm run lint` — passed.
- `git diff --check -- src/app/api/public/create-menu/route.ts src/app/api/public/create-menu/claim/route.ts` — passed after public create-menu fixes.
- `git diff --check -- src/app/api/msg-preview/[sessionId]/route.ts src/app/api/msg-preview/[sessionId]/fix/route.ts src/lib/owner-notifications/recipientResolver.ts functions/src/ownerNotifications/processor.ts functions/src/messaging/messagingEngine.ts functions/src/schedulers/messagingSessionCleanup.ts functions/src/schedulers/menulistMaintenanceScheduler.ts` — passed.
- Historical deploy evidence: `firebase deploy --only functions:menulistMaintenanceScheduler,functions:computeDecisionBlocksScores,functions:triggerDecisionBlocksScoring,functions:triggerStoreNightlyScheduler,functions:verifyMenuPublish --project ecomsai` ran predeploy lint/build, then failed before upload validation completed because Secret Manager returned HTTP 403 `billing-disabled` for `GEMINI_AI_KEY*`, `WHATSAPP_*`, `RAZORPAY_*`, and `SENTRY_DSN`.
- Current retry evidence must use the scoped `menulist-qa` deploy path from the External Certification Runbook and launch prerequisites; if messaging/notification/platform-alert work widens the target list, record the exact target list and reason before treating the deploy evidence as current.

Pending for this slice:

- Browser smoke for `/create-menu`, `/create-menu/preview/{draftId}`, `/msg-preview/{sessionId}`, and `/ops/owner-notifications`.
- Real WhatsApp provider smoke remains blocked until provider secrets/runtime flag are configured and Functions deployment succeeds.

---

## Completed Audit 15: Main Website, Public Claims, Trust/Legal, Resources, Pricing, and Discovery Files

### A. Feature Identification

**Feature cluster:** MenuList main website and public buyer/trust layer.

Relevant routes and files inspected:

- `src/app/(website)/`
- `src/components/website/`
- `src/content/websiteResources/`
- `public/locales/menulist.ai/`
- `src/app/sitemap.ts`
- `src/lib/seo/discoveryPolicy.ts`
- `public/robots.txt`
- `public/sitemap.xml`
- `public/llms.txt`
- `public/llms-full.txt`
- `public/manifest.json`
- `scripts/verification/verify-agent-readiness.js`
- `scripts/verification/verify-website-resource-locales.js`
- `src/config/websiteLanguages.ts`
- `__docs__/main-website/*`

Affected surfaces:

- owner dashboard: indirect, via pricing/auth/create-menu handoff copy
- mobile owner flow: indirect, via public setup and owner-phone-dashboard claims
- public menu/customer page: indirect, via website claims and resource guidance
- Official Business Page: indirect, via OBP/public discovery claims
- website/marketing claim: yes
- admin/internal operations: no direct runtime changes
- billing/entitlement: website pricing path inspected; no billing API changes
- analytics/feedback/reviews: website analytics consent and resource GA events inspected
- multi-location behavior: website multi-location page inspected
- cache/public truth: discovery files and sitemap/robots/LLM context inspected

### B. End-to-End Request/Data Flow

Website request flow:

1. Platform-domain requests are served by the website route group after middleware product/tenant separation.
2. Static website pages render server metadata, page-level structured data, header/footer, localized website copy, and no Firestore reads for anonymous visitors.
3. Website analytics scripts are not mounted from the layout directly; `WebsiteAnalyticsConsent` mounts Google Analytics and Clarity only after an accepted analytics choice and clears known analytics cookies after decline.
4. Resource hub/article pages read static content packs, emit resource JSON-LD, and use sessionStorage only for attribution IDs before sending GA events when `gtag` is already available.
5. `/create-menu` uses the already-audited auth-first public starter flow and localized `Website.CreateMenu` copy for upload, preview, error, empty, detected-detail, stats, and claim-form states.
6. `/pricing` serves anonymous visitors without Firestore reads. Authenticated visitors fetch active subscription and tenant details once to decide whether to show subscription management.
7. `/product` is guarded by middleware and the route page itself as a legacy redirect to `/how-it-works`.
8. `sitemap.ts`, `discoveryPolicy.ts`, static sitemap/robots, LLM files, and verification scripts keep platform discovery pages aligned and exclude tenant/client menu content from the platform sitemap.

### C. Correctness Findings

1. **Fixed: public website grid tracks could overflow narrow phones.**  
   Several website sections used fixed `auto-fit` grid minimums such as `minmax(320px, 1fr)`, `minmax(400px, 1fr)`, and `minmax(420px, 1fr)`. With section padding, those tracks can exceed narrow mobile containers and create horizontal scroll. The affected grids now use `minmax(min(100%, ...), 1fr)`.

2. **Verified: `/create-menu` website copy is localized through the `Website` namespace.**  
   Upload, link, auth, preview loading, processing, failure, expiry, empty, detected-detail, stats, tag, and claim states use `useTranslations('Website')` and matching `CreateMenu` locale keys in `en-US` and `hi-IN`.

3. **Verified: public discovery files are aligned.**  
   `npm run verify:agent-readiness` and `npm run verify:website-resource-locales` both passed. `/product` is omitted from sitemap/LLM discovery and redirects to `/how-it-works`.

4. **Verified: dormant locale files are not active website routes.**  
   `public/locales/menulist.ai` contains extra `en-GB`, `gu-IN`, and `zh-CN` JSON files, but `WEBSITE_LANGUAGES` exposes only `en-US`, `hi-IN`, `ta-IN`, `te-IN`, `mr-IN`, `bn-IN`, `ar-SA`, and `es-ES`. Missing active locale files: none.

5. **Verified: public trust/legal claims inspected in this slice are grounded in runtime.**  
   Analytics consent gates main website GA/Clarity loading; Razorpay checkout/payment verification handles payment details externally; owner-managed staff passcodes are set in Firebase Auth and not stored as plain text in user docs; prepared public media is redrawn through canvas-based preparation before storage, which strips source-file metadata for the public media pipeline.

### D. Firebase Cost Audit

- Anonymous website pages: 0 Firestore reads/writes.
- Website resources: 0 Firestore reads/writes; static content and optional GA events only after analytics is loaded.
- Website analytics consent: localStorage/cookie work only; no Firebase operations.
- `/pricing` anonymous path: 0 Firestore reads.
- `/pricing` authenticated path: 1 active-subscription lookup plus 1 tenant lookup when a tenant/store is present.
- `/create-menu`: uses the already-audited public create-menu API flow from Audit 14.
- Discovery files: static/dynamic metadata generation, no Firebase reads.

Cost impact of fixes:

- CSS-only grid changes; no Firebase cost change.

### E. UI/UX Audit

- Public pages keep product-truth positioning and do not present the website as a generic QR/menu maker.
- The create-menu funnel remains sign-in-first and owner-bound before AI processing.
- The trust/security page, contact page, multi-location page, pricing page, homepage, and legacy `/product` redirect were smoke-tested at 390px width with no horizontal overflow after the grid fix.
- No desktop-only owner action was introduced.
- Analytics consent remains compact and user-changeable through footer preferences.

### F. Product/Positioning Audit

The audited website layer supports MenuList as public-business truth infrastructure:

- Public copy avoids ranking guarantees, automatic Google/POS sync promises, and AI-menu-maker positioning.
- Resource articles that mention ranking or guarantees do so as explicit disclaimers.
- Public discovery claims are backed by sitemap, robots, JSON-LD, route metadata, and LLM context files.
- Legal/trust pages are written as practical operational disclosures rather than unsupported security-certification claims.

### G. Documentation Alignment

Updated docs:

- `__docs__/main-website/README.md`
- `__docs__/main-website/main-website_design-system.md`
- `__docs__/audits/menulist-production-readiness-audit.md`

No website marketing claim was added. The docs now describe the container-safe grid contract and the v3.6.52 website hardening pass.

### H. Fixes Made

Runtime files changed in this slice:

- `src/components/website/trust-security/TrustSecurityPage.tsx`
- `src/components/website/contact/ContactPage.tsx`
- `src/components/website/multi-location/MultiLocationPage.tsx`
- `src/components/website/product/ProductPage.tsx`
- `src/components/website/home/AnalyticsInsightsSection.tsx`
- `src/components/website/home/BusinessSection.tsx`
- `src/components/website/home/CustomerBrowseSection.tsx`
- `src/components/website/home/InteractiveWorkflowSection.tsx`
- `src/components/website/home/SmartFeaturesSection.tsx`

### I. Validation Performed

- `npx tsc --noEmit --incremental false --pretty false` — passed.
- `npm run lint` — passed.
- `git diff --check -- [website grid files]` — passed.
- `npm run verify:agent-readiness` — passed.
- `npm run verify:website-resource-locales` — passed.
- Local dev server smoke at `http://127.0.0.1:3015` with 390px viewport:
  - `/` — no horizontal overflow.
  - `/contact` — no horizontal overflow.
  - `/multi-location` — no horizontal overflow.
  - `/pricing` — no horizontal overflow.
  - `/trust-security` — no horizontal overflow.
  - `/product` — redirected to `/how-it-works`, no horizontal overflow.

Pending for this slice:

- Full desktop visual screenshot review across every website route was not repeated in this slice.
- `npm run build` remains global-final validation and has not been run for the full certification.

---

## Completed Audit 16: Physical Surfaces, Print/Export, QR/Menu Cards, and Use MenuList Assets

### A. Feature Identification

**Feature cluster:** Owner output hub, project Share modal, Menu Kit, QR/download assets, Print Assets, Menu Card Export, mobile Share, and physical-surface print generators.

Relevant routes and files inspected:

- `src/app/(main)/use-menulist/page.tsx`
- `src/app/(main)/use-menulist/print-assets/page.tsx`
- `src/app/(main)/use-menulist/menu-card-export/page.tsx`
- `src/components/templates/main-app/useMenuList/index.tsx`
- `src/components/templates/main-app/useMenuList/CommunicationKit.tsx`
- `src/components/templates/main-app/useMenuList/PresenceMonitor.tsx`
- `src/components/templates/main-app/printableAssetTemplates/PrintableAssetTemplatesRoute.tsx`
- `src/components/templates/main-app/projects/b2cView/shareModal/index.tsx`
- `src/components/templates/main-app/projects/b2cView/shareModal/MenuKitSection.tsx`
- `src/components/templates/main-app/projects/b2cView/shareModal/qrCodeView.tsx`
- `src/components/templates/main-app/projects/b2cView/shareModal/linkView.tsx`
- `src/components/templates/main-app/projects/b2cView/shareModal/socialShareView.tsx`
- `src/components/templates/main-app/menu-card-export/MenuCardExportRoute.tsx`
- `src/components/mobile/screens/MobileShareScreen.tsx`
- `src/hooks/useMenuCardExportController.ts`
- `src/lib/menu-kit/*`
- `src/lib/menu-card-export/*`
- `src/lib/printable-asset-templates/*`
- `src/lib/print-menu-surfaces/*`
- `src/lib/physical-surfaces/*`
- `src/app/api/menu-card-export/design-advisor/route.ts`
- `src/config/features.ts`
- `__docs__/use-menulist/*`
- `__docs__/menu-kit/*`
- `__docs__/menu-card-export/*`
- `__docs__/printable-asset-templates/*`

Affected surfaces:

- owner dashboard: yes
- mobile owner flow: yes
- public menu/customer page: yes, through generated QR/link destinations
- Official Business Page: yes, through OBP QR/link destinations
- website/marketing claim: indirect, via print-ready-kit claims already checked in Audit 15
- admin/internal operations: no direct admin UI changes
- billing/entitlement: yes, MenuList attribution and AI advisor plan gates
- analytics/feedback/reviews: yes, UTM/source-attributed QR and feedback QR links
- multi-location behavior: yes, master-owner outlet QR generation
- cache/public truth: yes, generated assets must point at current public truth; no cache writes are performed here

### B. End-to-End Request/Data Flow

Use MenuList desktop flow:

1. Owner opens `/use-menulist`.
2. The page reads existing platform store/tenant context from `PlatformGlobalDataContext`.
3. It reads project summaries with `getExistingProjectsListWithoutLoader(true)` and shows `no_menu` without creating default truth when no project exists.
4. It builds OBP, project slug, feedback, install-app, store-menu alias, and outlet alias URLs from existing store/project data.
5. It reads `getScreenState()` once to expose Menu Board and Highlights links when configured.
6. Copy/open/share actions add entry-source attribution where appropriate and use clipboard/window open only.
7. QR/Menu Kit/print asset actions generate files in the browser only after owner action.
8. PDF fallback actions read the full selected project only on tap, then call the Menu Card Export renderer bridge.
9. Print Assets route reads full project data only for `print_menu` previews/downloads and caches it in the route session.
10. Menu Card Export reads summary data, one selected full project, then renders preview/preflight/PDF/packet in the browser.
11. Optional Menu Card Export AI advisor is a separate authenticated POST route with safe mode, AI operation rate limiting, tenant verification, bounded JSON parsing, plan gate, capacity check, provider call, response normalization, and accounting after valid output.

Mobile flow:

1. Mobile Share stays inside `MobileShell` and uses `MobileProjectsProvider` project state.
2. Mobile builds the same OBP, store-menu alias, project slug, feedback, screen, install-app, and outlet QR links.
3. Mobile QR sheets, Menu Kit downloads/shares, Print Assets, Print Menu, JSON/XLSX export, screen links, POS setup copy, and guide sheets reuse shared primitives.
4. Full project data is loaded only when PDF/export/print-menu actions need item/category data.

Public/customer-visible result:

- Store Menu QR and outlet QR intentionally use the stable `/menu` alias for reprint-safe physical placements.
- Project Menu QR and direct share links use the canonical project slug URL.
- OBP QR uses the official business page root.
- Feedback QR uses the feedback source URL for the selected project.

### C. Correctness Findings

1. **Fixed: project Share modal could emit the prohibited `/menu/{slug}` fallback.**  
   `src/components/templates/main-app/projects/b2cView/shareModal/index.tsx` bypassed `generateProjectUrl()` when `subdomain` and `customDomain` were missing and generated `${origin}/menu/${slug}`. The shared URL doctrine explicitly forbids `/menu/{slug}`, and this could leak into QR downloads, social links, Menu Kit, and legacy PDF output from a partially initialized store. The modal now requires a store public address for customer-facing share assets and shows a clear not-ready state instead of generating an invalid public URL.

2. **Verified: Use MenuList desktop and mobile distinguish stable alias QRs from canonical project links.**  
   Physical Store Menu QR/outlet QR use `/menu` aliases intentionally for reprint safety. Project-specific links and QR assets use `generateProjectUrl()` and the real slug.

3. **Verified: read-only output hubs do not create project truth.**  
   Desktop Use MenuList and Menu Card Export use `getExistingProjectsListWithoutLoader(true)` and show empty states instead of auto-creating projects.

4. **Verified: object URL cleanup exists on heavier preview paths.**  
   Printable Asset Templates revokes preview URLs on close/unmount. Mobile Print Assets revokes the active preview URL when it changes or closes. Menu Kit immediate downloads revoke Blob URLs after click.

5. **Verified: Menu Card Export AI advisor is bounded and metered.**  
   The API route uses auth, tenant access verification, safe mode, operation rate limiting, a 128KB JSON body cap, Pro/Premium plan gate, capacity check, bounded Zod input, JSON normalization, and accounting after valid recommendation output.

### D. Firebase Cost Audit

Desktop Use MenuList page load:

- Store context: 0 additional reads from the hub; inherited provider context.
- Project summaries: 1 summary-doc read when not already cached.
- Screen state: 1 summary-doc read.
- Full selected project: 0 on page load; 0-1 on PDF/export/print-menu action.
- Writes: 0 on load; 0-1 existing starter activation signal write only when policy allows and only once per signal.

Mobile Share:

- Project state: usually inherited from `MobileProjectsProvider`.
- Screen state: 1 summary-doc read.
- Full selected project: 0-1 only when PDF/export/print-menu action needs full data.
- Writes: same starter-activation signal contract; local downloads/shares do not write Firebase.

Menu Kit / QR / Print Assets:

- QR and Menu Kit generation: 0 Firestore reads, 0 Firestore writes, 0 Storage writes, 0 Cloud Functions.
- Printable non-menu assets: use already-loaded summary/store context and browser generation.
- Printable `print_menu`: 0-1 full project read per selected project in that route session; cached for repeated template actions.
- Menu Card Export: one selected full project read after summary selection; browser PDF/ZIP/local history only.
- Menu Card Export local history: `localStorage`, no Firestore.
- AI advisor: separate owner-click server route; subscription/capacity/accounting reads/writes are AI billing-owned and only occur after plan/capacity gates.

Cost impact of fix:

- No Firebase cost increase. The Share modal now blocks invalid customer-facing asset generation when the store public address is missing.

### E. UI/UX Audit

- Owner output actions are grouped around Share, QR, Screens, Print, Menu Kit, and Resources without adding design-editor clutter.
- Desktop Print Assets gives a focused route for choosing asset type and finished style; Use MenuList remains the hub.
- Mobile Share covers the practical owner jobs without requiring a laptop: copy/open/share links, QR sheets, Menu Kit, individual assets, Print Assets, Print Menu, exports, screen links, feedback QR, POS setup copy, and guides.
- Missing public address state in the project Share modal now communicates "Public link is not ready" instead of exposing an invalid URL.
- Customer-facing generated outputs remain scan-first, non-admin-like, and tied to current public menu/OBP truth.

### F. Product/Positioning Audit

The audited slice supports MenuList's public-business truth infrastructure role:

- Generated files are deployment surfaces for the approved business/menu truth, not a generic design workspace.
- Menu Kit stays a launch/deployment pack; Menu Card Export owns full print-menu workflows.
- Public QR destinations are stable and purposeful: alias for reprint-safe placements, canonical slug for project-specific links, OBP root for business identity.
- No new owner settings, arbitrary template editing, Storage artifact system, or extra dashboard complexity was added.

### G. Documentation Alignment

Updated docs:

- `__docs__/use-menulist/use-menulist_impl.md`
- `__docs__/audits/menulist-production-readiness-audit.md`

Verified docs remain aligned:

- `__docs__/use-menulist/use-menulist_firebase.md`
- `__docs__/use-menulist/use-menulist_mobile-support.md`
- `__docs__/menu-kit/menu-kit_firebase.md`
- `__docs__/menu-card-export/menu-card-export_firebase.md`
- `__docs__/printable-asset-templates/printable-asset-templates_firebase.md`

### H. Fixes Made

Runtime files changed in this slice:

- `src/components/templates/main-app/projects/b2cView/shareModal/index.tsx`

Documentation files changed in this slice:

- `__docs__/use-menulist/use-menulist_impl.md`
- `__docs__/audits/menulist-production-readiness-audit.md`

### I. Validation Performed

- `npx tsc --noEmit --incremental false --pretty false` — passed.
- `npm run lint` — passed.
- `git diff --check -- src/components/templates/main-app/projects/b2cView/shareModal/index.tsx` — passed.
- `npm run verify:menu-card-export` — passed.

Pending for this slice:

- Authenticated browser smoke of the project Share modal, Use MenuList, Print Assets, and Menu Card Export was not run in this checkpoint.
- Physical scan testing with printed output was not run in this environment.
- `npm run build` remains global-final validation and has not been run for the full certification.

---

## Completed Audit 17: POS Sync, GBP/External Discovery, Hours/Temporary Status, and Pricing Integrity

### A. Feature Identification

Feature cluster:

- POS Webhook Sync / External Menu Sync
- Google Business Profile sync and pre-API discovery guidance
- Hours & Holiday Accuracy plus temporary public status
- Pricing Integrity and shared price display formatting

Routes, components, hooks, DAL/functions, collections, flags, cache paths, and docs inspected:

- POS routes: `src/app/api/pos-sync/deliver/route.ts`, `src/app/api/pos-sync/test/route.ts`
- POS UI/mobile: `src/components/templates/main-app/businessSettings/tabs/PosSyncTab.tsx`, `src/components/mobile/screens/MobilePosSyncScreen.tsx`
- POS libs: `src/lib/posSync/eventBuilder.ts`, `src/lib/posSync/payloadFormatter.ts`, `src/lib/posSync/signature.ts`, `src/lib/posSync/secretAudit.ts`, `src/lib/posSync/types.ts`, `src/lib/posSync/webhookUrl.ts`
- GBP/discovery UI: `src/components/templates/main-app/businessSettings/tabs/IntegrationsTab.tsx`, `src/components/mobile/screens/MobileIntegrationsScreen.tsx`, `src/components/templates/main-app/businessSettings/tabs/GoogleListingGuide.tsx`, `src/components/templates/main-app/dashboard/OwnerDashboard/GoogleListingCard.tsx`, `src/components/templates/main-app/useMenuList/PresenceMonitor.tsx`
- Hours/temp status: `src/lib/hours/hoursEngine.ts`, `src/lib/obp/hoursStatus.ts`, `src/lib/outputControl/hoursConfidence.ts`, `src/components/atoms/StoreStatusBadge/index.tsx`, `src/components/atoms/TrustSignals.tsx`, `src/components/templates/main-app/businessSettings/tabs/WorkingHoursTab.tsx`, `src/components/mobile/screens/MobileHoursScreen.tsx`, `src/components/mobile/screens/MobileWorkingHoursEditScreen.tsx`, `src/app/api/store/temp-status/route.ts`
- Pricing: `src/lib/pricing/formatMenuPrice.ts`, `src/lib/pricing/integrityEngine.ts`, `src/lib/pricing/molLogger.ts`, `src/lib/pricing/pdfQueue.ts`, `src/lib/validation/pricing.schema.ts`, `src/components/templates/main-app/projects/editorView/CommandCenterModal/actions/PricingAction.tsx`
- Public rendering: `src/components/templates/main-app/projects/b2cView/menuPage/menuPageNew.tsx`, OBP hours consumers, public temp-status banner path
- Feature flags: `ENABLE_POS_SYNC: true`, `ENABLE_GBP_SYNC: false`, `ENABLE_HOURS_STATUS_DISPLAY: true`, `ENABLE_OUTPUT_CONTROL: false`, `ENABLE_TEMP_STATUS: true`
- Firebase collections: `stores`, `stores/{storeId}/posDeliveryLogs`, `projects/{tId}/{sId}/{projectId}`, `menuChangeLog/{tId}/{sId}`, future/unused `posDeliveryQueue`
- Docs: `__docs__/pos-webhook-sync/*`, `__docs__/gbp-sync/*`, `__docs__/hours-holiday-accuracy/*`, `__docs__/pricing-integrity-system/*`

Affected surfaces:

- owner dashboard: yes, Business Settings, Today/hours, dashboard Google listing card
- mobile owner flow: yes, POS setup, Integrations, Hours, temporary status, Share POS setup copy
- public menu/customer page: yes, hours/trust signal, temp status, price rendering
- Official Business Page: yes, hours output and Google listing guidance destination
- website/marketing claim: yes, POS/GBP/pricing docs and public claim boundaries
- admin/internal operations: no new admin UI; server route hardening only
- billing/entitlement: indirect, POS flag and AI/billing untouched
- analytics/feedback/reviews: no direct analytics changes
- multi-location behavior: yes, POS is store-level and GBP/hours are store/outlet-level
- cache/public truth: yes, temp status and hours affect public truth; existing store update and temp-status invalidation paths inspected

### B. End-to-End Request/Data Flow

POS delivery flow:

1. Owner edits menu in the editor.
2. `triggerPosSyncDebounced()` waits 25 seconds and calls `POST /api/pos-sync/deliver` only when POS sync is enabled and a webhook URL exists.
3. Route enforces `withAuth()`, `ENABLE_POS_SYNC`, `MANAGE_INTEGRATIONS` or `PUBLISH_MENU`, an 8KB body cap before schema validation, tenant/store access, and rate limit.
4. Server reads `stores/{storeId}` via Admin SDK and validates `posSync.enabled`, webhook URL, and secret.
5. Server validates the webhook as a public HTTPS endpoint before any outbound fetch.
6. Server reads the project only from `projects/{tenantId}/{storeId}/{projectId}`; no client DAL or legacy/global fallback is used.
7. Server transaction increments `posSync.menuVersion`.
8. `buildMenuSnapshot()` builds the signed full snapshot from approved project-level extracted data, falling back to file-level extraction data only when top-level data is absent.
9. Server signs payload, POSTs to the webhook with a 5s timeout, writes one delivery log, trims logs to the last 20, and updates POS status/last error.
10. UI never blocks menu saving; owners see status/history in the POS settings panel.

POS test/setup flow:

1. Owner saves a public HTTPS provider URL from desktop or mobile.
2. Desktop/mobile use the same URL validator as server routes before saving.
3. Test route requires `MANAGE_INTEGRATIONS`, validates tenant/store, validates enabled POS config, signs a test payload, and fetches the validated endpoint.
4. Success marks status `healthy`; HTTP failures/timeouts/invalid stored URLs mark `connection_issue` with `lastError`.
5. "Send instructions" opens an owner email draft with the technical setup summary and tracks daily handoff count; MenuList does not send server-side email.

GBP/external discovery flow:

1. `ENABLE_GBP_SYNC` is false, so automated GBP OAuth/API sync UI is hidden or unavailable.
2. Desktop/manual bridge cards guide owners to copy the OBP URL into Google Business Profile.
3. Mobile integrations screen shows not available while the flag is off.
4. No GBP API routes, token storage, or sync functions run in current runtime.

Hours/temp status flow:

1. Desktop/mobile owners update working hours through existing store update paths.
2. Hour writes now stamp `hoursLastUpdatedAt` in the same `stores/{storeId}` write.
3. Public menu hours rendering uses existing loaded store data; output-control confidence remains flag-off, but the public menu now passes a freshness timestamp for future flag-on correctness.
4. Temporary status API requires auth, tenant/store session, `MANAGE_STORE` or `MANAGE_PUBLIC_PRESENCE`, applies the `DATA_WRITE` limiter, rejects bodies above 4KB before validation, validates payload/expiry, writes `tempStatus`, revalidates `menu-store-{storeId}`, `store-{storeId}`, and `client-stores`, and invalidates Owner Business Assistant packet cache.

Pricing flow:

1. Owner edits item/attribute prices in existing editor flows.
2. Public/menu/print/screen surfaces read the same project truth or generated output from that truth.
3. Shared `formatMenuPrice()` now preserves text prices and renders numeric ranges as ranges instead of coercing them to zero.

### C. Correctness Findings

1. **Fixed: POS delivery route used a client DAL reader from a server route.**  
   The old route imported `getProjectData(projectId)`, which depends on client/session context and includes a legacy fallback outside the request's tenant/store path. POS delivery now reads only `projects/{tenantId}/{storeId}/{projectId}` via Admin SDK and rejects missing/deleted projects.

2. **Fixed: POS routes could fetch owner-supplied internal/private URLs.**  
   Desktop/mobile save, test route, and delivery route now require a public HTTPS endpoint and reject localhost, `.local`, private IPv4, link-local, loopback, multicast, and private/link-local IPv6 hostnames before outbound fetch. Server-side test and delivery also resolve the validated hostname and reject DNS targets that resolve to blocked local/private addresses before project reads, version increments, payload construction, or outbound delivery.

3. **Fixed: POS snapshot could be empty for approved projects.**  
   `buildMenuSnapshot()` only read `project.files[].extractedData.data`. Projects whose approved menu lives on top-level `project.extractedData` could send empty payloads. The formatter now uses top-level extracted data first and falls back to file extraction data.

4. **Fixed: POS test failures did not persist status.**  
   Test failures and timeouts now mark `posSync.status = connection_issue`, `lastStatus = failed`, and `lastError`, so desktop/mobile status matches the actual connection result.

5. **Fixed: POS "Send instructions" UI implied a backend email that did not exist.**  
   The action now validates the provider email, opens an owner email draft with the technical summary, and tracks handoff count. Docs now say email draft, not server-side email.

6. **Verified: GBP sync remains blocked/flag-off.**  
   Runtime does not expose OAuth/API sync when `ENABLE_GBP_SYNC` is false. Manual Google listing guidance remains the current shipped behavior.

7. **Fixed: temporary public status mutation lacked explicit permission enforcement.**  
   `/api/store/temp-status` was authenticated and tenant-scoped but did not require a role permission before changing customer-visible open/closed messaging. It now requires `MANAGE_STORE` or `MANAGE_PUBLIC_PRESENCE`.

8. **Fixed: hours freshness had no stable stamp on owner hour edits.**  
   Desktop and mobile hour saves now write `hoursLastUpdatedAt` in the same store update. The public menu trust signal receives this timestamp for output-control correctness when that flag is enabled.

9. **Fixed: range prices could render as `₹0`.**  
   `formatMenuPrice('199-249')` and similar range strings now render as a range; text prices like `Market Price` are preserved.

10. **Fixed: POS command routes parsed JSON without route-level body caps.**
   POS test and delivery now reject bodies above 8KB before request validation, store/project reads, outbound webhook fetches, log writes, or status updates.

### D. Firebase Cost Audit

POS page/settings load:

- Store POS config: inherited from loaded store context on settings page.
- Delivery logs: one client query, `limit(20)`, only when POS sync is enabled and the POS tab loads.
- Secret rotation audit: one append-only MOL write only on rare owner rotation.

POS delivery:

- Reads: 1 store doc, 1 scoped project doc, transaction read of store doc for version increment.
- Writes: 1 version update in transaction, 1 delivery log write, 1 status update; plus bounded cleanup deletes after logs exceed 20.
- Outbound network: one 5-second-limited fetch per debounced delivery.
- Cost impact of fixes: no additional document reads beyond replacing the unsafe client DAL project read with the correct scoped Admin SDK read; validation and 8KB body admission are CPU-only.

POS test:

- Reads: 1 store doc.
- Writes: now 1 status update on success or failure. This is a deliberate correctness write; tests are owner-click, rate-limited, and low-frequency.

GBP:

- Runtime flag-off. No GBP token/doc reads, API calls, or Cloud Functions in current behavior.

Hours/temp status:

- Hours update: same single `stores/{storeId}` write now includes `hoursLastUpdatedAt`; no extra write/read.
- Public hours render: 0 incremental reads because hours are part of loaded store data.
- Temp status set/clear: one store write plus existing cache invalidation; permission check adds one store read through the existing permission helper. Rejected oversized or rate-limited requests do not reach the store write.

Pricing:

- `formatMenuPrice()` fix is local render logic with 0 Firebase cost.
- Background PDF regeneration remains disabled in runtime; no Cloud Function or queue cost from this audit.

### E. UI/UX Audit

- POS setup remains quiet when healthy and visible when broken.
- Desktop and mobile now reject unsafe/non-public webhook URLs at save time with clear owner-facing messages.
- Test failures now persist a visible connection state instead of disappearing after a transient toast.
- Provider setup action now opens a real email draft instead of silently incrementing a counter.
- GBP sync does not appear as a working automation while the API integration is blocked.
- Mobile hours and desktop hours both support owner edits; updated freshness supports future confidence states.
- Temporary status remains a simple customer-facing notice and now has a server permission boundary matching its public impact.
- Price ranges and text prices stay readable on narrow/customer surfaces because the formatter no longer coerces them into numeric zero.

### F. Product/Positioning Audit

This cluster supports MenuList as public-business truth infrastructure:

- POS sync broadcasts approved MenuList truth; it does not let external systems write back.
- GBP is positioned as manual public link alignment until real API access exists.
- Hours and temp status stay low-burden: one owner action changes customer-facing truth and invalidates public cache.
- Pricing fix protects trust without adding pricing automation or owner decision clutter.

Rejected/avoided:

- No speculative POS retry queue or Cloud Function worker added.
- No automated GBP claims exposed while `ENABLE_GBP_SYNC` is false.
- No new owner toggles for URL validation, hours freshness, or price formatting.

### G. Documentation Alignment

Updated docs:

- `__docs__/pos-webhook-sync/pos-webhook-sync_impl.md`
- `__docs__/pos-webhook-sync/pos-webhook-sync_firebase.md`
- `__docs__/pos-webhook-sync/pos-webhook-sync_helpdoc.md`
- `__docs__/pos-webhook-sync/pos-webhook-sync_marketing.md`
- `__docs__/pos-webhook-sync/pos-webhook-sync_website.md`
- `__docs__/hours-holiday-accuracy/hours-holiday-accuracy_firebase.md`
- `__docs__/pricing-integrity-system/pricing-integrity-system_spec.md`
- `__docs__/pricing-integrity-system/pricing-integrity-system_firebase.md`
- `__docs__/pricing-integrity-system/README.md`
- `__docs__/audits/menulist-production-readiness-audit.md`

Docs corrected after runtime verification:

- POS project read is Admin SDK and tenant/store-scoped.
- POS webhook endpoints must be public HTTPS URLs.
- POS setup uses an owner email draft, not server-side email.
- POS delivery remains single-attempt; retry worker/queue remains deferred.
- Hours writes include a freshness stamp in the same store write.
- Range/text price display is verified in runtime formatter.

### H. Fixes Made

Runtime files changed in this slice:

- `src/lib/posSync/webhookUrl.ts`
- `src/app/api/pos-sync/deliver/route.ts`
- `src/app/api/pos-sync/test/route.ts`
- `src/lib/posSync/payloadFormatter.ts`
- `src/components/templates/main-app/businessSettings/tabs/PosSyncTab.tsx`
- `src/components/mobile/screens/MobilePosSyncScreen.tsx`
- `src/app/api/store/temp-status/route.ts`
- `src/components/templates/main-app/businessSettings/index.tsx`
- `src/components/mobile/screens/MobileWorkingHoursEditScreen.tsx`
- `src/components/mobile/screens/MobileHoursScreen.tsx`
- `src/components/templates/main-app/projects/b2cView/menuPage/menuPageNew.tsx`
- `src/lib/pricing/formatMenuPrice.ts`

Documentation files changed in this slice are listed in §G.

### I. Validation Performed

- POS Sync boundary source gate: `npm run verify:pos-sync-boundary` — passed; source-only public-HTTPS/DNS/auth/tenant/mobile-shell gate.
- `npm run verify:production-readiness-local` — passed; 45/45 checks, including 41 child root `verify:*` scripts.
- `npx tsc --noEmit --incremental false --pretty false` — passed.
- `npm run lint` — passed.
- `git diff --check -- [Audit 17 touched runtime files]` — passed.
- `npx tsx -e "...formatMenuPrice(...)"` — passed:
  - `199-249` -> `₹199-249`
  - `₹199 - ₹249` -> `₹199-249`
  - `Market Price` -> `Market Price`
  - `299` -> `₹299`

Pending for this slice:

- Authenticated browser smoke for POS desktop/mobile settings was not run.
- Real external webhook provider smoke remains pending.
- DNS-resolution SSRF protection is implemented for POS test and delivery routes; browser smoke with a real external webhook provider remains pending.
- Physical/public customer rendering was not browser-smoked after the formatter fix.
- No Vercel deploy was run.

---

## Completed Audit 18: Staff Prompt and Today Staff-Facing Operational Output

### A. Feature Identification

Feature cluster:

- Staff Prompt read-only Today summary display
- Today desktop and mobile maintenance-card rendering
- Weekly Growth Pack staff-line copy

Routes, components, hooks, DAL/functions, collections, flags, cache paths, and docs inspected:

- `src/database/campaigns/index.ts`
- `src/hooks/useTodayCampaigns.ts`
- `src/components/templates/main-app/today/hooks/useTodayCampaigns.ts`
- `src/components/templates/main-app/today/index.tsx`
- `src/components/templates/main-app/today/components/StaffPromptSection/index.tsx`
- `src/components/mobile/screens/MobileHoursScreen.tsx`
- `src/lib/today/weeklyGrowthPack.ts`
- `src/lib/campaigns/todayActionExecutor.ts`
- `src/types/campaigns.ts`
- `__docs__/staff-prompt/*`

Affected surfaces:

- owner dashboard: yes, desktop Today
- mobile owner flow: yes, MobileShell Today/Hours
- public menu/customer page: no direct render
- Official Business Page: no
- website/marketing claim: docs only
- admin/internal operations: no new admin/internal writer in this slice; Staff Prompt remains a read-only consumer of `platformSummary/campaigns_{sId}`
- billing/entitlement: no
- analytics/feedback/reviews: indirect, summary may originate from analytics/campaign systems
- multi-location behavior: store-scoped summary doc per `sId`
- cache/public truth: no public cache writes; SWR Today summary cache only

### B. End-to-End Request/Data Flow

Runtime flow:

1. Desktop Today and mobile Today/Hours call shared `useTodayCampaigns()`.
2. Hook uses SWR key `today-campaigns`, `getTodayCampaigns()`, revalidate-on-focus/reconnect, and 30-second dedupe.
3. DAL reads one document: `platformSummary/campaigns_{sId}` for the active session.
4. If the summary date is not today, DAL returns an empty Today state and no staff prompt.
5. If the summary is current, DAL returns `today`, `staffPrompt`, and `physicalSurfaces`.
6. Desktop renders `StaffPromptSection` only when `staffPrompt.eligible` is true.
7. Mobile renders the same staff line card only when `staffPrompt.eligible` is true.
8. Weekly Growth Pack now receives staff prompt text only when the prompt is eligible.

### C. Correctness Findings

1. **Fixed: desktop Today hid eligible staff prompts on otherwise-empty days.**  
   The desktop state machine treated `today.isEmpty` as an empty screen before considering eligible staff prompts or physical surface maintenance cards. Mobile already rendered the card. Desktop now treats eligible maintenance cards, including Staff Prompt, as Today content.

2. **Fixed: Weekly Growth Pack could consume ineligible staff prompt text.**  
   Desktop and mobile now pass `staffPrompt.text` into weekly pack generation only when `staffPrompt.eligible` is true.

3. **Verified: Staff Prompt active runtime is read-only summary display.**  
   There is no separate staff-facing portal, no AI staff assistant, no owner settings, and no runtime helper engine in `src/lib/staff-prompt/`. Active code reads summary data only.

4. **Verified: stale summary protection exists.**  
   `getTodayCampaigns()` checks `data.today.date` against current local ISO date and suppresses staff prompt when the summary is stale.

### D. Firebase Cost Audit

- Reads: one `platformSummary/campaigns_{sId}` read per Today open/SWR cache miss, shared with campaigns and physical surfaces.
- Writes: none from Staff Prompt UI.
- Listeners: none; SWR revalidates on focus/reconnect with 30-second dedupe.
- Client fanout: none.
- Cost impact of fixes: no new Firestore reads/writes.

### E. UI/UX Audit

- Desktop and mobile now have parity for eligible Staff Prompt display.
- The component remains read-only with no owner decisions or settings.
- Empty-state behavior is clearer: if a staff line is available, Today is not empty.
- The prompt text appears as a single line owners can share verbally; it is not presented as a chat/training tool.

### F. Product/Positioning Audit

The active Staff Prompt behavior supports MenuList's infrastructure posture only as a quiet operational hint. It does not introduce a generic AI assistant, staff training product, or extra workflow. Documentation was corrected to prevent that over-positioning.

### G. Documentation Alignment

Updated docs:

- `__docs__/staff-prompt/README.md`
- `__docs__/staff-prompt/staff-prompt_mobile-support.md`
- `__docs__/staff-prompt/staff-prompt_firebase.md`
- `__docs__/staff-prompt/staff-prompt_helpdoc.md`
- `__docs__/staff-prompt/staff-prompt_spec.md`
- `__docs__/staff-prompt/staff-prompt_impl.md`
- `__docs__/staff-prompt/staff-prompt_validation.md`
- `__docs__/staff-prompt/staff-prompt_code-review.md`
- `__docs__/staff-prompt/staff-prompt_logic-verification.md`
- `__docs__/audits/menulist-production-readiness-audit.md`

Corrections made:

- Removed active-doc claims that Staff Prompt is a separate AI-powered staff training portal.
- Updated mobile support to reflect MobileShell parity.
- Updated Firebase docs to the one-summary-read runtime.
- Added runtime notes to older historical verification docs that still reference removed helper files.

### H. Fixes Made

Runtime files changed in this slice:

- `src/components/templates/main-app/today/index.tsx`
- `src/components/mobile/screens/MobileHoursScreen.tsx`

Documentation files changed in this slice are listed in §G.

### I. Validation Performed

- `npx tsc --noEmit --incremental false --pretty false` — passed.
- `npm run lint` — passed.
- `git diff --check -- [Audit 18 touched files]` — passed.

Pending for this slice:

- Authenticated browser smoke for desktop Today and mobile Today/Hours was not run.
- Upstream scheduler/generation logic for writing `staffPrompt` into the summary is outside this Staff Prompt display slice; current production boundary is the verified read-only consumer path, guarded by `npm run verify:staff-prompt-runtime`.

---

## Completed Audit 19: Safe Mode, Ops Controls, Platform Monitoring, Internal Admin Routes, and Security-Adjacent System Hardening

### A. Feature Identification

**Feature cluster:** Operational controls, safe mode, platform/internal monitors, platform entity blocks, admin subdomain rename, reseller management protection, and security-adjacent internal routes.

Relevant routes, components, DAL, and docs inspected:

- `src/app/api/ops/safe-mode/route.ts`
- `src/app/api/ops/mute-alerts/route.ts`
- `src/app/api/ops/platform-notifications/route.ts`
- `src/app/api/ops/owner-notifications/route.ts`
- `src/app/api/ops/messaging-onboarding/route.ts`
- `src/app/api/admin/subdomains/rename/route.ts`
- `src/app/api/platform/entity-blocks/route.ts`
- `src/app/api/platform/owner-business-assistant/monitor/route.ts`
- `src/app/api/platform/answerlattice-intake/route.ts` (separation check only)
- `src/lib/ops/safeMode.ts`
- `src/lib/ops/alerts.ts`
- `src/database/ops/index.ts`
- `src/database/ops/scheduler.ts`
- `src/database/ops/extraction.ts`
- `src/components/templates/main-app/platform/opsControlRoom/index.tsx`
- `src/components/mobile/screens/MobileOpsControlRoomScreen.tsx`
- `src/components/templates/main-app/platform/*Monitor/index.tsx`
- `src/components/templates/main-app/reseller/ResellerManagement.tsx`
- `src/components/mobile/screens/MobileResellerManagementScreen.tsx`
- `src/constants/user.ts`
- `src/config/features.ts`
- `__docs__/ops-control-room/ops-control-room_impl.md`
- `__docs__/reseller-dashboard/*`
- `__docs__/owner-notifications/owner-notifications_impl.md`
- `__docs__/messaging-onboarding/README.md`
- `__docs__/url-routing-architecture/README.md`

Affected surfaces:

- Owner dashboard: yes, indirectly through ops recovery and cache/public truth repair.
- Mobile owner flow: yes, mobile Ops Control Room and mobile reseller management.
- Public menu/customer page: yes, admin subdomain rename and platform entity blocking affect public routing/cache.
- Official Business Page: yes, store/subdomain cache invalidation and entity blocking.
- Website/marketing claim: no direct copy change.
- Admin/internal operations: yes.
- Billing/entitlement: yes, reseller management protection.
- Analytics/feedback/reviews: yes, internal monitors only.
- Multi-location behavior: yes, force-republish and store selectors read platform summaries.
- Cache/public truth: yes.

### B. End-to-End Request/Data Flow

Safe mode:

1. Platform operator opens desktop/mobile Ops Control Room.
2. UI waits for `platformRole === 'PLATFORM'` before client Firestore reads.
3. Operator toggles SAFE_MODE through `/api/ops/safe-mode`.
4. API rate-limits per operator, rejects bodies above 2KB before validation, writes `ops_config/system`, writes a classified `systemAlerts` alert, and returns `{ success, SAFE_MODE }`.
5. Expensive routes using `checkSafeMode()` read `ops_config/system` and return 503 when active; public menu rendering remains unaffected.

Platform monitors:

1. Platform operator opens a monitor route.
2. UI checks feature flag and platform role.
3. API routes enforce `withAuth()` / `withPlatformAuth()`, validate query/action payloads, and use bounded Admin SDK reads.
4. Recovery actions are operator-rate-limited, reject oversized JSON before parsing, and write only the relevant alert/event/delivery state.

Admin subdomain rename:

1. Platform operator posts tenant/store/new-subdomain/reason/ackRef.
2. API validates shape, reserved slugs, direct collisions, and active rename-chain collisions.
3. Transaction updates the store, storesSummary subdomain, and audit log.
4. Server revalidates `menu-store-{storeId}`, `store-{storeId}`, and `client-stores`, then clears the Business Health packet cache for the store.

Reseller management:

1. Page/mobile screen permits only `platformRole === 'PLATFORM'`.
2. Runtime loads reseller profiles and monthly summary through existing platform-only APIs.
3. No client-bundled password or static secret is used.

### C. Correctness Checks

Issues found and fixed:

1. **Fixed: client-bundled platform password existed in source and browser bundles.**
   `ECOMSAI_PLATFORM_PASSWORD` exposed a literal platform password from `src/constants/user.ts`; desktop/mobile reseller management compared it in client code. The constant and password gates were removed. The route now relies on page-level platform checks and platform-only APIs.

2. **Fixed: internal monitor APIs served data even when their UI feature flag was disabled.**
   Platform notifications, owner notification ops, messaging onboarding ops, and entity blocks now return `404` before Firestore reads/writes when their governing flags are off.

3. **Fixed: desktop Ops Control Room could start client Firestore reads before platform session confirmation.**
   The desktop effect now waits for session resolution and confirmed platform role before loading ops state.

4. **Fixed: bad or oversized JSON on ops/admin mutations could fall into expensive handling.**
   Mute alerts, SAFE_MODE, Platform Notifications actions, Owner Notifications recovery actions, entity blocks, and admin subdomain rename now treat malformed JSON as invalid input. Ops actions also run the operator limiter before bounded JSON parsing. Entity blocks and admin subdomain rename use bounded JSON admission before entity/store reads or public-truth writes.

5. **Fixed: admin subdomain rename missed public cache invalidation and denormalized summary sync.**
   Rename now writes the new subdomain into `platformSummary/storesSummary`, revalidates public menu/store/client-store cache tags, and clears the Business Health packet cache.

6. **Fixed: messaging onboarding ops health lookup required a missing Firestore document-id index.**
   The platform monitor queried `systemHealth` by document-id prefix and descending `__name__`, which failed for an authenticated platform owner in the built-server smoke. The API now reads the writer-owned `messaging_onboarding_control` document and then the referenced `lastSnapshotId`, avoiding the index requirement and bounding the latest-health lookup to one or two direct document reads.

7. **Fixed: Ops Control Room header actions overflowed horizontally in Chrome.**
   Authenticated Chrome visual QA at a 1512px desktop viewport showed the monitor links extending past the right edge. The header action group now wraps inside the content column instead of forcing page-level horizontal overflow.

8. **Fixed: Platform Notifications message cells collapsed into narrow wrapped columns.**
   Chrome visual QA showed alert metadata wrapping into an unreadable vertical strip. The Message column now has an explicit width and the table uses internal horizontal scroll, preserving page width while keeping operational alert details readable.

Separation checks:

- Answerlattice intake monitor remains behind its own feature flag and Answerlattice Firebase admin client.
- This audit did not change Answerlattice collections, functions, or behavior.

### D. Firebase Cost Audit

- Ops Control Room: client reads now occur only after confirmed platform role. Manual refresh only, no listener.
- SAFE_MODE toggle: one `ops_config/system` write plus one `systemAlerts` write after the operator limiter and 2KB body cap; delivery path may read `ops_config/system` once for mute status.
- Mute alerts: one `ops_config/system` write after the operator limiter and 1KB body cap.
- Platform notifications: bounded recent alert scan, five count aggregations, optional one detail read; no listener; zero reads when dashboard flag is off.
- Platform notification actions: acknowledge/manual handoff/manual alert writes happen only after the operator limiter and 8KB body cap.
- Owner notifications: bounded event scan, six count aggregations, optional delivery/scope reads on selected detail; action writes only after the operator limiter and 8KB body cap on retry/manual-send/manual-handoff; zero reads when master/dashboard flag is off.
- Messaging onboarding monitor: bounded Admin SDK counts and recent rows; latest health is one control-doc read plus one snapshot-doc read when present; zero reads when dashboard flag is off.
- Entity blocks: writes only on platform action after 64KB bounded JSON admission; tenant block reads `storesSummary`, queries tenant stores, and writes `tenantBlocked` to affected store docs plus the summary. Cache revalidation adds no Firestore write.
- Admin subdomain rename: adds one `platformSummary/storesSummary` merge write and cache invalidation after 8KB bounded JSON admission and the existing store/audit transaction. This improves correctness with negligible platform-only write cost.
- Removing the client password gate does not add Firebase cost; profile/monthly reads happen once on confirmed platform mount, matching the previous post-password flow.
- Chrome visual layout fixes add zero Firebase reads, writes, listeners, routes, or indexes.

### E. UI/UX Audit

- Desktop Ops Control Room and mobile Ops Control Room both keep safe-mode/mute/republish controls platform-only.
- Mobile Ops Control Room already waits for session state and shows a platform-only empty state for non-platform users.
- Reseller management desktop/mobile now removes an extra fake password step and shows the actual platform-admin experience directly after platform session confirmation.
- Ops Control Room desktop actions now wrap instead of creating horizontal page overflow.
- Platform Notifications keeps long alert messages readable through table-level horizontal scroll rather than page-level overflow or narrow text collapse.
- Public customer pages are unaffected except they now refresh promptly after platform subdomain rename.

### F. Product/Positioning Audit

The fixes preserve MenuList as public-business truth infrastructure:

- No owner-facing ops complexity was added.
- Public routing repairs stay platform-only and audited.
- Monitoring surfaces remain internal and feature-flagged.
- No generic QR/menu-tool behavior or AI dashboard clutter was added.

### G. Documentation Alignment

Docs updated:

- `__docs__/ops-control-room/ops-control-room_impl.md`
- `__docs__/reseller-dashboard/reseller-dashboard_impl.md`
- `__docs__/reseller-dashboard/reseller-dashboard_mobile-support.md`
- `__docs__/owner-notifications/owner-notifications_impl.md`
- `__docs__/messaging-onboarding/README.md`
- `__docs__/url-routing-architecture/README.md`
- `__docs__/changelog.md`

Docs corrected to match runtime:

- Reseller management is platform-role/API protected; no client-bundled platform password exists.
- Internal ops monitor APIs are feature-flag fail-closed.
- Messaging onboarding ops latest-health lookup uses the control doc instead of a document-id prefix query.
- Chrome visual QA findings for Ops Control Room and Platform Notifications are recorded.
- Admin subdomain rename updates store summary and invalidates public cache tags.

### H. Fixes Made

Runtime files changed in this slice:

- `src/constants/user.ts`
- `src/components/templates/main-app/reseller/ResellerManagement.tsx`
- `src/components/mobile/screens/MobileResellerManagementScreen.tsx`
- `src/app/(main)/reseller/manage/page.tsx`
- `src/app/api/ops/platform-notifications/route.ts`
- `src/app/api/ops/owner-notifications/route.ts`
- `src/app/api/ops/messaging-onboarding/route.ts`
- `src/app/api/ops/mute-alerts/route.ts`
- `src/app/api/platform/entity-blocks/route.ts`
- `src/components/templates/main-app/platform/opsControlRoom/index.tsx`
- `src/components/templates/main-app/platform/platformNotificationMonitor/index.tsx`
- `src/app/api/admin/subdomains/rename/route.ts`

### I. Validation Performed

- Ops Control Room boundary source gate: `npm run verify:ops-control-room-boundary` - passed; source-only SAFE_MODE/mute-alerts/force-republish route, desktop, mobile, and docs gate.
- `node --check scripts/verification/verify-ops-control-room-boundary.js` - passed.
- Ops Control Room focused sweep: `npm run verify:ops-control-room-boundary`, `npm run verify:menulist-api-tenant-safety`, `npm run verify:auth-security-failure-matrix`, `npm run verify:mobile-shell-route-map`, `npm run verify:doc-npm-scripts`, and `npm run docs:check-links` - passed.
- Platform entity-block boundary source gate: `npm run verify:platform-entity-blocks-boundary` - passed; source-only tenant/store/user block route, auth sync, public cache invalidation, desktop/mobile, and docs gate.
- `node --check scripts/verification/verify-platform-entity-blocks-boundary.js` - passed.
- Platform entity-block focused sweep: `npm run verify:platform-entity-blocks-boundary`, `npm run verify:menulist-api-tenant-safety`, `npm run verify:auth-security-failure-matrix`, `npm run verify:mobile-shell-route-map`, `npm run verify:public-business-truth`, `npm run verify:doc-npm-scripts`, and `npm run docs:check-links` - passed.
- Platform cost posture boundary source gate: `npm run verify:platform-cost-posture-boundary` - passed; source-only platform API admission, bounded Admin SDK reads, browser response guard, desktop/mobile navigation, and docs gate.
- `node --check scripts/verification/verify-platform-cost-posture-boundary.js` - passed.
- Platform cost posture focused sweep: `npm run verify:platform-cost-posture-boundary`, `npm run verify:menulist-api-tenant-safety`, `npm run verify:auth-security-failure-matrix`, `npm run verify:mobile-shell-route-map`, `npm run verify:doc-npm-scripts`, `npm run docs:check-links`, `npx tsc --noEmit --incremental false --pretty false`, `npm run lint`, and `git diff --check` - passed.
- Messaging onboarding monitor boundary source gate: `npm run verify:messaging-onboarding-monitor-boundary` - passed; source-only platform monitor route, bounded Admin SDK reads, masked event/session rows, desktop/mobile, and docs gate.
- `node --check scripts/verification/verify-messaging-onboarding-monitor-boundary.js` - passed.
- Messaging onboarding monitor focused sweep: `npm run verify:messaging-onboarding-monitor-boundary`, `npm run verify:menulist-api-tenant-safety`, `npm run verify:auth-security-failure-matrix`, `npm run verify:mobile-shell-route-map`, `npm run verify:doc-npm-scripts`, and `npm run docs:check-links` - passed.
- Scheduler monitor boundary source gate: `npm run verify:scheduler-monitor-boundary` - passed; source-only read-only scheduler DAL, bounded detail rendering, store-scoped manual recovery, desktop/mobile route, and docs gate.
- `node --check scripts/verification/verify-scheduler-monitor-boundary.js` - passed.
- Scheduler monitor focused sweep: `npm run verify:scheduler-monitor-boundary`, `npm run verify:menulist-api-tenant-safety`, `npm run verify:auth-security-failure-matrix`, `npm run verify:mobile-shell-route-map`, `npm run verify:doc-npm-scripts`, `npm run docs:check-links`, `npx tsc --noEmit --incremental false --pretty false`, `npm run lint`, and `git diff --check` - passed.
- Platform notification boundary source gate: `npm run verify:platform-notifications-boundary` - passed; source-only platform-notification registry/API/monitor/docs gate.
- `node --check scripts/verification/verify-platform-notifications-boundary.js` - passed.
- Platform-notification focused sweep: `npm run verify:platform-notifications-boundary`, `npm run verify:menulist-api-tenant-safety`, `npm run verify:auth-security-failure-matrix`, `npm run verify:doc-npm-scripts`, and `npm run docs:check-links` - passed.
- Current aggregate local readiness after the platform cost posture source gate: `npm run verify:production-readiness-local` - passed; 55/55 checks, including 51 child root `verify:*` scripts.
- `npx tsc --noEmit --incremental false --pretty false` - passed.
- `npm run lint` - passed.
- `npm run build` - passed. Build emitted the existing batch image generation warning that `BATCH_IMAGE_GENERATION_WORKER_SECRET` is missing from the app environment.
- `git diff --check -- [Audit 19 touched files]` - passed after removing one trailing-space doc line.
- Built-server HTTP smoke with `npm start -- --hostname 127.0.0.1 --port 3000`:
  - `GET /ops` returned `307` to `/platform/ops-control-room`.
  - `GET /platform/ops-control-room` with `x-forwarded-proto: https` returned `200`.
  - `GET /reseller/manage` with `x-forwarded-proto: https` returned `200`.
  - Unauthenticated `POST /api/ops/safe-mode` returned `401`.
  - Unauthenticated `POST /api/platform/entity-blocks` returned `401`.
- Authenticated platform-owner HTTP smoke with the provided test account:
  - NextAuth credentials flow returned `200`, established a session cookie, and `/api/auth/session` returned `platformRole: "PLATFORM"`.
  - `GET /platform/ops-control-room`, `/ops/platform-notifications`, `/ops/owner-notifications`, `/ops/messaging-onboarding`, and `/reseller/manage` returned `200` with no redirect, no client password prompt, and no access-restricted marker.
  - `GET /api/ops/platform-notifications?limit=5`, `/api/ops/owner-notifications?productId=ML&status=failed&limit=5`, and `/api/ops/messaging-onboarding` returned `200` JSON payloads with the expected top-level keys.
  - The first authenticated smoke exposed the missing `systemHealth` document-id index in `/api/ops/messaging-onboarding`; the bounded control-doc fix was applied, rebuilt, and the route then returned `200`.
- Authenticated Chrome visual QA:
  - Chrome could not open raw `localhost` or local IP URLs directly, so a temporary local proxy was used to supply the forwarded HTTPS header that production receives from the deployment proxy.
  - Signed in through Chrome as the platform test account.
  - Production-built visual pass loaded `/platform/ops-control-room`, `/ops/platform-notifications`, `/ops/owner-notifications`, `/ops/messaging-onboarding`, and `/reseller/manage`; no client password prompt, sign-in prompt, or access-restricted state appeared.
  - Found the Ops Control Room horizontal action overflow and the Platform Notifications collapsed Message column; both were fixed.
  - Source-level Chrome recheck confirmed Platform Notifications has no page-level overflow, the Message column renders at readable width, and the table owns its horizontal scroll.
  - Source-level Ops Control Room recheck confirmed no page-level horizontal overflow, but the synthetic proxy origin could not fully revalidate the loaded data state because `/api/auth/set-claims` and Firebase App Check reject the temporary `sslip.io` origin. This is a Chrome-local QA limitation, not a production route behavior.
- Built output search found no literal `MenuList@2026`, no `Enter platform password`, and no `ECOMSAI_PLATFORM_PASSWORD` reference outside docs that describe the removed issue.

Pending:

- Production-build visual recheck after the Chrome layout fixes was not rerun under the deploy/build guard. The earlier CampaignCue TypeScript drift is no longer the active typecheck blocker; later global `npx tsc --noEmit --incremental false --pretty false` runs passed. Browser-backed recheck remains pending because browser control was unavailable in the latest continuation and production-host smoke requires an explicit deploy/release window.

---

## Whole-App Visual And Localhost Dev QA Follow-Up - June 11, 2026

### Scope Audited

This follow-up used a Chrome-authenticated platform-owner session and local tenant host routing to move beyond platform/ops-only coverage. The pass covered:

- Public website and marketing/legal routes: `/`, `/features`, `/pricing`, `/get-started`, `/create-menu`, `/resources`, `/resources/menu-source-audit`, `/how-it-works`, `/industries/restaurants`, `/multi-location`, `/trust-security`, `/privacy-policy`, `/terms-of-service`, `/refund-policy`, `/contact`, `/about`, and `/signin`.
- Owner desktop routes: `/dashboard`, `/today`, `/today/history`, `/projects`, `/use-menulist`, `/use-menulist/print-assets`, `/use-menulist/menu-card-export`, `/assets`, `/qr-code`, `/qrCode`, `/feedback`, `/business-settings`, `/transactions`, `/locations`, `/billing`, `/users/list`, `/users/permissions`, `/help-center`, `/business-health`, `/growth-kits`, `/users`, `/reseller`, `/reseller/onboard`, and `/reseller/manage`.
- Platform/internal MenuList routes: `/platform`, `/platform/ops-control-room`, `/platform/scheduler-monitor`, `/platform/extraction-monitor`, `/platform/answerlattice-intake`, `/platform/entity-blocks`, `/platform/tenants`, `/platform/stores`, `/platform/users`, and `/platform/owner-business-assistant`.
- Forced mobile shell owner routes: `/dashboard?mobileAudit=1`, `/today?mobileAudit=1`, `/projects?mobileAudit=1`, `/use-menulist?mobileAudit=1`, `/feedback?mobileAudit=1`, `/business-health?mobileAudit=1`, `/billing?mobileAudit=1`, `/locations?mobileAudit=1`, and `/qr-code?mobileAudit=1`.
- Public tenant/customer routes on a real local tenant fixture: OBP root `/`, `/menu`, `/bar-menu`, `/spa-menus`, `/privacy`, `/terms`, `/refund`, and `/feedback/14-mn8d5jbz-15?source=menu_footer`.
- Public pull API guards: `/api/public/v1/business?subdomain=habibis` and `/api/public/v1/menu?subdomain=habibis&slug=bar-menu` without an API key.

### Findings

1. **Fixed: desktop QR routes rendered a placeholder page.**  
   `/qr-code` showed only `QrCodePage` while the mobile owner shell correctly mapped QR/share behavior into the Share screen. `/qr-code` and the legacy `/qrCode` alias now render the same Use MenuList owner surface directly. This keeps the sidebar route stable and avoids an app-router redirect path that returned a 404 shell under localhost dev.

2. **Fixed: public compliance pages inherited menu metadata.**  
   Tenant `/privacy`, `/terms`, and `/refund` pages rendered the correct public compliance content but inherited the default menu title. Metadata now resolves the compliance page title and description before project/menu metadata fallback.

3. **Fixed: Projects special-menu modal used a deprecated Ant Design prop.**
   The special-menu creation modal now uses `destroyOnHidden`, which is supported by the installed Ant Design typings and preserves the intended destroy-on-close behavior without the `destroyOnClose` deprecation warning. A follow-up sweep moved the remaining MenuList desktop Ant Design `Modal` / `Drawer` call sites found in owner, reseller, project, and platform-monitor surfaces to `destroyOnHidden`; mobile `Popup` branches intentionally keep their `destroyOnClose` prop because that is the wrapper contract in `src/components/mobile/antd.tsx`.

4. **Observed: local provider/media warnings did not break public rendering.**  
   Local public QA saw intermittent Upstash rate-limit provider timeouts and Firebase Storage image responses with upstream `402` responses. Public pages continued to render text and navigation. Legacy optional image-provider helpers now encode outbound params, clamp page/orientation/search-query inputs, apply a 10s provider timeout, keep bounded failure diagnostics, and pass Pexels credentials through Axios `headers`; provider and media-account health still remain production smoke items.

5. **Observed: `/today/history` canonicalizes to `/today`.**  
   The owner history route redirected to the Today surface in this environment. This was recorded as route behavior rather than changed, because no broken user-visible error or dead end appeared.

### Fixes Made

Changed runtime files in this follow-up:

- `src/app/(main)/qr-code/page.tsx`
- `src/app/(main)/qrCode/page.tsx`
- `src/app/client/[[...slug]]/page.tsx`
- `src/lib/security/inputValidation.ts`

Changed documentation:

- `__docs__/audits/menulist-production-readiness-audit.md`
- `__docs__/changelog.md`

### Firebase Cost Findings

- QR route surface reuse adds zero Firebase reads, writes, listeners, indexes, API calls, or cache invalidation work beyond the existing Use MenuList page behavior.
- Compliance metadata now exits before menu/project metadata fallback for compliance slugs. It reuses the store lookup already required for public metadata and avoids an unnecessary project fallback read for `/privacy`, `/terms`, and `/refund`.
- Public tenant menu pages remained on the existing cached public truth path with `client-stores`, `store-{storeId}`, and `menu-store-{storeId}` cache tags; no new listener or client fanout pattern was introduced.
- Public feedback rendering was checked without submitting feedback, so no customer feedback write was produced.
- Public pull APIs failed closed without an API key and did not expose tenant data. The API-key validation path remains the intended gate before any authorized business/menu payload read.
- The shared `validateAPIInput()` type contract now accepts schemas with unknown raw input and typed parsed output, matching Zod preprocess behavior without changing runtime validation.

### Desktop/Mobile/Public Parity

- Desktop owner navigation now has the same practical QR/share surface as the mobile Share route.
- Forced mobile shell routes rendered without auth wall, visible error, not-found state, or horizontal overflow. This proves shell routing and state coverage, but it is not a substitute for physical mobile or browser-emulated narrow-device QA.
- Public tenant OBP, menu, compliance, and feedback routes rendered without owner/admin leakage, auth wall, visible not-found state, or page-level horizontal overflow.
- The public feedback flow was verified through the menu footer project-scoped link. The bare tenant `/feedback` route is an owner route and is not the customer feedback entry point.

### Public Route And Cache Findings

- Local tenant routing required the dev server to run with `NEXT_PUBLIC_PLATFORM_DOMAIN=192-168-11-18.sslip.io` and tenant host `habibis.192-168-11-18.sslip.io`; raw localhost and synthetic proxy routing were not reliable proof for the tenant middleware path.
- Plain `http://localhost:3000` remains the correct dev target for owner/platform routes. Public customer tenant routes are host-sensitive and cannot be fully proven on plain localhost without a tenant hostname or configured local platform domain.
- OBP, default menu, non-default menu slug, compliance pages, and project-scoped feedback route all resolved on the tenant host.
- Compliance metadata now matches the rendered public surface and no longer leaks an unrelated menu title into legal/compliance pages.
- No write path was touched, so public cache invalidation requirements were unchanged.

### Validation Performed

- Chrome visual QA for the route sets listed above.
- Chrome automation could not open plain `http://localhost:3000` routes in this environment because Chrome returned `ERR_BLOCKED_BY_CLIENT`; localhost follow-up therefore used `npm run dev`, authenticated NextAuth HTTP route probes, and server logs.
- Targeted recheck after fixes:
  - `npm run dev` on `http://localhost:3000` returned `200` for authenticated `/qr-code`, `/qrCode`, and `/use-menulist`; all rendered Use MenuList content, did not render `QrCodePage`, and had no Next not-found marker.
  - `/privacy`, `/terms`, and `/refund` produced compliance-specific titles.
  - Forced mobile `/qr-code?mobileAudit=1` still resolved to the Share shell.
- Public API smoke without API key:
  - `/api/public/v1/business?subdomain=habibis` returned `MISSING_API_KEY`.
  - `/api/public/v1/menu?subdomain=habibis&slug=bar-menu` returned `MISSING_API_KEY`.
- `git diff --check -- src/app/(main)/qr-code/page.tsx src/app/(main)/qrCode/page.tsx src/app/client/[[...slug]]/page.tsx src/lib/security/inputValidation.ts __docs__/audits/menulist-production-readiness-audit.md __docs__/changelog.md` - passed.
- `npm run lint` - passed.
- `npx tsc --noEmit --incremental false --pretty false` - passed.
- `npm run build` - intentionally not used for this follow-up after owner instruction to validate with `npm run dev` on localhost.

### External Certification Gate 2 Tenant-Block Mirror Backfill Evidence - July 1, 2026

Gate: Tenant-Block Mirror Backfill Review.
Date: July 1, 2026.
Environment: local shell in `menulist-core`, target Firebase project `menulist-qa`, dry-run mode, no `--write`.
Command or manual path: `npx ts-node --compiler-options '{"module":"CommonJS"}' scripts/backfill-store-tenant-block-state.ts --project-id menulist-qa --limit 1`.
Expected: script prints the explicit project and `DRY RUN` mode, reads at most one store from the target project, prints bounded candidate counts, and performs no Firestore writes. Write mode must require matching `--confirm-project` plus `--tenant-id`, `--store-id`, or explicit `--all-stores`.
Actual: local refusal guards passed first: missing project target, mismatched `--confirm-project`, and unscoped write mode all failed before Firestore initialization with bounded `tenant_block_backfill_failed` output. The read-only dry-run printed `Project: menulist-qa` and `Mode: DRY RUN`, then failed before dataset review with `PERMISSION_DENIED` / `CONSUMER_INVALID` for project `menulist-qa`.
Result: blocked for target dataset review by Firebase project access, not by a script safety failure.
Evidence: dry-run failure summary was bounded: `code: 7`, `domain: googleapis.com`, `reason: CONSUMER_INVALID`, and message `7 PERMISSION_DENIED: Permission denied on resource project menulist-qa.`
Follow-up: rerun the same dry-run command only after the active Firebase account has read access to `menulist-qa`; run write mode only after dry-run review with `--write --confirm-project menulist-qa` and a scoped `--tenant-id`, `--store-id`, or explicit `--all-stores`.

### External Certification Gate 3 Active Customer Fixture Evidence - July 1, 2026

Gate: True Mobile And Browser QA, active public tenant/customer fixture subset.
Date: July 1, 2026.
Environment: local Next dev server on `http://localhost:3050` with `NEXT_PUBLIC_PLATFORM_DOMAIN=192-168-10-239.sslip.io`, in-app browser viewport `390x844`, tenant host `http://habibis.192-168-10-239.sslip.io:3050`.
Command or manual path: Node `fetch` warm-up plus in-app browser navigation for `/`, `/menu`, `/bar-menu`, `/spa-menus`, `/privacy`, `/terms`, `/refund`, and `/feedback/14-mn8d5jbz-15?source=menu_footer`.
Expected: active tenant OBP, menu, non-default menu slug, localized menu slug, compliance pages, and project-scoped feedback render without not-found fallback, auth wall, runtime error text, page-level horizontal overflow, or browser console errors.
Actual: all routes returned/rendered active `Habibis Restaurant` tenant content; menu routes showed menu sections/items, compliance routes showed tenant compliance copy, feedback showed the customer feedback form, `documentElement.scrollWidth` equaled `390`, no problem phrases were visible, and no tenant-host console errors were captured. Horizontally scrollable menu category chips were present inside their own scroll area but did not create page-level overflow. Page loads triggered the existing public analytics tracking endpoint, but no feedback form, owner action, payment action, or provider mutation was submitted.
Result: passed for the active public tenant/customer fixture subset.
Evidence: fetch warm-up returned `200` for every route; browser evidence recorded titles including `Habibis Restaurant | Breakfast, Coffee & Beverages`, `Menu | Habibis Restaurant`, `Menu | હબીબીસ રેસ્ટોરન્ટ`, and `Share Feedback | Habibis Restaurant - Grill Zilla`. Server logs also showed existing Upstash local timeout bypass and upstream Firebase Storage image `402` warnings during public menu rendering; those provider/media-account health items remain separately tracked and did not block text or navigation rendering.
Follow-up: true physical-device QA and authenticated owner-shell mobile QA remain pending.

### External Certification Gate 3 Authenticated Owner-Shell Evidence - July 1, 2026

Gate: True Mobile And Browser QA, authenticated owner-shell mobile subset.
Date: July 1, 2026.
Environment: local Next dev server on `http://localhost:3050`, `scripts/verification/verify-mobile-owner-menu.mjs`, headless Chrome mobile metrics `393x852`, iPhone user agent, touch emulation enabled, local NextAuth session cookie signed with `.env` `NEXTAUTH_SECRET`.
Command or manual path: `MOBILE_QA_BASE_URL=http://localhost:3050 MOBILE_QA_OUTPUT_DIR=/tmp/menulist-mobile-qa MOBILE_QA_DEBUG_PORT=9355 MOBILE_QA_CDP_TIMEOUT_MS=90000 node scripts/verification/verify-mobile-owner-menu.mjs`.
Expected: authenticated owner mobile Menu tab mounts `MobileShell`, shows mobile bottom navigation (`Menu`, `Share`, `More`), selects a menu project, and opens the Menu action sheets without page-level overflow or material browser errors.
Actual: authentication and Firebase claim sync succeeded; server logs showed authenticated user-context fetches and `/api/auth/set-claims` returned `200`. The page loaded `/projects#mobile/menu` at `393x852`, but `MobileShell` did not mount because the current QA account is not subscription/starter eligible. The visible state was the mobile `Subscribe to Get Started` card with `View Plans`, no sign-in wall, and no runtime error text.
Result: blocked for owner-shell certification by missing eligible QA subscription/starter fixture, not by a mobile-shell render failure.
Evidence: read-only store/subscription probe found the QA account's store `15` has pending or expired subscription records only, including an expired active record whose cycle ended on November 30, 2025, and store `53` has no subscription/starter activation. The verifier captured `/tmp/menulist-mobile-qa/mobile-owner-menu-initial-shell-failed-auto-selected-project.png` and now emits explicit `fixture_blocked` classification plus a bounded debug payload with `hasMobileShellScroll: false`, `hasMobileNavLabels: false`, and visible text beginning `Subscribe to Get Started`.
Follow-up: rerun only after a non-production owner test store has an active subscription or unexpired starter activation; use the current explicit fixture command from `__docs__/production-readiness/external-certification-runbook.md` with `MOBILE_QA_REQUIRE_EXPLICIT_FIXTURE=1`, `MOBILE_QA_EMAIL`, `MOBILE_QA_STORE_ID`, `MOBILE_QA_PROJECT_ID`, and `MOBILE_QA_PROJECT_NAME`. Do not mutate live billing/store state just to satisfy the QA gate.

### External Certification Gate 4 Local Payment Guard Evidence - July 1, 2026

Gate: Razorpay Sandbox Smoke, local guard subset only.
Date: July 1, 2026.
Environment: local shell in `menulist-core`; no Razorpay provider calls, checkout sessions, webhooks, Firestore writes, Vercel deploys, or Firebase deploys were performed.
Command or manual path: `npm run verify:menulist-api-tenant-safety`; `npm run verify:no-free-product-plans`; `npm run verify:auth-security-failure-matrix`.
Expected: local static guards continue to prove Razorpay webhook cheap-fail ordering, bounded raw-body and JSON admission, signature-before-mutation behavior, bounded payment/reseller diagnostics, no free public product plans, and auth/security failure anchors.
Actual: all three commands passed. The tenant-safety verifier covers `/api/razorpay/webhook` bounded raw-body/rate-limit/signature/idempotency ordering; authenticated Razorpay and reseller bounded-body/payment diagnostics; desktop/mobile billing payment error boundaries; and Razorpay plan/subscription entitlement diagnostic guards. The no-free-plans verifier passed. The auth/security failure matrix passed and includes the Razorpay webhook validator HMAC/timing-safe failure anchors.
Result: passed for local payment guard coverage only.
Evidence: `MenuList API tenant-safety verifier passed.`, `No free product plans verifier passed`, and `Auth/security failure matrix verification passed.`
Follow-up: this does not satisfy the real Gate 4 provider requirement. A Razorpay sandbox/test-mode run still must prove subscription checkout, payment verification, provider failure compensation, top-up purchase, reseller payment where in scope, invalid-signature webhook rejection, accepted webhook handling, local/provider state parity, and no real charge.

### External Certification Gate 5 Local WhatsApp Guard Evidence - July 1, 2026

Gate: WhatsApp Provider Smoke, local guard subset only.
Date: July 1, 2026.
Environment: local shell in `menulist-core`; no Meta WhatsApp Cloud API calls, inbound webhook deliveries, outbound sends, Firebase writes, Vercel deploys, or Firebase deploys were performed.
Command or manual path: `npm run verify:agent-readiness`; `npm run verify:env-targets`; `npm run verify:whatsapp-action-link-check`; `npm run verify:menulist-api-tenant-safety`.
Expected: active docs, env templates, Functions secret registry, app env validation, public WhatsApp Action Link Check boundaries, messaging onboarding ops guards, owner-notification WhatsApp diagnostics, and Functions WhatsApp adapter diagnostics remain aligned with the current `WHATSAPP_PHONE_NUMBER_ID`, `WHATSAPP_ACCESS_TOKEN`, `WHATSAPP_APP_SECRET`, and `WHATSAPP_VERIFY_TOKEN` contract.
Actual: all four commands passed. The agent-readiness and env-target checks verify current WhatsApp secret names across env validation, env templates, Functions secret registry, and messaging-onboarding setup docs. The WhatsApp Action Link Check verifier confirms the public tool does not send messages, verify phone numbers, open WhatsApp links, fetch external targets, mutate external platforms, call AI/search, or claim ranking outcomes. The tenant-safety verifier covers bounded messaging-onboarding/WhatsApp provider diagnostics, messaging onboarding ops read limiting, platform/owner notification WhatsApp handoff guards, and raw provider failure text exclusions.
Result: passed for local WhatsApp/provider guard coverage only.
Evidence: `Agent-readiness discovery surfaces verified`, `Environment target matrix verified`, `WhatsApp Action Link Check verification passed`, and `MenuList API tenant-safety verifier passed.`
Follow-up: this does not satisfy the real Gate 5 provider requirement. A non-production Meta app, test phone number, approved test recipient, configured Firebase secrets, deployed non-production webhook URL, and explicitly enabled `ENABLE_MESSAGING_ONBOARDING=true` target still must prove inbound text, inbound image/PDF, preview generation, approve/publish, outbound confirmation, `/ops/messaging-onboarding` visibility, unsafe/private media URL rejection, provider-mode evidence, and no dummy secrets or production WhatsApp number.

### Owner Action Mark-Done Session Scope Boundary checkpoint - July 5, 2026

`POST /api/analytics/owner-action/mark-done` now normalizes the authenticated session `tenantId` and `storeId` as exact positive numeric Firestore document IDs before DATA_WRITE rate limiting, bounded body parsing, dashboard-summary document construction, dashboard-summary read, or owner-action receipt write. Whitespace-mutated, nonnumeric, zero, negative, unsafe, or reserved session scope returns the existing owner-safe `Not onboarded` response and logs bounded analytics presence/length diagnostics only. This changes Next.js app source and verifier/docs evidence only; it adds no Firestore reads/writes/deletes beyond existing valid receipt behavior, no Storage operations, no Cloud Functions, no provider calls, no cache invalidations, no Firestore rules/indexes, no public routes, no owner-facing settings, no Firebase deploy requirement, and no Vercel deploy action. Follow-up gates passed: `node --check scripts/verification/verify-menulist-api-tenant-safety.js`, `npm run verify:menulist-api-tenant-safety`, `npx tsc --noEmit --incremental false --pretty false`, `npm run docs:check-links`, and focused `git diff --check`.

### POS Sync Target Document ID Boundary checkpoint - July 6, 2026

`/api/pos-sync/deliver` and `/api/pos-sync/test` now validate caller-supplied tenant/store IDs through `normalizePosSyncNumericDocumentId()` before rate-limit key material, store refs, project refs, menu-version transactions, delivery logs, POS status writes, or provider/webhook work. The guard combines exact positive numeric MenuList ID admission with the shared Firestore document-ID boundary, so malformed, whitespace-mutated, path-shaped, reserved, zero, negative, decimal, or nonnumeric tenant/store IDs fail as invalid input before Firestore or outbound work. Valid permissions, tenant access, webhook URL/DNS guard, signed payloads, delivery-log retention, failure threshold, test ping, and response shape remain unchanged. This changes Next.js app source and verifier/docs evidence only; it adds no Firestore reads/writes/deletes beyond existing valid POS behavior, no Storage operations, no Cloud Functions, no provider calls beyond existing valid webhook delivery/test behavior, no cache invalidations, no Firestore rules/indexes, no owner-facing settings, no Firebase deploy requirement, and no Vercel deploy action.

### External Certification Gate 6 Local POS Webhook Guard Evidence - July 1, 2026

Gate: POS Webhook Provider Smoke, local guard subset only.
Date: July 1, 2026.
Environment: local shell in `menulist-core`; no owner POS settings were changed, no external webhook endpoint was called, no Firestore writes, no Vercel deploys, and no Firebase deploys were performed.
Command or manual path: `npm run verify:public-business-truth`; `npm run verify:menulist-api-tenant-safety`; `npm run verify:auth-security-failure-matrix`; no-network `npx tsx -e` probe for `validatePosSyncWebhookUrl()` and `isBlockedPosSyncNetworkTarget()`.
Expected: local guards continue to prove POS desktop/mobile save/test request acknowledgement, shared no-redirect test request policy, bounded test response parsing, route auth/tenant/permission/body-cap admission, hashed store limiter keys, public-HTTPS URL validation, server DNS target blocking before outbound fetches, signed payload headers, owner-safe provider failures, and no raw provider failure text in UI/log paths.
Actual: all three package verifiers passed. The no-network URL probe passed for public HTTPS acceptance and rejection of HTTP, localhost, private IPv4, loopback IPv6, credentials, fragments, resolved private targets, and resolved link-local targets while allowing a public resolved address.
Result: passed for local POS webhook guard coverage only.
Evidence: `Public business truth verifier passed`, `MenuList API tenant-safety verifier passed.`, `Auth/security failure matrix verification passed.`, and no-network probe lines including `public https: passed`, `private ipv4 rejected: passed`, `resolved private target blocked: passed`, and `resolved public target allowed: passed`.
Follow-up: this does not satisfy the real Gate 6 provider requirement. A controlled public HTTPS receiver still must prove endpoint save, receiver-side signature verification, test connection, publish-triggered signed full-menu snapshot delivery, owner-safe failed endpoint handling, and secret rotation persistence before UI success.

### External Certification Gate 7 Local Batch Worker Config Evidence - July 1, 2026

Gate: Batch Image Worker Configuration, local preflight subset only.
Date: July 1, 2026.
Environment: local shell in `menulist-core`; no batch image job was started, no Cloud Task was enqueued, no image worker request was sent, no provider image generation ran, no Firestore writes, no Vercel deploy, and no Firebase deploy were performed.
Command or manual path: `npm run verify:ai-accounting`; `npm run verify:auth-security-failure-matrix`; no-task `npx tsx -e` probe for `getImageGenerationTaskConfigStatus()` with `FIREBASE_PROJECT_ID`, `FIREBASE_PROJECT_LOCATION`, `BATCH_IMAGE_GENERATION_QUEUE_ID`, `BATCH_IMAGE_GENERATION_WORKER_URL`, and `BATCH_IMAGE_GENERATION_WORKER_SECRET` explicitly unset.
Expected: local guards continue to prove batch-trigger bounded body and AI-accounting/capacity order, Cloud Tasks config readiness before capacity/enqueue, worker project-header and shared-secret admission, generated-image Admin upload handling, bounded batch job listener/result diagnostics, and retention cleanup source gates. With worker config explicitly unset, readiness must be false and no queue/enqueue path should run.
Actual: both package verifiers passed. The no-task probe emitted `cloud_tasks_batch_image_config_missing` with `ready:false`, `hasProjectId:false`, `hasQueueId:false`, `hasQueueLocation:false`, `hasWorkerSecret:false`, and `hasWorkerUrl:false`, then printed the same false readiness summary.
Result: passed for local batch worker preflight coverage only.
Evidence: `AI accounting hardening verification passed.`, `Auth/security failure matrix verification passed.`, and the no-task readiness JSON `{"ready":false,"hasProjectId":false,"hasQueueId":false,"hasQueueLocation":false,"hasWorkerSecret":false,"hasWorkerUrl":false}`.
Follow-up: this does not satisfy the real Gate 7 worker requirement. A configured non-production target still must prove queue id/project/location, batch job start, Cloud Tasks enqueue, worker rejection for missing/wrong secret, worker acceptance for correct secret, generated images reaching review state, and selected images persisting to project data.

### Remaining Risks From This Follow-Up

- Project extraction append jobs now have a code-side pre-AI document-size gate and explicit replacement policy. Desktop and mobile owner upload paths cap pending extraction batches at 15 files/pages before Storage upload; mobile also passes remaining PDF page slots into conversion before canvas rendering. `POST /api/menu-extraction/jobs` estimates the existing project document plus 100KB per incoming file against the shared 900KB save limit, rejects clearly oversized appends before provider extraction, deletes newly uploaded owner source files on that rejection, and tells the owner to reset the menu or create a new menu. The editor data model remains incremental: `files[].extractedData` is live editor data, so replacement cleanup is owner-controlled through reset/create-new rather than automatic stripping.
- Batch AI image generation now has a server-only prompt cache for one-prompt, one-image, non-reference generations. Cache source objects live under `system/aiImagePromptCache/`; cache hits copy bytes into the requesting store's own `media/menuItem/{tId}/{sId}/...` path and record a free `unitsConsumed: 0` AI operation. Expired cache source docs/objects are covered by bounded daily `ai_image_prompt_cache_cleanup` in `menulistMaintenanceScheduler`, pending Function deploy for live effect. The single-image browser draft route remains uncached because it returns base64 previews and does not write Storage until owner acceptance.
- Browser-emulated 390px QA now passed for key public website routes, the active `habibis` public tenant/customer fixture, and the local tenant-host safe fallback. True physical-device QA and authenticated owner-shell mobile QA remain pending.
- Authenticated owner-shell mobile QA reached a valid session but remains blocked by missing active subscription/starter fixture for the available QA stores.
- Razorpay local payment/security guards passed, but real Razorpay sandbox subscription/top-up/reseller/webhook smoke remains pending.
- WhatsApp local provider guards passed, but real Meta WhatsApp Cloud API inbound/outbound smoke remains pending.
- POS local webhook guards passed, but real external POS HTTPS receiver smoke remains pending.
- Batch image local preflight guards passed, but configured Cloud Tasks enqueue/worker processing smoke remains pending.
- Media provider/account health needs a separate production-environment check because local image proxy responses included upstream 402s. Code-side helper handling is hardened, but live provider/account status cannot be certified locally.
- No Vercel deploy or production-host smoke was run, per deployment guard.

---

## Global Validation Results

- `npm run lint` - passed after the platform Chrome fixes and after the whole-app Chrome follow-up fixes.
- `npx tsc --noEmit --incremental false --pretty false` - passed after the whole-app Chrome follow-up fixes.
- `npm run build` - intentionally not used for this follow-up after owner instruction to validate with `npm run dev` on localhost.
- `git diff --check -- [MenuList whole-app follow-up touched files]` - passed.
- Built-server HTTP smoke covered unauthenticated/local behavior for `/ops`, `/platform/ops-control-room`, `/reseller/manage`, `/api/ops/safe-mode`, and `/api/platform/entity-blocks`.
- Authenticated platform-owner built-server HTTP smoke passed for platform ops pages and the audited ops APIs.
- Authenticated Chrome visual QA ran for platform/internal routes and fixed two layout issues.
- Whole-app Chrome visual QA covered public website/sign-in, owner desktop, platform/internal, reseller, public tenant OBP/menu/compliance/feedback, forced mobile shell routes, and public API missing-key fail-closed behavior.
- Authenticated localhost `npm run dev` HTTP smoke established a platform-owner session and verified `/dashboard`, `/projects`, `/use-menulist`, `/qr-code`, `/qrCode`, `/feedback`, `/business-settings`, `/business-health`, `/reseller/manage`, `/platform/ops-control-room`, and `/platform/scheduler-monitor` returned `200` without the old QR placeholder.

Build warning retained as a known environment/config item:

- `BATCH_IMAGE_GENERATION_WORKER_SECRET` is missing, so batch image generation Cloud Tasks are not fully runnable until configured.

---

## Remaining Known Risks

1. Authenticated platform-owner HTTP and Chrome visual smoke passed for the audited platform/internal route set, and the follow-up Chrome pass covered owner desktop, forced mobile shell, public website, public tenant menu/OBP/compliance/feedback, reseller, and public API missing-key guards. Current 390px browser smokes passed key public website routes, the active `habibis` public tenant/customer fixture, and the local tenant-host safe fallback. Authenticated owner-shell mobile QA now reaches a valid local session but remains blocked by the available QA stores' missing active subscription/starter activation; true physical-device QA also remains pending.
2. Firebase Function deployments for `processMenuImages`, `processMenuImagesJob`, `menulistMaintenanceScheduler`, `computeDecisionBlocksScores`, `triggerDecisionBlocksScoring`, `triggerStoreNightlyScheduler`, `mapsPlaceCheck`, and `verifyMenuPublish` remain blocked. The source-file path hardening slice also changes `processMenuImages`, `processMenuImagesJob`, `startGeneration`, `embedArticleWorker`, and `regenerateEmbedding`; `processMenuImagesJob` carries the menu extraction SAFE_MODE worker guard; `menulistMaintenanceScheduler` carries reseller expiry maintenance, lifecycle/owner-notification retention, image batch retention cleanup, AI image prompt-cache source cleanup, Founder Monitor snapshots, and operational maintenance; the staleness lifecycle delivery slice changes the decision-block scheduler bundle through `functions/src/analytics/stalenessCheck.ts`; the scheduler-hour diagnostics slice changes `functions/src/utils/schedulerHour.ts` for `functions:messagingOnboarding` and `functions:backfillStoresSummary`; the Maps Place Check raw provider output slice changes `functions/src/logic/mapsPlaceCheck.ts` for `functions:mapsPlaceCheck`; the owner-notification template-output slice changes `functions/src/messaging/templates.ts` for `functions:verifyMenuPublish`, `functions:computeDecisionBlocksScores`, `functions:triggerDecisionBlocksScoring`, and `functions:triggerStoreNightlyScheduler`; the owner-notification flag/trigger diagnostics slice changes `functions/src/ownerNotifications/processor.ts` for the same target subset; and the legacy lifecycle event/status diagnostics slice changes `functions/src/messaging/messagingEngine.ts` for the same target subset. The latest documented scoped staging attempts against `menulist-qa` for the source-file path hardening subset, `functions:processMenuImagesJob`, `functions:menulistMaintenanceScheduler`, the staleness lifecycle delivery subset, the scheduler-hour diagnostics subset, the Maps Place Check raw provider output subset, and the owner-notification template-output, owner-notification flag/trigger diagnostics, and legacy lifecycle event/status diagnostics subsets passed predeploy lint/build and then failed with Cloud Resource Manager HTTP 403 caller permission before upload; the external certification runbook names the full current retry target set and the changed-function subset commands.
3. Batch image generation local preflight guards now pass and code-side cheap-fail when Cloud Tasks worker config is incomplete. Env templates, setup docs, and source gates now name the full configured worker set, but configured Cloud Tasks enqueue/worker processing still requires actual non-placeholder `FIREBASE_PROJECT_ID`, `FIREBASE_PROJECT_LOCATION`, `BATCH_IMAGE_GENERATION_WORKER_URL`, `BATCH_IMAGE_GENERATION_QUEUE_ID`, and `BATCH_IMAGE_GENERATION_WORKER_SECRET` values in the app environment.
4. Razorpay local payment/security guards now pass, but sandbox smoke was not run after the billing hardening.
5. WhatsApp local provider guards now pass, but real Meta WhatsApp Cloud API testing for messaging onboarding remains pending.
6. POS local webhook guards now pass, but external POS webhook provider smoke was not run after the POS hardening.
7. Legacy stores that have never been touched by platform tenant block/unblock can still incur a tenant-doc read on public store-cache misses. The backfill is guarded by explicit project targeting, matching write confirmation, and scoped write intent (`--tenant-id`, `--store-id`, or explicit `--all-stores`), but even a one-store `menulist-qa` dry-run currently fails with Firestore `PERMISSION_DENIED` / `CONSUMER_INVALID` for the active credentials.
8. Firestore rules for the Audit 10/11 slices deployed. Full index-file deploy remains blocked by an unrelated existing `kb_articles` index 409, but the two required `reviewsState` indexes were created directly with `gcloud` and both operations later reported `done: true`.
9. Reviews/Reputation remains disabled until GBP ingestion exists and owner mount points are intentionally added.
10. Onboarding compensation for local tenant/store/user creation followed by external provider setup failure is code-side covered for self-serve and reseller online onboarding; Razorpay sandbox smoke remains pending.
11. Reseller offline expiry/count maintenance is code-side covered in the consolidated maintenance scheduler, but production effect remains pending until the Firebase Function deploy blocker is cleared.
12. Documentation link and active-doc naming health is clean: the latest `npm run docs:check-links` run passed with 0 broken links and 0 naming violations.
13. No Vercel deploy was run, per deployment guard.

---

## Next Step To Continue From

Continue with **external and authenticated certification gates**:

- Use `__docs__/production-readiness/external-certification-runbook.md` as the checklist and evidence format for the remaining gates.
- Review the tenant-block mirror backfill in dry-run mode after Firestore access to the selected target project is available, then run it with `--write --confirm-project <projectId>` plus `--tenant-id`, `--store-id`, or explicit `--all-stores` only after selecting the exact project/scope.
- True physical-device QA for the owner shell and public customer menu, plus authenticated owner-shell mobile QA after provisioning or selecting a non-production store with active subscription or unexpired starter activation.
- Razorpay sandbox subscription/top-up/reseller payment smoke.
- Real WhatsApp/provider messaging onboarding smoke.
- External POS webhook test against a real HTTPS provider endpoint.
- Production-host smoke after an explicit Vercel deploy request.
- Retry Firebase Function deploys only after the active Firebase account has deploy access to `menulist-qa` and the required production/staging secret prerequisites are confirmed.

Previously completed feature-cluster files included:

- `src/components/mobile/screens/MobileMenuScreen.tsx`
- `src/app/api/descriptions/route.ts`
- `src/app/api/translations/route.ts`
- `src/components/templates/main-app/projects/editorView/*`
- `src/app/api/image-generation/*`
- `src/app/api/image-editing/route.ts`
- `src/app/screen/[token]/*`
- `src/app/api/screen/seen/route.ts`
- `src/components/templates/main-app/projects/b2cView/output/DecisionBlocks.tsx`
- `functions/src/decisionBlocksScoring.ts`
- `src/lib/intelligence/dal.ts`
- `src/lib/mce/*`
- `src/lib/outputControl/*`
- `src/app/api/owner-business-assistant/*`
- `src/lib/ownerBusinessAssistant/*`
- `src/components/templates/main-app/useMenuList/*`
- `src/components/templates/main-app/printableAssetTemplates/PrintableAssetTemplatesRoute.tsx`
- `src/components/templates/main-app/projects/b2cView/shareModal/*`
- `src/components/mobile/screens/MobileShareScreen.tsx`
- `src/lib/menu-kit/*`
- `src/lib/menu-card-export/*`
- `src/lib/printable-asset-templates/*`

---

## Final Verdict For This Checkpoint

**Not production ready** as a full MenuList certification.

The completed public truth routing/client menu/OBP cache slice, Stores/Business Settings/public store write slice, Projects/Menu Builder/output slice, AI extraction/upload/review slice, editor/translation/design slice, AI image/media slice, Digital Screens slice, Decision Intelligence/CMI/MCE/output-control slice, Business Health/Owner Business Assistant slice, Analytics/Guest Feedback/Reviews scaffolding slice, Roles/Permissions/Auth/Staff slice, Billing/Razorpay/Top-ups/Reseller slice, Multi-Outlet/Location Lifecycle slice, Public Create-Menu/Messaging/Notifications slice, Main Website/Public Claims/Discovery slice, Physical Surfaces/Print/Export/Use MenuList Assets slice, POS/GBP/Hours/Pricing Integrity slice, Staff Prompt/Today staff-facing output slice, and Safe Mode/Ops/Platform Monitoring/Internal Admin slice are **controlled owner testing ready after the fixes above**, with TypeScript, lint, authenticated platform-owner HTTP smoke, Chrome visual route smoke where available, active public tenant/customer 390px browser smoke, and authenticated localhost `npm run dev` route checks passing. Full production readiness remains blocked by physical-device mobile QA, authenticated owner-shell mobile QA with an eligible test store, provider sandbox/real-provider smoke, Firebase Function deployment, configured batch worker secret, production-host smoke, and the specific remaining risks above.
