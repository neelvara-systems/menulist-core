# Multi-Language Articles Help

Multi-language customer help is not currently available.

The disabled Governance surface can prepare private translation drafts for evaluation. It cannot approve, publish, index, or deliver those drafts to widget or hosted-help users.

Do not enable the feature flag for a customer as a workaround. A customer rollout requires an implemented review/publish flow, explicit locale fallback, fluent review, and deployed runtime evidence.

