# Instant Response Infrastructure Website Boundary

This implementation is supporting infrastructure. It should not receive a standalone public feature section until configured customer measurements support a meaningful claim.

Permitted supporting sentence:

> Repeated approved answers can use a freshness-checked cache, while stale or unavailable entries fall back to governed retrieval.

Do not publish latency, hit-rate, savings, free-tier, zero-configuration, or zero-staleness claims.
