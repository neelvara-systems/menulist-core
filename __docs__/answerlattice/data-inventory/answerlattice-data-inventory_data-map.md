# Answerlattice Data Inventory Data Map

**Status:** Audit map
**Product scope:** Answerlattice only
**Created:** 2026-06-15
**Last Updated:** 2026-07-20

## 1. Scope

This map covers the Answerlattice runtime in the current repository:

- Answerlattice app/API code under `src/app/(answerlattice)`, `src/app/api/answerlattice`, `src/app/api/widget`, `src/lib/answerlattice`, `src/database/answerlattice`, and shared support DAL files that use Answerlattice Firebase.
- Answerlattice Firebase assets: `firestore-answerlattice.rules`, `firestore-answerlattice.indexes.json`, `storage-answerlattice.rules`, and `functions-answerlattice/`.
- Shared support files that are product-aware and write to Answerlattice data, including AI operation logging, notifications, tickets, chats, feedback, KB, public API authentication, and owner notifications.

Excluded:

- MenuList SMB data inventory details, except where shared support utilities are product-aware and write into Answerlattice Firebase.
- Answerlattice website-only static copy unless it writes contact, onboarding, widget, public API, or hosted-help data.
- Deferred Owner Support Assistant summary/action/feedback/analytics collections that are not implemented. The live read-only assistant strictly reads six existing compact `platformSummary` documents and creates no collection or write path.

## 2. Identifier model

| Identifier | Meaning | Where used |
| --- | --- | --- |
| `pId` / `productId` | Product boundary. Answerlattice uses `AL`. | Almost every Answerlattice collection, AI operation rows, stores, contact enquiries, notifications, integration rows. |
| `tId` / `tenantId` | Answerlattice tenant/workspace owner scope. | Firestore rules, search, governance, support, scheduler, bundles, public API, integrations. |
| `sId` / `storeId` | Answerlattice workspace/store scope. | Firestore rules, `stores/{sId}`, support records, bundle paths, hosted help, widget keys, billing. |
| `uId` | User or system actor id. | AI/search history, chat sessions, signal events, audit logs, support actions. |
| `entityId` | Governed product concept id. | Entities, relations, canonical answers, signal events, search index, friction stats. |
| `answerId` | Canonical answer id. | Canonical answers, audit logs, releases, search history, mutation proposals. |
| `jobId` / `sourceId` / `itemId` | Knowledge Intake job/source/review ids. | Intake jobs, sources, review items, usage ledger, published KB/FAQ/surface/proposal records. |
| `publicBundleId` / `bundleVersion` | Compiled context bundle id/version. | Storage paths and bundle manifest in `platformSummary`. |

## 3. Firebase boundary

Answerlattice uses a dedicated Firebase client/admin runtime. The client file initializes a named `answerlattice` app when separate configuration is present, and the admin file initializes a named `answerlattice-admin` app with Answerlattice credentials or local ADC fallback.

Cloud Functions live in `functions-answerlattice/` and use separate Firebase Admin initialization. Firestore rules default-deny and then explicitly allow tenant-scoped reads/writes for specific collections. Storage rules are scoped to Answerlattice-only paths.

## 4. Storage atlas

### 4.1 Workspace, account, billing, and setup

| Storage target | What is stored | When written | How used | Why it exists | Current retention |
| --- | --- | --- | --- | --- | --- |
| `stores/{sId}` | Workspace/product profile, `pId`, `productId`, `productName`, HTTP(S)-only `productUrl`, `supportEmail`, `billingModel`, `primarySurfaces`, `timeZone`, `businessDayEndTime`, `answerlatticeWorkspaceProfileRevision`, `answerlatticeLaunchProfile`, widget config, widget key hashes/prefixes, hosted help config, runtime status, subscription summary. | On onboarding, revision-checked workspace profile transaction, widget config/key management, hosted help settings, runtime widget config requests, billing sync. | Main Answerlattice workspace truth, public/widget auth lookup, dashboard setup, scheduler timing, compiled context, billing fallback. | Keep one compact workspace document for product setup and runtime lookup; profile changes atomically patch the scheduler registry and compiled-context control plane. | Durable account/workspace record; no TTL found. |
| `subscriptions` | Subscription state, plan, amount, quantity, credits, status history, billing dates, provider subscription id. Answerlattice App Billing Document ID Boundary normalizes subscription document IDs before billing direct-doc reads, product-billing adapter create/update/get-by-id/entitlement refs, onboarding subscription writes, Knowledge Intake active-license shortcuts, AI accounting credit refresh/consumption, and Knowledge Intake support-credit refs. | Billing/provider flows and subscription sync. | Billing screens, active subscription lookup, Knowledge Intake usage/balance checks. | Paid access and plan enforcement. | Durable billing record; no Answerlattice-specific TTL found. |
| `payment_transactions` | Payment/order/subscription charge events, amount/currency/provider metadata. | Payment provider webhook/order flows. | Billing history screen, audit/support. | Payment audit trail. | Durable financial record; no TTL found. |
| `platformSummary/answerlatticeTenantsSummary` and tenant docs | Tenant/workspace registry and scheduler discovery summary. | Workspace profile updates, tenant summary sync, scheduler. | Scheduler tenant discovery, platform operations. | Avoid scanning `stores` in schedulers. | Durable summary; no TTL found. |
| `platformSummary/answerlatticeActivationSummary_*` | Setup readiness, bundle state, subscription/workspace readiness. | Activation summary API and scheduler/readiness flows. | Activation Command Center. | Summary-first dashboard data. | Durable summary; no TTL found. |
| `answerlattice_publicHelpSites/{domain}` | Hosted help domain registry, `tId`, `sId`, `pId`, enabled flag, hosted help config, DNS/provisioning status. | Hosted help settings save and refresh. | Hosted public help domain resolution through server cache. | Resolve anonymous hosted help domains with one direct registry doc. | Durable while domain is assigned; deleted when removed. |
| `platformSummary/integrationConfig_*` | Exact `pId/tId/sId` ownership, Slack webhook URL, email recipients, event filters, controlled Linear/GitHub config, circuit breaker settings. | Authenticated integration settings save; bounded legacy ownership claim. | Ownership-validating integration delivery processor, test route, and owner settings UI. | Owner-managed external workflow configuration. Conflicting/partial ownership fails closed. | Durable config; no TTL. Circuit-breaker changes are transactional. |
| `platformSummary/integrationHealth_*` | Last integration status, event id, event type, status code, duration, last success/failure. | Integration delivery logger. | Owner-facing compact health without scanning raw delivery logs. | Summary-first operational status. | Durable compact summary; no TTL. |
| `platformSummary/branding_{tId}_{sId}` | Private validated advanced-branding profile: bounded identity, six-digit colors, HTTPS asset/legal links, support email, and powered-by preference. | Only from the disabled Governance prototype after strict client and rule validation. | Private owner configuration only; no customer runtime reads it. | Reserved input for a future specifically approved delivery integration. | Durable until administrative/workspace lifecycle deletion; no TTL or client delete. |

### 4.2 Governed answer truth

| Storage target | What is stored | When written | How used | Why it exists | Current retention |
| --- | --- | --- | --- | --- | --- |
| `answerlattice_entities` | Product concepts/entities, type, name, slug, description, aliases, status, current version, tenant/store ids. Answerlattice App Entity DAL ID Boundary normalizes entity document IDs before update/deprecate refs, alias sync, merge reads/writes, merge audit state, or source-version IDs. | Entity create/update/deprecate, onboarding/bootstrap, candidate approval. | Retrieval, governance, product ontology, compiled bundles. | Stable product vocabulary for canonical answers. | Durable. Deletes disabled in rules; deprecation pattern used. |
| `answerlattice_entityRelations` | Links between entities and relationship metadata. Answerlattice App Entity DAL ID Boundary normalizes relation document IDs and relation endpoint entity IDs before relation delete refs, relation endpoint writes, merge relation rewrites, or source-version IDs. | Governance/product graph updates. | Knowledge graph expansion, compiled context bundles. | Express entity interaction and related concepts. | Durable; delete allowed by permission in rules. |
| `answerlattice_canonicalAnswers` | Authoritative answer content, structured summary, detailed explanation, procedure, scope, validation, governance/drift state. | Human-approved answer creation/update and governance flows. | Canonical retrieval, public API answer endpoint, widget/help search, compiled bundles. | Core Answerlattice authority layer. | Durable. Deletes disabled in rules. |
| `answerlattice_releases` | Release/version state for governed answer set. Answerlattice App Release ID Boundary: browser reads normalize IDs and exact stored scope; bounded create/activate actions use the authenticated Admin server transaction/lease lifecycle for ordering, drift audit and compiled-context invalidation. | Release management/governance. | Retrieval freshness, instant cache versioning, compiled context. | Versioned answer authority. | Durable. Deletes disabled in rules. |
| `answerlattice_mutationProposals` | Proposed answer changes from signals/intake, draft text, related entities, status, approval/rejection metadata, bounded signal/ticket evidence IDs, exact tracked evidence counts, and optional deterministic manual request identity. | Signal-to-knowledge pipeline, ticket-resolution extraction, Knowledge Intake publish to canonical proposal, Support Board proposal creation, governance review. | Human approval queue and proposal impact follow-up. | Never mutate canonical answers directly from signals, tickets, or AI; repeated ticket evidence is a review signal rather than approved truth. | Durable proposal record. Deletes disabled in rules. Ticket/signal lineage is capped so proposal growth stays bounded. |
| `answerlattice_auditLogs` | Append-only governance actions, answer history, actor, action, before/after context. Server-reserved actions include deterministic ticket-evidence merge lineage, manual Support Board proposal creation, Public API key rotation/revocation summaries without raw keys or hashes, and metadata-only Support Truth Export generation evidence without exported content. | Governance changes, same-transaction proposal creation/evidence merge, transactional Public API credential lifecycle, and each successful Support Truth Export build before file delivery. | Compliance/audit history, answer change trace, credential accountability, and sensitive export accountability. | Human governance accountability and replay-safe evidence lineage. | Durable; code comments state at least 3-year retention. |
| `answerlattice_entitySearchIndex` | Precomputed entity tokens, synonyms, weights, current version. Answerlattice App Entity DAL ID Boundary normalizes search-index document IDs and entity IDs before upsert refs or alias-sync query keys. | Entity/index rebuild and governance changes. | Fast canonical/entity matching, public entity bundle, context bundles. | Reduce runtime reads and computation. | Durable read model; no TTL. |
| `answerlattice_entityCandidates` | Candidate concepts produced by ingestion/intelligence, confidence, status. Answerlattice App Entity Candidate ID Boundary normalizes candidate document IDs before approval/rejection/promote/merge refs, promotion audit state, or promotion return metadata. | Auto knowledge, intake, bootstrap, signal processing. | Governance candidate review. | Human-reviewed ontology expansion. | Durable queue; no TTL found. |

### 4.3 Knowledge base, FAQs, surfaces, and changelog

| Storage target | What is stored | When written | How used | Why it exists | Current retention |
| --- | --- | --- | --- | --- | --- |
| `kb_categories` | Category/section/article navigation maps for a workspace. | KB management, Knowledge Intake article publish, bootstrap. | Help Center nav, public content cache, context bundles. | Published support navigation. | Durable. |
| `kb_articles` | Published/staged article content, plain text, TipTap doc, tags, embeddings, status, source links, likes/dislikes. | KB CRUD, generation pipeline, article embedding API, Knowledge Intake publish. | Help Center, RAG/vector search, compiled context, public content cache. | Answer source material and support docs. | Durable until owner delete/archive. |
| `kb_generation_jobs`, `kb_staging_sections`, `kb_staging_chunks` | KB generation/import job state, staging chunks, onboarding bootstrap progress, and fixed bootstrap failure code when a bootstrap tenant fails. | KB generation pipeline and onboarding bootstrap. | Review/publish generated help articles and track canonical-layer bootstrap progress. | Human-reviewed content generation and safe bootstrap recovery. | No global retention found in inspected Answerlattice code. Bootstrap failure text is fixed-code only. |
| `answerlattice_faqs` | Owner-reviewed question/answer pairs, status, active flag, tags, entity/context/surface links, review fields. | FAQ management and Knowledge Intake publish. | FAQ retrieval before RAG, Help Center FAQ tab, bundles. | Deterministic short-answer path. | Durable/archive by status. |
| `answerlattice_productSurfaces` | Route/page/workflow context map, visibility, entity hints, tags, active flag, exact `pId/tId/sId`. Stored documents are parsed through the product-surface boundary before summary rebuilds or browser reads. | Product Surface UI and Knowledge Intake publish. | Context-aware retrieval, related content, compiled bundles. | Route-aware support answers. | Durable/archive by status/active flag. |
| `platformSummary/contextContent_{tId}_{sId}` | Derived compact product-surface related-content summary with exact `pId/tId/sId`, validated surface keys, allowlisted article/FAQ/changelog refs, and bounded ticket counts. | Onboarding bootstrap and product-surface summary rebuilds after surface/content changes. | Search related-content enrichment, Product Surface owner screen, Activation Command Center, compiled-context fallback. | Avoid broad content/ticket/changelog scans in runtime surfaces. | Derived durable summary; malformed or cross-product rows are ignored until rebuilt. |
| `changelog/{tId}/{sId}/{pageId}` | Paginated release notes and changelog entries. | Changelog management. | Help Center release notes, context bundles, owner changelog. | Product update/support context. | Page model; no TTL found. |

### 4.4 Search, widget, and public API runtime

| Storage target | What is stored | When written | How used | Why it exists | Current retention |
| --- | --- | --- | --- | --- | --- |
| `aiSearchHistory` | Query, cache key, tenant/store/user, mount context, generated image query, image URL, crafted answer, bounded references/citations, canonical/answer source, canonical answer id, validated guided-procedure snapshot, FAQ id, matched entity ids, fallback reason, clarification, confidence, source versions, product context fields, `expiresAt`, `retentionDays`. | On instant-cache hits, canonical hits, FAQ hits, empty/no-result paths, and RAG answers. Widget feedback updates rows with `isGood` and `submittedAt`; guided terminal evidence reads the exact retained canonical procedure snapshot. | Search cache, widget feedback, guided-outcome proof, friction analysis, canonical miss signals, support debugging. | Runtime answer trace and feedback loop without permanent raw answer-event retention. | 90-day expiry on new rows, TTL override on `expiresAt`, public interaction rejection after expiry, bounded payload writer, and scheduler cleanup for legacy rows by `createdOn`. |
| `queryEmbeddings` | Cache key, original query, vector values, createdAt, hitCount, `expiresAt`, `retentionDays`. | When RAG embedding cache misses. | Avoid repeated embedding generation. | Provider cost and latency reduction. | 30-day expiry on new rows, TTL override on `expiresAt`, stale-read best-effort delete with fixed bounded diagnostics on cleanup failure, and scheduler cleanup by `createdAt`. |
| Upstash instant cache | Canonical-only deterministic answer payload keyed by tenant/store/entity/version/plan/role. | Canonical hit path when enabled. | Zero-Firestore hot path for canonical answers. | Low-latency/cost cache for governed answers. | Redis TTL is used by cache layer. |
| Public API request logs | Store id, endpoint, request metadata through shared `logApiRequest`. | Public API auth path. | API monitoring. | Rate/security trace. | Depends on shared public API logging implementation; not expanded in this audit. |
| `stores/{sId}.publicApi` | One active Answerlattice Public API credential: SHA-256 hash, bounded `al_` prefix, ISO creation time, exact `AL` product, `answerlattice_public_api` purpose, and explicit `public:read`, `signals:write`, or rollout-gated `mcp:read` scopes. | Protected owner create/rotate/revoke route. Raw key is returned once; shared browser rules preserve server-managed credentials. | Trusted-server authentication for public answers/entities, governed signals, and explicitly approved private MCP context. | Immediate revocation/rotation and scope-separated external distribution without recoverable secret storage. `public:read` alone cannot bootstrap MCP. | Durable until rotated, revoked, or workspace lifecycle removal. No positive auth cache. |
| MCP session token | Five-minute HMAC token containing exact audience, tenant/workspace, allowed runtime scopes, observed bundle version, issue time, and expiry. | Returned only after exact `mcp:read` credential, active workspace, configuration, rate, and ready-bundle checks. | Authenticates bounded MCP JSON-RPC tool calls against the current ready private bundle. | Prevents long-lived API credentials from being sent on every tool call while preserving a short revocation window. | Not persisted by Answerlattice. New sessions stop immediately after credential revocation; an issued token may remain valid for at most five minutes. |
| `stores/{sId}.answerlatticeWidgetApi` | Active widget key hashes plus bounded names, prefixes/suffixes, status, and create/update/revoke timestamps. | Widget key generate, rename, and revoke; legacy delete requests map to revocation semantics. | Widget/public key validation and dashboard summaries. | Narrow widget credential admission without storing recoverable raw keys. | Active until revoked; only bounded recent revoked summaries remain. Raw key is shown once. |
| `stores/{sId}.widgetConfig` / `widgetAllowedOrigins` | Bounded widget presentation/behavior config, strict route patterns, exact HTTP/HTTPS origins, schema/version timestamps. | Explicit protected widget config save. | Public widget config endpoint and compiled-context staleness. | Embeddable runtime presentation and access policy. | Durable until changed or workspace deletion. Empty origins are explicit open-origin mode. |
| `stores/{sId}.widgetRuntimeStatus` | Throttled runtime status/verification information. | Public widget config requests. | Owner install/readiness checks. | Prove widget is installed and working. | Durable compact status; no TTL found. |
| `answerlattice_signalEvents` | Public API signals and internal ticket/chat/feedback/escalation signals: type, normalized entity id or `unresolved`, best-effort sanitized metadata, timestamp, actor trace, optional dedupe key plus stable `identityFingerprint`, and `expiresAt`. The Answerlattice signal entity ID boundary normalizes malformed IDs to `unresolved`; resolution signals use a lifecycle event identity rather than a mutable actor identity. | Signal emitter, public signal ingestion, ticket/feedback/chat flows. Explicit external/request IDs use deterministic server document IDs and exact payload fingerprints. | Mutation governance, ticket-resolution extraction, friction aggregation, support board, predictive help. | Evidence, not automatic authority, with replay conflicts rejected instead of silently accepting changed payloads under an existing deterministic ID. | New writers set a rule-bounded 365-day `expiresAt`; Firestore TTL owns deletion. Legacy rows use the bounded migration in `firebase-forensic-audit.md`. Sanitization reduces common credential/PII exposure but is not a DLP guarantee. |

Public/widget/MCP write and AI-triggering routes keep their auth, product, purpose, scope, rate-limit, and origin checks before body parsing. MCP session exchange additionally requires explicit `mcp:read`; optional `signals:write` alone controls the missing-context tool. The public answers route caps JSON at 16KB, public signals at 32KB, widget feedback at 2KB, predictive help at 4KB, MCP JSON-RPC at 16KB, and widget search at the existing inline image allowance plus 64KB of JSON overhead. The maintained widget loader uses a fixed `/widget/embed` URL and sends its raw installation key only through exact-origin parent bootstrap after the iframe is ready; iframe requests use no-referrer policy. Public answers/entities/signals, widget search/config/feedback, predictive help, MCP failure diagnostics, and protected app-route success breadcrumbs use fixed runtime codes with bounded tenant/store metadata so runtime logs do not expose raw workspace identifiers.

### 4.5 Knowledge Intake

| Storage target | What is stored | When written | How used | Why it exists | Current retention |
| --- | --- | --- | --- | --- | --- |
| `answerlattice_knowledgeIntakeJobs` | Intake job title, description, source/review counts, default category/section, product URLs, audience, usage summary, status, timestamps, actor fields. | Job create/update/analyze/publish. | Intake Command Center and scheduler summary. | Structured import workflow. | Durable; no TTL found. |
| `answerlattice_knowledgeSources` | Source type, title, origin URL, file name, MIME, redacted `contentText`, excerpt, content hash, tags, context keys, entity ids, metadata, error state, timestamps. | Add source, repeated-reply import, public page fetch, media extraction output. | Intake analysis and review item generation. | Source evidence for support knowledge. | Durable source text; no compaction found. |
| `answerlattice_intakeReviewItems` | Generated/reviewable article/FAQ/surface/proposal candidates, status, target, body/answer/question/context/entity fields. | Intake analysis and update-review-item route. | Human review and publish. | No direct auto-publish into authoritative layers. | Durable; no TTL found. |
| Knowledge Intake document ID boundary | Firestore auto-ID shaped job IDs, deterministic `kis_` source IDs, and deterministic `kii_` review item IDs are normalized before protected route calls and shared service direct document refs. | Route params, publish `itemIds`, and service helper callers. | Job/source/review reads, writes, counters, publish workflow, support-credit work. | Malformed IDs stop before Firestore refs or provider-backed work. | Cost-neutral for valid IDs. |
| `answerlattice_intakeUsageLedger` | Reserved/finalized/refunded usage units, workspace, reservation/refund billing period, action, provider/model, byte size, token counts, token count source, metadata, monthly-vs-top-up debit source, actual refunded/expired credits, and subscription/store balance effects. Ledger IDs and `tId/sId` are revalidated before transaction-serialized finalize/refund. | Media extraction reserve/finalize/refund. | Billing/credit accountability. | Paid intake usage control. | Durable ledger; no TTL found. |
| `platformSummary/knowledgeIntakeSummary_*` | Compact active/recent job/readiness counts and last-published fields. | Job creation and nightly summary builder. | Dashboard/readiness summary. | Avoid raw intake scans in owner surfaces. | Durable summary. |

### 4.6 AI/accounting

| Storage target | What is stored | When written | How used | Why it exists | Current retention |
| --- | --- | --- | --- | --- | --- |
| `answerlattice_aiOperations/{tId}/{sId}/{docId}` | AI action, billing mode, token counts, token count source, units, model/provider context, source, timing, compact client response, support-credit `creditConsumption` when present, accounting fields, optional provider usage metadata plus response presence/length in detailed mode, `detailExpiresAt` in detailed mode. | Provider-backed Answerlattice calls such as Knowledge Intake OCR/transcription/embedding, widget/help search, article translation, FAQ generation, article embedding, manual and scheduled draft generation, article entity extraction, ticket-knowledge extraction, onboarding bootstrap, friction insight, and Cloud Function KB embedding. | Platform cost accounting and owner billing/support-credit usage through sanitized API responses; direct tenant Firestore reads are not allowed. The API route rate-limits before permission/read work and logs read failures with bounded scope/query metadata. | AI cost control and audit without raw prompt/provider payload exposure. | Default is accounting-only. Detailed payloads get `detailExpiresAt`, but no Answerlattice TTL field override for `detailExpiresAt` was found. |

### 4.7 Support, tickets, chat, and feedback

| Storage target | What is stored | When written | How used | Why it exists | Current retention |
| --- | --- | --- | --- | --- | --- |
| `chatSessions` | Chat title/history/messages, user/workspace fields from composer, feedback state, admin metadata, image URLs. | Chat save/update/message feedback. | User chat history, admin conversation monitoring, support debugging. | Support conversation continuity. | Durable; latest 50 user query cap but no cleanup found. |
| Storage `chatSessions/chatimages/{tId}/{sId}/{imageId}` | Chat images uploaded from base64 with private immutable metadata. | Chat image upload. | Chat/image search and support context. | Preserve user-provided visual context. | Images removed from a chat branch/session are currently retained because search history or another durable evidence object may share the URL. Cleanup is explicitly deferred until a reference-safe retention design exists. |
| `supportTickets` | Ticket subject, category, priority, client details, documents, messages, statuses, satisfaction, logs, client debug context, escalation context, deleted flag. | Ticket create/update/message/status/satisfaction/delete/restore. | Ticket inbox, support operations, signals, notifications. | Human support workflow. | Durable ticket workflow; hard delete reads persisted ticket data and deletes top-level documents plus message attachments before deleting Firestore doc. |
| Storage `supportTickets/documents/{tId}/{sId}/{fileId}` | Ticket top-level documents/images. | Ticket create/update. | Support evidence. | Attachments for support tickets. | Deleted by ticket hard delete. |
| Storage `supportTickets/messages/{tId}/{sId}/{fileId}` | Message attachments. | Ticket message add. | Conversation evidence. | Attachments for replies. | Deleted by ticket hard delete. |
| `feedback` | Ratings, comments, feature requests/issues, optional owner-assigned context/surface fields, server-derived user/workspace identity, deterministic request identity/fingerprint, and created/modified fields. | Authenticated 16 KiB submission API with a fail-closed 12-per-actor/workspace/hour gate; exact request replays reuse the same row. Owner Product Surface assignment is a separately constrained update. | Latest customer acknowledgement, bounded owner/platform review, deterministic signal retry, and Support Board handoff. | Private product/support evidence without treating browser metadata as authority. | Durable; customer/latest and owner/list queries are capped, direct client create/delete is denied, and no feature-local cleanup exists. |
| `answerlattice_supportBoardCards` | Internal private support cards, status, priority, capped prepend-only notes/status history, linked source/context IDs, bounded copied source identity/location, and one-way redaction markers. Non-manual source cards use deterministic normalized source identity. Nightly recurring-miss and signal-cluster cards store counts/context rather than raw source text. | Support Board UI, explicit gated source sync, live summary trigger, and disabled-by-default nightly preparation. | Owner/staff private support review and governed proposal handoff. | Convert selected support evidence into reviewable work without treating source records as approved truth or duplicating a generic ticket/project system. | Durable; no TTL or client delete. Copied identity/location can be cleared per card, but source-record and whole-workspace deletion remain separate retention responsibilities. |

Authenticated public-content cache reads (`/api/answerlattice/public-content`) keep tenant/store-scoped cache helpers for KB categories, articles, FAQs, and changelog pages; article detail requests validate KB article IDs through the shared Firestore document-ID boundary before the cached article read, and cache-load failures log fixed runtime codes with bounded content type and tenant/store metadata. Every Admin result then enters a positive public allowlist: FAQs, active navigation, published article render fields, published changelog entries, and safe attachment metadata only. Actor, trace, source-context, job/source, preparation, and future unknown persistence fields do not enter cached responses. Browser callers use `publicContentClient.ts`, which pins no-store cache, same-origin credentials, and manual redirect handling before the 1 MB bounded JSON parser and per-type runtime validators admit Help Center, FAQ, AI Search, and changelog state.

### 4.8 Scheduler, intelligence, and read models

| Storage target | What is stored | When written | How used | Why it exists | Current retention |
| --- | --- | --- | --- | --- | --- |
| `platformSummary/answerlatticeSchedulerState`, `answerlatticeNightlyState_*`, `answerlatticeNightlyLock_*`, task lease docs | Scheduler task status, last fixed task failure code, source error name/code/status metadata, leases, status, per-tenant lock data. | Answerlattice master scheduler. | Prevent overlapping scheduler work and show bounded operations status; the operations-status API rate-limits before permission/read work and logs failures with bounded tenant/store metadata. | Bounded safe scheduled operations. | Lease expiry fields exist; durable state docs remain. |
| `answerlattice_schedulerRunLogs` | Run id, trigger, start/update, status, duration, tenant discovery, totals, fixed failure codes, source error name/code/status metadata, bounded diagnostic details, tenant runs, feature flags/limits, retention cleanup totals, `expiresAt`, `retentionDays`. | Each Answerlattice nightly/manual scheduler run. | Platform ops/debugging. | Operational trace. | 90-day expiry on new rows, TTL override on `expiresAt`, and scheduler cleanup for legacy rows by `startedAt`. |
| `platformSummary/coverage_*` | Schema-v2 exact-scope rolling-24-hour approved-serve hits, fallbacks, total questions, rate, and complete-window metadata. | Nightly coverage aggregation after bounded search-history validation. | Coverage card and activation summary. | Compact approved-answer usage evidence without owner-side raw scans. | Durable compact summary; incomplete runs preserve the previous valid summary. |
| `platformSummary/trustMetrics_*` | Schema-v2 exact-scope coverage, no escalation, explicit confirmed outcomes, drift, active-entity answer coverage, review areas, source counts, and complete-window metadata. Compatibility fields remain stored for old readers but current surfaces use explicit metrics. | Nightly answer-evidence aggregation after coverage, answer, entity, and signal saturation checks. | Governance, dashboard, activation, weekly digest, and founder assistant. | One compact evidence view without an opaque trust score. | Durable compact summary; incomplete runs preserve the previous valid summary. |
| `answerlattice_frictionDailyStats` | Schema-v2 exact-scope per-entity UTC-day support-evidence counts and weighted load. | Nightly friction aggregation. | Completed-window trend calculation and snapshots. | Convert mapped support signals and canonical misses into bounded review evidence. | Cleanup deletes records older than 90 days. |
| `platformSummary/frictionSnapshot_*` | Schema-v2 exact-scope completed seven-day window, all-entity totals, top friction entities, emerging topics, volume-sensitive friction level, weighted load, unmapped evidence, and legacy-row counts. | Nightly friction aggregation after complete source validation. | Governance/readiness and predictive trigger sync. | Summary-first review queue; not a product-health or answer-quality score. | Durable compact summary; failed aggregation does not publish a partial replacement. |
| `platformSummary/friction_*` | Advisory weekly summary and bounded known-entity actions linked to a deterministic source snapshot timestamp. | Optional weekly provider call after strict parsing and source recheck. | Governance friction review. | Organize deterministic evidence without allowing the model to define metrics or truth. | Durable compact advisory summary. |
| `answerlattice_predictiveTriggers` | Draft/suggested/active/disabled/archived predictive-help and known-issue records, exact page/applicability conditions, bounded action, priority/cooldown, optional incident window, and server advisory engagement/friction fields. | Owner/governance UI and bounded nightly predictive sync. | Predictive widget/help suggestions and known-issue notices. | Govern exact-page proactive support; browser-created rows are manual, generated rows remain suggested, and engagement never mutates status automatically. | Durable until archived/deleted; owner deletion rebuilds the runtime summary. |
| `platformSummary/predictiveTriggers_*` | At most 200 strictly projected runtime triggers without creator, friction, or engagement fields. | Owner mutations and nightly predictive sync after cap-plus-one validation. | Public widget config, predictive-help evaluation, and interaction revalidation. | Avoid raw trigger scans and private/advisory fields in public runtime. | Durable compact summary; overflow or malformed scope preserves the previous valid summary. |
| `platformSummary/supportBoardSummary_*` | Exact workspace-wide open, Needs Answer, high-priority, and total card counts; live event/version markers; optional bounded status/priority/source/surface breakdown, last-sync counts, and completeness flags. | `answerlatticeSupportBoardSummaryOnWrite` after create/status/priority changes; disabled-by-default nightly Support Board preparation enriches breakdown fields. | Founder Daily Brief and optional Support Board summary UI without scanning cards. | Keep founder review counts current independently of the 120-card UI window and distinguish exact core counts from bounded breakdowns. | Durable compact summary; no TTL. Title/note-only writes skip recomputation; old retried events cannot replace newer live counts. |
| `insights/{tId}/stores/{sId}/ai/weekly` | Exact-scope deterministic completed-week narrative, bounded highlights/recommendations, repeated question, comparison metrics, source-day completeness, source hash, and generated timestamp. | Sunday UTC chat-intelligence task inside the existing Answerlattice scheduler, or the bounded support-manager prepare route; unchanged source hashes skip writes. | Weekly Digest and its text export. | Give founders settled support evidence without browser-side conversation scans or a model-generated report. | Latest durable projection; historical archive is not claimed. |
| `answerlattice_cacheVersions` | Source/version records for KB, canonical, surfaces/widget/context changes. | KB/canonical/surface/widget/profile writes and cache version bumps. | Search freshness, public content, compiled context staleness. | Cheap freshness validation. | Durable read model. |

### 4.9 Compiled context bundles and Storage

| Storage target | What is stored | When written | How used | Why it exists | Current retention |
| --- | --- | --- | --- | --- | --- |
| `platformSummary/answerlatticeContextBundleManifest_*` | Bundle status, public bundle id, active/last-ready version, source versions, object refs, stats, limits, errors. | Manual/server build and nightly repair build. The manual rebuild route rate-limits before permission/build work and logs failures with a fixed code plus bounded tenant/store metadata. | Widget/public API/entity reads and bundle proxy. | Single manifest for compiled context. | Durable manifest; scheduler cleanup uses it to retain active plus previous two ready versions. |
| `platformSummary/answerlatticeContextBundleLock_*` | Build lock status, lock id, start, expiry, source versions. | Bundle build start/end/fail. | Prevent concurrent bundle builds. | Build safety. | 10-minute expiry field on lock; doc remains. |
| Storage `answerlattice-context/public/{publicBundleId}/v{version}/...` | Public JSON bundle objects and manifest. | Context bundle build. | Public bundle proxy and public/runtime bootstraps. Cache misses are rate-limited before Storage access, fail closed if the limiter is unavailable while enabled, check Storage metadata and downloaded bytes against the 512 KB proxy ceiling, and log proxy/rate-limit/oversized failures with bounded path metadata. | Serve public-safe compiled context without Firestore reads or unbounded public Storage probing. | Public read allowed by Storage rules. Scheduler deletes old versions beyond active/last-ready plus previous two. |
| Storage `answerlattice-context/private/{tId}/{sId}/v{version}/...` | Private MCP/context JSON objects and manifest. | Context bundle build. | Server/public API fallback, entity index bundle, private context consumers. Cold reads check Storage metadata and downloaded bytes against the private-object ceiling before parsing. Oversized objects are treated as unavailable and log `answerlattice_context_bundle_object_oversized` with bounded path metadata. | Compiled private product context. | Client read/write/delete denied by Storage rules; admin writes. Scheduler deletes old versions beyond active/last-ready plus previous two. |

The active MCP runtime writes no `platformSummary/mcpSignal_*` document. Missing-context reports use the existing redacted, retained, idempotent `answerlattice_signalEvents` lifecycle. The `mcpSignal_*` ID helper is legacy-reserved and must not be treated as an active storage contract.

### 4.10 Integrations and notifications

| Storage target | What is stored | When written | How used | Why it exists | Current retention |
| --- | --- | --- | --- | --- | --- |
| `answerlattice_integrationEvents` | Event type, tenant/store, severity, sanitized payload, status, createdAt, expiresAt. | Integration event bus. | Integration processor on create. | Append-only outbound workflow events. | TTL field override exists on `expiresAt`. |
| `answerlattice_integrationDeliveryLogs` | Event id, adapter, attempt, status, status code, sanitized error, duration, createdAt, expiresAt. | Delivery attempt logger. | Ops/debugging and health summary. | Delivery audit without blocking caller. | TTL field override exists on `expiresAt`. |
| `answerlattice_integrationRateLimits` | Adapter/email delivery counters, bucket, recipient hash, expiresAt. | Integration rate limiter. | Prevent external delivery floods. | Persistent delivery caps. | TTL field override exists on `expiresAt`. |
| `answerlattice_notificationLogs` | Generic notification event type, recipient email, reference id, status, subject, message id, error/reason, createdAt, `expiresAt`, `retentionDays`. | Generic Answerlattice notification sender. | Email delivery diagnostics and idempotency/rate checks. | Support notification trace. | 90-day expiry on new rows, TTL override on `expiresAt`, and scheduler cleanup for legacy rows by `createdAt`. |
| `ownerNotificationEvents` | Product id, trigger type, tenant/store/workspace, reference id, dedupe key, recipient role/hints, metadata, priority, status, source, timestamps, `expiresAt`, `retentionDays` for Answerlattice events. | Owner-notification enqueue path when migration flag is enabled. | Product-aware owner notification processing. | Newer unified owner notification system. | 90-day expiry for Answerlattice rows, TTL override on `expiresAt`, and scheduler cleanup for legacy rows by `createdAt`. |
| `ownerNotificationDeliveries` | Event id, product, trigger, channel, recipient hash/masked, status, subject, template, provider id/error, attempt, timestamps, `expiresAt`, `retentionDays` for Answerlattice deliveries. | Owner-notification delivery writer. | Delivery audit. | Channel-level delivery trace. | 90-day expiry for Answerlattice rows, TTL override on `expiresAt`, and scheduler cleanup for legacy rows by `createdAt`. |
| `ownerNotificationRateLimits` | Recipient/store daily counters, hashes, date key, count, updatedAt, `expiresAt`, `retentionDays` for Answerlattice counters. | Owner-notification rate limiter. | Per-recipient/store delivery caps. | Prevent notification floods. | 2-day expiry for Answerlattice rows, TTL override on `expiresAt`, and scheduler cleanup for legacy rows by `updatedAt`. |
| `answerlattice_contactEnquiries` | Public contact form name, work email, phone, product URL, topic, message, consent, path/referrer/user-agent, hashed IP, status, timestamps, `expiresAt`, `retentionDays`. | Anonymous public contact API. | Sales/support follow-up and spam investigation. | Buyer/contact intake separated from MenuList. | 365-day expiry on new rows, TTL override on `expiresAt`, and scheduler cleanup for legacy rows by `createdAt`. |

The public contact API rate-limits before parsing, caps JSON at 8KB, validates form fields with Zod, ignores honeypot submissions, verifies Turnstile before writing to Answerlattice Firebase, and logs accepted/failed submissions with fixed runtime codes plus bounded enquiry/product-url metadata.

All current Next route files under `src/app/api/answerlattice`, `src/app/api/widget`, and `src/app/api/helpCenter`, plus adjacent Answerlattice routes under `src/app/api/revalidate/answerlattice` and `src/app/api/platform/answerlattice-intake`, now use bounded request-body readers instead of raw `request.json()`/`req.json()` parsing. Optional-body routes such as rebuild, cache revalidation, and Knowledge Intake commands use the optional bounded reader so empty bodies still normalize to `{}` without allowing unbounded reads.

## 5. Data flow map

### 5.1 Workspace setup and activation

1. Owner/onboarding creates or updates `stores/{sId}` with Answerlattice profile fields.
2. Workspace profile saves also update tenant summary and mark compiled context source versions changed. Client-triggered tenant-summary syncs use no-store same-origin manual-redirect browser requests, a bounded 2KB route payload, a 16KB bounded response acknowledgement, hashed user limiter identity, rate-limit before non-platform permission/sync work, and fixed-code bounded success/failure diagnostics.
3. Activation summary reads workspace, subscription, and bundle manifest state and returns a compact readiness view.
4. Scheduler discovers tenants from platform summary docs instead of scanning all workspace records.

### 5.2 Widget setup and public runtime

1. Owner generates an `al_*` widget key. Raw key is returned once; only hashes/prefix summaries are stored on `stores/{sId}.answerlatticeWidgetApi`.
2. Owner saves widget config and allowed origins on `stores/{sId}`.
3. Public widget config authenticates the API key, checks scope/origin/rate limits, and reads sanitized config plus the predictive summary. Public bundle refs are returned only when the dedicated bootstrap flag is enabled and every required ref validates; that flag is currently disabled because the widget does not consume the files. Runtime-status and optional-summary failures remain bounded and do not expose infrastructure errors.
4. Public/widget search calls `coreSearch`, which may use instant cache, Firestore search history cache, canonical retrieval, FAQ retrieval, or RAG. Inline image fallback and best-effort operation-log failures are logged with bounded metadata and do not leak raw workspace identifiers.
5. Every answer path writes `aiSearchHistory` for feedback/analytics; widget feedback validates search-history document IDs before updating the row and returns the authoritative persisted outcome. Answerlattice signal entity ID boundary: top-level signal `entityId` values are normalized through the governance document-ID boundary before `answerlattice_signalEvents` writes, with malformed IDs stored as `unresolved`. Negative feedback signal-emission failures are logged with bounded metadata and do not fail the feedback response.
6. An unresolved widget user may explicitly submit a reply email and optional details to `/api/widget/escalation`. After the same public widget admission contract, the server transaction reads the exact widget search-history row and deterministic ticket, creates the ticket when absent, links the history, and emits a deterministic best-effort `ESCALATION` signal. No notification is sent and automatic evaluator-driven suggestions remain separately flag-gated.
7. Protected Help Center search and article embedding routes rate-limit before parsing bounded JSON bodies, then validate request shape before calling search or embedding providers. Help Center search validation failures use the shared safe Zod detail payload rather than locally mapping issue messages.
8. Self-service onboarding rate-limits before Answerlattice account lookup, uses a 32KB bounded body before schema validation and durable writes, and logs failures with fixed-code bounded metadata.
9. Platform-only Answerlattice intake monitor retries parse a 2KB bounded trigger body, and owner cache revalidation parses an optional 4KB bounded body before revalidating public tags.

### 5.3 Governed answer lifecycle

1. Entities and canonical answers are created or updated through governance flows.
2. Releases/version docs and cache version docs represent answer set freshness.
3. Public answers API is read-only and uses canonical retrieval. Public entity registry reads compiled bundles first and falls back to scoped Firestore entity reads. Public signal API writes signals only; it never mutates canonical answers. Public API catch paths use bounded diagnostics, and JSON-writing routes reject oversized bodies before validation or retrieval/write work.
4. Mutation proposals are human-reviewed before canonical answer changes.
5. Manual AI governance operations such as draft regeneration, entity extraction, FAQ generation, and translation resolve session scope, check safe mode, rate-limit before permission/request-body/provider work, and log unexpected failures with bounded diagnostics. Translation is a disabled private draft generator: it stores source locale/hash and draft status only after a post-provider transaction confirms unchanged source and absent locale state; it does not bump public cache/context versions or enter customer projections. Manual draft regeneration validates mutation proposal IDs and related entity IDs with the shared Answerlattice governance document-ID boundary before proposal or entity document access. Manual draft helper, proposal status action DAL, and draft approval DAL normalize proposal IDs and related entity IDs before client proposal reads, proposal action transaction refs, entity reads, canonical-answer entity bindings, and audit metadata. Article embedding, article entity extraction, FAQ generation, translation, and public-content article reads validate article IDs with the shared Firestore document-ID boundary before direct `kb_articles` document access.
6. Audit logs preserve governance history.

### 5.4 Knowledge Intake lifecycle

1. An intake job is created under `answerlattice_knowledgeIntakeJobs`.
2. Sources are added from manual text, URLs, repeated replies, screenshots, audio, or video. Text is cleaned and common secrets are redacted before storage.
3. Media extraction reserves usage, calls AI extraction, logs an AI operation, stores extracted text as a source, and finalizes/refunds ledger rows.
4. Review items are generated and later published into KB articles, FAQs, product surfaces, or mutation proposals.
5. Summary docs keep owner/dashboard views compact.

### 5.5 Support loop

1. Users submit tickets, chat sessions, or feedback.
2. Tickets can emit signal events, send notifications, store attachments, and carry client debug/log context. A persisted resolved/closed lifecycle transition derives an idempotent resolution-event identity and may link a matched governed entity.
3. Feedback can emit signal events with summarized metadata.
4. Ticket-resolution extraction reads bounded, redacted signal evidence, treats the ticket conversation as untrusted input, validates any generated procedure, and selects either one exact active canonical target or a new-answer proposal path. Ambiguous active-answer matches require owner triage.
5. Matching pending ticket proposals merge only exact entity, mutation type, and canonical target lineage. New evidence resets stale generated draft fields and records a deterministic merge audit in the same transaction. Unrelated pending proposals block automatic creation rather than absorbing evidence.
6. Search misses and signals feed friction aggregation, support board sync, predictive triggers, and mutation proposal flows. Manual Support Board proposal creation uses a deterministic request identity and same-batch audit so retries cannot create changed duplicate work.
7. Implemented-proposal impact compares bounded, like-for-like signal windows for the 14 days before and after implementation. Saturated windows fail closed instead of publishing a partial improvement result.

### 5.6 Scheduler and summaries

1. `answerlatticeNightly` is the consolidated scheduled function exported from `functions-answerlattice/src/index.ts`.
2. The master scheduler acquires task and tenant leases in `platformSummary`.
3. Tenant work runs drift/proposal/friction/answer-evidence/support-board/intake/predictive/context-bundle tasks based on flags.
4. Run logs are written to `answerlattice_schedulerRunLogs`.
5. Summary docs such as coverage, answer evidence, friction snapshot, integration health, predictive triggers, support board summary, and Knowledge Intake summary are used to avoid raw reads in owner/runtime views.
6. Knowledge Intake review-item status edits update parent job counters directly instead of scanning every source and review item after each child edit.

## 6. Retention and compaction snapshot

| Surface | Existing retention/compaction | Gap |
| --- | --- | --- |
| Integration events/logs/rate counters | `expiresAt` plus Firestore TTL field overrides. | Good pattern to reuse elsewhere. |
| Signal events | Writers set a 365-day `expiresAt`; Firestore TTL deletes expired rows. | No per-tenant nightly cleanup query; legacy rows require the bounded TTL backfill. |
| Friction daily stats | Scheduler deletes stats older than 90 days. | Snapshot remains durable. |
| AI operation details | Default accounting-only. Detailed mode adds provider usage metadata, response presence/length, and `detailExpiresAt`; raw provider response text is not stored. | No Answerlattice TTL field override for AI detail subfields/docs found. |
| Query embeddings | 30-day expiry, TTL field override, stale-read best-effort delete with fixed bounded diagnostics on cleanup failure, and scheduler cleanup by `createdAt`. | Implemented. |
| Search history | 90-day expiry, TTL field override, bounded writer payload, and scheduler cleanup by `createdOn`. | Implemented for raw runtime retention. Long-term analytics should use compact summaries, not this raw collection. |
| Knowledge Intake sources/review items | Redaction and max field lengths. Raw media not retained after extraction. Review-item edits update parent job counters transactionally instead of full recounting. | Source text and review items remain durable because the current published-job workflow still returns them for review; closed-job compaction needs an explicit archive state/product retention decision. |
| Scheduler run logs | Bounded error/tenant arrays plus 90-day expiry and legacy cleanup by `startedAt`. | Implemented. |
| Generic notification logs | Dedup/rate queries, compact fields, 90-day expiry, TTL override, and legacy cleanup. | Implemented. |
| Owner notifications | Compact event/delivery/rate docs, 90-day event/delivery expiry, 2-day rate-limit expiry, TTL overrides, and legacy cleanup. | Implemented for Answerlattice rows only. |
| Contact enquiries | Sanitization, hashed IP, honeypot, public rate limit, 365-day expiry, TTL override, and legacy cleanup. | Implemented with a 365-day platform lifecycle. |
| Compiled context bundles | Manifest points to active/latest versions, in-memory proxy/cache, and scheduler Storage cleanup keeps active/last-ready plus previous two versions. | Implemented. |
| Chat/ticket attachments | Storage paths are tenant/store scoped. Chat images are retained after session mutations because URLs can be shared across sessions; ticket hard delete removes ticket documents and message attachments. | Chat scope-wide orphan reclamation remains deferred until bounded cross-session non-reference proof exists. |

## 7. Production interpretation

Firestore should keep Answerlattice's current app truth and compact read models:

- canonical answer truth;
- workspace/account truth;
- owner-approved content;
- audit/governance records;
- compact summaries and current operational state.

Firestore should not become the permanent raw warehouse for:

- every search/chat/support interaction forever;
- all query embedding cache rows forever;
- every scheduler run forever;
- every notification/contact/integration detail forever;
- all compiled bundle versions forever;
- large source/media/job detail after the workflow is closed.

The existing integration and signal TTL patterns, bounded friction cleanup, source-version manifests, and compiled context bundle architecture give Answerlattice the right direction. The missing work is to apply the same retention/compaction discipline to the remaining high-growth or sensitive operational collections.
