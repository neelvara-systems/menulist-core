# Compiled Context Distribution Website Boundary

This is supporting infrastructure, not a standalone public feature promise.

Safe supporting copy:

> Approved support context can be prepared as versioned, bounded runtime bundles. If a rebuild fails, Answerlattice preserves the previous ready version.

Public copy must not state that the widget currently loads compiled files. Public API and MCP remain rollout-gated at their own feature boundaries. Avoid latency, hit-rate, cost, zero-staleness, and universal source-sync claims until measured and verified.
