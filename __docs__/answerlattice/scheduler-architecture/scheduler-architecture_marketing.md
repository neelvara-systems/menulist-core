# Answerlattice Scheduler Architecture Marketing Notes

This is internal infrastructure, not a public marketing feature.

Allowed internal wording:

- Answerlattice runs governance checks automatically after each workspace's support day closes.
- Answerlattice keeps scheduler work centralized and cost-controlled.
- Answerlattice uses workspace-local time instead of one fixed global midnight.

Avoid public claims about guaranteed real-time processing. Answerlattice governance remains batch-oriented and deterministic.

