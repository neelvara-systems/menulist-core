# Menu Extraction Pipeline — Firebase

**Status:** Implemented
**Last Updated:** July 5, 2026

## Collections

| Collection | Change |
| --- | --- |
| `menuImageProcessingJobs` | Central queue for all extraction entry points. |
| `publicMenuDrafts` | Public drafts now receive extraction completion/failure from the central worker. |
| `menuLinkImportArtifacts` | Unchanged; authenticated link imports still store private artifacts before job creation. |
| `projects/{tId}/{sId}` | Project writes now require the project document to exist. |
| `messagingOnboardingSessions` | Unchanged watcher flow; destination metadata only. |
| `MENULIST_AI_OPERATIONS` | Platform extraction audit rows for real provider cost, token usage, and job context. |

## Rules

`menuImageProcessingJobs` client create is now denied. Owner surfaces create jobs through `POST /api/menu-extraction/jobs`; public and messaging jobs use Admin SDK.

Client reads, cancellation, and preview-review updates remain available through the existing owner job rules. Cancellation updates are field-restricted to status/timestamp fields so browser clients cannot mutate server-owned job payloads while cancelling.

The browser review helper also fails closed before any project write: it rejects missing jobs, non-`preview_ready` jobs, project mismatches, tenant/store mismatches, and user mismatches. Discard uses the same validation before marking a preview job cancelled.

## Storage Prefixes

The worker accepts only expected Storage prefixes:

- Owner upload: `projects/files/{tId}/{sId}/`
- Authenticated link import: `menuLinkImports/{tId}/{sId}/{projectId}/`
- Public draft: `publicMenuDrafts/{draftId}/`
- Messaging onboarding: `messagingOnboarding/{sessionId}/`

The worker also validates MIME type by source/destination before AI work. Public image drafts cannot use PDFs, while authenticated owner upload and link-import paths can still process PDF artifacts where those entry points allow them.

The lower-level Gemini upload helper re-checks the same Storage bucket/prefix contract immediately before fetching files for provider upload. It also validates the URL with the shared Functions public HTTPS/DNS target guard, allowing localhost only inside the Functions emulator, and rejects oversized source responses before Gemini upload using the shared job file-size limit. This keeps stale, manually created, or corrupted job payloads from causing the worker to fetch outside the expected Storage prefixes or buffer an oversized file even if they bypass the normal job-creation route.

The app-server menu-intake identity preflight helper also re-checks upload URLs immediately before reading files for identity analysis. Owner preflight files must stay under `projects/files/{tId}/{sId}/`; public create-menu identity files must stay under `publicMenuDrafts/{draftToken}/`; both paths must pass the shared app-server public DNS target guard before fetch, and the final fetch uses manual redirect handling. This adds one DNS validation for each readable preflight file and no Firestore reads/writes, Storage writes/deletes, job fields, rules, indexes, or Cloud Function changes.

## Cost

Owner uploads now add one protected API path before the same job write. The route verifies the authenticated tenant/store scope, applies a `FILE_UPLOAD` request gate, and rejects JSON bodies above 128KB before project reads, active-job queries, fingerprint work, or job writes. Normal new extractions still create one `menuImageProcessingJobs` document. Repeat uploads can now short-circuit before the AI-expensive rate limit, menu-intake identity check, and job write when the route finds a recent completed first-extraction project job with the same server-computed `sourceFingerprint`.

Menu Extraction retry job ID admission is cost-neutral for valid retries and fail-closed for malformed retry requests. `src/lib/menu-extraction/jobIdBoundary.ts` accepts only Firestore auto-ID shaped job IDs for `retriedFromJobId`; malformed, whitespace-mutated, path-shaped, or reserved IDs stop during request schema validation, and `loadRetryContext()` repeats the same normalizer before the original-job Firestore read. Malformed retry IDs fail before project reads, active-job queries, Storage metadata reads, identity preflight, AI-expensive rate-limit gates, provider work, job writes, or cleanup deletes.

Menu Extraction project ID admission is cost-neutral for valid owner upload, menu-link import, and identity-preflight requests. `src/lib/menu-extraction/projectIdBoundary.ts` preserves the existing bounded project ID shape while rejecting malformed, whitespace-mutated, path-shaped, or reserved Firestore document IDs before project document reads, menu-link Storage path construction, identity preflight context loading, active-job queries, Storage metadata reads, provider work, job writes, or cleanup deletes. The menu-intake identity server helper also re-normalizes the final project ID before the scoped project ref, so malformed helper inputs fail before Firestore access. This adds no Firestore reads/writes/deletes, Storage operations, provider calls, cache invalidations, rules, indexes, Cloud Function logic, Firebase deploy requirement, or Vercel deploy action.

Menu Intake Identity scope document-ID admission is cost-neutral for valid identity-preflight requests. `src/app/api/menu-intake-identity/route.ts` and `src/lib/menu-extraction/menuIntakeIdentityServer.ts` validate tenant/store scope with `normalizeMenuIntakeScopeDocumentId()` before tenant access checks, limiter hashing, scoped project/store context reads, Storage prefix checks, operation metadata, or provider work. Malformed tenant/store scope fails before Firestore access or provider calls; valid preflight requests keep the same reads and writes. This adds no Firestore reads/writes/deletes, Storage operations, provider calls, cache invalidations, rules, indexes, Cloud Function logic, Firebase deploy requirement, or Vercel deploy action.

June 29 limiter-key hardening is Firebase-cost neutral. The owner upload route and menu-intake identity route still use the same `FILE_UPLOAD`, `AI_EXPENSIVE`, and `AI_OPERATION` limiter profiles before downstream work, but owner, tenant, and store limiter key segments are HMAC-hashed before storage in Upstash. This adds no Firestore reads/writes/deletes, Storage operations, provider calls, cache invalidations, rules, indexes, schema changes, Cloud Function logic changes, owner-facing settings, or Firebase deploy requirement.

June 29 extraction response-size hardening is Firebase-cost neutral. `functions/src/logic/processMenuImages.ts` now reads fetched source responses through the shared Functions `readResponseUint8ArrayWithLimit()` helper, which checks declared `content-length`, cancels the response stream when it crosses `MENU_EXTRACTION_JOB_LIMITS.MAX_FILE_SIZE_BYTES`, and only then writes the temp file for Gemini upload. This adds no Firestore reads/writes/deletes, Storage writes/deletes, provider calls for valid files, cache invalidations, rules, indexes, schema changes, owner-facing settings, public routes, or job fields. Functions source changed for the extraction worker, so scoped Firebase Functions deploy is required after validation.

The owner-upload fingerprint uses Firebase Storage object metadata (`md5Hash` or `crc32c`) plus target languages, action, business type, and business category. It adds bounded Storage metadata reads for normal owner uploads and scans at most 50 completed jobs for the same project/user, but avoids a full provider extraction when the same owner re-uploads the same files to the same unchanged project within 24 hours. If Storage metadata lookup fails, the route logs bounded `menu_extraction_owner_upload_metadata_lookup_failed` diagnostics and continues without completed-job fingerprint reuse for that request. Duplicate newly-uploaded Storage objects are deleted when active/completed job reuse succeeds. No client-provided hash is trusted. This diagnostic change adds no new Firestore reads/writes/deletes, Storage writes/deletes, provider calls, cache invalidations, rules, indexes, schema changes, Cloud Function logic changes, owner-facing settings, Firebase deploy requirement, or Vercel deploy action.

July 5 desktop job-start debug breadcrumb cleanup is Firebase-cost neutral. `getProcessedFile.ts` still checks for an existing active job and calls `createMenuProcessingJob()` for new jobs, but no longer emits normal-path client debug logs with raw project/job identifiers. This adds no Firestore reads/writes/deletes, Storage reads/writes/deletes, provider calls, cache invalidations, rules, indexes, schema fields, Cloud Function logic changes, owner-facing settings, Firebase deploy requirement, or Vercel deploy action.

Public create-menu no longer runs extraction inside the API request. It adds one `menuImageProcessingJobs` write and normal worker status writes, while removing request-lifecycle AI work from the public route.

July 2 SAFE_MODE worker guard: after a pending extraction job is claimed, `processMenuImagesJob` reads the existing `ops_config/system` SAFE_MODE state through the Functions helper before any deterministic link parsing or Gemini provider work. When SAFE_MODE is active, the worker writes the existing failed-job shape with `error.code = SAFE_MODE_ACTIVE`, `retryable: true`, `retryAfterSeconds: 60`, fixed owner-safe pause copy, and normal timing fields. Public draft jobs also receive the existing failed extraction status update with fixed retry copy. This adds one cached Functions-side SAFE_MODE read per warm instance TTL before provider work, adds no collection/index/rules change, and avoids provider cost during SAFE_MODE. Functions source changed for `processMenuImagesJob`, so scoped Firebase Functions deploy is required after validation before live behavior changes.

Public create-menu preview polling can use `statusOnly=1`. It still costs one owner-bound `publicMenuDrafts` read per poll, but the response omits heavy `extractedData` until the draft is completed; the preview client then performs a single full read to load the final data for claim/review.

Public create-menu preview stored-error display boundary hardening is Firebase-cost neutral. Failed preview rendering now ignores stored `publicMenuDrafts.error` / `draft.error` text and uses fixed localized `CreateMenu.previewFailedFallback` copy from the website locale. This adds no Firestore reads/writes/deletes, Storage operations, Auth operations, Cloud Function logic, provider calls, route calls, cache invalidations, rules, indexes, schema changes, tenant-shape changes, owner-facing settings, Firebase deploy requirement, or Vercel deploy action.

Public create-menu success business-name query display boundary hardening is Firebase-cost neutral. The success page now normalizes the `name` query parameter before rendering it as business-name text, removes control characters, bounds display length, and logs only invalid-reason plus length metadata. This adds no Firestore reads/writes/deletes, Storage operations, Auth operations, Cloud Function logic, provider calls, route calls, cache invalidations, rules, indexes, schema changes, tenant-shape changes, owner-facing settings, Firebase deploy requirement, or Vercel deploy action.

Public create-menu claim target document-ID boundary hardening is Firebase-cost neutral. The claim route validates existing-account and newly-created tenant/store IDs with the shared Firestore document-ID guard and exact positive numeric MenuList ID guard before creating store, tenant, project-summary, or project document refs. This adds no Firestore reads/writes/deletes for valid claims, Storage operations, Auth operations, Cloud Function logic, provider calls, route calls, cache invalidations beyond the existing valid claim tags, rules, indexes, schema changes, tenant-shape changes, owner-facing settings, Firebase deploy requirement, or Vercel deploy action.

June 29 public create-menu browser response-parse hardening is Firebase-cost neutral. `src/app/(website)/create-menu/CreateMenuClient.tsx` caps upload/link POST response parsing at 8KB, logs `public_create_menu_response_parse_failed` / `public_create_menu_response_invalid`, and redirects only after a non-empty `draftId` is present. This adds no Firestore reads/writes/deletes, Storage operations, provider calls, source-acquisition calls, route calls, cache invalidations, rules, indexes, schema changes, Cloud Function logic changes, owner-facing settings, Firebase deploy requirement, or Vercel deploy action.

June 29 public create-menu limiter-key hardening is Firebase-cost neutral. The public entry, status polling, and claim routes keep the same limiter profiles and request ordering, but owner and draft key segments are HMAC-hashed before storage in Upstash. This adds no Firestore reads/writes/deletes, Storage operations, provider calls, cache invalidations, rules, indexes, schema changes, Cloud Function logic changes, owner-facing settings, or Firebase deploy requirement.

The legacy direct `processMenuImages` callable is intentionally disabled in code and returns `failed-precondition` instead of invoking Gemini. The production job queue remains the only supported AI extraction path, which keeps upload allowlists, tenant checks, identity checks, retry metadata, cleanup, and cache invalidation centralized. Historical June 11, 2026 callable-hardening deploy evidence targeted the retired `ecomsai` project only. Current retry evidence must use `npm run verify:functions-deploy-preflight` and the External Certification Runbook Gate 1 scoped deploy against `menulist-qa`, with production gated on QA evidence and explicit production deploy approval.

Linked-outlet review applies add one authenticated `POST /api/projects/outlet-save` call instead of direct browser `updateDoc`. That route uses existing Admin SDK validation for tenant/store access, local-only ID prefixes, outlet policy, and cache tag revalidation, avoiding a broad Firestore rules exception for linked-project `files` writes.

Linked-outlet review apply request and response hardening is Firebase-cost neutral. The browser calls `/api/projects/outlet-save` with the shared no-store, same-origin, manual-redirect request policy, caps the acknowledgement at 2MB, and requires a matching `success: true` save envelope before completing review apply state. This changes no Firestore reads/writes/deletes, Storage operations, provider calls, cache invalidations, route logic, rules, indexes, schema fields, Cloud Function logic, Firebase deploy requirement, or Vercel deploy action.

Extraction review apply ID admission is cost-neutral for valid review applies and discards. `src/lib/extraction/schemas.ts` validates review `projectId` and `jobId` through the existing Menu Extraction document-ID normalizers, and `src/lib/extraction/applyChanges.ts` repeats that check before client Firestore project or `menuImageProcessingJobs` document refs are built. Malformed, path-shaped, reserved, or non-auto-ID job IDs stop before project reads, job reads, linked-outlet saves, project writes, job completion writes, cache invalidation, MOL event writes, or discard writes. This adds no Firestore reads/writes/deletes for valid review requests, no Storage operation, no route call, no provider call, no Cloud Function logic, no rules/indexes/schema-field change, no owner-facing setting, no Firebase deploy requirement, and no Vercel deploy action.

Initial extraction remains a zero-unit owner operation. The worker still records platform-only provider telemetry in `MENULIST_AI_OPERATIONS`, including `jobId`, `tId`, `sId`, `uId`, `jobSource`, `destinationType`, `destinationId`, `jobMode`, token counts, paise-denominated `totalCharge`, and a real Firestore timestamp in `createdAt`.

Extraction job updates now include root `timings`, optional `sourceFingerprint`, and `result.summary` on existing status writes. This adds small scalar fields only; no new collection, index, or separate write path is introduced.

Client-side extraction job diagnostics now use `src/lib/firebase/menuProcessingDiagnostics.ts`. This changes only logging for job helper, development trigger, cancellation, duplicate-active-job, realtime status-hook failure paths, desktop/mobile upload callers, and PDF conversion failures. It adds no Firestore reads/writes, Storage operations, Cloud Function calls, AI provider calls, cache invalidations, job fields, indexes, or owner-facing settings.

June 29 client response-parse hardening is Firebase-cost neutral. `src/lib/firebase/menuProcessing.ts` caps `POST /api/menu-extraction/jobs` response parsing at 32KB and logs `menu_processing_job_start_response_parse_failed` / `menu_processing_job_start_response_invalid` before failing through the existing fixed job-start error. `src/lib/menu-intake-identity/client.ts` caps `POST /api/menu-intake-identity` response parsing at 32KB and logs `menu_intake_identity_response_parse_failed` / `menu_intake_identity_response_invalid` while preserving the existing upload-preflight fallback. This adds no Firestore reads/writes/deletes, Storage operations, provider calls, route calls, cache invalidations, API routes, public routes, rules, indexes, schema fields, Cloud Function logic, owner-facing settings, Firebase deploy requirement, or Vercel deploy action.

Menu-intake identity provider response parse diagnostics are Firebase-cost neutral. `src/lib/menu-extraction/menuIntakeIdentityServer.ts` still uses the existing Gemini identity preflight call and existing low-confidence fallback analysis when provider JSON is malformed or not object-shaped. Failed provider-response parsing logs `menu_intake_identity_provider_response_parse_failed` with bounded response-length, candidate-length, parse-stage, fence/object-fragment, and operation-shape metadata only. This adds no Firestore reads/writes/deletes, Storage operations, extra provider calls, route calls, cache invalidations, API routes, public routes, rules, indexes, schema fields, Cloud Function logic, owner-facing settings, Firebase deploy requirement, or Vercel deploy action.

June 30 extraction start request-policy hardening is Firebase-cost neutral. `src/lib/firebase/menuProcessing.ts` and `src/lib/menu-intake-identity/client.ts` now submit their existing protected POSTs with no-store cache, same-origin credentials, and manual redirect handling before bounded response parsing. This adds no Firestore reads/writes/deletes, Storage operations, provider calls, route behavior, cache invalidations, API routes, public routes, rules, indexes, schema fields, Cloud Function logic, owner-facing settings, Firebase deploy requirement, or Vercel deploy action.

App-side intake and protected job-route diagnostics now use the same helper for menu-intake operation-log failures, preflight completion/failure breadcrumbs, duplicate-upload cleanup failures, completed-job reuse breadcrumbs, owner job creation breadcrumbs, route-level job creation failures, and owner menu-link import cleanup failures. This changes diagnostics only; job creation writes, completed-job reuse behavior, Storage cleanup attempts, AI operation accounting, and owner responses are unchanged.

Mobile upload cap parity is Firebase-cost reducing and app-only. The mobile upload sheet now uses the same shared extraction file/page cap as desktop, counts existing pending project files plus selected files, passes remaining page slots into shared PDF conversion before browser canvas rendering, and rechecks the prepared-file count before any `uploadFile()` call. This avoids avoidable Storage writes and protected job-route rejections for over-limit mobile batches; it adds no Firestore reads/writes/deletes, Storage operations, provider calls, route calls, cache invalidations, rules, indexes, schema fields, Cloud Function logic, owner-facing settings, Firebase deploy requirement, or Vercel deploy action.

The DS-1 replacement policy is Firebase-cost protective and uses existing write paths. Extraction continues to append processed `project.files[]` because `files[].extractedData` is the live editor data. Oversized append attempts are rejected by the protected owner job route before AI work and newly uploaded owner source files are deleted on that rejection. Replacement uploads use the existing owner reset flow, which writes `files: []` and clears linked-outlet overrides when relevant before the owner uploads a fresh menu. This adds no new Firestore collection, index, Storage prefix, Cloud Function, rules change, owner setting, Firebase deploy requirement, or Vercel deploy action.

Desktop/mobile uploaded-file cleanup diagnostics are cost-neutral. Intake cancel, ignored-file cleanup, no-files-for-job cleanup, and existing-active-job reuse still attempt the same Storage deletes. Failed cleanup now logs bounded `menu_upload_uploaded_file_cleanup_failed` or `mobile_menu_upload_uploaded_file_cleanup_failed` diagnostics with cleanup reason and counts only. This adds no Firestore reads/writes/deletes, Storage operations beyond the already-attempted deletes, Cloud Function calls, provider calls, cache invalidations, rules, indexes, schema fields, owner-facing settings, Firebase deploy requirement, or Vercel deploy action.

June 30 extracted-profile project-default acknowledgement hardening is Firebase-cost neutral. Desktop Projects and Mobile Menu still attempt the same existing `updateProjectWithoutLoader()` write when extracted profile visual defaults fill missing project accent/image-background settings, but both callers now require `assertProjectUpdateSucceeded()` before local project state changes. This adds no Firestore reads/writes/deletes beyond the existing project write attempt, no Storage operations, no Cloud Function logic changes, no provider calls, no route calls, no API routes, no public routes, no cache invalidations beyond existing `updateProjectWithoutLoader()` behavior, no rules, no indexes, no schema changes, no owner-facing settings, no Firebase deploy requirement, and no Vercel deploy action.

June 30 upload-created project acknowledgement hardening is Firebase-cost neutral. Desktop and mobile upload flows still use the existing `addProject()` write when the owner creates a new menu during upload/link intake, but both callers now require `assertProjectUpdateSucceeded()` before selection, job creation, or upload flow state advances. This adds no Firestore reads/writes/deletes beyond the existing project create attempt, no Storage operation, no Cloud Function logic change, no provider call, no route call, no API route, no public route, no cache invalidation path beyond the existing project DAL, no rules, no indexes, no schema change, no owner-facing setting, no Firebase deploy requirement, and no Vercel deploy action.

June 30 mobile upload business-detail acknowledgement hardening is Firebase-cost neutral. The mobile upload sheet still uses the same existing `updateStore()` write when the owner accepts detected business details during intake, but it now requires `assertStoreUpdateSucceeded()` before local store state or success copy changes. This adds no Firestore reads/writes/deletes beyond the existing store update attempt, no Storage operation, no Cloud Function logic change, no provider call, no route call, no API route, no public route, no cache invalidation path beyond the existing store DAL, no rules, no indexes, no schema change, no owner-facing setting, no Firebase deploy requirement, and no Vercel deploy action.

Owner menu-link import cleanup diagnostics are cost-neutral in the success path. If route failure happens after artifact/Storage creation but before the extraction job document exists, the route still attempts the same best-effort artifact delete and Storage delete. Failed cleanup is now logged through bounded `menu_link_import_artifact_cleanup_failed` / `menu_link_import_storage_cleanup_failed` diagnostics only. This adds no Firestore reads/writes/deletes or Storage operations beyond the already-attempted cleanup operations, and it adds no rules, indexes, schema fields, Cloud Functions, owner-facing settings, or deploy requirements.

Menu-link render fallback diagnostics are cost-neutral. The Chrome render fallback is still optional and app-server local when enabled; failed rendering logs bounded `menu_link_import_render_fallback_failed` diagnostics with fixed `skip_rendered_html` fallback policy only. The Chrome render fallback already creates and removes a local user-data directory when enabled; failed removal logs bounded `menu_link_import_render_tmp_cleanup_failed` diagnostics only. This adds no Firestore reads/writes/deletes, Storage operations, provider calls, cache invalidations, rules, indexes, schema fields, Cloud Functions, owner-facing settings, Firebase deploy requirement, or Vercel deploy action.

Extraction review diagnostics now use the same helper for `applyExtractionChanges`, desktop/mobile review save and discard failures, and client redistribution missing-data failures. Normal comparison/apply/redistribution success paths stay quiet. Linked-outlet apply failures preserve only a coded failure and numeric status for internal diagnostics; owners see generic retry text. This adds no Firestore reads/writes, Storage operations, Cloud Function calls, AI provider calls, cache invalidations, job fields, indexes, owner-facing settings, or public route changes.

July 5 extraction review MOL diagnostics are Firebase-cost neutral. `applyExtractionChanges` still writes the acknowledged menu/project changes, linked-outlet saves through `/api/projects/outlet-save` when needed, updates the review job to `completed`, and triggers the same fire-and-forget MOL `EXTRACTION_APPLIED` event. If that non-blocking MOL event write fails, the helper logs bounded `menu_review_apply_mol_event_log_failed` diagnostics with project/job presence-length context, actor/tenant/store presence-length context, applied-change count, review mode, MOL version, and normalized source error metadata only. This adds no Firestore reads/writes/deletes beyond the already-attempted MOL event write, no Storage operation, no Cloud Function logic, no provider call, no cache invalidation, no rules/indexes/schema change, no owner-facing setting, and no Firebase deploy requirement.

Failed provider attempts also write `MENULIST_AI_OPERATIONS` rows with `status: failed`, `success: false`, `errorCode`, `retryable`, optional `retryAfterSeconds`, `promptVersion`, and zero `totalTokenCount`, `totalCharge`, and `unitsConsumed`. These rows are platform telemetry only; owners are not charged for failed extraction attempts.

Failed extraction audit rows store the fixed `errorMessage` value `Menu extraction failed`. The lower-level extraction helper logs and persists stable `MENU_IMAGE_*` failure codes plus bounded source error name/code/status metadata instead of raw provider, fetch, parser, retry, or runtime exception messages. This changes no Firestore collection, index, write count, Storage operation, owner setting, cache invalidation, or public route behavior.

The production Firestore trigger wrapper and emulator-only `dev_triggerProcessMenuImages` callable now log bounded trigger/request metadata only. This changes no Firestore collection, index, write count, Storage operation, Cloud Function invocation path, AI provider call, owner setting, cache invalidation, or public route behavior.

The `processMenuImagesJob` worker and `saveFilesToProject` persistence helper now log bounded job/project/store/tenant presence and length metadata, counts, fixed failure codes, and source error name/code/status only. They no longer log raw job IDs, project IDs, store IDs, tenant IDs, target-language arrays, full redistributed extraction payloads, full project update payloads, project paths, or raw exception messages. The diagnostic hardening changes logging only and adds no Firestore collection, index, write count, Storage operation, AI provider call, Cloud Function invocation path, cache invalidation, owner setting, public route, or rules behavior.

June 29 public draft status diagnostic hardening: `processMenuImagesJob` still attempts the same public draft `processing` / `failed` updates for `/create-menu` polling, but rejected draft status writes now log `PROCESS_MENU_IMAGES_JOB_PUBLIC_DRAFT_STATUS_UPDATE_FAILED` with bounded job/draft context and source error metadata instead of being silently swallowed. This adds no Firestore read/write/delete beyond the existing attempted draft update, no Storage operation, no AI provider call, no cache invalidation, no owner setting, no public route, no rules/index change, and no schema change. Functions source changed for `processMenuImagesJob`, so the scoped Functions deploy remains required after validation.

June 28 worker failure-classification hardening: `processMenuImagesJob` now derives failed-job `error.code`, `retryable`, and optional `retryAfterSeconds` from structured source error fields only. It no longer scans full exception text for classification or retry-delay parsing. This changes no Firestore collection, index, write count, Storage operation, AI provider call, Cloud Function invocation path, cache invalidation, owner setting, public route, or rules behavior; existing failed job rows keep the same fixed `Menu extraction failed` message.

June 28 summary-parity hardening: when extracted profile defaults replace a generic project name/description during project save, `saveFilesToProject` also merges those fields into the existing `platformSummary/projects_{sId}` summary row in the same transaction. This adds at most one existing summary-document merge write for affected first-extraction saves, no new collection, no new index, no Storage operation, no AI provider call, no Cloud Function invocation path change, no owner setting, no public route, and no rules behavior. The worker already refreshes the public store cache after first-extraction project saves, so public menu and OBP summaries see the mirrored name/description after cache refresh.

June 28 last-mile source-fetch hardening: `functions/src/logic/processMenuImages.ts` now rejects extraction file URLs before `fetch()` unless they match the expected Storage bucket and source/destination prefix and pass public DNS target validation. This adds one DNS validation for each valid file fetched for Gemini upload, no Firestore reads/writes, no Storage writes/deletes, no AI provider payload changes, no new Cloud Function invocation path, no cache invalidation, no owner setting, no public route, and no rules behavior. Functions source changed for the extraction worker, so scoped Firebase Functions deploy is required after validation.

June 28 extraction operation compaction hardening: when `AI_OPERATION_LOG_MODE` is `accounting_only`, `functions/src/logic/processMenuImages.ts` now writes `clientResponse.messagePresent` and `clientResponse.messageLength` instead of a stored `clientResponse.message` preview. The compact record still keeps language/category/item counts for owner transaction summaries. This changes no Firestore collection, document count, index, Storage operation, AI provider call, Cloud Function invocation path, cache invalidation, owner setting, public route, or rules behavior; it only reduces provider-derived text retained in the existing extraction audit row.

June 28 menu-link source metadata compaction: `src/lib/menu-link-import/sourceAcquisition.ts`, authenticated link import, and public create-menu link import now store `sourceTextPresent` and `sourceTextLength` in artifact/job/draft metadata instead of a raw `sourceTextPreview`. The private text artifact remains the AI extraction input. This changes no Firestore collection, document count, index, Storage write count, source fetch behavior, AI provider call, Cloud Function invocation path, cache invalidation, owner setting, public route, or rules behavior; it only reduces page-text retained in metadata documents.

June 29 deterministic menu-link diagnostic hardening: `functions/src/logic/menuLinkTextExtraction.ts` now caps source error names, source error codes, and source status values before logging deterministic text-extraction skip diagnostics. This changes no deterministic parsing behavior, accept/fallback threshold, extracted data shape, Firestore read/write count, Storage operation, AI provider call, Cloud Function invocation path, cache invalidation, owner setting, public route, or rules behavior.

June 28/29 menu-intake preflight source-fetch hardening: `src/lib/menu-extraction/menuIntakeIdentityServer.ts` rejects preflight file URLs before `fetch()` unless they match the expected Storage bucket/prefix and pass app-server public DNS target validation, then reads the validated response through the shared app-server bounded response helper. This adds one DNS validation for each readable preflight file, no Firestore reads/writes, no Storage writes/deletes, no job fields, no rules/indexes, no Cloud Function source changes, and no Firebase deploy requirement. This changes Next.js app-server code only; Vercel deployment was not run under the opt-in deploy guard.

July 5 menu-intake unreadable-file diagnostics are Firebase-cost neutral. `src/lib/menu-extraction/menuIntakeIdentityServer.ts` still skips an individual preflight file when fetch, redirect handling, or bounded response reading fails after Storage/prefix/network validation, and the full extraction path can still surface file-specific failures later. The degraded preflight skip now logs bounded `menu_intake_identity_preflight_file_unreadable` diagnostics only. This adds no Firestore reads/writes/deletes, no Storage writes/deletes, no job fields, no rules/indexes, no Cloud Function source changes, no provider-call change, no cache invalidation, no owner setting, and no Firebase deploy requirement. This changes Next.js app-server observability only; Vercel deployment was not run under the opt-in deploy guard.

Prompt version `parallel_v5` explicitly normalizes visible food labels before persistence: `VEG`/green-dot markers to `vegetarian`, `NON-VEG`/`NV`/red-dot markers to `non-vegetarian`, `GF` to `gluten-free`, `DF` to `dairy-free`, and `KETO` to `keto`. Food dietary labels are returned through structured `dietaryTags`, while legacy `tags` remains limited to explicit non-food audience labels such as `For Men`/`For Women`. Description strings are still normalized into multilingual description objects, extracted item attributes remain structured variant rows, and owner desktop/mobile item editors expose the same canonical food `dietaryTags` values through the shared metadata config.

New first-extraction project auto-save completions no longer write `result.combinedData` to the completed job document after `saveFilesToProject()` succeeds. They keep `result.summary`, quality, timing, transaction, capped provenance, and `result.dataPrunedReason = project_auto_saved_immediate`; public create-menu, messaging onboarding, and owner review flows still keep their required payloads. This changes the `processMenuImagesJob` Cloud Function logic and requires a scoped Function deploy for live effect, but adds no Firestore reads/writes beyond the existing completion update, no new collection, index, scheduled function, Storage operation, owner-facing setting, public route, or rules change.

The existing consolidated `menu_old_cleanup` maintenance task still prunes `result.combinedData` from legacy completed first-extraction project jobs older than two hours after `result.summary` exists. It scans the existing `status + completedAt` index with a 500-doc cap, then deliberately skips `skipProjectSave` jobs, non-project destinations, and non-first-extraction review jobs so public create-menu, messaging onboarding, and owner review flows keep their required payloads. No new collection, index, scheduled function, or Storage bucket was added.

Historical June 11, 2026 Functions deploy evidence targeted the retired `ecomsai` project and reached predeploy lint/build before Firebase Secret Manager validation was blocked by billing-disabled HTTP 403 for Gemini, WhatsApp, and Upstash secrets. Current retry evidence must not target `ecomsai`: treat that project as historical only.

Historical June 28-29, 2026 deploy attempts for bounded extraction helpers, deterministic link-text diagnostics, trigger-wrapper diagnostics, worker/project-save diagnostics, first-extraction summary parity, source-fetch target validation, extraction operation compaction, worker failure classification, response-size hardening, shared response-body helper coverage, and public draft status diagnostics reached Firebase predeploy lint/build and then failed before upload because Firebase could not read `menulist-qa` project metadata through Cloud Resource Manager: HTTP 403, caller does not have permission.

Do not reuse the older command shapes from those historical attempts. Current Menu Extraction retry evidence must start with `npm run verify:functions-deploy-preflight` and then use the External Certification Runbook Gate 1 scoped deploy against `menulist-qa`; if a smaller extraction-specific Functions subset is used, record the exact scoped target list and reason in the production-readiness audit before deploy retry. Production retry requires Gate 1 QA evidence and explicit production deploy approval.

Deployment of the July 2, 2026 SAFE_MODE worker guard was attempted with `firebase deploy --project menulist-qa --config firebase.json --only functions:processMenuImagesJob --non-interactive`. The predeploy lint/build completed, but Firebase failed to read `menulist-qa` project metadata through Cloud Resource Manager with HTTP 403: caller does not have permission.
