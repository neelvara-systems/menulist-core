# Configuration Safety Website Boundary

No website route or settings control is added for configuration safety.
Website capability claims remain derived from features actually enabled in
the released artifact and proven on the target environment.

Do not expose environment names, secrets, provider model identifiers, rollout
buckets, Firebase project internals, or operational kill-switch instructions
on public pages.
