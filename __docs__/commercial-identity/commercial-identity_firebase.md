# Commercial Identity Firebase

This change adds no collection, document, read, write, listener, index, rule,
Storage object, scheduled function, or backfill.

Commercial identity is server configuration until a settlement occurs. The
existing billing-document ledger stores the immutable supplier values from the
validated tax snapshot. Firestore is not used as a mutable legal-identity
configuration source.

## Cost Impact

Zero additional Firebase operations.
