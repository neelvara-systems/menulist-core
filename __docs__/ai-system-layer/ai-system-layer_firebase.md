# AI System Layer — Firebase Cost Tracking

**Feature:** Centralized AI Infrastructure for MenuList  
**Status:** ✅ CURRENT CODEBASE TRUTH — Existing ledgers plus daily provider health records
**Last Updated:** July 5, 2026

---

## Summary

- **Existing extraction audit ledger:** `MENULIST_AI_OPERATIONS`
- **Existing billable app-route ledger:** `menulistAiOperations/{tId}/{sId}/{docId}`
- **New MenuList health record:** `_health/aiProvider_gemini`
- **New Answerlattice health record:** `platformSummary/answerlatticeAiProviderHealth`
- **No new `aiUsageLog` collection:** That older design is not part of the active schema.

The AI System Layer should avoid creating a write per internal AI call unless the operation is billable, owner-visible, or needed for incident response. The daily provider health checks add only one small status write per product per UTC day.

---

## Firestore Operations

### Reads

| Operation | Backend | Trigger | Frequency | Docs Read | Notes |
| --- | --- | --- | --- | --- | --- |
| Route rate limit check | Upstash Redis | Protected AI app routes | Per guarded request | 0 Firestore | Existing route guard; not a Firestore read |
| Owner AI role permission check | Firestore/Admin SDK | Protected owner AI/capacity/status routes | Per guarded owner request | 1 existing store permission read | Rejects unauthorized users before capacity reads, media fetches, provider calls, task fanout, analytics reads, insight writes, operation writes, or credit consumption |
| Owner transaction-history page | Firestore Admin SDK | `GET /api/ai-operations` | Owner/platform request | Capped to requested page size, max 50 plus cursor lookup | Reads `menulistAiOperations/{tId}/{sId}` through an authenticated server route; owners receive allowlisted fields only and route diagnostics use bounded context |
| Scheduler health previous status | Firestore Admin SDK | Answerlattice health task | Once per scheduler run | 1 | Skips duplicate successful checks inside the same UTC day |

### Writes

| Operation | Collection/path | Trigger | Frequency | Docs Written | Notes |
| --- | --- | --- | --- | --- | --- |
| Extraction audit | `MENULIST_AI_OPERATIONS` | Menu extraction completion | Per extraction job | 1 | Existing detailed Cloud Functions audit record |
| Billable app-route operation | `menulistAiOperations/{tId}/{sId}` | Successful billable app-route AI output | Per charged operation | 1 | Existing route accounting path. Detailed mode stores usage metadata plus response presence/length only, not raw provider text. |
| MenuList provider health | `_health/aiProvider_gemini` | `menulistMaintenanceScheduler.ai_provider_health_check` | Daily | 1 | Records provider/model/latency/status/key stats |
| Answerlattice provider health | `platformSummary/answerlatticeAiProviderHealth` | `answerlatticeMasterScheduler.ai_provider_health_check` | Daily | 1 | Records provider/model/latency/status |

### Deletes

No new delete path was added for this hardening pass.

---

## Cost Estimate

### Daily Provider Health

| Resource | Operations/month | Monthly Cost Impact |
| --- | --- | --- |
| MenuList health writes | ~30 writes | Negligible |
| Answerlattice health reads | ~30 reads | Negligible |
| Answerlattice health writes | ~30 writes | Negligible |
| Gemini health prompts | ~60 tiny requests | Provider-billed, minimal |

The provider calls, not the health records, are the real production cost surface. Production scaling should be managed with billing, model-level quota monitoring, budget alerts, and quota increase requests.

---

## Security Rules

The health records are written with Admin SDK from Cloud Functions. Browser clients must not write provider health or operation accounting docs.

Existing rule coverage:

```text
firestore.rules
  match /MENULIST_AI_OPERATIONS/{docId}
  match /menulistAiOperations/{tId}/{sId}/{docId}
```

No new Firestore rules or indexes are required for the daily health records because they are Admin SDK writes and point reads by operators/schedulers.

---

## Current Accounting Boundaries

`MENULIST_AI_OPERATIONS` remains the detailed extraction audit collection. In `AI_OPERATION_LOG_MODE="accounting_only"`, Cloud Function extraction rows keep compact response counts plus message presence/length metadata instead of retaining the AI response message text.

`menulistAiOperations/{tId}/{sId}` remains the billable app-route operation ledger used by AI accounting paths. Owners read transaction history only through `/api/ai-operations`, which applies auth, rate limiting, tenant/store session scope, capped pagination, owner-safe field filtering, and bounded diagnostics for read failures and rate-limit events. The desktop transaction page's browser-side load/project-name lookup failures are diagnostic-only and use fixed `ai_transactions_*` codes with bounded filter/cursor/page metadata; this adds no Firestore reads/writes beyond the existing API read and existing read-only project summary lookup.

`menulistMaintenanceScheduler` runs `ai_operation_detail_cleanup` daily. It compacts expired detailed fields in both the top-level extraction audit collection and nested app-route operation ledgers when `detailExpiresAt` is due. In `AI_OPERATION_LOG_MODE="accounting_only"`, app-route operation rows store count/shape summaries for `clientResponse` instead of full generated text, translation maps, image arrays, or support-answer payloads. Full compact ledger document TTL/deletion is intentionally not enabled by this task because those rows remain the accounting/audit and owner transaction-history record.

June 29 app-route limiter-key hardening is Firebase-cost neutral. `/api/ai-operations` and `/api/analytics/weekly-narrative/generate-local` still use the same `DATA_READ` and `BATCH_OPERATION` limiter profiles before Firestore/provider work, but their owner, tenant, and store limiter key segments are HMAC-hashed before storage in Upstash. This adds no Firestore reads/writes/deletes, Storage operations, provider calls, cache invalidations, rules, indexes, schema changes, Cloud Function logic changes, owner-facing settings, or Firebase deploy requirement.

June 30 weekly narrative stored-analytics normalization is Firebase-cost neutral. `/api/analytics/weekly-narrative/generate-local` still reads the same current-week and previous-week chat analytics documents, calls Gemini once, writes the same `insights/{tId}/stores/{sId}/ai/weekly` document, and records the same best-effort AI operation. The route now bounds numeric counters, category text, and top-question scanning before building the prompt or fallback copy. This adds no Firestore reads/writes/deletes, Storage operations, provider calls beyond the existing narrative generation call, cache invalidations, rules, indexes, schema changes, Cloud Function logic changes, owner-facing settings, Firebase deploy requirement, or Vercel deploy action.

July 5 weekly narrative output boundary is Firebase-cost neutral. `/api/analytics/weekly-narrative/generate-local` still reads the same current-week and previous-week chat analytics documents, calls Gemini once, writes the same `insights/{tId}/stores/{sId}/ai/weekly` document, and records the same best-effort AI operation. The route now normalizes generated narrative/highlight/recommendation strings before that existing write, with capped text and deterministic fallback content. This adds no Firestore reads/writes/deletes, Storage operations, provider calls beyond the existing narrative generation call, AI accounting writes, credit consumption, cache invalidations, rules, indexes, schema changes, Cloud Function logic changes, owner-facing settings, Firebase deploy requirement, or Vercel deploy action.

June 30 SEO and Business Copy prompt-input normalization is Firebase-cost neutral. `/api/seo` and `/api/business-copy` still use the same bounded request bodies, Zod schemas, SAFE_MODE/rate-limit/capacity checks, Gemini calls, accounting writes, and response handling. The prompt builders now normalize text and list inputs before interpolation, with scalar/list caps and control/template-character stripping. This adds no Firestore reads/writes/deletes, Storage operations, provider calls beyond existing valid generation requests, cache invalidations, rules, indexes, schema changes, Cloud Function logic changes, owner-facing settings, Firebase deploy requirement, or Vercel deploy action.

July 5 text AI operation response summaries are Firebase-cost neutral. `/api/descriptions`, `/api/translations`, `/api/new-item-metadata`, `/api/business-copy`, `/api/seo`, `/api/reviews/suggest`, `/api/campaigns/caption`, and `/api/menu-card-export/design-advisor` still write the same AI operation row per successful billable request and consume credits in the same order. The route input now sends count/shape `clientResponse` summaries with `responseSummaryKind` markers instead of generated owner-facing text objects, and `src/lib/ai/operationLog.ts` preserves those pre-summarized payloads in `accounting_only` mode. This adds no Firestore reads/writes/deletes, Storage operations, extra provider calls, AI accounting writes, credit consumption, cache invalidations, rules, indexes, schema changes, Cloud Function logic changes, owner-facing settings, Firebase deploy requirement, or Vercel deploy action.

July 5 Campaign Caption provider-response parse diagnostics are Firebase-cost neutral. Empty, malformed non-object, malformed object-fragment, or non-object provider JSON now logs capped `campaign_caption_provider_response_parse_failed` or `campaign_caption_non_object_response` diagnostics with fixed `return_caption_generation_failed` policy and response-shape metadata only. The route still uses the existing bounded body admission, validation, permission gate, SAFE_MODE/rate-limit/capacity checks, Gemini call, phrase guard, response shape for valid output, `finalizeAiOperationAccounting()` write, and credit consumption order for valid output. Unusable provider responses still return the generic caption failure without a usable operation row or credit consumption. Bounded prompt summaries and caption response summaries in accounting input add no Firestore reads/writes/deletes, Storage operations, extra provider calls, AI accounting writes, credit consumption, cache invalidations, rules, indexes, schema changes, Cloud Function logic changes, owner-facing settings, Firebase deploy requirement, or Vercel deploy action.

July 5 new-item metadata provider-response parse diagnostics are Firebase-cost neutral. Empty, malformed non-object, or malformed object-fragment provider JSON now logs capped `new_item_metadata_provider_response_parse_failed` diagnostics with fixed `return_metadata_generation_failed` policy and response-shape metadata only. The route still uses the existing bounded body admission, validation, permission gate, SAFE_MODE/rate-limit/capacity checks, Gemini call, forbidden-field stripping, response handling, `finalizeAiOperationAccounting()` write, and credit consumption order for valid output. Unusable provider responses still return the existing generic metadata failure without a usable operation row or credit consumption. Shape-only local API response logs and bounded item/language summaries in accounting input add no Firestore reads/writes/deletes, Storage operations, extra provider calls, AI accounting writes, credit consumption, cache invalidations, rules, indexes, schema changes, Cloud Function logic changes, owner-facing settings, Firebase deploy requirement, or Vercel deploy action.

July 5 SEO provider-response parse diagnostics are Firebase-cost neutral. Empty, malformed non-object, or malformed object-fragment provider JSON now logs capped `seo_provider_response_parse_failed` diagnostics with fixed `return_seo_generation_failed` policy and response-shape metadata only. Local accounting-error logs now store bounded transaction/result summaries instead of full transaction objects. The route still uses the existing bounded body admission, validation, permission gate, SAFE_MODE/rate-limit/capacity checks, Gemini call, response normalization, `finalizeAiOperationAccounting()` write, and credit consumption order for valid output. Unusable provider responses still return the existing generic SEO failure without a usable operation row or credit consumption. This adds no Firestore reads/writes/deletes, Storage operations, extra provider calls, AI accounting writes, credit consumption, cache invalidations, rules, indexes, schema changes, Cloud Function logic changes, owner-facing settings, Firebase deploy requirement, or Vercel deploy action.

July 5 Business Copy provider-response parse diagnostics are Firebase-cost neutral. Empty, malformed non-object, or malformed object-fragment provider JSON now logs capped `business_copy_provider_response_parse_failed` diagnostics with fixed `retry_once_then_return_business_copy_failed` policy and response-shape metadata only. Local accounting-error logs now store bounded transaction/result summaries instead of full transaction objects. The route still uses the existing bounded body admission, validation, permission gate, SAFE_MODE/rate-limit/capacity checks, Gemini call/retry, response normalization, `finalizeAiOperationAccounting()` write, and credit consumption order for valid output. Unrecoverable first responses still retry once, and unrecoverable retry responses still return the existing generic Business Copy failure without a usable operation row or credit consumption. This adds no Firestore reads/writes/deletes, Storage operations, extra provider calls beyond the existing retry policy, AI accounting writes, credit consumption, cache invalidations, rules, indexes, schema changes, Cloud Function logic changes, owner-facing settings, Firebase deploy requirement, or Vercel deploy action.

July 5 app-route transaction DB local error diagnostics are Firebase-cost neutral. `/api/business-copy`, `/api/descriptions`, `/api/image-editing`, `/api/new-item-metadata`, `/api/seo`, and `/api/translations` now store bounded source-error metadata in local `TRANSACTION_DB_ERROR` logs instead of raw accounting exception objects. The routes still use the same `finalizeAiOperationAccounting()` calls, write counts, credit-consumption order, failure rethrow behavior, provider calls, and owner-facing outputs. This adds no Firestore reads/writes/deletes, Storage operations, provider calls, AI accounting writes, credit consumption, cache invalidations, rules, indexes, schema changes, Cloud Function logic changes, owner-facing settings, Firebase deploy requirement, or Vercel deploy action.

June 30 new-item metadata validated-payload hardening is Firebase-cost neutral. `/api/new-item-metadata` still uses the same bounded request body, Zod schema, SAFE_MODE/rate-limit/capacity checks, Gemini call, accounting write, and response handling. The route now sends the validated `item`, `targetLang`, and `sourceLang` objects to the prompt builder, rejects attribute price strings over 120 characters, and uses neutral `unspecified` prompt context when business type is missing. This adds no Firestore reads/writes/deletes, Storage operations, provider calls beyond existing valid metadata generation requests, cache invalidations, rules, indexes, schema changes outside request validation, Cloud Function logic changes, owner-facing settings, Firebase deploy requirement, or Vercel deploy action.

June 30 Menu Card design-advisor prompt normalization is Firebase-cost neutral. `/api/menu-card-export/design-advisor` still uses the same authenticated tenant-scoped route, 128KB body cap, Zod schema, Pro/Premium plan gate, AI capacity check, Gemini call, recommendation normalization, accounting write, and credit consumption order. The prompt builder now strips control/template characters, normalizes whitespace, caps scalar/list prompt text, caps warning/category scans, and serializes sanitized prompt payloads instead of raw menu-summary/preflight strings. This adds no Firestore reads/writes/deletes, Storage operations, provider calls beyond existing valid layout suggestions, cache invalidations, rules, indexes, schema changes, Cloud Function logic changes, owner-facing settings, Firebase deploy requirement, or Vercel deploy action.

June 30 Review Reply prompt-input normalization is Firebase-cost neutral. `/api/reviews/suggest` still uses the same disabled-feature gates, auth, tenant access, 16KB body cap, Zod schema, SAFE_MODE/rate-limit/capacity checks, Gemini call/fallback behavior, accounting write, and credit consumption order. The route now caps `businessType`, strips control/template characters from pasted review/business-type prompt inputs, escapes sanitized review text with JSON string serialization, and records sanitized prompt length/business type metadata. This adds no Firestore reads/writes/deletes, Storage operations, provider calls beyond existing valid reply suggestions, cache invalidations, rules, indexes, Cloud Function logic changes, owner-facing settings, Firebase deploy requirement, or Vercel deploy action.

June 30 Translation prompt-input normalization is Firebase-cost neutral. `/api/translations` still uses the same authenticated route, 1MB body cap, Zod schema, tenant/store session checks, linked-outlet policy check when `projectId` is present, SAFE_MODE/rate-limit/capacity checks, Gemini call, response normalization, translation coverage accounting, accounting write, and credit consumption order. The request schema now caps translation identifiers and values, and the prompt builder serializes a sanitized bounded copy of `inputJson` values while preserving original keys for response mapping and fallback. This adds no Firestore reads/writes/deletes beyond existing valid policy reads and accepted client-side project saves, no Storage operations, no provider calls beyond existing valid translations, no cache invalidations, no rules, no indexes, no Cloud Function logic changes, no owner-facing settings, no Firebase deploy requirement, and no Vercel deploy action.

June 30 Image Editing prompt-input normalization is Firebase-cost neutral. `/api/image-editing` still uses the same authenticated route, 64MB body cap, Zod schema, linked-outlet policy check, SAFE_MODE/rate-limit/capacity checks, scoped reference-image fetch guards, Gemini call, response handling, accounting write, and credit consumption order for valid edit requests. The route now rejects a missing generated edit prompt before provider work, and the prompt router sanitizes owner prompt text plus item name/category/description placeholders before template interpolation. This adds no Firestore reads/writes/deletes beyond existing valid policy reads and AI accounting writes, no Storage operations beyond existing valid scoped image reads, no provider calls beyond existing valid edit requests, no cache invalidations, no rules, no indexes, no Cloud Function logic changes, no owner-facing settings, no Firebase deploy requirement, and no Vercel deploy action.

June 30 Campaign Caption prompt-input normalization is Firebase-cost neutral. `/api/campaigns/caption` still uses the same authenticated route, 64KB body cap, Zod schema, tenant/store session check, SAFE_MODE/rate-limit/capacity checks, Gemini call, phrase guard, response handling, accounting write, and credit consumption order. The prompt module now strips control/template characters, normalizes whitespace, and caps item/business/language prompt fields before interpolation while resolving campaign/surface context through safe module-level maps. This adds no Firestore reads/writes/deletes beyond existing valid AI accounting writes, no Storage operations, no provider calls beyond existing valid caption requests, no cache invalidations, no rules, no indexes, no Cloud Function logic changes, no owner-facing settings, no Firebase deploy requirement, and no Vercel deploy action.

June 29 transaction-history response hardening is Firebase-cost neutral. Platform users still read the same capped query through `/api/ai-operations`, but the API now returns an explicit platform-visible allowlist instead of full operation documents. Raw provider responses, generation config, token-usage internals, raw batch/provider payloads, and tenant/store/user IDs are not sent to transaction-history browsers. Desktop and mobile platform debug panels keep accounting cost/token/model/project/file rows and no longer render full transaction JSON. Desktop image-processing transaction details no longer render raw `clientResponse` JSON for B2B tenants; they use the same extracted-row presentation as other tenants. This adds no Firestore reads/writes/deletes, Storage operations, provider calls, cache invalidations, rules, indexes, schema changes, Cloud Function logic changes, owner-facing settings, or Firebase deploy requirement.

June 30 AI-operation history client response hardening is Firebase-cost neutral. MenuList and Answerlattice AI-operation history DALs now cap transaction-history response parsing at 512 KB and require the paginated response shape before returning local transaction state. Malformed, oversized, rejected, or wrong-shape responses log bounded client diagnostics and use the existing fixed transaction-history failure path. This adds no Firestore reads/writes/deletes, Storage operations, provider calls, AI accounting writes, credit consumption, cache invalidations, rules, indexes, schema changes, Cloud Function logic changes, owner-facing settings, Firebase deploy requirement, or Vercel deploy action.

`AI_OPERATION_LOG_MODE="accounting_only"` stores no provider response object and stores only count/shape metadata for client responses. `AI_OPERATION_LOG_MODE="detailed"` stores sanitized usage metadata plus `responseTextPresent` and `responseTextLength` only for provider responses; raw provider response text is not retained in the operation document.

June 29 translation provider-response accounting hardening is Firebase-cost neutral. `/api/translations` still performs the same bounded body admission, validation, SAFE_MODE/rate-limit/capacity checks, Gemini call/retry, response normalization, `finalizeAiOperationAccounting()` write, and credit consumption, but it no longer pre-stringifies the Gemini response in the route transaction object before accounting/local development logging. The shared AI-operation serializer keeps provider response storage bounded to usage metadata plus response presence/length, and local development logs summarize response shape. This adds no Firestore reads/writes/deletes, Storage operations, provider calls, cache invalidations, rules, indexes, schema changes, Cloud Function logic changes, owner-facing settings, or Firebase deploy requirement.

Business copy, campaign caption, description, SEO, new-item metadata, translation, Menu Card design-advisor, single image generation, batch image trigger/helper, and image editing route diagnostic/accounting-input cleanup is Firebase-cost neutral. It adds no Firestore read/write/delete count, Storage operations, Cloud Function logic changes, provider calls, cache invalidations, rules, indexes, schema changes, owner-facing settings, or Firebase deploy requirement. Existing capacity checks, provider calls, accounting write count, credit consumption, local development logs, translation coverage accounting, media handling, design advice normalization, and owner success responses are unchanged; only production route failure/warning diagnostics, the batch-trigger no-prompt failure response shape, and selected operation ledger payload shapes changed.

June 30 owner AI route security-log boundary is Firebase-cost neutral. Business copy, campaign caption, descriptions, image generation/editing, batch image trigger, Menu Card design advisor, new-item metadata, review suggestion, SEO, and translation routes now use bounded user/request metadata for validation, tenant-scope, and outlet-policy `logger.security()` events instead of raw `buildSecurityContext()` output. Description, translation, new-item metadata, and batch-trigger validation attempted data also records bounded route metadata instead of raw item/project/job/language snippets. This adds no Firestore reads/writes/deletes, Storage operations, provider calls, AI accounting writes, credit consumption, cache invalidations, rules, indexes, schema changes, Cloud Function logic changes, owner-facing settings, Firebase deploy requirement, or Vercel deploy action.

June 30 campaign caption prompt-input normalization is Firebase-cost neutral. `/api/campaigns/caption` still uses the same auth, SAFE_MODE, rate limiting, bounded body admission, Zod validation, tenant check, capacity check, Gemini call, output phrase guard, response shape, accounting write, and credit consumption order. The prompt module now strips control/template characters from item/business/language prompt fields, caps prompt lengths, and clamps campaign type/surface lookups before interpolation. This adds no Firestore reads/writes/deletes, Storage operations, provider calls beyond existing valid caption generation, cache invalidations, rules, indexes, Cloud Function logic changes, owner-facing settings, Firebase deploy requirement, or Vercel deploy action.

June 29 description-generation client diagnostic cleanup is Firebase-cost neutral. It adds no Firestore reads/writes/deletes beyond the existing project persistence after successful generation, no Storage operations, no API routes, no provider calls beyond existing `/api/descriptions` requests, no cache invalidations, no rules, no indexes, no schema changes, no owner-facing settings, and no Firebase deploy requirement. Existing capacity handling, owner-visible copy, multi-outlet governance filtering, description merge behavior, and project persistence are unchanged; only modal/shared-client diagnostics changed.

AI capacity exhaustion lifecycle-message diagnostic cleanup is Firebase-cost neutral. It adds no Firestore reads/writes/deletes beyond the existing subscription credit transaction and existing lifecycle message-log attempt, no Storage operations, Cloud Function logic changes, extra provider calls, cache invalidations, rules, indexes, schema changes, owner-facing settings, or Firebase deploy requirement. Existing capacity math, balance writes, `CREDITS_EXHAUSTED` send attempt, and owner-facing capacity behavior are unchanged; only failed fire-and-forget lifecycle message diagnostics changed.

June 29 AI client response-parser hardening is Firebase-cost neutral. Shared `402` AI capacity responses now use an 8KB bounded JSON parser and Menu Card Export design-advisor browser responses use a 16KB bounded parser. Malformed or oversized responses log stable AI service diagnostics and keep existing owner-facing copy. This adds no Firestore reads/writes/deletes, Storage operations, provider calls, AI accounting writes, credit consumption, cache invalidations, rules, indexes, schema changes, Cloud Function logic changes, owner-facing settings, or Firebase deploy requirement.

June 30 AI client request-policy hardening is Firebase-cost neutral. Shared owner AI clients now send existing item metadata, SEO, description, translation, business-copy, image generation/editing, batch image trigger, and Menu Card design-advisor route calls with no-store cache policy, same-origin credentials, and manual redirect handling before existing capacity checks and bounded response parsing. This adds no Firestore reads/writes/deletes, Storage operations, provider calls, AI accounting writes, credit consumption, cache invalidations, rules, indexes, schema changes, Cloud Function logic changes, owner-facing settings, Firebase deploy requirement, or Vercel deploy action.

July 1 batch image Cloud Tasks config preflight is Firebase-cost neutral for rejected requests. `/api/image-generation/batch-trigger` now checks app-side Cloud Tasks config readiness before AI capacity reads or task fanout. Missing `FIREBASE_PROJECT_ID`, `FIREBASE_PROJECT_LOCATION`, `BATCH_IMAGE_GENERATION_QUEUE_ID`, `BATCH_IMAGE_GENERATION_WORKER_URL`, or `BATCH_IMAGE_GENERATION_WORKER_SECRET` marks the already-created batch job failed with owner-safe copy and logs a stable diagnostic. Valid configured runs keep the existing capacity check, Cloud Tasks enqueue, worker secret validation, worker provider call, Storage upload, accounting, and credit-consumption behavior. This adds no Firestore reads beyond the existing accepted batch-job status update path, no new writes for valid configured runs, no Storage operations, no provider calls, no cache invalidations, no rules, no indexes, no Cloud Function logic changes, no owner-facing settings, no Firebase deploy requirement, and no Vercel deploy action.

June 29 Help Center search response-parser hardening is Firebase-cost neutral. Help Chat and the AI Search modal now parse `/api/helpCenter/search-kb` responses through a shared 1MB bounded JSON parser and require the successful search envelope before updating UI state. This adds no Firestore reads/writes/deletes, Storage operations, provider calls, AI accounting writes, credit consumption, cache invalidations, rules, indexes, schema changes, Cloud Function logic changes, owner-facing settings, Firebase deploy requirement, or Vercel deploy action.

June 30 Help Center search request-policy hardening is Firebase-cost neutral. Help Chat and the AI Search modal now submit `/api/helpCenter/search-kb` with no-store cache, same-origin credentials, and manual redirect handling before the existing bounded response parser. This adds no Firestore reads/writes/deletes, Storage operations, provider calls, AI accounting writes, credit consumption, cache invalidations, rules, indexes, schema changes, Cloud Function logic changes, owner-facing settings, Firebase deploy requirement, or Vercel deploy action.

June 28 gateway structured-classification hardening is Firebase-cost neutral. It adds no Firestore reads/writes/deletes, Storage operations, API routes, provider calls beyond the existing Gemini calls, cache invalidations, rules, indexes, schema changes, owner-facing settings, or new env vars. Existing key rotation, hard-quota fast-fail, backoff retry, and final exception propagation behavior are intended to remain unchanged; retry decisions now use structured source error code/name/status/quota/limit indicators instead of raw provider message text. Cloud Functions source changed, so the affected Gemini consumer functions require Firebase Functions deploy; Next.js app-server gateway changes require normal Vercel deployment when app code is released.

Historical June 28, 2026 deploy evidence for gateway structured-classification hardening reached Firebase predeploy lint/build and then failed before upload because Firebase could not read `menulist-qa` project metadata through Cloud Resource Manager: HTTP 403, caller does not have permission. Do not reuse the older broad command shape from that attempt. Current retry evidence must route through `npm run verify:functions-deploy-preflight` and the External Certification Runbook Gate 1 scoped MenuList QA commands. Any additional Gemini consumer Function beyond the documented Gate 1 or source-file hardening subsets must be listed in the production-readiness audit before deploy retry.

June 28 shared app-route provider error helper hardening is Firebase-cost neutral and does not require Firebase deploy. It changes only `src/lib/ai/providerErrors.ts`, adds no Firestore reads/writes/deletes, Storage operations, Cloud Function logic changes, provider calls, cache invalidations, rules, indexes, schema changes, owner-facing settings, or new env vars. Help Center, widget search, article embedding, Answerlattice translation, and FAQ-generation rate-limit responses still use a 60-second fallback when the provider exposes no structured retry field. Vercel deployment was not run under the opt-in deploy guard.

`aiUsageLog` is not implemented. Do not add rules, indexes, cleanup jobs, or owner dashboards for it unless a separate source-backed cost-control implementation explicitly introduces that collection.

---

_Document Status: ✅ CURRENT — no new usage-log collection introduced_
