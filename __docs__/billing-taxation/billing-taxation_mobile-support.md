# Billing Taxation Mobile Support

The website setup modal is responsive and uses single-column fields on narrow screens. Existing owner PWA billing flows reuse the stored subscription billing profile for upgrades and enhancement packs.

The owner PWA must not display or edit public GSTIN/address fields as if they were billing details. A dedicated billing-profile editor can be added only against this same schema and server tax boundary.
