# Infrastructure Risk Tracker

**Purpose:** Centralized tracker for all infrastructure-level risks discovered during audits, production reviews, and implementation sessions. Cascade MUST update this file after every audit that identifies document size risks, storage growth issues, scaling concerns, or unbounded resource patterns.

**File:** `__docs__/production-readiness/infrastructure-risk-tracker.md`  
**Rule:** Rule 10.10 in `IDE_PROMPTS/MASTER-EXECUTION-PROMPT.md`  
**Last Updated:** March 13, 2026

---

## How This File Works

- After EVERY audit (Firebase cost, production, scalability, parity, system), Cascade checks for infrastructure-level risks
- Risks are categorized: **Document Sizes**, **Storage Growth**, **Collection Growth**, **Query Performance**, **Cost Scaling**
- Each risk has: Description, Severity, Source Audit, Affected Files, Recommended Fix, Status
- Status: `⚠️ OPEN` → `🔧 FIX PLANNED` → `✅ RESOLVED`
- Resolved items stay in the file (with resolution date + fix description) for historical reference

---

## Document Sizes

Risks related to Firestore document size limits (1MB max per document).

| #    | Risk                                                                                                                                                                                                                                    | Severity  | Collection / Path                  | Affected Files                                | Recommended Fix                                                                                                                                                              | Source Audit                                          | Status                                                                                                                                                                                |
| ---- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------- | ---------------------------------- | --------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| DS-1 | **Project `files[]` array grows unbounded** — Each extraction appends files with full `extractedData` (~20-100KB per file). Multiple re-uploads accumulate without cleanup. 10 re-uploads × 100KB = approaching 1MB limit.              | ⚠️ HIGH   | `projects/{tId}/{sId}/{projectId}` | `functions/src/logic/saveFilesToProject.ts`   | (a) Mark old file entries as `deleted: true` and strip their `extractedData` after new extraction succeeds, OR (b) Add pre-write size check — warn at 800KB, refuse at 950KB | AI Data Extraction Firebase Cost Audit (Mar 13, 2026) | 🔧 PARTIAL — Size guard added (900KB hard block, 700KB warning) in `saveFilesToProject.ts`. Prevents silent 1MB overflow. Root cause (unbounded growth) still needs cleanup strategy. |
| DS-2 | **Job document `result.combinedData` can be large** — Completed job stores full extracted menu data. 400-item menu = ~200KB. 1000+ items could approach limits. `rawBatchResponses` capped at 10KB/batch but `combinedData` has no cap. | ⚠️ MEDIUM | `menuImageProcessingJobs/{jobId}`  | `functions/src/logic/processMenuImagesJob.ts` | Strip `result.combinedData` from terminal jobs after data is saved to project (it's redundant at that point)                                                                 | AI Data Extraction Firebase Cost Audit (Mar 13, 2026) | ⚠️ OPEN                                                                                                                                                                               |

---

## Storage Growth

Risks related to Firebase Storage / Cloud Storage growing without bounds.

| #    | Risk                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Severity       | Storage Path                                                        | Affected Files                                                                                                 | Recommended Fix                                                                                                                                                                                                                                                                                                 | Source Audit                                          | Status  |
| ---- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------- | ------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------- | ------- |
| SG-1 | **Menu images never deleted** — Every upload persists forever. No lifecycle rules. At 100K extractions/month × 3 files × 3MB = 900GB/year. Old images for deleted/inactive stores still consume storage.                                                                                                                                                                                                                                              | ⚠️ MEDIUM-HIGH | `MenuListAi/project/files/{timestamp}-{uid}`                        | `src/components/templates/main-app/projects/` (upload flow)                                                    | Add Firebase Storage lifecycle rule: move images > 12 months to Coldline ($0.004/GB vs $0.026/GB). Consider deleting images for stores inactive > 2 years.                                                                                                                                                      | AI Data Extraction Firebase Cost Audit (Mar 13, 2026) | ⚠️ OPEN |
| SG-2 | **Legacy storage paths lack tenant isolation** — `MenuListAi/project/files/{fileId}`, `generated/{projectId}/{fileId}`, `edited/{projectId}/{fileId}`, `custom/{projectId}/{fileId}` only check `isAuthenticated()` in storage rules. Any authenticated user could theoretically read/write/delete files if they know the path. File paths include timestamps+UIDs (unpredictable) and download URLs include access tokens, so practical risk is LOW. | ⚠️ LOW         | `MenuListAi/project/files/*`, `generated/*`, `edited/*`, `custom/*` | `storage.rules` lines 122-156, `src/lib/storage/pathGenerator.ts` (migration utility exists)                   | Migrate to tenant-scoped storage pattern: `MenuListAi/project/files/{tId}/{sId}/{fileId}`. The `generateStoragePath()` utility at `src/lib/storage/pathGenerator.ts` already exists for this. Update storage rules to use `belongsToStore(tId, sId)` checks.                                                    | Security Surface Audit (Mar 13, 2026)                 | ⚠️ OPEN |
| SG-3 | **AI-generated images not optimized before upload** — Generated images are uploaded to Firebase Storage at full resolution from the AI model without running through `optimizeImage.ts` (which exists at `src/lib/image/optimizeImage.ts` and caps at 1500px/70% quality). At 100K stores × 30 images × ~400KB avg = 1.2TB/year unoptimized vs ~500GB optimized.                                                                                      | ⚠️ MEDIUM      | `MenuListAi/project/itemImages/*`                                   | `src/app/api/image-generation/generators.ts`, `src/lib/image/optimizeImage.ts` (exists, unused in AI gen flow) | Apply `optimizeImage()` to generated images before upload in the batch worker pipeline. Also apply to single-generation flow when user clicks "Upload". Estimated 40-60% storage reduction.                                                                                                                     | AI Image Gen ChatGPT Review (Mar 13, 2026)            | ⚠️ OPEN |
| SG-4 | **Discarded/unselected batch images may persist in storage** — When user selects some images and uploads, unselected images are deleted via `deleteFileByUrl()`. But if user abandons the modal without choosing (closes browser, navigates away), generated images in storage have no automatic cleanup. No TTL or lifecycle rule on `itemImages/` path.                                                                                             | ⚠️ LOW         | `MenuListAi/project/itemImages/*`                                   | `src/components/.../BatchImageGenerationResultView.tsx`                                                        | Add Firebase Storage lifecycle rule to auto-delete files in `itemImages/` older than 7 days that aren't referenced by any project document. OR add nightly cleanup job that scans `IMAGE_BATCH_PROCESSING_JOBS` for abandoned jobs (status=completed, older than 24h, never finished) and deletes their images. | AI Image Gen ChatGPT Review (Mar 13, 2026)            | ⚠️ OPEN |

---

## Collection Growth

Risks related to Firestore collections growing without TTL or cleanup.

| #    | Risk                                                                                                                                                                                                                                                                                                                       | Severity  | Collection                                | Affected Files                                     | Recommended Fix                                                                                                                                                                    | Source Audit                                          | Status  |
| ---- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------- | ----------------------------------------- | -------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------- | ------- |
| CG-1 | **`MENULIST_AI_OPERATIONS` unbounded growth** — 1 doc per extraction, no TTL, no cleanup. Docs contain full `clientResponse` (~50-200KB each). At 100K/month = 1.2M docs/year, ~120GB/year storage.                                                                                                                        | ⚠️ HIGH   | `MENULIST_AI_OPERATIONS`                  | `functions/src/logic/processMenuImages.ts:317-333` | Add TTL cleanup: delete docs > 90 days in nightly scheduler. OR strip `clientResponse` (heavy field) after 7 days, keeping only cost/token metadata.                               | AI Data Extraction Firebase Cost Audit (Mar 13, 2026) | ⚠️ OPEN |
| CG-2 | **`IMAGE_BATCH_PROCESSING_JOBS` unbounded growth** — 1 doc per batch job, no TTL. Terminal jobs (finished/discarded/cancelled) persist forever with full `itemsList[]` arrays containing generated image URLs and metadata. At 10K stores × 2 batches/month = 240K docs/year. Each doc can be 50-500KB depending on items. | ⚠️ MEDIUM | `IMAGE_BATCH_PROCESSING_JOBS/{tId}/{sId}` | `src/database/imageBatchProcessing/index.tsx`      | Add nightly cleanup: delete terminal-status jobs older than 30 days. Strip `itemsList` from jobs older than 7 days (image URLs are already copied to project documents on upload). | AI Image Gen ChatGPT Review (Mar 13, 2026)            | ⚠️ OPEN |

---

## Query Performance

Risks related to slow or expensive Firestore queries at scale.

| #    | Risk                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Severity | Query Location                                               | Affected Files                   | Recommended Fix                                                                                                                                                                                                                  | Source Audit                                          | Status  |
| ---- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | ------------------------------------------------------------ | -------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------- | ------- |
| QP-1 | **Monitoring dashboard reads 220 docs per load** — Health (100) + Quality (50) + Cost (50) + Feed (20) = 220 reads. OK for founder-only access. With auto-refresh at 60s interval → 316,800 reads/day, exceeds free tier.                                                                                                                                                                                                                                                                                            | ⚠️ LOW   | `src/database/ops/extraction.ts`                             | `src/database/ops/extraction.ts` | Add SWR caching with 5-min dedup. If auto-refresh added, use server-side pre-aggregation instead.                                                                                                                                | AI Data Extraction Firebase Cost Audit (Mar 13, 2026) | ⚠️ OPEN |
| QP-2 | **Flat collections lack tenant-level read filtering in Firestore rules** — `analytics`, `chatSessions`, `aiSearchHistory`, `queryEmbeddings`, `feedback`, `supportTickets`, `ownerControlUsage`, `imageBatchProcessingJobs`, `masterOperationalState` all use `allow read: if isAuthenticated()` without tenant-scoped checks. Any authenticated user could read docs from other tenants if they know/guess the doc ID. DAL code filters by `tId`/`sId` in queries, but Firestore rules don't enforce this boundary. | ⚠️ LOW   | Multiple flat collections in `firestore.rules` lines 193-263 | `firestore.rules`                | Add `resource.data.tId == request.auth.token.tenantId` checks to flat collection read rules. Requires verifying all docs in these collections consistently have `tId` field. Not extraction-specific — broader codebase pattern. | Security Surface Audit (Mar 13, 2026)                 | ⚠️ OPEN |

---

## Cost Scaling

Risks related to Firebase costs growing non-linearly or unexpectedly at scale.

| #    | Risk                                                                                                                                                                                                                                                                                                                                                                  | Severity | Area                   | Projected Cost at Scale                                                                | Recommended Fix                                                                                                                                                                                               | Source Audit                               | Status  |
| ---- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | ---------------------- | -------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------ | ------- |
| CS-1 | **Legacy storage with no tenant scoping has linear cost growth** — Without tenant-scoped paths, cannot implement per-tenant storage cleanup or lifecycle rules. As tenant count grows, all files are in a flat namespace making it impossible to delete a single tenant's files efficiently (e.g., GDPR compliance, account deletion).                                | ⚠️ LOW   | `MenuListAi/project/*` | `storage.rules`, `src/lib/storage/pathGenerator.ts`                                    | Migrate to tenant-scoped paths (see SG-2). This is a prerequisite for per-tenant storage cost tracking and efficient cleanup.                                                                                 | Security Surface Audit (Mar 13, 2026)      | ⚠️ OPEN |
| CS-2 | **AI image generation cost scales linearly with no prompt caching** — Common dishes (Cappuccino, Margherita Pizza, French Fries) generate near-identical prompts across thousands of stores. Each generates a fresh AI call (~$0.03). At 100K stores × 30 images = 3M AI calls = ~$90K. Prompt-hash caching could reuse results for identical prompts, saving 20-40%. | ⚠️ LOW   | AI generation pipeline | `src/app/api/image-generation/generators.ts`, `src/app/api/image-generation/prompt.ts` | Implement prompt-hash cache: `hash(prompt + preset) → cached image URL`. Before AI call, check cache. If hit, return cached image. Cache in Firestore or Redis with 90-day TTL. Only valuable at 10K+ stores. | AI Image Gen ChatGPT Review (Mar 13, 2026) | ⚠️ OPEN |

---

## Resolved Items

Items that were previously open but have been fixed. Kept for historical reference.

| #                       | Risk | Resolution | Resolved Date | Fix Applied |
| ----------------------- | ---- | ---------- | ------------- | ----------- |
| _No resolved items yet_ |      |            |               |             |

---

_Last Updated: March 13, 2026_  
_Updated By: Cascade (AI Image Generation ChatGPT Review Session)_  
_Note: Added SG-3 (unoptimized AI images), SG-4 (orphan batch images), CG-2 (batch job collection growth), CS-2 (AI generation cost scaling without prompt cache). Previous: DS-1 partially mitigated with pre-write size guard. CG-1 remains highest priority for pre-10K-extractions/month._
