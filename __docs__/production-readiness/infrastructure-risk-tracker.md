# Infrastructure Risk Tracker

**Purpose:** Centralized tracker for all infrastructure-level risks discovered during audits, production reviews, and implementation sessions. Cascade MUST update this file after every audit that identifies document size risks, storage growth issues, scaling concerns, or unbounded resource patterns.

**File:** `__docs__/production-readiness/infrastructure-risk-tracker.md`  
**Rule:** Rule 10.10 in `IDE_PROMPTS/MASTER-EXECUTION-PROMPT.md`  
**Last Updated:** July 17, 2026

---

## How This File Works

- After EVERY audit (Firebase cost, production, scalability, parity, system), Cascade checks for infrastructure-level risks
- Risks are categorized: **Runtime & Dependencies**, **Document Sizes**, **Storage Growth**, **Collection Growth**, **Query Performance**, **Cost Scaling**
- Each risk has: Description, Severity, Source Audit, Affected Files, Recommended Fix, Status
- Status: `⚠️ OPEN` → `🔧 FIX PLANNED` → `✅ RESOLVED`
- Resolved items stay in the file (with resolution date + fix description) for historical reference

---

## Runtime and Dependency Support

Risks related to unsupported shared runtimes, dependency compatibility, and production-only framework behavior.

| #    | Risk | Severity | Runtime / Area | Affected Files | Recommended Fix | Source Audit | Status |
| ---- | ---- | -------- | -------------- | -------------- | --------------- | ------------ | ------ |
| RT-1 | **The shared Next.js runtime migration is locally complete; release certification remains external** — The root app is frozen on Next.js 16.2.11 and React 19.2.8. Async request APIs, immediate public cache invalidation, ESLint 9 flat config, native Turbopack and Webpack builds, Serwist owner-worker isolation, and the Next 16 proxy convention are implemented. The remaining npm audit findings belong to controlled Fabric, latest-Next transitive, Firebase Admin, and ExcelJS families and are not represented as cleared. | 🟡 RELEASE EVIDENCE | Root Next.js runtime, React, lint, PWA, build/start, shared routing | `package.json`, `package-lock.json`, `next.config.js`, `src/proxy.ts`, `src/middleware/auth.ts`, `src/app/**`, `src/components/**`, `scripts/verification/verify-dependency-freeze.js`, `scripts/verification/verify-next-runtime-migration.js`, `scripts/verification/verify-next-build-compatibility.js` | Keep the exact freeze and local gates green. Before release, run an explicitly authorized Vercel preview, cross-product browser/device smoke, legacy-to-Serwist worker replacement tests, and production-host verification. Track the Fabric 7 security migration separately; do not use `npm audit fix --force`. | Shared Next.js runtime migration implementation and validation (July 24, 2026) | ✅ LOCAL MIGRATION VERIFIED — both production bundlers generate 439 pages, production-start Host matrix passes, and all focused cross-product gates pass. No Vercel or Firebase deploy was performed. |

---

## Document Sizes

Risks related to Firestore document size limits (1MB max per document).

| #    | Risk                                                                                                                                                                                                                                    | Severity  | Collection / Path                  | Affected Files                                | Recommended Fix                                                                                                                                                              | Source Audit                                          | Status                                                                                                                                                                                |
| ---- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------- | ---------------------------------- | --------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| DS-1 | **Project `files[]` append growth is bounded by an explicit reset/create-new policy** — Each extraction appends files with full `extractedData` because `files[].extractedData` is the live editor data. The system does not auto-strip old processed files; oversized append attempts fail safe and tell the owner to reset the menu or create a new menu before uploading more files. | ⚠️ HIGH   | `projects/{tId}/{sId}/{projectId}` | `src/components/templates/main-app/projects/index.tsx`, `src/components/templates/main-app/projects/ProjectDetails/ProjectConfirmModal.tsx`, `src/components/mobile/sheets/MenuUploadSheet.tsx`, `src/components/templates/main-app/projects/utils/pdfUtils.ts`, `src/app/api/menu-extraction/jobs/route.ts`, `functions/src/logic/saveFilesToProject.ts`, `src/data/shared/menuExtractionProjectSize.ts`, `functions/src/sharedData/menuExtractionProjectSize.ts` | Keep shared document-size limits mirrored, block client batches over the backend file/page cap before Storage upload, block projected oversized project append jobs before expensive AI work, clean up newly uploaded owner source files on that rejection, keep the final transaction save guard, and source-gate the owner-controlled reset path that clears files and extracted data. | AI Data Extraction Firebase Cost Audit (Mar 13, 2026); July 15 extraction cross-check | ✅ RESOLVED IN CODE — Save guard remains 900KB hard block / 700KB warning. Desktop and mobile upload cap pending extraction batches at the shared 15 file/page limit before Storage upload, and mobile passes remaining PDF page slots into conversion before canvas rendering. The owner job route reserves 100KB for one file and caps larger-batch headroom at 200KB against the same 900KB limit, so the supported maximum batch is not rejected solely by an unbounded linear estimate. Attempts without the reserved headroom are rejected before extraction and newly uploaded owner source files are deleted when the route sees them. Owner project reset writes `files: []` (and clears linked overrides), and the reset confirmation states that uploaded files and extracted data are cleared. Automatic stripping remains intentionally out of scope unless the editor data model changes. |
| DS-2 | **Job document `result.combinedData` can be large** — Completed project-save jobs used to store full extracted menu data even after the same data was saved into the project document. 400-item menus can be ~200KB and larger menus can approach document limits. `rawBatchResponses` remains capped, and extraction-only/review flows still need their active payloads. | ⚠️ MEDIUM | `menuImageProcessingJobs/{jobId}`  | `functions/src/logic/processMenuImagesJob.ts`, `functions/src/schedulers/menuJobCleanup.ts`, `scripts/verification/verify-menu-extraction-pipeline.js` | Keep immediate `result.combinedData` omission for first-extraction project auto-save jobs, retain `result.summary`, and keep legacy `menu_old_cleanup` pruning for older completed project jobs. Public draft, messaging, and preview/review jobs must keep payloads while downstream flows need them. | AI Data Extraction Firebase Cost Audit (Mar 13, 2026); July 1 production-readiness audit | ✅ RESOLVED IN CODE — first-extraction project auto-save jobs now write `result.summary` plus `dataPrunedReason: project_auto_saved_immediate` instead of `result.combinedData`; legacy completed project jobs remain covered by delayed cleanup. Pending Firebase Function deploy for live effect. |
| DS-3 | **Owner-notification metadata could inflate the queue event document** — Lifecycle callers attach trigger-specific metadata before an event is created. Without an application boundary, malformed or unexpectedly large metadata could approach Firestore's 1MB document limit and repeat provider/retry work. | ⚠️ LOW | `ownerNotificationEvents/{eventId}` | `src/data/shared/ownerNotificationDeliveryBoundary.ts`, `functions/src/sharedData/ownerNotificationDeliveryBoundary.ts`, `src/lib/owner-notifications/index.ts`, `functions/src/ownerNotifications/processor.ts` | Keep the mirrored 128KB full-event JSON admission check before Firestore creation and provider work. Do not raise it without measured template/metadata evidence. | July 16 owner notifications and messaging onboarding cross-check | ✅ RESOLVED IN CODE — app and Functions paths use the byte-identical event-size guard; circular or oversized payloads skip before queue/provider side effects. Pending approved app and scoped Functions release. |
| DS-4 | **Analytics daily maps retain owner-visible detail but can grow in width** — Search terms, item labels, hourly item clicks, and UTM content are compacted into top-N read models only after the daily document has accumulated them. | ⚠️ MEDIUM | `analytics/{tId}_{sId}_{projectId}_daily_{date}` | `src/lib/analytics/writePolicy.ts`, `src/lib/analytics/serverWrite.ts`, `functions/src/analytics/dashboardSummaryAggregation.ts`, `firestore.indexes.json` | Keep bounded field grammars and coalescing. The five verified non-query high-cardinality parents are now exempt from automatic single-field indexing; deploy that config. Do not add a Firestore read/transaction to every public event merely to enforce top-N. Introduce counts-only/basic-mode reductions only after preserving current owner search/item decisions and measuring daily document width. | SMB data inventory; July 16 feature-flow scale cross-check | ⚠️ PARTIALLY MITIGATED — index fanout fix is source-complete and deploy-pending; document-width policy remains an explicit measured follow-up rather than a behavior-breaking rewrite. |
| DS-5 | **CMI per-item maps and audit entries caused unused index fanout** — `menuIntelligence/{tId}_{sId}_{projectId}` is read only by exact document ID, but automatic indexing previously expanded its high-cardinality maps and audit array on every nightly replacement. | ⚠️ MEDIUM | `menuIntelligence/{tId}_{sId}_{projectId}` | `functions/src/intelligence/menuIntelligence.ts`, `src/lib/intelligence/dal.ts`, `firestore.indexes.json` | Keep direct-document access and exempt the six non-query high-cardinality fields from automatic single-field indexing. Reintroduce an index only with a concrete query and measured need. | July 17 feature 15 Firebase cost optimization pass | ✅ RESOLVED IN SOURCE — index exemptions are verifier-backed; scoped Firestore index deployment is pending release-operator access. |

---

## Storage Growth

Risks related to Firebase Storage / Cloud Storage growing without bounds.

| #    | Risk                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Severity       | Storage Path                                                        | Affected Files                                                                                                 | Recommended Fix                                                                                                                                                                                                                                                                                                 | Source Audit                                          | Status  |
| ---- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------- | ------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------- | ------- |
| SG-1 | **Legacy extraction uploads have a COLDLINE lifecycle artifact** — The repo now includes `infra/storage/menulist-storage-lifecycle.json`, which applies a Cloud Storage `SetStorageClass` rule for `MenuListAi/project/files/` after 365 days. The rule intentionally does not delete owner/source uploads because old project file previews and audit evidence may still reference them. | ⚠️ MEDIUM-HIGH | `MenuListAi/project/files/{timestamp}-{uid}`                        | `infra/storage/menulist-storage-lifecycle.json`, `scripts/verification/verify-storage-lifecycle-config.js`, `__docs__/production-readiness/launch-prerequisites.md` | Apply the lifecycle config to `menulist-qa` first, verify the bucket lifecycle JSON, wait for the Cloud Storage lifecycle propagation window, then apply to `menulist` when production deployment is approved. Verify with `npm run verify:storage-lifecycle`. | AI Data Extraction Firebase Cost Audit (Mar 13, 2026); July 1 production-readiness audit | ✅ RESOLVED IN CODE — pending bucket lifecycle apply |
| SG-2 | **Active tenant uploads use fail-closed scoped Storage paths; legacy project Storage paths are read-only** — The shared generator requires canonical tenant/store identity and valid path segments instead of falling back to platform or synthetic scope. Project uploads write `projects/{fileType}/{tId}/{sId}/{fileId}`; note attachments add validated `{noteId}/{fileId}` segments. Legacy `MenuListAi/project/...` objects remain authenticated-readable for old references, but new writes and deletes are denied in `storage.rules`. | ⚠️ LOW         | Active: `{collection}/{fileType}/{tId}/{sId}/{fileId}`; notes: `notes/documents/{tId}/{sId}/{noteId}/{fileId}`; legacy read-only: `MenuListAi/project/*` | `src/lib/storage/pathGenerator.ts`, `src/database/projects/index.ts`, `src/database/notes/index.ts`, `storage.rules`, `scripts/verification/test-storage-path-boundary.ts`, `scripts/verification/verify-storage-path-hardening.js` | Keep tenant uploads on the fail-closed shared generator, preserve explicit note identity/compensation, keep legacy project paths read-only, and verify with `npm run verify:storage-paths`. Deploy the already-pending Storage rules cutover to `menulist-qa` first, then production only after QA evidence and explicit production approval. | Security Surface Audit (Mar 13, 2026); July 1 production-readiness audit; July 2 storage rules cutover; July 13 datastore-first audit | ✅ RESOLVED IN CODE — current callers fail before Storage on malformed scope, note attachments no longer collide below `undefined`, and legacy project paths remain read-only. The application helper/Notes fix needs no Firebase deploy. The separate prior Storage-rules cutover remains pending; its latest QA attempt failed before upload with Service Usage HTTP 403. |
| SG-3 | **AI-generated images are profile-prepared and immutable before Storage save** — Accepted single-image uploads route through the shared browser media profile path. Batch workers use `prepareMediaImageAdmin()` and Admin create-only writes, so deterministic retries verify existing identity and reuse its Firebase download token instead of overwriting bytes. Prompt-cache destination copies use the same boundary. | ⚠️ MEDIUM      | `media/menuItem/{tId}/{sId}/*`                                      | `src/lib/storage/adminImmutableObject.ts`, `src/database/storage/uploadBase64MediaImageAdmin.ts`, `src/lib/media/prepareMediaImageAdmin.ts`, `src/lib/ai/imageGenerationPromptCache.ts`, `src/app/api/image-generation/batch-generation/route.ts` | Keep browser and server generated-image uploads on the shared profile/immutable contract; verify with `npm run test:admin-immutable-object-boundary`, `npm run verify:storage-paths`, and `npm run verify:ai-accounting`. Production effect waits for approved app/Vercel deployment. | AI Image Gen ChatGPT Review (Mar 13, 2026); July 1 and July 16 production-readiness audits | ✅ RESOLVED IN CODE — pending app/Vercel deploy |
| SG-4 | **Discarded/unselected public batch objects can accumulate, but current state cannot prove safe deletion** — Browser selection, one job row, and one project document do not cover acknowledgement-recovered writes, duplicated projects, or outlet projections that can retain the same URL. Direct browser cleanup and scheduled public-media deletion are therefore disabled; the scheduler still prunes `itemsList` after seven days and terminal job rows after 30 days. | ⚠️ MEDIUM      | `media/menuItem/{tId}/{sId}/*`                                      | `src/components/templates/main-app/projects/editorView/AiImageGenerator/batchImageGeneration/BatchImageGenerationResultView.tsx`, `functions/src/schedulers/imageBatchRetentionBoundary.ts`, `functions/src/schedulers/menulistMaintenanceScheduler.ts` | Measure QA/production orphan growth first. Add physical cleanup only with a global project/outlet/store reference ledger or equivalent exclusive-reference proof, a grace period, bounded work in the existing scheduler, and rollback/monitoring evidence. | AI Image Gen ChatGPT Review (Mar 13, 2026); July 16 global media lifecycle cross-check | ⚠️ ACCEPTED SAFE COST DEBT — metadata growth is bounded; physical media cleanup intentionally pending measured evidence and global reference authority. |

---

## Collection Growth

Risks related to Firestore collections growing without TTL or cleanup.

| #    | Risk                                                                                                                                                                                                                                                                                                                       | Severity  | Collection                                | Affected Files                                     | Recommended Fix                                                                                                                                                                    | Source Audit                                          | Status  |
| ---- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------- | ----------------------------------------- | -------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------- | ------- |
| CG-1 | **AI operation ledger detail growth is bounded by compact-not-delete retention** — `MENULIST_AI_OPERATIONS` and nested `menulistAiOperations/{tId}/{sId}` retain compact accounting/audit rows for platform cost review and owner transaction history, but raw/generated response details are not retained indefinitely. | ⚠️ MEDIUM | `MENULIST_AI_OPERATIONS`; `menulistAiOperations/{tId}/{sId}` | `functions/src/logic/processMenuImages.ts`, `src/lib/ai/operationLog.ts`, `src/lib/ai/operationPresentation.ts`, `src/app/api/ai-operations/route.ts`, `functions/src/schedulers/menulistMaintenanceScheduler.ts` | Keep `AI_OPERATION_LOG_MODE="accounting_only"` as the default, keep owner transaction reads behind `/api/ai-operations` allowlists and capped pagination, and keep `ai_operation_detail_cleanup` deployed for any detailed-mode rows. Full compact ledger row deletion remains intentionally disabled because those rows are the accounting/audit and owner transaction-history record. | AI Data Extraction Firebase Cost Audit (Mar 13, 2026); July 1 production-readiness audit | ✅ RESOLVED IN CODE — Extraction rows use `AI_OPERATION_LOG_MODE`; in accounting-only mode they retain compact response counts, message presence/length, summarized file metadata, cost/token/accounting fields, and no raw provider response object. App-route rows store count/shape summaries for `clientResponse` in `accounting_only` mode and use `detailExpiresAt` only in `detailed` mode. `menulistMaintenanceScheduler` runs `ai_operation_detail_cleanup` daily and deletes detailed `clientResponse`, `files`, `geminiResponse`, `rawBatchResponses`, and `detailExpiresAt` when due. Full compact ledger rows are retained by design for platform audit and owner transaction history. Pending Firebase Function deploy for live scheduler effect. |
| CG-2 | **`IMAGE_BATCH_PROCESSING_JOBS` unbounded growth** — Terminal jobs receive `itemsExpiresAt` and `expiresAt` from both browser and Admin SDK status-update paths. The maintenance scheduler prunes heavy `itemsList[]` payloads after 7 days and deletes terminal job docs after 30 days. A bounded legacy pass handles older terminal docs that predate the retention markers, and deterministic UTC-day page rotation covers active stores beyond the first 200 without a new cursor document. | ⚠️ MEDIUM | `imageBatchProcessingJobs/{tId}/{sId}` | `src/database/imageBatchProcessing/index.tsx`, `src/database/imageBatchProcessing/server.ts`, `functions/src/schedulers/imageBatchRetentionBoundary.ts`, `functions/src/schedulers/menulistMaintenanceScheduler.ts` | Keep the terminal retention markers and `image_batch_job_retention_cleanup` deployed. No new composite index or standalone scheduled Function is required. | AI Image Gen ChatGPT Review (Mar 13, 2026); July 1 production-readiness audit; July 15 scale cross-check | ✅ RESOLVED IN CODE — pending Firebase Function deploy. Latest QA deploy attempt on July 15, 2026 with `firebase deploy --project menulist-qa --config firebase.json --only functions:menulistMaintenanceScheduler --non-interactive` completed predeploy lint/build, then Cloud Resource Manager returned HTTP 403 caller permission before upload. |
| CG-3 | **Publish snapshots require bounded application retention because the nested path cannot use one native TTL collection group** — Full snapshots could outlive the 90-day window if cleanup repeatedly covers only an early store slice. Linked-outlet snapshots previously also captured raw outlet-local state and oversized payloads relied on Firestore rejection. | ⚠️ MEDIUM | `menuSnapshots/{tId}/{sId}/{snapshotId}` | `src/database/projects/index.ts`, `src/lib/menu/menuSnapshotBoundary.ts`, `functions/src/schedulers/menulistMaintenanceScheduler.ts`, `functions/src/schedulers/retentionStorePageBoundary.ts` | Keep the 900 KiB preflight and resolved linked snapshot path. Use the existing leased cleanup task with deterministic bounded all-store paging, include inactive stores, and keep per-store deletes capped. Do not claim a `menuSnapshots` collection-group TTL policy while store IDs are the terminal collection names. | Menu correctness/quality/trust audits (July 16-17, 2026) | ✅ RESOLVED IN SOURCE — deterministic all-known-store cleanup is verifier-backed; scoped maintenance Function deploy and live expiry evidence remain pending owner/release access. |

---

## Query Performance

Risks related to slow or expensive Firestore queries at scale.

| #    | Risk                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Severity | Query Location                                               | Affected Files                   | Recommended Fix                                                                                                                                                                                                                  | Source Audit                                          | Status  |
| ---- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | ------------------------------------------------------------ | -------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------- | ------- |
| QP-1 | **Extraction monitor dashboard read dedupe is active** — The dashboard already uses `getExtractionDashboardSnapshot()` so health, quality, and job feed reuse one job query. July 1 hardening adds SWR with a 5-minute dedupe window to `/ops/extraction`, so duplicate mounts/revalidations in that window reuse the cached snapshot. Cache misses and explicit Refresh still read up to 150 job docs + 100 cost docs. Auto-refresh remains disabled. | ⚠️ LOW   | `src/components/templates/main-app/platform/extractionMonitor/index.tsx`, `src/database/ops/extraction.ts` | `src/components/templates/main-app/platform/extractionMonitor/index.tsx`, `scripts/verification/verify-menu-extraction-pipeline.js` | Keep the 5-minute SWR dedupe contract. If automatic refresh or broader operator access is added, move the dashboard to server-side pre-aggregation before enabling it. | AI Data Extraction Firebase Cost Audit (Mar 13, 2026); July 1 production-readiness audit | ✅ RESOLVED IN CODE |
| QP-2 | **Flat collections lacked tenant-level read filtering in Firestore rules** — `analytics`, `chatSessions`, `aiSearchHistory`, `feedback`, `supportTickets`, `ownerControlUsage`, `imageBatchProcessingJobs`, `decisionBlocks`, `menuIntelligence`, and `masterOperationalState` now enforce platform or tenant/store-scoped rules. `queryEmbeddings` is server/admin only. | ✅ FIXED | Multiple flat collections in `firestore.rules`                 | `firestore.rules`, `firestore-answerlattice.rules` | Implemented tenant/store helper checks, doc-id fallback checks for compound IDs, server-only query embeddings, and Answerlattice-scoped chat/support/KB/cache-version rules. Deployed ecomsai Firestore rules on May 18, 2026. | Security Surface Audit (Mar 13, 2026); Firebase rules hardening pass (May 18, 2026) | ✅ CLOSED |
| QP-3 | **Digital Screen listener mirrors exposed bearer tokens and admitted anonymous collection reads** — Predictable `platformSummary/screen_{storeId}` documents contained `screenToken`, undermining token entropy and allowing public query/list exposure of listener rows. Current source removes the token, permits exact anonymous `get` only, and provides an ordered guarded backfill. | ⚠️ HIGH | `platformSummary/screen_{storeId}` | `src/lib/screen/publicScreenState.ts`, `src/lib/screen/serverScreenInvalidation.ts`, `functions/src/logic/publicCacheRevalidation.ts`, `firestore.rules`, `scripts/backfill-digital-screen-public-mirrors.ts` | Deploy token-free app and Functions writers, run/verify the exact-project mirror backfill, then deploy the tightened rule. Never deploy the rule first or allow an old writer to recreate the legacy shape. | Digital Screens end-to-end audit (July 16, 2026) | ✅ RESOLVED IN CODE — ordered QA and production rollout pending |
| QP-4 | **Reseller client caps were applied before deterministic ordering, and shared entitlement repair was hidden behind the reseller flag** — The platform/reseller client list could return an arbitrary capped subset instead of the newest clients, while ordinary Razorpay entitlement mirror failures could remain pending whenever the reseller dashboard was disabled. | ⚠️ MEDIUM | `subscriptions` reseller client queries and `billingEntitlementSyncPending` repair | `src/app/api/reseller/clients/route.ts`, `functions/src/schedulers/menulistMaintenanceScheduler.ts`, `firestore.indexes.json` | Keep `createdOn desc` ordering in Firestore before the existing 100/200-row cap, retain the two scoped composites, and run the bounded shared entitlement repair before the reseller-only expiry branch. | Reseller-assisted onboarding, licenses, and billing scale audit (July 17, 2026) | ✅ RESOLVED IN SOURCE — scoped index and maintenance Function deployment remain pending |
| QP-5 | **Report Lead Ops scanned unrelated contact enquiries and could hide matching leads behind the shared 120-row cap** — The accepted contact write already stores queryable `sourceKind`, but the ops route ordered all enquiries and filtered report leads in memory. | ⚠️ MEDIUM | `landingPageEnquiries` report-lead triage | `src/app/api/ops/report-leads/route.ts`, `firestore.indexes.json`, `src/components/templates/main-app/platform/reportLeadMonitor/index.tsx` | Query `sourceKind == shareable_tool_report` ordered by `createdOn desc` through one scoped composite. Retain the manual refresh, current-user authorization read, rate limit, 120-row cap, and incomplete-history disclosure. | Public Truth Tools and Report Lead Ops scale audit (July 17, 2026) | ✅ RESOLVED IN SOURCE — scoped Firestore index deployment remains pending |

---

## Cost Scaling

Risks related to Firebase costs growing non-linearly or unexpectedly at scale.

| #    | Risk                                                                                                                                                                                                                                                                                                                                                                  | Severity | Area                   | Projected Cost at Scale                                                                | Recommended Fix                                                                                                                                                                                               | Source Audit                               | Status  |
| ---- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | ---------------------- | -------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------ | ------- |
| CS-1 | **Active project storage is tenant-scoped; legacy objects remain cost debt but cannot grow through legacy writes** — New project fallback uploads use `projects/files/{tId}/{sId}/{fileId}`, so tenant/store cleanup is possible for newly written project uploads. Existing `MenuListAi/project/*` objects remain authenticated-readable for compatibility, but legacy writes/deletes are denied so the old path cannot keep accumulating new project uploads.                                | ⚠️ LOW   | Active: `projects/files/{tId}/{sId}/{fileId}`; legacy read-only: `MenuListAi/project/*` | `src/database/projects/index.ts`, `storage.rules`, `infra/storage/menulist-storage-lifecycle.json`                                    | Keep scoped active writes plus the legacy COLDLINE lifecycle artifact, keep legacy project Storage paths read-only, and deploy the scoped Storage rules update to QA first.                                                                                 | Security Surface Audit (Mar 13, 2026); July 1 production-readiness audit; July 2 storage rules cutover      | ✅ RESOLVED IN CODE — current project fallback uploads write tenant-scoped paths, and legacy project Storage paths are read-only. Pending scoped Firebase Storage rules deploy for live effect; latest `menulist-qa` deploy attempt failed before rules upload with Service Usage HTTP 403: project not found or permission denied. |
| CS-2 | **AI image generation prompt caching uses store-owned media copies and bounded source retention** — Common dishes can generate near-identical prompts across stores, but a naive cache that returns the first generated `media/menuItem/{tId}/{sId}/...` URL would attach another store's Storage object to the requester. The media URL is public-readable, but write/delete ownership, cleanup, and lifecycle remain tied to the original tenant/store. | ⚠️ LOW   | AI generation pipeline | `src/app/api/image-generation/batch-generation/route.ts`, `src/lib/ai/imageGenerationPromptCache.ts`, `functions/src/schedulers/menulistMaintenanceScheduler.ts`, `src/app/api/image-generation/generators.ts`, `src/app/api/image-generation/prompt.ts`, `src/database/storage/uploadBase64MediaImageAdmin.ts`, `storage.rules` | Keep the batch non-reference prompt cache server-only: cache sources live under `system/aiImagePromptCache/`; copies cached source bytes into the requesting store's own `media/menuItem/{tId}/{sId}/...` path; cache-hit AI operations record `unitsConsumed: 0`; expired prompt-cache source objects and `aiImagePromptCache` docs are cleaned by `ai_image_prompt_cache_cleanup`; do not implement tenant-URL reuse; single-image browser draft generation remains uncached because it returns base64 previews and performs no Storage write until owner acceptance. | AI Image Gen ChatGPT Review (Mar 13, 2026); July 1 production-readiness audit | ✅ RESOLVED IN CODE — batch worker cache implemented for one-prompt, one-image, non-reference generations; cache hits copy into store-owned media paths and record free AI operation rows. Cache version 2 uses immutable source versions, transactionally replaces the active row, and the scheduler conditionally deletes a row only after current source/expiry revalidation, preventing refresh-versus-cleanup loss. Single-image browser draft generation remains intentionally uncached because caching it would create server-owned draft Storage objects before owner acceptance. Pending Firebase Function deploy for live cleanup effect; prior QA deploy attempts passed predeploy lint/build, then Cloud Resource Manager returned HTTP 403 caller permission. |
| CS-3 | **Store OBP content maps were automatically indexed even though runtime never filters or orders by them** — Gallery URLs, localized public copy, business-copy metadata, controlled attributes, and hours created avoidable single-field index entries on owner saves and consumed index storage as store content grew. | ⚠️ LOW | `stores` owner/public identity document | `firestore.indexes.json`, `src/database/stores/index.tsx`, `src/app/client/obp/*` | Keep routing/query scalars indexed, but exempt `publicPresence`, `businessCopyMeta`, `businessAttributes`, and `workingHours` as whole nested maps. Add a targeted index only if a future bounded query genuinely needs one of their child fields. | Store identity and OBP Firebase cost audit (July 17, 2026) | ✅ RESOLVED IN SOURCE — scoped Firestore index deployment remains pending |
| CS-4 | **Presentation-only project saves created a multi-outlet observation write despite being explicitly non-operational** — Theme/layout/background publishes already skipped the operational-version signal, but still queued one `menuChangeLog` revision row, adding write volume and audit noise with no outlet action value. | ⚠️ LOW | Project design save and Menu Observation Layer | `src/database/projects/index.ts`, `src/lib/multiOutlet/masterUpdateDiff.ts`, `src/lib/multiOutlet/molEvents.ts` | Compute operational status once and reuse it for both optional writes. Keep observation and signal writes for actual item/price/availability/variant/category changes; skip both for cosmetic presentation-only saves. | Menu design and customer presentation cost audit (July 17, 2026) | ✅ RESOLVED IN SOURCE |
| CS-5 | **Store time-slot preset arrays were automatically indexed without any runtime query using them** — Each preset save could create avoidable array/nested-map index entries even though preset truth is always loaded from the exact store document. | ⚠️ LOW | `stores.timeSlotPresets` | `firestore.indexes.json`, `src/database/stores/index.tsx`, `src/database/projects/index.ts` | Keep the existing bounded project cascade, but exempt the store preset array from automatic single-field indexing. Add a reference read model only if measured production scans justify its extra write contract. | Working hours and time slots Firebase cost audit (July 17, 2026) | ✅ RESOLVED IN SOURCE — scoped Firestore index deployment remains pending |
| CS-6 | **Temporary-status maps were automatically indexed despite having no query or history use** — Manual and special-menu set/clear operations paid nested-field index fanout even though every consumer already reads the exact store document and applies expiry in memory. | ⚠️ LOW | `stores.tempStatus` | `firestore.indexes.json`, `src/app/api/store/temp-status/route.ts`, `src/lib/tempStatus/statusBoundary.ts` | Exempt the bounded map from automatic single-field indexing. Keep expiry as a read-time correctness boundary; do not add a TTL worker or event collection for one optional store field. | Temporary Status Firebase cost audit (July 17, 2026) | ✅ RESOLVED IN SOURCE — scoped Firestore index deployment remains pending |
| CS-7 | **Public Menu Entry retained an obsolete cleanup composite and indexed large draft payload maps** — Cleanup no longer filters on `claimed`, while extracted menu/profile/source/attribution data is never used for Firestore filtering or ordering. Index storage and write fanout therefore grew with short-lived extraction payload size without serving a query. | ⚠️ MEDIUM | `publicMenuDrafts` | `firestore.indexes.json`, `src/app/api/public/create-menu/route.ts`, `functions/src/schedulers/menulistMaintenanceScheduler.ts` | Remove the `claimed + expiresAt` composite, retain scalar indexes used by the bounded owner-dedupe and expiry queries, and exempt the four large payload maps. Keep the existing capped cleanup because unclaimed Storage objects require coordinated deletion. | Public Menu Entry Firebase cost audit (July 17, 2026) | ✅ RESOLVED IN SOURCE — scoped Firestore index and maintenance Function deployments remain pending |
| CS-8 | **Starter activation evidence paid nested-map index fanout without a query consumer** — Each acknowledged link/share/download action updates the store-local evidence map, but setup and concierge surfaces always consume it after exact store resolution. | ⚠️ LOW | `stores.starterActivationSignals` | `firestore.indexes.json`, `src/database/stores/index.tsx`, `src/lib/onboarding/starterActivation.ts` | Exempt the parent map from automatic single-field indexing. Preserve the current exact store read and one-write acknowledgement path; add a targeted child-field index only if a future bounded cross-store query is implemented. | Menu Setup Progress and Activation Concierge Firebase cost audit (July 17, 2026) | ✅ RESOLVED IN SOURCE — scoped Firestore index deployment remains pending |
| CS-9 | **Menu presence confirmations paid nested-map index fanout despite using exact store truth** — The presence map is transactionally updated and projected into `storesSummary`, but no browser, server, or scheduler query filters or orders by the canonical store field. | ⚠️ LOW | `stores.menuPresence` | `firestore.indexes.json`, `src/database/stores/index.tsx`, `src/lib/menuPresence/presenceReadiness.ts` | Exempt the canonical parent map from automatic indexing while retaining the existing atomic store/summary transaction. Keep presence owner-private and do not add cache invalidation or a separate event ledger. | Menu Presence and Public-Truth Monitoring Firebase cost audit (July 17, 2026) | ✅ RESOLVED IN SOURCE — scoped Firestore index deployment remains pending |
| CS-10 | **Support-ticket conversation and audit arrays were automatically indexed even though all consumers resolve tickets through scoped list/detail queries first** — Replies and status changes rewrite growing arrays, multiplying index work with conversation length. | ⚠️ MEDIUM | `supportTickets.messages`, `statuses`, `documents`, `logs` | `firestore-answerlattice.indexes.json`, `firestore.indexes.json`, `src/database/tickets/index.ts` | Exempt the four exact-document fields in both deployment modes. Preserve product/scope/deleted/created query indexes, append-only transactions/rules, attachment validation, and current ticket caps. | MenuList Help Center and Answerlattice support flow Firebase cost audit (July 17, 2026) | ✅ RESOLVED IN SOURCE — dedicated and shared scoped Firestore index deployments remain pending |
| CS-11 | **Central store/project summary maps were automatically indexed even though consumers resolve them by exact document ID** — As the number of stores and project rows grows, every summary mutation can amplify nested index work and storage despite no query using those maps. | ⚠️ HIGH | `platformSummary.stores`, `platformSummary.projects` | `firestore.indexes.json`, `src/database/platformSummary/index.ts`, `src/lib/firestore/summaryProjectsWriter.ts` | Exempt both parent maps from automatic indexing. Preserve independently queried top-level scalars such as `specialMenuNextTransitionAt`, exact-document parsing, canonical write ownership, and document-size admission/backfill ceilings. | Internal Ops and platform summary Firebase cost audit (July 17, 2026) | ✅ RESOLVED IN SOURCE — scoped Firestore index deployment remains pending |
| CS-12 | **Public contact free text and nested report context were automatically indexed without a query consumer** — Every accepted contact/report follow-up write could create avoidable index entries for up to 2,000 characters of message text and nested setup metadata. | ⚠️ LOW | `landingPageEnquiries.message`, `landingPageEnquiries.sourceContext` | `firestore.indexes.json`, `src/app/api/public/contact/route.ts`, `src/app/api/ops/report-leads/route.ts` | Exempt only the exact-document payload fields. Keep top-level routing/query scalars and the `sourceKind + createdOn` composite indexed so Report Lead Ops remains bounded and selective. | Main Website and public contact Firebase cost audit (July 17, 2026) | ✅ RESOLVED IN SOURCE — scoped Firestore index deployment remains pending |

---

## Resolved Items

Items that were previously open but have been fixed. Kept for historical reference.

| #                       | Risk | Resolution | Resolved Date | Fix Applied |
| ----------------------- | ---- | ---------- | ------------- | ----------- |
| _No resolved items yet_ |      |            |               |             |

---

_Last Updated: July 17, 2026_
_Updated By: Codex (feature-by-feature Firebase cost and scale audit)_
_Note: DS-2 is code-resolved for new first-extraction project auto-save jobs but requires the updated `processMenuImagesJob` Firebase Function to be deployed before it is live. CG-1 is code-resolved as compact-not-delete ledger retention; `ai_operation_detail_cleanup`, image-batch metadata retention, and CS-2 prompt-cache source retention require the updated `menulistMaintenanceScheduler` Function before live effect. SG-4 physical public-media reclamation is intentionally not code-resolved because current state cannot prove global exclusive ownership. Prior QA Functions and Storage deploy attempts were blocked before upload by project/IAM or Service Usage HTTP 403; current target evidence remains pending._
