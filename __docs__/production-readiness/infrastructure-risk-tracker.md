# Infrastructure Risk Tracker

**Purpose:** Centralized tracker for all infrastructure-level risks discovered during audits, production reviews, and implementation sessions. Cascade MUST update this file after every audit that identifies document size risks, storage growth issues, scaling concerns, or unbounded resource patterns.

**File:** `__docs__/production-readiness/infrastructure-risk-tracker.md`  
**Rule:** Rule 10.10 in `IDE_PROMPTS/MASTER-EXECUTION-PROMPT.md`  
**Last Updated:** July 9, 2026

---

## How This File Works

- After EVERY audit (Firebase cost, production, scalability, parity, system), Cascade checks for infrastructure-level risks
- Risks are categorized: **Document Sizes**, **Storage Growth**, **Collection Growth**, **Query Performance**, **Cost Scaling**
- Each risk has: Description, Severity, Source Audit, Affected Files, Recommended Fix, Status
- Status: `⚠️ OPEN` → `🔧 FIX PLANNED` → `✅ RESOLVED`
- Resolved items stay in the file (with resolution date + fix description) for historical reference

---

## Document Sizes

Risks related to Firestore document size limits (1MB max per document).

| #    | Risk                                                                                                                                                                                                                                    | Severity  | Collection / Path                  | Affected Files                                | Recommended Fix                                                                                                                                                              | Source Audit                                          | Status                                                                                                                                                                                |
| ---- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------- | ---------------------------------- | --------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| DS-1 | **Project `files[]` append growth is bounded by an explicit reset/create-new policy** — Each extraction appends files with full `extractedData` because `files[].extractedData` is the live editor data. The system does not auto-strip old processed files; oversized append attempts fail safe and tell the owner to reset the menu or create a new menu before uploading more files. | ⚠️ HIGH   | `projects/{tId}/{sId}/{projectId}` | `src/components/templates/main-app/projects/index.tsx`, `src/components/templates/main-app/projects/ProjectDetails/ProjectConfirmModal.tsx`, `src/components/mobile/sheets/MenuUploadSheet.tsx`, `src/components/templates/main-app/projects/utils/pdfUtils.ts`, `src/app/api/menu-extraction/jobs/route.ts`, `functions/src/logic/saveFilesToProject.ts`, `src/data/shared/menuExtractionProjectSize.ts`, `functions/src/sharedData/menuExtractionProjectSize.ts` | Keep shared document-size limits mirrored, block client batches over the backend file/page cap before Storage upload, block projected oversized project append jobs before expensive AI work, clean up newly uploaded owner source files on that rejection, keep the final transaction save guard, and source-gate the owner-controlled reset path that clears files and extracted data. | AI Data Extraction Firebase Cost Audit (Mar 13, 2026); July 1 production-readiness audit | ✅ RESOLVED IN CODE — Save guard remains 900KB hard block / 700KB warning. Desktop and mobile upload now cap pending extraction batches at the shared 15 file/page limit before Storage upload, and mobile passes remaining PDF page slots into conversion before canvas rendering. The owner job route projects 100KB per incoming file against the same 900KB limit before AI job creation. Oversized owner-upload attempts are rejected before extraction and newly uploaded source files are deleted when the route sees them. Owner project reset writes `files: []` (and clears linked overrides), and the reset confirmation states that uploaded files and extracted data are cleared. Automatic stripping remains intentionally out of scope unless the editor data model changes. |
| DS-2 | **Job document `result.combinedData` can be large** — Completed project-save jobs used to store full extracted menu data even after the same data was saved into the project document. 400-item menus can be ~200KB and larger menus can approach document limits. `rawBatchResponses` remains capped, and extraction-only/review flows still need their active payloads. | ⚠️ MEDIUM | `menuImageProcessingJobs/{jobId}`  | `functions/src/logic/processMenuImagesJob.ts`, `functions/src/schedulers/menuJobCleanup.ts`, `scripts/verification/verify-menu-extraction-pipeline.js` | Keep immediate `result.combinedData` omission for first-extraction project auto-save jobs, retain `result.summary`, and keep legacy `menu_old_cleanup` pruning for older completed project jobs. Public draft, messaging, and preview/review jobs must keep payloads while downstream flows need them. | AI Data Extraction Firebase Cost Audit (Mar 13, 2026); July 1 production-readiness audit | ✅ RESOLVED IN CODE — first-extraction project auto-save jobs now write `result.summary` plus `dataPrunedReason: project_auto_saved_immediate` instead of `result.combinedData`; legacy completed project jobs remain covered by delayed cleanup. Pending Firebase Function deploy for live effect. |

---

## Storage Growth

Risks related to Firebase Storage / Cloud Storage growing without bounds.

| #    | Risk                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Severity       | Storage Path                                                        | Affected Files                                                                                                 | Recommended Fix                                                                                                                                                                                                                                                                                                 | Source Audit                                          | Status  |
| ---- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------- | ------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------- | ------- |
| SG-1 | **Legacy extraction uploads have a COLDLINE lifecycle artifact** — The repo now includes `infra/storage/menulist-storage-lifecycle.json`, which applies a Cloud Storage `SetStorageClass` rule for `MenuListAi/project/files/` after 365 days. The rule intentionally does not delete owner/source uploads because old project file previews and audit evidence may still reference them. | ⚠️ MEDIUM-HIGH | `MenuListAi/project/files/{timestamp}-{uid}`                        | `infra/storage/menulist-storage-lifecycle.json`, `scripts/verification/verify-storage-lifecycle-config.js`, `__docs__/production-readiness/launch-prerequisites.md` | Apply the lifecycle config to `menulist-qa` first, verify the bucket lifecycle JSON, wait for the Cloud Storage lifecycle propagation window, then apply to `menulist` when production deployment is approved. Verify with `npm run verify:storage-lifecycle`. | AI Data Extraction Firebase Cost Audit (Mar 13, 2026); July 1 production-readiness audit | ✅ RESOLVED IN CODE — pending bucket lifecycle apply |
| SG-2 | **Active project uploads now use tenant-scoped Storage paths; legacy project Storage paths are read-only** — Current project fallback uploads write tenant-scoped paths through `generateStoragePath()` as `projects/{fileType}/{tId}/{sId}/{fileId}` after sanitizing the file id. Legacy `MenuListAi/project/...` objects remain authenticated-readable for old references, but new writes and deletes are denied in `storage.rules`. | ⚠️ LOW         | Active: `projects/{fileType}/{tId}/{sId}/{fileId}`; legacy read-only: `MenuListAi/project/*` | `src/database/projects/index.ts`, `storage.rules`, `src/lib/storage/pathGenerator.ts`, `scripts/verification/verify-storage-path-hardening.js` | Keep active writes on scoped paths, keep legacy project Storage paths read-only, and verify with `npm run verify:storage-paths`. Deploy Storage rules to `menulist-qa` first, then production only after QA evidence and explicit production approval. | Security Surface Audit (Mar 13, 2026); July 1 production-readiness audit; July 2 storage rules cutover | ✅ RESOLVED IN CODE — current project fallback uploads write tenant-scoped paths, and legacy project Storage paths are read-only. Pending scoped Firebase Storage rules deploy for live effect; latest `menulist-qa` deploy attempt failed before rules upload with Service Usage HTTP 403: project not found or permission denied. |
| SG-3 | **AI-generated images are profile-prepared before Storage save** — Accepted single-image uploads already route through the shared `uploadFile(..., 'itemImages')` / `prepareMediaImage()` media profile path. July 1 hardening adds `prepareMediaImageAdmin()` to the Cloud Tasks batch worker path so generated item images are converted to the `menuItem` profile before Storage save: WebP, max 1200px, 500KB target, allowed aspect-ratio crop/fill, and original/prepared size metadata. | ⚠️ MEDIUM      | `media/menuItem/{tId}/{sId}/*`                                      | `src/database/storage/uploadBase64MediaImageAdmin.ts`, `src/lib/media/prepareMediaImageAdmin.ts`, `src/app/api/image-generation/batch-generation/route.ts` | Keep browser and worker generated-image uploads on the shared media profile contract; verify with `npm run verify:ai-accounting`. No Firebase Function/rules/index deploy is required for this app/API slice; production effect waits for app/Vercel deployment approval. | AI Image Gen ChatGPT Review (Mar 13, 2026); July 1 production-readiness audit | ✅ RESOLVED IN CODE — pending app/Vercel deploy |
| SG-4 | **Discarded/unselected batch images may persist in storage** — Old generated batch images are now cleaned up in code for terminal jobs that cannot contain owner-accepted project images. `completed`, `failed`, and `discarded` jobs attempt same-tenant/store `media/menuItem/{tId}/{sId}/` Storage deletion before `itemsList` pruning. `finished` and `cancelled` jobs intentionally skip Storage deletion because they can include accepted project images. | ⚠️ LOW         | `media/menuItem/{tId}/{sId}/*`                                      | `functions/src/schedulers/menulistMaintenanceScheduler.ts`, `src/database/imageBatchProcessing/server.ts`, `src/database/imageBatchProcessing/index.tsx` | Keep `image_batch_job_retention_cleanup` deployed in `menulistMaintenanceScheduler`. It prunes generated-image URLs after 7 days and deletes terminal job docs after 30 days using per-store marker queries. | AI Image Gen ChatGPT Review (Mar 13, 2026); July 1 production-readiness audit | ✅ RESOLVED IN CODE — pending Firebase Function deploy. Latest QA deploy attempt on July 2, 2026 with `firebase deploy --project menulist-qa --config firebase.json --only functions:menulistMaintenanceScheduler --non-interactive` completed predeploy lint/build, then Cloud Resource Manager returned HTTP 403 caller permission before upload. |

---

## Collection Growth

Risks related to Firestore collections growing without TTL or cleanup.

| #    | Risk                                                                                                                                                                                                                                                                                                                       | Severity  | Collection                                | Affected Files                                     | Recommended Fix                                                                                                                                                                    | Source Audit                                          | Status  |
| ---- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------- | ----------------------------------------- | -------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------- | ------- |
| CG-1 | **AI operation ledger detail growth is bounded by compact-not-delete retention** — `MENULIST_AI_OPERATIONS` and nested `menulistAiOperations/{tId}/{sId}` retain compact accounting/audit rows for platform cost review and owner transaction history, but raw/generated response details are not retained indefinitely. | ⚠️ MEDIUM | `MENULIST_AI_OPERATIONS`; `menulistAiOperations/{tId}/{sId}` | `functions/src/logic/processMenuImages.ts`, `src/lib/ai/operationLog.ts`, `src/lib/ai/operationPresentation.ts`, `src/app/api/ai-operations/route.ts`, `functions/src/schedulers/menulistMaintenanceScheduler.ts` | Keep `AI_OPERATION_LOG_MODE="accounting_only"` as the default, keep owner transaction reads behind `/api/ai-operations` allowlists and capped pagination, and keep `ai_operation_detail_cleanup` deployed for any detailed-mode rows. Full compact ledger row deletion remains intentionally disabled because those rows are the accounting/audit and owner transaction-history record. | AI Data Extraction Firebase Cost Audit (Mar 13, 2026); July 1 production-readiness audit | ✅ RESOLVED IN CODE — Extraction rows use `AI_OPERATION_LOG_MODE`; in accounting-only mode they retain compact response counts, message presence/length, summarized file metadata, cost/token/accounting fields, and no raw provider response object. App-route rows store count/shape summaries for `clientResponse` in `accounting_only` mode and use `detailExpiresAt` only in `detailed` mode. `menulistMaintenanceScheduler` runs `ai_operation_detail_cleanup` daily and deletes detailed `clientResponse`, `files`, `geminiResponse`, `rawBatchResponses`, and `detailExpiresAt` when due. Full compact ledger rows are retained by design for platform audit and owner transaction history. Pending Firebase Function deploy for live scheduler effect. |
| CG-2 | **`IMAGE_BATCH_PROCESSING_JOBS` unbounded growth** — Terminal jobs now receive `itemsExpiresAt` and `expiresAt` from both browser and Admin SDK status-update paths. The maintenance scheduler prunes heavy `itemsList[]` payloads after 7 days and deletes terminal job docs after 30 days. A bounded legacy pass handles older terminal docs that predate the retention markers. | ⚠️ MEDIUM | `imageBatchProcessingJobs/{tId}/{sId}` | `src/database/imageBatchProcessing/index.tsx`, `src/database/imageBatchProcessing/server.ts`, `functions/src/schedulers/menulistMaintenanceScheduler.ts` | Keep the terminal retention markers and `image_batch_job_retention_cleanup` deployed. No new composite index or standalone scheduled Function is required. | AI Image Gen ChatGPT Review (Mar 13, 2026); July 1 production-readiness audit | ✅ RESOLVED IN CODE — pending Firebase Function deploy. Latest QA deploy attempt on July 2, 2026 with `firebase deploy --project menulist-qa --config firebase.json --only functions:menulistMaintenanceScheduler --non-interactive` completed predeploy lint/build, then Cloud Resource Manager returned HTTP 403 caller permission before upload. |

---

## Query Performance

Risks related to slow or expensive Firestore queries at scale.

| #    | Risk                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Severity | Query Location                                               | Affected Files                   | Recommended Fix                                                                                                                                                                                                                  | Source Audit                                          | Status  |
| ---- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | ------------------------------------------------------------ | -------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------- | ------- |
| QP-1 | **Extraction monitor dashboard read dedupe is active** — The dashboard already uses `getExtractionDashboardSnapshot()` so health, quality, and job feed reuse one job query. July 1 hardening adds SWR with a 5-minute dedupe window to `/ops/extraction`, so duplicate mounts/revalidations in that window reuse the cached snapshot. Cache misses and explicit Refresh still read up to 150 job docs + 100 cost docs. Auto-refresh remains disabled. | ⚠️ LOW   | `src/components/templates/main-app/platform/extractionMonitor/index.tsx`, `src/database/ops/extraction.ts` | `src/components/templates/main-app/platform/extractionMonitor/index.tsx`, `scripts/verification/verify-menu-extraction-pipeline.js` | Keep the 5-minute SWR dedupe contract. If automatic refresh or broader operator access is added, move the dashboard to server-side pre-aggregation before enabling it. | AI Data Extraction Firebase Cost Audit (Mar 13, 2026); July 1 production-readiness audit | ✅ RESOLVED IN CODE |
| QP-2 | **Flat collections lacked tenant-level read filtering in Firestore rules** — `analytics`, `chatSessions`, `aiSearchHistory`, `feedback`, `supportTickets`, `ownerControlUsage`, `imageBatchProcessingJobs`, `decisionBlocks`, `menuIntelligence`, and `masterOperationalState` now enforce platform or tenant/store-scoped rules. `queryEmbeddings` is server/admin only. | ✅ FIXED | Multiple flat collections in `firestore.rules`                 | `firestore.rules`, `firestore-answerlattice.rules` | Implemented tenant/store helper checks, doc-id fallback checks for compound IDs, server-only query embeddings, and Answerlattice-scoped chat/support/KB/cache-version rules. Deployed ecomsai Firestore rules on May 18, 2026. | Security Surface Audit (Mar 13, 2026); Firebase rules hardening pass (May 18, 2026) | ✅ CLOSED |

---

## Cost Scaling

Risks related to Firebase costs growing non-linearly or unexpectedly at scale.

| #    | Risk                                                                                                                                                                                                                                                                                                                                                                  | Severity | Area                   | Projected Cost at Scale                                                                | Recommended Fix                                                                                                                                                                                               | Source Audit                               | Status  |
| ---- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | ---------------------- | -------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------ | ------- |
| CS-1 | **Active project storage is tenant-scoped; legacy objects remain cost debt but cannot grow through legacy writes** — New project fallback uploads use `projects/files/{tId}/{sId}/{fileId}`, so tenant/store cleanup is possible for newly written project uploads. Existing `MenuListAi/project/*` objects remain authenticated-readable for compatibility, but legacy writes/deletes are denied so the old path cannot keep accumulating new project uploads.                                | ⚠️ LOW   | Active: `projects/files/{tId}/{sId}/{fileId}`; legacy read-only: `MenuListAi/project/*` | `src/database/projects/index.ts`, `storage.rules`, `infra/storage/menulist-storage-lifecycle.json`                                    | Keep scoped active writes plus the legacy COLDLINE lifecycle artifact, keep legacy project Storage paths read-only, and deploy the scoped Storage rules update to QA first.                                                                                 | Security Surface Audit (Mar 13, 2026); July 1 production-readiness audit; July 2 storage rules cutover      | ✅ RESOLVED IN CODE — current project fallback uploads write tenant-scoped paths, and legacy project Storage paths are read-only. Pending scoped Firebase Storage rules deploy for live effect; latest `menulist-qa` deploy attempt failed before rules upload with Service Usage HTTP 403: project not found or permission denied. |
| CS-2 | **AI image generation prompt caching uses store-owned media copies and bounded source retention** — Common dishes can generate near-identical prompts across stores, but a naive cache that returns the first generated `media/menuItem/{tId}/{sId}/...` URL would attach another store's Storage object to the requester. The media URL is public-readable, but write/delete ownership, cleanup, and lifecycle remain tied to the original tenant/store. | ⚠️ LOW   | AI generation pipeline | `src/app/api/image-generation/batch-generation/route.ts`, `src/lib/ai/imageGenerationPromptCache.ts`, `functions/src/schedulers/menulistMaintenanceScheduler.ts`, `src/app/api/image-generation/generators.ts`, `src/app/api/image-generation/prompt.ts`, `src/database/storage/uploadBase64MediaImageAdmin.ts`, `storage.rules` | Keep the batch non-reference prompt cache server-only: cache sources live under `system/aiImagePromptCache/`; copies cached source bytes into the requesting store's own `media/menuItem/{tId}/{sId}/...` path; cache-hit AI operations record `unitsConsumed: 0`; expired prompt-cache source objects and `aiImagePromptCache` docs are cleaned by `ai_image_prompt_cache_cleanup`; do not implement tenant-URL reuse; single-image browser draft generation remains uncached because it returns base64 previews and performs no Storage write until owner acceptance. | AI Image Gen ChatGPT Review (Mar 13, 2026); July 1 production-readiness audit | ✅ RESOLVED IN CODE — batch worker cache implemented for one-prompt, one-image, non-reference generations; cache hits copy into store-owned media paths and record free AI operation rows. Cache source docs include `expiresAt`, and `menulistMaintenanceScheduler` runs a bounded daily `ai_image_prompt_cache_cleanup` that deletes expired source objects only under `system/aiImagePromptCache/` before deleting cache docs. Single-image browser draft generation remains intentionally uncached because caching it would create server-owned draft Storage objects before owner acceptance. Pending Firebase Function deploy for live cleanup effect; QA deploy attempt with `firebase deploy --project menulist-qa --config firebase.json --only functions:menulistMaintenanceScheduler --non-interactive` passed predeploy lint/build, then Cloud Resource Manager returned HTTP 403 caller permission. |

---

## Resolved Items

Items that were previously open but have been fixed. Kept for historical reference.

| #                       | Risk | Resolution | Resolved Date | Fix Applied |
| ----------------------- | ---- | ---------- | ------------- | ----------- |
| _No resolved items yet_ |      |            |               |             |

---

_Last Updated: July 9, 2026_
_Updated By: Codex (MenuList production-readiness audit continuation)_
_Note: DS-2 is code-resolved for new first-extraction project auto-save jobs but requires the updated `processMenuImagesJob` Firebase Function to be deployed before it is live. CG-1 is code-resolved as compact-not-delete ledger retention; `ai_operation_detail_cleanup` still requires the updated `menulistMaintenanceScheduler` Firebase Function to be deployed before detailed-mode cleanup is live. SG-4, CG-2, and CS-2 prompt-cache source retention are code-resolved in `menulistMaintenanceScheduler` but require the updated scheduler Firebase Function to be deployed before they are live mitigations. The latest July 9, 2026 package-local scoped `menulist-qa` Functions retry targeted the current Gate 1 function set, passed predeploy lint/build, and was blocked by Cloud Resource Manager HTTP 403 caller permission before upload. The latest July 9, 2026 `menulist-qa` Storage rules retry passed `npm run verify:storage-paths`, then was blocked before rules upload while checking/enabling `firebasestorage.googleapis.com` with Service Usage HTTP 403: project `menulist-qa` not found or permission denied._
