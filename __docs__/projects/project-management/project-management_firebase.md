# Project Management - Firebase Cost Tracking

**Feature:** Project CRUD and Menu Builder Lifecycle
**Status:** Production implementation exists; audited July 16, 2026
**Last Updated:** July 16, 2026

## Summary

- **Collections used:** `projects/{tId}/{sId}`, `platformSummary/projects_{sId}`
- **Storage used:** Firebase Storage for project/file/media uploads through shared media helpers.
- **Cloud Functions:** None for core CRUD. Optional extraction/scheduler features are documented separately.
- **Public cache:** Project and summary writes must invalidate `menu-store-{storeId}`, `store-{storeId}`, `client-stores`, screen content version, and Owner Business Assistant browser cache through `revalidatePublicClientCacheForProject()`.
- **Tenant isolation:** Client DAL resolves Firestore paths from the active authenticated session (`tId`, `sId`). Cross-store master reads are explicit and tenant-checked in multi-outlet flows.

## Reads

| Operation | Collection/path | Reads | Notes |
| --- | --- | ---: | --- |
| List projects for editor/onboarding | `platformSummary/projects_{sId}` | 1 | `getProjectsList*()` auto-creates a default project if empty. |
| List projects for read-only surfaces | `platformSummary/projects_{sId}` | 1 | `getExistingProjectsListWithoutLoader()` never writes. Used by dashboard selector, Use MenuList, OBP link card, Past Activity, GrowthOS, print/export surfaces. |
| Load one project | `projects/{tId}/{sId}/{projectId}` | 1 | Full menu/editor data. |
| Legacy project fallback | `projects/{projectId}` | +1 only after scoped miss | Returned only when runtime project/tenant/store identity matches the exact requested scope. No new legacy writes are allowed. |
| Load project with summary | summary doc + project doc | 2 | Used when both metadata and full content are required. |
| Delete guard for linked outlets | project doc plus conditional linked-outlet checks | 1+ | Only when multi-outlet is enabled and caller cannot skip the guard from tenant context. |
| Restore project | project doc + summary doc | 2 | Prevents duplicate default projects and rebuilds summary from tombstone. |
| Create project | 2 exact deleted-project reservation queries + summary transaction read | 3 minimum | Exact `slug` and `previousSlugs` queries are capped at 25 matches each and replace the old arbitrary 50-row deleted scan. A supplied deterministic ID adds one project read so retry recovery cannot overwrite existing menu data. |
| Update metadata without name/slug | summary transaction read | 1 | Serializes concurrent metadata writers and avoids lost summary fields. |
| Rename/change slug | preflight summary + 2 exact deleted-project reservation queries + summary transaction | 4 minimum | Re-checks current/redirect ownership in the transaction; exact queries avoid unrelated tombstone reads. |
| Duplicate | 2 exact deleted-project reservation queries + source project + summary transaction reads | 4 minimum | Source validation and summary collision resolution are transactionally consistent. |
| Time-slot preset cascade discovery | `projects/{tId}/{sId}` ordered by document ID | N, paged 100 at a time | Low-frequency preset edit/delete only. Runtime scope checks discard malformed, deleted, or sibling project rows. |
| Time-slot preset cascade mutation | matching project docs | +1 transaction read per candidate | Re-reads current project truth so a stale discovery snapshot cannot overwrite a concurrent menu edit. |
| Save project data | project preflight + project transaction read | 2 | The preflight resolves stored outlet linkage before routing; the standalone transaction rechecks current scope/deletion/linkage and validates MCE against the same state it writes. |
| Publish project | project preflight + project transaction read | 2 | Standalone publish compares the caller/preflight `modifiedOn` with the transaction-local project before deriving the next menu version. A linked publish adds one preflight master-project read, then forwards `expectedModifiedOnMillis` to the server transaction that reads caller store, outlet store, master store, tenant, outlet project, and master project before commit. |
| Append generated image selections | project transaction read | 1 | Standalone append rejects deleted, scope-conflicting, or newly linked state and writes files from current truth. Linked append uses the guarded outlet/master transaction. |

## Writes

| Operation | Writes | Public cache impact | Notes |
| --- | ---: | --- | --- |
| Create project | 1 project doc + 1 summary write | Yes | One transaction; deterministic-ID recovery writes only the missing/updated summary and never rewrites the existing project. Default handoff is folded into the summary mutation. |
| Auto-create default project from editor/mobile provider | 1 project doc + 1 summary write | Yes | Intentional only in menu-management entry points. Requires `projects_list_default_project_create_rejected` acknowledgement before returning the default project in memory. |
| Read-only project list | 0 | None | Must use `getExistingProjectsListWithoutLoader()`. |
| Update metadata | 1 summary write | Yes | Transactional merge prevents concurrent metadata/image/language updates from replacing each other. Default handoff remains in the same write. |
| Save project data | 1 project doc write | Yes | The standalone transaction persists a partial merge and its MCE projection from one current-state snapshot. May add optional MOL/master-update writes when feature flags are enabled. |
| Publish project | 1 project doc write | Yes | The standalone transaction increments `menuVersion` from persisted truth and returns the full post-merge project used by snapshots/observations. Linked publish writes the outlet project and adds one summary write only when `active` is explicitly changed. Optional snapshot/event writes are flag-gated. |
| Append generated image selections | 1 project doc write | Yes | Files-only append from transaction-local truth. Linked outlets use one outlet write after current outlet/master and policy checks. |
| Soft delete | 1 project write + 1 summary write | Yes | One transaction reads current project/summary, stores `deletedSummary`, removes the entry, and promotes a fallback from current truth. |
| Restore | 1 project write + 1 summary write | Yes | One transaction; does not restore `isDefault` if another active default already exists. |
| Duplicate | 1 project doc + 1 summary write | Yes | One transaction; unique slug, no default, and no copied special-menu/deletion metadata. |
| Active/inactive toggle | 1 project doc + 1 summary field write | Yes | One transaction with one project read; inherited deactivation may add one master-store policy read. |
| Time-slot preset edit/delete cascade | 0-N files-only project writes | Yes per changed project | Transactions update only `files` plus `modifiedOn`; they do not rewrite a stale full project. Keeps public category visibility aligned without an extra public store read. |

## Cost Findings From June 11, 2026 Audit

- Deterministic first-project recovery uses the already documented supplied-ID project read. `firestore.rules` admits the absent resource only after exact tenant/store membership and uses `resource == null`, so it adds no dependent `get()`/`exists()` rule lookup. Existing project reads still require path-bound project, tenant, and store identity; cross-store and cross-tenant missing reads remain denied. No DAL read/write, document, collection, index, Function, or provider call was added.

- July 16 second-pass hardening replaced the create/rename/duplicate scan of up to 50 arbitrary deleted project documents with exact current-slug and redirect-slug queries capped at 25 matches each. Empty queries have a two-query minimum, but unrelated tombstones are no longer billed, the total result cap remains 50, and stores with more than 50 unrelated deletions cannot silently lose a recent URL reservation. A full matching page or query failure fails closed. No write, collection, rule, or composite index was added.

- Hidden default-project creation was removed from read-only owner surfaces. Dashboard selector, OBP link card, Use MenuList, Past Activity, and GrowthOS now use `getExistingProjectsListWithoutLoader()`.
- Editor and mobile project-management providers still use the auto-create helper intentionally so an empty store gets a usable first menu when the owner enters menu management.
- Delete/restore now preserves a summary tombstone in the soft-deleted project doc. This adds no extra write beyond the existing delete write and prevents lossy restore.
- Public cache invalidation is present on project data writes, summary writes, publish, duplicate, delete, restore, active toggle, special-menu changes, and time-slot cascades.
- Desktop Projects page diagnostic hardening is cost-neutral. `src/components/templates/main-app/projects/utils/projectPageDiagnostics.ts` changes only failure logging around existing project actions, ProjectDetails modal failures, and load errors; it adds no Firestore reads/writes, Storage operations, Cloud Functions, provider calls, cache invalidations, fields, rules, indexes, or owner settings.
- Project view diagnostic cleanup is cost-neutral. B2B JSON edit failures and B2C menu-page design utilities changed only browser diagnostics; no project view reads/writes, Storage calls, provider calls, cache invalidations, fields, rules, indexes, API routes, Cloud Functions, or owner settings were added.
- Project share modal diagnostic and browser-handoff hardening is cost-neutral. `src/components/templates/main-app/projects/b2cView/shareModal/index.tsx`, `MenuKitSection.tsx`, and the dormant legacy `linkView.tsx` / `socialShareView.tsx` change only failure logging, acknowledged browser-local copy, and `noopener,noreferrer` browser opens around existing QR download, social handoff, direct link copy/open, structured export, PDF generation, Menu Kit generation, share-message copy, staff-script copy, and WhatsApp handoff paths. Failed copies and blocked opens may log URL/message lengths plus clipboard/fallback support booleans only; they add no Firestore reads/writes, Storage operations, Cloud Functions, provider calls, cache invalidations, fields, rules, indexes, API routes, or owner settings.
- B2B ShareModal external endpoint request hardening is cost-neutral. The external menu JSON POST still happens only after the owner enters a public HTTPS endpoint, but the browser request now uses no-store cache, `credentials: 'omit'`, manual redirect handling, and no-referrer behavior. This adds no Firestore reads/writes/deletes, Storage operations, Cloud Functions, provider calls, cache invalidations, routes, rules, indexes, schema fields, owner settings, Firebase deploy requirement, or Vercel deploy action.
- Mobile project selector and design diagnostic hardening is cost-neutral. `src/components/mobile/utils/mobileProjectDiagnostics.ts` changes only failure logging around existing mobile project image preparation, delete, create/edit/duplicate save, active toggle, reset, public-link copy, public-content translation repair, design publish, post-publish verification handoff, design link copy, and design native-share failures; project-selector and design copied feedback waits for acknowledged browser-local copy success. This adds no Firestore reads/writes beyond the existing project mutation attempts, Storage operations beyond existing image upload/generation paths, Cloud Functions beyond the existing attempted publish verification callable, provider calls beyond existing translation/image paths, cache invalidations, fields, rules, indexes, API routes, or owner settings.
- Project save optional-hook diagnostics are cost-neutral. `project_current_state_load_failed` and `project_master_cache_invalidation_failed` only expose failures in the existing previous-project read and client-side master-cache invalidation paths; they add no Firestore reads/writes/deletes, Storage operations, Cloud Functions, provider calls, cache invalidations, fields, rules, indexes, API routes, owner settings, Firebase deployment, or Vercel deployment.
- Extracted profile project-default acknowledgement hardening is cost-neutral. Desktop Projects and Mobile Menu still use the existing `updateProjectWithoutLoader()` project write when extracted profile visual defaults fill missing project accent/image-background settings, but they now require `assertProjectUpdateSucceeded()` before local project state changes; this adds no reads, writes, deletes, Storage operations, routes, rules, indexes, schema fields, Cloud Functions, owner settings, Firebase deployment, or Vercel deployment.
- Mobile Menu project-persist acknowledgement hardening is cost-neutral. Debounced mobile menu saves still use the existing `updateProjectWithoutLoader()` project write and retry timer, but the screen now requires `assertProjectUpdateSucceeded()` before marking the pending snapshot as persisted; this adds no reads, writes, deletes, Storage operations, routes, rules, indexes, schema fields, Cloud Functions, owner settings, Firebase deployment, or Vercel deployment.
- Mobile Menu item-image acknowledgement hardening is cost-neutral. Item image modal saves still upload images and then use the existing `updateProjectWithoutLoader()` project write, but the screen now requires `assertProjectUpdateSucceeded()` before local menu image/cache state or success copy changes; this adds no reads, writes, deletes, Storage operations beyond the existing item image upload, routes, rules, indexes, schema fields, Cloud Functions, owner settings, Firebase deployment, or Vercel deployment.
- Mobile project selector acknowledgement hardening is cost-neutral. Create, duplicate, edit, translation repair, active toggle, and reset flows still use their existing `addProject()`, `duplicateProject()`, `updateProjectMetadata()`, `updateProjectWithoutLoader()`, and `setProjectActive()` writes, but the sheet now requires `assertProjectUpdateSucceeded()` before local cache, form, selection, or success-copy updates; this adds no reads, writes, deletes, Storage operations, routes, rules, indexes, schema fields, Cloud Functions, owner settings, Firebase deployment, or Vercel deployment.
- Desktop Projects create/duplicate/edit/translation acknowledgement hardening is cost-neutral. Create, duplicate, metadata, language/default-language, active/default handoff, and public-content translation repair still use the existing project DAL writes, but the page now requires `assertProjectUpdateSucceeded()` before selected project, SWR list, draft baseline, or success-copy updates; this adds no reads, writes, deletes, Storage operations, routes, rules, indexes, schema fields, Cloud Functions, owner settings, Firebase deployment, or Vercel deployment.
- Desktop Projects reset acknowledgement hardening is cost-neutral. Reset still uses the existing `updateProject()` write to clear files and linked-outlet overrides, but the page now requires `assertProjectUpdateSucceeded()` before cache revalidation, success copy, or modal close; this adds no reads, writes, deletes, Storage operations, routes, rules, indexes, schema fields, Cloud Functions, owner settings, Firebase deployment, or Vercel deployment.
- Editor translation metadata acknowledgement hardening is cost-neutral. Desktop Editor, Command Center, mobile Bulk Actions, and mobile Manage Languages still use existing project metadata writes for translated name/description/special-menu summary fields, but they now require `assertProjectUpdateSucceeded()` before local saved state or success-copy updates; this adds no reads, writes, deletes, Storage operations, routes, rules, indexes, schema fields, Cloud Functions, owner settings, Firebase deployment, or Vercel deployment.
- Desktop menu editor acknowledgement hardening is cost-neutral. Editor sync, shared editor persistence, and project-detail translation repair still use the existing `updateProject()` and `updateProjectMetadata()` writes, but the editor now requires `assertProjectUpdateSucceeded()` before local editor state, dirty-state, POS sync handoff, last-saved timestamps, or success copy changes; this adds no reads, writes, deletes, Storage operations, routes, rules, indexes, schema fields, Cloud Functions, owner settings, Firebase deployment, or Vercel deployment.
- Desktop editor helper acknowledgement hardening is cost-neutral. Item image deletion, description generation, and batch image result upload direct-save fallbacks still use existing `updateProject()` writes, but now require `assertProjectUpdateSucceeded()` before active-project state, generated-description persistence, selected-image project state, or success copy changes; this adds no reads, writes, deletes, Storage operations beyond existing image cleanup/upload behavior, routes, rules, indexes, schema fields, Cloud Functions, owner settings, Firebase deployment, or Vercel deployment. The description-generation returned-error diagnostic also adds no Firebase operations because it only replaces a raw local logger warning with bounded menu-editor metadata.
- Design publish acknowledgement hardening is cost-neutral. Desktop B2C and Mobile Design publish still use existing `publishProject()` writes, but now require `assertProjectUpdateSucceeded()` before local published/cached state, success copy, or post-publish verification setup changes; this adds no reads, writes, deletes, Storage operations beyond existing background image upload behavior, routes, rules, indexes, schema fields, Cloud Functions, owner settings, Firebase deployment, or Vercel deployment.
- Generated project-image persistence writes only `{ projectImage }` through transactional `updateProjectMetadata()` after Storage upload. It cannot replace stale name/default/language/slug state or recreate a deleted summary. Failed persistence preserves the deterministic prepared-media object because content-addressed paths can already be referenced by another retry or record; future cleanup must be retention-based and reference-aware. This adds no collection, document, route, rule, index, Cloud Function, provider call, or owner setting.
- Default handoff is atomic inside the project DAL. Desktop and mobile create/edit flows pass previous-default or replacement-default ids through the `addProject()` / `updateProjectMetadata()` default-handoff option, and the DAL folds those `isDefault` field changes into the same `platformSummary/projects_{sId}` summary write as the target project metadata. This removes the older caller-side follow-up metadata writes, adds no collections, documents, routes, rules, indexes, schema fields, Cloud Functions, owner settings, Firebase deployment, or Vercel deployment, and preserves public cache invalidation through the affected project/store cache path.
- Project lifecycle atomicity hardening adds no collection or durable document. Create, restore, and duplicate now couple full-project and summary mutations in one transaction; metadata mutations serialize on the existing summary document. The only added read cost is the transaction/current-summary evidence needed to prevent overwrites, lost updates, duplicate slugs, and split lifecycle state.
- Legacy project scope hardening adds no Firebase operation. The fallback read already existed; the DAL now rejects its result unless the document belongs to the exact requested tenant/store/project tuple. Current scoped reads apply the same runtime identity check.
- Active/delete lifecycle hardening adds no collection or durable record. Active toggle adds one project transaction read to reject missing/deleted/stale identities instead of creating phantom state. Delete already read summary and, under multi-outlet checks, project state; its final transaction now rechecks current truth and atomically promotes the fallback. Revalidating the fallback may add browser-local/cache endpoint work but no Firestore document read.
- Time-slot preset persistence validates active-store scope, bounded preset count, unique IDs/labels, strict `HH:mm` values, distinct start/end times, and safe color values before the store write. Overnight ranges such as `22:00` to `02:00` are valid and use the starting weekday for the after-midnight segment. Cascades page through project discovery in groups of 100, use concurrency 4, and transactionally re-read only candidate projects before a files-only write. Compared with the previous batch projection, this adds one transaction read per candidate but prevents a stale full-project snapshot from replacing concurrent menu edits. No collection, index, Cloud Function, or public store read was added.
- Project save and publish treat persisted linkage as authoritative. A caller cannot omit or replace `masterProjectId` to bypass the linked-outlet route. Standalone writes re-read current project truth in a transaction; publish also compares the caller's loaded modification version, and linked publish enforces the same version inside the Admin transaction. This uses existing reads and adds no document, collection, index, rule, or write while preventing a stale design screen from replacing newer menu truth.
- Project publish uploads use a unique attempt suffix in every Storage object path. A failed pre-commit standalone attempt can delete only its own new object and cannot overwrite or delete bytes referenced by the previous project state. Linked requests retain newly uploaded objects when the network outcome is ambiguous because the server transaction may already have committed the URL.
- Project upload payload admission now matches the owner UI contract exactly: JPEG/PNG/WebP images up to 10 MB and PDF up to 50 MB. Claimed type and data-URL MIME must agree, base64 must be valid, and the decoded signature must match before Storage. SVG, GIF, Office, text and font payloads are rejected by this project-specific boundary; shared font/document helpers are unchanged.

## Remaining Cost Risk

- Time-slot preset cascade reads every project in the current store and transactionally re-reads candidates that reference the preset. Pagination bounds memory, not total billed reads. This remains acceptable only for low-frequency settings edits and should not be used from high-frequency UI events.
- Optional MOL, MCE, menu snapshots, and multi-outlet awareness add writes when their flags are enabled. Those are documented in their feature areas and should remain flag-gated.
- Immutable retry-safe upload paths can leave an unreferenced object when a linked request has an ambiguous network result. Do not delete it immediately: the server may have committed the reference. Any future cleanup must be retention-based and reference-aware across duplicated projects rather than deleting the previous URL during replacement.
